mw.loader.implement("ext.centralNotice.bannerHistoryLogger@0y11a6i", function($, jQuery, require, module) {
    (function() {
        var cn = mw.centralNotice,
            bhLogger, mixin = new cn.Mixin('bannerHistoryLogger'),
            doNotTrackEnabled = /1|yes/.test(navigator.doNotTrack) || window.doNotTrack === '1',
            waitLogNoSendBeacon, now = Math.round((new Date()).getTime() / 1000),
            log, readyToLogDeferredObj = $.Deferred(),
            logSent = !1,
            inSample, BANNER_HISTORY_KV_STORE_KEY = 'banner_history',
            BANNER_HISTORY_KV_STORE_TTL = 365,
            BANNER_HISTORY_LOG_ENTRY_VERSION = 1,
            EVENT_LOGGING_SCHEMA = 'CentralNoticeBannerHistory',
            TIMESTAMP_RANDOM_SHIFT_MAX = 60;

        function loadLog() {
            log = cn.kvStore.getItem(BANNER_HISTORY_KV_STORE_KEY, cn.kvStore.contexts.GLOBAL);
            if (!log) {
                log = [];
            }
        }

        function makeLogEntry() {
            var data = cn.data,
                randomTimeShift = Math.round(Math.random() * TIMESTAMP_RANDOM_SHIFT_MAX) - (TIMESTAMP_RANDOM_SHIFT_MAX / 2),
                time = now + randomTimeShift,
                logEntry = {
                    version: BANNER_HISTORY_LOG_ENTRY_VERSION,
                    language: data.uselang,
                    country: data.country,
                    isAnon: data.anonymous,
                    campaign: data.
                    campaign,
                    campaignCategory: data.campaignCategory,
                    bucket: data.bucket,
                    time: time,
                    status: data.status,
                    statusCode: data.statusCode,
                    bannersNotGuaranteedToDisplay: !!data.bannersNotGuaranteedToDisplay
                };
            if (data.banner) {
                logEntry.banner = data.banner;
            }
            if (data.bannerCanceledReason) {
                logEntry.bannerCanceledReason = data.bannerCanceledReason;
            }
            if (data.bannerLoadedButHiddenReason) {
                logEntry.bannerLoadedButHiddenReason = data.bannerLoadedButHiddenReason;
            }
            return logEntry;
        }

        function purgeOldLogEntries(maxEntryAge, maxEntries) {
            var i = 0,
                cutoff = now - maxEntryAge * 86400;
            if (log.length > maxEntries) {
                log = log.slice(0 - maxEntries);
            }
            while (i < log.length && log[i].time < cutoff) {
                i++;
            }
            log = log.slice(i);
        }

        function storeLog() {
            cn.kvStore.setItem(BANNER_HISTORY_KV_STORE_KEY, log, cn.kvStore.contexts.GLOBAL, BANNER_HISTORY_KV_STORE_TTL);
        }

        function makeEventLoggingData(rate) {
            var elData = {},
                kvError = cn.kvStore.getError(),
                i, logEntry, elLogEntry;
            elData.i = bhLogger.id;
            if (rate) {
                elData.r = rate;
            }
            if (kvError) {
                elData.e = kvError.message;
                return elData;
            }
            elData.n = log.
            length;
            elData.l = [];
            i = log.length - 1;
            while (i >= 0) {
                logEntry = log[i];
                elLogEntry = [logEntry.banner || '', logEntry.campaign, logEntry.time, logEntry.statusCode];
                elData.l.unshift(elLogEntry.join('|'));
                if (!checkEventLoggingURLSize(elData)) {
                    elData.l.shift();
                    break;
                }
                i--;
            }
            return elData;
        }

        function sendLog(elData) {
            var deferred = $.Deferred(),
                elPromise;
            if (navigator.sendBeacon) {
                elPromise = mw.eventLog.logEvent(EVENT_LOGGING_SCHEMA, elData);
            } else {
                setTimeout(function() {
                    deferred.reject();
                }, waitLogNoSendBeacon);
                elPromise = $.ajax({
                    url: makeEventLoggingURL(elData),
                    timeout: waitLogNoSendBeacon,
                    dataType: 'script',
                    cache: !0
                });
            }
            elPromise.then(function() {
                deferred.resolve();
            }, function() {
                deferred.reject();
            });
            return deferred.promise();
        }

        function checkEventLoggingURLSize(elData) {
            return (makeEventLoggingURL(elData).length <= mw.eventLog.maxUrlSize);
        }

        function makeEventLoggingURL(elData) {
            return mw.eventLog.makeBeaconUrl({
                event: elData,
                revision: 19079897,
                schema: EVENT_LOGGING_SCHEMA,
                webHost: location.hostname,
                wiki: mw.config.get('wgDBname')
            });
        }
        mixin.setPostBannerHandler(function(mixinParams) {
            waitLogNoSendBeacon = mixinParams.waitLogNoSendBeacon;
            mw.requestIdleCallback(function() {
                if (!cn.kvStore.isAvailable()) {
                    cn.kvStore.setNotAvailableError();
                } else {
                    loadLog();
                    if (!doNotTrackEnabled) {
                        log.push(makeLogEntry());
                    }
                    purgeOldLogEntries(mixinParams.maxEntryAge, mixinParams.maxEntries);
                    storeLog();
                }
                if (doNotTrackEnabled) {
                    return;
                }
                mw.loader.using(['ext.eventLogging', 'mediawiki.util', 'mediawiki.user']).done(function() {
                    var rateParam = mw.util.getParamValue('bannerHistoryLogRate'),
                        rate = rateParam !== null ? parseFloat(rateParam) : mixinParams.rate;
                    bhLogger.id = mw.user.generateRandomSessionId();
                    if (Math.random() < rate) {
                        sendLog(makeEventLoggingData(rate)).always(function() {
                            inSample = !0;
                            logSent = !0;
                            readyToLogDeferredObj.resolve();
                        });
                    } else {
                        readyToLogDeferredObj.resolve();
                    }
                });
            });
        });
        cn.registerCampaignMixin(mixin);
        cn.bannerHistoryLogger = bhLogger = {
            id: null,
            ensureLogSent: function() {
                var deferred = $.Deferred();
                if (doNotTrackEnabled) {
                    deferred.resolve();
                    return deferred.
                    promise();
                }
                readyToLogDeferredObj.done(function() {
                    if (logSent) {
                        deferred.resolve();
                    } else {
                        sendLog(makeEventLoggingData()).then(function() {
                            deferred.resolve();
                        }, function() {
                            deferred.reject();
                        });
                    }
                });
                return deferred.promise();
            }
        };
    }());
});
mw.loader.implement("ext.centralNotice.choiceData@0xb2fg1", function($, jQuery, require, module) {
    mw.centralNotice = (mw.centralNotice || {});
    mw.centralNotice.choiceData = [{
        "name": "C1920_enJP_dsk_FR",
        "start": 1567548000,
        "end": 1569967200,
        "preferred": 2,
        "throttle": 100,
        "bucket_count": 4,
        "geotargeted": !0,
        "banners": [{
            "name": "B1920_0903_enJP_dsk_p1_lg_twin1",
            "bucket": 0,
            "weight": 25,
            "category": "fundraising",
            "display_anon": !0,
            "display_account": !1,
            "devices": ["desktop"]
        }, {
            "name": "B1920_0903_enJP_dsk_p1_lg_twin2",
            "bucket": 1,
            "weight": 25,
            "category": "fundraising",
            "display_anon": !0,
            "display_account": !1,
            "devices": ["desktop"]
        }, {
            "name": "B1920_0903_enJP_dsk_p2_sm_twin1",
            "bucket": 2,
            "weight": 25,
            "category": "fundraising",
            "display_anon": !0,
            "display_account": !1,
            "devices": ["desktop"]
        }, {
            "name": "B1920_0903_enJP_dsk_p2_sm_twin2",
            "bucket": 3,
            "weight": 25,
            "category": "fundraising",
            "display_anon": !0,
            "display_account": !1,
            "devices": ["desktop"]
        }],
        "countries": ["JP"],
        "mixins": {
            "bannerHistoryLogger": {
                "maxEntries": 30,
                "maxEntryAge": 30,
                "rate": 0.01,
                "waitLogNoSendBeacon": 200
            },
            "impressionDiet": {
                "cookieName": "bannercount_fundraiser_2019",
                "maximumSeen": 10,
                "restartCycleDelay": 2592000,
                "skipInitial": 0
            },
            "largeBannerLimit": {
                "days": 250,
                "identifier": "centralnotice-frbanner-seen-fullscreen",
                "randomize": !0
            },
            "legacySupport": {
                "bannersNotGuaranteedToDisplay": !0,
                "setSRISampleRate": !0,
                "sriSampleRate": 1
            }
        }
    }, {
        "name": "C1920_enJP_m_FR",
        "start": 1567548000,
        "end": 1569967200,
        "preferred": 2,
        "throttle": 100,
        "bucket_count": 4,
        "geotargeted": !0,
        "banners": [{
            "name": "B1920_0903_enJP_m_p1_lg_twin1",
            "bucket": 0,
            "weight": 25,
            "category": "fundraising",
            "display_anon": !0,
            "display_account": !1,
            "devices": ["android", "ipad", "iphone"]
        }, {
            "name": "B1920_0903_enJP_m_p1_lg_twin2",
            "bucket": 1,
            "weight": 25,
            "category": "fundraising",
            "display_anon": !0,
            "display_account": !1,
            "devices": ["android", "ipad", "iphone"]
        }, {
            "name": "B1920_0903_enJP_m_p2_sm_twin1",
            "bucket": 2,
            "weight": 25,
            "category": "fundraising",
            "display_anon": !0,
            "display_account": !1,
            "devices": ["android", "ipad", "iphone"]
        }, {
            "name": "B1920_0903_enJP_m_p2_sm_twin2",
            "bucket": 3,
            "weight": 25,
            "category": "fundraising",
            "display_anon": !0,
            "display_account": !1,
            "devices": ["android", "ipad", "iphone"]
        }],
        "countries": ["JP"],
        "mixins": {
            "bannerHistoryLogger": {
                "maxEntries": 30,
                "maxEntryAge": 30,
                "rate": 0.01,
                "waitLogNoSendBeacon": 200
            },
            "impressionDiet": {
                "cookieName": "bannercount_fundraiser_2019",
                "maximumSeen": 10,
                "restartCycleDelay": 2592000,
                "skipInitial": 0
            },
            "largeBannerLimit": {
                "days": 250,
                "identifier": "centralnotice-frbanner-seen-fullscreen",
                "randomize": !0
            },
            "legacySupport": {
                "bannersNotGuaranteedToDisplay": !0,
                "setSRISampleRate": !0,
                "sriSampleRate": 1
            }
        }
    }, {
        "name": "wlm 2019",
        "start": 1567260000,
        "end": 1570480140,
        "preferred": 1,
        "throttle": 100,
        "bucket_count": 1,
        "geotargeted": !0,
        "banners": [{
            "name": "wlm2019_version2",
            "bucket": 0,
            "weight": 25,
            "category": "wlm2019",
            "display_anon": !0,
            "display_account": !0,
            "devices": ["android", "desktop", "ipad", "iphone", "unknown"]
        }],
        "countries": ["AC", "AI", "AL", "AM",
            "AS", "AU", "AX", "AZ", "BD", "BJ", "BL", "BM", "BR", "CA", "CC", "CH", "CP", "CX", "DG", "DK", "EA", "ES", "FI", "FK", "FO", "FR", "GB", "GF", "GG", "GH", "GI", "GN", "GP", "GR", "GS", "GU", "HM", "HR", "IC", "IE", "IM", "IN", "IO", "IR", "IT", "JE", "KR", "KY", "MF", "MP", "MQ", "MS", "MT", "MY", "NC", "NF", "NG", "NP", "PE", "PF", "PH", "PL", "PM", "PN", "PR", "PT", "RE", "RO", "SE", "SH", "SI", "SK", "TA", "TC", "TF", "TH", "TW", "UA", "UG", "UM", "US", "VG", "VI", "WF", "XK", "YT"
        ],
        "mixins": []
    }, {
        "name": "wlm 2019-de",
        "start": 1567288860,
        "end": 1569880200,
        "preferred": 1,
        "throttle": 100,
        "bucket_count": 1,
        "geotargeted": !0,
        "banners": [{
            "name": "wlm_2019_de",
            "bucket": 0,
            "weight": 25,
            "category": "wlm2019de",
            "display_anon": !0,
            "display_account": !0,
            "devices": ["android", "desktop", "ipad", "iphone", "unknown"]
        }],
        "countries": ["DE"],
        "mixins": []
    }, {
        "name": "wlm 2019-dz",
        "start": 1567292400,
        "end": 1569883800,
        "preferred": 1,
        "throttle": 100,
        "bucket_count": 1,
        "geotargeted": !0,
        "banners": [{
            "name": "wlm_2019_dz",
            "bucket": 0,
            "weight": 100,
            "category": "wlm2019dz",
            "display_anon": !0,
            "display_account": !0,
            "devices": ["android", "desktop", "ipad", "iphone", "unknown"]
        }],
        "countries": ["DZ"],
        "mixins": []
    }, {
        "name": "wlm 2019-ma",
        "start": 1567292400,
        "end": 1569883800,
        "preferred": 1,
        "throttle": 100,
        "bucket_count": 1,
        "geotargeted": !0,
        "banners": [{
            "name": "wlm_2019_ma",
            "bucket": 0,
            "weight": 100,
            "category": "wlm2019ma",
            "display_anon": !0,
            "display_account": !0,
            "devices": ["android", "desktop", "ipad", "iphone", "unknown"]
        }],
        "countries": ["MA"],
        "mixins": []
    }, {
        "name": "wlm 2019-no",
        "start": 1567288800,
        "end": 1569880200,
        "preferred": 1,
        "throttle": 100,
        "bucket_count": 1,
        "geotargeted": !0,
        "banners": [{
            "name": "wlm_2019_no",
            "bucket": 0,
            "weight": 100,
            "category": "wlm2019no",
            "display_anon": !0,
            "display_account": !0,
            "devices": ["android", "desktop", "ipad", "iphone", "unknown"]
        }],
        "countries": ["BV", "NO", "SJ"],
        "mixins": []
    }, {
        "name": "wlm 2019-ru",
        "start": 1567296000,
        "end": 1569887400,
        "preferred": 1,
        "throttle": 100,
        "bucket_count": 1,
        "geotargeted": !0,
        "banners": [{
            "name": "wlm_2019_ru",
            "bucket": 0,
            "weight": 100,
            "category": "wlm2019ru",
            "display_anon": !0,
            "display_account": !0,
            "devices": ["android", "desktop", "ipad", "iphone", "unknown"]
        }],
        "countries": ["RU"],
        "mixins": []
    }];
});
mw.loader.implement("ext.centralNotice.display@0mtrxhx", {
    "main": "index.js",
    "files": {
        "index.js": function(require, module) {
            (function() {
                var cn, Mixin, campaignMixins = {},
                    bannerLoadedDeferredObj, requestedBannerName = null,
                    MAX_RECORD_IMPRESSION_DELAY = 250,
                    IMPRESSION_EVENT_LOGGING_SCHEMA = 'CentralNoticeImpression',
                    IMPRESSION_EVENT_LOGGING_SCHEMA_REVISION = 19108542;
                Mixin = function(name) {
                    this.name = name;
                };
                Mixin.prototype.setPreBannerHandler = function(handlerFunc) {
                    this.preBannerHandler = handlerFunc;
                };
                Mixin.prototype.setPostBannerHandler = function(handlerFunc) {
                    this.postBannerHandler = handlerFunc;
                };

                function runMixinHooks(hookPropertyName) {
                    var state = cn.internal.state;
                    $.each(state.getCampaign().mixins, function(mixinName, mixinParams) {
                        var handler;
                        if (!(mixinName in campaignMixins)) {
                            mw.log.warn('Mixin ' + mixinName + ' not registered.');
                            return;
                        }
                        if (!(hookPropertyName in campaignMixins[mixinName])) {
                            return;
                        }
                        handler = campaignMixins[mixinName][hookPropertyName];
                        if (typeof handler !== 'function') {
                            mw.log.warn(hookPropertyName +
                                ' for ' + mixinName + ' not a function.');
                            return;
                        }
                        handler(mixinParams);
                    });
                }

                function runPreBannerMixinHooks() {
                    runMixinHooks('preBannerHandler');
                }

                function runPostBannerMixinHooks() {
                    runMixinHooks('postBannerHandler');
                }

                function setUpDataProperty() {
                    try {
                        Object.defineProperty(cn, 'data', {
                            get: function() {
                                return cn.internal.state.getData();
                            }
                        });
                        return;
                    } catch (e) {}
                    cn.data = cn.internal.state.getData();
                }

                function setUpBannerLoadedPromise() {
                    bannerLoadedDeferredObj = $.Deferred();
                    cn.bannerLoadedPromise = bannerLoadedDeferredObj.promise();
                    cn.events = {};
                    cn.events.bannerLoaded = cn.bannerLoadedPromise;
                }

                function fetchBanner() {
                    var data = cn.internal.state.getData(),
                        urlBase = new mw.Uri(mw.config.get('wgCentralNoticeActiveBannerDispatcher')),
                        urlQuery = ['banner=' + mw.Uri.encode(data.banner), 'uselang=' + mw.Uri.encode(data.uselang), 'debug=' + (!!data.debug).toString()];
                    if (data.campaign) {
                        urlQuery.unshift('campaign=' + mw.Uri.encode(data.campaign));
                    }
                    if (urlBase.query.title) {
                        urlQuery.unshift('title=' + mw.util.wikiUrlencode(urlBase.query.title));
                    }
                    urlBase.query = {};
                    urlBase.fragment = '';
                    $.ajax({
                        url: urlBase.toString() + '?' + urlQuery.join('&'),
                        dataType: 'script',
                        cache: !0
                    }).fail(function(jqXHR, status, error) {
                        cn.handleBannerLoaderError(status + ': ' + error);
                    });
                }

                function injectBannerHTML(bannerHtml) {
                    $('div#centralNotice').attr('class', mw.html.escape('cn-' + cn.internal.state.getData().bannerCategory)).prepend(bannerHtml);
                    if (window.performance && performance.mark) {
                        performance.mark('mwCentralNoticeBanner');
                    }
                }

                function resolveRecordImpressionDeferred() {
                    cn.recordImpressionDeferredObj.done(reallyRecordImpression);
                    cn.recordImpressionDeferredObj.resolve();
                }

                function recordImpression() {
                    var timeout, timeoutHasRun = !1;
                    if (cn.recordImpressionDelayPromises.length === 0) {
                        reallyRecordImpression();
                        return;
                    }
                    timeout = setTimeout(function() {
                        timeoutHasRun = !0;
                        resolveRecordImpressionDeferred();
                    }, MAX_RECORD_IMPRESSION_DELAY);
                    $.when.apply($, cn.recordImpressionDelayPromises).always(function() {
                        if (!timeoutHasRun) {
                            clearTimeout(timeout);
                            resolveRecordImpressionDeferred();
                        }
                    });
                }

                function reallyRecordImpression() {
                    var state = cn.internal.state,
                        random = Math.random(),
                        url, dataCopy, elBaseUrl, elData, elQueryString;
                    if (random <= state.getData().recordImpressionSampleRate) {
                        url = new mw.Uri(mw.config.get('wgCentralBannerRecorder'));
                        dataCopy = state.getDataCopy(true);
                        url.extend(dataCopy);
                        sendBeacon(url.toString());
                    }
                    if (random <= state.getData().impressionEventSampleRate) {
                        elBaseUrl = mw.config.get('wgEventLoggingBaseUri');
                        if (elBaseUrl) {
                            dataCopy = dataCopy || state.getDataCopy(true);
                            elData = {
                                event: dataCopy,
                                revision: IMPRESSION_EVENT_LOGGING_SCHEMA_REVISION,
                                schema: IMPRESSION_EVENT_LOGGING_SCHEMA,
                                webHost: location.hostname,
                                wiki: mw.config.get('wgDBname')
                            };
                            elQueryString = encodeURIComponent(JSON.stringify(elData));
                            sendBeacon(elBaseUrl + '?' + elQueryString + ';');
                        }
                    }
                }

                function sendBeacon(urlStr) {
                    if (navigator.sendBeacon) {
                        try {
                            navigator.sendBeacon(urlStr);
                        } catch (e) {}
                    } else {
                        setTimeout(function() {
                            document.createElement('img').src = urlStr;
                        }, 0);
                    }
                }

                function reallyChooseAndMaybeDisplay() {
                    var chooser = cn.internal.
                    chooser, bucketer = cn.internal.bucketer, state = cn.internal.state, hide = cn.internal.hide, campaign, banner;
                    state.setUp();
                    setUpDataProperty();
                    if (!chooser.choiceDataSeemsFresh(cn.choiceData)) {
                        state.setChoiceDataStale();
                        return;
                    }
                    campaign = chooser.chooseCampaign(cn.choiceData, state.getData().country, state.getData().anonymous, state.getData().device, state.getData().randomcampaign);
                    if (campaign === null) {
                        return;
                    }
                    state.setCampaign(campaign);
                    bucketer.setCampaign(campaign);
                    hide.setCategory(state.getData().campaignCategory);
                    if (cn.kvStore) {
                        cn.kvStore.setCampaignName(state.getData().campaign);
                        cn.kvStore.setCategory(state.getData().campaignCategory);
                    }
                    bucketer.process();
                    state.setBucket(bucketer.getBucket());
                    state.setReducedBucket(bucketer.getReducedBucket());
                    hide.processCookie();
                    if (hide.shouldHide()) {
                        state.cancelBanner(hide.getReason());
                        runPreBannerMixinHooks();
                        runPostBannerMixinHooks();
                        recordImpression();
                        return;
                    }
                    runPreBannerMixinHooks();
                    if (state.isBannerCanceled()) {
                        runPostBannerMixinHooks();
                        recordImpression();
                        return;
                    }
                    if (requestedBannerName) {
                        banner = chooser.requestBanner(campaign, state.getData().reducedBucket, state.getData().anonymous, state.getData().device, requestedBannerName);
                        if (!banner) {
                            state.setRequestedBannerNotAvailable(requestedBannerName);
                        }
                    } else {
                        banner = chooser.chooseBanner(campaign, state.getData().reducedBucket, state.getData().anonymous, state.getData().device, state.getData().randombanner);
                        if (!banner) {
                            state.setNoBannerAvailable();
                        }
                    }
                    if (!banner) {
                        runPostBannerMixinHooks();
                        recordImpression();
                        return;
                    }
                    state.setBanner(banner);
                    if (cn.kvStore) {
                        cn.kvStore.setBannerName(banner.name);
                    }
                    cn.bannerData.bannerName = banner.name;
                    setUpBannerLoadedPromise();
                    fetchBanner();
                }

                function processAfterBannerFetch() {
                    if (!cn.internal.state.getData().testingBanner) {
                        runPostBannerMixinHooks();
                        recordImpression();
                    }
                }
                cn = {
                    reallyInsertBanner: function(bannerJson) {
                        var state = cn.internal.state,
                            shownAfterLoadingBanner = !0,
                            bannerLoadedButHiddenReason, tmpData;
                        injectBannerHTML(bannerJson.bannerHtml);
                        bannerLoadedDeferredObj.resolve(cn.internal.state.getData());
                        if (state.getData().bannersNotGuaranteedToDisplay) {
                            if (typeof cn.bannerData.alterImpressionData === 'function') {
                                tmpData = state.getDataCopy();
                                shownAfterLoadingBanner = cn.bannerData.alterImpressionData(tmpData);
                                if (!shownAfterLoadingBanner) {
                                    bannerLoadedButHiddenReason = tmpData.reason || '';
                                    state.setBannerLoadedButHidden(bannerLoadedButHiddenReason);
                                }
                                if (tmpData.banner_count) {
                                    state.setBannerCount(tmpData.banner_count);
                                }
                            } else {
                                state.setAlterFunctionMissing();
                            }
                        }
                        if (shownAfterLoadingBanner) {
                            state.setBannerShown();
                        }
                        processAfterBannerFetch();
                    },
                    recordImpressionDelayPromises: [],
                    recordImpressionDeferredObj: null,
                    internal: {},
                    setBannersNotGuaranteedToDisplay: function() {
                        cn.internal.state.setBannersNotGuaranteedToDisplay();
                    },
                    cancelBanner: function(reason) {
                        cn.internal.state.cancelBanner(reason);
                    },
                    isBannerCanceled: function() {
                        return cn.internal.state.isBannerCanceled();
                    },
                    isBannerShown: function() {
                        return cn.internal.state.isBannerShown();
                    },
                    setBannerLoadedButHidden: function(reason) {
                        cn.internal.
                        state.setBannerLoadedButHidden(reason);
                    },
                    setRecordImpressionSampleRate: function(rate) {
                        cn.internal.state.setRecordImpressionSampleRate(rate);
                    },
                    setImpressionEventSampleRate: function(rate) {
                        cn.internal.state.setImpressionEventSampleRate(rate);
                    },
                    bannerData: {},
                    Mixin: Mixin,
                    registerCampaignMixin: function(mixin) {
                        campaignMixins[mixin.name] = mixin;
                    },
                    chooseAndMaybeDisplay: function() {
                        mw.geoIP.getPromise().fail(cn.internal.state.setInvalidGeoData).done(cn.internal.state.setGeoData).always(reallyChooseAndMaybeDisplay);
                    },
                    displayTestingBanner: function() {
                        mw.geoIP.getPromise().fail(cn.internal.state.setInvalidGeoData).done(cn.internal.state.setGeoData).always(function() {
                            cn.internal.state.setUpForTestingBanner();
                            setUpDataProperty();
                            setUpBannerLoadedPromise();
                            fetchBanner();
                        });
                    },
                    insertBanner: function(bannerJson) {
                        $(function() {
                            cn.reallyInsertBanner(bannerJson);
                        });
                    },
                    handleBannerLoaderError: function(msg) {
                        cn.internal.state.setBannerLoaderError(msg);
                        bannerLoadedDeferredObj.reject(cn.internal.state.getData());
                        processAfterBannerFetch();
                    },
                    hideBannerWithCloseButton: function() {
                        $('#centralNotice').hide();
                        cn.internal.hide.setHideWithCloseButtonCookies();
                    },
                    customHideBanner: function(reason, duration) {
                        $('#centralNotice').hide();
                        cn.internal.hide.setHideCookies(reason, duration);
                    },
                    hideBanner: function() {
                        cn.hideBannerWithCloseButton();
                    },
                    setBucket: function(bucket) {
                        cn.internal.bucketer.setBucket(bucket);
                        cn.internal.state.setBucket(bucket);
                        cn.internal.state.setReducedBucket(cn.internal.bucketer.getReducedBucket());
                    },
                    requestBanner: function(banner) {
                        requestedBannerName = banner;
                    },
                    registerTest: function(identifier) {
                        cn.internal.state.registerTest(identifier);
                    },
                    setDebugInfo: function(str) {
                        cn.internal.state.setDebugInfo(str);
                    },
                    requestRecordImpressionDelay: function(promise) {
                        cn.recordImpressionDelayPromises.push(promise);
                        cn.recordImpressionDeferredObj = cn.recordImpressionDeferredObj || $.Deferred();
                        return cn.recordImpressionDeferredObj.promise();
                    },
                    getDataProperty: function(prop) {
                        return cn.internal.state.getData()[prop];
                    }
                };
                if (mw.centralNotice === undefined) {
                    mw.centralNotice = cn;
                } else {
                    $.extend(mw.centralNotice, cn);
                    cn = mw.centralNotice;
                }
                mw.log.deprecate(window, 'insertBanner', cn.insertBanner, 'Use mw.centralNotice method instead');
                mw.log.deprecate(window, 'hideBanner', cn.hideBanner, 'Use mw.centralNotice method instead');
                mw.log.deprecate(window, 'toggleNotice', cn.hideBanner, 'Use mw.centralNotice method instead');
                require('./state.js');
                require('./chooser.js');
                require('./bucketer.js');
                require('./hide.js');
            }());
        },
        "state.js": function(require, module) {
            (function() {
                var state, status, config = require('./config.json'),
                    impressionEventSampleRateOverridden = !1,
                    UNKNOWN_GEO_CODE = 'XX',
                    CAMPAIGN_CATEGORY_FOR_MIXED_BANNER_CATEGORIES = 'mixed_banner_categories',
                    DEVICES = {
                        DESKTOP: 'desktop',
                        IPHONE: 'iphone',
                        IPAD: 'ipad',
                        ANDROID: 'android',
                        UNKNOWN: 'unknown'
                    },
                    STATUSES = {
                        CAMPAIGN_NOT_CHOSEN: new Status('campaign_not_chosen', 0),
                        CAMPAIGN_CHOSEN: new Status('campaign_chosen', 1),
                        BANNER_CANCELED: new Status('banner_canceled', 2),
                        NO_BANNER_AVAILABLE: new Status('no_banner_available', 3),
                        BANNER_CHOSEN: new Status('banner_chosen', 4),
                        BANNER_LOADED_BUT_HIDDEN: new Status('banner_loaded_but_hidden', 5),
                        BANNER_SHOWN: new Status('banner_shown', 6),
                        BANNER_LOADER_ERROR: new Status('banner_loader_error', 7),
                        CHOICE_DATA_STALE: new Status('choice_data_stale', 8)
                    },
                    REASONS = {
                        other: 0,
                        close: 1,
                        waitdate: 2,
                        waitimps: 3,
                        waiterr: 4,
                        belowMinEdits: 5,
                        viewLimit: 6,
                        'seen-fullscreen': 7,
                        'cookies-disabled': 8,
                        donate: 9,
                        cookies: 10,
                        seen: 11,
                        empty: 12,
                        waitnorestart: 13,
                        waitnostorage: 14,
                        namespace: 15,
                        noStorage: 16,
                        requestedBannerNotAvailable: 17,
                        jsonParamError: 18,
                        bannerSequenceEmptyStep: 19,
                        bannerSequenceAllStepsSkipped: 20
                    };

                function Status(key, code) {
                    this.key = key;
                    this.code = code;
                }

                function getDeviceCode() {
                    var ua;
                    if (mw.config.get('skin') !== 'minerva') {
                        return DEVICES.DESKTOP;
                    }
                    ua = navigator.userAgent;
                    if (ua.match(/iphone/i)) {
                        return DEVICES.IPHONE;
                    }
                    if (ua.match(/ipad/i)) {
                        return DEVICES.IPAD;
                    }
                    if (ua.match(/android/i)) {
                        return DEVICES.ANDROID;
                    }
                    return DEVICES.UNKNOWN;
                }

                function setInitialData() {
                    var urlParams = $.
                    extend(state.urlParams, (new mw.Uri()).query), impressionEventSampleRateFromUrl;
                    state.data.anonymous = (mw.config.get('wgUserName') === null);
                    state.data.project = mw.config.get('wgNoticeProject');
                    state.data.db = mw.config.get('wgDBname');
                    state.data.uselang = mw.config.get('wgUserLanguage');
                    state.data.device = urlParams.device || getDeviceCode();
                    state.data.country = urlParams.country || state.data.country || UNKNOWN_GEO_CODE;
                    state.data.region = urlParams.region || (state.data.region !== undefined ? state.data.region : false) || UNKNOWN_GEO_CODE;
                    state.data.debug = (urlParams.debug !== undefined);
                    state.data.randomcampaign = numericalUrlParamOrVal(urlParams.randomcampaign, Math.random());
                    state.data.randombanner = numericalUrlParamOrVal(urlParams.randombanner, Math.random());
                    state.data.recordImpressionSampleRate = numericalUrlParamOrVal(urlParams.recordImpressionSampleRate, mw.config.get('wgCentralNoticeSampleRate'));
                    impressionEventSampleRateFromUrl = numericalUrlParamOrVal(urlParams.impressionEventSampleRate, null);
                    if (
                        impressionEventSampleRateFromUrl !== null) {
                        state.data.impressionEventSampleRate = impressionEventSampleRateFromUrl;
                        impressionEventSampleRateOverridden = !0;
                    } else {
                        state.data.impressionEventSampleRate = mw.config.get('wgCentralNoticeImpressionEventSampleRate');
                    }
                    state.data.getVars = urlParams;
                }

                function numericalUrlParamOrVal(urlParam, val) {
                    var urlParamAsFloat = parseFloat(urlParam);
                    return !isNaN(urlParamAsFloat) ? urlParamAsFloat : val;
                }

                function setTestingBannerData() {
                    state.data.campaign = state.urlParams.campaign;
                    state.data.banner = state.urlParams.banner;
                    state.data.testingBanner = !0;
                }

                function setStatus(s, reason) {
                    var reasonCodeStr = reason ? ('.' + state.lookupReasonCode(reason)) : '';
                    status = s;
                    state.data.status = s.key;
                    state.data.statusCode = s.code.toString() + reasonCodeStr;
                }
                state = mw.centralNotice.internal.state = {
                    STATUSES: STATUSES,
                    urlParams: {},
                    data: {},
                    campaign: null,
                    banner: null,
                    setGeoData: function(geo) {
                        if (geo) {
                            state.data.country = geo.country;
                            state.data.region = geo.region;
                        }
                    },
                    setInvalidGeoData: function() {
                        state.data.country = UNKNOWN_GEO_CODE;
                        state.data.region = UNKNOWN_GEO_CODE;
                    },
                    setUp: function() {
                        setInitialData();
                        setStatus(STATUSES.CAMPAIGN_NOT_CHOSEN);
                    },
                    setUpForTestingBanner: function() {
                        setInitialData();
                        setTestingBannerData();
                        setStatus(STATUSES.BANNER_CHOSEN);
                    },
                    getData: function() {
                        return state.data;
                    },
                    getDataCopy: function(cleanForLogging) {
                        var dataCopy = $.extend(true, {}, state.data);
                        if (cleanForLogging) {
                            delete dataCopy.getVars;
                            delete dataCopy.mixins;
                            delete dataCopy.tests;
                            delete dataCopy.reducedBucket;
                        }
                        return dataCopy;
                    },
                    setCampaign: function(c) {
                        var prop, i, category, campaignCategory = null;
                        state.campaign = c;
                        state.data.campaign = state.campaign.name;
                        setStatus(STATUSES.CAMPAIGN_CHOSEN);
                        state.data.mixins = {};
                        for (prop in state.campaign.mixins) {
                            if (Object.prototype.hasOwnProperty.call(state.campaign.mixins, prop)) {
                                state.data.mixins[prop] = !0;
                            }
                        }
                        for (i = 0; i < state.campaign.banners.length; i++) {
                            category = state.campaign.banners[i].category;
                            if (campaignCategory === null) {
                                campaignCategory = category;
                            } else if (campaignCategory !== category) {
                                campaignCategory = CAMPAIGN_CATEGORY_FOR_MIXED_BANNER_CATEGORIES;
                                break;
                            }
                        }
                        state.data.campaignCategory = campaignCategory;
                        state.data.campaignCategoryUsesLegacy = config.categoriesUsingLegacy.indexOf(campaignCategory) !== -1;
                    },
                    getCampaign: function() {
                        return state.campaign;
                    },
                    setBanner: function(b) {
                        state.banner = b;
                        state.data.banner = state.banner.name;
                        state.data.bannerCategory = state.banner.category;
                        setStatus(STATUSES.BANNER_CHOSEN);
                    },
                    setBucket: function(bucket) {
                        state.data.bucket = bucket;
                    },
                    setReducedBucket: function(reducedBucket) {
                        state.data.reducedBucket = reducedBucket;
                    },
                    setBannersNotGuaranteedToDisplay: function() {
                        state.data.bannersNotGuaranteedToDisplay = !0;
                    },
                    cancelBanner: function(reason) {
                        state.data.bannerCanceledReason = reason;
                        setStatus(STATUSES.BANNER_CANCELED, reason);
                        state.data.result = 'hide';
                        state.data.reason = reason;
                    },
                    isBannerCanceled: function() {
                        return status === STATUSES.BANNER_CANCELED;
                    },
                    isBannerShown: function() {
                        return status === STATUSES.BANNER_SHOWN;
                    },
                    setNoBannerAvailable: function() {
                        setStatus(STATUSES.NO_BANNER_AVAILABLE);
                        state.data.result = 'hide';
                        state.data.reason = 'empty';
                    },
                    setRequestedBannerNotAvailable: function(bannerName) {
                        state.data.requestedBanner = bannerName;
                        setStatus(STATUSES.NO_BANNER_AVAILABLE, 'requestedBannerNotAvailable');
                    },
                    setBannerLoadedButHidden: function(reason) {
                        state.data.bannerLoadedButHiddenReason = reason;
                        setStatus(STATUSES.BANNER_LOADED_BUT_HIDDEN, reason);
                        state.data.result = 'hide';
                        state.data.reason = reason;
                    },
                    setAlterFunctionMissing: function() {
                        state.data.alterFunctionMissing = !0;
                    },
                    setBannerShown: function() {
                        setStatus(STATUSES.BANNER_SHOWN);
                        state.data.result = 'show';
                    },
                    setBannerCount: function(bannerCount) {
                        state.data.banner_count = bannerCount;
                    },
                    setRecordImpressionSampleRate: function(rate) {
                        state.data.recordImpressionSampleRate = rate;
                    },
                    setImpressionEventSampleRate: function(rate) {
                        if (!impressionEventSampleRateOverridden) {
                            state.data.impressionEventSampleRate = rate;
                        }
                    },
                    setBannerLoaderError: function(msg) {
                        if (msg) {
                            state.data.errorMsg = msg;
                        }
                        setStatus(STATUSES.BANNER_LOADER_ERROR);
                    },
                    setChoiceDataStale: function() {
                        setStatus(STATUSES.CHOICE_DATA_STALE);
                    },
                    registerTest: function(identifier) {
                        var tests = state.data.tests = state.data.tests || [];
                        if (tests.indexOf(identifier) === -1) {
                            tests.push(identifier);
                            if (tests.length === 1) {
                                state.data.testIdentifiers = identifier;
                            } else {
                                state.data.testIdentifiers += ',' + identifier;
                            }
                        }
                    },
                    setDebugInfo: function(str) {
                        if (!state.data.debugInfo) {
                            state.data.debugInfo = str;
                        } else {
                            state.data.debugInfo += '|' + str;
                        }
                    },
                    lookupReasonCode: function(reasonName) {
                        if (reasonName in REASONS) {
                            return REASONS[reasonName];
                        }
                        return REASONS.other;
                    }
                };
            }());
        },
        "chooser.js": function(require, module) {
            (function() {
                var cn = mw.centralNotice,
                    CAMPAIGN_STALENESS_LEEWAY = 15;

                function makeFilteredChoiceData(choiceData, country, anon, device) {
                    var i, campaign, j, banner, keepCampaign, filteredChoiceData = [];
                    for (i = 0; i < choiceData.length; i++) {
                        campaign = choiceData[i];
                        keepCampaign = !1;
                        if (campaign.geotargeted && (campaign.countries.indexOf(country) === -1)) {
                            continue;
                        }
                        for (j = 0; j < campaign.banners.length; j++) {
                            banner = campaign.
                            banners[j];
                            if (anon && !banner.display_anon) {
                                continue;
                            }
                            if (!anon && !banner.display_account) {
                                continue;
                            }
                            if (banner.devices.indexOf(device) === -1) {
                                continue;
                            }
                            keepCampaign = !0;
                            break;
                        }
                        if (keepCampaign) {
                            filteredChoiceData.push(campaign);
                        }
                    }
                    return filteredChoiceData;
                }

                function setCampaignAllocations(filteredChoiceData) {
                    var i, campaign, campaignPriority, campaignsByPriority = [],
                        priorities = [],
                        priority, campaignsAtThisPriority, remainingAllocation = 1,
                        j, campaignsAtThisPriorityCount, currentFullAllocation, actualAllocation;
                    if (filteredChoiceData.length === 1) {
                        filteredChoiceData[0].allocation = filteredChoiceData[0].throttle / 100;
                        return;
                    }
                    for (i = 0; i < filteredChoiceData.length; i++) {
                        campaign = filteredChoiceData[i];
                        campaignPriority = campaign.preferred;
                        if (!campaignsByPriority[campaignPriority]) {
                            campaignsByPriority[campaignPriority] = [];
                        }
                        campaignsByPriority[campaignPriority].push(campaign);
                    }
                    for (priority in campaignsByPriority) {
                        priorities.push(priority);
                    }
                    priorities.sort();
                    priorities.reverse();
                    for (i = 0; i < priorities.length; i++) {
                        campaignsAtThisPriority = campaignsByPriority[priorities[i]];
                        if (remainingAllocation < 0.01) {
                            for (j = 0; j < campaignsAtThisPriority.length; j++) {
                                campaignsAtThisPriority[j].allocation = 0;
                            }
                            continue;
                        }
                        campaignsAtThisPriority.sort(function(a, b) {
                            if (a.throttle < b.throttle) {
                                return -1;
                            }
                            if (a.throttle > b.throttle) {
                                return 1;
                            }
                            return 0;
                        });
                        campaignsAtThisPriorityCount = campaignsAtThisPriority.length;
                        for (j = 0; j < campaignsAtThisPriorityCount; j++) {
                            campaign = campaignsAtThisPriority[j];
                            currentFullAllocation = remainingAllocation / (campaignsAtThisPriorityCount - j);
                            actualAllocation = Math.min(currentFullAllocation, campaign.throttle / 100);
                            campaign.allocation = actualAllocation;
                            remainingAllocation -= actualAllocation;
                        }
                    }
                }

                function makePossibleBanners(campaign, bucket, anon, device) {
                    var i, campaignName, banner, possibleBanners = [];
                    campaignName = campaign.name;
                    for (i = 0; i < campaign.banners.length; i++) {
                        banner = campaign.banners[i];
                        if (bucket !== banner.bucket) {
                            continue;
                        }
                        if (anon && !banner.display_anon) {
                            continue;
                        }
                        if (!anon && !banner.display_account) {
                            continue;
                        }
                        if (
                            banner.devices.indexOf(device) === -1) {
                            continue;
                        }
                        possibleBanners.push(banner);
                    }
                    return possibleBanners;
                }

                function setBannerAllocations(possibleBanners) {
                    var i, banner, totalWeights = 0;
                    if (possibleBanners.length === 1) {
                        possibleBanners[0].allocation = 1;
                        return;
                    }
                    for (i = 0; i < possibleBanners.length; i++) {
                        totalWeights += possibleBanners[i].weight;
                    }
                    for (i = 0; i < possibleBanners.length; i++) {
                        banner = possibleBanners[i];
                        banner.allocation = banner.weight / totalWeights;
                    }
                }

                function chooseObjInAllocatedArray(random, allocatedArray) {
                    var blockStart = 0,
                        i, obj, blockEnd;
                    for (i = 0; i < allocatedArray.length; i++) {
                        obj = allocatedArray[i];
                        blockEnd = blockStart + obj.allocation;
                        if ((random >= blockStart) && (random < blockEnd)) {
                            return obj;
                        }
                        blockStart = blockEnd;
                    }
                    return null;
                }
                cn.internal.chooser = {
                    choiceDataSeemsFresh: function(choiceData) {
                        var i, campaign, now = new Date(),
                            campaignEndDatePlusLeeway;
                        for (i = 0; i < choiceData.length; i++) {
                            campaign = choiceData[i];
                            campaignEndDatePlusLeeway = new Date();
                            campaignEndDatePlusLeeway.setTime((campaign.end * 1000) + (
                                CAMPAIGN_STALENESS_LEEWAY * 60000));
                            if (campaignEndDatePlusLeeway < now) {
                                return false;
                            }
                        }
                        return true;
                    },
                    chooseCampaign: function(choiceData, country, anon, device, random) {
                        var filteredChoiceData = makeFilteredChoiceData(choiceData, country, anon, device);
                        if (filteredChoiceData.length === 0) {
                            return null;
                        }
                        setCampaignAllocations(filteredChoiceData);
                        return chooseObjInAllocatedArray(random, filteredChoiceData);
                    },
                    chooseBanner: function(campaign, bucket, anon, device, random) {
                        var possibleBanners = makePossibleBanners(campaign, bucket, anon, device);
                        if (possibleBanners.length === 0) {
                            return null;
                        }
                        setBannerAllocations(possibleBanners);
                        return chooseObjInAllocatedArray(random, possibleBanners);
                    },
                    requestBanner: function(campaign, bucket, anon, device, requestedBannerName) {
                        var i, possibleBanner, possibleBanners = makePossibleBanners(campaign, bucket, anon, device);
                        for (i = 0; i < possibleBanners.length; i++) {
                            possibleBanner = possibleBanners[i];
                            if (possibleBanner.name === requestedBannerName) {
                                return possibleBanner;
                            }
                        }
                        return null;
                    }
                };
            }());
        },
        "bucketer.js": function(require, module) {
            (function() {
                var bucketer, buckets = null,
                    campaign = null,
                    kvStore = mw.centralNotice.kvStore,
                    multiStorageOption, LEGACY_COOKIE = 'CN',
                    STORAGE_KEY = 'buckets';

                function escapeCampaignName(name) {
                    return name.replace(/[*!]/g, function(match) {
                        return '&#' + match.charCodeAt(0);
                    });
                }

                function decodeCampaignName(name) {
                    return name.replace(/&#(33|42)/, function(match, $1) {
                        return String.fromCharCode($1);
                    });
                }

                function parseSerializedBuckets(serialized) {
                    var parsedBuckets = {};
                    serialized.split('*').forEach(function(strBucket) {
                        var parts = strBucket.split('!'),
                            key = decodeCampaignName(parts[0]),
                            start = parseInt(parts[1], 10) + 14e8,
                            end = start + parseInt(parts[2], 10),
                            val = parseInt(parts[3], 10);
                        if (key && start && end && !isNaN(val)) {
                            parsedBuckets[key] = {
                                start: start,
                                end: end,
                                val: val
                            };
                        }
                    });
                    return parsedBuckets;
                }

                function possiblyLoadAndMigrateLegacyBuckets() {
                    var cookieVal = $.cookie(LEGACY_COOKIE);
                    if (cookieVal) {
                        buckets = parseSerializedBuckets(cookieVal);
                        storeBuckets();
                        $.removeCookie(LEGACY_COOKIE, {
                            path: '/'
                        });
                        return true;
                    }
                    return false;
                }

                function loadBuckets() {
                    var val = kvStore.getItem(STORAGE_KEY, kvStore.contexts.GLOBAL, multiStorageOption);
                    buckets = (val ? parseSerializedBuckets(val) : {});
                }

                function storeBuckets() {
                    var expires = Math.ceil((new Date()) / 1000),
                        serialized = $.map(buckets, function(opts, key) {
                            var parts = [escapeCampaignName(key), Math.floor(opts.start - 14e8), Math.ceil(opts.end - opts.start), opts.val];
                            if (opts.end > expires) {
                                expires = Math.ceil(opts.end);
                            }
                            return parts.join('!');
                        }).join('*');
                    kvStore.setItem(STORAGE_KEY, serialized, kvStore.contexts.GLOBAL, Math.ceil((expires - (new Date()) / 1000) / 86400), multiStorageOption);
                }

                function getRandomBucket() {
                    return Math.floor(Math.random() * mw.config.get('wgNoticeNumberOfControllerBuckets'));
                }

                function retrieveProcessAndGet() {
                    var campaignName = campaign.name,
                        campaignStartDate, bucket, bucketEndDate, retrievedBucketEndDate, val, extension = mw.config.get('wgCentralNoticePerCampaignBucketExtension'),
                        now = new Date(),
                        bucketsModified = !1;
                    campaignStartDate = new Date();
                    campaignStartDate.setTime(campaign.start *
                        1000);
                    bucketEndDate = new Date();
                    bucketEndDate.setTime(campaign.end * 1000);
                    bucketEndDate.setUTCDate(bucketEndDate.getUTCDate() + extension);
                    multiStorageOption = kvStore.getMultiStorageOption(true);
                    if (!possiblyLoadAndMigrateLegacyBuckets()) {
                        loadBuckets();
                    }
                    bucket = buckets[campaignName];
                    if (bucket && bucketEndDate > now) {
                        retrievedBucketEndDate = new Date();
                        retrievedBucketEndDate.setTime(bucket.end * 1000);
                        if (retrievedBucketEndDate.getTime() !== bucketEndDate.getTime()) {
                            bucket.end = bucketEndDate.getTime() / 1000;
                            bucketsModified = !0;
                        }
                    } else {
                        val = getRandomBucket();
                        bucket = {
                            val: val,
                            start: campaignStartDate.getTime() / 1000,
                            end: bucketEndDate.getTime() / 1000
                        };
                        buckets[campaignName] = bucket;
                        bucketsModified = !0;
                    }
                    for (campaignName in buckets) {
                        bucketEndDate = new Date();
                        bucketEndDate.setTime(buckets[campaignName].end * 1000);
                        if (bucketEndDate < now) {
                            delete buckets[campaignName];
                            bucketsModified = !0;
                        }
                    }
                    if (bucketsModified) {
                        storeBuckets();
                    }
                }
                bucketer = mw.centralNotice.internal.bucketer = {
                    setCampaign: function(c) {
                        campaign = c;
                    },
                    process: function() {
                        retrieveProcessAndGet();
                    },
                    getBucket: function() {
                        return buckets[campaign.name].val;
                    },
                    getReducedBucket: function() {
                        return bucketer.getBucket() % campaign.bucket_count;
                    },
                    setBucket: function(val) {
                        buckets[campaign.name].val = val;
                        storeBuckets();
                    }
                };
            }());
        },
        "hide.js": function(require, module) {
            (function() {
                var hide, category, cookieName, shouldHide = !1,
                    reason, durations = mw.config.get('wgNoticeCookieDurations'),
                    HIDE_COOKIE_PREFIX = 'centralnotice_hide_',
                    MAX_CUSTOM_HIDE_DURATION = 2592000;

                function removeCookie() {
                    $.cookie(cookieName, null, {
                        path: '/'
                    });
                }
                hide = mw.centralNotice.internal.hide = {
                    setCategory: function(c) {
                        category = c;
                        cookieName = HIDE_COOKIE_PREFIX + category;
                    },
                    processCookie: function() {
                        var rawCookieVal = $.cookie(cookieName),
                            hideData, now;
                        if (!rawCookieVal) {
                            return;
                        }
                        if (rawCookieVal === 'hide') {
                            removeCookie();
                            return;
                        }
                        try {
                            hideData = JSON.parse(rawCookieVal);
                        } catch (e) {
                            removeCookie();
                            return;
                        }
                        now = new Date().getTime() / 1000;
                        if (now < hideData.created + (durations[hideData.reason] || MAX_CUSTOM_HIDE_DURATION)) {
                            shouldHide = !0;
                            reason =
                                hideData.reason;
                        }
                    },
                    shouldHide: function() {
                        return shouldHide;
                    },
                    getReason: function() {
                        return reason;
                    },
                    setHideCookies: function(reason, duration) {
                        var date = new Date(),
                            hideData = {
                                v: 1,
                                created: Math.floor(date.getTime() / 1000),
                                reason: reason
                            };
                        if (!(reason in durations)) {
                            duration = Math.min(MAX_CUSTOM_HIDE_DURATION, duration);
                        }
                        date.setSeconds(date.getSeconds() + duration);
                        $.cookie(cookieName, JSON.stringify(hideData), {
                            expires: date,
                            path: '/'
                        });
                        mw.config.get('wgNoticeHideUrls').forEach(function(val) {
                            var url = new mw.Uri(val);
                            url.extend({
                                duration: duration,
                                category: category,
                                reason: reason
                            });
                            document.createElement('img').src = url.toString();
                        });
                    },
                    setHideWithCloseButtonCookies: function() {
                        hide.setHideCookies('close', durations.close);
                    }
                };
            }());
        },
        "config.json": {
            "categoriesUsingLegacy": ["Fundraising", "fundraising"]
        }
    }
}, {
    "css": [
        "@media print{#centralNotice{display:none}}.cn-closeButton{display:inline-block;zoom:1;background:url(data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAABQAAAAUBAMAAAB/pwA+AAAAElBMVEUAAAAQEBDPz88AAABAQEDv7+9oe1vvAAAABnRSTlMA3rLe3rJS22KzAAAARElEQVQI12PAAUIUQCSTK5BwFgIxFU1AhKECUFAYKAAioXwwBeZChMGCEGGQIFQYJohgIhQgtCEMQ7ECYTHCOciOxA4AADgJTXIb9s8AAAAASUVORK5CYII=) no-repeat;background:url(/w/extensions/CentralNotice/resources/ext.centralNotice.display/images/close.png?8e3d8) no-repeat!ie;width:20px;height:20px;text-indent:20px;white-space:nowrap;overflow:hidden}"
    ]
});
mw.loader.implement("ext.centralNotice.geoIP@1k62gia", function($, jQuery, require, module) {
    (function() {
        var COOKIE_NAME = 'GeoIP',
            geoPromise;

        function parseCookieValue(cookieValue) {
            var matches = cookieValue.match(/([^:]*):([^:]*):([^:]*):([^:]*):([^:]*):([^;]*)/) || cookieValue.match(/([^:]*):([^:]*):([^:]*):([^:]*):([^;]*)/);
            if (!matches) {
                return null;
            }
            if (matches.length === 6) {
                matches = matches.slice(0, 2).concat(['']).concat(matches.slice(2));
            }
            if ((typeof matches[1] !== 'string') || (matches[1].length === 0)) {
                return null;
            }
            return {
                country: matches[1],
                region: matches[2],
                city: matches[3],
                lat: matches[4] && parseFloat(matches[4]),
                lon: matches[5] && parseFloat(matches[5]),
                af: matches[6]
            };
        }

        function storeGeoInCookie(geo) {
            var parts = [geo.country, geo.region || '', (geo.city && geo.city.replace(/[^a-z]/i, '_')) || '', geo.lat || '', geo.lon || '', geo.af || ''],
                cookieValue = parts.join(':');
            $.cookie(COOKIE_NAME, cookieValue, {
                path: '/'
            });
        }
        mw.geoIP = {
            makeGeoWithPromise: function() {
                var cookieValue = $.cookie(COOKIE_NAME),
                    geo, lookupModule;
                if (cookieValue) {
                    geo
                        = parseCookieValue(cookieValue);
                    if (geo) {
                        geoPromise = $.Deferred().resolve(geo).promise();
                        return;
                    }
                }
                lookupModule = mw.config.get('wgCentralNoticeGeoIPBackgroundLookupModule');
                if (lookupModule) {
                    geoPromise = mw.loader.using(lookupModule).then(function() {
                        var lookupCallback = require(lookupModule);
                        return lookupCallback();
                    });
                    geoPromise.then(function(geo) {
                        storeGeoInCookie(geo);
                    });
                } else {
                    geoPromise = $.Deferred().reject().promise();
                }
            },
            getPromise: function() {
                return geoPromise;
            }
        };
        mw.geoIP.makeGeoWithPromise();
        geoPromise.done(function(geo) {
            window.Geo = geo;
        });
    }());
});
mw.loader.implement("ext.centralNotice.impressionDiet@1kxxq34", function($, jQuery, require, module) {
    (function() {
        'use strict';
        var identifier, multiStorageOption, cn = mw.centralNotice,
            mixin = new cn.Mixin('impressionDiet'),
            counts, now = new Date().getTime(),
            STORAGE_KEY = 'impression_diet',
            WAIT_COOKIE_SUFFIX = '-wait',
            COUNTS_STORAGE_TTL = 365;
        mixin.setPreBannerHandler(function(mixinParams) {
            var hide;
            if (mw.util.getParamValue('force')) {
                return;
            }
            identifier = mixinParams.cookieName;
            multiStorageOption = cn.kvStore.getMultiStorageOption(cn.getDataProperty('campaignCategoryUsesLegacy'));
            possiblyMigrateLegacyCookies();
            if (cn.isBannerCanceled()) {
                return;
            }
            if (multiStorageOption === cn.kvStore.multiStorageOptions.NO_STORAGE) {
                cn.cancelBanner('waitnostorage');
                return;
            }
            if (mw.util.getParamValue('reset') === '1') {
                counts = getZeroedCounts();
            } else {
                counts = getCounts();
            }
            if (now > counts.nextCycleStart && counts.seenThisCycle >= mixinParams.maximumSeen) {
                if (mixinParams.restartCycleDelay !== 0) {
                    counts.skippedThisCycle = 0;
                    counts.seenThisCycle = 0;
                }
            }
            if (
                counts.seenThisCycle < mixinParams.maximumSeen) {
                if (counts.skippedThisCycle < mixinParams.skipInitial) {
                    hide = 'waitimps';
                    counts.skippedThisCycle += 1;
                } else {
                    hide = !1;
                }
            } else {
                hide = 'waitdate';
            }
            if (hide) {
                cn.cancelBanner(hide);
            } else {
                counts.seenThisCycle += 1;
                counts.seenCount += 1;
                counts.nextCycleStart = now + (mixinParams.restartCycleDelay * 1000);
            }
            storeCounts(counts);
        });

        function getZeroedCounts() {
            return {
                seenCount: 0,
                skippedThisCycle: 0,
                nextCycleStart: 0,
                seenThisCycle: 0
            };
        }

        function fixCountNames(kvStoreCounts) {
            if (kvStoreCounts.skippedThisCycle !== undefined) {
                return kvStoreCounts;
            }
            return {
                seenCount: kvStoreCounts.seenCount,
                skippedThisCycle: kvStoreCounts.waitCount,
                nextCycleStart: kvStoreCounts.waitUntil,
                seenThisCycle: kvStoreCounts.waitSeenCount
            };
        }

        function possiblyMigrateLegacyCookies() {
            var rawCookie, rawWaitCookie, waitData, cookieCounts;
            if (!identifier) {
                return;
            }
            rawCookie = $.cookie(identifier);
            if (!rawCookie) {
                return;
            }
            rawWaitCookie = $.cookie(identifier + WAIT_COOKIE_SUFFIX);
            waitData = (rawWaitCookie || '').split(/[|]/);
            cookieCounts = {
                seenCount: parseInt(rawCookie, 10) || 0,
                skippedThisCycle: parseInt(waitData[0], 10) || 0,
                nextCycleStart: parseInt(waitData[1], 10) || 0,
                seenThisCycle: parseInt(waitData[2], 10) || 0
            };
            storeCounts(cookieCounts);
            $.removeCookie(identifier, {
                path: '/'
            });
            $.removeCookie(identifier + WAIT_COOKIE_SUFFIX, {
                path: '/'
            });
        }

        function getCounts() {
            var counts;
            if (identifier) {
                counts = cn.kvStore.getItem(STORAGE_KEY + '_' + identifier, cn.kvStore.contexts.GLOBAL, multiStorageOption);
            } else {
                counts = cn.kvStore.getItem(STORAGE_KEY, cn.kvStore.contexts.CATEGORY, multiStorageOption);
            }
            counts = counts || getZeroedCounts();
            return fixCountNames(counts);
        }

        function storeCounts(counts) {
            if (identifier) {
                cn.kvStore.setItem(STORAGE_KEY + '_' + identifier, counts, cn.kvStore.contexts.GLOBAL, COUNTS_STORAGE_TTL, multiStorageOption);
            } else {
                cn.kvStore.setItem(STORAGE_KEY, counts, cn.kvStore.contexts.CATEGORY, COUNTS_STORAGE_TTL, multiStorageOption);
            }
        }
        cn.registerCampaignMixin(mixin);
    }());
});
mw.loader.implement("ext.centralNotice.kvStore@0sgn2qe", {
    "main": "index.js",
    "files": {
        "index.js": function(require, module) {
            mw.centralNotice = (mw.centralNotice || {});
            mw.centralNotice.kvStore = require('./kvStore.js');
        },
        "kvStore.js": function(require, module) {
            (function() {
                var KVStorageContext, kvStore, error = null,
                    campaignName = null,
                    bannerName = null,
                    category = null,
                    cookiesEnabled = null,
                    localStorageAvailable = null,
                    now = Math.round((new Date()).getTime() / 1000),
                    SEPARATOR = '|',
                    SEPARATOR_IN_COOKIES = '!',
                    FIND_KEY_REGEX = /\|([^|]*)$/,
                    PREFIX = 'CentralNoticeKV',
                    PREFIX_IN_COOKIES = 'CN',
                    DEFAULT_ITEM_TTL = 15768000;
                KVStorageContext = function(key, keyInCookies) {
                    this.key = key;
                    this.keyInCookies = keyInCookies;
                };

                function areCookiesEnabled() {
                    if (cookiesEnabled === null) {
                        $.cookie('cookieTest', 'testVal');
                        cookiesEnabled = ($.cookie('cookieTest') === 'testVal');
                        $.removeCookie('cookieTest');
                    }
                    return cookiesEnabled;
                }

                function isLocalStorageAvailable() {
                    if (localStorageAvailable === null) {
                        try {
                            if (!window.localStorage) {
                                localStorageAvailable = !1;
                            } else {
                                localStorage.setItem('localStorageTest', 'testVal');
                                localStorageAvailable = (localStorage.getItem('localStorageTest') === 'testVal');
                                localStorage.removeItem('localStorageTest');
                            }
                        } catch (e) {
                            localStorageAvailable = !1;
                        }
                    }
                    return localStorageAvailable;
                }

                function setError(message, key, value, context) {
                    error = {
                        message: message,
                        key: key,
                        value: value,
                        context: context ? context.key : null,
                        time: new Date()
                    };
                    error.campaign = campaignName;
                    error.banner = bannerName;
                    mw.log('CentralNotice KV storage error: ' + JSON.stringify(error));
                }

                function makeKeyForLocalStorage(key, context) {
                    var base = PREFIX + SEPARATOR + context.key + SEPARATOR;
                    switch (context.key) {
                        case kvStore.contexts.CAMPAIGN.key:
                            return base + campaignName + SEPARATOR + key;
                        case kvStore.contexts.CATEGORY.key:
                            return base + category + SEPARATOR + key;
                        case kvStore.contexts.GLOBAL.key:
                            return base + key;
                        default:
                            setError('Invalid KV storage context', key, null, context);
                            return base + 'invalidContext' + SEPARATOR + key;
                    }
                }

                function makeKeyForCookie(key, context) {
                    var base = PREFIX_IN_COOKIES + SEPARATOR_IN_COOKIES +
                        context.keyInCookies + SEPARATOR_IN_COOKIES;
                    switch (context.key) {
                        case kvStore.contexts.CAMPAIGN.key:
                            return base + campaignName + SEPARATOR_IN_COOKIES + key;
                        case kvStore.contexts.CATEGORY.key:
                            return base + category + SEPARATOR_IN_COOKIES + key;
                        case kvStore.contexts.GLOBAL.key:
                            return base + key;
                        default:
                            setError('Invalid KV storage context', key, null, context);
                            return base + 'invalidContext' + SEPARATOR_IN_COOKIES + key;
                    }
                }

                function setLocalStorageItem(key, value, context, ttl) {
                    var lsKey, encodedWrappedValue;
                    lsKey = makeKeyForLocalStorage(key, context);
                    encodedWrappedValue = JSON.stringify({
                        expiry: ttl ? (ttl * 86400) + now : DEFAULT_ITEM_TTL + now,
                        val: value
                    });
                    try {
                        localStorage.setItem(lsKey, encodedWrappedValue);
                        if (localStorage.getItem(lsKey) !== encodedWrappedValue) {
                            setError('Couldn\'t write value', key, value, context);
                            return false;
                        }
                        return true;
                    } catch (e) {
                        setError('Couldn\'t write value due to LocalStorage exception ' + e.toString(), key, value, context);
                        return false;
                    }
                }

                function setCookieItem(key, value, context, ttl) {
                    return Boolean($.cookie(
                        makeKeyForCookie(key, context), encodeURIComponent(JSON.stringify(value)), {
                            expires: ttl,
                            path: '/'
                        }));
                }

                function getLocalStorageItem(key, context) {
                    var lsKey = makeKeyForLocalStorage(key, context),
                        rawValue, wrappedValue;
                    try {
                        rawValue = localStorage.getItem(lsKey);
                    } catch (e) {
                        setError('Couldn\'t read value due to LocalStorage exception ' + e.toString(), key, null, context);
                        return null;
                    }
                    if (rawValue === null) {
                        return null;
                    }
                    try {
                        wrappedValue = JSON.parse(rawValue);
                    } catch (e) {
                        if (e instanceof SyntaxError) {
                            setError('Couldn\'t parse value, removing. ' + e.message, key, rawValue, context);
                            try {
                                localStorage.removeItem(lsKey);
                            } catch (ex) {
                                setError('Couldn\'t remove value due to LocalStorage exception ' + ex.toString(), key, rawValue, context);
                            }
                            return null;
                        } else {
                            setError('Couldn\'t read value ' + e.message, key, rawValue, context);
                            throw e;
                        }
                    }
                    if (!wrappedValue.expiry || wrappedValue.expiry < now) {
                        return null;
                    }
                    return wrappedValue.val;
                }

                function getCookieItem(key, context) {
                    var storageKey = makeKeyForCookie(key, context),
                        rawCookie = $.cookie(storageKey);
                    try {
                        return JSON.parse(decodeURIComponent(rawCookie));
                    } catch (e) {
                        $.removeCookie(storageKey, {
                            path: '/'
                        });
                        return null;
                    }
                }

                function removeLocalStorageItem(key, context) {
                    try {
                        localStorage.removeItem(makeKeyForLocalStorage(key, context));
                    } catch (e) {
                        setError('Couldn\'t remove value due to LocalStorage exception ' + e.toString(), key, null, context);
                    }
                }

                function removeCookieItem(key, context) {
                    $.removeCookie(makeKeyForCookie(key, context), {
                        path: '/'
                    });
                }
                kvStore = {
                    contexts: {
                        CAMPAIGN: new KVStorageContext('campaign', 'c'),
                        CATEGORY: new KVStorageContext('category', 't'),
                        GLOBAL: new KVStorageContext('global', 'g')
                    },
                    multiStorageOptions: {
                        LOCAL_STORAGE: 'kv_store',
                        COOKIE: 'cookie',
                        NO_STORAGE: 'no_storage'
                    },
                    setItem: function(key, value, context, ttl, multiStorageOption) {
                        if ((key.indexOf(SEPARATOR) !== -1) || (key.indexOf(SEPARATOR_IN_COOKIES) !== -1)) {
                            setError('Invalid key', key, value, context);
                            return false;
                        }
                        multiStorageOption = multiStorageOption || kvStore.multiStorageOptions.LOCAL_STORAGE;
                        switch (multiStorageOption) {
                            case kvStore.multiStorageOptions.
                            LOCAL_STORAGE:
                                return setLocalStorageItem(key, value, context, ttl);
                            case kvStore.multiStorageOptions.COOKIE:
                                return setCookieItem(key, value, context, ttl);
                            case kvStore.multiStorageOptions.NO_STORAGE:
                                return false;
                            default:
                                throw new Error('Unexpected multi-storage option');
                        }
                    },
                    getItem: function(key, context, multiStorageOption) {
                        multiStorageOption = multiStorageOption || kvStore.multiStorageOptions.LOCAL_STORAGE;
                        switch (multiStorageOption) {
                            case kvStore.multiStorageOptions.LOCAL_STORAGE:
                                return getLocalStorageItem(key, context);
                            case kvStore.multiStorageOptions.COOKIE:
                                return getCookieItem(key, context);
                            case kvStore.multiStorageOptions.NO_STORAGE:
                                return null;
                            default:
                                throw new Error('Unexpected multi-storage option');
                        }
                    },
                    removeItem: function(key, context, multiStorageOption) {
                        multiStorageOption = multiStorageOption || kvStore.multiStorageOptions.LOCAL_STORAGE;
                        switch (multiStorageOption) {
                            case kvStore.multiStorageOptions.LOCAL_STORAGE:
                                removeLocalStorageItem(key, context);
                                return;
                            case kvStore.multiStorageOptions.COOKIE:
                                removeCookieItem(key, context);
                                return;
                            case kvStore.multiStorageOptions.NO_STORAGE:
                                return;
                            default:
                                throw new Error('Unexpected multi-storage option');
                        }
                    },
                    isAvailable: function() {
                        return (kvStore.getMultiStorageOption(false) !== kvStore.multiStorageOptions.NO_STORAGE);
                    },
                    getMultiStorageOption: function(cookieAllowed) {
                        if (isLocalStorageAvailable()) {
                            return kvStore.multiStorageOptions.LOCAL_STORAGE;
                        }
                        if (cookieAllowed && areCookiesEnabled()) {
                            return kvStore.multiStorageOptions.COOKIE;
                        }
                        return kvStore.multiStorageOptions.NO_STORAGE;
                    },
                    getError: function() {
                        return error;
                    },
                    setNotAvailableError: function() {
                        setError('LocalStorage not available.', null, null);
                    },
                    setMaintenanceError: function(lsKey) {
                        var m = lsKey.match(FIND_KEY_REGEX),
                            key = m ? m[1] : null;
                        setError('Error during KVStore maintenance.', key, null);
                    },
                    setCampaignName: function(cName) {
                        campaignName = cName;
                    },
                    setBannerName: function(bName) {
                        bannerName = bName;
                    },
                    setCategory: function(c) {
                        category = c;
                    }
                };
                module.exports = kvStore;
            }());
        }
    }
});
mw.loader.implement("ext.centralNotice.largeBannerLimit@1a2sg8p", function($, jQuery, require, module) {
    (function() {
        'use strict';
        var identifier, days, multiStorageOption, cn = mw.centralNotice,
            forced = mw.util.getParamValue('force'),
            mixin = new cn.Mixin('largeBannerLimit'),
            STORAGE_KEY = 'large_banner_limit',
            BANNER_SEQUENCE_FLAG_STORAGE_KEY = 'banner_sequence_step_seen';

        function isLarge() {
            return (cn.getDataProperty('reducedBucket') <= 1);
        }

        function possiblyMigrateLegacyCookie() {
            if (!identifier) {
                return;
            }
            if (mw.cookie.get(identifier, '')) {
                cn.setDebugInfo('lbl: setting flag from legacy cookie');
                setFlag();
                mw.cookie.set(identifier, null, {
                    path: '/',
                    prefix: ''
                });
            }
        }

        function checkFlag() {
            if (identifier) {
                return Boolean(cn.kvStore.getItem(STORAGE_KEY + '_' + identifier, cn.kvStore.contexts.GLOBAL, multiStorageOption)) || Boolean(cn.kvStore.getItem(BANNER_SEQUENCE_FLAG_STORAGE_KEY + '_' + identifier, cn.kvStore.contexts.GLOBAL, multiStorageOption));
            }
            return Boolean(cn.kvStore.getItem(STORAGE_KEY, cn.kvStore.contexts.CATEGORY, multiStorageOption));
        }

        function setFlag() {
            var nowTS = Math.round(Date.now() / 1000);
            if (identifier) {
                cn.kvStore.setItem(STORAGE_KEY + '_' + identifier, nowTS, cn.kvStore.contexts.GLOBAL, days, multiStorageOption);
            } else {
                cn.kvStore.setItem(STORAGE_KEY, nowTS, cn.kvStore.contexts.CATEGORY, days, multiStorageOption);
            }
        }
        mixin.setPreBannerHandler(function(mixinParams) {
            var switchToHigherBucket = !1;
            if (forced) {
                return;
            }
            identifier = mixinParams.identifier;
            days = mixinParams.days;
            multiStorageOption = cn.kvStore.getMultiStorageOption(cn.getDataProperty('campaignCategoryUsesLegacy'));
            possiblyMigrateLegacyCookie();
            if (cn.isBannerCanceled()) {
                cn.setDebugInfo('lbl: hidden');
                return;
            }
            if (!isLarge()) {
                cn.setDebugInfo('lbl: previously switched');
                return;
            }
            if (multiStorageOption === cn.kvStore.multiStorageOptions.NO_STORAGE) {
                cn.setDebugInfo('lbl: no storage, switching');
                switchToHigherBucket = !0;
            } else if (checkFlag()) {
                cn.setDebugInfo('lbl: flag found, switching');
                switchToHigherBucket = !0;
            }
            if (switchToHigherBucket) {
                if (mixinParams.randomize) {
                    cn.setBucket(Math.floor(Math.random() * 2) + 2);
                } else {
                    cn.setBucket(cn.getDataProperty('bucket') + 2);
                }
            }
        });
        mixin.setPostBannerHandler(function() {
            if (isLarge() && !forced && cn.isBannerShown()) {
                cn.setDebugInfo('lbl: setting flag');
                setFlag();
            }
        });
        cn.registerCampaignMixin(mixin);
    }());
});
mw.loader.implement("ext.centralNotice.legacySupport@0q9ylxz", function($, jQuery, require, module) {
    (function() {
        var cn = mw.centralNotice,
            mixin = new cn.Mixin('legacySupport');
        mixin.setPreBannerHandler(function(mixinParams) {
            if (mixinParams.setSRISampleRate) {
                cn.setRecordImpressionSampleRate(mixinParams.sriSampleRate);
            }
            if (mixinParams.bannersNotGuaranteedToDisplay) {
                cn.setBannersNotGuaranteedToDisplay();
            }
        });
        cn.registerCampaignMixin(mixin);
    }());
});
mw.loader.implement("ext.centralNotice.startUp@0cw8q4o", {
    "main": "index.js",
    "files": {
        "index.js": function(require, module) {
            (function() {
                var cn = mw.centralNotice,
                    config = require('./config.json'),
                    cookiesToDelete = config.cookiesToDelete,
                    testingBannerName = mw.util.getParamValue('banner'),
                    kvStoreMaintenance = require('./kvStoreMaintenance.js'),
                    NULL_BANNER_NAME = 'null';
                cn.kvStoreMaintenance = kvStoreMaintenance;
                if (cookiesToDelete && cookiesToDelete.length > 0) {
                    mw.requestIdleCallback(deleteOldCookies);
                }
                $(function() {
                    $('#siteNotice').prepend('<div id="centralNotice"></div>');
                });
                if (testingBannerName) {
                    if (testingBannerName === NULL_BANNER_NAME) {
                        return;
                    }
                    mw.loader.using('ext.centralNotice.display').done(function() {
                        cn.displayTestingBanner();
                    });
                    return;
                }
                if (cn.choiceData === undefined) {
                    mw.log.warn('No choice data set for CentralNotice campaign ' + 'and banner selection.');
                    return;
                }
                mw.requestIdleCallback(kvStoreMaintenance.doMaintenance);
                if (cn.choiceData.length === 0) {
                    return;
                }
                if (!cn.chooseAndMaybeDisplay) {
                    mw.log.warn(
                        'Possible campaign(s) received in choiceData, but ' + 'mw.centralNotice.chooseAndMaybeDisplay() is not available');
                    return;
                }
                cn.chooseAndMaybeDisplay();

                function deleteOldCookies() {
                    mw.loader.using('mediawiki.cookie').done(function() {
                        mw.requestIdleCallback(function(deadline) {
                            while (cookiesToDelete.length > 0 && deadline.timeRemaining() > 3) {
                                mw.cookie.set(cookiesToDelete.shift(), null, {
                                    path: '/',
                                    prefix: ''
                                });
                            }
                        });
                    });
                }
            }());
        },
        "kvStoreMaintenance.js": function(require, module) {
            (function() {
                var kvStoreMaintenance, now = new Date().getTime() / 1000,
                    PREFIX_REGEX = /^CentralNoticeKV/,
                    PREFIX_AND_SEPARATOR_IN_COOKIES = 'CN!',
                    LEEWAY_FOR_REMOVAL = 86400,
                    MIN_WORK_TIME = 3;

                function getKeys() {
                    return $.Deferred(function(d) {
                        mw.requestIdleCallback(function(deadline) {
                            var key, keys = [],
                                index = localStorage.length;
                            while (index-- > 0 && deadline.timeRemaining() > MIN_WORK_TIME) {
                                key = localStorage.key(index);
                                if (key !== null && PREFIX_REGEX.test(key)) {
                                    keys.push(key);
                                }
                            }
                            d.resolve(keys);
                        });
                    }).promise();
                }

                function processKeys(queue) {
                    return $.Deferred(function(d) {
                        mw.requestIdleCallback(function iterate(deadline) {
                            var key, rawValue, value;
                            while (queue[0] !== undefined && deadline.timeRemaining() > MIN_WORK_TIME) {
                                key = queue.shift();
                                try {
                                    rawValue = localStorage.getItem(key);
                                    if (rawValue) {
                                        value = JSON.parse(rawValue);
                                        if (!value.expiry || (value.expiry + LEEWAY_FOR_REMOVAL) < now) {
                                            localStorage.removeItem(key);
                                        }
                                    }
                                } catch (e) {
                                    localStorage.removeItem(key);
                                    mw.log.warn('CentralNotice kvStoreMaintenance error for key ' + key, e);
                                }
                            }
                            if (queue[0] !== undefined) {
                                mw.requestIdleCallback(iterate);
                            } else {
                                d.resolve();
                            }
                        });
                    }).promise();
                }

                function purgeFallbackCookies() {
                    var cookies = document.cookie.split(';'),
                        i, matches, r = new RegExp('^' + PREFIX_AND_SEPARATOR_IN_COOKIES + '[^=]*(?==)');
                    for (i = 0; i < cookies.length; i++) {
                        matches = cookies[i].trim().match(r);
                        if (matches) {
                            document.cookie = matches[0] + '=; expires=Thu, 01 Jan 1970 00:00:00 GMT; path=/';
                        }
                    }
                }
                kvStoreMaintenance = {
                    doMaintenance: function() {
                        try {
                            if (!window.localStorage || !localStorage.length) {
                                return $.Deferred().resolve();
                            }
                        } catch (e) {
                            return $.Deferred().resolve();
                        }
                        if (document.cookie.indexOf(PREFIX_AND_SEPARATOR_IN_COOKIES) !== -1) {
                            purgeFallbackCookies();
                        }
                        return getKeys().then(processKeys);
                    }
                };
                module.exports = kvStoreMaintenance;
            }());
        },
        "config.json": {
            "cookiesToDelete": []
        }
    }
});
mw.loader.implement("ext.centralauth.centralautologin@0f63rmk", {
    "main": "ext.centralauth.centralautologin.js",
    "files": {
        "ext.centralauth.centralautologin.js": function(require, module) {
            (function() {
                var url, params, len, param, i;
                if (mw.config.get('wgUserName') !== null) {
                    return;
                }
                if (mw.config.get('wgCanonicalSpecialPageName') !== 'Userlogin') {
                    try {
                        if (+localStorage.getItem('CentralAuthAnon') > Date.now()) {
                            return;
                        }
                    } catch (e) {}
                    if (/(^|; )CentralAuthAnon=1/.test(document.cookie)) {
                        return;
                    }
                }
                url = require('./data.json').checkLoggedInURL;
                if (url) {
                    url += '&proto=' + encodeURIComponent(location.protocol.replace(':', ''));
                    if (mw.config.get('wgCentralAuthMobileDomain') === true) {
                        url += '&mobile=1';
                    }
                    if (mw.config.get('wgCanonicalSpecialPageName') === 'Userlogin') {
                        url += '&return=1';
                        params = location.search.slice(1).split('&');
                        len = params.length;
                        for (i = 0; i < len; i++) {
                            param = params[i].split('=');
                            param = decodeURIComponent(param[0]);
                            if (param === 'returnto' || param === 'returntoquery') {
                                url += '&' + params[i];
                            }
                        }
                    }
                    mw.loader.load(url);
                }
            }());
        },
        "data.json": {
            "checkLoggedInURL": "//login.wikimedia.org/wiki/Special:CentralAutoLogin/checkLoggedIn?type=script\u0026wikiid=enwiki\u0026mobile=1"
        }
    }
}, {
    "css": [
        "@-webkit-keyframes centralAuthPPersonalAnimation{0%{opacity:0;-webkit-transform:translateY(-20px)}100%{opacity:1;-webkit-transform:translateY(0)}}@-moz-keyframes centralAuthPPersonalAnimation{0%{opacity:0;-moz-transform:translateY(-20px)}100%{opacity:1;-moz-transform:translateY(0)}}@-o-keyframes centralAuthPPersonalAnimation{0%{opacity:0;-o-transform:translateY(-20px)}100%{opacity:1;-o-transform:translateY(0)}}@keyframes centralAuthPPersonalAnimation{0%{opacity:0;transform:translateY(-20px)}100%{opacity:1;transform:translateY(0)}}.centralAuthPPersonalAnimation{-webkit-animation-duration:1s;-moz-animation-duration:1s;-o-animation-duration:1s;animation-duration:1s;-webkit-animation-fill-mode:both;-moz-animation-fill-mode:both;-o-animation-fill-mode:both;animation-fill-mode:both;-webkit-animation-name:centralAuthPPersonalAnimation;-moz-animation-name:centralAuthPPersonalAnimation;-o-animation-name:centralAuthPPersonalAnimation;animation-name:centralAuthPPersonalAnimation}"
    ]
});
mw.loader.implement("ext.eventLogging@1qxckkx", {
    "main": "subscriber.js",
    "files": {
        "subscriber.js": function(require, module) {
            (function() {
                'use strict';
                mw.eventLog = require('./core.js');
                mw.eventLog.Schema = require('./Schema.js');

                function titleCase(word) {
                    return word[0].toUpperCase() + word.slice(1);
                }

                function handleTrackedEvent(topic, event) {
                    var schema = titleCase(topic.slice(topic.indexOf('.') + 1));
                    mw.eventLog.logEvent(schema, event);
                }

                function init() {
                    mw.trackSubscribe('event.', handleTrackedEvent);
                }
                if (document.readyState === 'complete') {
                    mw.requestIdleCallback(init);
                } else {
                    $(window).one('load', init);
                }
            }());
        },
        "core.js": function(require, module) {
            (function() {
                'use strict';
                var core, baseUrl, debugMode;
                baseUrl = require('./data.json').baseUrl;
                debugMode = Number(mw.user.options.get('eventlogging-display-web')) === 1;
                core = {
                    maxUrlSize: 2000,
                    getRevision: function(schemaName) {
                        return mw.config.get('wgEventLoggingSchemaRevision')[schemaName] || -1;
                    },
                    prepare: function(schemaName, eventData) {
                        return {
                            event: eventData,
                            revision: core.
                            getRevision(schemaName),
                            schema: schemaName,
                            webHost: location.hostname,
                            wiki: mw.config.get('wgDBname')
                        };
                    },
                    makeBeaconUrl: function(data) {
                        var queryString = encodeURIComponent(JSON.stringify(data));
                        return baseUrl + '?' + queryString + ';';
                    },
                    checkUrlSize: function(schemaName, url) {
                        var message;
                        if (url.length > core.maxUrlSize) {
                            message = 'Url exceeds maximum length';
                            mw.eventLog.logFailure(schemaName, 'urlSize');
                            mw.track('eventlogging.error', mw.format('[$1] $2', schemaName, message));
                            return message;
                        }
                    },
                    sendBeacon: (/1|yes/.test(navigator.doNotTrack) || window.doNotTrack === '1' || !baseUrl) ? function() {} : navigator.sendBeacon ? function(url) {
                        try {
                            navigator.sendBeacon(url);
                        } catch (e) {}
                    } : function(url) {
                        document.createElement('img').src = url;
                    },
                    logEvent: function(schemaName, eventData) {
                        var event = core.prepare(schemaName, eventData),
                            url = core.makeBeaconUrl(event),
                            sizeError = core.checkUrlSize(schemaName, url),
                            deferred = $.Deferred();
                        if (!sizeError) {
                            core.sendBeacon(url);
                            if (debugMode) {
                                mw.track('eventlogging.debug', event);
                            }
                            deferred.resolveWith(event, [event]);
                        } else {
                            deferred.rejectWith(event, [event, sizeError]);
                        }
                        return deferred.promise();
                    },
                    logFailure: function(schemaName, errorCode) {
                        mw.track('counter.eventlogging.client_errors.' + schemaName + '.' + errorCode);
                    },
                    randomTokenMatch: function(populationSize, explicitToken) {
                        var token = explicitToken || mw.user.generateRandomSessionId(),
                            rand = parseInt(token.slice(0, 8), 16);
                        return rand % populationSize === 0;
                    },
                    sessionInSample: function(populationSize) {
                        return this.randomTokenMatch(populationSize, mw.user.sessionId());
                    },
                    inSample: function(populationSize) {
                        return this.eventInSample(populationSize);
                    },
                    eventInSample: function(populationSize) {
                        return this.randomTokenMatch(populationSize, mw.user.getPageviewToken());
                    }
                };
                module.exports = core;
            }());
        },
        "Schema.js": function(require, module) {
            (function() {
                'use strict';

                function Schema(name, samplingRate, defaults) {
                    if (!name) {
                        throw new Error('name is required');
                    }
                    this.name = name;
                    this.populationSize = samplingRate !== undefined ? (1 / samplingRate) : 1;
                    this.defaults = defaults || {};
                }
                Schema.prototype.log =
                    function(data, samplingRate) {
                        var pop = samplingRate !== undefined ? (1 / samplingRate) : this.populationSize;
                        if (mw.eventLog.inSample(pop)) {
                            mw.track('event.' + this.name, $.extend({}, this.defaults, data));
                        }
                    };
                module.exports = Schema;
            }());
        },
        "data.json": {
            "baseUrl": "https://en.wikipedia.org/beacon/event"
        }
    }
});
mw.loader.implement("ext.navigationTiming@1s7ja55", {
    "main": "ext.navigationTiming.js",
    "files": {
        "ext.navigationTiming.js": function(require, module) {
            (function() {
                'use strict';
                var visibilityEvent, visibilityChanged, mediaWikiLoadEnd, surveyDisplayed, cpuBenchmarkDone, layoutJankEmitted = 0,
                    config = require('./config.json'),
                    collectedPaintEntries = {},
                    collectedElementEntries = 0,
                    policyViolationEmitted = 0;

                function emitPaintTiming(entry, oversample, observer) {
                    var mobileMode, event = {
                        pageviewToken: mw.user.getPageviewToken(),
                        name: entry.name,
                        startTime: Math.round(entry.startTime),
                        isOversample: oversample !== !1,
                        isAnon: mw.config.get('wgUserId') === null
                    };
                    mobileMode = mw.config.get('wgMFMode');
                    if (typeof mobileMode === 'string' && mobileMode.indexOf('desktop') === -1) {
                        event.mobileMode = mobileMode;
                    }
                    if (oversample) {
                        event.oversampleReason = JSON.stringify(oversample);
                    }
                    if (window.Geo) {
                        if (typeof Geo.country === 'string') {
                            event.originCountry = Geo.country;
                        }
                    }
                    mw.eventLog.logEvent('PaintTiming', event);
                    collectedPaintEntries[entry.name] = !0;
                    if (
                        observer && collectedPaintEntries['first-paint'] && collectedPaintEntries['first-contentful-paint']) {
                        observer.disconnect();
                    }
                }

                function processExistingPaintTiming(oversample, observer) {
                    var chromeLoadTimes, paintEntries, timing = window.performance && performance.timing,
                        entry = {};
                    try {
                        paintEntries = performance.getEntriesByType('paint');
                    } catch (e) {
                        paintEntries = [];
                    }
                    if (paintEntries.length) {
                        paintEntries.forEach(function(paintEntry) {
                            if (paintEntry.name === 'first-paint' || paintEntry.name === 'first-contentful-paint') {
                                emitPaintTiming(paintEntry, oversample, observer);
                            }
                        });
                    } else if (timing && timing.msFirstPaint > timing.navigationStart) {
                        entry.name = 'first-paint';
                        entry.startTime = timing.msFirstPaint - timing.navigationStart;
                        emitPaintTiming(entry, oversample, observer);
                    } else if (window.chrome && chrome.loadTimes) {
                        chromeLoadTimes = chrome.loadTimes();
                        if (chromeLoadTimes.firstPaintTime > chromeLoadTimes.startLoadTime) {
                            entry.name = 'first-paint';
                            entry.startTime = 1000 * (chromeLoadTimes.firstPaintTime - chromeLoadTimes.startLoadTime);
                            emitPaintTiming
                                (entry, oversample, observer);
                        }
                    }
                }

                function emitAndObservePaintTiming(oversample) {
                    var observer;
                    if (window.PerformanceObserver) {
                        observer = new PerformanceObserver(function(list, observer) {
                            list.getEntries().forEach(function(entry) {
                                emitPaintTiming(entry, oversample, observer);
                            });
                        });
                        try {
                            observer.observe({
                                entryTypes: ['paint']
                            });
                        } catch (e) {}
                    }
                    processExistingPaintTiming(oversample, observer);
                }

                function emitRUMSpeedIndex() {
                    var paintEntries, resourceEntries, ptFirstPaint, event = {};
                    try {
                        paintEntries = performance.getEntriesByType('paint');
                        resourceEntries = performance.getEntriesByType('resource');
                    } catch (e) {
                        resourceEntries = [];
                        paintEntries = [];
                    }
                    if (resourceEntries.length && paintEntries.length) {
                        paintEntries.forEach(function(entry) {
                            if (entry.name === 'first-paint') {
                                ptFirstPaint = Math.round(entry.startTime);
                            }
                        });
                        if (ptFirstPaint !== undefined && ptFirstPaint > 0 && ptFirstPaint < 120000) {
                            event.pageviewToken = mw.user.getPageviewToken();
                            return mw.loader.using('ext.navigationTiming.rumSpeedIndex').then(function() {
                                var rumSpeedIndex = require(
                                    'ext.navigationTiming.rumSpeedIndex');
                                event.RSI = Math.round(rumSpeedIndex());
                                mw.eventLog.logEvent('RUMSpeedIndex', event);
                            });
                        }
                    }
                    return $.Deferred().resolve();
                }

                function emitServerTiming() {
                    var navigationEntry;
                    try {
                        navigationEntry = performance.getEntriesByType('navigation')[0];
                    } catch (e) {
                        navigationEntry = !1;
                    }
                    if (navigationEntry && navigationEntry.serverTiming) {
                        navigationEntry.serverTiming.forEach(function(serverTimingEntry) {
                            var event = {
                                pageviewToken: mw.user.getPageviewToken(),
                                description: serverTimingEntry.description,
                                name: serverTimingEntry.name,
                                duration: Math.round(1000 * serverTimingEntry.duration)
                            };
                            mw.eventLog.logEvent('ServerTiming', event);
                        });
                    }
                }

                function observeElementTiming(list, observer) {
                    var event;
                    list.getEntries().forEach(function(entry) {
                        event = {
                            pageviewToken: mw.user.getPageviewToken(),
                            identifier: entry.identifier,
                            name: entry.name,
                            url: entry.url,
                            loadTime: Math.round(entry.loadTime),
                            startTime: Math.round(entry.startTime),
                            renderTime: Math.round(entry.renderTime),
                            bottom: entry.intersectionRect.bottom,
                            height: entry.intersectionRect.height,
                            left: entry.intersectionRect.left,
                            right: entry.intersectionRect.right,
                            top: entry.intersectionRect.top,
                            width: entry.intersectionRect.width,
                            x: entry.intersectionRect.x,
                            y: entry.intersectionRect.y
                        };
                        mw.eventLog.logEvent('ElementTiming', event);
                        collectedElementEntries++;
                        if (collectedElementEntries > 20) {
                            observer.disconnect();
                        }
                    });
                }

                function setupElementTimingObserver() {
                    var observer;
                    if (!window.PerformanceObserver) {
                        return;
                    }
                    observer = new PerformanceObserver(observeElementTiming);
                    try {
                        observer.observe({
                            type: 'element',
                            buffered: !0
                        });
                    } catch (e) {}
                }

                function getNavTimingLevel2() {
                    var navigationEntry, res = {};
                    try {
                        navigationEntry = performance.getEntriesByType('navigation')[0];
                    } catch (e) {
                        navigationEntry = !1;
                    }
                    if (navigationEntry) {
                        res.transferSize = navigationEntry.transferSize;
                    }
                    return res;
                }

                function getNavTimingLevel1() {
                    var timing = window.performance && performance.timing,
                        navStart = timing && timing.navigationStart,
                        timingData = {};
                    if (!timing) {
                        return timingData;
                    }
                    $.each(['connectEnd', 'connectStart', 'domComplete', 'domInteractive', 'fetchStart', 'loadEventEnd', 'loadEventStart', 'requestStart', 'responseEnd', 'responseStart', 'secureConnectionStart'], function(i, marker) {
                        var value = timing[marker];
                        if (typeof value === 'number' && value >= 0) {
                            if (marker === 'secureConnectionStart' && value === 0) {
                                timingData[marker] = 0;
                            } else {
                                timingData[marker] = value - navStart;
                            }
                        }
                    });
                    timingData.dnsLookup = timing.domainLookupEnd - timing.domainLookupStart;
                    if (timing.redirectStart) {
                        timingData.redirecting = timing.redirectEnd - timing.redirectStart;
                    } else {
                        timingData.redirecting = 0;
                    }
                    if (timing.unloadEventStart) {
                        timingData.unload = timing.unloadEventEnd - timing.unloadEventStart;
                    } else {
                        timingData.unload = 0;
                    }
                    timingData.gaps = timing.domainLookupStart - timing.fetchStart;
                    timingData.gaps += timing.connectStart - timing.domainLookupEnd;
                    timingData.gaps += timing.requestStart - timing.connectEnd;
                    timingData.gaps += timing.loadEventStart - timing.domComplete;
                    return timingData;
                }

                function emitCpuBenchmark() {
                    var blob, worker, work, deferred = $.Deferred();
                    if (cpuBenchmarkDone || !
                        window.Blob || !window.URL || !window.URL.createObjectURL || !window.Worker || !window.performance) {
                        return deferred.resolve();
                    }
                    cpuBenchmarkDone = !0;

                    function onMessage() {
                        var i, startTime, amount = 100000000;
                        if (!self.performance) {
                            postMessage(false);
                            return;
                        }
                        startTime = performance.now();
                        for (i = amount; i > 0; i--) {}
                        postMessage(Math.round(performance.now() - startTime));
                    }
                    work = 'onmessage = ' + String(onMessage);
                    blob = new Blob([work], {
                        type: 'application/javascript'
                    });
                    worker = new Worker(URL.createObjectURL(blob));
                    deferred.then(function(result) {
                        var event;
                        if (!result) {
                            return;
                        }
                        event = {
                            pageviewToken: mw.user.getPageviewToken(),
                            score: result
                        };
                        mw.eventLog.logEvent('CpuBenchmark', event);
                    });
                    worker.onmessage = function(e) {
                        deferred.resolve(e.data);
                        worker.terminate();
                    };
                    worker.postMessage(false);
                    return deferred;
                }

                function showPerformanceSurvey() {
                    var isMainPage = mw.config.get('wgIsMainPage'),
                        isArticle = mw.config.get('wgNamespaceNumber') === 0,
                        isViewing = mw.config.get('wgAction') === 'view',
                        isVE = mw.util.getParamValue('veaction'),
                        exists = mw.config
                        .get('wgCurRevisionId') > 0,
                        surveyName = config.surveyName,
                        loggedOutSamplingFactor = config.surveySamplingFactor || 0,
                        loggedInSamplingFactor = config.surveyAuthenticatedSamplingFactor || 0,
                        isInSurveySample;
                    if (isMainPage || !isArticle || !isViewing || isVE || !exists || !surveyName || surveyDisplayed) {
                        return;
                    }
                    surveyDisplayed = !0;
                    if (mw.config.get('wgUserId') !== null) {
                        isInSurveySample = mw.eventLog.randomTokenMatch(loggedInSamplingFactor || loggedOutSamplingFactor);
                    } else {
                        isInSurveySample = mw.eventLog.randomTokenMatch(loggedOutSamplingFactor);
                    }
                    if (!isInSurveySample) {
                        return;
                    }
                    mw.loader.using('ext.quicksurveys.init').then(function() {
                        mw.extQuickSurveys.showSurvey(surveyName);
                    });
                    emitCpuBenchmark();
                }

                function makeResourceTimingEvent(resource, label) {
                    var event, key, value, fields = ['startTime', 'workerStart', 'redirectStart', 'redirectEnd', 'fetchStart', 'domainLookupStart', 'domainLookupEnd', 'connectStart', 'secureConnectionStart', 'connectEnd', 'requestStart', 'responseStart', 'responseEnd', 'encodedBodySize', 'decodedBodySize',
                        'initiatorType', 'duration', 'name', 'nextHopProtocol', 'transferSize'
                    ];
                    event = {
                        pageviewToken: mw.user.getPageviewToken(),
                        label: label
                    };
                    for (key in resource) {
                        value = resource[key];
                        if (fields.indexOf(key) !== -1) {
                            if (typeof value === 'number') {
                                event[key] = Math.round(value);
                            } else {
                                event[key] = value;
                            }
                        }
                    }
                    return event;
                }

                function emitTopImageResourceTiming() {
                    var img, resources, srcset, urls = [];
                    if (!window.performance || !performance.getEntriesByType) {
                        return;
                    }
                    resources = performance.getEntriesByType('resource');
                    img = $('.mw-parser-output img').filter(function(idx, e) {
                        return e.width * e.height > 100 * 100;
                    })[0];
                    if (!resources || !img) {
                        return;
                    }
                    urls.push(img.src);
                    if (img.srcset) {
                        srcset = img.srcset;
                        srcset.split(',').forEach(function(src) {
                            var url = src.trim().split(' ')[0];
                            if (url) {
                                urls.push(url);
                            }
                        });
                    }
                    resources.forEach(function(resource) {
                        if (resource.initiatorType !== 'img') {
                            return;
                        }
                        urls.forEach(function(url) {
                            var resourceUri, uri;
                            resourceUri = resource.name.substr(resource.name.indexOf('//'));
                            uri = url.substr(url.indexOf('//'));
                            if (resourceUri === uri) {
                                mw.eventLog.logEvent('ResourceTiming', makeResourceTimingEvent(resource, 'top-image'));
                            }
                        });
                    });
                }

                function emitCentralNoticeTiming(existingObserver) {
                    var event, mark, marks, observer;
                    if (!window.performance || !performance.getEntriesByName) {
                        return;
                    }
                    marks = performance.getEntriesByName('mwCentralNoticeBanner', 'mark');
                    if (!marks || !marks.length) {
                        if (!window.PerformanceObserver) {
                            return;
                        }
                        if (existingObserver) {
                            return;
                        }
                        observer = new PerformanceObserver(function() {
                            emitCentralNoticeTiming(observer);
                        });
                        observer.observe({
                            entryTypes: ['mark']
                        });
                        return;
                    } else {
                        if (existingObserver) {
                            existingObserver.disconnect();
                        }
                        mark = marks[0];
                        event = {
                            pageviewToken: mw.user.getPageviewToken(),
                            time: Math.round(mark.startTime)
                        };
                        mw.eventLog.logEvent('CentralNoticeTiming', event);
                    }
                }

                function isRegularNavigation() {
                    var TYPE_NAVIGATE = 0;
                    return window.performance && performance.timing && performance.navigation && performance.navigation.type === TYPE_NAVIGATE;
                }

                function emitNavigationTimingWithOversample(oversample) {
                    var mobileMode, veaction, event = {};
                    showPerformanceSurvey();
                    event.mediaWikiVersion = mw.config.get('wgVersion');
                    event.phpEngine = mw.config.get('wgPoweredByHHVM') ? 'hhvm' : 'zend';
                    event.isAnon = mw.config.get('wgUserId') === null;
                    if (mw.config.get('wgCanonicalSpecialPageName')) {
                        event.mwSpecialPageName = mw.config.get('wgCanonicalSpecialPageName');
                    } else {
                        event.namespaceId = mw.config.get('wgNamespaceNumber');
                        event.revId = mw.config.get('wgCurRevisionId');
                        event.action = mw.config.get('wgAction');
                    }
                    veaction = mw.util.getParamValue('veaction');
                    if (veaction !== null) {
                        event.veaction = veaction;
                    }
                    mobileMode = mw.config.get('wgMFMode');
                    if (typeof mobileMode === 'string' && mobileMode.indexOf('desktop') === -1) {
                        event.mobileMode = mobileMode;
                    }
                    if (mediaWikiLoadEnd) {
                        event.mediaWikiLoadEnd = mediaWikiLoadEnd;
                    }
                    if (window.Geo) {
                        if (typeof Geo.country === 'string') {
                            event.originCountry = Geo.country;
                        }
                    }
                    event.pageviewToken = mw.user.getPageviewToken();
                    event.isOversample = oversample !== !1;
                    if (oversample) {
                        event.oversampleReason = JSON.stringify(oversample);
                    }
                    if (navigator.connection) {
                        if (typeof navigator.connection.effectiveType === 'string') {
                            event.netinfoEffectiveConnectionType = navigator.connection.effectiveType;
                        }
                        if (typeof navigator.connection.type === 'string') {
                            event.netinfoConnectionType = navigator.connection.type;
                        }
                        if (navigator.connection.rtt !== undefined) {
                            event.netinfoRtt = navigator.connection.rtt;
                        }
                        if (navigator.connection.downlink !== undefined) {
                            event.netinfoDownlink = navigator.connection.downlink;
                        }
                    }
                    if (navigator.deviceMemory) {
                        event.deviceMemory = navigator.deviceMemory;
                    }
                    $.extend(event, getNavTimingLevel1(), getNavTimingLevel2());
                    mw.eventLog.logEvent('NavigationTiming', event);
                    emitAndObservePaintTiming(oversample);
                }

                function emitNavigationTiming() {
                    emitNavigationTimingWithOversample(false);
                }

                function emitSaveTiming() {
                    var timing = window.performance && performance.timing,
                        responseStart;
                    if (!mw.config.get('wgPostEdit') || !timing) {
                        return;
                    }
                    responseStart = timing.responseStart - timing.navigationStart;
                    if (!responseStart) {
                        return;
                    }
                    mw.eventLog.logEvent('SaveTiming', {
                        mediaWikiVersion: mw.config.get('wgVersion'),
                        saveTiming: responseStart
                    });
                }

                function setMwLoadEnd() {
                    if (window.performance && performance.now) {
                        mediaWikiLoadEnd = Math.round(performance.now());
                    }
                }

                function onMwLoadEnd() {
                    var deferred = $.Deferred(),
                        modules = window.RLPAGEMODULES;
                    if (!modules) {
                        mw.log.warn('Fallback RLPAGEMODULES');
                        modules = mw.loader.getModuleNames().filter(function(module) {
                            return mw.loader.getState(module) === 'loading';
                        });
                    }
                    mw.loader.using(modules).done(function() {
                        setMwLoadEnd();
                        deferred.resolve();
                    }).fail(function() {
                        var i, count = modules.length;

                        function decrement() {
                            count--;
                            if (count === 0) {
                                setMwLoadEnd();
                                deferred.resolve();
                            }
                        }
                        for (i = 0; i < modules.length; i++) {
                            mw.loader.using(modules[i]).always(decrement);
                        }
                    });
                    return deferred;
                }

                function onLoadComplete(callback) {
                    onMwLoadEnd().then(function() {
                        if (document.readyState === 'complete') {
                            setTimeout(callback);
                        } else {
                            window.addEventListener('load', function() {
                                setTimeout(callback);
                            });
                        }
                    });
                }

                function testGeoOversamples(geos) {
                    var myGeo, geoOversamples = [];
                    if (!window.Geo) {
                        return geoOversamples;
                    }
                    myGeo = Geo.country || Geo.
                    country_code;
                    if (myGeo in geos) {
                        if (mw.eventLog.randomTokenMatch(geos[myGeo])) {
                            geoOversamples.push(myGeo);
                        }
                    }
                    return geoOversamples;
                }

                function testUAOversamples(userAgents) {
                    var userAgent, userAgentSamples = [];
                    if (!navigator.userAgent) {
                        return userAgentSamples;
                    }
                    for (userAgent in userAgents) {
                        if (navigator.userAgent.indexOf(userAgent) >= 0) {
                            if (mw.eventLog.randomTokenMatch(userAgents[userAgent])) {
                                userAgentSamples.push(userAgent);
                            }
                        }
                    }
                    return userAgentSamples;
                }

                function testPageNameOversamples(pageNames) {
                    var pageNamesSamples = [],
                        pageName = mw.config.get('wgPageName');
                    if (pageName in pageNames) {
                        if (mw.eventLog.randomTokenMatch(pageNames[pageName])) {
                            pageNamesSamples.push(pageName);
                        }
                    }
                    return pageNamesSamples;
                }

                function setVisibilityChanged() {
                    visibilityChanged = !0;
                }

                function emitLayoutJank(entries, observer) {
                    entries.forEach(function(entry) {
                        var event = {
                            pageviewToken: mw.user.getPageviewToken(),
                            fraction: entry.fraction
                        };
                        mw.eventLog.logEvent('LayoutJank', event);
                        layoutJankEmitted++;
                    });
                    if (layoutJankEmitted > 20) {
                        observer.
                        disconnect();
                    }
                }

                function emitAndObserveLayoutJank() {
                    var observer, layoutJankEntries;
                    if (!window.PerformanceObserver || !window.performance) {
                        return;
                    }
                    observer = new PerformanceObserver(function(list, observer) {
                        emitLayoutJank(list.getEntries(), observer);
                    });
                    try {
                        observer.observe({
                            entryTypes: ['layoutShift']
                        });
                    } catch (e) {}
                    try {
                        layoutJankEntries = performance.getEntriesByType('layoutShift');
                    } catch (e) {
                        layoutJankEntries = [];
                    }
                    if (layoutJankEntries.length) {
                        emitLayoutJank(layoutJankEntries, observer);
                    }
                }

                function emitFeaturePolicyViolation(reports, observer) {
                    reports.forEach(function(report) {
                        var event = {
                            pageviewToken: mw.user.getPageviewToken(),
                            url: report.url,
                            featureId: report.body.featureId
                        };
                        if (report.body.sourceFile) {
                            event.sourceFile = report.body.sourceFile;
                        }
                        if (report.body.lineNumber) {
                            event.lineNumber = report.body.lineNumber;
                        }
                        if (report.body.columnNumber) {
                            event.columnNumber = report.body.columnNumber;
                        }
                        mw.eventLog.logEvent('FeaturePolicyViolation', event);
                        policyViolationEmitted++;
                    });
                    if (policyViolationEmitted > 20) {
                        observer.
                        disconnect();
                    }
                }

                function setupFeaturePolicyViolationObserver() {
                    var observer;
                    if (!window.ReportingObserver) {
                        return;
                    }
                    observer = new ReportingObserver(emitFeaturePolicyViolation, {
                        buffered: !0,
                        types: ['feature-policy-violation']
                    });
                    observer.observe();
                }

                function loadCallback() {
                    var oversamples, oversampleReasons, isInSample;
                    mw.hook('postEdit').add(emitSaveTiming);
                    $(document).off(visibilityEvent, setVisibilityChanged);
                    if (visibilityChanged) {
                        return;
                    }
                    oversamples = config.oversampleFactor;
                    oversampleReasons = [];
                    if (oversamples) {
                        if ('geo' in oversamples) {
                            testGeoOversamples(oversamples.geo).forEach(function(key) {
                                oversampleReasons.push('geo:' + key);
                            });
                        }
                        if ('userAgent' in oversamples) {
                            testUAOversamples(oversamples.userAgent).forEach(function(key) {
                                oversampleReasons.push('ua:' + key);
                            });
                        }
                        if ('pageName' in oversamples) {
                            testPageNameOversamples(oversamples.pageName).forEach(function(key) {
                                oversampleReasons.push('pagename:' + key);
                            });
                        }
                        if ('wiki' in oversamples) {
                            if (mw.eventLog.randomTokenMatch(oversamples.wiki)) {
                                oversampleReasons.push('wiki:' + mw.config.get('wgDBname'));
                            }
                        }
                    }
                    isInSample = mw.eventLog.inSample(config.samplingFactor || 0);
                    if (!oversampleReasons.length && !isInSample) {
                        return;
                    }
                    if (isRegularNavigation()) {
                        emitCentralNoticeTiming();
                        emitTopImageResourceTiming();
                        emitServerTiming();
                        emitRUMSpeedIndex();
                        setupElementTimingObserver();
                        setupFeaturePolicyViolationObserver();
                        if (mw.eventLog.randomTokenMatch(config.cpuBenchmarkSamplingFactor || 0)) {
                            emitCpuBenchmark();
                        }
                        if (isInSample) {
                            emitNavigationTiming();
                        }
                        if (oversampleReasons.length) {
                            emitNavigationTimingWithOversample(oversampleReasons);
                        }
                        emitAndObserveLayoutJank();
                    }
                }

                function main() {
                    if (typeof document.hidden !== 'undefined') {
                        visibilityChanged = document.hidden;
                        visibilityEvent = 'visibilitychange';
                    } else if (typeof document.mozHidden !== 'undefined') {
                        visibilityChanged = document.mozHidden;
                        visibilityEvent = 'mozvisibilitychange';
                    } else if (typeof document.msHidden !== 'undefined') {
                        visibilityChanged = document.msHidden;
                        visibilityEvent = 'msvisibilitychange';
                    } else if (typeof document.webkitHidden !==
                        'undefined') {
                        visibilityChanged = document.webkitHidden;
                        visibilityEvent = 'webkitvisibilitychange';
                    } else {
                        visibilityChanged = !1;
                    }
                    if (!visibilityChanged) {
                        $(document).one(visibilityEvent, setVisibilityChanged);
                    }
                    onLoadComplete(loadCallback);
                }
                main();
                if (typeof QUnit !== 'undefined') {
                    module.exports = {
                        emitNavTiming: emitNavigationTiming,
                        emitNavigationTimingWithOversample: emitNavigationTimingWithOversample,
                        makeResourceTimingEvent: makeResourceTimingEvent,
                        emitServerTiming: emitServerTiming,
                        emitTopImageResourceTiming: emitTopImageResourceTiming,
                        emitCentralNoticeTiming: emitCentralNoticeTiming,
                        testGeoOversamples: testGeoOversamples,
                        testUAOversamples: testUAOversamples,
                        testPageNameOversamples: testPageNameOversamples,
                        loadCallback: loadCallback,
                        onMwLoadEnd: onMwLoadEnd,
                        emitCpuBenchmark: emitCpuBenchmark,
                        emitRUMSpeedIndex: emitRUMSpeedIndex,
                        emitFeaturePolicyViolation: emitFeaturePolicyViolation,
                        reinit: function() {
                            setMwLoadEnd();
                            visibilityChanged = !1;
                            policyViolationEmitted = 0;
                        }
                    };
                    config = {
                        samplingFactor: 1,
                        oversampleFactor: {
                            geo: {
                                XX: 1
                            },
                            wiki: 1
                        }
                    };
                }
            }());
        },
        "config.json": {
            "samplingFactor": 1000,
            "surveySamplingFactor": 0,
            "surveyAuthenticatedSamplingFactor": 0,
            "surveyName": !1,
            "cpuBenchmarkSamplingFactor": 2,
            "oversampleFactor": !1
        }
    }
});
mw.loader.implement("ext.wikimediaEvents@0905391", function($, jQuery, require, module) {
    (function() {
        mw.trackSubscribe('wikimedia.event.', function(topic, event) {
            mw.track(topic.replace(/^wikimedia\./, ''), event);
        });
    }());
    (function() {
        var timer = null,
            queue = [],
            batchSize = 50,
            baseUrl = mw.config.get('wgWMEStatsdBaseUri'),
            sendBeacon = navigator.sendBeacon ? function(url) {
                try {
                    navigator.sendBeacon(url);
                } catch (e) {}
            } : function(url) {
                (new Image()).src = url;
            },
            isDntEnabled = /1|yes/.test(navigator.doNotTrack) || window.doNotTrack === '1';
        if (!baseUrl || isDntEnabled) {
            return;
        }

        function dispatch() {
            var i, values;
            timer = null;
            while (queue.length) {
                values = queue.splice(0, batchSize);
                for (i = 0; i < values.length; i++) {
                    values[i] = values[i].key + '=' + values[i].value;
                }
                sendBeacon(baseUrl + '?' + values.join('&'));
            }
        }

        function schedule() {
            if (!timer) {
                timer = setTimeout(dispatch, 2000);
            } else if (queue.length < batchSize) {
                clearTimeout(timer);
                timer = setTimeout(dispatch, 2000);
            }
        }

        function unscheduleAndDispatch() {
            if (timer) {
                clearTimeout(timer);
            }
            dispatch();
        }
        window.
        addEventListener('pagehide', unscheduleAndDispatch);
        document.addEventListener('visibilitychange', function() {
            if (document.hidden) {
                unscheduleAndDispatch();
            }
        });
        mw.trackSubscribe('timing.', function(topic, time) {
            queue.push({
                key: topic.substring('timing.'.length),
                value: Math.round(time) + 'ms'
            });
            schedule();
        });
        mw.trackSubscribe('counter.', function(topic, count) {
            count = Math.round(count);
            if (isNaN(count)) {
                count = 1;
            }
            queue.push({
                key: topic.substring('counter.'.length),
                value: count + 'c'
            });
            schedule();
        });
        mw.trackSubscribe('gauge.', function(topic, value) {
            value = Math.round(value);
            if (isNaN(value)) {
                return;
            }
            queue.push({
                key: topic.substring('gauge.'.length),
                value: value + 'g'
            });
            schedule();
        });
    }());
    (function(user, mwExperiments) {
        function logEvent(action) {
            var skin = mw.config.get('skin');
            if (skin !== 'vector' && skin !== 'minerva') {
                skin = 'other';
            }
            mw.track('event.Print', {
                sessionToken: user.sessionId(),
                isAnon: user.isAnon(),
                pageTitle: mw.config.get('wgPageName'),
                namespaceId: mw.config.get('wgNamespaceNumber'),
                skin: skin,
                action: action
            });
        }

        function
        isInSample(samplingRate) {
            var bucket = mwExperiments.getBucket({
                name: 'WMEPrint',
                enabled: !0,
                buckets: {
                    control: 1 - samplingRate,
                    A: samplingRate
                }
            }, user.sessionId());
            return bucket === 'A';
        }

        function setupClickLogging() {
            var EVENT_NAME = 'clickPrintableVersion',
                $link = $('a', '#t-print');

            function log() {
                $link.off('click', log);
                logEvent(EVENT_NAME);
            }
            $link.on('click', log);
        }

        function setupMinervaLogging() {
            mw.trackSubscribe('minerva.downloadAsPDF', function(topic, data) {
                switch (data.action) {
                    case 'fetchImages':
                        logEvent('clickPrintableVersion');
                        break;
                    case 'buttonVisible':
                        logEvent('shownPrintButton');
                        break;
                    default:
                }
            });
        }

        function setupPrintLogging() {
            var mediaQueryList, EVENT_NAME = 'onBeforePrint';

            function logMatchMedia(event) {
                if (event.matches) {
                    mediaQueryList.removeListener(logMatchMedia);
                    logEvent(EVENT_NAME);
                }
            }

            function logBeforePrint() {
                $(window).off('beforeprint', logBeforePrint);
                logEvent(EVENT_NAME);
            }
            if ('matchMedia' in window && !('onbeforeprint' in window)) {
                mediaQueryList = window.matchMedia('print');
                mediaQueryList.addListener(
                    logMatchMedia);
            } else {
                $(window).on('beforeprint', logBeforePrint);
            }
        }
        if (mw.config.get('wgWMEPrintEnabled') && isInSample(mw.config.get('wgWMEPrintSamplingRate', 0))) {
            $(function() {
                setupClickLogging();
                setupPrintLogging();
                setupMinervaLogging();
            });
        }
    }(mw.user, mw.experiments));
    (function(user, mwExperiments, config, loader) {
        var pausedAt, sessionId, EVENT, MINERVA_ENTRY_POINT = 'skins.minerva.scripts',
            skin = config.get('skin'),
            trackingIsEnabled, eventData = {},
            msPaused = 0,
            perf = window.performance,
            SCHEMA_NAME = 'ReadingDepth',
            dependencies = [],
            DEFAULT_SAMPLE_GROUP = 'default_sample';

        function supportsBeacon() {
            return !!navigator.sendBeacon;
        }

        function supportsNavigationTiming() {
            return !!(perf && perf.timing && perf.timing.navigationStart);
        }

        function checkCapability() {
            return mw.config.get('wgWMEReadingDepthEnabled') && supportsNavigationTiming() && supportsBeacon();
        }
        if (!checkCapability()) {
            return;
        }
        sessionId = user.sessionId();

        function isInSample(samplingRate) {
            var bucket = mwExperiments.getBucket({
                name: 'WME.' + SCHEMA_NAME,
                enabled: !0,
                buckets: {
                    control: 1 - samplingRate,
                    A: samplingRate
                }
            }, sessionId);
            return bucket === 'A';
        }

        function getFirstPaintTime(perf) {
            var chromeLoadTimes, timing = perf.timing;
            if (timing.msFirstPaint > timing.navigationStart) {
                return timing.msFirstPaint;
            } else if (window.chrome && chrome.loadTimes) {
                chromeLoadTimes = chrome.loadTimes();
                if (chromeLoadTimes.firstPaintTime > chromeLoadTimes.startLoadTime) {
                    return Math.round(1000 * chromeLoadTimes.firstPaintTime);
                }
            }
        }

        function pause(at) {
            if (!pausedAt) {
                pausedAt = at || mw.now();
            }
        }

        function resume() {
            if (pausedAt) {
                msPaused += mw.now() - pausedAt;
                pausedAt = null;
            }
        }

        function logEvent(action) {
            var now, timing = perf.timing,
                domInteractive = timing.domInteractive,
                navigationStart = timing.navigationStart,
                firstPaint = getFirstPaintTime(perf),
                delta, adjustedEpoch, hiddenFor, data = $.extend({}, EVENT, {
                    action: action,
                    domInteractiveTime: domInteractive - navigationStart,
                    firstPaintTime: firstPaint ? firstPaint - navigationStart : undefined
                }, eventData);
            if (action === 'pageUnloaded') {
                now = mw.now();
                delta = firstPaint ? (firstPaint - domInteractive) : 0;
                adjustedEpoch = domInteractive + delta;
                hiddenFor = Math.max(0, msPaused - delta);
                data.totalLength = Math.round(now - adjustedEpoch);
                data.visibleLength = Math.round(data.totalLength - hiddenFor);
            }
            mw.track('event.' + SCHEMA_NAME, data);
        }

        function onBeforeUnload() {
            logEvent('pageUnloaded');
        }

        function onLoad() {
            if (document.hidden) {
                pause(perf.timing.domInteractive);
            }
            $(document).on('visibilitychange', function() {
                if (document.hidden) {
                    pause();
                } else {
                    resume();
                }
            });
            logEvent('pageLoaded');
        }

        function enableTracking() {
            if (!trackingIsEnabled) {
                trackingIsEnabled = !0;
                EVENT = {
                    pageTitle: mw.config.get('wgTitle'),
                    namespaceId: mw.config.get('wgNamespaceNumber'),
                    skin: mw.config.get('skin'),
                    isAnon: user.isAnon(),
                    pageToken: user.getPageviewToken(),
                    sessionToken: sessionId
                };
                $(window).on('beforeunload', onBeforeUnload);
                onLoad();
            }
        }

        function onExternalBucketEnabled(topic, boolProperty) {
            eventData[boolProperty] = !0;
        }
        if (skin === 'minerva' && loader.getState(MINERVA_ENTRY_POINT) !== null) {
            dependencies.push(MINERVA_ENTRY_POINT);
        }
        loader.using(dependencies).then(
            function() {
                mw.trackSubscribe('wikimedia.ReadingDepthSchema.enable', onExternalBucketEnabled);
                if (isInSample(mw.config.get('wgWMEReadingDepthSamplingRate', 0))) {
                    eventData[DEFAULT_SAMPLE_GROUP] = !0;
                }
                if (Object.keys(eventData).length) {
                    enableTracking();
                }
            });
    }(mw.user, mw.experiments, mw.config, mw.loader));
    (function(mwUser, mwExperiments, mwEventLog) {
        var POPULATION_SIZE = mw.config.get('wgWMECitationUsagePopulationSize', 0),
            PL_POPULATION_SIZE = mw.config.get('wgWMECitationUsagePageLoadPopulationSize', 0),
            HOVER_TIMEOUT = 1000,
            SCHEMA_NAME = 'CitationUsage',
            PL_SCHEMA_NAME = 'CitationUsagePageLoad',
            REFERRER_MAX_LENGTH = 100,
            getBaseData, getLinkOccurence, getExtLinkPosition;
        getBaseData = (function() {
            var baseData;
            return function() {
                if (!baseData) {
                    baseData = {
                        dom_interactive_time: window.performance.timing.domInteractive,
                        revision_id: mw.config.get('wgRevisionId'),
                        page_id: mw.config.get('wgArticleId'),
                        namespace_id: mw.config.get('wgNamespaceNumber'),
                        page_token: mwUser.getPageviewToken(),
                        session_token: mwUser.sessionId(),
                        referrer: document.referrer.slice(0, REFERRER_MAX_LENGTH),
                        skin: mw.config.get('skin'),
                        mode: mw.config.get('wgMFMode') ? 'mobile' : 'desktop'
                    };
                }
                return baseData;
            };
        }());

        function logEvent(schemaName, data) {
            var baseData = getBaseData();
            mwEventLog.logEvent(schemaName, $.extend({}, baseData, data, {
                event_offset_time: Math.round(mw.now() - baseData.dom_interactive_time)
            }));
        }
        getLinkOccurence = (function() {
            var links;
            return function(href) {
                var $links;
                if (!links) {
                    $links = $('#content a[href]');
                    links = {};
                    $links.each(function(i, link) {
                        if (link.href in links) {
                            links[link.href] += 1;
                        } else {
                            links[link.href] = 1;
                        }
                    });
                }
                return links[href];
            };
        }());
        getExtLinkPosition = (function() {
            var links;
            return function(href) {
                var $links;
                if (!links) {
                    $links = $('#content a.external');
                    links = {};
                    $links.each(function(i, link) {
                        links[link.href] = i + 1;
                    });
                }
                return links[href];
            };
        }());

        function normalizeSpaces(text) {
            return text.trim().replace(/\s+/g, ' ');
        }

        function getSectionId($link) {
            var $headings = $link.parents().prevAll('h2:first, h3:first, h4:first, h5:first, h6:first'),
                $headline;
            if (
                $headings.length) {
                $headline = $headings.eq(0).find('.mw-headline');
                if ($headline.length) {
                    return $headline.attr('id');
                }
            }
            return undefined;
        }

        function isInInfobox($link) {
            return $link.parentsUntil('#content', '.infobox').length > 0;
        }

        function getLinkStats($link) {
            var href = $link.prop('href');
            return {
                section_id: getSectionId($link),
                in_infobox: isInInfobox($link),
                link_text: normalizeSpaces($link.text()),
                link_url: href,
                link_occurrence: getLinkOccurence(href)
            };
        }

        function getExtLinkStats($link) {
            var $refText = $link.closest('.reference-text'),
                $linkLi = $link.parents('.references li'),
                data = getLinkStats($link);
            if ($refText.length) {
                data.citation_in_text_refs = $refText.prevAll('.mw-cite-backlink').find('a').length;
            }
            data.ext_position = getExtLinkPosition($link.prop('href'));
            data.freely_accessible = $link.parent().hasClass('cs1-lock-free');
            if ($linkLi.length) {
                data.footnote_number = $linkLi.index() + 1;
            }
            return data;
        }

        function logPageLoad() {
            logEvent(PL_SCHEMA_NAME, {
                action: 'pageLoad'
            });
        }

        function setupExtLogging() {
            $('#content').on('click', 'a.external', function() {
                var data = getExtLinkStats($(this));
                data.action = 'extClick';
                logEvent(SCHEMA_NAME, data);
            });
        }

        function setupUpLogging() {
            $('.references').on('click', '.mw-cite-backlink a', function() {
                var data = getLinkStats($(this));
                data.action = 'upClick';
                logEvent(SCHEMA_NAME, data);
            });
        }

        function setupFnLogging() {
            var hoverTimeout;

            function logHover(link) {
                var data = getLinkStats($(link));
                data.action = 'fnHover';
                logEvent(SCHEMA_NAME, data);
            }
            $('#content').on('mouseover', 'sup.reference a', function() {
                if (!hoverTimeout) {
                    hoverTimeout = setTimeout(logHover, HOVER_TIMEOUT, this);
                }
            }).on('mouseout', 'sup.reference a', function() {
                clearTimeout(hoverTimeout);
                hoverTimeout = null;
            }).on('click', 'sup.reference a', function() {
                var data = getLinkStats($(this));
                clearTimeout(hoverTimeout);
                hoverTimeout = null;
                data.action = 'fnClick';
                logEvent(SCHEMA_NAME, data);
            });
        }

        function shouldLog(populationSize) {
            return (window.performance && window.performance.timing && window.performance.timing.domInteractive && !mw.config.get('wgIsMainPage') && mw.config.get('wgNamespaceNumber') === 0 && mw.config.get('wgAction') === 'view' && mwUser.isAnon() && mwEventLog.randomTokenMatch(populationSize, mwUser.sessionId()));
        }
        $(function() {
            if (shouldLog(PL_POPULATION_SIZE)) {
                mw.loader.using('ext.eventLogging', function() {
                    mw.requestIdleCallback(logPageLoad);
                });
            }
            if (shouldLog(POPULATION_SIZE)) {
                mw.loader.using('ext.eventLogging', function() {
                    setupExtLogging();
                    setupUpLogging();
                    setupFnLogging();
                });
            }
        });
    }(mw.user, mw.experiments, mw.eventLog));
    (function() {
        var enableLoggedIn = !0;
        if (!enableLoggedIn && !mw.user.isAnon()) {
            return;
        }

        function getSeed() {
            var id = $.cookie('mwPhp7Seed');
            if (!id || isNaN(parseInt(id, 16))) {
                id = mw.user.generateRandomSessionId().slice(0, 3);
                $.cookie('mwPhp7Seed', id, {
                    expires: 60,
                    path: '/'
                });
            }
            return id;
        }

        function ensureCookie() {
            var sampleRate = mw.config.get('wgWMEPhp7SamplingRate', 0),
                hasCookie = /PHP_ENGINE=php7/.test(document.cookie),
                inSample = mw.eventLog.randomTokenMatch(sampleRate, getSeed());
            if (inSample && !hasCookie) {
                $.cookie('PHP_ENGINE', 'php7', {
                    expires: 7,
                    path: '/'
                });
            } else
            if (!inSample && hasCookie) {
                $.removeCookie('PHP_ENGINE', {
                    path: '/'
                });
            }
        }
        mw.requestIdleCallback(ensureCookie);
    }());
    (function(config, user, mwExperiments, Schema) {
        var schemaMobileWebUIActionsTracking;

        function getModes() {
            var mode = config.get('wgMFMode', 'desktop'),
                modes;
            modes = [mode];
            if (mode !== 'desktop' && config.get('wgMFAmc', false)) {
                modes.push('amc');
            }
            return modes;
        }

        function getEditCountBucket(editCount) {
            if (editCount >= 1000) {
                return '1000+ edits';
            }
            if (editCount >= 100) {
                return '100-999 edits';
            }
            if (editCount >= 5) {
                return '5-99 edits';
            }
            if (editCount >= 1) {
                return '1-4 edits';
            }
            return '0 edits';
        }
        schemaMobileWebUIActionsTracking = new Schema('MobileWebUIActionsTracking', config.get('wgWMEMobileWebUIActionsTracking', 0), {
            isAnon: user.isAnon(),
            editCountBucket: getEditCountBucket(config.get('wgUserEditCount')),
            modes: getModes().join(',')
        });
        $('body').on('click', function(event) {
            var element = event.target,
                name = element.getAttribute('data-event-name');
            if (name) {
                schemaMobileWebUIActionsTracking.log({
                    action: 'click',
                    name: name,
                    token: user.
                    sessionId(),
                    destination: element.getAttribute('href')
                });
            }
        });
    }(mw.config, mw.user, mw.experiments, mw.eventLog.Schema));
});
mw.loader.implement("ext.quicksurveys.init@0whcbmy", function($, jQuery, require, module) {
    (function() {
        var isMainPage = mw.config.get('wgIsMainPage'),
            isArticle = mw.config.get('wgIsArticle'),
            isDntEnabled = window.doNotTrack === '1' || (navigator.doNotTrack === '1' || navigator.doNotTrack === 'yes' || navigator.msDoNotTrack === '1'),
            forcedSurvey = mw.util.getParamValue('quicksurvey');
        if (isMainPage || !isArticle || isDntEnabled) {
            return;
        }
        if (mw.config.get('skin') === 'minerva') {
            mw.trackSubscribe('mobile.betaoptin', function(topic, data) {
                if (data.isPanelShown === false) {
                    mw.extQuickSurveys.showSurvey(forcedSurvey);
                }
            });
        } else {
            mw.extQuickSurveys.showSurvey(forcedSurvey);
        }
    }());
});
mw.loader.implement("ext.quicksurveys.lib@0lq9sbt", function($, jQuery, require, module) {
    (function() {
        var survey, availableSurveys = [],
            $window = $(window),
            hasOwn = Object.hasOwnProperty,
            surveyImpressionLogger;

        function logSurveyImpression($el, config) {
            var event;
            if (mw.viewport.isElementInViewport($el.get(0))) {
                $window.off('scroll.quickSurveys', surveyImpressionLogger);
                if (mw.eventLog) {
                    event = {
                        surveySessionToken: config.surveySessionToken,
                        pageviewToken: config.pageviewToken,
                        surveyCodeName: config.survey.name,
                        eventName: 'impression'
                    };
                    if (window.performance && performance.now) {
                        event.performanceNow = Math.round(performance.now());
                    }
                    mw.eventLog.logEvent('QuickSurveyInitiation', event);
                }
            }
        }

        function insertPanel($bodyContent, $panel, isMobileLayout) {
            var $place;
            if (isMobileLayout) {
                $place = $bodyContent.find('> div > div').eq(0).find(' > p').eq(0);
            }
            if ($place && $place.length) {
                $panel.insertAfter($place);
            } else {
                $place = $bodyContent.find(['.infobox', '> div > div > .thumb', '> div > .thumb', '> .thumb', 'h1, h2, h3, h4, h5, h6'].join(',')).
                filter(':not(' + ['.toc-mobile h2', '.toc h2'].join(',') + ')').eq(0);
                if ($place.length) {
                    $panel.insertBefore($place);
                } else {
                    $place = $bodyContent.find('p').eq(0);
                    if (!$place.length) {
                        $place = $('> div').eq(0);
                    }
                    $panel.insertAfter($place);
                }
            }
        }

        function isValidSurvey() {
            return true;
        }

        function isInCountry(audience, geo) {
            return audience.countries === undefined ? true : audience.countries.indexOf(geo.country) > -1;
        }

        function registrationDateNotInRange(user, registrationStart, registrationEnd) {
            var from, to;
            if (user.getRegistration() === false) {
                return true;
            }
            from = registrationStart ? new Date(registrationStart + 'T00:00:00+00:00') : new Date(false);
            to = registrationEnd ? new Date(registrationEnd + 'T23:59:59+0000') : new Date();
            return from > user.getRegistration() || user.getRegistration() > to;
        }

        function isInAudience(audience, user, editCount, geo) {
            var hasMinEditAudience = audience.minEdits !== undefined,
                hasMaxEditAudience = audience.maxEdits !== undefined;
            if ((audience.registrationStart || audience.registrationEnd) && registrationDateNotInRange(user, audience.registrationStart, audience.registrationEnd)) {
                return false;
            } else if (audience.anons !== undefined && audience.anons !== user.isAnon()) {
                return false;
            } else if (editCount === null && hasMinEditAudience) {
                return false;
            } else if ((hasMinEditAudience && editCount < audience.minEdits) || (hasMaxEditAudience && editCount > audience.maxEdits)) {
                return false;
            }
            geo = geo || {
                country: '??'
            };
            return audience.countries ? isInCountry(audience, geo) : !0;
        }

        function getSurveyFromQueryString(queryString, availableSurveys) {
            var i, surveyType, surveyName, survey;
            if (queryString === 'true') {
                i = Math.floor(Math.random() * availableSurveys.length);
                return availableSurveys[i];
            }
            if (queryString.indexOf('internal-survey-') === 0) {
                surveyType = 'internal';
            } else if (queryString.indexOf('external-survey-') === 0) {
                surveyType = 'external';
            }
            if (surveyType) {
                surveyName = queryString.split('-').slice(2).join('-');
                for (i = 0; i < availableSurveys.length; i++) {
                    survey = availableSurveys[i];
                    if (survey.name === surveyName && survey.type === surveyType) {
                        return survey;
                    }
                }
            }
            return null;
        }

        function
        getSurveyStorageKey(survey) {
            return 'ext-quicksurvey-' + survey.name.replace(/ /g, '-');
        }

        function getSurveyToken(survey) {
            return mw.storage.get(getSurveyStorageKey(survey));
        }

        function getBucketForSurvey(survey) {
            var control = 1 - survey.coverage,
                a = survey.coverage,
                storageId = getSurveyStorageKey(survey),
                token = getSurveyToken(survey);
            if (!token) {
                token = mw.user.generateRandomSessionId();
                mw.storage.set(storageId, token);
            }
            return mw.experiments.getBucket({
                name: survey.name,
                enabled: !0,
                buckets: {
                    control: Number(control),
                    A: Number(a)
                }
            }, token);
        }

        function surveyMatchesPlatform(survey, mode) {
            var platformKey = mode ? 'mobile' : 'desktop',
                platformValue = mode || 'stable';
            return hasOwn.call(survey.platforms, platformKey) && survey.platforms[platformKey].indexOf(platformValue) !== -1;
        }

        function showSurvey(forcedSurvey) {
            var enabledSurveys = mw.config.get('wgEnabledQuickSurveys'),
                $panel = $('<div class="ext-qs-loader-bar mw-ajax-loader"></div>'),
                $bodyContent = $('#bodyContent'),
                isMobileLayout = window.innerWidth <= 768,
                enabledSurvey;
            if (forcedSurvey) {
                enabledSurvey = getSurveyFromQueryString(forcedSurvey || '', enabledSurveys);
                if (enabledSurvey && isValidSurvey(enabledSurvey)) {
                    availableSurveys.push(enabledSurvey);
                }
            } else {
                enabledSurveys.forEach(function(enabledSurvey) {
                    if (getSurveyToken(enabledSurvey) !== '~' && getBucketForSurvey(enabledSurvey) === 'A' && isValidSurvey(enabledSurvey) && isInAudience(enabledSurvey.audience, mw.user, mw.config.get('wgUserEditCount'), window.Geo) && surveyMatchesPlatform(enabledSurvey, mw.config.get('wgMFMode'))) {
                        availableSurveys.push(enabledSurvey);
                    }
                });
            }
            if (availableSurveys.length) {
                survey = availableSurveys[Math.floor(Math.random() * availableSurveys.length)];
                insertPanel($bodyContent, $panel, isMobileLayout);
                mw.loader.using(['ext.quicksurveys.views', survey.module]).done(function() {
                    var panel, options = {
                        survey: survey,
                        templateData: {
                            question: mw.msg(survey.question),
                            description: survey.description ? mw.msg(survey.description) : ''
                        },
                        surveySessionToken: mw.user.sessionId() + '-quicksurveys',
                        pageviewToken: mw.user.getPageviewToken(),
                        isMobileLayout: isMobileLayout
                    };
                    if (survey.type === 'internal') {
                        panel = new mw.extQuickSurveys.QuickSurvey(options);
                    } else {
                        panel = new mw.extQuickSurveys.ExternalSurvey(options);
                    }
                    panel.on('dismiss', function() {
                        mw.storage.set(getSurveyStorageKey(survey), '~');
                    });
                    $panel.replaceWith(panel.$element);
                    surveyImpressionLogger = function() {
                        logSurveyImpression(panel.$element, options);
                    };
                    $window.on('scroll.quickSurveys', $.debounce(250, surveyImpressionLogger));
                    surveyImpressionLogger();
                });
            }
        }
        mw.extQuickSurveys = {
            isInAudience: isInAudience,
            surveyMatchesPlatform: surveyMatchesPlatform,
            _insertPanel: insertPanel,
            showSurvey: showSurvey
        };
    }());
}, {
    "css": [".ext-quick-survey-panel,.ext-qs-loader-bar{width:auto;background-color:#eaecf0} .ext-qs-loader-bar{display:block;height:100px;margin-left:1.4em;clear:right;float:right;background-color:#eaecf0}.ext-qs-loader-bar.mw-ajax-loader{top:0}@media all and (min-width:720px){.ext-qs-loader-bar,.ext-quick-survey-panel{margin-left:1.4em;width:300px;clear:right;float:right}}"]
});
mw.loader.implement("ext.relatedArticles.readMore.bootstrap@0ychxqy", {
    "main": "index.js",
    "files": {
        "index.js": function(require, module) {
            (function() {
                var data = require('./data.json'),
                    relatedPages = new mw.relatedPages.RelatedPagesGateway(new mw.Api(), mw.config.get('wgPageName'), mw.config.get('wgRelatedArticles'), data.useCirrusSearch, data.onlyUseCirrusSearch, data.descriptionSource),
                    LIMIT = mw.config.get('wgRelatedArticlesCardLimit', 3),
                    debouncedLoad = $.debounce(100, function() {
                        loadRelatedArticles();
                    }),
                    $window = $(window);

                function loadRelatedArticles() {
                    var readMore = $('.read-more-container').get(0),
                        scrollThreshold = $window.height() * 2;
                    if (mw.viewport.isElementCloseToViewport(readMore, scrollThreshold)) {
                        $.when(mw.loader.using(['ext.relatedArticles.cards', 'ext.relatedArticles.readMore']), relatedPages.getForCurrentPage(LIMIT)).then(function(_, pages) {
                            if (pages.length) {
                                mw.track('ext.relatedArticles.init', pages);
                            } else {
                                $(readMore).remove();
                            }
                        });
                        $window.off('scroll', debouncedLoad);
                    }
                }

                function showReadMore() {
                    $window.on('scroll', debouncedLoad);
                    loadRelatedArticles();
                }
                $(showReadMore);
            }());
        },
        "data.json": {
            "useCirrusSearch": !0,
            "onlyUseCirrusSearch": !1,
            "descriptionSource": "wikidata"
        }
    }
});
mw.loader.implement("ext.relatedArticles.readMore.gateway@00vbs1t", function($, jQuery, require, module) {
    (function() {
        mw.relatedPages = {};

        function RelatedPagesGateway(api, currentPage, editorCuratedPages, useCirrusSearch, onlyUseCirrusSearch, descriptionSource) {
            this.api = api;
            this.currentPage = currentPage;
            this.useCirrusSearch = useCirrusSearch;
            this.descriptionSource = descriptionSource;
            if (onlyUseCirrusSearch) {
                editorCuratedPages = [];
            }
            this.editorCuratedPages = editorCuratedPages || [];
        }
        OO.initClass(RelatedPagesGateway);

        function getPages(result) {
            return result && result.query && result.query.pages ? result.query.pages : [];
        }
        RelatedPagesGateway.prototype.getForCurrentPage = function(limit) {
            var parameters = {
                    action: 'query',
                    formatversion: 2,
                    prop: 'pageimages',
                    piprop: 'thumbnail',
                    pithumbsize: 160
                },
                relatedPages = this.editorCuratedPages.slice(0, limit);
            switch (this.descriptionSource) {
                case 'wikidata':
                    parameters.prop += '|description';
                    break;
                case 'textextracts':
                    parameters.prop += '|extracts';
                    parameters.exsentences = '1';
                    parameters.exintro = '1';
                    parameters.explaintext = '1';
                    break;
                case 'pagedescription':
                    parameters.prop += '|pageprops';
                    parameters.ppprop = 'description';
                    break;
            }
            if (relatedPages.length) {
                parameters.pilimit = relatedPages.length;
                parameters.continue = '';
                parameters.titles = relatedPages;
            } else if (this.useCirrusSearch) {
                parameters.pilimit = limit;
                parameters.generator = 'search';
                parameters.gsrsearch = 'morelike:' + this.currentPage;
                parameters.gsrnamespace = '0';
                parameters.gsrlimit = limit;
                parameters.gsrqiprofile = 'classic_noboostlinks';
                parameters.uselang = 'content';
                parameters.smaxage = 86400;
                parameters.maxage = 86400;
            } else {
                return $.Deferred().resolve([]);
            }
            return this.api.get(parameters).then(getPages);
        };
        mw.relatedPages.RelatedPagesGateway = RelatedPagesGateway;
    }());
});
mw.loader.implement("jquery@1nollmt", function($, jQuery, require, module) {
    (function(global, factory) {
        "use strict";
        if (typeof module === "object" && typeof module.exports === "object") {
            module.exports = global.document ? factory(global, true) : function(w) {
                if (!w.document) {
                    throw new Error("jQuery requires a window with a document");
                }
                return factory(w);
            };
        } else {
            factory(global);
        }
    })(typeof window !== "undefined" ? window : this, function(window, noGlobal) {
        "use strict";
        var arr = [];
        var document = window.document;
        var getProto = Object.getPrototypeOf;
        var slice = arr.slice;
        var concat = arr.concat;
        var push = arr.push;
        var indexOf = arr.indexOf;
        var class2type = {};
        var toString = class2type.toString;
        var hasOwn = class2type.hasOwnProperty;
        var fnToString = hasOwn.toString;
        var ObjectFunctionString = fnToString.call(Object);
        var support = {};
        var isFunction = function isFunction(obj) {
            return typeof obj === "function" && typeof obj.nodeType !== "number";
        };
        var isWindow = function isWindow(obj) {
            return obj != null && obj === obj.window;
        };
        var preservedScriptAttributes = {
            type: !0,
            src: !0,
            noModule: !0
        };

        function DOMEval(code, doc, node) {
            doc = doc || document;
            var i, script = doc.createElement("script");
            script.text = code;
            if (node) {
                for (i in preservedScriptAttributes) {
                    if (node[i]) {
                        script[i] = node[i];
                    }
                }
            }
            doc.head.appendChild(script).parentNode.removeChild(script);
        }

        function toType(obj) {
            if (obj == null) {
                return obj + "";
            }
            return typeof obj === "object" || typeof obj === "function" ? class2type[toString.call(obj)] || "object" : typeof obj;
        }
        var version = "3.3.1",
            jQuery = function(selector, context) {
                return new jQuery.fn.init(selector, context);
            },
            rtrim = /^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g;
        jQuery.fn = jQuery.prototype = {
            jquery: version,
            constructor: jQuery,
            length: 0,
            toArray: function() {
                return slice.call(this);
            },
            get: function(num) {
                if (num == null) {
                    return slice.call(this);
                }
                return num < 0 ? this[num + this.length] : this[num];
            },
            pushStack: function(elems) {
                var ret = jQuery.merge(this.constructor(), elems);
                ret.prevObject = this;
                return ret;
            },
            each: function(callback) {
                return jQuery.each(this, callback);
            },
            map: function(callback) {
                return this.
                pushStack(jQuery.map(this, function(elem, i) {
                    return callback.call(elem, i, elem);
                }));
            },
            slice: function() {
                return this.pushStack(slice.apply(this, arguments));
            },
            first: function() {
                return this.eq(0);
            },
            last: function() {
                return this.eq(-1);
            },
            eq: function(i) {
                var len = this.length,
                    j = +i + (i < 0 ? len : 0);
                return this.pushStack(j >= 0 && j < len ? [this[j]] : []);
            },
            end: function() {
                return this.prevObject || this.constructor();
            },
            push: push,
            sort: arr.sort,
            splice: arr.splice
        };
        jQuery.extend = jQuery.fn.extend = function() {
            var options, name, src, copy, copyIsArray, clone, target = arguments[0] || {},
                i = 1,
                length = arguments.length,
                deep = !1;
            if (typeof target === "boolean") {
                deep = target;
                target = arguments[i] || {};
                i++;
            }
            if (typeof target !== "object" && !isFunction(target)) {
                target = {};
            }
            if (i === length) {
                target = this;
                i--;
            }
            for (; i < length; i++) {
                if ((options = arguments[i]) != null) {
                    for (name in options) {
                        src = target[name];
                        copy = options[name];
                        if (name === "__proto__" || target === copy) {
                            continue;
                        }
                        if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray(copy)))) {
                            if (copyIsArray) {
                                copyIsArray = !1;
                                clone = src && Array.isArray(src) ? src : [];
                            } else {
                                clone = src && jQuery.isPlainObject(src) ? src : {};
                            }
                            target[name] = jQuery.extend(deep, clone, copy);
                        } else if (copy !== undefined) {
                            target[name] = copy;
                        }
                    }
                }
            }
            return target;
        };
        jQuery.extend({
            expando: "jQuery" + (version + Math.random()).replace(/\D/g, ""),
            isReady: !0,
            error: function(msg) {
                throw new Error(msg);
            },
            noop: function() {},
            isPlainObject: function(obj) {
                var proto, Ctor;
                if (!obj || toString.call(obj) !== "[object Object]") {
                    return false;
                }
                proto = getProto(obj);
                if (!proto) {
                    return true;
                }
                Ctor = hasOwn.call(proto, "constructor") && proto.constructor;
                return typeof Ctor === "function" && fnToString.call(Ctor) === ObjectFunctionString;
            },
            isEmptyObject: function(obj) {
                var name;
                for (name in obj) {
                    return false;
                }
                return true;
            },
            globalEval: function(code) {
                DOMEval(code);
            },
            each: function(obj, callback) {
                var length, i = 0;
                if (isArrayLike(obj)) {
                    length = obj.length;
                    for (; i < length; i++) {
                        if (callback.call(obj[i], i, obj[i]) === false) {
                            break;
                        }
                    }
                } else {
                    for (i in obj) {
                        if (callback.call(obj[i], i, obj[i]) === false) {
                            break;
                        }
                    }
                }
                return obj;
            },
            trim: function(text) {
                return text == null ? "" : (text + "").replace(rtrim, "");
            },
            makeArray: function(arr, results) {
                var ret = results || [];
                if (arr != null) {
                    if (isArrayLike(Object(arr))) {
                        jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
                    } else {
                        push.call(ret, arr);
                    }
                }
                return ret;
            },
            inArray: function(elem, arr, i) {
                return arr == null ? -1 : indexOf.call(arr, elem, i);
            },
            merge: function(first, second) {
                var len = +second.length,
                    j = 0,
                    i = first.length;
                for (; j < len; j++) {
                    first[i++] = second[j];
                }
                first.length = i;
                return first;
            },
            grep: function(elems, callback, invert) {
                var callbackInverse, matches = [],
                    i = 0,
                    length = elems.length,
                    callbackExpect = !invert;
                for (; i < length; i++) {
                    callbackInverse = !callback(elems[i], i);
                    if (callbackInverse !== callbackExpect) {
                        matches.push(elems[i]);
                    }
                }
                return matches;
            },
            map: function(elems, callback, arg) {
                var length, value, i = 0,
                    ret = [];
                if (isArrayLike(elems)) {
                    length = elems.length;
                    for (; i < length; i++) {
                        value = callback(elems[i], i, arg);
                        if (value != null) {
                            ret.push(value);
                        }
                    }
                } else {
                    for (i in elems) {
                        value = callback(elems[i], i, arg);
                        if (value != null) {
                            ret.push(value);
                        }
                    }
                }
                return concat.apply([], ret);
            },
            guid: 1,
            support: support
        });
        if (typeof Symbol === "function") {
            jQuery.fn[Symbol.iterator] = arr[Symbol.iterator];
        }
        jQuery.each("Boolean Number String Function Array Date RegExp Object Error Symbol".split(" "), function(i, name) {
            class2type["[object " + name + "]"] = name.toLowerCase();
        });

        function isArrayLike(obj) {
            var length = !!obj && "length" in obj && obj.length,
                type = toType(obj);
            if (isFunction(obj) || isWindow(obj)) {
                return false;
            }
            return type === "array" || length === 0 || typeof length === "number" && length > 0 && (length - 1) in obj;
        }
        var Sizzle = (function(window) {
            var i, support, Expr, getText, isXML, tokenize, compile, select, outermostContext, sortInput, hasDuplicate, setDocument, document, docElem, documentIsHTML, rbuggyQSA, rbuggyMatches, matches, contains, expando = "sizzle" + 1 * new Date(),
                preferredDoc = window.document,
                dirruns = 0,
                done = 0,
                classCache = createCache(),
                tokenCache = createCache(),
                compilerCache = createCache(),
                sortOrder = function(a, b) {
                    if (a === b) {
                        hasDuplicate = !0;
                    }
                    return 0;
                },
                hasOwn = ({}).hasOwnProperty,
                arr = [],
                pop = arr.pop,
                push_native = arr.push,
                push = arr.push,
                slice = arr.slice,
                indexOf = function(list, elem) {
                    var i = 0,
                        len = list.length;
                    for (; i < len; i++) {
                        if (list[i] === elem) {
                            return i;
                        }
                    }
                    return -1;
                },
                booleans = "checked|selected|async|autofocus|autoplay|controls|defer|disabled|hidden|ismap|loop|multiple|open|readonly|required|scoped",
                whitespace = "[\\x20\\t\\r\\n\\f]",
                identifier = "(?:\\\\.|[\\w-]|[^\0-\\xa0])+",
                attributes = "\\[" + whitespace + "*(" + identifier + ")(?:" + whitespace + "*([*^$|!~]?=)" + whitespace + "*(?:'((?:\\\\.|[^\\\\'])*)'|\"((?:\\\\.|[^\\\\\"])*)\"|(" + identifier + "))|)" + whitespace + "*\\]",
                pseudos = ":(" + identifier + ")(?:\\((" + "('((?:\\\\.|[^\\\\'])*)'|\"((?:\\\\.|[^\\\\\"])*)\")|" + "((?:\\\\.|[^\\\\()[\\]]|" + attributes + ")*)|" + ".*" + ")\\)|)",
                rwhitespace = new RegExp(whitespace + "+", "g"),
                rtrim = new RegExp("^" + whitespace + "+|((?:^|[^\\\\])(?:\\\\.)*)" + whitespace + "+$", "g"),
                rcomma = new RegExp("^" + whitespace + "*," + whitespace + "*"),
                rcombinators = new RegExp("^" + whitespace + "*([>+~]|" + whitespace + ")" + whitespace + "*"),
                rattributeQuotes = new
            RegExp("=" + whitespace + "*([^\\]'\"]*?)" + whitespace + "*\\]", "g"), rpseudo = new RegExp(pseudos), ridentifier = new RegExp("^" + identifier + "$"), matchExpr = {
                "ID": new RegExp("^#(" + identifier + ")"),
                "CLASS": new RegExp("^\\.(" + identifier + ")"),
                "TAG": new RegExp("^(" + identifier + "|[*])"),
                "ATTR": new RegExp("^" + attributes),
                "PSEUDO": new RegExp("^" + pseudos),
                "CHILD": new RegExp("^:(only|first|last|nth|nth-last)-(child|of-type)(?:\\(" + whitespace + "*(even|odd|(([+-]|)(\\d*)n|)" + whitespace + "*(?:([+-]|)" + whitespace + "*(\\d+)|))" + whitespace + "*\\)|)", "i"),
                "bool": new RegExp("^(?:" + booleans + ")$", "i"),
                "needsContext": new RegExp("^" + whitespace + "*[>+~]|:(even|odd|eq|gt|lt|nth|first|last)(?:\\(" + whitespace + "*((?:-\\d)?\\d*)" + whitespace + "*\\)|)(?=[^-]|$)", "i")
            }, rinputs = /^(?:input|select|textarea|button)$/i, rheader = /^h\d$/i, rnative = /^[^{]+\{\s*\[native \w/, rquickExpr = /^(?:#([\w-]+)|(\w+)|\.([\w-]+))$/, rsibling = /[+~]/, runescape = new RegExp("\\\\([\\da-f]{1,6}" + whitespace + "?|(" + whitespace + ")|.)", "ig"), funescape = function(_, escaped,
                escapedWhitespace) {
                var high = "0x" + escaped - 0x10000;
                return high !== high || escapedWhitespace ? escaped : high < 0 ? String.fromCharCode(high + 0x10000) : String.fromCharCode(high >> 10 | 0xD800, high & 0x3FF | 0xDC00);
            }, rcssescape = /([\0-\x1f\x7f]|^-?\d)|^-$|[^\0-\x1f\x7f-\uFFFF\w-]/g, fcssescape = function(ch, asCodePoint) {
                if (asCodePoint) {
                    if (ch === "\0") {
                        return "\uFFFD";
                    }
                    return ch.slice(0, -1) + "\\" + ch.charCodeAt(ch.length - 1).toString(16) + " ";
                }
                return "\\" + ch;
            }, unloadHandler = function() {
                setDocument();
            }, disabledAncestor = addCombinator(function(elem) {
                return elem.disabled === !0 && ("form" in elem || "label" in elem);
            }, {
                dir: "parentNode",
                next: "legend"
            });
            try {
                push.apply((arr = slice.call(preferredDoc.childNodes)), preferredDoc.childNodes);
                arr[preferredDoc.childNodes.length].nodeType;
            } catch (e) {
                push = {
                    apply: arr.length ? function(target, els) {
                        push_native.apply(target, slice.call(els));
                    } : function(target, els) {
                        var j = target.length,
                            i = 0;
                        while ((target[j++] = els[i++])) {}
                        target.length = j - 1;
                    }
                };
            }

            function Sizzle(selector, context, results, seed) {
                var m, i, elem, nid,
                    match, groups, newSelector, newContext = context && context.ownerDocument,
                    nodeType = context ? context.nodeType : 9;
                results = results || [];
                if (typeof selector !== "string" || !selector || nodeType !== 1 && nodeType !== 9 && nodeType !== 11) {
                    return results;
                }
                if (!seed) {
                    if ((context ? context.ownerDocument || context : preferredDoc) !== document) {
                        setDocument(context);
                    }
                    context = context || document;
                    if (documentIsHTML) {
                        if (nodeType !== 11 && (match = rquickExpr.exec(selector))) {
                            if ((m = match[1])) {
                                if (nodeType === 9) {
                                    if ((elem = context.getElementById(m))) {
                                        if (elem.id === m) {
                                            results.push(elem);
                                            return results;
                                        }
                                    } else {
                                        return results;
                                    }
                                } else {
                                    if (newContext && (elem = newContext.getElementById(m)) && contains(context, elem) && elem.id === m) {
                                        results.push(elem);
                                        return results;
                                    }
                                }
                            } else if (match[2]) {
                                push.apply(results, context.getElementsByTagName(selector));
                                return results;
                            } else if ((m = match[3]) && support.getElementsByClassName && context.getElementsByClassName) {
                                push.apply(results, context.getElementsByClassName(m));
                                return results;
                            }
                        }
                        if (support.qsa && !compilerCache[selector + " "] && (!
                                rbuggyQSA || !rbuggyQSA.test(selector))) {
                            if (nodeType !== 1) {
                                newContext = context;
                                newSelector = selector;
                            } else if (context.nodeName.toLowerCase() !== "object") {
                                if ((nid = context.getAttribute("id"))) {
                                    nid = nid.replace(rcssescape, fcssescape);
                                } else {
                                    context.setAttribute("id", (nid = expando));
                                }
                                groups = tokenize(selector);
                                i = groups.length;
                                while (i--) {
                                    groups[i] = "#" + nid + " " + toSelector(groups[i]);
                                }
                                newSelector = groups.join(",");
                                newContext = rsibling.test(selector) && testContext(context.parentNode) || context;
                            }
                            if (newSelector) {
                                try {
                                    push.apply(results, newContext.querySelectorAll(newSelector));
                                    return results;
                                } catch (qsaError) {} finally {
                                    if (nid === expando) {
                                        context.removeAttribute("id");
                                    }
                                }
                            }
                        }
                    }
                }
                return select(selector.replace(rtrim, "$1"), context, results, seed);
            }

            function createCache() {
                var keys = [];

                function cache(key, value) {
                    if (keys.push(key + " ") > Expr.cacheLength) {
                        delete cache[keys.shift()];
                    }
                    return (cache[key + " "] = value);
                }
                return cache;
            }

            function markFunction(fn) {
                fn[expando] = !0;
                return fn;
            }

            function assert(fn) {
                var el = document.createElement(
                    "fieldset");
                try {
                    return !!fn(el);
                } catch (e) {
                    return false;
                } finally {
                    if (el.parentNode) {
                        el.parentNode.removeChild(el);
                    }
                    el = null;
                }
            }

            function addHandle(attrs, handler) {
                var arr = attrs.split("|"),
                    i = arr.length;
                while (i--) {
                    Expr.attrHandle[arr[i]] = handler;
                }
            }

            function siblingCheck(a, b) {
                var cur = b && a,
                    diff = cur && a.nodeType === 1 && b.nodeType === 1 && a.sourceIndex - b.sourceIndex;
                if (diff) {
                    return diff;
                }
                if (cur) {
                    while ((cur = cur.nextSibling)) {
                        if (cur === b) {
                            return -1;
                        }
                    }
                }
                return a ? 1 : -1;
            }

            function createInputPseudo(type) {
                return function(elem) {
                    var name = elem.nodeName.toLowerCase();
                    return name === "input" && elem.type === type;
                };
            }

            function createButtonPseudo(type) {
                return function(elem) {
                    var name = elem.nodeName.toLowerCase();
                    return (name === "input" || name === "button") && elem.type === type;
                };
            }

            function createDisabledPseudo(disabled) {
                return function(elem) {
                    if ("form" in elem) {
                        if (elem.parentNode && elem.disabled === false) {
                            if ("label" in elem) {
                                if ("label" in elem.parentNode) {
                                    return elem.parentNode.disabled === disabled;
                                } else {
                                    return elem.disabled === disabled;
                                }
                            }
                            return elem
                                .isDisabled === disabled || elem.isDisabled !== !disabled && disabledAncestor(elem) === disabled;
                        }
                        return elem.disabled === disabled;
                    } else if ("label" in elem) {
                        return elem.disabled === disabled;
                    }
                    return false;
                };
            }

            function createPositionalPseudo(fn) {
                return markFunction(function(argument) {
                    argument = +argument;
                    return markFunction(function(seed, matches) {
                        var j, matchIndexes = fn([], seed.length, argument),
                            i = matchIndexes.length;
                        while (i--) {
                            if (seed[(j = matchIndexes[i])]) {
                                seed[j] = !(matches[j] = seed[j]);
                            }
                        }
                    });
                });
            }

            function testContext(context) {
                return context && typeof context.getElementsByTagName !== "undefined" && context;
            }
            support = Sizzle.support = {};
            isXML = Sizzle.isXML = function(elem) {
                var documentElement = elem && (elem.ownerDocument || elem).documentElement;
                return documentElement ? documentElement.nodeName !== "HTML" : !1;
            };
            setDocument = Sizzle.setDocument = function(node) {
                var hasCompare, subWindow, doc = node ? node.ownerDocument || node : preferredDoc;
                if (doc === document || doc.nodeType !== 9 || !doc.documentElement) {
                    return document;
                }
                document = doc;
                docElem =
                    document.documentElement;
                documentIsHTML = !isXML(document);
                if (preferredDoc !== document && (subWindow = document.defaultView) && subWindow.top !== subWindow) {
                    if (subWindow.addEventListener) {
                        subWindow.addEventListener("unload", unloadHandler, false);
                    } else if (subWindow.attachEvent) {
                        subWindow.attachEvent("onunload", unloadHandler);
                    }
                }
                support.attributes = assert(function(el) {
                    el.className = "i";
                    return !el.getAttribute("className");
                });
                support.getElementsByTagName = assert(function(el) {
                    el.appendChild(document.createComment(""));
                    return !el.getElementsByTagName("*").length;
                });
                support.getElementsByClassName = rnative.test(document.getElementsByClassName);
                support.getById = assert(function(el) {
                    docElem.appendChild(el).id = expando;
                    return !document.getElementsByName || !document.getElementsByName(expando).length;
                });
                if (support.getById) {
                    Expr.filter["ID"] = function(id) {
                        var attrId = id.replace(runescape, funescape);
                        return function(elem) {
                            return elem.getAttribute("id") === attrId;
                        };
                    };
                    Expr.find["ID"] = function(id, context) {
                        if (typeof context.getElementById !== "undefined" && documentIsHTML) {
                            var elem = context.getElementById(id);
                            return elem ? [elem] : [];
                        }
                    };
                } else {
                    Expr.filter["ID"] = function(id) {
                        var attrId = id.replace(runescape, funescape);
                        return function(elem) {
                            var node = typeof elem.getAttributeNode !== "undefined" && elem.getAttributeNode("id");
                            return node && node.value === attrId;
                        };
                    };
                    Expr.find["ID"] = function(id, context) {
                        if (typeof context.getElementById !== "undefined" && documentIsHTML) {
                            var node, i, elems, elem = context.getElementById(id);
                            if (elem) {
                                node = elem.getAttributeNode("id");
                                if (node && node.value === id) {
                                    return [elem];
                                }
                                elems = context.getElementsByName(id);
                                i = 0;
                                while ((elem = elems[i++])) {
                                    node = elem.getAttributeNode("id");
                                    if (node && node.value === id) {
                                        return [elem];
                                    }
                                }
                            }
                            return [];
                        }
                    };
                }
                Expr.find["TAG"] = support.getElementsByTagName ? function(tag, context) {
                    if (typeof context.getElementsByTagName !== "undefined") {
                        return context.getElementsByTagName(tag);
                    } else if (support.qsa) {
                        return context.querySelectorAll(tag);
                    }
                } : function(tag, context) {
                    var elem, tmp = [],
                        i = 0,
                        results = context.
                    getElementsByTagName(tag);
                    if (tag === "*") {
                        while ((elem = results[i++])) {
                            if (elem.nodeType === 1) {
                                tmp.push(elem);
                            }
                        }
                        return tmp;
                    }
                    return results;
                };
                Expr.find["CLASS"] = support.getElementsByClassName && function(className, context) {
                    if (typeof context.getElementsByClassName !== "undefined" && documentIsHTML) {
                        return context.getElementsByClassName(className);
                    }
                };
                rbuggyMatches = [];
                rbuggyQSA = [];
                if ((support.qsa = rnative.test(document.querySelectorAll))) {
                    assert(function(el) {
                        docElem.appendChild(el).innerHTML = "<a id='" + expando + "'></a>" + "<select id='" + expando + "-\r\\' msallowcapture=''>" + "<option selected=''></option></select>";
                        if (el.querySelectorAll("[msallowcapture^='']").length) {
                            rbuggyQSA.push("[*^$]=" + whitespace + "*(?:''|\"\")");
                        }
                        if (!el.querySelectorAll("[selected]").length) {
                            rbuggyQSA.push("\\[" + whitespace + "*(?:value|" + booleans + ")");
                        }
                        if (!el.querySelectorAll("[id~=" + expando + "-]").length) {
                            rbuggyQSA.push("~=");
                        }
                        if (!el.querySelectorAll(":checked").length) {
                            rbuggyQSA.push(":checked");
                        }
                        if (!el.querySelectorAll("a#" + expando +
                                "+*").length) {
                            rbuggyQSA.push(".#.+[+~]");
                        }
                    });
                    assert(function(el) {
                        el.innerHTML = "<a href='' disabled='disabled'></a>" + "<select disabled='disabled'><option/></select>";
                        var input = document.createElement("input");
                        input.setAttribute("type", "hidden");
                        el.appendChild(input).setAttribute("name", "D");
                        if (el.querySelectorAll("[name=d]").length) {
                            rbuggyQSA.push("name" + whitespace + "*[*^$|!~]?=");
                        }
                        if (el.querySelectorAll(":enabled").length !== 2) {
                            rbuggyQSA.push(":enabled", ":disabled");
                        }
                        docElem.appendChild(el).disabled = !0;
                        if (el.querySelectorAll(":disabled").length !== 2) {
                            rbuggyQSA.push(":enabled", ":disabled");
                        }
                        el.querySelectorAll("*,:x");
                        rbuggyQSA.push(",.*:");
                    });
                }
                if ((support.matchesSelector = rnative.test((matches = docElem.matches || docElem.webkitMatchesSelector || docElem.mozMatchesSelector || docElem.oMatchesSelector || docElem.msMatchesSelector)))) {
                    assert(function(el) {
                        support.disconnectedMatch = matches.call(el, "*");
                        matches.call(el, "[s!='']:x");
                        rbuggyMatches.push("!=", pseudos);
                    });
                }
                rbuggyQSA = rbuggyQSA.length && new
                RegExp(rbuggyQSA.join("|"));
                rbuggyMatches = rbuggyMatches.length && new RegExp(rbuggyMatches.join("|"));
                hasCompare = rnative.test(docElem.compareDocumentPosition);
                contains = hasCompare || rnative.test(docElem.contains) ? function(a, b) {
                    var adown = a.nodeType === 9 ? a.documentElement : a,
                        bup = b && b.parentNode;
                    return a === bup || !!(bup && bup.nodeType === 1 && (adown.contains ? adown.contains(bup) : a.compareDocumentPosition && a.compareDocumentPosition(bup) & 16));
                } : function(a, b) {
                    if (b) {
                        while ((b = b.parentNode)) {
                            if (b === a) {
                                return true;
                            }
                        }
                    }
                    return false;
                };
                sortOrder = hasCompare ? function(a, b) {
                    if (a === b) {
                        hasDuplicate = !0;
                        return 0;
                    }
                    var compare = !a.compareDocumentPosition - !b.compareDocumentPosition;
                    if (compare) {
                        return compare;
                    }
                    compare = (a.ownerDocument || a) === (b.ownerDocument || b) ? a.compareDocumentPosition(b) : 1;
                    if (compare & 1 || (!support.sortDetached && b.compareDocumentPosition(a) === compare)) {
                        if (a === document || a.ownerDocument === preferredDoc && contains(preferredDoc, a)) {
                            return -1;
                        }
                        if (b === document || b.ownerDocument === preferredDoc && contains(preferredDoc,
                                b)) {
                            return 1;
                        }
                        return sortInput ? (indexOf(sortInput, a) - indexOf(sortInput, b)) : 0;
                    }
                    return compare & 4 ? -1 : 1;
                } : function(a, b) {
                    if (a === b) {
                        hasDuplicate = !0;
                        return 0;
                    }
                    var cur, i = 0,
                        aup = a.parentNode,
                        bup = b.parentNode,
                        ap = [a],
                        bp = [b];
                    if (!aup || !bup) {
                        return a === document ? -1 : b === document ? 1 : aup ? -1 : bup ? 1 : sortInput ? (indexOf(sortInput, a) - indexOf(sortInput, b)) : 0;
                    } else if (aup === bup) {
                        return siblingCheck(a, b);
                    }
                    cur = a;
                    while ((cur = cur.parentNode)) {
                        ap.unshift(cur);
                    }
                    cur = b;
                    while ((cur = cur.parentNode)) {
                        bp.unshift(cur);
                    }
                    while (ap[i] === bp[i]) {
                        i++;
                    }
                    return i ? siblingCheck(ap[i], bp[i]) : ap[i] === preferredDoc ? -1 : bp[i] === preferredDoc ? 1 : 0;
                };
                return document;
            };
            Sizzle.matches = function(expr, elements) {
                return Sizzle(expr, null, null, elements);
            };
            Sizzle.matchesSelector = function(elem, expr) {
                if ((elem.ownerDocument || elem) !== document) {
                    setDocument(elem);
                }
                expr = expr.replace(rattributeQuotes, "='$1']");
                if (support.matchesSelector && documentIsHTML && !compilerCache[expr + " "] && (!rbuggyMatches || !rbuggyMatches.test(expr)) && (!rbuggyQSA || !rbuggyQSA.test(expr))) {
                    try {
                        var ret = matches.call(elem, expr);
                        if (ret || support.disconnectedMatch || elem.document && elem.document.nodeType !== 11) {
                            return ret;
                        }
                    } catch (e) {}
                }
                return Sizzle(expr, document, null, [elem]).length > 0;
            };
            Sizzle.contains = function(context, elem) {
                if ((context.ownerDocument || context) !== document) {
                    setDocument(context);
                }
                return contains(context, elem);
            };
            Sizzle.attr = function(elem, name) {
                if ((elem.ownerDocument || elem) !== document) {
                    setDocument(elem);
                }
                var fn = Expr.attrHandle[name.toLowerCase()],
                    val = fn && hasOwn.call(Expr.attrHandle, name.toLowerCase()) ? fn(elem, name, !documentIsHTML) : undefined;
                return val !== undefined ? val : support.attributes || !documentIsHTML ? elem.getAttribute(name) : (val = elem.getAttributeNode(name)) && val.specified ? val.value : null;
            };
            Sizzle.escape = function(sel) {
                return (sel + "").replace(rcssescape, fcssescape);
            };
            Sizzle.error = function(msg) {
                throw new Error("Syntax error, unrecognized expression: " + msg);
            };
            Sizzle.uniqueSort = function(results) {
                var elem, duplicates = [],
                    j = 0,
                    i = 0;
                hasDuplicate = !support.detectDuplicates;
                sortInput = !
                    support.sortStable && results.slice(0);
                results.sort(sortOrder);
                if (hasDuplicate) {
                    while ((elem = results[i++])) {
                        if (elem === results[i]) {
                            j = duplicates.push(i);
                        }
                    }
                    while (j--) {
                        results.splice(duplicates[j], 1);
                    }
                }
                sortInput = null;
                return results;
            };
            getText = Sizzle.getText = function(elem) {
                var node, ret = "",
                    i = 0,
                    nodeType = elem.nodeType;
                if (!nodeType) {
                    while ((node = elem[i++])) {
                        ret += getText(node);
                    }
                } else if (nodeType === 1 || nodeType === 9 || nodeType === 11) {
                    if (typeof elem.textContent === "string") {
                        return elem.textContent;
                    } else {
                        for (elem = elem.firstChild; elem; elem = elem.nextSibling) {
                            ret += getText(elem);
                        }
                    }
                } else if (nodeType === 3 || nodeType === 4) {
                    return elem.nodeValue;
                }
                return ret;
            };
            Expr = Sizzle.selectors = {
                cacheLength: 50,
                createPseudo: markFunction,
                match: matchExpr,
                attrHandle: {},
                find: {},
                relative: {
                    ">": {
                        dir: "parentNode",
                        first: !0
                    },
                    " ": {
                        dir: "parentNode"
                    },
                    "+": {
                        dir: "previousSibling",
                        first: !0
                    },
                    "~": {
                        dir: "previousSibling"
                    }
                },
                preFilter: {
                    "ATTR": function(match) {
                        match[1] = match[1].replace(runescape, funescape);
                        match[3] = (match[3] || match[4] || match[5] || "").
                        replace(runescape, funescape);
                        if (match[2] === "~=") {
                            match[3] = " " + match[3] + " ";
                        }
                        return match.slice(0, 4);
                    },
                    "CHILD": function(match) {
                        match[1] = match[1].toLowerCase();
                        if (match[1].slice(0, 3) === "nth") {
                            if (!match[3]) {
                                Sizzle.error(match[0]);
                            }
                            match[4] = +(match[4] ? match[5] + (match[6] || 1) : 2 * (match[3] === "even" || match[3] === "odd"));
                            match[5] = +((match[7] + match[8]) || match[3] === "odd");
                        } else if (match[3]) {
                            Sizzle.error(match[0]);
                        }
                        return match;
                    },
                    "PSEUDO": function(match) {
                        var excess, unquoted = !match[6] && match[2];
                        if (matchExpr["CHILD"].test(match[0])) {
                            return null;
                        }
                        if (match[3]) {
                            match[2] = match[4] || match[5] || "";
                        } else if (unquoted && rpseudo.test(unquoted) && (excess = tokenize(unquoted, true)) && (excess = unquoted.indexOf(")", unquoted.length - excess) - unquoted.length)) {
                            match[0] = match[0].slice(0, excess);
                            match[2] = unquoted.slice(0, excess);
                        }
                        return match.slice(0, 3);
                    }
                },
                filter: {
                    "TAG": function(nodeNameSelector) {
                        var nodeName = nodeNameSelector.replace(runescape, funescape).toLowerCase();
                        return nodeNameSelector === "*" ? function() {
                                return true;
                            } :
                            function(elem) {
                                return elem.nodeName && elem.nodeName.toLowerCase() === nodeName;
                            };
                    },
                    "CLASS": function(className) {
                        var pattern = classCache[className + " "];
                        return pattern || (pattern = new RegExp("(^|" + whitespace + ")" + className + "(" + whitespace + "|$)")) && classCache(className, function(elem) {
                            return pattern.test(typeof elem.className === "string" && elem.className || typeof elem.getAttribute !== "undefined" && elem.getAttribute("class") || "");
                        });
                    },
                    "ATTR": function(name, operator, check) {
                        return function(elem) {
                            var result = Sizzle.attr(elem, name);
                            if (result == null) {
                                return operator === "!=";
                            }
                            if (!operator) {
                                return true;
                            }
                            result += "";
                            return operator === "=" ? result === check : operator === "!=" ? result !== check : operator === "^=" ? check && result.indexOf(check) === 0 : operator === "*=" ? check && result.indexOf(check) > -1 : operator === "$=" ? check && result.slice(-check.length) === check : operator === "~=" ? (" " + result.replace(rwhitespace, " ") + " ").indexOf(check) > -1 : operator === "|=" ? result === check || result.slice(0, check.length + 1) === check + "-" : !1;
                        };
                    },
                    "CHILD": function(type,
                        what, argument, first, last) {
                        var simple = type.slice(0, 3) !== "nth",
                            forward = type.slice(-4) !== "last",
                            ofType = what === "of-type";
                        return first === 1 && last === 0 ? function(elem) {
                            return !!elem.parentNode;
                        } : function(elem, context, xml) {
                            var cache, uniqueCache, outerCache, node, nodeIndex, start, dir = simple !== forward ? "nextSibling" : "previousSibling",
                                parent = elem.parentNode,
                                name = ofType && elem.nodeName.toLowerCase(),
                                useCache = !xml && !ofType,
                                diff = !1;
                            if (parent) {
                                if (simple) {
                                    while (dir) {
                                        node = elem;
                                        while ((node = node[dir])) {
                                            if (ofType ? node.nodeName.toLowerCase() === name : node.nodeType === 1) {
                                                return false;
                                            }
                                        }
                                        start = dir = type === "only" && !start && "nextSibling";
                                    }
                                    return true;
                                }
                                start = [forward ? parent.firstChild : parent.lastChild];
                                if (forward && useCache) {
                                    node = parent;
                                    outerCache = node[expando] || (node[expando] = {});
                                    uniqueCache = outerCache[node.uniqueID] || (outerCache[node.uniqueID] = {});
                                    cache = uniqueCache[type] || [];
                                    nodeIndex = cache[0] === dirruns && cache[1];
                                    diff = nodeIndex && cache[2];
                                    node = nodeIndex && parent.childNodes[nodeIndex];
                                    while ((node = ++nodeIndex && node && node[dir] || (diff = nodeIndex = 0) || start.pop())) {
                                        if (node.nodeType === 1 && ++diff && node === elem) {
                                            uniqueCache[type] = [dirruns, nodeIndex, diff];
                                            break;
                                        }
                                    }
                                } else {
                                    if (useCache) {
                                        node = elem;
                                        outerCache = node[expando] || (node[expando] = {});
                                        uniqueCache = outerCache[node.uniqueID] || (outerCache[node.uniqueID] = {});
                                        cache = uniqueCache[type] || [];
                                        nodeIndex = cache[0] === dirruns && cache[1];
                                        diff = nodeIndex;
                                    }
                                    if (diff === false) {
                                        while ((node = ++nodeIndex && node && node[dir] || (diff = nodeIndex = 0) || start.pop())) {
                                            if ((ofType ? node.nodeName.toLowerCase() === name : node.nodeType === 1) && ++diff) {
                                                if (useCache) {
                                                    outerCache = node[expando] || (node[expando] = {});
                                                    uniqueCache = outerCache[node.uniqueID] || (outerCache[node.uniqueID] = {});
                                                    uniqueCache[type] = [dirruns, diff];
                                                }
                                                if (node === elem) {
                                                    break;
                                                }
                                            }
                                        }
                                    }
                                }
                                diff -= last;
                                return diff === first || (diff % first === 0 && diff / first >= 0);
                            }
                        };
                    },
                    "PSEUDO": function(pseudo, argument) {
                        var args, fn = Expr.pseudos[pseudo] || Expr.setFilters[pseudo.toLowerCase()] || Sizzle.error("unsupported pseudo: " + pseudo);
                        if (fn[expando]) {
                            return fn(argument);
                        }
                        if (fn.length > 1) {
                            args = [pseudo, pseudo, "", argument];
                            return Expr.setFilters.hasOwnProperty(pseudo.toLowerCase()) ? markFunction(function(seed, matches) {
                                var idx, matched = fn(seed, argument),
                                    i = matched.length;
                                while (i--) {
                                    idx = indexOf(seed, matched[i]);
                                    seed[idx] = !(matches[idx] = matched[i]);
                                }
                            }) : function(elem) {
                                return fn(elem, 0, args);
                            };
                        }
                        return fn;
                    }
                },
                pseudos: {
                    "not": markFunction(function(selector) {
                        var input = [],
                            results = [],
                            matcher = compile(selector.replace(rtrim, "$1"));
                        return matcher[expando] ? markFunction(function(seed, matches, context, xml) {
                            var elem, unmatched = matcher(seed, null, xml, []),
                                i = seed.length;
                            while (i--) {
                                if ((elem = unmatched[i])) {
                                    seed[i] = !(matches[i] = elem);
                                }
                            }
                        }) : function(elem, context, xml) {
                            input[0] = elem;
                            matcher(input, null, xml, results);
                            input[0] = null;
                            return !results.pop();
                        };
                    }),
                    "has": markFunction(function(selector) {
                        return function(elem) {
                            return Sizzle(selector, elem).length > 0;
                        };
                    }),
                    "contains": markFunction(function(text) {
                        text = text.replace(runescape, funescape);
                        return function(elem) {
                            return (elem.textContent || elem.innerText || getText(elem)).indexOf(
                                text) > -1;
                        };
                    }),
                    "lang": markFunction(function(lang) {
                        if (!ridentifier.test(lang || "")) {
                            Sizzle.error("unsupported lang: " + lang);
                        }
                        lang = lang.replace(runescape, funescape).toLowerCase();
                        return function(elem) {
                            var elemLang;
                            do {
                                if ((elemLang = documentIsHTML ? elem.lang : elem.getAttribute("xml:lang") || elem.getAttribute("lang"))) {
                                    elemLang = elemLang.toLowerCase();
                                    return elemLang === lang || elemLang.indexOf(lang + "-") === 0;
                                }
                            } while ((elem = elem.parentNode) && elem.nodeType === 1);
                            return false;
                        };
                    }),
                    "target": function(elem) {
                        var hash = window.location && window.location.hash;
                        return hash && hash.slice(1) === elem.id;
                    },
                    "root": function(elem) {
                        return elem === docElem;
                    },
                    "focus": function(elem) {
                        return elem === document.activeElement && (!document.hasFocus || document.hasFocus()) && !!(elem.type || elem.href || ~elem.tabIndex);
                    },
                    "enabled": createDisabledPseudo(false),
                    "disabled": createDisabledPseudo(true),
                    "checked": function(elem) {
                        var nodeName = elem.nodeName.toLowerCase();
                        return (nodeName === "input" && !!elem.checked) || (nodeName === "option" && !!elem.selected);
                    },
                    "selected": function(elem) {
                        if (elem.parentNode) {
                            elem.parentNode.selectedIndex;
                        }
                        return elem.selected === !0;
                    },
                    "empty": function(elem) {
                        for (elem = elem.firstChild; elem; elem = elem.nextSibling) {
                            if (elem.nodeType < 6) {
                                return false;
                            }
                        }
                        return true;
                    },
                    "parent": function(elem) {
                        return !Expr.pseudos["empty"](elem);
                    },
                    "header": function(elem) {
                        return rheader.test(elem.nodeName);
                    },
                    "input": function(elem) {
                        return rinputs.test(elem.nodeName);
                    },
                    "button": function(elem) {
                        var name = elem.nodeName.toLowerCase();
                        return name === "input" && elem.type === "button" || name === "button";
                    },
                    "text": function(elem) {
                        var attr;
                        return elem.nodeName.toLowerCase() === "input" && elem.type === "text" && ((attr = elem.getAttribute("type")) == null || attr.toLowerCase() === "text");
                    },
                    "first": createPositionalPseudo(function() {
                        return [0];
                    }),
                    "last": createPositionalPseudo(function(matchIndexes, length) {
                        return [length - 1];
                    }),
                    "eq": createPositionalPseudo(function(matchIndexes, length, argument) {
                        return [argument < 0 ? argument + length : argument];
                    }),
                    "even": createPositionalPseudo(function(
                        matchIndexes, length) {
                        var i = 0;
                        for (; i < length; i += 2) {
                            matchIndexes.push(i);
                        }
                        return matchIndexes;
                    }),
                    "odd": createPositionalPseudo(function(matchIndexes, length) {
                        var i = 1;
                        for (; i < length; i += 2) {
                            matchIndexes.push(i);
                        }
                        return matchIndexes;
                    }),
                    "lt": createPositionalPseudo(function(matchIndexes, length, argument) {
                        var i = argument < 0 ? argument + length : argument;
                        for (; --i >= 0;) {
                            matchIndexes.push(i);
                        }
                        return matchIndexes;
                    }),
                    "gt": createPositionalPseudo(function(matchIndexes, length, argument) {
                        var i = argument < 0 ? argument + length : argument;
                        for (; ++i < length;) {
                            matchIndexes.push(i);
                        }
                        return matchIndexes;
                    })
                }
            };
            Expr.pseudos["nth"] = Expr.pseudos["eq"];
            for (i in {
                    radio: !0,
                    checkbox: !0,
                    file: !0,
                    password: !0,
                    image: !0
                }) {
                Expr.pseudos[i] = createInputPseudo(i);
            }
            for (i in {
                    submit: !0,
                    reset: !0
                }) {
                Expr.pseudos[i] = createButtonPseudo(i);
            }

            function setFilters() {}
            setFilters.prototype = Expr.filters = Expr.pseudos;
            Expr.setFilters = new setFilters();
            tokenize = Sizzle.tokenize = function(selector, parseOnly) {
                var matched, match, tokens, type, soFar, groups, preFilters,
                    cached = tokenCache[selector + " "];
                if (cached) {
                    return parseOnly ? 0 : cached.slice(0);
                }
                soFar = selector;
                groups = [];
                preFilters = Expr.preFilter;
                while (soFar) {
                    if (!matched || (match = rcomma.exec(soFar))) {
                        if (match) {
                            soFar = soFar.slice(match[0].length) || soFar;
                        }
                        groups.push((tokens = []));
                    }
                    matched = !1;
                    if ((match = rcombinators.exec(soFar))) {
                        matched = match.shift();
                        tokens.push({
                            value: matched,
                            type: match[0].replace(rtrim, " ")
                        });
                        soFar = soFar.slice(matched.length);
                    }
                    for (type in Expr.filter) {
                        if ((match = matchExpr[type].exec(soFar)) && (!preFilters[type] || (match = preFilters[type](match)))) {
                            matched = match.shift();
                            tokens.push({
                                value: matched,
                                type: type,
                                matches: match
                            });
                            soFar = soFar.slice(matched.length);
                        }
                    }
                    if (!matched) {
                        break;
                    }
                }
                return parseOnly ? soFar.length : soFar ? Sizzle.error(selector) : tokenCache(selector, groups).slice(0);
            };

            function toSelector(tokens) {
                var i = 0,
                    len = tokens.length,
                    selector = "";
                for (; i < len; i++) {
                    selector += tokens[i].value;
                }
                return selector;
            }

            function addCombinator(matcher, combinator, base) {
                var dir = combinator.dir,
                    skip = combinator.next,
                    key = skip || dir,
                    checkNonElements = base && key === "parentNode",
                    doneName = done++;
                return combinator.first ? function(elem, context, xml) {
                    while ((elem = elem[dir])) {
                        if (elem.nodeType === 1 || checkNonElements) {
                            return matcher(elem, context, xml);
                        }
                    }
                    return false;
                } : function(elem, context, xml) {
                    var oldCache, uniqueCache, outerCache, newCache = [dirruns, doneName];
                    if (xml) {
                        while ((elem = elem[dir])) {
                            if (elem.nodeType === 1 || checkNonElements) {
                                if (matcher(elem, context, xml)) {
                                    return true;
                                }
                            }
                        }
                    } else {
                        while ((elem = elem[dir])) {
                            if (elem.nodeType === 1 || checkNonElements) {
                                outerCache = elem[expando] || (elem[expando] = {});
                                uniqueCache = outerCache[elem.uniqueID] || (outerCache[elem.uniqueID] = {});
                                if (skip && skip === elem.nodeName.toLowerCase()) {
                                    elem = elem[dir] || elem;
                                } else if ((oldCache = uniqueCache[key]) && oldCache[0] === dirruns && oldCache[1] === doneName) {
                                    return (newCache[2] = oldCache[2]);
                                } else {
                                    uniqueCache[key] = newCache;
                                    if ((newCache[2] = matcher(elem, context, xml))) {
                                        return true;
                                    }
                                }
                            }
                        }
                    }
                    return false;
                };
            }

            function elementMatcher(matchers) {
                return matchers.length > 1 ? function(elem,
                    context, xml) {
                    var i = matchers.length;
                    while (i--) {
                        if (!matchers[i](elem, context, xml)) {
                            return false;
                        }
                    }
                    return true;
                } : matchers[0];
            }

            function multipleContexts(selector, contexts, results) {
                var i = 0,
                    len = contexts.length;
                for (; i < len; i++) {
                    Sizzle(selector, contexts[i], results);
                }
                return results;
            }

            function condense(unmatched, map, filter, context, xml) {
                var elem, newUnmatched = [],
                    i = 0,
                    len = unmatched.length,
                    mapped = map != null;
                for (; i < len; i++) {
                    if ((elem = unmatched[i])) {
                        if (!filter || filter(elem, context, xml)) {
                            newUnmatched.push(elem);
                            if (mapped) {
                                map.push(i);
                            }
                        }
                    }
                }
                return newUnmatched;
            }

            function setMatcher(preFilter, selector, matcher, postFilter, postFinder, postSelector) {
                if (postFilter && !postFilter[expando]) {
                    postFilter = setMatcher(postFilter);
                }
                if (postFinder && !postFinder[expando]) {
                    postFinder = setMatcher(postFinder, postSelector);
                }
                return markFunction(function(seed, results, context, xml) {
                    var temp, i, elem, preMap = [],
                        postMap = [],
                        preexisting = results.length,
                        elems = seed || multipleContexts(selector || "*", context.nodeType ? [context] : context, []),
                        matcherIn =
                        preFilter && (seed || !selector) ? condense(elems, preMap, preFilter, context, xml) : elems,
                        matcherOut = matcher ? postFinder || (seed ? preFilter : preexisting || postFilter) ? [] : results : matcherIn;
                    if (matcher) {
                        matcher(matcherIn, matcherOut, context, xml);
                    }
                    if (postFilter) {
                        temp = condense(matcherOut, postMap);
                        postFilter(temp, [], context, xml);
                        i = temp.length;
                        while (i--) {
                            if ((elem = temp[i])) {
                                matcherOut[postMap[i]] = !(matcherIn[postMap[i]] = elem);
                            }
                        }
                    }
                    if (seed) {
                        if (postFinder || preFilter) {
                            if (postFinder) {
                                temp = [];
                                i = matcherOut.length;
                                while (i--) {
                                    if ((elem = matcherOut[i])) {
                                        temp.push((matcherIn[i] = elem));
                                    }
                                }
                                postFinder(null, (matcherOut = []), temp, xml);
                            }
                            i = matcherOut.length;
                            while (i--) {
                                if ((elem = matcherOut[i]) && (temp = postFinder ? indexOf(seed, elem) : preMap[i]) > -1) {
                                    seed[temp] = !(results[temp] = elem);
                                }
                            }
                        }
                    } else {
                        matcherOut = condense(matcherOut === results ? matcherOut.splice(preexisting, matcherOut.length) : matcherOut);
                        if (postFinder) {
                            postFinder(null, results, matcherOut, xml);
                        } else {
                            push.apply(results, matcherOut);
                        }
                    }
                });
            }

            function matcherFromTokens(tokens) {
                var checkContext,
                    matcher, j, len = tokens.length,
                    leadingRelative = Expr.relative[tokens[0].type],
                    implicitRelative = leadingRelative || Expr.relative[" "],
                    i = leadingRelative ? 1 : 0,
                    matchContext = addCombinator(function(elem) {
                        return elem === checkContext;
                    }, implicitRelative, true),
                    matchAnyContext = addCombinator(function(elem) {
                        return indexOf(checkContext, elem) > -1;
                    }, implicitRelative, true),
                    matchers = [function(elem, context, xml) {
                        var ret = (!leadingRelative && (xml || context !== outermostContext)) || ((checkContext = context).nodeType ? matchContext(elem, context, xml) : matchAnyContext(elem, context, xml));
                        checkContext = null;
                        return ret;
                    }];
                for (; i < len; i++) {
                    if ((matcher = Expr.relative[tokens[i].type])) {
                        matchers = [addCombinator(elementMatcher(matchers), matcher)];
                    } else {
                        matcher = Expr.filter[tokens[i].type].apply(null, tokens[i].matches);
                        if (matcher[expando]) {
                            j = ++i;
                            for (; j < len; j++) {
                                if (Expr.relative[tokens[j].type]) {
                                    break;
                                }
                            }
                            return setMatcher(i > 1 && elementMatcher(matchers), i > 1 && toSelector(tokens.slice(0, i - 1).concat({
                                value: tokens[i - 2].type === " " ? "*" : ""
                            })).replace(
                                rtrim, "$1"), matcher, i < j && matcherFromTokens(tokens.slice(i, j)), j < len && matcherFromTokens((tokens = tokens.slice(j))), j < len && toSelector(tokens));
                        }
                        matchers.push(matcher);
                    }
                }
                return elementMatcher(matchers);
            }

            function matcherFromGroupMatchers(elementMatchers, setMatchers) {
                var bySet = setMatchers.length > 0,
                    byElement = elementMatchers.length > 0,
                    superMatcher = function(seed, context, xml, results, outermost) {
                        var elem, j, matcher, matchedCount = 0,
                            i = "0",
                            unmatched = seed && [],
                            setMatched = [],
                            contextBackup = outermostContext,
                            elems = seed || byElement && Expr.find["TAG"]("*", outermost),
                            dirrunsUnique = (dirruns += contextBackup == null ? 1 : Math.random() || 0.1),
                            len = elems.length;
                        if (outermost) {
                            outermostContext = context === document || context || outermost;
                        }
                        for (; i !== len && (elem = elems[i]) != null; i++) {
                            if (byElement && elem) {
                                j = 0;
                                if (!context && elem.ownerDocument !== document) {
                                    setDocument(elem);
                                    xml = !documentIsHTML;
                                }
                                while ((matcher = elementMatchers[j++])) {
                                    if (matcher(elem, context || document, xml)) {
                                        results.push(elem);
                                        break;
                                    }
                                }
                                if (outermost) {
                                    dirruns = dirrunsUnique;
                                }
                            }
                            if (bySet) {
                                if ((elem = !matcher && elem)) {
                                    matchedCount--;
                                }
                                if (seed) {
                                    unmatched.push(elem);
                                }
                            }
                        }
                        matchedCount += i;
                        if (bySet && i !== matchedCount) {
                            j = 0;
                            while ((matcher = setMatchers[j++])) {
                                matcher(unmatched, setMatched, context, xml);
                            }
                            if (seed) {
                                if (matchedCount > 0) {
                                    while (i--) {
                                        if (!(unmatched[i] || setMatched[i])) {
                                            setMatched[i] = pop.call(results);
                                        }
                                    }
                                }
                                setMatched = condense(setMatched);
                            }
                            push.apply(results, setMatched);
                            if (outermost && !seed && setMatched.length > 0 && (matchedCount + setMatchers.length) > 1) {
                                Sizzle.uniqueSort(results);
                            }
                        }
                        if (outermost) {
                            dirruns = dirrunsUnique;
                            outermostContext = contextBackup;
                        }
                        return unmatched;
                    };
                return bySet ? markFunction(superMatcher) : superMatcher;
            }
            compile = Sizzle.compile = function(selector, match) {
                var i, setMatchers = [],
                    elementMatchers = [],
                    cached = compilerCache[selector + " "];
                if (!cached) {
                    if (!match) {
                        match = tokenize(selector);
                    }
                    i = match.length;
                    while (i--) {
                        cached = matcherFromTokens(match[i]);
                        if (cached[expando]) {
                            setMatchers.push(cached);
                        } else {
                            elementMatchers.push(cached);
                        }
                    }
                    cached = compilerCache(selector, matcherFromGroupMatchers(
                        elementMatchers, setMatchers));
                    cached.selector = selector;
                }
                return cached;
            };
            select = Sizzle.select = function(selector, context, results, seed) {
                var i, tokens, token, type, find, compiled = typeof selector === "function" && selector,
                    match = !seed && tokenize((selector = compiled.selector || selector));
                results = results || [];
                if (match.length === 1) {
                    tokens = match[0] = match[0].slice(0);
                    if (tokens.length > 2 && (token = tokens[0]).type === "ID" && context.nodeType === 9 && documentIsHTML && Expr.relative[tokens[1].type]) {
                        context = (Expr.find["ID"](token.matches[0].replace(runescape, funescape), context) || [])[0];
                        if (!context) {
                            return results;
                        } else if (compiled) {
                            context = context.parentNode;
                        }
                        selector = selector.slice(tokens.shift().value.length);
                    }
                    i = matchExpr["needsContext"].test(selector) ? 0 : tokens.length;
                    while (i--) {
                        token = tokens[i];
                        if (Expr.relative[(type = token.type)]) {
                            break;
                        }
                        if ((find = Expr.find[type])) {
                            if ((seed = find(token.matches[0].replace(runescape, funescape), rsibling.test(tokens[0].type) && testContext(context.parentNode) || context))) {
                                tokens.splice(i, 1);
                                selector = seed.length && toSelector(tokens);
                                if (!selector) {
                                    push.apply(results, seed);
                                    return results;
                                }
                                break;
                            }
                        }
                    }
                }(compiled || compile(selector, match))(seed, context, !documentIsHTML, results, !context || rsibling.test(selector) && testContext(context.parentNode) || context);
                return results;
            };
            support.sortStable = expando.split("").sort(sortOrder).join("") === expando;
            support.detectDuplicates = !!hasDuplicate;
            setDocument();
            support.sortDetached = assert(function(el) {
                return el.compareDocumentPosition(document.createElement("fieldset")) & 1;
            });
            if (!assert(function(el) {
                    el.innerHTML = "<a href='#'></a>";
                    return el.firstChild.getAttribute("href") === "#";
                })) {
                addHandle("type|href|height|width", function(elem, name, isXML) {
                    if (!isXML) {
                        return elem.getAttribute(name, name.toLowerCase() === "type" ? 1 : 2);
                    }
                });
            }
            if (!support.attributes || !assert(function(el) {
                    el.innerHTML = "<input/>";
                    el.firstChild.setAttribute("value", "");
                    return el.firstChild.getAttribute("value") === "";
                })) {
                addHandle("value", function(elem, name, isXML) {
                    if (!isXML && elem.nodeName.toLowerCase() === "input") {
                        return elem.defaultValue;
                    }
                });
            }
            if (!assert(function(el) {
                    return el.getAttribute("disabled") == null;
                })) {
                addHandle(booleans, function(elem, name, isXML) {
                    var val;
                    if (!isXML) {
                        return elem[name] === !0 ? name.toLowerCase() : (val = elem.getAttributeNode(name)) && val.specified ? val.value : null;
                    }
                });
            }
            return Sizzle;
        })(window);
        jQuery.find = Sizzle;
        jQuery.expr = Sizzle.selectors;
        jQuery.expr[":"] = jQuery.expr.pseudos;
        jQuery.uniqueSort = jQuery.unique = Sizzle.uniqueSort;
        jQuery.text = Sizzle.getText;
        jQuery.isXMLDoc = Sizzle.isXML;
        jQuery.contains = Sizzle.contains;
        jQuery.escapeSelector = Sizzle.escape;
        var dir = function(elem, dir, until) {
            var matched = [],
                truncate = until !== undefined;
            while ((elem = elem[dir]) && elem.nodeType !== 9) {
                if (elem.nodeType === 1) {
                    if (truncate && jQuery(elem).is(until)) {
                        break;
                    }
                    matched.push(elem);
                }
            }
            return matched;
        };
        var siblings = function(n, elem) {
            var matched = [];
            for (; n; n = n.nextSibling) {
                if (n.nodeType === 1 && n !== elem) {
                    matched.push(n);
                }
            }
            return matched;
        };
        var rneedsContext = jQuery.expr.match.needsContext;

        function
        nodeName(elem, name) {
            return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
        };
        var rsingleTag = (/^<([a-z][^\/\0>:\x20\t\r\n\f]*)[\x20\t\r\n\f]*\/?>(?:<\/\1>|)$/i);

        function winnow(elements, qualifier, not) {
            if (isFunction(qualifier)) {
                return jQuery.grep(elements, function(elem, i) {
                    return !!qualifier.call(elem, i, elem) !== not;
                });
            }
            if (qualifier.nodeType) {
                return jQuery.grep(elements, function(elem) {
                    return (elem === qualifier) !== not;
                });
            }
            if (typeof qualifier !== "string") {
                return jQuery.grep(elements, function(elem) {
                    return (indexOf.call(qualifier, elem) > -1) !== not;
                });
            }
            return jQuery.filter(qualifier, elements, not);
        }
        jQuery.filter = function(expr, elems, not) {
            var elem = elems[0];
            if (not) {
                expr = ":not(" + expr + ")";
            }
            if (elems.length === 1 && elem.nodeType === 1) {
                return jQuery.find.matchesSelector(elem, expr) ? [elem] : [];
            }
            return jQuery.find.matches(expr, jQuery.grep(elems, function(elem) {
                return elem.nodeType === 1;
            }));
        };
        jQuery.fn.extend({
            find: function(selector) {
                var i, ret, len = this.length,
                    self = this;
                if (typeof selector !== "string") {
                    return this.pushStack(jQuery(selector).filter(function() {
                        for (i = 0; i < len; i++) {
                            if (jQuery.contains(self[i], this)) {
                                return true;
                            }
                        }
                    }));
                }
                ret = this.pushStack([]);
                for (i = 0; i < len; i++) {
                    jQuery.find(selector, self[i], ret);
                }
                return len > 1 ? jQuery.uniqueSort(ret) : ret;
            },
            filter: function(selector) {
                return this.pushStack(winnow(this, selector || [], false));
            },
            not: function(selector) {
                return this.pushStack(winnow(this, selector || [], true));
            },
            is: function(selector) {
                return !!winnow(this, typeof selector === "string" && rneedsContext.test(selector) ? jQuery(selector) : selector || [], false).length;
            }
        });
        var rootjQuery, rquickExpr = /^(?:\s*(<[\w\W]+>)[^>]*|#([\w-]+))$/,
            init = jQuery.fn.init = function(selector, context, root) {
                var match, elem;
                if (!selector) {
                    return this;
                }
                root = root || rootjQuery;
                if (typeof selector === "string") {
                    if (selector[0] === "<" && selector[selector.length - 1] === ">" && selector.length >= 3) {
                        match = [null, selector, null];
                    } else {
                        match = rquickExpr.exec(selector);
                    }
                    if (match && (match[1] || !context)) {
                        if (match[1]) {
                            context = context instanceof jQuery ? context[0] :
                                context;
                            jQuery.merge(this, jQuery.parseHTML(match[1], context && context.nodeType ? context.ownerDocument || context : document, true));
                            if (rsingleTag.test(match[1]) && jQuery.isPlainObject(context)) {
                                for (match in context) {
                                    if (isFunction(this[match])) {
                                        this[match](context[match]);
                                    } else {
                                        this.attr(match, context[match]);
                                    }
                                }
                            }
                            return this;
                        } else {
                            elem = document.getElementById(match[2]);
                            if (elem) {
                                this[0] = elem;
                                this.length = 1;
                            }
                            return this;
                        }
                    } else if (!context || context.jquery) {
                        return (context || root).find(selector);
                    } else {
                        return this.constructor(context).find(selector);
                    }
                } else if (selector.nodeType) {
                    this[0] = selector;
                    this.length = 1;
                    return this;
                } else if (isFunction(selector)) {
                    return root.ready !== undefined ? root.ready(selector) : selector(jQuery);
                }
                return jQuery.makeArray(selector, this);
            };
        init.prototype = jQuery.fn;
        rootjQuery = jQuery(document);
        var rparentsprev = /^(?:parents|prev(?:Until|All))/,
            guaranteedUnique = {
                children: !0,
                contents: !0,
                next: !0,
                prev: !0
            };
        jQuery.fn.extend({
            has: function(target) {
                var targets = jQuery(target, this),
                    l =
                    targets.length;
                return this.filter(function() {
                    var i = 0;
                    for (; i < l; i++) {
                        if (jQuery.contains(this, targets[i])) {
                            return true;
                        }
                    }
                });
            },
            closest: function(selectors, context) {
                var cur, i = 0,
                    l = this.length,
                    matched = [],
                    targets = typeof selectors !== "string" && jQuery(selectors);
                if (!rneedsContext.test(selectors)) {
                    for (; i < l; i++) {
                        for (cur = this[i]; cur && cur !== context; cur = cur.parentNode) {
                            if (cur.nodeType < 11 && (targets ? targets.index(cur) > -1 : cur.nodeType === 1 && jQuery.find.matchesSelector(cur, selectors))) {
                                matched.push(cur);
                                break;
                            }
                        }
                    }
                }
                return this.pushStack(matched.length > 1 ? jQuery.uniqueSort(matched) : matched);
            },
            index: function(elem) {
                if (!elem) {
                    return (this[0] && this[0].parentNode) ? this.first().prevAll().length : -1;
                }
                if (typeof elem === "string") {
                    return indexOf.call(jQuery(elem), this[0]);
                }
                return indexOf.call(this, elem.jquery ? elem[0] : elem);
            },
            add: function(selector, context) {
                return this.pushStack(jQuery.uniqueSort(jQuery.merge(this.get(), jQuery(selector, context))));
            },
            addBack: function(selector) {
                return this.add(selector == null ? this.prevObject :
                    this.prevObject.filter(selector));
            }
        });

        function sibling(cur, dir) {
            while ((cur = cur[dir]) && cur.nodeType !== 1) {}
            return cur;
        }
        jQuery.each({
            parent: function(elem) {
                var parent = elem.parentNode;
                return parent && parent.nodeType !== 11 ? parent : null;
            },
            parents: function(elem) {
                return dir(elem, "parentNode");
            },
            parentsUntil: function(elem, i, until) {
                return dir(elem, "parentNode", until);
            },
            next: function(elem) {
                return sibling(elem, "nextSibling");
            },
            prev: function(elem) {
                return sibling(elem, "previousSibling");
            },
            nextAll: function(elem) {
                return dir(elem, "nextSibling");
            },
            prevAll: function(elem) {
                return dir(elem, "previousSibling");
            },
            nextUntil: function(elem, i, until) {
                return dir(elem, "nextSibling", until);
            },
            prevUntil: function(elem, i, until) {
                return dir(elem, "previousSibling", until);
            },
            siblings: function(elem) {
                return siblings((elem.parentNode || {}).firstChild, elem);
            },
            children: function(elem) {
                return siblings(elem.firstChild);
            },
            contents: function(elem) {
                if (nodeName(elem, "iframe")) {
                    return elem.contentDocument;
                }
                if (nodeName(elem, "template")) {
                    elem = elem
                        .content || elem;
                }
                return jQuery.merge([], elem.childNodes);
            }
        }, function(name, fn) {
            jQuery.fn[name] = function(until, selector) {
                var matched = jQuery.map(this, fn, until);
                if (name.slice(-5) !== "Until") {
                    selector = until;
                }
                if (selector && typeof selector === "string") {
                    matched = jQuery.filter(selector, matched);
                }
                if (this.length > 1) {
                    if (!guaranteedUnique[name]) {
                        jQuery.uniqueSort(matched);
                    }
                    if (rparentsprev.test(name)) {
                        matched.reverse();
                    }
                }
                return this.pushStack(matched);
            };
        });
        var rnothtmlwhite = (/[^\x20\t\r\n\f]+/g);

        function createOptions(options) {
            var object = {};
            jQuery.each(options.match(rnothtmlwhite) || [], function(_, flag) {
                object[flag] = !0;
            });
            return object;
        }
        jQuery.Callbacks = function(options) {
            options = typeof options === "string" ? createOptions(options) : jQuery.extend({}, options);
            var firing, memory, fired, locked, list = [],
                queue = [],
                firingIndex = -1,
                fire = function() {
                    locked = locked || options.once;
                    fired = firing = !0;
                    for (; queue.length; firingIndex = -1) {
                        memory = queue.shift();
                        while (++firingIndex < list.length) {
                            if (list[firingIndex].apply(memory[0], memory[
                                    1]) === false && options.stopOnFalse) {
                                firingIndex = list.length;
                                memory = !1;
                            }
                        }
                    }
                    if (!options.memory) {
                        memory = !1;
                    }
                    firing = !1;
                    if (locked) {
                        if (memory) {
                            list = [];
                        } else {
                            list = "";
                        }
                    }
                },
                self = {
                    add: function() {
                        if (list) {
                            if (memory && !firing) {
                                firingIndex = list.length - 1;
                                queue.push(memory);
                            }(function add(args) {
                                jQuery.each(args, function(_, arg) {
                                    if (isFunction(arg)) {
                                        if (!options.unique || !self.has(arg)) {
                                            list.push(arg);
                                        }
                                    } else if (arg && arg.length && toType(arg) !== "string") {
                                        add(arg);
                                    }
                                });
                            })(arguments);
                            if (memory && !firing) {
                                fire();
                            }
                        }
                        return this;
                    },
                    remove: function() {
                        jQuery.each(arguments, function(_, arg) {
                            var index;
                            while ((index = jQuery.inArray(arg, list, index)) > -1) {
                                list.splice(index, 1);
                                if (index <= firingIndex) {
                                    firingIndex--;
                                }
                            }
                        });
                        return this;
                    },
                    has: function(fn) {
                        return fn ? jQuery.inArray(fn, list) > -1 : list.length > 0;
                    },
                    empty: function() {
                        if (list) {
                            list = [];
                        }
                        return this;
                    },
                    disable: function() {
                        locked = queue = [];
                        list = memory = "";
                        return this;
                    },
                    disabled: function() {
                        return !list;
                    },
                    lock: function() {
                        locked = queue = [];
                        if (!memory && !firing) {
                            list = memory = "";
                        }
                        return this;
                    },
                    locked: function() {
                        return !!locked;
                    },
                    fireWith: function(context, args) {
                        if (!locked) {
                            args = args || [];
                            args = [context, args.slice ? args.slice() : args];
                            queue.push(args);
                            if (!firing) {
                                fire();
                            }
                        }
                        return this;
                    },
                    fire: function() {
                        self.fireWith(this, arguments);
                        return this;
                    },
                    fired: function() {
                        return !!fired;
                    }
                };
            return self;
        };

        function Identity(v) {
            return v;
        }

        function Thrower(ex) {
            throw ex;
        }

        function adoptValue(value, resolve, reject, noValue) {
            var method;
            try {
                if (value && isFunction((method = value.promise))) {
                    method.call(value).done(resolve).fail(reject);
                } else if (value && isFunction((method = value.then))) {
                    method.call(value, resolve, reject);
                } else {
                    resolve.apply(undefined, [value].slice(noValue));
                }
            } catch (value) {
                reject.apply(undefined, [value]);
            }
        }
        jQuery.extend({
            Deferred: function(func) {
                var tuples = [
                        ["notify", "progress", jQuery.Callbacks("memory"), jQuery.Callbacks("memory"), 2],
                        ["resolve", "done", jQuery.Callbacks("once memory"), jQuery.Callbacks("once memory"), 0, "resolved"],
                        ["reject", "fail", jQuery.Callbacks("once memory"), jQuery.Callbacks(
                            "once memory"), 1, "rejected"]
                    ],
                    state = "pending",
                    promise = {
                        state: function() {
                            return state;
                        },
                        always: function() {
                            deferred.done(arguments).fail(arguments);
                            return this;
                        },
                        "catch": function(fn) {
                            return promise.then(null, fn);
                        },
                        pipe: function() {
                            var fns = arguments;
                            return jQuery.Deferred(function(newDefer) {
                                jQuery.each(tuples, function(i, tuple) {
                                    var fn = isFunction(fns[tuple[4]]) && fns[tuple[4]];
                                    deferred[tuple[1]](function() {
                                        var returned = fn && fn.apply(this, arguments);
                                        if (returned && isFunction(returned.promise)) {
                                            returned.promise().progress(newDefer.notify).done(newDefer.resolve).fail(newDefer.reject);
                                        } else {
                                            newDefer[tuple[0] + "With"](this, fn ? [returned] : arguments);
                                        }
                                    });
                                });
                                fns = null;
                            }).promise();
                        },
                        then: function(onFulfilled, onRejected, onProgress) {
                            var maxDepth = 0;

                            function resolve(depth, deferred, handler, special) {
                                return function() {
                                    var that = this,
                                        args = arguments,
                                        mightThrow = function() {
                                            var returned, then;
                                            if (depth < maxDepth) {
                                                return;
                                            }
                                            returned = handler.apply(that, args);
                                            if (returned === deferred.promise()) {
                                                throw new TypeError(
                                                    "Thenable self-resolution");
                                            }
                                            then = returned && (typeof returned === "object" || typeof returned === "function") && returned.then;
                                            if (isFunction(then)) {
                                                if (special) {
                                                    then.call(returned, resolve(maxDepth, deferred, Identity, special), resolve(maxDepth, deferred, Thrower, special));
                                                } else {
                                                    maxDepth++;
                                                    then.call(returned, resolve(maxDepth, deferred, Identity, special), resolve(maxDepth, deferred, Thrower, special), resolve(maxDepth, deferred, Identity, deferred.notifyWith));
                                                }
                                            } else {
                                                if (handler !== Identity) {
                                                    that = undefined;
                                                    args = [returned];
                                                }(special || deferred.resolveWith)(that, args);
                                            }
                                        },
                                        process = special ? mightThrow : function() {
                                            try {
                                                mightThrow();
                                            } catch (e) {
                                                if (jQuery.Deferred.exceptionHook) {
                                                    jQuery.Deferred.exceptionHook(e, process.stackTrace);
                                                }
                                                if (depth + 1 >= maxDepth) {
                                                    if (handler !== Thrower) {
                                                        that = undefined;
                                                        args = [e];
                                                    }
                                                    deferred.rejectWith(that, args);
                                                }
                                            }
                                        };
                                    if (depth) {
                                        process();
                                    } else {
                                        if (jQuery.Deferred.getStackHook) {
                                            process.stackTrace = jQuery.Deferred.getStackHook();
                                        }
                                        window.setTimeout(process);
                                    }
                                };
                            }
                            return jQuery.Deferred(function(newDefer) {
                                tuples[0][3].
                                add(resolve(0, newDefer, isFunction(onProgress) ? onProgress : Identity, newDefer.notifyWith));
                                tuples[1][3].add(resolve(0, newDefer, isFunction(onFulfilled) ? onFulfilled : Identity));
                                tuples[2][3].add(resolve(0, newDefer, isFunction(onRejected) ? onRejected : Thrower));
                            }).promise();
                        },
                        promise: function(obj) {
                            return obj != null ? jQuery.extend(obj, promise) : promise;
                        }
                    },
                    deferred = {};
                jQuery.each(tuples, function(i, tuple) {
                    var list = tuple[2],
                        stateString = tuple[5];
                    promise[tuple[1]] = list.add;
                    if (stateString) {
                        list.add(function() {
                            state = stateString;
                        }, tuples[3 - i][2].disable, tuples[3 - i][3].disable, tuples[0][2].lock, tuples[0][3].lock);
                    }
                    list.add(tuple[3].fire);
                    deferred[tuple[0]] = function() {
                        deferred[tuple[0] + "With"](this === deferred ? undefined : this, arguments);
                        return this;
                    };
                    deferred[tuple[0] + "With"] = list.fireWith;
                });
                promise.promise(deferred);
                if (func) {
                    func.call(deferred, deferred);
                }
                return deferred;
            },
            when: function(singleValue) {
                var remaining = arguments.length,
                    i = remaining,
                    resolveContexts = Array(i),
                    resolveValues = slice.call(arguments),
                    master =
                    jQuery.Deferred(),
                    updateFunc = function(i) {
                        return function(value) {
                            resolveContexts[i] = this;
                            resolveValues[i] = arguments.length > 1 ? slice.call(arguments) : value;
                            if (!(--remaining)) {
                                master.resolveWith(resolveContexts, resolveValues);
                            }
                        };
                    };
                if (remaining <= 1) {
                    adoptValue(singleValue, master.done(updateFunc(i)).resolve, master.reject, !remaining);
                    if (master.state() === "pending" || isFunction(resolveValues[i] && resolveValues[i].then)) {
                        return master.then();
                    }
                }
                while (i--) {
                    adoptValue(resolveValues[i], updateFunc(i), master.reject);
                }
                return master.promise();
            }
        });
        var rerrorNames = /^(Eval|Internal|Range|Reference|Syntax|Type|URI)Error$/;
        jQuery.Deferred.exceptionHook = function(error, stack) {
            if (window.console && window.console.warn && error && rerrorNames.test(error.name)) {
                window.console.warn("jQuery.Deferred exception: " + error.message, error.stack, stack);
            }
        };
        jQuery.readyException = function(error) {
            window.setTimeout(function() {
                throw error;
            });
        };
        var readyList = jQuery.Deferred();
        jQuery.fn.ready = function(fn) {
            readyList.then(fn).catch(function(
                error) {
                jQuery.readyException(error);
            });
            return this;
        };
        jQuery.extend({
            isReady: !1,
            readyWait: 1,
            ready: function(wait) {
                if (wait === true ? --jQuery.readyWait : jQuery.isReady) {
                    return;
                }
                jQuery.isReady = !0;
                if (wait !== true && --jQuery.readyWait > 0) {
                    return;
                }
                readyList.resolveWith(document, [jQuery]);
            }
        });
        jQuery.ready.then = readyList.then;

        function completed() {
            document.removeEventListener("DOMContentLoaded", completed);
            window.removeEventListener("load", completed);
            jQuery.ready();
        }
        if (document.readyState === "complete" || (document.readyState !== "loading" && !document.documentElement.doScroll)) {
            window.setTimeout(jQuery.ready);
        } else {
            document.addEventListener("DOMContentLoaded", completed);
            window.addEventListener("load", completed);
        }
        var access = function(elems, fn, key, value, chainable, emptyGet, raw) {
            var i = 0,
                len = elems.length,
                bulk = key == null;
            if (toType(key) === "object") {
                chainable = !0;
                for (i in key) {
                    access(elems, fn, i, key[i], true, emptyGet, raw);
                }
            } else if (value !== undefined) {
                chainable = !0;
                if (!isFunction(value)) {
                    raw = !0;
                }
                if (bulk) {
                    if (raw) {
                        fn.call(elems, value);
                        fn = null;
                    } else {
                        bulk = fn;
                        fn = function(elem, key, value) {
                            return bulk.call(jQuery(elem), value);
                        };
                    }
                }
                if (fn) {
                    for (; i < len; i++) {
                        fn(elems[i], key, raw ? value : value.call(elems[i], i, fn(elems[i], key)));
                    }
                }
            }
            if (chainable) {
                return elems;
            }
            if (bulk) {
                return fn.call(elems);
            }
            return len ? fn(elems[0], key) : emptyGet;
        };
        var rmsPrefix = /^-ms-/,
            rdashAlpha = /-([a-z])/g;

        function fcamelCase(all, letter) {
            return letter.toUpperCase();
        }

        function camelCase(string) {
            return string.replace(rmsPrefix, "ms-").replace(rdashAlpha, fcamelCase);
        }
        var acceptData = function(owner) {
            return owner.nodeType === 1 || owner.nodeType === 9 || !(+owner.nodeType);
        };

        function Data() {
            this.expando = jQuery.expando + Data.uid++;
        }
        Data.uid = 1;
        Data.prototype = {
            cache: function(owner) {
                var value = owner[this.expando];
                if (!value) {
                    value = {};
                    if (acceptData(owner)) {
                        if (owner.nodeType) {
                            owner[this.expando] = value;
                        } else {
                            Object.defineProperty(owner, this.expando, {
                                value: value,
                                configurable: !0
                            });
                        }
                    }
                }
                return value;
            },
            set: function(owner, data, value) {
                var prop, cache = this.cache(owner);
                if (typeof data === "string") {
                    cache[camelCase(data)] = value;
                } else {
                    for (prop in data) {
                        cache[camelCase(prop)] = data[prop];
                    }
                }
                return cache;
            },
            get: function(owner, key) {
                return key === undefined ? this.cache(owner) : owner[this.expando] && owner[this.expando][camelCase(key)];
            },
            access: function(owner, key, value) {
                if (key === undefined || ((key && typeof key === "string") && value === undefined)) {
                    return this.get(owner, key);
                }
                this.set(owner, key, value);
                return value !== undefined ? value : key;
            },
            remove: function(owner, key) {
                var i, cache = owner[this.expando];
                if (cache === undefined) {
                    return;
                }
                if (key !== undefined) {
                    if (Array.isArray(key)) {
                        key = key.map(camelCase);
                    } else {
                        key = camelCase(key);
                        key = key in cache ? [key] : (key.match(rnothtmlwhite) || []);
                    }
                    i = key.length;
                    while (i--) {
                        delete cache[key[i]];
                    }
                }
                if (key === undefined || jQuery.isEmptyObject(cache)) {
                    if (owner.nodeType) {
                        owner[this.expando] = undefined;
                    } else {
                        delete owner[this.expando];
                    }
                }
            },
            hasData: function(owner) {
                var cache = owner[this.expando];
                return cache !== undefined && !jQuery.isEmptyObject(cache);
            }
        };
        var dataPriv = new Data();
        var
            dataUser = new Data();
        var rbrace = /^(?:\{[\w\W]*\}|\[[\w\W]*\])$/,
            rmultiDash = /[A-Z]/g;

        function getData(data) {
            if (data === "true") {
                return true;
            }
            if (data === "false") {
                return false;
            }
            if (data === "null") {
                return null;
            }
            if (data === +data + "") {
                return +data;
            }
            if (rbrace.test(data)) {
                return JSON.parse(data);
            }
            return data;
        }

        function dataAttr(elem, key, data) {
            var name;
            if (data === undefined && elem.nodeType === 1) {
                name = "data-" + key.replace(rmultiDash, "-$&").toLowerCase();
                data = elem.getAttribute(name);
                if (typeof data === "string") {
                    try {
                        data = getData(data);
                    } catch (e) {}
                    dataUser.set(elem, key, data);
                } else {
                    data = undefined;
                }
            }
            return data;
        }
        jQuery.extend({
            hasData: function(elem) {
                return dataUser.hasData(elem) || dataPriv.hasData(elem);
            },
            data: function(elem, name, data) {
                return dataUser.access(elem, name, data);
            },
            removeData: function(elem, name) {
                dataUser.remove(elem, name);
            },
            _data: function(elem, name, data) {
                return dataPriv.access(elem, name, data);
            },
            _removeData: function(elem, name) {
                dataPriv.remove(elem, name);
            }
        });
        jQuery.fn.extend({
            data: function(key, value) {
                var i, name, data, elem = this[0],
                    attrs = elem && elem.attributes;
                if (key === undefined) {
                    if (this.length) {
                        data = dataUser.get(elem);
                        if (elem.nodeType === 1 && !dataPriv.get(elem, "hasDataAttrs")) {
                            i = attrs.length;
                            while (i--) {
                                if (attrs[i]) {
                                    name = attrs[i].name;
                                    if (name.indexOf("data-") === 0) {
                                        name = camelCase(name.slice(5));
                                        dataAttr(elem, name, data[name]);
                                    }
                                }
                            }
                            dataPriv.set(elem, "hasDataAttrs", true);
                        }
                    }
                    return data;
                }
                if (typeof key === "object") {
                    return this.each(function() {
                        dataUser.set(this, key);
                    });
                }
                return access(this, function(value) {
                    var data;
                    if (elem && value === undefined) {
                        data = dataUser.get(elem, key);
                        if (data !== undefined) {
                            return data;
                        }
                        data = dataAttr(elem, key);
                        if (data !== undefined) {
                            return data;
                        }
                        return;
                    }
                    this.each(function() {
                        dataUser.set(this, key, value);
                    });
                }, null, value, arguments.length > 1, null, true);
            },
            removeData: function(key) {
                return this.each(function() {
                    dataUser.remove(this, key);
                });
            }
        });
        jQuery.extend({
            queue: function(elem, type, data) {
                var queue;
                if (elem) {
                    type = (type || "fx") + "queue";
                    queue = dataPriv.get(elem, type);
                    if (data) {
                        if (!queue || Array.isArray(data)) {
                            queue = dataPriv.access(elem, type, jQuery.makeArray(data));
                        } else {
                            queue.push(data);
                        }
                    }
                    return queue || [];
                }
            },
            dequeue: function(elem, type) {
                type = type || "fx";
                var queue = jQuery.queue(elem, type),
                    startLength = queue.length,
                    fn = queue.shift(),
                    hooks = jQuery._queueHooks(elem, type),
                    next = function() {
                        jQuery.dequeue(elem, type);
                    };
                if (fn === "inprogress") {
                    fn = queue.shift();
                    startLength--;
                }
                if (fn) {
                    if (type === "fx") {
                        queue.unshift("inprogress");
                    }
                    delete hooks.stop;
                    fn.call(elem, next, hooks);
                }
                if (!startLength && hooks) {
                    hooks.empty.fire();
                }
            },
            _queueHooks: function(elem, type) {
                var key = type + "queueHooks";
                return dataPriv.get(elem, key) || dataPriv.access(elem, key, {
                    empty: jQuery.Callbacks("once memory").add(function() {
                        dataPriv.remove(elem, [type + "queue", key]);
                    })
                });
            }
        });
        jQuery.fn.extend({
            queue: function(type, data) {
                var setter = 2;
                if (typeof type !== "string") {
                    data = type;
                    type = "fx";
                    setter--;
                }
                if (arguments.length < setter) {
                    return jQuery.queue(this[0], type);
                }
                return data === undefined ? this : this.each(function() {
                    var queue = jQuery.queue(this, type, data);
                    jQuery._queueHooks(
                        this, type);
                    if (type === "fx" && queue[0] !== "inprogress") {
                        jQuery.dequeue(this, type);
                    }
                });
            },
            dequeue: function(type) {
                return this.each(function() {
                    jQuery.dequeue(this, type);
                });
            },
            clearQueue: function(type) {
                return this.queue(type || "fx", []);
            },
            promise: function(type, obj) {
                var tmp, count = 1,
                    defer = jQuery.Deferred(),
                    elements = this,
                    i = this.length,
                    resolve = function() {
                        if (!(--count)) {
                            defer.resolveWith(elements, [elements]);
                        }
                    };
                if (typeof type !== "string") {
                    obj = type;
                    type = undefined;
                }
                type = type || "fx";
                while (i--) {
                    tmp = dataPriv.get(elements[i], type + "queueHooks");
                    if (tmp && tmp.empty) {
                        count++;
                        tmp.empty.add(resolve);
                    }
                }
                resolve();
                return defer.promise(obj);
            }
        });
        var pnum = (/[+-]?(?:\d*\.|)\d+(?:[eE][+-]?\d+|)/).source;
        var rcssNum = new RegExp("^(?:([+-])=|)(" + pnum + ")([a-z%]*)$", "i");
        var cssExpand = ["Top", "Right", "Bottom", "Left"];
        var isHiddenWithinTree = function(elem, el) {
            elem = el || elem;
            return elem.style.display === "none" || elem.style.display === "" && jQuery.contains(elem.ownerDocument, elem) && jQuery.css(elem, "display") === "none";
        };
        var swap = function(
            elem, options, callback, args) {
            var ret, name, old = {};
            for (name in options) {
                old[name] = elem.style[name];
                elem.style[name] = options[name];
            }
            ret = callback.apply(elem, args || []);
            for (name in options) {
                elem.style[name] = old[name];
            }
            return ret;
        };

        function adjustCSS(elem, prop, valueParts, tween) {
            var adjusted, scale, maxIterations = 20,
                currentValue = tween ? function() {
                    return tween.cur();
                } : function() {
                    return jQuery.css(elem, prop, "");
                },
                initial = currentValue(),
                unit = valueParts && valueParts[3] || (jQuery.cssNumber[prop] ? "" : "px"),
                initialInUnit = (jQuery.cssNumber[prop] || unit !== "px" && +initial) && rcssNum.exec(jQuery.css(elem, prop));
            if (initialInUnit && initialInUnit[3] !== unit) {
                initial = initial / 2;
                unit = unit || initialInUnit[3];
                initialInUnit = +initial || 1;
                while (maxIterations--) {
                    jQuery.style(elem, prop, initialInUnit + unit);
                    if ((1 - scale) * (1 - (scale = currentValue() / initial || 0.5)) <= 0) {
                        maxIterations = 0;
                    }
                    initialInUnit = initialInUnit / scale;
                }
                initialInUnit = initialInUnit * 2;
                jQuery.style(elem, prop, initialInUnit + unit);
                valueParts = valueParts || [];
            }
            if (valueParts) {
                initialInUnit = +initialInUnit || +initial || 0;
                adjusted = valueParts[1] ? initialInUnit + (valueParts[1] + 1) * valueParts[2] : +valueParts[2];
                if (tween) {
                    tween.unit = unit;
                    tween.start = initialInUnit;
                    tween.end = adjusted;
                }
            }
            return adjusted;
        }
        var defaultDisplayMap = {};

        function getDefaultDisplay(elem) {
            var temp, doc = elem.ownerDocument,
                nodeName = elem.nodeName,
                display = defaultDisplayMap[nodeName];
            if (display) {
                return display;
            }
            temp = doc.body.appendChild(doc.createElement(nodeName));
            display = jQuery.css(temp, "display");
            temp.parentNode.removeChild(temp);
            if (display === "none") {
                display = "block";
            }
            defaultDisplayMap[nodeName] = display;
            return display;
        }

        function showHide(elements, show) {
            var display, elem, values = [],
                index = 0,
                length = elements.length;
            for (; index < length; index++) {
                elem = elements[index];
                if (!elem.style) {
                    continue;
                }
                display = elem.style.display;
                if (show) {
                    if (display === "none") {
                        values[index] = dataPriv.get(elem, "display") || null;
                        if (!values[index]) {
                            elem.style.display = "";
                        }
                    }
                    if (elem.style.display === "" && isHiddenWithinTree(elem)) {
                        values[index] =
                            getDefaultDisplay(elem);
                    }
                } else {
                    if (display !== "none") {
                        values[index] = "none";
                        dataPriv.set(elem, "display", display);
                    }
                }
            }
            for (index = 0; index < length; index++) {
                if (values[index] != null) {
                    elements[index].style.display = values[index];
                }
            }
            return elements;
        }
        jQuery.fn.extend({
            show: function() {
                return showHide(this, true);
            },
            hide: function() {
                return showHide(this);
            },
            toggle: function(state) {
                if (typeof state === "boolean") {
                    return state ? this.show() : this.hide();
                }
                return this.each(function() {
                    if (isHiddenWithinTree(this)) {
                        jQuery(this).show();
                    } else {
                        jQuery(this).hide();
                    }
                });
            }
        });
        var rcheckableType = (/^(?:checkbox|radio)$/i);
        var rtagName = (/<([a-z][^\/\0>\x20\t\r\n\f]+)/i);
        var rscriptType = (/^$|^module$|\/(?:java|ecma)script/i);
        var wrapMap = {
            option: [1, "<select multiple='multiple'>", "</select>"],
            thead: [1, "<table>", "</table>"],
            col: [2, "<table><colgroup>", "</colgroup></table>"],
            tr: [2, "<table><tbody>", "</tbody></table>"],
            td: [3, "<table><tbody><tr>", "</tr></tbody></table>"],
            _default: [0, "", ""]
        };
        wrapMap.optgroup = wrapMap.option;
        wrapMap.tbody =
            wrapMap.tfoot = wrapMap.colgroup = wrapMap.caption = wrapMap.thead;
        wrapMap.th = wrapMap.td;

        function getAll(context, tag) {
            var ret;
            if (typeof context.getElementsByTagName !== "undefined") {
                ret = context.getElementsByTagName(tag || "*");
            } else if (typeof context.querySelectorAll !== "undefined") {
                ret = context.querySelectorAll(tag || "*");
            } else {
                ret = [];
            }
            if (tag === undefined || tag && nodeName(context, tag)) {
                return jQuery.merge([context], ret);
            }
            return ret;
        }

        function setGlobalEval(elems, refElements) {
            var i = 0,
                l = elems.length;
            for (; i < l; i++) {
                dataPriv.set(elems[i], "globalEval", !refElements || dataPriv.get(refElements[i], "globalEval"));
            }
        }
        var rhtml = /<|&#?\w+;/;

        function buildFragment(elems, context, scripts, selection, ignored) {
            var elem, tmp, tag, wrap, contains, j, fragment = context.createDocumentFragment(),
                nodes = [],
                i = 0,
                l = elems.length;
            for (; i < l; i++) {
                elem = elems[i];
                if (elem || elem === 0) {
                    if (toType(elem) === "object") {
                        jQuery.merge(nodes, elem.nodeType ? [elem] : elem);
                    } else if (!rhtml.test(elem)) {
                        nodes.push(context.createTextNode(elem));
                    } else {
                        tmp = tmp || fragment.
                        appendChild(context.createElement("div"));
                        tag = (rtagName.exec(elem) || ["", ""])[1].toLowerCase();
                        wrap = wrapMap[tag] || wrapMap._default;
                        tmp.innerHTML = wrap[1] + jQuery.htmlPrefilter(elem) + wrap[2];
                        j = wrap[0];
                        while (j--) {
                            tmp = tmp.lastChild;
                        }
                        jQuery.merge(nodes, tmp.childNodes);
                        tmp = fragment.firstChild;
                        tmp.textContent = "";
                    }
                }
            }
            fragment.textContent = "";
            i = 0;
            while ((elem = nodes[i++])) {
                if (selection && jQuery.inArray(elem, selection) > -1) {
                    if (ignored) {
                        ignored.push(elem);
                    }
                    continue;
                }
                contains = jQuery.contains(elem.ownerDocument, elem);
                tmp = getAll(fragment.appendChild(elem), "script");
                if (contains) {
                    setGlobalEval(tmp);
                }
                if (scripts) {
                    j = 0;
                    while ((elem = tmp[j++])) {
                        if (rscriptType.test(elem.type || "")) {
                            scripts.push(elem);
                        }
                    }
                }
            }
            return fragment;
        }(function() {
            var fragment = document.createDocumentFragment(),
                div = fragment.appendChild(document.createElement("div")),
                input = document.createElement("input");
            input.setAttribute("type", "radio");
            input.setAttribute("checked", "checked");
            input.setAttribute("name", "t");
            div.appendChild(input);
            support.checkClone =
                div.cloneNode(true).cloneNode(true).lastChild.checked;
            div.innerHTML = "<textarea>x</textarea>";
            support.noCloneChecked = !!div.cloneNode(true).lastChild.defaultValue;
        })();
        var documentElement = document.documentElement;
        var rkeyEvent = /^key/,
            rmouseEvent = /^(?:mouse|pointer|contextmenu|drag|drop)|click/,
            rtypenamespace = /^([^.]*)(?:\.(.+)|)/;

        function returnTrue() {
            return true;
        }

        function returnFalse() {
            return false;
        }

        function safeActiveElement() {
            try {
                return document.activeElement;
            } catch (err) {}
        }

        function on(elem, types, selector, data, fn, one) {
            var origFn, type;
            if (typeof types === "object") {
                if (typeof selector !== "string") {
                    data = data || selector;
                    selector = undefined;
                }
                for (type in types) {
                    on(elem, type, selector, data, types[type], one);
                }
                return elem;
            }
            if (data == null && fn == null) {
                fn = selector;
                data = selector = undefined;
            } else if (fn == null) {
                if (typeof selector === "string") {
                    fn = data;
                    data = undefined;
                } else {
                    fn = data;
                    data = selector;
                    selector = undefined;
                }
            }
            if (fn === false) {
                fn = returnFalse;
            } else if (!fn) {
                return elem;
            }
            if (one === 1) {
                origFn = fn;
                fn = function(event) {
                    jQuery().off(event);
                    return origFn.apply(this, arguments);
                };
                fn.guid = origFn.guid || (origFn.guid = jQuery.guid++);
            }
            return elem.each(function() {
                jQuery.event.add(this, types, fn, data, selector);
            });
        }
        jQuery.event = {
            global: {},
            add: function(elem, types, handler, data, selector) {
                var handleObjIn, eventHandle, tmp, events, t, handleObj, special, handlers, type, namespaces, origType, elemData = dataPriv.get(elem);
                if (!elemData) {
                    return;
                }
                if (handler.handler) {
                    handleObjIn = handler;
                    handler = handleObjIn.handler;
                    selector = handleObjIn.selector;
                }
                if (selector) {
                    jQuery.find.matchesSelector(documentElement, selector);
                }
                if (!handler.guid) {
                    handler.guid = jQuery.guid++;
                }
                if (!(events = elemData.events)) {
                    events = elemData.events = {};
                }
                if (!(eventHandle = elemData.handle)) {
                    eventHandle = elemData.handle = function(e) {
                        return typeof jQuery !== "undefined" && jQuery.event.triggered !== e.type ? jQuery.event.dispatch.apply(elem, arguments) : undefined;
                    };
                }
                types = (types || "").match(rnothtmlwhite) || [""];
                t = types.length;
                while (t--) {
                    tmp = rtypenamespace.exec(types[t]) || [];
                    type = origType = tmp[1];
                    namespaces = (tmp[2] || "").split(".").sort();
                    if (!type) {
                        continue;
                    }
                    special = jQuery.event.special[type] || {};
                    type = (selector ? special.delegateType : special.bindType) || type;
                    special = jQuery.event.special[type] || {};
                    handleObj = jQuery.extend({
                        type: type,
                        origType: origType,
                        data: data,
                        handler: handler,
                        guid: handler.guid,
                        selector: selector,
                        needsContext: selector && jQuery.expr.match.needsContext.test(selector),
                        namespace: namespaces.join(".")
                    }, handleObjIn);
                    if (!(handlers = events[type])) {
                        handlers = events[type] = [];
                        handlers.delegateCount = 0;
                        if (!special.setup || special.setup.call(elem, data, namespaces, eventHandle) === false) {
                            if (elem.addEventListener) {
                                elem.addEventListener(type, eventHandle);
                            }
                        }
                    }
                    if (special.add) {
                        special.add.call(elem, handleObj);
                        if (!handleObj.handler.guid) {
                            handleObj.handler.guid = handler.guid;
                        }
                    }
                    if (selector) {
                        handlers.splice(handlers.delegateCount++, 0, handleObj);
                    } else {
                        handlers.push(handleObj);
                    }
                    jQuery.event.global[type] = !0;
                }
            },
            remove: function(elem, types, handler, selector, mappedTypes) {
                var j, origCount, tmp, events, t,
                    handleObj, special, handlers, type, namespaces, origType, elemData = dataPriv.hasData(elem) && dataPriv.get(elem);
                if (!elemData || !(events = elemData.events)) {
                    return;
                }
                types = (types || "").match(rnothtmlwhite) || [""];
                t = types.length;
                while (t--) {
                    tmp = rtypenamespace.exec(types[t]) || [];
                    type = origType = tmp[1];
                    namespaces = (tmp[2] || "").split(".").sort();
                    if (!type) {
                        for (type in events) {
                            jQuery.event.remove(elem, type + types[t], handler, selector, true);
                        }
                        continue;
                    }
                    special = jQuery.event.special[type] || {};
                    type = (selector ? special.delegateType : special.bindType) || type;
                    handlers = events[type] || [];
                    tmp = tmp[2] && new RegExp("(^|\\.)" + namespaces.join("\\.(?:.*\\.|)") + "(\\.|$)");
                    origCount = j = handlers.length;
                    while (j--) {
                        handleObj = handlers[j];
                        if ((mappedTypes || origType === handleObj.origType) && (!handler || handler.guid === handleObj.guid) && (!tmp || tmp.test(handleObj.namespace)) && (!selector || selector === handleObj.selector || selector === "**" && handleObj.selector)) {
                            handlers.splice(j, 1);
                            if (handleObj.selector) {
                                handlers.delegateCount--;
                            }
                            if (special.remove) {
                                special.
                                remove.call(elem, handleObj);
                            }
                        }
                    }
                    if (origCount && !handlers.length) {
                        if (!special.teardown || special.teardown.call(elem, namespaces, elemData.handle) === false) {
                            jQuery.removeEvent(elem, type, elemData.handle);
                        }
                        delete events[type];
                    }
                }
                if (jQuery.isEmptyObject(events)) {
                    dataPriv.remove(elem, "handle events");
                }
            },
            dispatch: function(nativeEvent) {
                var event = jQuery.event.fix(nativeEvent);
                var i, j, ret, matched, handleObj, handlerQueue, args = new Array(arguments.length),
                    handlers = (dataPriv.get(this, "events") || {})[event.type] || [],
                    special = jQuery.event.special[event.type] || {};
                args[0] = event;
                for (i = 1; i < arguments.length; i++) {
                    args[i] = arguments[i];
                }
                event.delegateTarget = this;
                if (special.preDispatch && special.preDispatch.call(this, event) === false) {
                    return;
                }
                handlerQueue = jQuery.event.handlers.call(this, event, handlers);
                i = 0;
                while ((matched = handlerQueue[i++]) && !event.isPropagationStopped()) {
                    event.currentTarget = matched.elem;
                    j = 0;
                    while ((handleObj = matched.handlers[j++]) && !event.isImmediatePropagationStopped()) {
                        if (!event.rnamespace || event.rnamespace.test(handleObj.namespace)) {
                            event.handleObj = handleObj;
                            event.data = handleObj.data;
                            ret = ((jQuery.event.special[handleObj.origType] || {}).handle || handleObj.handler).apply(matched.elem, args);
                            if (ret !== undefined) {
                                if ((event.result = ret) === false) {
                                    event.preventDefault();
                                    event.stopPropagation();
                                }
                            }
                        }
                    }
                }
                if (special.postDispatch) {
                    special.postDispatch.call(this, event);
                }
                return event.result;
            },
            handlers: function(event, handlers) {
                var i, handleObj, sel, matchedHandlers, matchedSelectors, handlerQueue = [],
                    delegateCount = handlers.delegateCount,
                    cur = event.target;
                if (delegateCount && cur.nodeType && !(event.type === "click" && event.button >= 1)) {
                    for (; cur !== this; cur = cur.parentNode || this) {
                        if (cur.nodeType === 1 && !(event.type === "click" && cur.disabled === true)) {
                            matchedHandlers = [];
                            matchedSelectors = {};
                            for (i = 0; i < delegateCount; i++) {
                                handleObj = handlers[i];
                                sel = handleObj.selector + " ";
                                if (matchedSelectors[sel] === undefined) {
                                    matchedSelectors[sel] = handleObj.needsContext ? jQuery(sel, this).index(cur) > -1 : jQuery.find(sel, this, null, [cur]).length;
                                }
                                if (
                                    matchedSelectors[sel]) {
                                    matchedHandlers.push(handleObj);
                                }
                            }
                            if (matchedHandlers.length) {
                                handlerQueue.push({
                                    elem: cur,
                                    handlers: matchedHandlers
                                });
                            }
                        }
                    }
                }
                cur = this;
                if (delegateCount < handlers.length) {
                    handlerQueue.push({
                        elem: cur,
                        handlers: handlers.slice(delegateCount)
                    });
                }
                return handlerQueue;
            },
            addProp: function(name, hook) {
                Object.defineProperty(jQuery.Event.prototype, name, {
                    enumerable: !0,
                    configurable: !0,
                    get: isFunction(hook) ? function() {
                        if (this.originalEvent) {
                            return hook(this.originalEvent);
                        }
                    } : function() {
                        if (this.originalEvent) {
                            return this.originalEvent[name];
                        }
                    },
                    set: function(value) {
                        Object.defineProperty(this, name, {
                            enumerable: !0,
                            configurable: !0,
                            writable: !0,
                            value: value
                        });
                    }
                });
            },
            fix: function(originalEvent) {
                return originalEvent[jQuery.expando] ? originalEvent : new jQuery.Event(originalEvent);
            },
            special: {
                load: {
                    noBubble: !0
                },
                focus: {
                    trigger: function() {
                        if (this !== safeActiveElement() && this.focus) {
                            this.focus();
                            return false;
                        }
                    },
                    delegateType: "focusin"
                },
                blur: {
                    trigger: function() {
                        if (this === safeActiveElement() && this.blur) {
                            this.blur();
                            return false;
                        }
                    },
                    delegateType: "focusout"
                },
                click: {
                    trigger: function() {
                        if (this.type === "checkbox" && this.click && nodeName(this, "input")) {
                            this.click();
                            return false;
                        }
                    },
                    _default: function(event) {
                        return nodeName(event.target, "a");
                    }
                },
                beforeunload: {
                    postDispatch: function(event) {
                        if (event.result !== undefined && event.originalEvent) {
                            event.originalEvent.returnValue = event.result;
                        }
                    }
                }
            }
        };
        jQuery.removeEvent = function(elem, type, handle) {
            if (elem.removeEventListener) {
                elem.removeEventListener(type, handle);
            }
        };
        jQuery.Event = function(src, props) {
            if (!(this instanceof jQuery.Event)) {
                return new jQuery.Event(src, props);
            }
            if (src && src.type) {
                this.originalEvent = src;
                this.type = src.type;
                this.isDefaultPrevented = src.defaultPrevented || src.defaultPrevented === undefined && src.returnValue === !1 ? returnTrue : returnFalse;
                this.target = (src.target && src.target.nodeType === 3) ? src.target.parentNode : src.target;
                this.currentTarget = src.currentTarget;
                this.relatedTarget = src.relatedTarget;
            } else {
                this.type = src;
            }
            if (props) {
                jQuery.extend(this,
                    props);
            }
            this.timeStamp = src && src.timeStamp || Date.now();
            this[jQuery.expando] = !0;
        };
        jQuery.Event.prototype = {
            constructor: jQuery.Event,
            isDefaultPrevented: returnFalse,
            isPropagationStopped: returnFalse,
            isImmediatePropagationStopped: returnFalse,
            isSimulated: !1,
            preventDefault: function() {
                var e = this.originalEvent;
                this.isDefaultPrevented = returnTrue;
                if (e && !this.isSimulated) {
                    e.preventDefault();
                }
            },
            stopPropagation: function() {
                var e = this.originalEvent;
                this.isPropagationStopped = returnTrue;
                if (e && !this.isSimulated) {
                    e.stopPropagation();
                }
            },
            stopImmediatePropagation: function() {
                var e = this.originalEvent;
                this.isImmediatePropagationStopped = returnTrue;
                if (e && !this.isSimulated) {
                    e.stopImmediatePropagation();
                }
                this.stopPropagation();
            }
        };
        jQuery.each({
            altKey: !0,
            bubbles: !0,
            cancelable: !0,
            changedTouches: !0,
            ctrlKey: !0,
            detail: !0,
            eventPhase: !0,
            metaKey: !0,
            pageX: !0,
            pageY: !0,
            shiftKey: !0,
            view: !0,
            "char": !0,
            charCode: !0,
            key: !0,
            keyCode: !0,
            button: !0,
            buttons: !0,
            clientX: !0,
            clientY: !0,
            offsetX: !0,
            offsetY: !0,
            pointerId: !0,
            pointerType: !0,
            screenX: !0,
            screenY: !0,
            targetTouches: !0,
            toElement: !0,
            touches: !0,
            which: function(event) {
                var button = event.button;
                if (event.which == null && rkeyEvent.test(event.type)) {
                    return event.charCode != null ? event.charCode : event.keyCode;
                }
                if (!event.which && button !== undefined && rmouseEvent.test(event.type)) {
                    if (button & 1) {
                        return 1;
                    }
                    if (button & 2) {
                        return 3;
                    }
                    if (button & 4) {
                        return 2;
                    }
                    return 0;
                }
                return event.which;
            }
        }, jQuery.event.addProp);
        jQuery.each({
            mouseenter: "mouseover",
            mouseleave: "mouseout",
            pointerenter: "pointerover",
            pointerleave: "pointerout"
        }, function(orig, fix) {
            jQuery.event.special[orig] = {
                delegateType: fix,
                bindType: fix,
                handle: function(event) {
                    var ret, target = this,
                        related = event.relatedTarget,
                        handleObj = event.handleObj;
                    if (!related || (related !== target && !jQuery.contains(target, related))) {
                        event.type = handleObj.origType;
                        ret = handleObj.handler.apply(this, arguments);
                        event.type = fix;
                    }
                    return ret;
                }
            };
        });
        jQuery.fn.extend({
            on: function(types, selector, data, fn) {
                return on(this, types,
                    selector, data, fn);
            },
            one: function(types, selector, data, fn) {
                return on(this, types, selector, data, fn, 1);
            },
            off: function(types, selector, fn) {
                var handleObj, type;
                if (types && types.preventDefault && types.handleObj) {
                    handleObj = types.handleObj;
                    jQuery(types.delegateTarget).off(handleObj.namespace ? handleObj.origType + "." + handleObj.namespace : handleObj.origType, handleObj.selector, handleObj.handler);
                    return this;
                }
                if (typeof types === "object") {
                    for (type in types) {
                        this.off(type, selector, types[type]);
                    }
                    return this;
                }
                if (selector === false || typeof selector === "function") {
                    fn = selector;
                    selector = undefined;
                }
                if (fn === false) {
                    fn = returnFalse;
                }
                return this.each(function() {
                    jQuery.event.remove(this, types, fn, selector);
                });
            }
        });
        var rxhtmlTag = /<(?!area|br|col|embed|hr|img|input|link|meta|param)(([a-z][^\/\0>\x20\t\r\n\f]*)[^>]*)\/>/gi,
            rnoInnerhtml = /<script|<style|<link/i,
            rchecked = /checked\s*(?:[^=]|=\s*.checked.)/i,
            rcleanScript = /^\s*<!(?:\[CDATA\[|--)|(?:\]\]|--)>\s*$/g;

        function manipulationTarget(elem, content) {
            if (nodeName(elem, "table") &&
                nodeName(content.nodeType !== 11 ? content : content.firstChild, "tr")) {
                return jQuery(elem).children("tbody")[0] || elem;
            }
            return elem;
        }

        function disableScript(elem) {
            elem.type = (elem.getAttribute("type") !== null) + "/" + elem.type;
            return elem;
        }

        function restoreScript(elem) {
            if ((elem.type || "").slice(0, 5) === "true/") {
                elem.type = elem.type.slice(5);
            } else {
                elem.removeAttribute("type");
            }
            return elem;
        }

        function cloneCopyEvent(src, dest) {
            var i, l, type, pdataOld, pdataCur, udataOld, udataCur, events;
            if (dest.nodeType !== 1) {
                return;
            }
            if (dataPriv.hasData(src)) {
                pdataOld = dataPriv.access(src);
                pdataCur = dataPriv.set(dest, pdataOld);
                events = pdataOld.events;
                if (events) {
                    delete pdataCur.handle;
                    pdataCur.events = {};
                    for (type in events) {
                        for (i = 0, l = events[type].length; i < l; i++) {
                            jQuery.event.add(dest, type, events[type][i]);
                        }
                    }
                }
            }
            if (dataUser.hasData(src)) {
                udataOld = dataUser.access(src);
                udataCur = jQuery.extend({}, udataOld);
                dataUser.set(dest, udataCur);
            }
        }

        function fixInput(src, dest) {
            var nodeName = dest.nodeName.toLowerCase();
            if (nodeName === "input" && rcheckableType.test(src.type)) {
                dest.checked = src.checked;
            } else if (nodeName === "input" || nodeName === "textarea") {
                dest.defaultValue = src.defaultValue;
            }
        }

        function domManip(collection, args, callback, ignored) {
            args = concat.apply([], args);
            var fragment, first, scripts, hasScripts, node, doc, i = 0,
                l = collection.length,
                iNoClone = l - 1,
                value = args[0],
                valueIsFunction = isFunction(value);
            if (valueIsFunction || (l > 1 && typeof value === "string" && !support.checkClone && rchecked.test(value))) {
                return collection.each(function(index) {
                    var self = collection.eq(index);
                    if (valueIsFunction) {
                        args[0] = value.call(this, index, self.html());
                    }
                    domManip(self, args, callback, ignored);
                });
            }
            if (l) {
                fragment = buildFragment(args, collection[0].ownerDocument, false, collection, ignored);
                first = fragment.firstChild;
                if (fragment.childNodes.length === 1) {
                    fragment = first;
                }
                if (first || ignored) {
                    scripts = jQuery.map(getAll(fragment, "script"), disableScript);
                    hasScripts = scripts.length;
                    for (; i < l; i++) {
                        node = fragment;
                        if (i !== iNoClone) {
                            node = jQuery.clone(node, true, true);
                            if (hasScripts) {
                                jQuery.merge(scripts,
                                    getAll(node, "script"));
                            }
                        }
                        callback.call(collection[i], node, i);
                    }
                    if (hasScripts) {
                        doc = scripts[scripts.length - 1].ownerDocument;
                        jQuery.map(scripts, restoreScript);
                        for (i = 0; i < hasScripts; i++) {
                            node = scripts[i];
                            if (rscriptType.test(node.type || "") && !dataPriv.access(node, "globalEval") && jQuery.contains(doc, node)) {
                                if (node.src && (node.type || "").toLowerCase() !== "module") {
                                    if (jQuery._evalUrl) {
                                        jQuery._evalUrl(node.src);
                                    }
                                } else {
                                    DOMEval(node.textContent.replace(rcleanScript, ""), doc, node);
                                }
                            }
                        }
                    }
                }
            }
            return collection;
        }

        function remove(elem, selector, keepData) {
            var node, nodes = selector ? jQuery.filter(selector, elem) : elem,
                i = 0;
            for (;
                (node = nodes[i]) != null; i++) {
                if (!keepData && node.nodeType === 1) {
                    jQuery.cleanData(getAll(node));
                }
                if (node.parentNode) {
                    if (keepData && jQuery.contains(node.ownerDocument, node)) {
                        setGlobalEval(getAll(node, "script"));
                    }
                    node.parentNode.removeChild(node);
                }
            }
            return elem;
        }
        jQuery.extend({
            htmlPrefilter: function(html) {
                return html.replace(rxhtmlTag, "<$1></$2>");
            },
            clone: function(elem, dataAndEvents, deepDataAndEvents) {
                var i, l, srcElements, destElements, clone = elem.cloneNode(true),
                    inPage = jQuery.contains(elem.ownerDocument, elem);
                if (!support.noCloneChecked && (elem.nodeType === 1 || elem.nodeType === 11) && !jQuery.isXMLDoc(elem)) {
                    destElements = getAll(clone);
                    srcElements = getAll(elem);
                    for (i = 0, l = srcElements.length; i < l; i++) {
                        fixInput(srcElements[i], destElements[i]);
                    }
                }
                if (dataAndEvents) {
                    if (deepDataAndEvents) {
                        srcElements = srcElements || getAll(elem);
                        destElements = destElements || getAll(clone);
                        for (i = 0, l = srcElements.length; i < l; i++) {
                            cloneCopyEvent(srcElements[i], destElements[i]);
                        }
                    } else {
                        cloneCopyEvent(elem, clone);
                    }
                }
                destElements = getAll(clone, "script");
                if (destElements.length > 0) {
                    setGlobalEval(destElements, !inPage && getAll(elem, "script"));
                }
                return clone;
            },
            cleanData: function(elems) {
                var data, elem, type, special = jQuery.event.special,
                    i = 0;
                for (;
                    (elem = elems[i]) !== undefined; i++) {
                    if (acceptData(elem)) {
                        if ((data = elem[dataPriv.expando])) {
                            if (data.events) {
                                for (type in data.events) {
                                    if (special[type]) {
                                        jQuery.event.remove(elem, type);
                                    } else {
                                        jQuery.removeEvent(elem, type,
                                            data.handle);
                                    }
                                }
                            }
                            elem[dataPriv.expando] = undefined;
                        }
                        if (elem[dataUser.expando]) {
                            elem[dataUser.expando] = undefined;
                        }
                    }
                }
            }
        });
        jQuery.fn.extend({
            detach: function(selector) {
                return remove(this, selector, true);
            },
            remove: function(selector) {
                return remove(this, selector);
            },
            text: function(value) {
                return access(this, function(value) {
                    return value === undefined ? jQuery.text(this) : this.empty().each(function() {
                        if (this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9) {
                            this.textContent = value;
                        }
                    });
                }, null, value, arguments.length);
            },
            append: function() {
                return domManip(this, arguments, function(elem) {
                    if (this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9) {
                        var target = manipulationTarget(this, elem);
                        target.appendChild(elem);
                    }
                });
            },
            prepend: function() {
                return domManip(this, arguments, function(elem) {
                    if (this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9) {
                        var target = manipulationTarget(this, elem);
                        target.insertBefore(elem, target.firstChild);
                    }
                });
            },
            before: function() {
                return domManip(this, arguments, function(elem) {
                    if (this.parentNode) {
                        this.parentNode.insertBefore(elem, this);
                    }
                });
            },
            after: function() {
                return domManip(this, arguments, function(elem) {
                    if (this.parentNode) {
                        this.parentNode.insertBefore(elem, this.nextSibling);
                    }
                });
            },
            empty: function() {
                var elem, i = 0;
                for (;
                    (elem = this[i]) != null; i++) {
                    if (elem.nodeType === 1) {
                        jQuery.cleanData(getAll(elem, false));
                        elem.textContent = "";
                    }
                }
                return this;
            },
            clone: function(dataAndEvents, deepDataAndEvents) {
                dataAndEvents = dataAndEvents == null ? false : dataAndEvents;
                deepDataAndEvents = deepDataAndEvents == null ? dataAndEvents : deepDataAndEvents;
                return this.map(function() {
                    return jQuery.clone(this, dataAndEvents, deepDataAndEvents);
                });
            },
            html: function(value) {
                return access(this, function(value) {
                    var elem = this[0] || {},
                        i = 0,
                        l = this.length;
                    if (value === undefined && elem.nodeType === 1) {
                        return elem.innerHTML;
                    }
                    if (typeof value === "string" && !rnoInnerhtml.test(value) && !wrapMap[(rtagName.exec(value) || ["", ""])[1].toLowerCase()]) {
                        value = jQuery.htmlPrefilter(value);
                        try {
                            for (; i < l; i++) {
                                elem = this[i] || {};
                                if (elem.nodeType === 1) {
                                    jQuery.cleanData(getAll(
                                        elem, false));
                                    elem.innerHTML = value;
                                }
                            }
                            elem = 0;
                        } catch (e) {}
                    }
                    if (elem) {
                        this.empty().append(value);
                    }
                }, null, value, arguments.length);
            },
            replaceWith: function() {
                var ignored = [];
                return domManip(this, arguments, function(elem) {
                    var parent = this.parentNode;
                    if (jQuery.inArray(this, ignored) < 0) {
                        jQuery.cleanData(getAll(this));
                        if (parent) {
                            parent.replaceChild(elem, this);
                        }
                    }
                }, ignored);
            }
        });
        jQuery.each({
            appendTo: "append",
            prependTo: "prepend",
            insertBefore: "before",
            insertAfter: "after",
            replaceAll: "replaceWith"
        }, function(name, original) {
            jQuery.fn[name] = function(selector) {
                var elems, ret = [],
                    insert = jQuery(selector),
                    last = insert.length - 1,
                    i = 0;
                for (; i <= last; i++) {
                    elems = i === last ? this : this.clone(true);
                    jQuery(insert[i])[original](elems);
                    push.apply(ret, elems.get());
                }
                return this.pushStack(ret);
            };
        });
        var rnumnonpx = new RegExp("^(" + pnum + ")(?!px)[a-z%]+$", "i");
        var getStyles = function(elem) {
            var view = elem.ownerDocument.defaultView;
            if (!view || !view.opener) {
                view = window;
            }
            return view.getComputedStyle(elem);
        };
        var rboxStyle = new RegExp(cssExpand.join("|"), "i");
        (function() {
            function computeStyleTests() {
                if (!div) {
                    return;
                }
                container.style.cssText = "position:absolute;left:-11111px;width:60px;" + "margin-top:1px;padding:0;border:0";
                div.style.cssText = "position:relative;display:block;box-sizing:border-box;overflow:scroll;" + "margin:auto;border:1px;padding:1px;" + "width:60%;top:1%";
                documentElement.appendChild(container).appendChild(div);
                var divStyle = window.getComputedStyle(div);
                pixelPositionVal = divStyle.top !== "1%";
                reliableMarginLeftVal = roundPixelMeasures(divStyle.marginLeft) === 12;
                div.style.right = "60%";
                pixelBoxStylesVal = roundPixelMeasures(divStyle.right) === 36;
                boxSizingReliableVal = roundPixelMeasures(divStyle.width) === 36;
                div.style.position = "absolute";
                scrollboxSizeVal = div.offsetWidth === 36 || "absolute";
                documentElement.removeChild(container);
                div = null;
            }

            function roundPixelMeasures(measure) {
                return Math.round(parseFloat(measure));
            }
            var pixelPositionVal, boxSizingReliableVal, scrollboxSizeVal, pixelBoxStylesVal, reliableMarginLeftVal, container = document.createElement(
                    "div"),
                div = document.createElement("div");
            if (!div.style) {
                return;
            }
            div.style.backgroundClip = "content-box";
            div.cloneNode(true).style.backgroundClip = "";
            support.clearCloneStyle = div.style.backgroundClip === "content-box";
            jQuery.extend(support, {
                boxSizingReliable: function() {
                    computeStyleTests();
                    return boxSizingReliableVal;
                },
                pixelBoxStyles: function() {
                    computeStyleTests();
                    return pixelBoxStylesVal;
                },
                pixelPosition: function() {
                    computeStyleTests();
                    return pixelPositionVal;
                },
                reliableMarginLeft: function() {
                    computeStyleTests();
                    return reliableMarginLeftVal;
                },
                scrollboxSize: function() {
                    computeStyleTests();
                    return scrollboxSizeVal;
                }
            });
        })();

        function curCSS(elem, name, computed) {
            var width, minWidth, maxWidth, ret, style = elem.style;
            computed = computed || getStyles(elem);
            if (computed) {
                ret = computed.getPropertyValue(name) || computed[name];
                if (ret === "" && !jQuery.contains(elem.ownerDocument, elem)) {
                    ret = jQuery.style(elem, name);
                }
                if (!support.pixelBoxStyles() && rnumnonpx.test(ret) && rboxStyle.test(name)) {
                    width = style.width;
                    minWidth = style.
                    minWidth;
                    maxWidth = style.maxWidth;
                    style.minWidth = style.maxWidth = style.width = ret;
                    ret = computed.width;
                    style.width = width;
                    style.minWidth = minWidth;
                    style.maxWidth = maxWidth;
                }
            }
            return ret !== undefined ? ret + "" : ret;
        }

        function addGetHookIf(conditionFn, hookFn) {
            return {
                get: function() {
                    if (conditionFn()) {
                        delete this.get;
                        return;
                    }
                    return (this.get = hookFn).apply(this, arguments);
                }
            };
        }
        var rdisplayswap = /^(none|table(?!-c[ea]).+)/,
            rcustomProp = /^--/,
            cssShow = {
                position: "absolute",
                visibility: "hidden",
                display: "block"
            },
            cssNormalTransform = {
                letterSpacing: "0",
                fontWeight: "400"
            },
            cssPrefixes = ["Webkit", "Moz", "ms"],
            emptyStyle = document.createElement("div").style;

        function vendorPropName(name) {
            if (name in emptyStyle) {
                return name;
            }
            var capName = name[0].toUpperCase() + name.slice(1),
                i = cssPrefixes.length;
            while (i--) {
                name = cssPrefixes[i] + capName;
                if (name in emptyStyle) {
                    return name;
                }
            }
        }

        function finalPropName(name) {
            var ret = jQuery.cssProps[name];
            if (!ret) {
                ret = jQuery.cssProps[name] = vendorPropName(name) || name;
            }
            return ret;
        }

        function setPositiveNumber(elem, value, subtract) {
            var matches = rcssNum.exec(value);
            return matches ? Math.max(0, matches[2] - (subtract || 0)) + (matches[3] || "px") : value;
        }

        function boxModelAdjustment(elem, dimension, box, isBorderBox, styles, computedVal) {
            var i = dimension === "width" ? 1 : 0,
                extra = 0,
                delta = 0;
            if (box === (isBorderBox ? "border" : "content")) {
                return 0;
            }
            for (; i < 4; i += 2) {
                if (box === "margin") {
                    delta += jQuery.css(elem, box + cssExpand[i], true, styles);
                }
                if (!isBorderBox) {
                    delta += jQuery.css(elem, "padding" + cssExpand[i], true, styles);
                    if (box !== "padding") {
                        delta += jQuery.css(elem, "border" + cssExpand[i] + "Width", true, styles);
                    } else {
                        extra += jQuery.css(elem, "border" + cssExpand[i] + "Width", true, styles);
                    }
                } else {
                    if (box === "content") {
                        delta -= jQuery.css(elem, "padding" + cssExpand[i], true, styles);
                    }
                    if (box !== "margin") {
                        delta -= jQuery.css(elem, "border" + cssExpand[i] + "Width", true, styles);
                    }
                }
            }
            if (!isBorderBox && computedVal >= 0) {
                delta += Math.max(0, Math.ceil(elem["offset" + dimension[0].toUpperCase() + dimension.slice(1)] - computedVal - delta - extra - 0.5));
            }
            return delta;
        }

        function getWidthOrHeight(elem,
            dimension, extra) {
            var styles = getStyles(elem),
                val = curCSS(elem, dimension, styles),
                isBorderBox = jQuery.css(elem, "boxSizing", false, styles) === "border-box",
                valueIsBorderBox = isBorderBox;
            if (rnumnonpx.test(val)) {
                if (!extra) {
                    return val;
                }
                val = "auto";
            }
            valueIsBorderBox = valueIsBorderBox && (support.boxSizingReliable() || val === elem.style[dimension]);
            if (val === "auto" || !parseFloat(val) && jQuery.css(elem, "display", false, styles) === "inline") {
                val = elem["offset" + dimension[0].toUpperCase() + dimension.slice(1)];
                valueIsBorderBox = !0;
            }
            val = parseFloat(val) || 0;
            return (val + boxModelAdjustment(elem, dimension, extra || (isBorderBox ? "border" : "content"), valueIsBorderBox, styles, val)) + "px";
        }
        jQuery.extend({
            cssHooks: {
                opacity: {
                    get: function(elem, computed) {
                        if (computed) {
                            var ret = curCSS(elem, "opacity");
                            return ret === "" ? "1" : ret;
                        }
                    }
                }
            },
            cssNumber: {
                "animationIterationCount": !0,
                "columnCount": !0,
                "fillOpacity": !0,
                "flexGrow": !0,
                "flexShrink": !0,
                "fontWeight": !0,
                "lineHeight": !0,
                "opacity": !0,
                "order": !0,
                "orphans": !0,
                "widows": !0,
                "zIndex":
                    !0,
                "zoom": !0
            },
            cssProps: {},
            style: function(elem, name, value, extra) {
                if (!elem || elem.nodeType === 3 || elem.nodeType === 8 || !elem.style) {
                    return;
                }
                var ret, type, hooks, origName = camelCase(name),
                    isCustomProp = rcustomProp.test(name),
                    style = elem.style;
                if (!isCustomProp) {
                    name = finalPropName(origName);
                }
                hooks = jQuery.cssHooks[name] || jQuery.cssHooks[origName];
                if (value !== undefined) {
                    type = typeof value;
                    if (type === "string" && (ret = rcssNum.exec(value)) && ret[1]) {
                        value = adjustCSS(elem, name, ret);
                        type = "number";
                    }
                    if (value == null || value !== value) {
                        return;
                    }
                    if (type === "number") {
                        value += ret && ret[3] || (jQuery.cssNumber[origName] ? "" : "px");
                    }
                    if (!support.clearCloneStyle && value === "" && name.indexOf("background") === 0) {
                        style[name] = "inherit";
                    }
                    if (!hooks || !("set" in hooks) || (value = hooks.set(elem, value, extra)) !== undefined) {
                        if (isCustomProp) {
                            style.setProperty(name, value);
                        } else {
                            style[name] = value;
                        }
                    }
                } else {
                    if (hooks && "get" in hooks && (ret = hooks.get(elem, false, extra)) !== undefined) {
                        return ret;
                    }
                    return style[name];
                }
            },
            css: function(elem, name, extra, styles) {
                var val,
                    num, hooks, origName = camelCase(name),
                    isCustomProp = rcustomProp.test(name);
                if (!isCustomProp) {
                    name = finalPropName(origName);
                }
                hooks = jQuery.cssHooks[name] || jQuery.cssHooks[origName];
                if (hooks && "get" in hooks) {
                    val = hooks.get(elem, true, extra);
                }
                if (val === undefined) {
                    val = curCSS(elem, name, styles);
                }
                if (val === "normal" && name in cssNormalTransform) {
                    val = cssNormalTransform[name];
                }
                if (extra === "" || extra) {
                    num = parseFloat(val);
                    return extra === !0 || isFinite(num) ? num || 0 : val;
                }
                return val;
            }
        });
        jQuery.each(["height", "width"], function(i, dimension) {
            jQuery.cssHooks[dimension] = {
                get: function(elem, computed, extra) {
                    if (computed) {
                        return rdisplayswap.test(jQuery.css(elem, "display")) && (!elem.getClientRects().length || !elem.getBoundingClientRect().width) ? swap(elem, cssShow, function() {
                            return getWidthOrHeight(elem, dimension, extra);
                        }) : getWidthOrHeight(elem, dimension, extra);
                    }
                },
                set: function(elem, value, extra) {
                    var matches, styles = getStyles(elem),
                        isBorderBox = jQuery.css(elem, "boxSizing", false, styles) === "border-box",
                        subtract = extra &&
                        boxModelAdjustment(elem, dimension, extra, isBorderBox, styles);
                    if (isBorderBox && support.scrollboxSize() === styles.position) {
                        subtract -= Math.ceil(elem["offset" + dimension[0].toUpperCase() + dimension.slice(1)] - parseFloat(styles[dimension]) - boxModelAdjustment(elem, dimension, "border", false, styles) - 0.5);
                    }
                    if (subtract && (matches = rcssNum.exec(value)) && (matches[3] || "px") !== "px") {
                        elem.style[dimension] = value;
                        value = jQuery.css(elem, dimension);
                    }
                    return setPositiveNumber(elem, value, subtract);
                }
            };
        });
        jQuery.cssHooks.marginLeft = addGetHookIf(support.reliableMarginLeft, function(elem, computed) {
            if (computed) {
                return (parseFloat(curCSS(elem, "marginLeft")) || elem.getBoundingClientRect().left - swap(elem, {
                    marginLeft: 0
                }, function() {
                    return elem.getBoundingClientRect().left;
                })) + "px";
            }
        });
        jQuery.each({
            margin: "",
            padding: "",
            border: "Width"
        }, function(prefix, suffix) {
            jQuery.cssHooks[prefix + suffix] = {
                expand: function(value) {
                    var i = 0,
                        expanded = {},
                        parts = typeof value === "string" ? value.split(" ") : [value];
                    for (; i < 4; i++) {
                        expanded[prefix + cssExpand[i] +
                            suffix] = parts[i] || parts[i - 2] || parts[0];
                    }
                    return expanded;
                }
            };
            if (prefix !== "margin") {
                jQuery.cssHooks[prefix + suffix].set = setPositiveNumber;
            }
        });
        jQuery.fn.extend({
            css: function(name, value) {
                return access(this, function(elem, name, value) {
                    var styles, len, map = {},
                        i = 0;
                    if (Array.isArray(name)) {
                        styles = getStyles(elem);
                        len = name.length;
                        for (; i < len; i++) {
                            map[name[i]] = jQuery.css(elem, name[i], false, styles);
                        }
                        return map;
                    }
                    return value !== undefined ? jQuery.style(elem, name, value) : jQuery.css(elem, name);
                }, name, value, arguments.length > 1);
            }
        });

        function Tween(elem, options, prop, end, easing) {
            return new Tween.prototype.init(elem, options, prop, end, easing);
        }
        jQuery.Tween = Tween;
        Tween.prototype = {
            constructor: Tween,
            init: function(elem, options, prop, end, easing, unit) {
                this.elem = elem;
                this.prop = prop;
                this.easing = easing || jQuery.easing._default;
                this.options = options;
                this.start = this.now = this.cur();
                this.end = end;
                this.unit = unit || (jQuery.cssNumber[prop] ? "" : "px");
            },
            cur: function() {
                var hooks = Tween.propHooks[this.prop];
                return hooks && hooks.get ? hooks.get(this) :
                    Tween.propHooks._default.get(this);
            },
            run: function(percent) {
                var eased, hooks = Tween.propHooks[this.prop];
                if (this.options.duration) {
                    this.pos = eased = jQuery.easing[this.easing](percent, this.options.duration * percent, 0, 1, this.options.duration);
                } else {
                    this.pos = eased = percent;
                }
                this.now = (this.end - this.start) * eased + this.start;
                if (this.options.step) {
                    this.options.step.call(this.elem, this.now, this);
                }
                if (hooks && hooks.set) {
                    hooks.set(this);
                } else {
                    Tween.propHooks._default.set(this);
                }
                return this;
            }
        };
        Tween.prototype.init.prototype = Tween.prototype;
        Tween.propHooks = {
            _default: {
                get: function(tween) {
                    var result;
                    if (tween.elem.nodeType !== 1 || tween.elem[tween.prop] != null && tween.elem.style[tween.prop] == null) {
                        return tween.elem[tween.prop];
                    }
                    result = jQuery.css(tween.elem, tween.prop, "");
                    return !result || result === "auto" ? 0 : result;
                },
                set: function(tween) {
                    if (jQuery.fx.step[tween.prop]) {
                        jQuery.fx.step[tween.prop](tween);
                    } else if (tween.elem.nodeType === 1 && (tween.elem.style[jQuery.cssProps[tween.prop]] != null || jQuery.cssHooks[tween.prop])) {
                        jQuery.style(tween.elem, tween.prop, tween.now + tween.unit);
                    } else {
                        tween.elem[tween.prop] = tween.now;
                    }
                }
            }
        };
        Tween.propHooks.scrollTop = Tween.propHooks.scrollLeft = {
            set: function(tween) {
                if (tween.elem.nodeType && tween.elem.parentNode) {
                    tween.elem[tween.prop] = tween.now;
                }
            }
        };
        jQuery.easing = {
            linear: function(p) {
                return p;
            },
            swing: function(p) {
                return 0.5 - Math.cos(p * Math.PI) / 2;
            },
            _default: "swing"
        };
        jQuery.fx = Tween.prototype.init;
        jQuery.fx.step = {};
        var fxNow, inProgress, rfxtypes = /^(?:toggle|show|hide)$/,
            rrun = /queueHooks$/;

        function schedule() {
            if (inProgress) {
                if (document.hidden === false && window.requestAnimationFrame) {
                    window.requestAnimationFrame(schedule);
                } else {
                    window.setTimeout(schedule, jQuery.fx.interval);
                }
                jQuery.fx.tick();
            }
        }

        function createFxNow() {
            window.setTimeout(function() {
                fxNow = undefined;
            });
            return (fxNow = Date.now());
        }

        function genFx(type, includeWidth) {
            var which, i = 0,
                attrs = {
                    height: type
                };
            includeWidth = includeWidth ? 1 : 0;
            for (; i < 4; i += 2 - includeWidth) {
                which = cssExpand[i];
                attrs["margin" + which] = attrs["padding" + which] = type;
            }
            if (
                includeWidth) {
                attrs.opacity = attrs.width = type;
            }
            return attrs;
        }

        function createTween(value, prop, animation) {
            var tween, collection = (Animation.tweeners[prop] || []).concat(Animation.tweeners["*"]),
                index = 0,
                length = collection.length;
            for (; index < length; index++) {
                if ((tween = collection[index].call(animation, prop, value))) {
                    return tween;
                }
            }
        }

        function defaultPrefilter(elem, props, opts) {
            var prop, value, toggle, hooks, oldfire, propTween, restoreDisplay, display, isBox = "width" in props || "height" in props,
                anim = this,
                orig = {},
                style = elem.style,
                hidden = elem.nodeType && isHiddenWithinTree(elem),
                dataShow = dataPriv.get(elem, "fxshow");
            if (!opts.queue) {
                hooks = jQuery._queueHooks(elem, "fx");
                if (hooks.unqueued == null) {
                    hooks.unqueued = 0;
                    oldfire = hooks.empty.fire;
                    hooks.empty.fire = function() {
                        if (!hooks.unqueued) {
                            oldfire();
                        }
                    };
                }
                hooks.unqueued++;
                anim.always(function() {
                    anim.always(function() {
                        hooks.unqueued--;
                        if (!jQuery.queue(elem, "fx").length) {
                            hooks.empty.fire();
                        }
                    });
                });
            }
            for (prop in props) {
                value = props[prop];
                if (rfxtypes.test(value)) {
                    delete props[prop];
                    toggle = toggle || value === "toggle";
                    if (value === (hidden ? "hide" : "show")) {
                        if (value === "show" && dataShow && dataShow[prop] !== undefined) {
                            hidden = !0;
                        } else {
                            continue;
                        }
                    }
                    orig[prop] = dataShow && dataShow[prop] || jQuery.style(elem, prop);
                }
            }
            propTween = !jQuery.isEmptyObject(props);
            if (!propTween && jQuery.isEmptyObject(orig)) {
                return;
            }
            if (isBox && elem.nodeType === 1) {
                opts.overflow = [style.overflow, style.overflowX, style.overflowY];
                restoreDisplay = dataShow && dataShow.display;
                if (restoreDisplay == null) {
                    restoreDisplay = dataPriv.get(elem, "display");
                }
                display = jQuery.css(elem, "display");
                if (display === "none") {
                    if (restoreDisplay) {
                        display = restoreDisplay;
                    } else {
                        showHide([elem], true);
                        restoreDisplay = elem.style.display || restoreDisplay;
                        display = jQuery.css(elem, "display");
                        showHide([elem]);
                    }
                }
                if (display === "inline" || display === "inline-block" && restoreDisplay != null) {
                    if (jQuery.css(elem, "float") === "none") {
                        if (!propTween) {
                            anim.done(function() {
                                style.display = restoreDisplay;
                            });
                            if (restoreDisplay == null) {
                                display = style.display;
                                restoreDisplay = display === "none" ? "" :
                                    display;
                            }
                        }
                        style.display = "inline-block";
                    }
                }
            }
            if (opts.overflow) {
                style.overflow = "hidden";
                anim.always(function() {
                    style.overflow = opts.overflow[0];
                    style.overflowX = opts.overflow[1];
                    style.overflowY = opts.overflow[2];
                });
            }
            propTween = !1;
            for (prop in orig) {
                if (!propTween) {
                    if (dataShow) {
                        if ("hidden" in dataShow) {
                            hidden = dataShow.hidden;
                        }
                    } else {
                        dataShow = dataPriv.access(elem, "fxshow", {
                            display: restoreDisplay
                        });
                    }
                    if (toggle) {
                        dataShow.hidden = !hidden;
                    }
                    if (hidden) {
                        showHide([elem], true);
                    }
                    anim.done(function() {
                        if (!hidden) {
                            showHide([elem]);
                        }
                        dataPriv.remove(elem, "fxshow");
                        for (prop in orig) {
                            jQuery.style(elem, prop, orig[prop]);
                        }
                    });
                }
                propTween = createTween(hidden ? dataShow[prop] : 0, prop, anim);
                if (!(prop in dataShow)) {
                    dataShow[prop] = propTween.start;
                    if (hidden) {
                        propTween.end = propTween.start;
                        propTween.start = 0;
                    }
                }
            }
        }

        function propFilter(props, specialEasing) {
            var index, name, easing, value, hooks;
            for (index in props) {
                name = camelCase(index);
                easing = specialEasing[name];
                value = props[index];
                if (Array.isArray(value)) {
                    easing = value[1];
                    value = props[index] =
                        value[0];
                }
                if (index !== name) {
                    props[name] = value;
                    delete props[index];
                }
                hooks = jQuery.cssHooks[name];
                if (hooks && "expand" in hooks) {
                    value = hooks.expand(value);
                    delete props[name];
                    for (index in value) {
                        if (!(index in props)) {
                            props[index] = value[index];
                            specialEasing[index] = easing;
                        }
                    }
                } else {
                    specialEasing[name] = easing;
                }
            }
        }

        function Animation(elem, properties, options) {
            var result, stopped, index = 0,
                length = Animation.prefilters.length,
                deferred = jQuery.Deferred().always(function() {
                    delete tick.elem;
                }),
                tick = function() {
                    if (stopped) {
                        return false;
                    }
                    var currentTime = fxNow || createFxNow(),
                        remaining = Math.max(0, animation.startTime + animation.duration - currentTime),
                        temp = remaining / animation.duration || 0,
                        percent = 1 - temp,
                        index = 0,
                        length = animation.tweens.length;
                    for (; index < length; index++) {
                        animation.tweens[index].run(percent);
                    }
                    deferred.notifyWith(elem, [animation, percent, remaining]);
                    if (percent < 1 && length) {
                        return remaining;
                    }
                    if (!length) {
                        deferred.notifyWith(elem, [animation, 1, 0]);
                    }
                    deferred.resolveWith(elem, [animation]);
                    return false;
                },
                animation =
                deferred.promise({
                    elem: elem,
                    props: jQuery.extend({}, properties),
                    opts: jQuery.extend(true, {
                        specialEasing: {},
                        easing: jQuery.easing._default
                    }, options),
                    originalProperties: properties,
                    originalOptions: options,
                    startTime: fxNow || createFxNow(),
                    duration: options.duration,
                    tweens: [],
                    createTween: function(prop, end) {
                        var tween = jQuery.Tween(elem, animation.opts, prop, end, animation.opts.specialEasing[prop] || animation.opts.easing);
                        animation.tweens.push(tween);
                        return tween;
                    },
                    stop: function(gotoEnd) {
                        var index = 0,
                            length = gotoEnd ? animation.tweens.length : 0;
                        if (stopped) {
                            return this;
                        }
                        stopped = !0;
                        for (; index < length; index++) {
                            animation.tweens[index].run(1);
                        }
                        if (gotoEnd) {
                            deferred.notifyWith(elem, [animation, 1, 0]);
                            deferred.resolveWith(elem, [animation, gotoEnd]);
                        } else {
                            deferred.rejectWith(elem, [animation, gotoEnd]);
                        }
                        return this;
                    }
                }),
                props = animation.props;
            propFilter(props, animation.opts.specialEasing);
            for (; index < length; index++) {
                result = Animation.prefilters[index].call(animation, elem, props, animation.opts);
                if (result) {
                    if (isFunction(result.stop)) {
                        jQuery._queueHooks(animation.elem, animation.opts.queue).stop = result.stop.bind(result);
                    }
                    return result;
                }
            }
            jQuery.map(props, createTween, animation);
            if (isFunction(animation.opts.start)) {
                animation.opts.start.call(elem, animation);
            }
            animation.progress(animation.opts.progress).done(animation.opts.done, animation.opts.complete).fail(animation.opts.fail).always(animation.opts.always);
            jQuery.fx.timer(jQuery.extend(tick, {
                elem: elem,
                anim: animation,
                queue: animation.opts.queue
            }));
            return animation;
        }
        jQuery.Animation = jQuery.extend(Animation, {
            tweeners: {
                "*": [function(prop, value) {
                    var tween = this.createTween(prop, value);
                    adjustCSS(tween.elem, prop, rcssNum.exec(value), tween);
                    return tween;
                }]
            },
            tweener: function(props, callback) {
                if (isFunction(props)) {
                    callback = props;
                    props = ["*"];
                } else {
                    props = props.match(rnothtmlwhite);
                }
                var prop, index = 0,
                    length = props.length;
                for (; index < length; index++) {
                    prop = props[index];
                    Animation.tweeners[prop] = Animation.tweeners[prop] || [];
                    Animation.tweeners[prop].unshift(callback);
                }
            },
            prefilters: [
                defaultPrefilter
            ],
            prefilter: function(callback, prepend) {
                if (prepend) {
                    Animation.prefilters.unshift(callback);
                } else {
                    Animation.prefilters.push(callback);
                }
            }
        });
        jQuery.speed = function(speed, easing, fn) {
            var opt = speed && typeof speed === "object" ? jQuery.extend({}, speed) : {
                complete: fn || !fn && easing || isFunction(speed) && speed,
                duration: speed,
                easing: fn && easing || easing && !isFunction(easing) && easing
            };
            if (jQuery.fx.off) {
                opt.duration = 0;
            } else {
                if (typeof opt.duration !== "number") {
                    if (opt.duration in jQuery.fx.speeds) {
                        opt.duration = jQuery.fx.speeds[opt.duration];
                    } else {
                        opt.duration = jQuery.fx.speeds._default;
                    }
                }
            }
            if (opt.queue == null || opt.queue === true) {
                opt.queue = "fx";
            }
            opt.old = opt.complete;
            opt.complete = function() {
                if (isFunction(opt.old)) {
                    opt.old.call(this);
                }
                if (opt.queue) {
                    jQuery.dequeue(this, opt.queue);
                }
            };
            return opt;
        };
        jQuery.fn.extend({
            fadeTo: function(speed, to, easing, callback) {
                return this.filter(isHiddenWithinTree).css("opacity", 0).show().end().animate({
                    opacity: to
                }, speed, easing, callback);
            },
            animate: function(prop, speed, easing,
                callback) {
                var empty = jQuery.isEmptyObject(prop),
                    optall = jQuery.speed(speed, easing, callback),
                    doAnimation = function() {
                        var anim = Animation(this, jQuery.extend({}, prop), optall);
                        if (empty || dataPriv.get(this, "finish")) {
                            anim.stop(true);
                        }
                    };
                doAnimation.finish = doAnimation;
                return empty || optall.queue === !1 ? this.each(doAnimation) : this.queue(optall.queue, doAnimation);
            },
            stop: function(type, clearQueue, gotoEnd) {
                var stopQueue = function(hooks) {
                    var stop = hooks.stop;
                    delete hooks.stop;
                    stop(gotoEnd);
                };
                if (typeof type !== "string") {
                    gotoEnd = clearQueue;
                    clearQueue = type;
                    type = undefined;
                }
                if (clearQueue && type !== false) {
                    this.queue(type || "fx", []);
                }
                return this.each(function() {
                    var dequeue = !0,
                        index = type != null && type + "queueHooks",
                        timers = jQuery.timers,
                        data = dataPriv.get(this);
                    if (index) {
                        if (data[index] && data[index].stop) {
                            stopQueue(data[index]);
                        }
                    } else {
                        for (index in data) {
                            if (data[index] && data[index].stop && rrun.test(index)) {
                                stopQueue(data[index]);
                            }
                        }
                    }
                    for (index = timers.length; index--;) {
                        if (timers[index].elem === this && (type == null || timers[index].queue === type)) {
                            timers[index].anim.stop(gotoEnd);
                            dequeue = !1;
                            timers.splice(index, 1);
                        }
                    }
                    if (dequeue || !gotoEnd) {
                        jQuery.dequeue(this, type);
                    }
                });
            },
            finish: function(type) {
                if (type !== false) {
                    type = type || "fx";
                }
                return this.each(function() {
                    var index, data = dataPriv.get(this),
                        queue = data[type + "queue"],
                        hooks = data[type + "queueHooks"],
                        timers = jQuery.timers,
                        length = queue ? queue.length : 0;
                    data.finish = !0;
                    jQuery.queue(this, type, []);
                    if (hooks && hooks.stop) {
                        hooks.stop.call(this, true);
                    }
                    for (index = timers.length; index--;) {
                        if (timers[index].elem === this && timers[index].queue === type) {
                            timers[index].anim.stop(true);
                            timers.splice(index, 1);
                        }
                    }
                    for (index = 0; index < length; index++) {
                        if (queue[index] && queue[index].finish) {
                            queue[index].finish.call(this);
                        }
                    }
                    delete data.finish;
                });
            }
        });
        jQuery.each(["toggle", "show", "hide"], function(i, name) {
            var cssFn = jQuery.fn[name];
            jQuery.fn[name] = function(speed, easing, callback) {
                return speed == null || typeof speed === "boolean" ? cssFn.apply(this, arguments) : this.animate(genFx(name, true), speed, easing, callback);
            };
        });
        jQuery
            .each({
                slideDown: genFx("show"),
                slideUp: genFx("hide"),
                slideToggle: genFx("toggle"),
                fadeIn: {
                    opacity: "show"
                },
                fadeOut: {
                    opacity: "hide"
                },
                fadeToggle: {
                    opacity: "toggle"
                }
            }, function(name, props) {
                jQuery.fn[name] = function(speed, easing, callback) {
                    return this.animate(props, speed, easing, callback);
                };
            });
        jQuery.timers = [];
        jQuery.fx.tick = function() {
            var timer, i = 0,
                timers = jQuery.timers;
            fxNow = Date.now();
            for (; i < timers.length; i++) {
                timer = timers[i];
                if (!timer() && timers[i] === timer) {
                    timers.splice(i--, 1);
                }
            }
            if (!timers.length) {
                jQuery.fx.stop();
            }
            fxNow = undefined;
        };
        jQuery.fx.timer = function(timer) {
            jQuery.timers.push(timer);
            jQuery.fx.start();
        };
        jQuery.fx.interval = 13;
        jQuery.fx.start = function() {
            if (inProgress) {
                return;
            }
            inProgress = !0;
            schedule();
        };
        jQuery.fx.stop = function() {
            inProgress = null;
        };
        jQuery.fx.speeds = {
            slow: 600,
            fast: 200,
            _default: 400
        };
        jQuery.fn.delay = function(time, type) {
            time = jQuery.fx ? jQuery.fx.speeds[time] || time : time;
            type = type || "fx";
            return this.queue(type, function(next, hooks) {
                var timeout = window.setTimeout(next, time);
                hooks.stop = function() {
                    window.clearTimeout(timeout);
                };
            });
        };
        (function() {
            var input = document.createElement("input"),
                select = document.createElement("select"),
                opt = select.appendChild(document.createElement("option"));
            input.type = "checkbox";
            support.checkOn = input.value !== "";
            support.optSelected = opt.selected;
            input = document.createElement("input");
            input.value = "t";
            input.type = "radio";
            support.radioValue = input.value === "t";
        })();
        var boolHook, attrHandle = jQuery.expr.attrHandle;
        jQuery.fn.extend({
            attr: function(name, value) {
                return access(this, jQuery.attr, name, value, arguments.length > 1);
            },
            removeAttr: function(name) {
                return this.each(function() {
                    jQuery.removeAttr(this, name);
                });
            }
        });
        jQuery.extend({
            attr: function(elem, name, value) {
                var ret, hooks, nType = elem.nodeType;
                if (nType === 3 || nType === 8 || nType === 2) {
                    return;
                }
                if (typeof elem.getAttribute === "undefined") {
                    return jQuery.prop(elem, name, value);
                }
                if (nType !== 1 || !jQuery.isXMLDoc(elem)) {
                    hooks = jQuery.attrHooks[name.toLowerCase()] || (jQuery.expr.match.bool.test(name) ? boolHook : undefined);
                }
                if (value !==
                    undefined) {
                    if (value === null) {
                        jQuery.removeAttr(elem, name);
                        return;
                    }
                    if (hooks && "set" in hooks && (ret = hooks.set(elem, value, name)) !== undefined) {
                        return ret;
                    }
                    elem.setAttribute(name, value + "");
                    return value;
                }
                if (hooks && "get" in hooks && (ret = hooks.get(elem, name)) !== null) {
                    return ret;
                }
                ret = jQuery.find.attr(elem, name);
                return ret == null ? undefined : ret;
            },
            attrHooks: {
                type: {
                    set: function(elem, value) {
                        if (!support.radioValue && value === "radio" && nodeName(elem, "input")) {
                            var val = elem.value;
                            elem.setAttribute("type", value);
                            if (val) {
                                elem.value = val;
                            }
                            return value;
                        }
                    }
                }
            },
            removeAttr: function(elem, value) {
                var name, i = 0,
                    attrNames = value && value.match(rnothtmlwhite);
                if (attrNames && elem.nodeType === 1) {
                    while ((name = attrNames[i++])) {
                        elem.removeAttribute(name);
                    }
                }
            }
        });
        boolHook = {
            set: function(elem, value, name) {
                if (value === false) {
                    jQuery.removeAttr(elem, name);
                } else {
                    elem.setAttribute(name, name);
                }
                return name;
            }
        };
        jQuery.each(jQuery.expr.match.bool.source.match(/\w+/g), function(i, name) {
            var getter = attrHandle[name] || jQuery.find.attr;
            attrHandle[name] = function(
                elem, name, isXML) {
                var ret, handle, lowercaseName = name.toLowerCase();
                if (!isXML) {
                    handle = attrHandle[lowercaseName];
                    attrHandle[lowercaseName] = ret;
                    ret = getter(elem, name, isXML) != null ? lowercaseName : null;
                    attrHandle[lowercaseName] = handle;
                }
                return ret;
            };
        });
        var rfocusable = /^(?:input|select|textarea|button)$/i,
            rclickable = /^(?:a|area)$/i;
        jQuery.fn.extend({
            prop: function(name, value) {
                return access(this, jQuery.prop, name, value, arguments.length > 1);
            },
            removeProp: function(name) {
                return this.each(function() {
                    delete this[jQuery.propFix[name] || name];
                });
            }
        });
        jQuery.extend({
            prop: function(elem, name, value) {
                var ret, hooks, nType = elem.nodeType;
                if (nType === 3 || nType === 8 || nType === 2) {
                    return;
                }
                if (nType !== 1 || !jQuery.isXMLDoc(elem)) {
                    name = jQuery.propFix[name] || name;
                    hooks = jQuery.propHooks[name];
                }
                if (value !== undefined) {
                    if (hooks && "set" in hooks && (ret = hooks.set(elem, value, name)) !== undefined) {
                        return ret;
                    }
                    return (elem[name] = value);
                }
                if (hooks && "get" in hooks && (ret = hooks.get(elem, name)) !== null) {
                    return ret;
                }
                return elem[name];
            },
            propHooks: {
                tabIndex: {
                    get: function(elem) {
                        var tabindex = jQuery.find.attr(elem, "tabindex");
                        if (tabindex) {
                            return parseInt(tabindex, 10);
                        }
                        if (rfocusable.test(elem.nodeName) || rclickable.test(elem.nodeName) && elem.href) {
                            return 0;
                        }
                        return -1;
                    }
                }
            },
            propFix: {
                "for": "htmlFor",
                "class": "className"
            }
        });
        if (!support.optSelected) {
            jQuery.propHooks.selected = {
                get: function(elem) {
                    var parent = elem.parentNode;
                    if (parent && parent.parentNode) {
                        parent.parentNode.selectedIndex;
                    }
                    return null;
                },
                set: function(elem) {
                    var parent = elem.parentNode;
                    if (parent) {
                        parent.selectedIndex;
                        if (parent.parentNode) {
                            parent.parentNode.selectedIndex;
                        }
                    }
                }
            };
        }
        jQuery.each(["tabIndex", "readOnly", "maxLength", "cellSpacing", "cellPadding", "rowSpan", "colSpan", "useMap", "frameBorder", "contentEditable"], function() {
            jQuery.propFix[this.toLowerCase()] = this;
        });

        function stripAndCollapse(value) {
            var tokens = value.match(rnothtmlwhite) || [];
            return tokens.join(" ");
        }

        function getClass(elem) {
            return elem.getAttribute && elem.getAttribute("class") || "";
        }

        function classesToArray(value) {
            if (Array.isArray(value)) {
                return value;
            }
            if (typeof value === "string") {
                return value.match(rnothtmlwhite) || [];
            }
            return [];
        }
        jQuery.fn.extend({
            addClass: function(value) {
                var classes, elem, cur, curValue, clazz, j, finalValue, i = 0;
                if (isFunction(value)) {
                    return this.each(function(j) {
                        jQuery(this).addClass(value.call(this, j, getClass(this)));
                    });
                }
                classes = classesToArray(value);
                if (classes.length) {
                    while ((elem = this[i++])) {
                        curValue = getClass(elem);
                        cur = elem.nodeType === 1 && (" " + stripAndCollapse(curValue) + " ");
                        if (cur) {
                            j = 0;
                            while ((clazz = classes[j++])) {
                                if (cur.indexOf(" " + clazz + " ") < 0) {
                                    cur += clazz + " ";
                                }
                            }
                            finalValue = stripAndCollapse(cur);
                            if (curValue !== finalValue) {
                                elem.setAttribute("class", finalValue);
                            }
                        }
                    }
                }
                return this;
            },
            removeClass: function(value) {
                var classes, elem, cur, curValue, clazz, j, finalValue, i = 0;
                if (isFunction(value)) {
                    return this.each(function(j) {
                        jQuery(this).removeClass(value.call(this, j, getClass(this)));
                    });
                }
                if (!arguments.length) {
                    return this.attr("class", "");
                }
                classes = classesToArray(value);
                if (classes.length) {
                    while ((elem = this[i++])) {
                        curValue = getClass(
                            elem);
                        cur = elem.nodeType === 1 && (" " + stripAndCollapse(curValue) + " ");
                        if (cur) {
                            j = 0;
                            while ((clazz = classes[j++])) {
                                while (cur.indexOf(" " + clazz + " ") > -1) {
                                    cur = cur.replace(" " + clazz + " ", " ");
                                }
                            }
                            finalValue = stripAndCollapse(cur);
                            if (curValue !== finalValue) {
                                elem.setAttribute("class", finalValue);
                            }
                        }
                    }
                }
                return this;
            },
            toggleClass: function(value, stateVal) {
                var type = typeof value,
                    isValidValue = type === "string" || Array.isArray(value);
                if (typeof stateVal === "boolean" && isValidValue) {
                    return stateVal ? this.addClass(value) : this.removeClass(value);
                }
                if (isFunction(value)) {
                    return this.each(function(i) {
                        jQuery(this).toggleClass(value.call(this, i, getClass(this), stateVal), stateVal);
                    });
                }
                return this.each(function() {
                    var className, i, self, classNames;
                    if (isValidValue) {
                        i = 0;
                        self = jQuery(this);
                        classNames = classesToArray(value);
                        while ((className = classNames[i++])) {
                            if (self.hasClass(className)) {
                                self.removeClass(className);
                            } else {
                                self.addClass(className);
                            }
                        }
                    } else if (value === undefined || type === "boolean") {
                        className = getClass(this);
                        if (className) {
                            dataPriv.set(this, "__className__", className);
                        }
                        if (this.setAttribute) {
                            this.setAttribute("class", className || value === false ? "" : dataPriv.get(this, "__className__") || "");
                        }
                    }
                });
            },
            hasClass: function(selector) {
                var className, elem, i = 0;
                className = " " + selector + " ";
                while ((elem = this[i++])) {
                    if (elem.nodeType === 1 && (" " + stripAndCollapse(getClass(elem)) + " ").indexOf(className) > -1) {
                        return true;
                    }
                }
                return false;
            }
        });
        var rreturn = /\r/g;
        jQuery.fn.extend({
            val: function(value) {
                var hooks, ret, valueIsFunction, elem = this[0];
                if (!arguments.length) {
                    if (elem) {
                        hooks = jQuery.valHooks[elem.type] || jQuery.valHooks[elem.nodeName.toLowerCase()];
                        if (hooks && "get" in hooks && (ret = hooks.get(elem, "value")) !== undefined) {
                            return ret;
                        }
                        ret = elem.value;
                        if (typeof ret === "string") {
                            return ret.replace(rreturn, "");
                        }
                        return ret == null ? "" : ret;
                    }
                    return;
                }
                valueIsFunction = isFunction(value);
                return this.each(function(i) {
                    var val;
                    if (this.nodeType !== 1) {
                        return;
                    }
                    if (valueIsFunction) {
                        val = value.call(this, i, jQuery(this).val());
                    } else {
                        val = value;
                    }
                    if (val == null) {
                        val = "";
                    } else if (typeof val ===
                        "number") {
                        val += "";
                    } else if (Array.isArray(val)) {
                        val = jQuery.map(val, function(value) {
                            return value == null ? "" : value + "";
                        });
                    }
                    hooks = jQuery.valHooks[this.type] || jQuery.valHooks[this.nodeName.toLowerCase()];
                    if (!hooks || !("set" in hooks) || hooks.set(this, val, "value") === undefined) {
                        this.value = val;
                    }
                });
            }
        });
        jQuery.extend({
            valHooks: {
                option: {
                    get: function(elem) {
                        var val = jQuery.find.attr(elem, "value");
                        return val != null ? val : stripAndCollapse(jQuery.text(elem));
                    }
                },
                select: {
                    get: function(elem) {
                        var value, option, i, options = elem.options,
                            index = elem.selectedIndex,
                            one = elem.type === "select-one",
                            values = one ? null : [],
                            max = one ? index + 1 : options.length;
                        if (index < 0) {
                            i = max;
                        } else {
                            i = one ? index : 0;
                        }
                        for (; i < max; i++) {
                            option = options[i];
                            if ((option.selected || i === index) && !option.disabled && (!option.parentNode.disabled || !nodeName(option.parentNode, "optgroup"))) {
                                value = jQuery(option).val();
                                if (one) {
                                    return value;
                                }
                                values.push(value);
                            }
                        }
                        return values;
                    },
                    set: function(elem, value) {
                        var optionSet, option, options = elem.options,
                            values = jQuery.makeArray(value),
                            i = options
                            .length;
                        while (i--) {
                            option = options[i];
                            if (option.selected = jQuery.inArray(jQuery.valHooks.option.get(option), values) > -1) {
                                optionSet = !0;
                            }
                        }
                        if (!optionSet) {
                            elem.selectedIndex = -1;
                        }
                        return values;
                    }
                }
            }
        });
        jQuery.each(["radio", "checkbox"], function() {
            jQuery.valHooks[this] = {
                set: function(elem, value) {
                    if (Array.isArray(value)) {
                        return (elem.checked = jQuery.inArray(jQuery(elem).val(), value) > -1);
                    }
                }
            };
            if (!support.checkOn) {
                jQuery.valHooks[this].get = function(elem) {
                    return elem.getAttribute("value") === null ? "on" : elem.value;
                };
            }
        });
        support.focusin = "onfocusin" in window;
        var rfocusMorph = /^(?:focusinfocus|focusoutblur)$/,
            stopPropagationCallback = function(e) {
                e.stopPropagation();
            };
        jQuery.extend(jQuery.event, {
            trigger: function(event, data, elem, onlyHandlers) {
                var i, cur, tmp, bubbleType, ontype, handle, special, lastElement, eventPath = [elem || document],
                    type = hasOwn.call(event, "type") ? event.type : event,
                    namespaces = hasOwn.call(event, "namespace") ? event.namespace.split(".") : [];
                cur = lastElement = tmp = elem = elem || document;
                if (elem.nodeType === 3 || elem.nodeType === 8) {
                    return;
                }
                if (rfocusMorph.test(type + jQuery.event.triggered)) {
                    return;
                }
                if (type.indexOf(".") > -1) {
                    namespaces = type.split(".");
                    type = namespaces.shift();
                    namespaces.sort();
                }
                ontype = type.indexOf(":") < 0 && "on" + type;
                event = event[jQuery.expando] ? event : new jQuery.Event(type, typeof event === "object" && event);
                event.isTrigger = onlyHandlers ? 2 : 3;
                event.namespace = namespaces.join(".");
                event.rnamespace = event.namespace ? new RegExp("(^|\\.)" + namespaces.join("\\.(?:.*\\.|)") + "(\\.|$)") : null;
                event.result = undefined;
                if (!event.target) {
                    event.target = elem;
                }
                data = data == null ? [event] : jQuery.makeArray(data, [event]);
                special = jQuery.event.special[type] || {};
                if (!onlyHandlers && special.trigger && special.trigger.apply(elem, data) === false) {
                    return;
                }
                if (!onlyHandlers && !special.noBubble && !isWindow(elem)) {
                    bubbleType = special.delegateType || type;
                    if (!rfocusMorph.test(bubbleType + type)) {
                        cur = cur.parentNode;
                    }
                    for (; cur; cur = cur.parentNode) {
                        eventPath.push(cur);
                        tmp = cur;
                    }
                    if (tmp === (elem.ownerDocument || document)) {
                        eventPath.push(tmp.defaultView || tmp.parentWindow || window);
                    }
                }
                i = 0;
                while ((cur = eventPath[i++]) && !event.isPropagationStopped()) {
                    lastElement = cur;
                    event.type = i > 1 ? bubbleType : special.bindType || type;
                    handle = (dataPriv.get(cur, "events") || {})[event.type] && dataPriv.get(cur, "handle");
                    if (handle) {
                        handle.apply(cur, data);
                    }
                    handle = ontype && cur[ontype];
                    if (handle && handle.apply && acceptData(cur)) {
                        event.result = handle.apply(cur, data);
                        if (event.result === false) {
                            event.preventDefault();
                        }
                    }
                }
                event.type = type;
                if (!onlyHandlers && !event.isDefaultPrevented()) {
                    if ((!special._default || special._default.apply(eventPath.pop(), data) === false) && acceptData(elem)) {
                        if (ontype && isFunction(elem[type]) && !isWindow(elem)) {
                            tmp = elem[ontype];
                            if (tmp) {
                                elem[ontype] = null;
                            }
                            jQuery.event.triggered = type;
                            if (event.isPropagationStopped()) {
                                lastElement.addEventListener(type, stopPropagationCallback);
                            }
                            elem[type]();
                            if (event.isPropagationStopped()) {
                                lastElement.removeEventListener(type, stopPropagationCallback);
                            }
                            jQuery.event.triggered = undefined;
                            if (tmp) {
                                elem[ontype] = tmp;
                            }
                        }
                    }
                }
                return event.result;
            },
            simulate: function(type, elem, event) {
                var e = jQuery.extend(new jQuery.Event(), event, {
                    type: type,
                    isSimulated: !0
                });
                jQuery.event.trigger(e, null, elem);
            }
        });
        jQuery.fn.extend({
            trigger: function(type, data) {
                return this.each(function() {
                    jQuery.event.trigger(type, data, this);
                });
            },
            triggerHandler: function(type, data) {
                var elem = this[0];
                if (elem) {
                    return jQuery.event.trigger(type, data, elem, true);
                }
            }
        });
        if (!support.focusin) {
            jQuery.each({
                focus: "focusin",
                blur: "focusout"
            }, function(orig, fix) {
                var handler = function(event) {
                    jQuery.event.simulate(fix, event.target, jQuery.event.fix(event));
                };
                jQuery.event.special[fix] = {
                    setup: function() {
                        var doc = this.ownerDocument || this,
                            attaches = dataPriv.access(doc, fix);
                        if (!attaches) {
                            doc.addEventListener(orig, handler, true);
                        }
                        dataPriv.access(doc, fix, (attaches || 0) + 1);
                    },
                    teardown: function() {
                        var doc = this.ownerDocument || this,
                            attaches = dataPriv.access(doc, fix) - 1;
                        if (!attaches) {
                            doc.removeEventListener(orig, handler, true);
                            dataPriv.remove(doc, fix);
                        } else {
                            dataPriv.access(doc, fix, attaches);
                        }
                    }
                };
            });
        }
        var location = window.
        location;
        var nonce = Date.now();
        var rquery = (/\?/);
        jQuery.parseXML = function(data) {
            var xml;
            if (!data || typeof data !== "string") {
                return null;
            }
            try {
                xml = (new window.DOMParser()).parseFromString(data, "text/xml");
            } catch (e) {
                xml = undefined;
            }
            if (!xml || xml.getElementsByTagName("parsererror").length) {
                jQuery.error("Invalid XML: " + data);
            }
            return xml;
        };
        var rbracket = /\[\]$/,
            rCRLF = /\r?\n/g,
            rsubmitterTypes = /^(?:submit|button|image|reset|file)$/i,
            rsubmittable = /^(?:input|select|textarea|keygen)/i;

        function buildParams(prefix, obj, traditional, add) {
            var name;
            if (Array.isArray(obj)) {
                jQuery.each(obj, function(i, v) {
                    if (traditional || rbracket.test(prefix)) {
                        add(prefix, v);
                    } else {
                        buildParams(prefix + "[" + (typeof v === "object" && v != null ? i : "") + "]", v, traditional, add);
                    }
                });
            } else if (!traditional && toType(obj) === "object") {
                for (name in obj) {
                    buildParams(prefix + "[" + name + "]", obj[name], traditional, add);
                }
            } else {
                add(prefix, obj);
            }
        }
        jQuery.param = function(a, traditional) {
            var prefix, s = [],
                add = function(key, valueOrFunction) {
                    var value = isFunction(valueOrFunction) ? valueOrFunction() : valueOrFunction;
                    s[s.length] = encodeURIComponent(key) + "=" + encodeURIComponent(value == null ? "" : value);
                };
            if (Array.isArray(a) || (a.jquery && !jQuery.isPlainObject(a))) {
                jQuery.each(a, function() {
                    add(this.name, this.value);
                });
            } else {
                for (prefix in a) {
                    buildParams(prefix, a[prefix], traditional, add);
                }
            }
            return s.join("&");
        };
        jQuery.fn.extend({
            serialize: function() {
                return jQuery.param(this.serializeArray());
            },
            serializeArray: function() {
                return this.map(function() {
                    var elements = jQuery.prop(this, "elements");
                    return elements ? jQuery.makeArray(elements) : this;
                }).filter(function() {
                    var type = this.type;
                    return this.name && !jQuery(this).is(":disabled") && rsubmittable.test(this.nodeName) && !rsubmitterTypes.test(type) && (this.checked || !rcheckableType.test(type));
                }).map(function(i, elem) {
                    var val = jQuery(this).val();
                    if (val == null) {
                        return null;
                    }
                    if (Array.isArray(val)) {
                        return jQuery.map(val, function(val) {
                            return {
                                name: elem.name,
                                value: val.replace(rCRLF, "\r\n")
                            };
                        });
                    }
                    return {
                        name: elem.name,
                        value: val.replace(rCRLF, "\r\n")
                    };
                }).
                get();
            }
        });
        var r20 = /%20/g,
            rhash = /#.*$/,
            rantiCache = /([?&])_=[^&]*/,
            rheaders = /^(.*?):[ \t]*([^\r\n]*)$/mg,
            rlocalProtocol = /^(?:about|app|app-storage|.+-extension|file|res|widget):$/,
            rnoContent = /^(?:GET|HEAD)$/,
            rprotocol = /^\/\//,
            prefilters = {},
            transports = {},
            allTypes = "*/".concat("*"),
            originAnchor = document.createElement("a");
        originAnchor.href = location.href;

        function addToPrefiltersOrTransports(structure) {
            return function(dataTypeExpression, func) {
                if (typeof dataTypeExpression !== "string") {
                    func = dataTypeExpression;
                    dataTypeExpression = "*";
                }
                var dataType, i = 0,
                    dataTypes = dataTypeExpression.toLowerCase().match(rnothtmlwhite) || [];
                if (isFunction(func)) {
                    while ((dataType = dataTypes[i++])) {
                        if (dataType[0] === "+") {
                            dataType = dataType.slice(1) || "*";
                            (structure[dataType] = structure[dataType] || []).unshift(func);
                        } else {
                            (structure[dataType] = structure[dataType] || []).push(func);
                        }
                    }
                }
            };
        }

        function inspectPrefiltersOrTransports(structure, options, originalOptions, jqXHR) {
            var inspected = {},
                seekingTransport = (structure === transports);

            function
            inspect(dataType) {
                var selected;
                inspected[dataType] = !0;
                jQuery.each(structure[dataType] || [], function(_, prefilterOrFactory) {
                    var dataTypeOrTransport = prefilterOrFactory(options, originalOptions, jqXHR);
                    if (typeof dataTypeOrTransport === "string" && !seekingTransport && !inspected[dataTypeOrTransport]) {
                        options.dataTypes.unshift(dataTypeOrTransport);
                        inspect(dataTypeOrTransport);
                        return false;
                    } else if (seekingTransport) {
                        return !(selected = dataTypeOrTransport);
                    }
                });
                return selected;
            }
            return inspect(options.dataTypes[0]) || !inspected["*"] && inspect("*");
        }

        function ajaxExtend(target, src) {
            var key, deep, flatOptions = jQuery.ajaxSettings.flatOptions || {};
            for (key in src) {
                if (src[key] !== undefined) {
                    (flatOptions[key] ? target : (deep || (deep = {})))[key] = src[key];
                }
            }
            if (deep) {
                jQuery.extend(true, target, deep);
            }
            return target;
        }

        function ajaxHandleResponses(s, jqXHR, responses) {
            var ct, type, finalDataType, firstDataType, contents = s.contents,
                dataTypes = s.dataTypes;
            while (dataTypes[0] === "*") {
                dataTypes.shift();
                if (ct === undefined) {
                    ct = s.mimeType || jqXHR.
                    getResponseHeader("Content-Type");
                }
            }
            if (ct) {
                for (type in contents) {
                    if (contents[type] && contents[type].test(ct)) {
                        dataTypes.unshift(type);
                        break;
                    }
                }
            }
            if (dataTypes[0] in responses) {
                finalDataType = dataTypes[0];
            } else {
                for (type in responses) {
                    if (!dataTypes[0] || s.converters[type + " " + dataTypes[0]]) {
                        finalDataType = type;
                        break;
                    }
                    if (!firstDataType) {
                        firstDataType = type;
                    }
                }
                finalDataType = finalDataType || firstDataType;
            }
            if (finalDataType) {
                if (finalDataType !== dataTypes[0]) {
                    dataTypes.unshift(finalDataType);
                }
                return responses[finalDataType];
            }
        }

        function ajaxConvert(s, response, jqXHR, isSuccess) {
            var conv2, current, conv, tmp, prev, converters = {},
                dataTypes = s.dataTypes.slice();
            if (dataTypes[1]) {
                for (conv in s.converters) {
                    converters[conv.toLowerCase()] = s.converters[conv];
                }
            }
            current = dataTypes.shift();
            while (current) {
                if (s.responseFields[current]) {
                    jqXHR[s.responseFields[current]] = response;
                }
                if (!prev && isSuccess && s.dataFilter) {
                    response = s.dataFilter(response, s.dataType);
                }
                prev = current;
                current = dataTypes.shift();
                if (current) {
                    if (current === "*") {
                        current
                            = prev;
                    } else if (prev !== "*" && prev !== current) {
                        conv = converters[prev + " " + current] || converters["* " + current];
                        if (!conv) {
                            for (conv2 in converters) {
                                tmp = conv2.split(" ");
                                if (tmp[1] === current) {
                                    conv = converters[prev + " " + tmp[0]] || converters["* " + tmp[0]];
                                    if (conv) {
                                        if (conv === true) {
                                            conv = converters[conv2];
                                        } else if (converters[conv2] !== true) {
                                            current = tmp[0];
                                            dataTypes.unshift(tmp[1]);
                                        }
                                        break;
                                    }
                                }
                            }
                        }
                        if (conv !== true) {
                            if (conv && s.throws) {
                                response = conv(response);
                            } else {
                                try {
                                    response = conv(response);
                                } catch (e) {
                                    return {
                                        state: "parsererror",
                                        error: conv ? e : "No conversion from " + prev + " to " + current
                                    };
                                }
                            }
                        }
                    }
                }
            }
            return {
                state: "success",
                data: response
            };
        }
        jQuery.extend({
            active: 0,
            lastModified: {},
            etag: {},
            ajaxSettings: {
                url: location.href,
                type: "GET",
                isLocal: rlocalProtocol.test(location.protocol),
                global: !0,
                processData: !0,
                async: !0,
                contentType: "application/x-www-form-urlencoded; charset=UTF-8",
                accepts: {
                    "*": allTypes,
                    text: "text/plain",
                    html: "text/html",
                    xml: "application/xml, text/xml",
                    json: "application/json, text/javascript"
                },
                contents: {
                    xml: /\bxml\b/,
                    html: /\bhtml/,
                    json: /\bjson\b/
                },
                responseFields: {
                    xml: "responseXML",
                    text: "responseText",
                    json: "responseJSON"
                },
                converters: {
                    "* text": String,
                    "text html": !0,
                    "text json": JSON.parse,
                    "text xml": jQuery.parseXML
                },
                flatOptions: {
                    url: !0,
                    context: !0
                }
            },
            ajaxSetup: function(target, settings) {
                return settings ? ajaxExtend(ajaxExtend(target, jQuery.ajaxSettings), settings) : ajaxExtend(jQuery.ajaxSettings, target);
            },
            ajaxPrefilter: addToPrefiltersOrTransports(prefilters),
            ajaxTransport: addToPrefiltersOrTransports(transports),
            ajax: function(url, options) {
                if (typeof url === "object") {
                    options = url;
                    url = undefined;
                }
                options = options || {};
                var transport, cacheURL, responseHeadersString, responseHeaders, timeoutTimer, urlAnchor, completed, fireGlobals, i, uncached, s = jQuery.ajaxSetup({}, options),
                    callbackContext = s.context || s,
                    globalEventContext = s.context && (callbackContext.nodeType || callbackContext.jquery) ? jQuery(callbackContext) : jQuery.event,
                    deferred = jQuery.Deferred(),
                    completeDeferred = jQuery.Callbacks("once memory"),
                    statusCode = s.statusCode || {},
                    requestHeaders = {},
                    requestHeadersNames = {},
                    strAbort = "canceled",
                    jqXHR = {
                        readyState: 0,
                        getResponseHeader: function(key) {
                            var match;
                            if (completed) {
                                if (!responseHeaders) {
                                    responseHeaders = {};
                                    while ((match = rheaders.exec(responseHeadersString))) {
                                        responseHeaders[match[1].toLowerCase()] = match[2];
                                    }
                                }
                                match = responseHeaders[key.toLowerCase()];
                            }
                            return match == null ? null : match;
                        },
                        getAllResponseHeaders: function() {
                            return completed ? responseHeadersString : null;
                        },
                        setRequestHeader: function(name, value) {
                            if (completed == null) {
                                name = requestHeadersNames[name.toLowerCase()] = requestHeadersNames[name.toLowerCase()] || name;
                                requestHeaders[name] = value;
                            }
                            return this;
                        },
                        overrideMimeType: function(type) {
                            if (completed == null) {
                                s.mimeType = type;
                            }
                            return this;
                        },
                        statusCode: function(map) {
                            var code;
                            if (map) {
                                if (completed) {
                                    jqXHR.always(map[jqXHR.status]);
                                } else {
                                    for (code in map) {
                                        statusCode[code] = [statusCode[code], map[code]];
                                    }
                                }
                            }
                            return this;
                        },
                        abort: function(statusText) {
                            var finalText = statusText || strAbort;
                            if (transport) {
                                transport.abort(finalText);
                            }
                            done(0,
                                finalText);
                            return this;
                        }
                    };
                deferred.promise(jqXHR);
                s.url = ((url || s.url || location.href) + "").replace(rprotocol, location.protocol + "//");
                s.type = options.method || options.type || s.method || s.type;
                s.dataTypes = (s.dataType || "*").toLowerCase().match(rnothtmlwhite) || [""];
                if (s.crossDomain == null) {
                    urlAnchor = document.createElement("a");
                    try {
                        urlAnchor.href = s.url;
                        urlAnchor.href = urlAnchor.href;
                        s.crossDomain = originAnchor.protocol + "//" + originAnchor.host !== urlAnchor.protocol + "//" + urlAnchor.host;
                    } catch (e) {
                        s.crossDomain = !0;
                    }
                }
                if (s.data && s.processData && typeof s.data !== "string") {
                    s.data = jQuery.param(s.data, s.traditional);
                }
                inspectPrefiltersOrTransports(prefilters, s, options, jqXHR);
                if (completed) {
                    return jqXHR;
                }
                fireGlobals = jQuery.event && s.global;
                if (fireGlobals && jQuery.active++ === 0) {
                    jQuery.event.trigger("ajaxStart");
                }
                s.type = s.type.toUpperCase();
                s.hasContent = !rnoContent.test(s.type);
                cacheURL = s.url.replace(rhash, "");
                if (!s.hasContent) {
                    uncached = s.url.slice(cacheURL.length);
                    if (s.data && (s.processData || typeof s.data === "string")) {
                        cacheURL += (rquery.test(cacheURL) ? "&" : "?") + s.data;
                        delete s.data;
                    }
                    if (s.cache === false) {
                        cacheURL = cacheURL.replace(rantiCache, "$1");
                        uncached = (rquery.test(cacheURL) ? "&" : "?") + "_=" + (nonce++) + uncached;
                    }
                    s.url = cacheURL + uncached;
                } else if (s.data && s.processData && (s.contentType || "").indexOf("application/x-www-form-urlencoded") === 0) {
                    s.data = s.data.replace(r20, "+");
                }
                if (s.ifModified) {
                    if (jQuery.lastModified[cacheURL]) {
                        jqXHR.setRequestHeader("If-Modified-Since", jQuery.lastModified[cacheURL]);
                    }
                    if (jQuery.etag[cacheURL]) {
                        jqXHR.setRequestHeader("If-None-Match", jQuery.etag[cacheURL]);
                    }
                }
                if (s.data && s.hasContent && s.contentType !== false || options.contentType) {
                    jqXHR.setRequestHeader("Content-Type", s.contentType);
                }
                jqXHR.setRequestHeader("Accept", s.dataTypes[0] && s.accepts[s.dataTypes[0]] ? s.accepts[s.dataTypes[0]] + (s.dataTypes[0] !== "*" ? ", " + allTypes + "; q=0.01" : "") : s.accepts["*"]);
                for (i in s.headers) {
                    jqXHR.setRequestHeader(i, s.headers[i]);
                }
                if (s.beforeSend && (s.beforeSend.call(callbackContext, jqXHR, s) === false || completed)) {
                    return jqXHR.abort();
                }
                strAbort = "abort";
                completeDeferred.add(s.complete);
                jqXHR.done(s.success);
                jqXHR.fail(s.error);
                transport = inspectPrefiltersOrTransports(transports, s, options, jqXHR);
                if (!transport) {
                    done(-1, "No Transport");
                } else {
                    jqXHR.readyState = 1;
                    if (fireGlobals) {
                        globalEventContext.trigger("ajaxSend", [jqXHR, s]);
                    }
                    if (completed) {
                        return jqXHR;
                    }
                    if (s.async && s.timeout > 0) {
                        timeoutTimer = window.setTimeout(function() {
                            jqXHR.abort("timeout");
                        }, s.timeout);
                    }
                    try {
                        completed = !1;
                        transport.send(requestHeaders, done);
                    } catch (e) {
                        if (completed) {
                            throw e;
                        }
                        done(-1, e);
                    }
                }

                function done(status, nativeStatusText, responses, headers) {
                    var isSuccess, success, error, response, modified, statusText = nativeStatusText;
                    if (completed) {
                        return;
                    }
                    completed = !0;
                    if (timeoutTimer) {
                        window.clearTimeout(timeoutTimer);
                    }
                    transport = undefined;
                    responseHeadersString = headers || "";
                    jqXHR.readyState = status > 0 ? 4 : 0;
                    isSuccess = status >= 200 && status < 300 || status === 304;
                    if (responses) {
                        response = ajaxHandleResponses(s, jqXHR, responses);
                    }
                    response = ajaxConvert(s, response, jqXHR,
                        isSuccess);
                    if (isSuccess) {
                        if (s.ifModified) {
                            modified = jqXHR.getResponseHeader("Last-Modified");
                            if (modified) {
                                jQuery.lastModified[cacheURL] = modified;
                            }
                            modified = jqXHR.getResponseHeader("etag");
                            if (modified) {
                                jQuery.etag[cacheURL] = modified;
                            }
                        }
                        if (status === 204 || s.type === "HEAD") {
                            statusText = "nocontent";
                        } else if (status === 304) {
                            statusText = "notmodified";
                        } else {
                            statusText = response.state;
                            success = response.data;
                            error = response.error;
                            isSuccess = !error;
                        }
                    } else {
                        error = statusText;
                        if (status || !statusText) {
                            statusText = "error";
                            if (status < 0) {
                                status = 0;
                            }
                        }
                    }
                    jqXHR.status = status;
                    jqXHR.statusText = (nativeStatusText || statusText) + "";
                    if (isSuccess) {
                        deferred.resolveWith(callbackContext, [success, statusText, jqXHR]);
                    } else {
                        deferred.rejectWith(callbackContext, [jqXHR, statusText, error]);
                    }
                    jqXHR.statusCode(statusCode);
                    statusCode = undefined;
                    if (fireGlobals) {
                        globalEventContext.trigger(isSuccess ? "ajaxSuccess" : "ajaxError", [jqXHR, s, isSuccess ? success : error]);
                    }
                    completeDeferred.fireWith(callbackContext, [jqXHR, statusText]);
                    if (fireGlobals) {
                        globalEventContext.trigger("ajaxComplete", [jqXHR, s]);
                        if (!(--jQuery.active)) {
                            jQuery.event.trigger("ajaxStop");
                        }
                    }
                }
                return jqXHR;
            },
            getJSON: function(url, data, callback) {
                return jQuery.get(url, data, callback, "json");
            },
            getScript: function(url, callback) {
                return jQuery.get(url, undefined, callback, "script");
            }
        });
        jQuery.each(["get", "post"], function(i, method) {
            jQuery[method] = function(url, data, callback, type) {
                if (isFunction(data)) {
                    type = type || callback;
                    callback = data;
                    data = undefined;
                }
                return jQuery.ajax(jQuery.extend({
                    url: url,
                    type: method,
                    dataType: type,
                    data: data,
                    success: callback
                }, jQuery.isPlainObject(url) && url));
            };
        });
        jQuery._evalUrl = function(url) {
            return jQuery.ajax({
                url: url,
                type: "GET",
                dataType: "script",
                cache: !0,
                async: !1,
                global: !1,
                "throws": !0
            });
        };
        jQuery.fn.extend({
            wrapAll: function(html) {
                var wrap;
                if (this[0]) {
                    if (isFunction(html)) {
                        html = html.call(this[0]);
                    }
                    wrap = jQuery(html, this[0].ownerDocument).eq(0).clone(true);
                    if (this[0].parentNode) {
                        wrap.insertBefore(this[0]);
                    }
                    wrap.map(function() {
                        var elem = this;
                        while (elem.firstElementChild) {
                            elem = elem.firstElementChild;
                        }
                        return elem;
                    }).append(this);
                }
                return this;
            },
            wrapInner: function(html) {
                if (isFunction(html)) {
                    return this.each(function(i) {
                        jQuery(this).wrapInner(html.call(this, i));
                    });
                }
                return this.each(function() {
                    var self = jQuery(this),
                        contents = self.contents();
                    if (contents.length) {
                        contents.wrapAll(html);
                    } else {
                        self.append(html);
                    }
                });
            },
            wrap: function(html) {
                var htmlIsFunction = isFunction(html);
                return this.each(function(i) {
                    jQuery(this).wrapAll(htmlIsFunction ? html.call(this, i) : html);
                });
            },
            unwrap: function(selector) {
                this.parent(selector).not("body").each(function() {
                    jQuery(this).replaceWith(this.childNodes);
                });
                return this;
            }
        });
        jQuery.expr.pseudos.hidden = function(elem) {
            return !jQuery.expr.pseudos.visible(elem);
        };
        jQuery.expr.pseudos.visible = function(elem) {
            return !!(elem.offsetWidth || elem.offsetHeight || elem.getClientRects().length);
        };
        jQuery.ajaxSettings.xhr = function() {
            try {
                return new window.XMLHttpRequest();
            } catch (e) {}
        };
        var xhrSuccessStatus = {
                0: 200,
                1223: 204
            },
            xhrSupported = jQuery.
        ajaxSettings.xhr();
        support.cors = !!xhrSupported && ("withCredentials" in xhrSupported);
        support.ajax = xhrSupported = !!xhrSupported;
        jQuery.ajaxTransport(function(options) {
            var callback, errorCallback;
            if (support.cors || xhrSupported && !options.crossDomain) {
                return {
                    send: function(headers, complete) {
                        var i, xhr = options.xhr();
                        xhr.open(options.type, options.url, options.async, options.username, options.password);
                        if (options.xhrFields) {
                            for (i in options.xhrFields) {
                                xhr[i] = options.xhrFields[i];
                            }
                        }
                        if (options.mimeType && xhr.overrideMimeType) {
                            xhr.overrideMimeType(options.mimeType);
                        }
                        if (!options.crossDomain && !headers["X-Requested-With"]) {
                            headers["X-Requested-With"] = "XMLHttpRequest";
                        }
                        for (i in headers) {
                            xhr.setRequestHeader(i, headers[i]);
                        }
                        callback = function(type) {
                            return function() {
                                if (callback) {
                                    callback = errorCallback = xhr.onload = xhr.onerror = xhr.onabort = xhr.ontimeout = xhr.onreadystatechange = null;
                                    if (type === "abort") {
                                        xhr.abort();
                                    } else if (type === "error") {
                                        if (typeof xhr.status !== "number") {
                                            complete(0, "error");
                                        } else {
                                            complete(xhr.status, xhr
                                                .statusText);
                                        }
                                    } else {
                                        complete(xhrSuccessStatus[xhr.status] || xhr.status, xhr.statusText, (xhr.responseType || "text") !== "text" || typeof xhr.responseText !== "string" ? {
                                            binary: xhr.response
                                        } : {
                                            text: xhr.responseText
                                        }, xhr.getAllResponseHeaders());
                                    }
                                }
                            };
                        };
                        xhr.onload = callback();
                        errorCallback = xhr.onerror = xhr.ontimeout = callback("error");
                        if (xhr.onabort !== undefined) {
                            xhr.onabort = errorCallback;
                        } else {
                            xhr.onreadystatechange = function() {
                                if (xhr.readyState === 4) {
                                    window.setTimeout(function() {
                                        if (callback) {
                                            errorCallback();
                                        }
                                    });
                                }
                            };
                        }
                        callback = callback("abort");
                        try {
                            xhr.send(options.hasContent && options.data || null);
                        } catch (e) {
                            if (callback) {
                                throw e;
                            }
                        }
                    },
                    abort: function() {
                        if (callback) {
                            callback();
                        }
                    }
                };
            }
        });
        jQuery.ajaxPrefilter(function(s) {
            if (s.crossDomain) {
                s.contents.script = !1;
            }
        });
        jQuery.ajaxSetup({
            accepts: {
                script: "text/javascript, application/javascript, " + "application/ecmascript, application/x-ecmascript"
            },
            contents: {
                script: /\b(?:java|ecma)script\b/
            },
            converters: {
                "text script": function(text) {
                    jQuery.globalEval(text);
                    return text;
                }
            }
        });
        jQuery.ajaxPrefilter("script", function(s) {
            if (s.cache === undefined) {
                s.cache = !1;
            }
            if (s.crossDomain) {
                s.type = "GET";
            }
        });
        jQuery.ajaxTransport("script", function(s) {
            if (s.crossDomain) {
                var script, callback;
                return {
                    send: function(_, complete) {
                        script = jQuery("<script>").prop({
                            charset: s.scriptCharset,
                            src: s.url
                        }).on("load error", callback = function(evt) {
                            script.remove();
                            callback = null;
                            if (evt) {
                                complete(evt.type === "error" ? 404 : 200, evt.type);
                            }
                        });
                        document.head.appendChild(script[0]);
                    },
                    abort: function() {
                        if (callback) {
                            callback();
                        }
                    }
                };
            }
        });
        var oldCallbacks = [],
            rjsonp = /(=)\?(?=&|$)|\?\?/;
        jQuery.ajaxSetup({
            jsonp: "callback",
            jsonpCallback: function() {
                var callback = oldCallbacks.pop() || (jQuery.expando + "_" + (nonce++));
                this[callback] = !0;
                return callback;
            }
        });
        jQuery.ajaxPrefilter("json jsonp", function(s, originalSettings, jqXHR) {
            var callbackName, overwritten, responseContainer, jsonProp = s.jsonp !== !1 && (rjsonp.test(s.url) ? "url" : typeof s.data === "string" && (s.contentType || "").indexOf("application/x-www-form-urlencoded") === 0 && rjsonp.test(s
                .data) && "data");
            if (jsonProp || s.dataTypes[0] === "jsonp") {
                callbackName = s.jsonpCallback = isFunction(s.jsonpCallback) ? s.jsonpCallback() : s.jsonpCallback;
                if (jsonProp) {
                    s[jsonProp] = s[jsonProp].replace(rjsonp, "$1" + callbackName);
                } else if (s.jsonp !== false) {
                    s.url += (rquery.test(s.url) ? "&" : "?") + s.jsonp + "=" + callbackName;
                }
                s.converters["script json"] = function() {
                    if (!responseContainer) {
                        jQuery.error(callbackName + " was not called");
                    }
                    return responseContainer[0];
                };
                s.dataTypes[0] = "json";
                overwritten = window[callbackName];
                window[callbackName] = function() {
                    responseContainer = arguments;
                };
                jqXHR.always(function() {
                    if (overwritten === undefined) {
                        jQuery(window).removeProp(callbackName);
                    } else {
                        window[callbackName] = overwritten;
                    }
                    if (s[callbackName]) {
                        s.jsonpCallback = originalSettings.jsonpCallback;
                        oldCallbacks.push(callbackName);
                    }
                    if (responseContainer && isFunction(overwritten)) {
                        overwritten(responseContainer[0]);
                    }
                    responseContainer = overwritten = undefined;
                });
                return "script";
            }
        });
        support.createHTMLDocument = (function() {
            var body = document.
            implementation.createHTMLDocument("").body;
            body.innerHTML = "<form></form><form></form>";
            return body.childNodes.length === 2;
        })();
        jQuery.parseHTML = function(data, context, keepScripts) {
            if (typeof data !== "string") {
                return [];
            }
            if (typeof context === "boolean") {
                keepScripts = context;
                context = !1;
            }
            var base, parsed, scripts;
            if (!context) {
                if (support.createHTMLDocument) {
                    context = document.implementation.createHTMLDocument("");
                    base = context.createElement("base");
                    base.href = document.location.href;
                    context.head.appendChild(base);
                } else {
                    context = document;
                }
            }
            parsed = rsingleTag.exec(data);
            scripts = !keepScripts && [];
            if (parsed) {
                return [context.createElement(parsed[1])];
            }
            parsed = buildFragment([data], context, scripts);
            if (scripts && scripts.length) {
                jQuery(scripts).remove();
            }
            return jQuery.merge([], parsed.childNodes);
        };
        jQuery.fn.load = function(url, params, callback) {
            var selector, type, response, self = this,
                off = url.indexOf(" ");
            if (off > -1) {
                selector = stripAndCollapse(url.slice(off));
                url = url.slice(0, off);
            }
            if (isFunction(params)) {
                callback = params;
                params = undefined;
            } else if (params && typeof params === "object") {
                type = "POST";
            }
            if (self.length > 0) {
                jQuery.ajax({
                    url: url,
                    type: type || "GET",
                    dataType: "html",
                    data: params
                }).done(function(responseText) {
                    response = arguments;
                    self.html(selector ? jQuery("<div>").append(jQuery.parseHTML(responseText)).find(selector) : responseText);
                }).always(callback && function(jqXHR, status) {
                    self.each(function() {
                        callback.apply(this, response || [jqXHR.responseText, status, jqXHR]);
                    });
                });
            }
            return this;
        };
        jQuery.each(["ajaxStart", "ajaxStop", "ajaxComplete", "ajaxError", "ajaxSuccess", "ajaxSend"], function(i, type) {
            jQuery.fn[type] = function(fn) {
                return this.on(type, fn);
            };
        });
        jQuery.expr.pseudos.animated = function(elem) {
            return jQuery.grep(jQuery.timers, function(fn) {
                return elem === fn.elem;
            }).length;
        };
        jQuery.offset = {
            setOffset: function(elem, options, i) {
                var curPosition, curLeft, curCSSTop, curTop, curOffset, curCSSLeft, calculatePosition, position = jQuery.css(elem, "position"),
                    curElem = jQuery(elem),
                    props = {};
                if (position === "static") {
                    elem.style.position = "relative";
                }
                curOffset = curElem.offset();
                curCSSTop = jQuery.css(elem, "top");
                curCSSLeft = jQuery.css(elem, "left");
                calculatePosition = (position === "absolute" || position === "fixed") && (curCSSTop + curCSSLeft).indexOf("auto") > -1;
                if (calculatePosition) {
                    curPosition = curElem.position();
                    curTop = curPosition.top;
                    curLeft = curPosition.left;
                } else {
                    curTop = parseFloat(curCSSTop) || 0;
                    curLeft = parseFloat(curCSSLeft) || 0;
                }
                if (isFunction(options)) {
                    options = options.call(elem, i, jQuery.extend({}, curOffset));
                }
                if (options.top != null) {
                    props.top = (options.top - curOffset.top) + curTop;
                }
                if (options.left != null) {
                    props.left = (options.left - curOffset.left) + curLeft;
                }
                if ("using" in options) {
                    options.using.call(elem, props);
                } else {
                    curElem.css(props);
                }
            }
        };
        jQuery.fn.extend({
            offset: function(options) {
                if (arguments.length) {
                    return options === undefined ? this : this.each(function(i) {
                        jQuery.offset.setOffset(this, options, i);
                    });
                }
                var rect, win, elem = this[0];
                if (!elem) {
                    return;
                }
                if (!elem.getClientRects().length) {
                    return {
                        top: 0,
                        left: 0
                    };
                }
                rect = elem.getBoundingClientRect();
                win = elem.ownerDocument.
                defaultView;
                return {
                    top: rect.top + win.pageYOffset,
                    left: rect.left + win.pageXOffset
                };
            },
            position: function() {
                if (!this[0]) {
                    return;
                }
                var offsetParent, offset, doc, elem = this[0],
                    parentOffset = {
                        top: 0,
                        left: 0
                    };
                if (jQuery.css(elem, "position") === "fixed") {
                    offset = elem.getBoundingClientRect();
                } else {
                    offset = this.offset();
                    doc = elem.ownerDocument;
                    offsetParent = elem.offsetParent || doc.documentElement;
                    while (offsetParent && (offsetParent === doc.body || offsetParent === doc.documentElement) && jQuery.css(offsetParent, "position") === "static") {
                        offsetParent = offsetParent.parentNode;
                    }
                    if (offsetParent && offsetParent !== elem && offsetParent.nodeType === 1) {
                        parentOffset = jQuery(offsetParent).offset();
                        parentOffset.top += jQuery.css(offsetParent, "borderTopWidth", true);
                        parentOffset.left += jQuery.css(offsetParent, "borderLeftWidth", true);
                    }
                }
                return {
                    top: offset.top - parentOffset.top - jQuery.css(elem, "marginTop", true),
                    left: offset.left - parentOffset.left - jQuery.css(elem, "marginLeft", true)
                };
            },
            offsetParent: function() {
                return this.map(function() {
                    var offsetParent =
                        this.offsetParent;
                    while (offsetParent && jQuery.css(offsetParent, "position") === "static") {
                        offsetParent = offsetParent.offsetParent;
                    }
                    return offsetParent || documentElement;
                });
            }
        });
        jQuery.each({
            scrollLeft: "pageXOffset",
            scrollTop: "pageYOffset"
        }, function(method, prop) {
            var top = "pageYOffset" === prop;
            jQuery.fn[method] = function(val) {
                return access(this, function(elem, method, val) {
                    var win;
                    if (isWindow(elem)) {
                        win = elem;
                    } else if (elem.nodeType === 9) {
                        win = elem.defaultView;
                    }
                    if (val === undefined) {
                        return win ? win[prop] : elem[method];
                    }
                    if (win) {
                        win.scrollTo(!top ? val : win.pageXOffset, top ? val : win.pageYOffset);
                    } else {
                        elem[method] = val;
                    }
                }, method, val, arguments.length);
            };
        });
        jQuery.each(["top", "left"], function(i, prop) {
            jQuery.cssHooks[prop] = addGetHookIf(support.pixelPosition, function(elem, computed) {
                if (computed) {
                    computed = curCSS(elem, prop);
                    return rnumnonpx.test(computed) ? jQuery(elem).position()[prop] + "px" : computed;
                }
            });
        });
        jQuery.each({
            Height: "height",
            Width: "width"
        }, function(name, type) {
            jQuery.each({
                padding: "inner" + name,
                content: type,
                "": "outer" + name
            }, function(defaultExtra, funcName) {
                jQuery.fn[funcName] = function(margin, value) {
                    var chainable = arguments.length && (defaultExtra || typeof margin !== "boolean"),
                        extra = defaultExtra || (margin === true || value === true ? "margin" : "border");
                    return access(this, function(elem, type, value) {
                        var doc;
                        if (isWindow(elem)) {
                            return funcName.indexOf("outer") === 0 ? elem["inner" + name] : elem.document.documentElement["client" + name];
                        }
                        if (elem.nodeType === 9) {
                            doc = elem.documentElement;
                            return Math.max(elem.body["scroll" + name], doc["scroll" + name], elem.body["offset" + name], doc["offset" + name], doc["client" + name]);
                        }
                        return value === undefined ? jQuery.css(elem, type, extra) : jQuery.style(elem, type, value, extra);
                    }, type, chainable ? margin : undefined, chainable);
                };
            });
        });
        jQuery.each(("blur focus focusin focusout resize scroll click dblclick " + "mousedown mouseup mousemove mouseover mouseout mouseenter mouseleave " + "change select submit keydown keypress keyup contextmenu").split(" "), function(i, name) {
            jQuery.fn[name] = function(data, fn) {
                return arguments
                    .length > 0 ? this.on(name, null, data, fn) : this.trigger(name);
            };
        });
        jQuery.fn.extend({
            hover: function(fnOver, fnOut) {
                return this.mouseenter(fnOver).mouseleave(fnOut || fnOver);
            }
        });
        jQuery.fn.extend({
            bind: function(types, data, fn) {
                return this.on(types, null, data, fn);
            },
            unbind: function(types, fn) {
                return this.off(types, null, fn);
            },
            delegate: function(selector, types, data, fn) {
                return this.on(types, selector, data, fn);
            },
            undelegate: function(selector, types, fn) {
                return arguments.length === 1 ? this.off(selector, "**") : this.off(types, selector || "**", fn);
            }
        });
        jQuery.proxy = function(fn, context) {
            var tmp, args, proxy;
            if (typeof context === "string") {
                tmp = fn[context];
                context = fn;
                fn = tmp;
            }
            if (!isFunction(fn)) {
                return undefined;
            }
            args = slice.call(arguments, 2);
            proxy = function() {
                return fn.apply(context || this, args.concat(slice.call(arguments)));
            };
            proxy.guid = fn.guid = fn.guid || jQuery.guid++;
            return proxy;
        };
        jQuery.holdReady = function(hold) {
            if (hold) {
                jQuery.readyWait++;
            } else {
                jQuery.ready(true);
            }
        };
        jQuery.isArray = Array.isArray;
        jQuery.parseJSON = JSON.parse;
        jQuery.nodeName = nodeName;
        jQuery.isFunction = isFunction;
        jQuery.isWindow = isWindow;
        jQuery.camelCase = camelCase;
        jQuery.type = toType;
        jQuery.now = Date.now;
        jQuery.isNumeric = function(obj) {
            var type = jQuery.type(obj);
            return (type === "number" || type === "string") && !isNaN(obj - parseFloat(obj));
        };
        if (typeof define === "function" && define.amd) {
            define("jquery", [], function() {
                return jQuery;
            });
        }
        var _jQuery = window.jQuery,
            _$ = window.$;
        jQuery.noConflict = function(deep) {
            if (window.$ === jQuery) {
                window.$ = _$;
            }
            if (deep && window.jQuery === jQuery) {
                window.jQuery = _jQuery;
            }
            return jQuery;
        };
        if (!noGlobal) {
            window.jQuery = window.$ = jQuery;
        }
        return jQuery;
    });;
    (function(factory) {
        if (typeof define === "function" && define.amd) {
            define(["jquery"], window, factory);
        } else if (typeof module === "object" && module.exports) {
            module.exports = factory(require("jquery"), window);
        } else {
            factory(window.jQuery, window);
        }
    })(function(jQuery, window) {
        "use strict";
        jQuery.migrateVersion = "3.0.1";
        (function() {
            var rbadVersions = /^[12]\./;
            if (!window.console || !window.console.log) {
                return;
            }
            if (
                !jQuery || rbadVersions.test(jQuery.fn.jquery)) {
                window.console.log("JQMIGRATE: jQuery 3.0.0+ REQUIRED");
            }
            if (jQuery.migrateWarnings) {
                window.console.log("JQMIGRATE: Migrate plugin loaded multiple times");
            }
            window.console.log("JQMIGRATE: Migrate is installed" + (jQuery.migrateMute ? "" : " with logging active") + ", version " + jQuery.migrateVersion);
        })();
        var warnedAbout = {};
        jQuery.migrateWarnings = [];
        if (jQuery.migrateTrace === undefined) {
            jQuery.migrateTrace = !1;
        }
        jQuery.migrateReset = function() {
            warnedAbout = {};
            jQuery.migrateWarnings.length = 0;
        };

        function migrateWarn(msg) {
            var console = window.console;
            if (!warnedAbout[msg]) {
                warnedAbout[msg] = !0;
                jQuery.migrateWarnings.push(msg);
                if (window.mw && window.mw.track) {
                    window.mw.track("mw.deprecate", "jquery-migrate");
                }
                if (console && console.warn && !jQuery.migrateMute) {
                    console.warn("JQMIGRATE: " + msg);
                    if (jQuery.migrateTrace && console.trace) {
                        console.trace();
                    }
                }
            }
        }

        function migrateWarnProp(obj, prop, value, msg) {
            Object.defineProperty(obj, prop, {
                configurable: !0,
                enumerable: !0,
                get: function() {
                    migrateWarn(msg);
                    return value;
                },
                set: function(newValue) {
                    migrateWarn(msg);
                    value = newValue;
                }
            });
        }

        function migrateWarnFunc(obj, prop, newFunc, msg) {
            obj[prop] = function() {
                migrateWarn(msg);
                return newFunc.apply(this, arguments);
            };
        }
        if (window.document.compatMode === "BackCompat") {
            migrateWarn("jQuery is not compatible with Quirks Mode");
        }
        var oldInit = jQuery.fn.init,
            oldIsNumeric = jQuery.isNumeric,
            oldFind = jQuery.find,
            rattrHashTest = /\[(\s*[-\w]+\s*)([~|^$*]?=)\s*([-\w#]*?#[-\w#]*)\s*\]/,
            rattrHashGlob = /\[(\s*[-\w]+\s*)([~|^$*]?=)\s*([-\w#]*?#[-\w#]*)\s*\]/g;
        jQuery.fn.init = function(arg1) {
            var args = Array.prototype.slice.call(arguments);
            if (typeof arg1 === "string" && arg1 === "#") {
                migrateWarn("jQuery( '#' ) is not a valid selector");
                args[0] = [];
            }
            return oldInit.apply(this, args);
        };
        jQuery.fn.init.prototype = jQuery.fn;
        jQuery.find = function(selector) {
            var args = Array.prototype.slice.call(arguments);
            if (typeof selector === "string" && rattrHashTest.test(selector)) {
                try {
                    window.document.querySelector(selector);
                } catch (err1) {
                    selector = selector.replace(rattrHashGlob, function(_, attr, op, value) {
                        return "[" + attr + op + "\"" + value + "\"]";
                    });
                    try {
                        window.document.querySelector(selector);
                        migrateWarn("Attribute selector with '#' must be quoted: " + args[0]);
                        args[0] = selector;
                    } catch (err2) {
                        migrateWarn("Attribute selector with '#' was not fixed: " + args[0]);
                    }
                }
            }
            return oldFind.apply(this, args);
        };
        var findProp;
        for (findProp in oldFind) {
            if (Object.prototype.hasOwnProperty.call(oldFind, findProp)) {
                jQuery.find[findProp] = oldFind[findProp];
            }
        }
        jQuery.fn.size = function() {
            migrateWarn("jQuery.fn.size() is deprecated and removed; use the .length property");
            return this.length;
        };
        jQuery.parseJSON = function() {
            migrateWarn("jQuery.parseJSON is deprecated; use JSON.parse");
            return JSON.parse.apply(null, arguments);
        };
        jQuery.isNumeric = function(val) {
            function isNumeric2(obj) {
                var realStringObj = obj && obj.toString();
                return !jQuery.isArray(obj) && (realStringObj - parseFloat(realStringObj) + 1) >= 0;
            }
            var newValue = oldIsNumeric(val),
                oldValue = isNumeric2(val);
            if (newValue !== oldValue) {
                migrateWarn("jQuery.isNumeric() should not be called on constructed objects");
            }
            return oldValue;
        };
        migrateWarnFunc(jQuery, "holdReady", jQuery.holdReady, "jQuery.holdReady is deprecated");
        migrateWarnFunc(jQuery, "unique", jQuery.uniqueSort, "jQuery.unique is deprecated; use jQuery.uniqueSort");
        migrateWarnProp(jQuery.expr, "filters", jQuery.expr.pseudos, "jQuery.expr.filters is deprecated; use jQuery.expr.pseudos");
        migrateWarnProp(jQuery.expr, ":", jQuery.expr.pseudos, "jQuery.expr[':'] is deprecated; use jQuery.expr.pseudos");
        var oldAjax = jQuery.ajax;
        jQuery.ajax = function() {
            var jQXHR = oldAjax.apply(this, arguments);
            if (jQXHR.promise) {
                migrateWarnFunc(jQXHR, "success", jQXHR.done, "jQXHR.success is deprecated and removed");
                migrateWarnFunc(jQXHR, "error", jQXHR.fail, "jQXHR.error is deprecated and removed");
                migrateWarnFunc(jQXHR, "complete", jQXHR.always, "jQXHR.complete is deprecated and removed");
            }
            return jQXHR;
        };
        var oldRemoveAttr = jQuery.fn.removeAttr,
            oldToggleClass = jQuery.fn.toggleClass,
            rmatchNonSpace = /\S+/g;
        jQuery.fn.removeAttr = function(name) {
            var self = this;
            jQuery.each(name.match(rmatchNonSpace), function(i, attr) {
                if (jQuery.expr.match.bool.test(attr)) {
                    migrateWarn("jQuery.fn.removeAttr no longer sets boolean properties: " + attr);
                    self.prop(attr, false);
                }
            });
            return oldRemoveAttr.apply(this, arguments);
        };
        jQuery.fn.toggleClass = function(state) {
            if (state !== undefined && typeof state !== "boolean") {
                return oldToggleClass.apply(this, arguments);
            }
            migrateWarn("jQuery.fn.toggleClass( boolean ) is deprecated");
            return this.each(function() {
                var className = this.getAttribute && this.getAttribute("class") || "";
                if (className) {
                    jQuery.data(this, "__className__", className);
                }
                if (this.setAttribute) {
                    this.setAttribute("class", className || state === false ? "" : jQuery.data(this, "__className__") || "");
                }
            });
        };
        var internalSwapCall = !1;
        if (jQuery.swap) {
            jQuery.each(["height", "width", "reliableMarginRight"], function(_, name) {
                var oldHook = jQuery.cssHooks[name] && jQuery.cssHooks[name].get;
                if (oldHook) {
                    jQuery.cssHooks[name].get = function() {
                        var ret;
                        internalSwapCall = !0;
                        ret = oldHook.apply(this, arguments);
                        internalSwapCall = !1;
                        return ret;
                    };
                }
            });
        }
        jQuery.swap = function(elem, options, callback, args) {
            var ret, name, old = {};
            if (!internalSwapCall) {
                migrateWarn("jQuery.swap() is undocumented and deprecated");
            }
            for (name in options) {
                old[name] = elem.style[name];
                elem.style[name] = options[name];
            }
            ret = callback.apply(elem, args || []);
            for (name in options) {
                elem.style[name] = old[name];
            }
            return ret;
        };
        var oldData = jQuery.data;
        jQuery.data = function(elem, name, value) {
            var curData;
            if (name && typeof name === "object" && arguments.length === 2) {
                curData = jQuery.hasData(elem) && oldData.call(this, elem);
                var sameKeys = {};
                for (var key in name) {
                    if (key !== jQuery.camelCase(key)) {
                        migrateWarn("jQuery.data() always sets/gets camelCased names: " + key);
                        curData[key] = name[key];
                    } else {
                        sameKeys[key] = name[key];
                    }
                }
                oldData.call(this, elem, sameKeys);
                return name;
            }
            if (name && typeof name === "string" && name !== jQuery.camelCase(name)) {
                curData = jQuery.hasData(elem) && oldData.call(this, elem);
                if (curData && name in curData) {
                    migrateWarn("jQuery.data() always sets/gets camelCased names: " + name);
                    if (arguments.length > 2) {
                        curData[name] = value;
                    }
                    return curData[name];
                }
            }
            return oldData.apply(this, arguments);
        };
        var oldTweenRun = jQuery.Tween.prototype.run;
        var linearEasing = function(pct) {
            return pct;
        };
        jQuery.Tween.prototype.run = function() {
            if (jQuery.easing[this.easing].length > 1) {
                migrateWarn("'jQuery.easing." + this.easing.toString() + "' should use only one argument");
                jQuery.easing[this.easing] = linearEasing;
            }
            oldTweenRun.apply(this, arguments);
        };
        jQuery.fx.interval = jQuery.fx.interval || 13;
        if (window.requestAnimationFrame) {
            migrateWarnProp(jQuery.fx, "interval", jQuery.fx.interval, "jQuery.fx.interval is deprecated");
        }
        var oldLoad = jQuery.fn.load,
            oldEventAdd = jQuery.event.add,
            originalFix = jQuery.event.fix;
        jQuery.event.props = [];
        jQuery.event.fixHooks = {};
        migrateWarnProp(jQuery.event.props, "concat", jQuery.event.props.concat, "jQuery.event.props.concat() is deprecated and removed");
        jQuery.event.fix = function(originalEvent) {
            var event, type =
                originalEvent.type,
                fixHook = this.fixHooks[type],
                props = jQuery.event.props;
            if (props.length) {
                migrateWarn("jQuery.event.props are deprecated and removed: " + props.join());
                while (props.length) {
                    jQuery.event.addProp(props.pop());
                }
            }
            if (fixHook && !fixHook._migrated_) {
                fixHook._migrated_ = !0;
                migrateWarn("jQuery.event.fixHooks are deprecated and removed: " + type);
                if ((props = fixHook.props) && props.length) {
                    while (props.length) {
                        jQuery.event.addProp(props.pop());
                    }
                }
            }
            event = originalFix.call(this, originalEvent);
            return fixHook && fixHook.filter ? fixHook.filter(event, originalEvent) : event;
        };
        jQuery.event.add = function(elem, types) {
            if (elem === window && types === "load" && window.document.readyState === "complete") {
                migrateWarn("jQuery(window).on('load'...) called after load event occurred");
            }
            return oldEventAdd.apply(this, arguments);
        };
        jQuery.each(["load", "unload", "error"], function(_, name) {
            jQuery.fn[name] = function() {
                var args = Array.prototype.slice.call(arguments, 0);
                if (name === "load" && typeof args[0] === "string") {
                    return oldLoad.apply(this,
                        args);
                }
                migrateWarn("jQuery.fn." + name + "() is deprecated");
                args.splice(0, 0, name);
                if (arguments.length) {
                    return this.on.apply(this, args);
                }
                this.triggerHandler.apply(this, args);
                return this;
            };
        });
        jQuery(function() {
            jQuery(window.document).triggerHandler("ready");
        });
        jQuery.event.special.ready = {
            setup: function() {
                if (this === window.document) {
                    migrateWarn("'ready' event is deprecated");
                }
            }
        };
        jQuery.fn.extend({
            bind: function(types, data, fn) {
                migrateWarn("jQuery.fn.bind() is deprecated");
                return this.on(types, null, data, fn);
            },
            unbind: function(types, fn) {
                migrateWarn("jQuery.fn.unbind() is deprecated");
                return this.off(types, null, fn);
            },
            delegate: function(selector, types, data, fn) {
                migrateWarn("jQuery.fn.delegate() is deprecated");
                return this.on(types, selector, data, fn);
            },
            undelegate: function(selector, types, fn) {
                migrateWarn("jQuery.fn.undelegate() is deprecated");
                return arguments.length === 1 ? this.off(selector, "**") : this.off(types, selector || "**", fn);
            },
            hover: function(fnOver, fnOut) {
                migrateWarn("jQuery.fn.hover() is deprecated");
                return this.on("mouseenter", fnOver).on("mouseleave", fnOut || fnOver);
            }
        });
        var oldOffset = jQuery.fn.offset;
        jQuery.fn.offset = function() {
            var docElem, elem = this[0],
                origin = {
                    top: 0,
                    left: 0
                };
            if (!elem || !elem.nodeType) {
                migrateWarn("jQuery.fn.offset() requires a valid DOM element");
                return origin;
            }
            docElem = (elem.ownerDocument || window.document).documentElement;
            if (!jQuery.contains(docElem, elem)) {
                migrateWarn("jQuery.fn.offset() requires an element connected to a document");
                return origin;
            }
            return oldOffset.apply(this, arguments);
        };
        var oldParam = jQuery.param;
        jQuery.param = function(data, traditional) {
            var ajaxTraditional = jQuery.ajaxSettings && jQuery.ajaxSettings.traditional;
            if (traditional === undefined && ajaxTraditional) {
                migrateWarn("jQuery.param() no longer uses jQuery.ajaxSettings.traditional");
                traditional = ajaxTraditional;
            }
            return oldParam.call(this, data, traditional);
        };
        var oldSelf = jQuery.fn.andSelf || jQuery.fn.addBack;
        jQuery.fn.andSelf = function() {
            migrateWarn(
                "jQuery.fn.andSelf() is deprecated and removed, use jQuery.fn.addBack()");
            return oldSelf.apply(this, arguments);
        };
        var oldDeferred = jQuery.Deferred,
            tuples = [
                ["resolve", "done", jQuery.Callbacks("once memory"), jQuery.Callbacks("once memory"), "resolved"],
                ["reject", "fail", jQuery.Callbacks("once memory"), jQuery.Callbacks("once memory"), "rejected"],
                ["notify", "progress", jQuery.Callbacks("memory"), jQuery.Callbacks("memory")]
            ];
        jQuery.Deferred = function(func) {
            var deferred = oldDeferred(),
                promise = deferred.promise();
            deferred.pipe = promise.pipe = function() {
                var fns = arguments;
                migrateWarn("deferred.pipe() is deprecated");
                return jQuery.Deferred(function(newDefer) {
                    jQuery.each(tuples, function(i, tuple) {
                        var fn = jQuery.isFunction(fns[i]) && fns[i];
                        deferred[tuple[1]](function() {
                            var returned = fn && fn.apply(this, arguments);
                            if (returned && jQuery.isFunction(returned.promise)) {
                                returned.promise().done(newDefer.resolve).fail(newDefer.reject).progress(newDefer.notify);
                            } else {
                                newDefer[tuple[0] + "With"](this === promise ? newDefer.promise() :
                                    this, fn ? [returned] : arguments);
                            }
                        });
                    });
                    fns = null;
                }).promise();
            };
            if (func) {
                func.call(deferred, deferred);
            }
            return deferred;
        };
        jQuery.Deferred.exceptionHook = oldDeferred.exceptionHook;
        return jQuery;
    });
});
mw.loader.implement("oojs@0o5o6ml", function($, jQuery, require, module) {
    (function(global) {
        'use strict';
        var oo = {},
            hasOwn = oo.hasOwnProperty,
            toString = oo.toString;
        oo.initClass = function(fn) {
            fn.static = fn.static || {};
        };
        oo.inheritClass = function(targetFn, originFn) {
            var targetConstructor;
            if (!originFn) {
                throw new Error('inheritClass: Origin is not a function (actually ' + originFn + ')');
            }
            if (targetFn.prototype instanceof originFn) {
                throw new Error('inheritClass: Target already inherits from origin');
            }
            targetConstructor = targetFn.prototype.constructor;
            targetFn.super = targetFn.parent = originFn;
            targetFn.prototype = Object.create(originFn.prototype, {
                constructor: {
                    value: targetConstructor,
                    enumerable: !1,
                    writable: !0,
                    configurable: !0
                }
            });
            oo.initClass(originFn);
            targetFn.static = Object.create(originFn.static);
        };
        oo.mixinClass = function(targetFn, originFn) {
            var key;
            if (!originFn) {
                throw new Error('mixinClass: Origin is not a function (actually ' + originFn + ')');
            }
            for (key in originFn.prototype) {
                if (key !== 'constructor' && hasOwn.call(
                        originFn.prototype, key)) {
                    targetFn.prototype[key] = originFn.prototype[key];
                }
            }
            oo.initClass(targetFn);
            if (originFn.static) {
                for (key in originFn.static) {
                    if (hasOwn.call(originFn.static, key)) {
                        targetFn.static[key] = originFn.static[key];
                    }
                }
            } else {
                oo.initClass(originFn);
            }
        };
        oo.isSubclass = function(testFn, baseFn) {
            return testFn === baseFn || testFn.prototype instanceof baseFn;
        };
        oo.getProp = function(obj) {
            var i, retval = obj;
            for (i = 1; i < arguments.length; i++) {
                if (retval === undefined || retval === null) {
                    return undefined;
                }
                retval = retval[arguments[i]];
            }
            return retval;
        };
        oo.setProp = function(obj) {
            var i, prop = obj;
            if (Object(obj) !== obj || arguments.length < 2) {
                return;
            }
            for (i = 1; i < arguments.length - 2; i++) {
                if (prop[arguments[i]] === undefined) {
                    prop[arguments[i]] = {};
                }
                if (Object(prop[arguments[i]]) !== prop[arguments[i]]) {
                    return;
                }
                prop = prop[arguments[i]];
            }
            prop[arguments[arguments.length - 2]] = arguments[arguments.length - 1];
        };
        oo.deleteProp = function(obj) {
            var i, prop = obj,
                props = [prop];
            if (Object(obj) !== obj || arguments.length < 2) {
                return;
            }
            for (i = 1; i < arguments.length -
                1; i++) {
                if (prop[arguments[i]] === undefined || Object(prop[arguments[i]]) !== prop[arguments[i]]) {
                    return;
                }
                prop = prop[arguments[i]];
                props.push(prop);
            }
            delete prop[arguments[i]];
            while (props.length > 1 && (prop = props.pop()) && oo.isPlainObject(prop) && !Object.keys(prop).length) {
                delete props[props.length - 1][arguments[props.length]];
            }
        };
        oo.cloneObject = function(origin) {
            var key, r;
            r = Object.create(origin.constructor.prototype);
            for (key in origin) {
                if (hasOwn.call(origin, key)) {
                    r[key] = origin[key];
                }
            }
            return r;
        };
        oo.getObjectValues = function(obj) {
            var key, values;
            if (obj !== Object(obj)) {
                throw new TypeError('Called on non-object');
            }
            values = [];
            for (key in obj) {
                if (hasOwn.call(obj, key)) {
                    values[values.length] = obj[key];
                }
            }
            return values;
        };
        oo.binarySearch = function(arr, searchFunc, forInsertion) {
            var mid, cmpResult, left = 0,
                right = arr.length;
            while (left < right) {
                mid = (left + right) >> 1;
                cmpResult = searchFunc(arr[mid]);
                if (cmpResult < 0) {
                    right = mid;
                } else if (cmpResult > 0) {
                    left = mid + 1;
                } else {
                    return mid;
                }
            }
            return forInsertion ? right : null;
        };
        oo.compare = function(a, b,
            asymmetrical) {
            var aValue, bValue, aType, bType, k;
            if (a === b) {
                return true;
            }
            a = a || {};
            b = b || {};
            if (typeof a.nodeType === 'number' && typeof a.isEqualNode === 'function') {
                return a.isEqualNode(b);
            }
            for (k in a) {
                if (!hasOwn.call(a, k) || a[k] === undefined || a[k] === b[k]) {
                    continue;
                }
                aValue = a[k];
                bValue = b[k];
                aType = typeof aValue;
                bType = typeof bValue;
                if (aType !== bType || ((aType === 'string' || aType === 'number' || aType === 'boolean') && aValue !== bValue) || (aValue === Object(aValue) && !oo.compare(aValue, bValue, true))) {
                    return false;
                }
            }
            return asymmetrical ? true : oo.compare(b, a, true);
        };
        oo.copy = function(source, leafCallback, nodeCallback) {
            var key, destination;
            if (nodeCallback) {
                destination = nodeCallback(source);
                if (destination !== undefined) {
                    return destination;
                }
            }
            if (Array.isArray(source)) {
                destination = new Array(source.length);
            } else if (source && typeof source.clone === 'function') {
                return leafCallback ? leafCallback(source.clone()) : source.clone();
            } else if (source && typeof source.cloneNode === 'function') {
                return leafCallback ? leafCallback(source.cloneNode(true)) : source
                    .cloneNode(true);
            } else if (oo.isPlainObject(source)) {
                destination = {};
            } else {
                return leafCallback ? leafCallback(source) : source;
            }
            for (key in source) {
                destination[key] = oo.copy(source[key], leafCallback, nodeCallback);
            }
            return destination;
        };
        oo.getHash = function(val) {
            return JSON.stringify(val, oo.getHash.keySortReplacer);
        };
        oo.getHash.keySortReplacer = function(key, val) {
            var normalized, keys, i, len;
            if (val && typeof val.getHashObject === 'function') {
                val = val.getHashObject();
            }
            if (!Array.isArray(val) && Object(val) === val) {
                normalized = {};
                keys = Object.keys(val).sort();
                i = 0;
                len = keys.length;
                for (; i < len; i += 1) {
                    normalized[keys[i]] = val[keys[i]];
                }
                return normalized;
            } else {
                return val;
            }
        };
        oo.unique = function(arr) {
            return arr.reduce(function(result, current) {
                if (result.indexOf(current) === -1) {
                    result.push(current);
                }
                return result;
            }, []);
        };
        oo.simpleArrayUnion = function() {
            var i, ilen, arr, j, jlen, obj = {},
                result = [];
            for (i = 0, ilen = arguments.length; i < ilen; i++) {
                arr = arguments[i];
                for (j = 0, jlen = arr.length; j < jlen; j++) {
                    if (!obj[arr[j]]) {
                        obj[arr[j]] = !0;
                        result.push(arr[j]);
                    }
                }
            }
            return result;
        };

        function simpleArrayCombine(a, b, includeB) {
            var i, ilen, isInB, bObj = {},
                result = [];
            for (i = 0, ilen = b.length; i < ilen; i++) {
                bObj[b[i]] = !0;
            }
            for (i = 0, ilen = a.length; i < ilen; i++) {
                isInB = !!bObj[a[i]];
                if (isInB === includeB) {
                    result.push(a[i]);
                }
            }
            return result;
        }
        oo.simpleArrayIntersection = function(a, b) {
            return simpleArrayCombine(a, b, true);
        };
        oo.simpleArrayDifference = function(a, b) {
            return simpleArrayCombine(a, b, false);
        };
        oo.isPlainObject = $.isPlainObject;
        (function() {
            oo.EventEmitter = function OoEventEmitter() {
                this.bindings = {};
            };
            oo.initClass(oo.EventEmitter);

            function validateMethod(method, context) {
                if (typeof method === 'string') {
                    if (context === undefined || context === null) {
                        throw new Error('Method name "' + method + '" has no context.');
                    }
                    if (typeof context[method] !== 'function') {
                        throw new Error('Property "' + method + '" is not a function');
                    }
                } else if (typeof method !== 'function') {
                    throw new Error('Invalid callback. Function or method name expected.');
                }
            }

            function addBinding(eventEmitter, event, binding) {
                var bindings;
                if (hasOwn.call(eventEmitter.bindings, event)) {
                    bindings = eventEmitter.bindings[event];
                } else {
                    bindings = eventEmitter.bindings[event] = [];
                }
                bindings.push(binding);
            }
            oo.EventEmitter.prototype.on = function(event, method, args, context) {
                validateMethod(method, context);
                addBinding(this, event, {
                    method: method,
                    args: args,
                    context: (arguments.length < 4) ? null : context,
                    once: !1
                });
                return this;
            };
            oo.EventEmitter.prototype.once = function(event, listener) {
                validateMethod(listener);
                addBinding(this, event, {
                    method: listener,
                    args: undefined,
                    context: null,
                    once: !0
                });
                return this;
            };
            oo.EventEmitter.prototype.off = function(event, method, context) {
                var i, bindings;
                if (arguments.length === 1) {
                    delete this.bindings[event];
                    return this;
                }
                validateMethod(method, context);
                if (!hasOwn.call(this.bindings, event) || !this.bindings[event].length) {
                    return this;
                }
                if (arguments.length < 3) {
                    context = null;
                }
                bindings = this.bindings[event];
                i = bindings.length;
                while (i--) {
                    if (bindings[i].method === method && bindings[i].context === context) {
                        bindings.splice(i, 1);
                    }
                }
                if (bindings.length === 0) {
                    delete this.bindings[event];
                }
                return this;
            };
            oo.EventEmitter.prototype.emit = function(event) {
                var args = [],
                    i, len, binding, bindings, method;
                if (hasOwn.call(this.bindings, event)) {
                    bindings = this.bindings[event].slice();
                    for (i = 1, len = arguments.length; i < len; i++) {
                        args.push(arguments[i]);
                    }
                    for (i = 0, len = bindings.length; i < len; i++) {
                        binding = bindings[i];
                        if (typeof binding.method === 'string') {
                            method = binding.context[binding.method];
                        } else {
                            method = binding.method;
                        }
                        if (binding.once) {
                            this.off(event, method);
                        }
                        method.apply(binding.context, binding.args ? binding.args.concat(args) : args);
                    }
                    return true;
                }
                return false;
            };
            oo.EventEmitter.prototype.connect = function(context, methods) {
                var method, args, event;
                for (event in methods) {
                    method = methods[event];
                    if (Array.isArray(method)) {
                        args = method.slice(1);
                        method = method[0];
                    } else {
                        args = [];
                    }
                    this.on(event, method, args, context);
                }
                return this;
            };
            oo.EventEmitter.prototype.disconnect = function(context, methods) {
                var i, event, method, bindings;
                if (methods) {
                    for (event in methods) {
                        method = methods[event];
                        if (Array.isArray(
                                method)) {
                            method = method[0];
                        }
                        this.off(event, method, context);
                    }
                } else {
                    for (event in this.bindings) {
                        bindings = this.bindings[event];
                        i = bindings.length;
                        while (i--) {
                            if (bindings[i] && bindings[i].context === context) {
                                this.off(event, bindings[i].method, context);
                            }
                        }
                    }
                }
                return this;
            };
        }());
        (function() {
            oo.EmitterList = function OoEmitterList() {
                this.items = [];
                this.aggregateItemEvents = {};
            };

            function normalizeArrayIndex(arr, index) {
                return (index === undefined || index < 0 || index >= arr.length) ? arr.length : index;
            }
            oo.EmitterList.prototype.getItems = function() {
                return this.items.slice(0);
            };
            oo.EmitterList.prototype.getItemIndex = function(item) {
                return this.items.indexOf(item);
            };
            oo.EmitterList.prototype.getItemCount = function() {
                return this.items.length;
            };
            oo.EmitterList.prototype.isEmpty = function() {
                return !this.items.length;
            };
            oo.EmitterList.prototype.aggregate = function(events) {
                var i, item, add, remove, itemEvent, groupEvent;
                for (itemEvent in events) {
                    groupEvent = events[itemEvent];
                    if (Object.prototype.hasOwnProperty.call(this.aggregateItemEvents,
                            itemEvent)) {
                        if (groupEvent) {
                            throw new Error('Duplicate item event aggregation for ' + itemEvent);
                        }
                        for (i = 0; i < this.items.length; i++) {
                            item = this.items[i];
                            if (item.connect && item.disconnect) {
                                remove = {};
                                remove[itemEvent] = ['emit', this.aggregateItemEvents[itemEvent], item];
                                item.disconnect(this, remove);
                            }
                        }
                        delete this.aggregateItemEvents[itemEvent];
                    }
                    if (groupEvent) {
                        this.aggregateItemEvents[itemEvent] = groupEvent;
                        for (i = 0; i < this.items.length; i++) {
                            item = this.items[i];
                            if (item.connect && item.disconnect) {
                                add = {};
                                add[itemEvent] = ['emit', groupEvent, item];
                                item.connect(this, add);
                            }
                        }
                    }
                }
            };
            oo.EmitterList.prototype.addItems = function(items, index) {
                var i, oldIndex;
                if (!Array.isArray(items)) {
                    items = [items];
                }
                if (items.length === 0) {
                    return this;
                }
                index = normalizeArrayIndex(this.items, index);
                for (i = 0; i < items.length; i++) {
                    oldIndex = this.items.indexOf(items[i]);
                    if (oldIndex !== -1) {
                        index = this.moveItem(items[i], index);
                        this.emit('move', items[i], index, oldIndex);
                    } else {
                        index = this.insertItem(items[i], index);
                        this.emit('add', items[i], index);
                    }
                    index++;
                }
                return this;
            };
            oo.EmitterList.prototype.moveItem = function(item, newIndex) {
                var existingIndex = this.items.indexOf(item);
                if (existingIndex === -1) {
                    throw new Error('Item cannot be moved, because it is not in the list.');
                }
                newIndex = normalizeArrayIndex(this.items, newIndex);
                this.items.splice(existingIndex, 1);
                if (existingIndex < newIndex) {
                    newIndex--;
                }
                this.items.splice(newIndex, 0, item);
                return newIndex;
            };
            oo.EmitterList.prototype.insertItem = function(item, index) {
                var events, event;
                if (item.connect && item.disconnect) {
                    events = {};
                    for (event in this.aggregateItemEvents) {
                        events[event] = ['emit', this.aggregateItemEvents[event], item];
                    }
                    item.connect(this, events);
                }
                index = normalizeArrayIndex(this.items, index);
                this.items.splice(index, 0, item);
                return index;
            };
            oo.EmitterList.prototype.removeItems = function(items) {
                var i, item, index;
                if (!Array.isArray(items)) {
                    items = [items];
                }
                if (items.length === 0) {
                    return this;
                }
                for (i = 0; i < items.length; i++) {
                    item = items[i];
                    index = this.items.indexOf(item);
                    if (index !== -1) {
                        if (item.connect && item.disconnect) {
                            item.
                            disconnect(this);
                        }
                        this.items.splice(index, 1);
                        this.emit('remove', item, index);
                    }
                }
                return this;
            };
            oo.EmitterList.prototype.clearItems = function() {
                var i, item, cleared = this.items.splice(0, this.items.length);
                for (i = 0; i < cleared.length; i++) {
                    item = cleared[i];
                    if (item.connect && item.disconnect) {
                        item.disconnect(this);
                    }
                }
                this.emit('clear');
                return this;
            };
        }());
        oo.SortedEmitterList = function OoSortedEmitterList(sortingCallback) {
            oo.EmitterList.call(this);
            this.sortingCallback = sortingCallback;
            this.aggregate({
                sortChange: 'itemSortChange'
            });
            this.connect(this, {
                itemSortChange: 'onItemSortChange'
            });
        };
        oo.mixinClass(oo.SortedEmitterList, oo.EmitterList);
        oo.SortedEmitterList.prototype.onItemSortChange = function(item) {
            this.removeItems(item);
            this.addItems(item);
        };
        oo.SortedEmitterList.prototype.setSortingCallback = function(sortingCallback) {
            var items = this.getItems();
            this.sortingCallback = sortingCallback;
            this.clearItems();
            this.addItems(items);
        };
        oo.SortedEmitterList.prototype.addItems = function(items) {
            var index, i, insertionIndex;
            if (!
                Array.isArray(items)) {
                items = [items];
            }
            if (items.length === 0) {
                return this;
            }
            for (i = 0; i < items.length; i++) {
                insertionIndex = this.findInsertionIndex(items[i]);
                if (insertionIndex <= this.items.length && this.items[insertionIndex] && this.sortingCallback(this.items[insertionIndex], items[i]) === 0) {
                    this.removeItems(this.items[insertionIndex]);
                }
                index = this.insertItem(items[i], insertionIndex);
                this.emit('add', items[i], index);
            }
            return this;
        };
        oo.SortedEmitterList.prototype.findInsertionIndex = function(item) {
            var list = this;
            return oo.binarySearch(this.items, function(otherItem) {
                return list.sortingCallback(item, otherItem);
            }, true);
        };
        oo.Registry = function OoRegistry() {
            oo.EventEmitter.call(this);
            this.registry = {};
        };
        oo.mixinClass(oo.Registry, oo.EventEmitter);
        oo.Registry.prototype.register = function(name, data) {
            var i, len;
            if (typeof name === 'string') {
                this.registry[name] = data;
                this.emit('register', name, data);
            } else if (Array.isArray(name)) {
                for (i = 0, len = name.length; i < len; i++) {
                    this.register(name[i], data);
                }
            } else {
                throw new Error(
                    'Name must be a string or array, cannot be a ' + typeof name);
            }
        };
        oo.Registry.prototype.unregister = function(name) {
            var i, len, data;
            if (typeof name === 'string') {
                data = this.lookup(name);
                if (data !== undefined) {
                    delete this.registry[name];
                    this.emit('unregister', name, data);
                }
            } else if (Array.isArray(name)) {
                for (i = 0, len = name.length; i < len; i++) {
                    this.unregister(name[i]);
                }
            } else {
                throw new Error('Name must be a string or array, cannot be a ' + typeof name);
            }
        };
        oo.Registry.prototype.lookup = function(name) {
            if (hasOwn.call(this.registry, name)) {
                return this.registry[name];
            }
        };
        oo.Factory = function OoFactory() {
            oo.Factory.super.call(this);
        };
        oo.inheritClass(oo.Factory, oo.Registry);
        oo.Factory.prototype.register = function(constructor) {
            var name;
            if (typeof constructor !== 'function') {
                throw new Error('constructor must be a function, cannot be a ' + typeof constructor);
            }
            name = constructor.static && constructor.static.name;
            if (typeof name !== 'string' || name === '') {
                throw new Error('Name must be a string and must not be empty');
            }
            oo.Factory.super.
            prototype.register.call(this, name, constructor);
        };
        oo.Factory.prototype.unregister = function(constructor) {
            var name;
            if (typeof constructor !== 'function') {
                throw new Error('constructor must be a function, cannot be a ' + typeof constructor);
            }
            name = constructor.static && constructor.static.name;
            if (typeof name !== 'string' || name === '') {
                throw new Error('Name must be a string and must not be empty');
            }
            oo.Factory.super.prototype.unregister.call(this, name);
        };
        oo.Factory.prototype.create = function(name) {
            var obj, i, args = [],
                constructor = this.lookup(name);
            if (!constructor) {
                throw new Error('No class registered by that name: ' + name);
            }
            for (i = 1; i < arguments.length; i++) {
                args.push(arguments[i]);
            }
            obj = Object.create(constructor.prototype);
            constructor.apply(obj, args);
            return obj;
        };
        if (typeof module !== 'undefined' && module.exports) {
            module.exports = oo;
        } else {
            global.OO = oo;
        }
    }(this));
    window.OO = module.exports;
});
mw.loader.implement("oojs-router@1meh8at", function($, jQuery, require, module) {
    (function($) {
        'use strict';
        OO.Router = function OoRouter() {
            var router = this;
            OO.Router.parent.call(this);
            this.enabled = !0;
            this.oldHash = this.getPath();
            $(window).on('popstate', function() {
                router.emit('popstate');
            });
            $(window).on('hashchange', function() {
                router.emit('hashchange');
            });
            this.on('hashchange', function() {
                var routeEvent;
                if (router.enabled) {
                    routeEvent = $.Event('route', {
                        path: router.getPath()
                    });
                    router.emit('route', routeEvent);
                    if (!routeEvent.isDefaultPrevented()) {
                        router.checkRoute();
                    } else {
                        router.enabled = !1;
                        router.navigate(router.oldHash);
                    }
                } else {
                    router.enabled = !0;
                }
                router.oldHash = router.getPath();
            });
        };
        OO.inheritClass(OO.Router, OO.Registry);
        OO.Router.static.isSupported = function() {
            return 'onhashchange' in window;
        };
        OO.Router.prototype.checkRoute = function() {
            var id, entry, match, hash = this.getPath();
            for (id in this.registry) {
                entry = this.registry[id];
                match = hash.match(entry.path);
                if (match) {
                    entry.callback.apply(this, match.slice(1));
                    return;
                }
            }
        };
        OO.Router.prototype.addRoute = function(path, callback) {
            var entry = {
                path: typeof path === 'string' ? new RegExp('^' + path.replace(/[\\^$*+?.()|[\]{}]/g, '\\$&') + '$') : path,
                callback: callback
            };
            this.register(entry.path.toString(), entry);
        };
        OO.Router.prototype.route = OO.Router.prototype.addRoute;
        OO.Router.prototype.navigateTo = function(title, options) {
            if (options.useReplaceState) {
                history.replaceState(null, title, options.path);
            } else {
                history.pushState(null, title, options.path);
            }
        };
        OO.Router.prototype.navigate = function(path) {
            if (path === '') {
                path = window.location.href.replace(/#.*$/, '');
                history.pushState(null, document.title, path);
                this.checkRoute();
            } else {
                window.location.hash = path;
            }
        };
        OO.Router.prototype.back = function() {
            var timeoutID, router = this,
                deferred = $.Deferred();
            this.once('popstate', function() {
                clearTimeout(timeoutID);
                deferred.resolve();
            });
            window.history.back();
            timeoutID = setTimeout(function() {
                router.off('popstate');
                deferred.resolve();
            }, 50);
            return deferred.promise();
        };
        OO.Router.prototype.getPath =
            function() {
                return window.location.hash.slice(1);
            };
        OO.Router.prototype.isSupported = OO.Router.static.isSupported;
        if (typeof module !== 'undefined' && module.exports) {
            module.exports = OO.Router;
        }
    }(jQuery));
});
mw.loader.implement("jquery.accessKeyLabel@1fpzh15", function($, jQuery, require, module) {
    (function() {
        var cachedAccessKeyModifiers, useTestPrefix = !1,
            labelable = 'button, input, textarea, keygen, meter, output, progress, select';

        function getAccessKeyModifiers(ua) {
            var profile, accessKeyModifiers;
            if (!ua && cachedAccessKeyModifiers) {
                return cachedAccessKeyModifiers;
            }
            profile = $.client.profile(ua);
            switch (profile.name) {
                case 'chrome':
                case 'opera':
                    if (profile.name === 'opera' && profile.versionNumber < 15) {
                        accessKeyModifiers = ['shift', 'esc'];
                    } else if (profile.platform === 'mac') {
                        accessKeyModifiers = ['ctrl', 'option'];
                    } else {
                        accessKeyModifiers = ['alt', 'shift'];
                    }
                    break;
                case 'firefox':
                case 'iceweasel':
                    if (profile.versionBase < 2) {
                        accessKeyModifiers = ['alt'];
                    } else {
                        if (profile.platform === 'mac') {
                            if (profile.versionNumber < 14) {
                                accessKeyModifiers = ['ctrl'];
                            } else {
                                accessKeyModifiers = ['ctrl', 'option'];
                            }
                        } else {
                            accessKeyModifiers = ['alt', 'shift'];
                        }
                    }
                    break;
                case 'safari':
                case 'konqueror':
                    if (profile.platform === 'win') {
                        accessKeyModifiers = ['alt'];
                    } else {
                        if (profile.layoutVersion > 526) {
                            accessKeyModifiers = ['ctrl', profile.platform === 'mac' ? 'option' : 'alt'];
                        } else {
                            accessKeyModifiers = ['ctrl'];
                        }
                    }
                    break;
                case 'msie':
                case 'edge':
                    accessKeyModifiers = ['alt'];
                    break;
                default:
                    accessKeyModifiers = profile.platform === 'mac' ? ['ctrl'] : ['alt'];
                    break;
            }
            if (!ua) {
                cachedAccessKeyModifiers = accessKeyModifiers;
            }
            return accessKeyModifiers;
        }

        function getAccessKeyLabel(element) {
            if (!element.accessKey) {
                return '';
            }
            if (!useTestPrefix && element.accessKeyLabel) {
                return element.accessKeyLabel;
            }
            return (useTestPrefix ? 'test' : getAccessKeyModifiers().join('-')) + '-' + element.accessKey;
        }

        function updateTooltipOnElement(element, titleElement) {
            var oldTitle, parts, regexp, newTitle, accessKeyLabel, separatorMsg = mw.message('word-separator').plain();
            oldTitle = titleElement.title;
            if (!oldTitle) {
                return;
            }
            parts = (separatorMsg + mw.message('brackets').plain()).split('$1');
            regexp = new RegExp(parts.map(mw.RegExp.escape).join('.*?') + '$');
            newTitle = oldTitle.replace(regexp, '');
            accessKeyLabel = getAccessKeyLabel(element);
            if (
                accessKeyLabel) {
                newTitle += separatorMsg + mw.message('brackets', accessKeyLabel).plain();
            }
            if (oldTitle !== newTitle) {
                titleElement.title = newTitle;
            }
        }

        function updateTooltip(element) {
            var id, $element, $label, $labelParent;
            updateTooltipOnElement(element, element);
            $element = $(element);
            if ($element.is(labelable)) {
                id = element.id.replace(/"/g, '\\"');
                if (id) {
                    $label = $('label[for="' + id + '"]');
                    if ($label.length === 1) {
                        updateTooltipOnElement(element, $label[0]);
                    }
                }
                $labelParent = $element.parents('label');
                if ($labelParent.length === 1) {
                    updateTooltipOnElement(element, $labelParent[0]);
                }
            }
        }
        $.fn.updateTooltipAccessKeys = function() {
            return this.each(function() {
                updateTooltip(this);
            });
        };
        $.fn.updateTooltipAccessKeys.getAccessKeyModifiers = getAccessKeyModifiers;
        $.fn.updateTooltipAccessKeys.getAccessKeyLabel = getAccessKeyLabel;
        $.fn.updateTooltipAccessKeys.getAccessKeyPrefix = function(ua) {
            return getAccessKeyModifiers(ua).join('-') + '-';
        };
        $.fn.updateTooltipAccessKeys.setTestMode = function(mode) {
            useTestPrefix = mode;
        };
    }());
}, {}, {
    "brackets": "[$1]",
    "word-separator": " "
});
mw.loader.implement("jquery.client@0cwc6tv", function($, jQuery, require, module) {
    (function() {
        var profileCache = {};
        $.client = {
            profile: function(nav) {
                if (nav === undefined) {
                    nav = window.navigator;
                }
                if (profileCache[nav.userAgent + '|' + nav.platform] !== undefined) {
                    return profileCache[nav.userAgent + '|' + nav.platform];
                }
                var versionNumber, key = nav.userAgent + '|' + nav.platform,
                    uk = 'unknown',
                    x = 'x',
                    wildUserAgents = ['Opera', 'Navigator', 'Minefield', 'KHTML', 'Chrome', 'PLAYSTATION 3', 'Iceweasel'],
                    userAgentTranslations = [
                        [/(Firefox|MSIE|KHTML,?\slike\sGecko|Konqueror)/, ''],
                        ['Chrome Safari', 'Chrome'],
                        ['KHTML', 'Konqueror'],
                        ['Minefield', 'Firefox'],
                        ['Navigator', 'Netscape'],
                        ['PLAYSTATION 3', 'PS3']
                    ],
                    versionPrefixes = ['camino', 'chrome', 'firefox', 'iceweasel', 'netscape', 'netscape6', 'opera', 'version', 'konqueror', 'lynx', 'msie', 'safari', 'ps3', 'android'],
                    versionSuffix = '(\\/|\\;?\\s|)([a-z0-9\\.\\+]*?)(\\;|dev|rel|\\)|\\s|$)',
                    names = ['camino', 'chrome', 'firefox', 'iceweasel', 'netscape', 'konqueror', 'lynx', 'msie', 'opera', 'safari', 'ipod',
                        'iphone', 'blackberry', 'ps3', 'rekonq', 'android'
                    ],
                    nameTranslations = [],
                    layouts = ['gecko', 'konqueror', 'msie', 'trident', 'edge', 'opera', 'webkit'],
                    layoutTranslations = [
                        ['konqueror', 'khtml'],
                        ['msie', 'trident'],
                        ['opera', 'presto']
                    ],
                    layoutVersions = ['applewebkit', 'gecko', 'trident', 'edge'],
                    platforms = ['win', 'wow64', 'mac', 'linux', 'sunos', 'solaris', 'iphone'],
                    platformTranslations = [
                        ['sunos', 'solaris'],
                        ['wow64', 'win']
                    ],
                    translate = function(source, translations) {
                        var i;
                        for (i = 0; i < translations.length; i++) {
                            source = source.replace(translations[i][0], translations[i][1]);
                        }
                        return source;
                    },
                    ua = nav.userAgent,
                    match, name = uk,
                    layout = uk,
                    layoutversion = uk,
                    platform = uk,
                    version = x;
                if ((match = new RegExp('(' + wildUserAgents.join('|') + ')').exec(ua))) {
                    ua = translate(ua, userAgentTranslations);
                }
                ua = ua.toLowerCase();
                if (ua.match(/android/) && ua.match(/firefox/)) {
                    ua = ua.replace(new RegExp('android' + versionSuffix), '');
                }
                if ((match = new RegExp('(' + names.join('|') + ')').exec(ua))) {
                    name = translate(match[1], nameTranslations);
                }
                if ((match = new RegExp('(' + layouts.join('|') + ')').exec(ua))) {
                    layout = translate(match[1], layoutTranslations);
                }
                if ((match = new RegExp('(' + layoutVersions.join('|') + ')\\/(\\d+)').exec(ua))) {
                    layoutversion = parseInt(match[2], 10);
                }
                if ((match = new RegExp('(' + platforms.join('|') + ')').exec(nav.platform.toLowerCase()))) {
                    platform = translate(match[1], platformTranslations);
                }
                if ((match = new RegExp('(' + versionPrefixes.join('|') + ')' + versionSuffix).exec(ua))) {
                    version = match[3];
                }
                if (name === 'safari' && version > 400) {
                    version = '2.0';
                }
                if (name === 'opera' && version >= 9.8) {
                    match = ua.match(/\bversion\/([0-9.]*)/);
                    if (match && match[1]) {
                        version = match[1];
                    } else {
                        version = '10';
                    }
                }
                if (name === 'chrome' && (match = ua.match(/\bopr\/([0-9.]*)/))) {
                    if (match[1]) {
                        name = 'opera';
                        version = match[1];
                    }
                }
                if (layout === 'trident' && layoutversion >= 7 && (match = ua.match(/\brv[ :/]([0-9.]*)/))) {
                    if (match[1]) {
                        name = 'msie';
                        version = match[1];
                    }
                }
                if (name === 'chrome' && (match = ua.match(/\bedge\/([0-9.]*)/))) {
                    name = 'edge';
                    version = match[1];
                    layout = 'edge';
                    layoutversion = parseInt(match[1], 10);
                }
                if ((match = ua.match(
                        /\bsilk\/([0-9.\-_]*)/))) {
                    if (match[1]) {
                        name = 'silk';
                        version = match[1];
                    }
                }
                versionNumber = parseFloat(version, 10) || 0.0;
                profileCache[key] = {
                    name: name,
                    layout: layout,
                    layoutVersion: layoutversion,
                    platform: platform,
                    version: version,
                    versionBase: (version !== x ? Math.floor(versionNumber).toString() : x),
                    versionNumber: versionNumber
                };
                return profileCache[key];
            },
            test: function(map, profile, exactMatchOnly) {
                var conditions, dir, i, op, val, j, pieceVersion, pieceVal, compare;
                profile = $.isPlainObject(profile) ? profile : $.client.profile();
                if (map.ltr && map.rtl) {
                    dir = $(document.body).is('.rtl') ? 'rtl' : 'ltr';
                    map = map[dir];
                }
                if (typeof map !== 'object' || map[profile.name] === undefined) {
                    return !exactMatchOnly;
                }
                conditions = map[profile.name];
                if (conditions === false) {
                    return false;
                }
                if (conditions === null) {
                    return true;
                }
                for (i = 0; i < conditions.length; i++) {
                    op = conditions[i][0];
                    val = conditions[i][1];
                    if (typeof val === 'string') {
                        pieceVersion = profile.version.toString().split('.');
                        pieceVal = val.split('.');
                        while (pieceVersion.length < pieceVal.length) {
                            pieceVersion.push('0');
                        }
                        while (pieceVal.length < pieceVersion.length) {
                            pieceVal.push('0');
                        }
                        compare = 0;
                        for (j = 0; j < pieceVersion.length; j++) {
                            if (Number(pieceVersion[j]) < Number(pieceVal[j])) {
                                compare = -1;
                                break;
                            } else if (Number(pieceVersion[j]) > Number(pieceVal[j])) {
                                compare = 1;
                                break;
                            }
                        }
                        if (!(eval(String(compare + op + '0')))) {
                            return false;
                        }
                    } else if (typeof val === 'number') {
                        if (!(eval('profile.versionNumber' + op + val))) {
                            return false;
                        }
                    }
                }
                return true;
            }
        };
    }());
});
mw.loader.implement("jquery.cookie@1fdv0i0", function($, jQuery, require, module) {
    (function($, document, undefined) {
        var pluses = /\+/g;

        function raw(s) {
            return s;
        }

        function decoded(s) {
            return unRfc2068(decodeURIComponent(s.replace(pluses, ' ')));
        }

        function unRfc2068(value) {
            if (value.indexOf('"') === 0) {
                value = value.slice(1, -1).replace(/\\"/g, '"').replace(/\\\\/g, '\\');
            }
            return value;
        }

        function fromJSON(value) {
            return config.json ? JSON.parse(value) : value;
        }
        var config = $.cookie = function(key, value, options) {
            if (value !== undefined) {
                options = $.extend({}, config.defaults, options);
                if (value === null) {
                    options.expires = -1;
                }
                if (typeof options.expires === 'number') {
                    var days = options.expires,
                        t = options.expires = new Date();
                    t.setDate(t.getDate() + days);
                }
                value = config.json ? JSON.stringify(value) : String(value);
                return (document.cookie = [encodeURIComponent(key), '=', config.raw ? value : encodeURIComponent(value), options.expires ? '; expires=' + options.expires.toUTCString() : '', options.path ? '; path=' + options.path : '', options.domain ? '; domain=' + options.
                    domain : '', options.secure ? '; secure' : ''
                ].join(''));
            }
            var decode = config.raw ? raw : decoded;
            var cookies = document.cookie.split('; ');
            var result = key ? null : {};
            for (var i = 0, l = cookies.length; i < l; i++) {
                var parts = cookies[i].split('=');
                var name = decode(parts.shift());
                var cookie = decode(parts.join('='));
                if (key && key === name) {
                    result = fromJSON(cookie);
                    break;
                }
                if (!key) {
                    result[name] = fromJSON(cookie);
                }
            }
            return result;
        };
        config.defaults = {};
        $.removeCookie = function(key, options) {
            if ($.cookie(key) !== null) {
                $.cookie(key, null, options);
                return true;
            }
            return false;
        };
    })(jQuery, document);
});
mw.loader.implement("jquery.textSelection@1fsndek", function($, jQuery, require, module) {
    (function() {
        $.fn.textSelection = function(command, options) {
            var fn, alternateFn, retval;
            fn = {
                getContents: function() {
                    return this.val();
                },
                setContents: function(content) {
                    return this.each(function() {
                        var scrollTop = this.scrollTop;
                        $(this).val(content);
                        this.scrollTop = scrollTop;
                    });
                },
                getSelection: function() {
                    var retval, el = this.get(0);
                    if (!el) {
                        retval = '';
                    } else {
                        retval = el.value.substring(el.selectionStart, el.selectionEnd);
                    }
                    return retval;
                },
                replaceSelection: function(value) {
                    return this.each(function() {
                        var allText, currSelection, startPos, endPos;
                        allText = $(this).textSelection('getContents');
                        currSelection = $(this).textSelection('getCaretPosition', {
                            startAndEnd: !0
                        });
                        startPos = currSelection[0];
                        endPos = currSelection[1];
                        $(this).textSelection('setContents', allText.slice(0, startPos) + value + allText.slice(endPos));
                        $(this).textSelection('setSelection', {
                            start: startPos,
                            end: startPos + value.length
                        });
                    });
                },
                encapsulateSelection: function(options) {
                    return this
                        .each(function() {
                            var selText, allText, currSelection, insertText, combiningCharSelectionBug = !1,
                                isSample, startPos, endPos, pre = options.pre,
                                post = options.post;

                            function checkSelectedText() {
                                if (!selText) {
                                    selText = options.peri;
                                    isSample = !0;
                                } else if (options.replace) {
                                    selText = options.peri;
                                } else {
                                    while (selText.charAt(selText.length - 1) === ' ') {
                                        selText = selText.slice(0, -1);
                                        post += ' ';
                                    }
                                    while (selText.charAt(0) === ' ') {
                                        selText = selText.slice(1);
                                        pre = ' ' + pre;
                                    }
                                }
                            }

                            function doSplitLines(selText, pre, post) {
                                var i, insertText = '',
                                    selTextArr = selText.split('\n');
                                for (i = 0; i < selTextArr.length; i++) {
                                    insertText += pre + selTextArr[i] + post;
                                    if (i !== selTextArr.length - 1) {
                                        insertText += '\n';
                                    }
                                }
                                return insertText;
                            }
                            isSample = !1;
                            $(this).trigger('focus');
                            if (options.selectionStart !== undefined) {
                                $(this).textSelection('setSelection', {
                                    start: options.selectionStart,
                                    end: options.selectionEnd
                                });
                            }
                            selText = $(this).textSelection('getSelection');
                            allText = $(this).textSelection('getContents');
                            currSelection = $(this).textSelection('getCaretPosition', {
                                startAndEnd:
                                    !0
                            });
                            startPos = currSelection[0];
                            endPos = currSelection[1];
                            checkSelectedText();
                            if (options.selectionStart !== undefined && endPos - startPos !== options.selectionEnd - options.selectionStart) {
                                startPos = options.selectionStart;
                                combiningCharSelectionBug = !0;
                            }
                            insertText = pre + selText + post;
                            if (options.splitlines) {
                                insertText = doSplitLines(selText, pre, post);
                            }
                            if (options.ownline) {
                                if (startPos !== 0 && allText.charAt(startPos - 1) !== '\n' && allText.charAt(startPos - 1) !== '\r') {
                                    insertText = '\n' + insertText;
                                    pre += '\n';
                                }
                                if (allText.charAt(endPos) !== '\n' && allText.charAt(endPos) !== '\r') {
                                    insertText += '\n';
                                    post += '\n';
                                }
                            }
                            if (combiningCharSelectionBug) {
                                $(this).textSelection('setContents', allText.slice(0, startPos) + insertText + allText.slice(endPos));
                            } else {
                                $(this).textSelection('replaceSelection', insertText);
                            }
                            if (isSample && options.selectPeri && (!options.splitlines || (options.splitlines && selText.indexOf('\n') === -1))) {
                                $(this).textSelection('setSelection', {
                                    start: startPos + pre.length,
                                    end: startPos + pre.length + selText.length
                                });
                            } else {
                                $(this).
                                textSelection('setSelection', {
                                    start: startPos + insertText.length
                                });
                            }
                            $(this).trigger('encapsulateSelection', [options.pre, options.peri, options.post, options.ownline, options.replace, options.splitlines]);
                        });
                },
                getCaretPosition: function(options) {
                    function getCaret(e) {
                        var caretPos = 0,
                            endPos = 0;
                        if (e) {
                            caretPos = e.selectionStart;
                            endPos = e.selectionEnd;
                        }
                        return options.startAndEnd ? [caretPos, endPos] : caretPos;
                    }
                    return getCaret(this.get(0));
                },
                setSelection: function(options) {
                    return this.each(function() {
                        if (options.start > this.selectionEnd) {
                            this.selectionEnd = options.end;
                            this.selectionStart = options.start;
                        } else {
                            this.selectionStart = options.start;
                            this.selectionEnd = options.end;
                        }
                    });
                },
                scrollToCaretPosition: function(options) {
                    return this.each(function() {
                        var clientHeight = this.clientHeight,
                            origValue = this.value,
                            origSelectionStart = this.selectionStart,
                            origSelectionEnd = this.selectionEnd,
                            origScrollTop = this.scrollTop,
                            calcScrollTop;
                        this.value = this.value.slice(0, this.selectionEnd);
                        this.scrollTop = this.scrollHeight;
                        calcScrollTop = this.
                        scrollTop;
                        this.value = origValue;
                        this.selectionStart = origSelectionStart;
                        this.selectionEnd = origSelectionEnd;
                        if (!options.force) {
                            if (calcScrollTop < origScrollTop && origScrollTop - calcScrollTop < clientHeight) {
                                calcScrollTop = origScrollTop;
                            }
                        }
                        this.scrollTop = calcScrollTop;
                        $(this).trigger('scrollToPosition');
                    });
                }
            };
            alternateFn = $(this).data('jquery.textSelection');
            switch (command) {
                case 'encapsulateSelection':
                    options = $.extend({
                        pre: '',
                        peri: '',
                        post: '',
                        ownline: !1,
                        replace: !1,
                        selectPeri: !0,
                        splitlines: !1,
                        selectionStart: undefined,
                        selectionEnd: undefined
                    }, options);
                    break;
                case 'getCaretPosition':
                    options = $.extend({
                        startAndEnd: !1
                    }, options);
                    break;
                case 'setSelection':
                    options = $.extend({
                        start: undefined,
                        end: undefined
                    }, options);
                    if (options.end === undefined) {
                        options.end = options.start;
                    }
                    break;
                case 'scrollToCaretPosition':
                    options = $.extend({
                        force: !1
                    }, options);
                    break;
                case 'register':
                    if (alternateFn) {
                        throw new Error('Another textSelection API was already registered');
                    }
                    $(this).data('jquery.textSelection', options);
                    return;
                case 'unregister':
                    $(this).removeData('jquery.textSelection');
                    return;
            }
            retval = (alternateFn && alternateFn[command] || fn[command]).call(this, options);
            return retval;
        };
    }());
});
mw.loader.implement("jquery.throttle-debounce@19vxvii", function($, jQuery, require, module) {
    mw.log.warn("This page is using the deprecated ResourceLoader module \"jquery.throttle-debounce\".\nPlease use OO.ui.throttle/debounce instead. See https://phabricator.wikimedia.org/T213426");
    (function(window, undefined) {
        '$:nomunge';
        var $ = window.jQuery || window.Cowboy || (window.Cowboy = {}),
            jq_throttle;
        $.throttle = jq_throttle = function(delay, no_trailing, callback, debounce_mode) {
            var timeout_id, last_exec = 0;
            if (typeof no_trailing !== 'boolean') {
                debounce_mode = callback;
                callback = no_trailing;
                no_trailing = undefined;
            }

            function wrapper() {
                var that = this,
                    elapsed = +new Date() - last_exec,
                    args = arguments;

                function exec() {
                    last_exec = +new Date();
                    callback.apply(that, args);
                };

                function clear() {
                    timeout_id = undefined;
                };
                if (debounce_mode && !timeout_id) {
                    exec();
                }
                timeout_id && clearTimeout(timeout_id);
                if (debounce_mode === undefined && elapsed > delay) {
                    exec();
                } else if (no_trailing !== true) {
                    timeout_id = setTimeout(debounce_mode ? clear : exec, debounce_mode ===
                        undefined ? delay - elapsed : delay);
                }
            };
            if ($.guid) {
                wrapper.guid = callback.guid = callback.guid || $.guid++;
            }
            return wrapper;
        };
        $.debounce = function(delay, at_begin, callback) {
            return callback === undefined ? jq_throttle(delay, at_begin, false) : jq_throttle(delay, callback, at_begin !== false);
        };
    })(this);
});
mw.loader.implement("mediawiki.RegExp@10ymrcm", function($, jQuery, require, module) {
    (function() {
        mw.RegExp = {
            escape: function(str) {
                return str.replace(/([\\{}()|.?*+\-^$\[\]])/g, '\\$1');
            }
        };
    }());
});
mw.loader.implement("mediawiki.String@152v5tv", function($, jQuery, require, module) {
    (function() {
        function byteLength(str) {
            return str.replace(/[\u0080-\u07FF\uD800-\uDFFF]/g, '**').replace(/[\u0800-\uD7FF\uE000-\uFFFF]/g, '***').length;
        }

        function codePointLength(str) {
            return str.replace(/[\uD800-\uDBFF][\uDC00-\uDFFF]/g, '*').length;
        }

        function codePointAt(string, offset, backwards) {
            var maybePair = backwards ? string.slice(offset - 1, offset + 1) : string.slice(offset, offset + 2);
            if (/^[\uD800-\uDBFF][\uDC00-\uDFFF]$/.test(maybePair)) {
                return maybePair;
            } else {
                return string.charAt(offset);
            }
        }

        function trimLength(safeVal, newVal, length, lengthFn) {
            var startMatches, endMatches, matchesLen, inpParts, chopOff, oldChar, newChar, oldVal = safeVal;
            if (lengthFn(newVal) <= length) {
                return {
                    newVal: newVal,
                    trimmed: !1
                };
            }
            startMatches = 0;
            endMatches = 0;
            matchesLen = Math.min(newVal.length, oldVal.length);
            while (startMatches < matchesLen) {
                oldChar = codePointAt(oldVal, startMatches, false);
                newChar = codePointAt(newVal, startMatches, false);
                if (oldChar !== newChar) {
                    break;
                }
                startMatches += oldChar.length;
            }
            while (endMatches < (matchesLen - startMatches)) {
                oldChar = codePointAt(oldVal, oldVal.length - 1 - endMatches, true);
                newChar = codePointAt(newVal, newVal.length - 1 - endMatches, true);
                if (oldChar !== newChar) {
                    break;
                }
                endMatches += oldChar.length;
            }
            inpParts = [newVal.slice(0, startMatches), newVal.slice(startMatches, newVal.length - endMatches), newVal.slice(newVal.length - endMatches)];
            while (lengthFn(inpParts.join('')) > length && inpParts[1].length > 0) {
                chopOff = /[\uD800-\uDBFF][\uDC00-\uDFFF]$/.test(inpParts[1]) ? 2 : 1;
                inpParts[1] = inpParts[1].slice(0, -chopOff);
            }
            return {
                newVal: inpParts.join(''),
                trimmed: newVal !== inpParts.join('')
            };
        }

        function trimByteLength(safeVal, newVal, byteLimit, filterFunction) {
            var lengthFn;
            if (filterFunction) {
                lengthFn = function(val) {
                    return byteLength(filterFunction(val));
                };
            } else {
                lengthFn = byteLength;
            }
            return trimLength(safeVal, newVal, byteLimit, lengthFn);
        }

        function trimCodePointLength(safeVal, newVal, codePointLimit, filterFunction) {
            var lengthFn;
            if (filterFunction) {
                lengthFn = function(val) {
                    return codePointLength(filterFunction(val));
                };
            } else {
                lengthFn = codePointLength;
            }
            return trimLength(safeVal, newVal, codePointLimit, lengthFn);
        }
        module.exports = {
            byteLength: byteLength,
            codePointLength: codePointLength,
            trimByteLength: trimByteLength,
            trimCodePointLength: trimCodePointLength
        };
    }());
});
mw.loader.implement("mediawiki.Title@0u6msks", {
    "main": "Title.js",
    "files": {
        "Title.js": function(require, module) {
            var mwString = require('mediawiki.String'),
                toUpperMapping = require('./phpCharToUpper.json'),
                namespaceIds = mw.config.get('wgNamespaceIds'),
                NS_MAIN = namespaceIds[''],
                NS_TALK = namespaceIds.talk,
                NS_SPECIAL = namespaceIds.special,
                NS_MEDIA = namespaceIds.media,
                NS_FILE = namespaceIds.file,
                FILENAME_MAX_BYTES = 240,
                TITLE_MAX_BYTES = 255,
                getNsIdByName = function(ns) {
                    var id;
                    if (typeof ns !== 'string') {
                        return false;
                    }
                    id = mw.config.get('wgNamespaceIds')[ns.toLowerCase()];
                    if (id === undefined) {
                        return false;
                    }
                    return id;
                },
                getNamespacePrefix = function(namespace) {
                    return namespace === NS_MAIN ? '' : (mw.config.get('wgFormattedNamespaces')[namespace].replace(/ /g, '_') + ':');
                },
                rUnderscoreTrim = /^_+|_+$/g,
                rSplit = /^(.+?)_*:_*(.*)$/,
                rInvalid = new RegExp('[^' + mw.config.get('wgLegalTitleChars') + ']' + '|%[0-9A-Fa-f]{2}' + '|&[A-Za-z0-9\u0080-\uFFFF]+;' + '|&#[0-9]+;' + '|&#x[0-9A-Fa-f]+;'),
                rWhitespace =
                /[ _\u00A0\u1680\u180E\u2000-\u200A\u2028\u2029\u202F\u205F\u3000]+/g,
                rUnicodeBidi = /[\u200E\u200F\u202A-\u202E]/g,
                sanitationRules = [{
                    pattern: /~{3}/g,
                    replace: '',
                    generalRule: !0
                }, {
                    pattern: /[\x00-\x1f\x7f]/g,
                    replace: '',
                    generalRule: !0
                }, {
                    pattern: /%([0-9A-Fa-f]{2})/g,
                    replace: '% $1',
                    generalRule: !0
                }, {
                    pattern: /&(([A-Za-z0-9\x80-\xff]+|#[0-9]+|#x[0-9A-Fa-f]+);)/g,
                    replace: '& $1',
                    generalRule: !0
                }, {
                    pattern: new RegExp('[' + mw.config.get('wgIllegalFileChars', '') + ']', 'g'),
                    replace: '-',
                    fileRule: !0
                }, {
                    pattern: /[}\]>]/g,
                    replace: ')',
                    generalRule: !0
                }, {
                    pattern: /[{[<]/g,
                    replace: '(',
                    generalRule: !0
                }, {
                    pattern: new RegExp(rInvalid.source, 'g'),
                    replace: '-',
                    generalRule: !0
                }, {
                    pattern: /^(\.|\.\.|\.\/.*|\.\.\/.*|.*\/\.\/.*|.*\/\.\.\/.*|.*\/\.|.*\/\.\.)$/g,
                    replace: '',
                    generalRule: !0
                }],
                parse = function(title, defaultNamespace) {
                    var namespace, m, id, i, fragment, ext;
                    namespace = defaultNamespace === undefined ? NS_MAIN : defaultNamespace;
                    title = title.replace(rUnicodeBidi, '').replace(rWhitespace, '_').replace(rUnderscoreTrim, '');
                    if (
                        title !== '' && title[0] === ':') {
                        namespace = NS_MAIN;
                        title = title.slice(1).replace(rUnderscoreTrim, '');
                    }
                    if (title === '') {
                        return false;
                    }
                    m = title.match(rSplit);
                    if (m) {
                        id = getNsIdByName(m[1]);
                        if (id !== false) {
                            namespace = id;
                            title = m[2];
                            if (namespace === NS_TALK && (m = title.match(rSplit))) {
                                if (getNsIdByName(m[1]) !== false) {
                                    return false;
                                }
                            }
                        }
                    }
                    i = title.indexOf('#');
                    if (i === -1) {
                        fragment = null;
                    } else {
                        fragment = title.slice(i + 1).replace(/_/g, ' ');
                        title = title.slice(0, i).replace(rUnderscoreTrim, '');
                    }
                    if (title.match(rInvalid)) {
                        return false;
                    }
                    if (title.indexOf('.') !== -1 && (title === '.' || title === '..' || title.indexOf('./') === 0 || title.indexOf('../') === 0 || title.indexOf('/./') !== -1 || title.indexOf('/../') !== -1 || title.slice(-2) === '/.' || title.slice(-3) === '/..')) {
                        return false;
                    }
                    if (title.indexOf('~~~') !== -1) {
                        return false;
                    }
                    if (namespace !== NS_SPECIAL && mwString.byteLength(title) > TITLE_MAX_BYTES) {
                        return false;
                    }
                    if (title === '' && namespace !== NS_MAIN) {
                        return false;
                    }
                    if (title[0] === ':') {
                        return false;
                    }
                    i = title.lastIndexOf('.');
                    if (i === -1 || title.length <= i + 1) {
                        ext =
                            null;
                    } else {
                        ext = title.slice(i + 1);
                        title = title.slice(0, i);
                    }
                    return {
                        namespace: namespace,
                        title: title,
                        ext: ext,
                        fragment: fragment
                    };
                },
                text = function(s) {
                    return s.replace(/_/g, ' ');
                },
                sanitize = function(s, filter) {
                    var i, ruleLength, rule, m, filterLength, rules = sanitationRules;
                    for (i = 0, ruleLength = rules.length; i < ruleLength; ++i) {
                        rule = rules[i];
                        for (m = 0, filterLength = filter.length; m < filterLength; ++m) {
                            if (rule[filter[m]]) {
                                s = s.replace(rule.pattern, rule.replace);
                            }
                        }
                    }
                    return s;
                },
                trimToByteLength = function(s, length) {
                    return mwString.trimByteLength('', s, length).newVal;
                },
                trimFileNameToByteLength = function(name, extension) {
                    return trimToByteLength(name, FILENAME_MAX_BYTES - extension.length - 1) + '.' + extension;
                };

            function Title(title, namespace) {
                var parsed = parse(title, namespace);
                if (!parsed) {
                    throw new Error('Unable to parse title');
                }
                this.namespace = parsed.namespace;
                this.title = parsed.title;
                this.ext = parsed.ext;
                this.fragment = parsed.fragment;
            }
            Title.newFromText = function(title, namespace) {
                var t, parsed = parse(title, namespace);
                if (!parsed) {
                    return null;
                }
                t = Object.create(Title.prototype);
                t.namespace = parsed.namespace;
                t.title = parsed.title;
                t.ext = parsed.ext;
                t.fragment = parsed.fragment;
                return t;
            };
            Title.makeTitle = function(namespace, title) {
                return mw.Title.newFromText(getNamespacePrefix(namespace) + title);
            };
            Title.newFromUserInput = function(title, defaultNamespaceOrOptions, options) {
                var namespace, m, id, ext, lastDot, defaultNamespace;
                if (arguments.length < 3 && typeof defaultNamespace === 'object') {
                    options = defaultNamespaceOrOptions;
                } else {
                    defaultNamespace = defaultNamespaceOrOptions;
                }
                options = $.extend({
                    forUploading: !0
                }, options);
                namespace = defaultNamespace === undefined ? NS_MAIN : defaultNamespace;
                title = title.replace(/\s/g, ' ').trim();
                if (title !== '' && title[0] === ':') {
                    namespace = NS_MAIN;
                    title = title.substr(1).replace(rUnderscoreTrim, '');
                }
                m = title.match(rSplit);
                if (m) {
                    id = getNsIdByName(m[1]);
                    if (id !== false) {
                        namespace = id;
                        title = m[2];
                    }
                }
                if (namespace === NS_MEDIA || (options.forUploading && (namespace === NS_FILE))) {
                    title = sanitize(title, ['generalRule', 'fileRule']);
                    lastDot = title.lastIndexOf(
                        '.');
                    if (lastDot === -1 || lastDot >= title.length - 1) {
                        return null;
                    }
                    ext = title.slice(lastDot + 1);
                    title = title.slice(0, lastDot).trim();
                    title = trimFileNameToByteLength(title, ext);
                } else {
                    title = sanitize(title, ['generalRule']);
                    if (namespace !== NS_SPECIAL) {
                        title = trimToByteLength(title, TITLE_MAX_BYTES);
                    }
                }
                title = title.replace(/^:+/, '');
                return Title.newFromText(title, namespace);
            };
            Title.newFromFileName = function(uncleanName) {
                return Title.newFromUserInput('File:' + uncleanName, {
                    forUploading: !0
                });
            };
            Title.newFromImg = function(img) {
                var matches, i, regex, src, decodedSrc, thumbPhpRegex = /thumb\.php/,
                    regexes = [/\/[a-f0-9]\/[a-f0-9]{2}\/([^\s/]+)\/[^\s/]+-[^\s/]*$/, /\/[a-f0-9]\/[a-f0-9]{2}\/([^\s/]+)$/, /\/([^\s/]+)\/[^\s/]+-(?:\1|thumbnail)[^\s/]*$/, /\/([^\s/]+)$/],
                    recount = regexes.length;
                src = img.jquery ? img[0].src : img.src;
                matches = src.match(thumbPhpRegex);
                if (matches) {
                    return mw.Title.newFromText('File:' + mw.util.getParamValue('f', src));
                }
                decodedSrc = decodeURIComponent(src);
                for (i = 0; i < recount; i++) {
                    regex = regexes[i];
                    matches = decodedSrc.
                    match(regex);
                    if (matches && matches[1]) {
                        return mw.Title.newFromText('File:' + matches[1]);
                    }
                }
                return null;
            };
            Title.isTalkNamespace = function(namespaceId) {
                return !!(namespaceId > NS_MAIN && namespaceId % 2);
            };
            Title.wantSignaturesNamespace = function(namespaceId) {
                return Title.isTalkNamespace(namespaceId) || mw.config.get('wgExtraSignatureNamespaces').indexOf(namespaceId) !== -1;
            };
            Title.exists = function(title) {
                var match, obj = Title.exist.pages;
                if (typeof title === 'string') {
                    match = obj[title];
                } else if (title instanceof Title) {
                    match = obj[title.toString()];
                } else {
                    throw new Error('mw.Title.exists: title must be a string or an instance of Title');
                }
                if (typeof match !== 'boolean') {
                    return null;
                }
                return match;
            };
            Title.exist = {
                pages: {},
                set: function(titles, state) {
                    var i, len, pages = this.pages;
                    titles = Array.isArray(titles) ? titles : [titles];
                    state = state === undefined ? true : !!state;
                    for (i = 0, len = titles.length; i < len; i++) {
                        pages[titles[i]] = state;
                    }
                    return true;
                }
            };
            Title.normalizeExtension = function(extension) {
                var lower = extension.toLowerCase(),
                    squish = {
                        htm: 'html',
                        jpeg: 'jpg',
                        mpeg: 'mpg',
                        tiff: 'tif',
                        ogv: 'ogg'
                    };
                if (Object.prototype.hasOwnProperty.call(squish, lower)) {
                    return squish[lower];
                } else if (/^[0-9a-z]+$/.test(lower)) {
                    return lower;
                } else {
                    return '';
                }
            };
            Title.phpCharToUpper = function(chr) {
                var mapped = toUpperMapping[chr];
                return mapped || chr.toUpperCase();
            };
            Title.prototype = {
                constructor: Title,
                getNamespaceId: function() {
                    return this.namespace;
                },
                getNamespacePrefix: function() {
                    return getNamespacePrefix(this.namespace);
                },
                getName: function() {
                    if (mw.config.get('wgCaseSensitiveNamespaces').indexOf(this.namespace) !== -1 || !this.title.length) {
                        return this.title;
                    }
                    return mw.Title.phpCharToUpper(this.title[0]) + this.title.slice(1);
                },
                getNameText: function() {
                    return text(this.getName());
                },
                getExtension: function() {
                    return this.ext;
                },
                getDotExtension: function() {
                    return this.ext === null ? '' : '.' + this.ext;
                },
                getMain: function() {
                    return this.getName() + this.getDotExtension();
                },
                getMainText: function() {
                    return text(this.getMain());
                },
                getPrefixedDb: function() {
                    return this.getNamespacePrefix() + this
                        .getMain();
                },
                getPrefixedText: function() {
                    return text(this.getPrefixedDb());
                },
                getRelativeText: function(namespace) {
                    if (this.getNamespaceId() === namespace) {
                        return this.getMainText();
                    } else if (this.getNamespaceId() === NS_MAIN) {
                        return ':' + this.getPrefixedText();
                    } else {
                        return this.getPrefixedText();
                    }
                },
                getFragment: function() {
                    return this.fragment;
                },
                getUrl: function(params) {
                    var fragment = this.getFragment();
                    if (fragment) {
                        return mw.util.getUrl(this.toString() + '#' + fragment, params);
                    } else {
                        return mw.util.getUrl(this.toString(), params);
                    }
                },
                isTalkPage: function() {
                    return Title.isTalkNamespace(this.getNamespaceId());
                },
                getTalkPage: function() {
                    if (!this.canHaveTalkPage()) {
                        return null;
                    }
                    return this.isTalkPage() ? this : Title.makeTitle(this.getNamespaceId() + 1, this.getMainText());
                },
                getSubjectPage: function() {
                    return this.isTalkPage() ? Title.makeTitle(this.getNamespaceId() - 1, this.getMainText()) : this;
                },
                canHaveTalkPage: function() {
                    return this.getNamespaceId() >= NS_MAIN;
                },
                exists: function() {
                    return Title.exists(this);
                }
            };
            Title.prototype.
            toString = Title.prototype.getPrefixedDb;
            Title.prototype.toText = Title.prototype.getPrefixedText;
            mw.Title = Title;
        },
        "phpCharToUpper.json": {
            "ß": "ß",
            "ŉ": "ŉ",
            "ǅ": "ǅ",
            "ǆ": "ǅ",
            "ǈ": "ǈ",
            "ǉ": "ǈ",
            "ǋ": "ǋ",
            "ǌ": "ǋ",
            "ǰ": "ǰ",
            "ǲ": "ǲ",
            "ǳ": "ǲ",
            "ʝ": "Ʝ",
            "ͅ": "ͅ",
            "ΐ": "ΐ",
            "ΰ": "ΰ",
            "և": "և",
            "ᏸ": "Ᏸ",
            "ᏹ": "Ᏹ",
            "ᏺ": "Ᏺ",
            "ᏻ": "Ᏻ",
            "ᏼ": "Ᏼ",
            "ᏽ": "Ᏽ",
            "ẖ": "ẖ",
            "ẗ": "ẗ",
            "ẘ": "ẘ",
            "ẙ": "ẙ",
            "ẚ": "ẚ",
            "ὐ": "ὐ",
            "ὒ": "ὒ",
            "ὔ": "ὔ",
            "ὖ": "ὖ",
            "ᾀ": "ᾈ",
            "ᾁ": "ᾉ",
            "ᾂ": "ᾊ",
            "ᾃ": "ᾋ",
            "ᾄ": "ᾌ",
            "ᾅ": "ᾍ",
            "ᾆ": "ᾎ",
            "ᾇ": "ᾏ",
            "ᾈ": "ᾈ",
            "ᾉ": "ᾉ",
            "ᾊ": "ᾊ",
            "ᾋ": "ᾋ",
            "ᾌ": "ᾌ",
            "ᾍ": "ᾍ",
            "ᾎ": "ᾎ",
            "ᾏ": "ᾏ",
            "ᾐ": "ᾘ",
            "ᾑ": "ᾙ",
            "ᾒ": "ᾚ",
            "ᾓ": "ᾛ",
            "ᾔ": "ᾜ",
            "ᾕ": "ᾝ",
            "ᾖ": "ᾞ",
            "ᾗ": "ᾟ",
            "ᾘ": "ᾘ",
            "ᾙ": "ᾙ",
            "ᾚ": "ᾚ",
            "ᾛ": "ᾛ",
            "ᾜ": "ᾜ",
            "ᾝ": "ᾝ",
            "ᾞ": "ᾞ",
            "ᾟ": "ᾟ",
            "ᾠ": "ᾨ",
            "ᾡ": "ᾩ",
            "ᾢ": "ᾪ",
            "ᾣ": "ᾫ",
            "ᾤ": "ᾬ",
            "ᾥ": "ᾭ",
            "ᾦ": "ᾮ",
            "ᾧ": "ᾯ",
            "ᾨ": "ᾨ",
            "ᾩ": "ᾩ",
            "ᾪ": "ᾪ",
            "ᾫ": "ᾫ",
            "ᾬ": "ᾬ",
            "ᾭ": "ᾭ",
            "ᾮ": "ᾮ",
            "ᾯ": "ᾯ",
            "ᾲ": "ᾲ",
            "ᾳ": "ᾼ",
            "ᾴ": "ᾴ",
            "ᾶ": "ᾶ",
            "ᾷ": "ᾷ",
            "ᾼ": "ᾼ",
            "ῂ": "ῂ",
            "ῃ": "ῌ",
            "ῄ": "ῄ",
            "ῆ": "ῆ",
            "ῇ": "ῇ",
            "ῌ": "ῌ",
            "ῒ": "ῒ",
            "ΐ": "ΐ",
            "ῖ": "ῖ",
            "ῗ": "ῗ",
            "ῢ": "ῢ",
            "ΰ": "ΰ",
            "ῤ": "ῤ",
            "ῦ": "ῦ",
            "ῧ": "ῧ",
            "ῲ": "ῲ",
            "ῳ": "ῼ",
            "ῴ": "ῴ",
            "ῶ": "ῶ",
            "ῷ": "ῷ",
            "ῼ": "ῼ",
            "ⅰ": "ⅰ",
            "ⅱ": "ⅱ",
            "ⅲ": "ⅲ",
            "ⅳ": "ⅳ",
            "ⅴ": "ⅴ",
            "ⅵ": "ⅵ",
            "ⅶ": "ⅶ",
            "ⅷ": "ⅷ",
            "ⅸ": "ⅸ",
            "ⅹ": "ⅹ",
            "ⅺ": "ⅺ",
            "ⅻ": "ⅻ",
            "ⅼ": "ⅼ",
            "ⅽ": "ⅽ",
            "ⅾ": "ⅾ",
            "ⅿ": "ⅿ",
            "ⓐ": "ⓐ",
            "ⓑ": "ⓑ",
            "ⓒ": "ⓒ",
            "ⓓ": "ⓓ",
            "ⓔ": "ⓔ",
            "ⓕ": "ⓕ",
            "ⓖ": "ⓖ",
            "ⓗ": "ⓗ",
            "ⓘ": "ⓘ",
            "ⓙ": "ⓙ",
            "ⓚ": "ⓚ",
            "ⓛ": "ⓛ",
            "ⓜ": "ⓜ",
            "ⓝ": "ⓝ",
            "ⓞ": "ⓞ",
            "ⓟ": "ⓟ",
            "ⓠ": "ⓠ",
            "ⓡ": "ⓡ",
            "ⓢ": "ⓢ",
            "ⓣ": "ⓣ",
            "ⓤ": "ⓤ",
            "ⓥ": "ⓥ",
            "ⓦ": "ⓦ",
            "ⓧ": "ⓧ",
            "ⓨ": "ⓨ",
            "ⓩ": "ⓩ",
            "ꞵ": "Ꞵ",
            "ꞷ": "Ꞷ",
            "ꭓ": "Ꭓ",
            "ꭰ": "Ꭰ",
            "ꭱ": "Ꭱ",
            "ꭲ": "Ꭲ",
            "ꭳ": "Ꭳ",
            "ꭴ": "Ꭴ",
            "ꭵ": "Ꭵ",
            "ꭶ": "Ꭶ",
            "ꭷ": "Ꭷ",
            "ꭸ": "Ꭸ",
            "ꭹ": "Ꭹ",
            "ꭺ": "Ꭺ",
            "ꭻ": "Ꭻ",
            "ꭼ": "Ꭼ",
            "ꭽ": "Ꭽ",
            "ꭾ": "Ꭾ",
            "ꭿ": "Ꭿ",
            "ꮀ": "Ꮀ",
            "ꮁ": "Ꮁ",
            "ꮂ": "Ꮂ",
            "ꮃ": "Ꮃ",
            "ꮄ": "Ꮄ",
            "ꮅ": "Ꮅ",
            "ꮆ": "Ꮆ",
            "ꮇ": "Ꮇ",
            "ꮈ": "Ꮈ",
            "ꮉ": "Ꮉ",
            "ꮊ": "Ꮊ",
            "ꮋ": "Ꮋ",
            "ꮌ": "Ꮌ",
            "ꮍ": "Ꮍ",
            "ꮎ": "Ꮎ",
            "ꮏ": "Ꮏ",
            "ꮐ": "Ꮐ",
            "ꮑ": "Ꮑ",
            "ꮒ": "Ꮒ",
            "ꮓ": "Ꮓ",
            "ꮔ": "Ꮔ",
            "ꮕ": "Ꮕ",
            "ꮖ": "Ꮖ",
            "ꮗ": "Ꮗ",
            "ꮘ": "Ꮘ",
            "ꮙ": "Ꮙ",
            "ꮚ": "Ꮚ",
            "ꮛ": "Ꮛ",
            "ꮜ": "Ꮜ",
            "ꮝ": "Ꮝ",
            "ꮞ": "Ꮞ",
            "ꮟ": "Ꮟ",
            "ꮠ": "Ꮠ",
            "ꮡ": "Ꮡ",
            "ꮢ": "Ꮢ",
            "ꮣ": "Ꮣ",
            "ꮤ": "Ꮤ",
            "ꮥ": "Ꮥ",
            "ꮦ": "Ꮦ",
            "ꮧ": "Ꮧ",
            "ꮨ": "Ꮨ",
            "ꮩ": "Ꮩ",
            "ꮪ": "Ꮪ",
            "ꮫ": "Ꮫ",
            "ꮬ": "Ꮬ",
            "ꮭ": "Ꮭ",
            "ꮮ": "Ꮮ",
            "ꮯ": "Ꮯ",
            "ꮰ": "Ꮰ",
            "ꮱ": "Ꮱ",
            "ꮲ": "Ꮲ",
            "ꮳ": "Ꮳ",
            "ꮴ": "Ꮴ",
            "ꮵ": "Ꮵ",
            "ꮶ": "Ꮶ",
            "ꮷ": "Ꮷ",
            "ꮸ": "Ꮸ",
            "ꮹ": "Ꮹ",
            "ꮺ": "Ꮺ",
            "ꮻ": "Ꮻ",
            "ꮼ": "Ꮼ",
            "ꮽ": "Ꮽ",
            "ꮾ": "Ꮾ",
            "ꮿ": "Ꮿ",
            "ﬀ": "ﬀ",
            "ﬁ": "ﬁ",
            "ﬂ": "ﬂ",
            "ﬃ": "ﬃ",
            "ﬄ": "ﬄ",
            "ﬅ": "ﬅ",
            "ﬆ": "ﬆ",
            "ﬓ": "ﬓ",
            "ﬔ": "ﬔ",
            "ﬕ": "ﬕ",
            "ﬖ": "ﬖ",
            "ﬗ": "ﬗ"
        }
    }
});
mw.loader.implement("mediawiki.Uri@0m5gdom", function($, jQuery, require, module) {
    (function() {
        var parser, properties;

        function cat(pre, val, post, raw) {
            if (val === undefined || val === null || val === '') {
                return '';
            }
            return pre + (raw ? val : mw.Uri.encode(val)) + post;
        }
        parser = {
            strict: mw.template.get('mediawiki.Uri', 'strict.regexp').render(),
            loose: mw.template.get('mediawiki.Uri', 'loose.regexp').render()
        };
        properties = ['protocol', 'user', 'password', 'host', 'port', 'path', 'query', 'fragment'];
        mw.UriRelative = function(documentLocation) {
            var getDefaultUri = (function() {
                var href, uri;
                return function() {
                    var hrefCur = typeof documentLocation === 'string' ? documentLocation : documentLocation();
                    if (href === hrefCur) {
                        return uri;
                    }
                    href = hrefCur;
                    uri = new Uri(href);
                    return uri;
                };
            }());

            function Uri(uri, options) {
                var prop, hrefCur, hasOptions = (options !== undefined),
                    defaultUri = getDefaultUri();
                options = typeof options === 'object' ? options : {
                    strictMode: !!options
                };
                options = $.extend({
                    strictMode: !1,
                    overrideKeys: !1,
                    arrayParams: !1
                }, options);
                this.arrayParams = options
                    .arrayParams;
                if (uri !== undefined && uri !== null && uri !== '') {
                    if (typeof uri === 'string') {
                        this.parse(uri, options);
                    } else if (typeof uri === 'object') {
                        for (prop in uri) {
                            if (Object.prototype.hasOwnProperty.call(uri, prop)) {
                                if (Array.isArray(uri[prop]) || $.isPlainObject(uri[prop])) {
                                    this[prop] = $.extend(true, {}, uri[prop]);
                                } else {
                                    this[prop] = uri[prop];
                                }
                            }
                        }
                        if (!this.query) {
                            this.query = {};
                        }
                    }
                } else if (hasOptions) {
                    hrefCur = typeof documentLocation === 'string' ? documentLocation : documentLocation();
                    this.parse(hrefCur, options);
                } else {
                    return defaultUri.clone();
                }
                if (!this.protocol) {
                    this.protocol = defaultUri.protocol;
                }
                if (!this.host) {
                    this.host = defaultUri.host;
                    if (!this.port) {
                        this.port = defaultUri.port;
                    }
                }
                if (this.path && this.path[0] !== '/') {
                    throw new Error('Bad constructor arguments');
                }
                if (!(this.protocol && this.host && this.path)) {
                    throw new Error('Bad constructor arguments');
                }
            }
            Uri.encode = function(s) {
                return encodeURIComponent(s).replace(/!/g, '%21').replace(/'/g, '%27').replace(/\(/g, '%28').replace(/\)/g, '%29').replace(/\*/g, '%2A').replace(
                    /%20/g, '+');
            };
            Uri.decode = function(s) {
                return decodeURIComponent(s.replace(/\+/g, '%20'));
            };
            Uri.prototype = {
                parse: function(str, options) {
                    var q, matches, uri = this,
                        hasOwn = Object.prototype.hasOwnProperty;
                    matches = parser[options.strictMode ? 'strict' : 'loose'].exec(str);
                    properties.forEach(function(property, i) {
                        uri[property] = matches[i + 1];
                    });
                    q = {};
                    if (uri.query) {
                        uri.query.replace(/(?:^|&)([^&=]*)(?:(=)([^&]*))?/g, function(match, k, eq, v) {
                            var arrayKeyMatch, i;
                            if (k) {
                                k = Uri.decode(k);
                                v = (eq === '' || eq === undefined) ? null : Uri.decode(v);
                                arrayKeyMatch = k.match(/^([^[]+)\[(\d*)\]$/);
                                if (options.arrayParams && arrayKeyMatch) {
                                    k = arrayKeyMatch[1];
                                    if (!Array.isArray(q[k])) {
                                        q[k] = [];
                                    }
                                    i = arrayKeyMatch[2];
                                    if (i === '') {
                                        i = q[k].length;
                                    }
                                    q[k][i] = v;
                                } else if (options.arrayParams || options.overrideKeys || !hasOwn.call(q, k)) {
                                    q[k] = v;
                                } else {
                                    if (typeof q[k] === 'string') {
                                        q[k] = [q[k]];
                                    }
                                    if (Array.isArray(q[k])) {
                                        q[k].push(v);
                                    }
                                }
                            }
                        });
                    }
                    uri.query = q;
                    if (uri.fragment !== undefined) {
                        uri.fragment = Uri.decode(uri.fragment);
                    }
                },
                getUserInfo: function() {
                    return cat('', this.user, cat(':', this.password, ''));
                },
                getHostPort: function() {
                    return this.host + cat(':', this.port, '');
                },
                getAuthority: function() {
                    return cat('', this.getUserInfo(), '@') + this.getHostPort();
                },
                getQueryString: function() {
                    var args = [],
                        arrayParams = this.arrayParams;
                    $.each(this.query, function(key, val) {
                        var k = Uri.encode(key),
                            isArrayParam = Array.isArray(val),
                            vals = isArrayParam ? val : [val];
                        vals.forEach(function(v, i) {
                            var ki = k;
                            if (arrayParams && isArrayParam) {
                                ki += Uri.encode('[' + i + ']');
                            }
                            if (v === null) {
                                args.push(ki);
                            } else if (k === 'title') {
                                args.push(ki + '=' + mw.util.wikiUrlencode(v));
                            } else {
                                args.push(ki + '=' + Uri.encode(v));
                            }
                        });
                    });
                    return args.join('&');
                },
                getRelativePath: function() {
                    return this.path + cat('?', this.getQueryString(), '', true) + cat('#', this.fragment, '');
                },
                toString: function() {
                    return this.protocol + '://' + this.getAuthority() + this.getRelativePath();
                },
                clone: function() {
                    return new Uri(this);
                },
                extend: function(parameters) {
                    $.extend(this.query, parameters);
                    return this;
                }
            };
            return Uri;
        };
        mw.Uri = mw.UriRelative(function() {
            return location.href;
        });
    }());
}, {}, {}, {
    "strict.regexp": "^\n(?:(?\u003Cprotocol\u003E[^:/?#]+):)?\n(?://(?:\n\t(?:\n\t\t(?\u003Cuser\u003E[^:@/?#]*)\n\t\t(?::(?\u003Cpassword\u003E[^:@/?#]*))?\n\t)?@)?\n\t(?\u003Chost\u003E[^:/?#]*)\n\t(?::(?\u003Cport\u003E\\d*))?\n)?\n(?\u003Cpath\u003E(?:[^?#/]*/)*[^?#]*)\n(?:\\?(?\u003Cquery\u003E[^#]*))?\n(?:\\#(?\u003Cfragment\u003E.*))?\n",
    "loose.regexp": "^\n(?:\n\t(?![^:@]+:[^:@/]*@)\n\t(?\u003Cprotocol\u003E[^:/?#.]+):\n)?\n(?://)?\n(?:(?:\n\t(?\u003Cuser\u003E[^:@/?#]*)\n\t(?::(?\u003Cpassword\u003E[^:@/?#]*))?\n)?@)?\n(?\u003Chost\u003E[^:/?#]*)\n(?::(?\u003Cport\u003E\\d*))?\n(\n\t(?:/\n\t\t(?:[^?#]\n\t\t\t(?![^?#/]*\\.[^?#/.]+(?:[?#]|$))\n\t\t)*/?\n\t)?\n\t[^?#/]*\n)\n(?:\\?(?\u003Cquery\u003E[^#]*))?\n(?:\\#(?\u003Cfragment\u003E.*))?\n"
});
mw.loader.implement("mediawiki.api@0nli0da", function($, jQuery, require, module) {
    (function() {
        var defaultOptions = {
                parameters: {
                    action: 'query',
                    format: 'json'
                },
                ajax: {
                    url: mw.util.wikiScript('api'),
                    timeout: 30 * 1000,
                    dataType: 'json'
                }
            },
            promises = {};

        function mapLegacyToken(action) {
            var csrfActions = ['edit', 'delete', 'protect', 'move', 'block', 'unblock', 'email', 'import', 'options'];
            if (csrfActions.indexOf(action) !== -1) {
                mw.track('mw.deprecate', 'apitoken_' + action);
                mw.log.warn('Use of the "' + action + '" token is deprecated. Use "csrf" instead.');
                return 'csrf';
            }
            return action;
        }
        promises[defaultOptions.ajax.url] = {};
        $.each(mw.user.tokens.get(), function(key, value) {
            promises[defaultOptions.ajax.url][key] = $.Deferred().resolve(value).promise({
                abort: function() {}
            });
        });
        mw.Api = function(options) {
            var defaults = $.extend({}, options),
                setsUrl = options && options.ajax && options.ajax.url !== undefined;
            defaults.parameters = $.extend({}, defaultOptions.parameters, defaults.parameters);
            defaults.ajax = $.extend({}, defaultOptions.ajax, defaults.ajax);
            if (
                setsUrl) {
                defaults.ajax.url = String(defaults.ajax.url);
            }
            if (defaults.useUS === undefined) {
                defaults.useUS = !setsUrl;
            }
            this.defaults = defaults;
            this.requests = [];
        };
        mw.Api.prototype = {
            abort: function() {
                this.requests.forEach(function(request) {
                    if (request) {
                        request.abort();
                    }
                });
            },
            get: function(parameters, ajaxOptions) {
                ajaxOptions = ajaxOptions || {};
                ajaxOptions.type = 'GET';
                return this.ajax(parameters, ajaxOptions);
            },
            post: function(parameters, ajaxOptions) {
                ajaxOptions = ajaxOptions || {};
                ajaxOptions.type = 'POST';
                return this.ajax(parameters, ajaxOptions);
            },
            preprocessParameters: function(parameters, useUS) {
                var key;
                for (key in parameters) {
                    if (Array.isArray(parameters[key])) {
                        if (!useUS || parameters[key].join('').indexOf('|') === -1) {
                            parameters[key] = parameters[key].join('|');
                        } else {
                            parameters[key] = '\x1f' + parameters[key].join('\x1f');
                        }
                    } else if (parameters[key] === false || parameters[key] === undefined) {
                        delete parameters[key];
                    }
                }
            },
            ajax: function(parameters, ajaxOptions) {
                var token, requestIndex, api = this,
                    apiDeferred = $.Deferred(),
                    xhr, key, formData;
                parameters = $.extend({}, this.defaults.parameters, parameters);
                ajaxOptions = $.extend({}, this.defaults.ajax, ajaxOptions);
                if (parameters.token) {
                    token = parameters.token;
                    delete parameters.token;
                }
                this.preprocessParameters(parameters, this.defaults.useUS);
                if (ajaxOptions.type === 'POST' && window.FormData && ajaxOptions.contentType === 'multipart/form-data') {
                    formData = new FormData();
                    for (key in parameters) {
                        formData.append(key, parameters[key]);
                    }
                    if (token) {
                        formData.append('token', token);
                    }
                    ajaxOptions.data = formData;
                    ajaxOptions.processData = !1;
                    ajaxOptions.contentType = !1;
                } else {
                    ajaxOptions.data = $.param(parameters);
                    if (token) {
                        ajaxOptions.data += '&token=' + encodeURIComponent(token);
                    }
                    ajaxOptions.data = ajaxOptions.data.replace(/\./g, '%2E');
                    if (ajaxOptions.contentType === 'multipart/form-data') {
                        delete ajaxOptions.contentType;
                    }
                }
                xhr = $.ajax(ajaxOptions).fail(function(xhr, textStatus, exception) {
                    apiDeferred.reject('http', {
                        xhr: xhr,
                        textStatus: textStatus,
                        exception: exception
                    });
                }).done(function(result, textStatus, jqXHR) {
                    var code;
                    if (
                        result === undefined || result === null || result === '') {
                        apiDeferred.reject('ok-but-empty', 'OK response but empty result (check HTTP headers?)', result, jqXHR);
                    } else if (result.error) {
                        code = result.error.code === undefined ? 'unknown' : result.error.code;
                        apiDeferred.reject(code, result, result, jqXHR);
                    } else if (result.errors) {
                        code = result.errors[0].code === undefined ? 'unknown' : result.errors[0].code;
                        apiDeferred.reject(code, result, result, jqXHR);
                    } else {
                        apiDeferred.resolve(result, jqXHR);
                    }
                });
                requestIndex = this.requests.length;
                this.requests.push(xhr);
                xhr.always(function() {
                    api.requests[requestIndex] = null;
                });
                return apiDeferred.promise({
                    abort: xhr.abort
                }).fail(function(code, details) {
                    if (!(code === 'http' && details && details.textStatus === 'abort')) {
                        mw.log('mw.Api error: ', code, details);
                    }
                });
            },
            postWithToken: function(tokenType, params, ajaxOptions) {
                var api = this,
                    abortedPromise = $.Deferred().reject('http', {
                        textStatus: 'abort',
                        exception: 'abort'
                    }).promise(),
                    abortable, aborted;
                return api.getToken(tokenType, params.assert).then(function(token) {
                    params.token = token;
                    if (aborted) {
                        return abortedPromise;
                    }
                    return (abortable = api.post(params, ajaxOptions)).catch(function(code) {
                        if (code === 'badtoken') {
                            api.badToken(tokenType);
                            params.token = undefined;
                            abortable = null;
                            return api.getToken(tokenType, params.assert).then(function(token) {
                                params.token = token;
                                if (aborted) {
                                    return abortedPromise;
                                }
                                return (abortable = api.post(params, ajaxOptions));
                            });
                        }
                        return $.Deferred().rejectWith(this, arguments);
                    });
                }).promise({
                    abort: function() {
                        if (abortable) {
                            abortable.abort();
                        } else {
                            aborted = !0;
                        }
                    }
                });
            },
            getToken: function(type, assert) {
                var apiPromise, promiseGroup, d, reject;
                type = mapLegacyToken(type);
                promiseGroup = promises[this.defaults.ajax.url];
                d = promiseGroup && promiseGroup[type + 'Token'];
                if (!promiseGroup) {
                    promiseGroup = promises[this.defaults.ajax.url] = {};
                }
                if (!d) {
                    apiPromise = this.get({
                        action: 'query',
                        meta: 'tokens',
                        type: type,
                        assert: assert
                    });
                    reject = function() {
                        delete promiseGroup[type + 'Token'];
                        return $.Deferred().rejectWith(this, arguments);
                    };
                    d = apiPromise.then(function(res) {
                        if (!res.query) {
                            return reject('query-missing', res);
                        }
                        if (!res.query.tokens[type + 'token']) {
                            return $.Deferred().reject('token-missing', res);
                        }
                        return res.query.tokens[type + 'token'];
                    }, reject).promise({
                        abort: apiPromise.abort
                    });
                    promiseGroup[type + 'Token'] = d;
                }
                return d;
            },
            badToken: function(type) {
                var promiseGroup = promises[this.defaults.ajax.url];
                type = mapLegacyToken(type);
                if (promiseGroup) {
                    delete promiseGroup[type + 'Token'];
                }
            }
        };
    }());
    (function() {
        $.extend(mw.Api.prototype, {
            isCategory: function(title) {
                var apiPromise = this.get({
                    formatversion: 2,
                    prop: 'categoryinfo',
                    titles: [String(title)]
                });
                return apiPromise.then(function(data) {
                    return !!(data.query && data.query.pages && data.query.pages[0].categoryinfo);
                }).promise({
                    abort: apiPromise.abort
                });
            },
            getCategoriesByPrefix: function(prefix) {
                var apiPromise = this.get({
                    formatversion: 2,
                    list: 'allpages',
                    apprefix: prefix,
                    apnamespace: mw.config.get('wgNamespaceIds').category
                });
                return apiPromise.then(function(data) {
                    return data.query.allpages.map(function(category) {
                        return new mw.Title(category.title).
                        getMainText();
                    });
                }).promise({
                    abort: apiPromise.abort
                });
            },
            getCategories: function(title) {
                var apiPromise = this.get({
                    formatversion: 2,
                    prop: 'categories',
                    titles: [String(title)]
                });
                return apiPromise.then(function(data) {
                    var page;
                    if (!data.query || !data.query.pages) {
                        return false;
                    }
                    page = data.query.pages[0];
                    if (!page.categories) {
                        return false;
                    }
                    return page.categories.map(function(cat) {
                        return new mw.Title(cat.title);
                    });
                }).promise({
                    abort: apiPromise.abort
                });
            }
        });
    }());
    (function() {
        $.extend(mw.Api.prototype, {
            postWithEditToken: function(params, ajaxOptions) {
                return this.postWithToken('csrf', params, ajaxOptions);
            },
            getEditToken: function() {
                return this.getToken('csrf');
            },
            create: function(title, params, content) {
                return this.postWithEditToken($.extend({
                    action: 'edit',
                    title: String(title),
                    text: content,
                    formatversion: '2',
                    assert: mw.config.get('wgUserName') ? 'user' : undefined,
                    createonly: !0
                }, params)).then(function(data) {
                    return data.edit;
                });
            },
            edit: function(title, transform) {
                var basetimestamp, curtimestamp, api = this;
                title = String(title);
                return api.get({
                    action: 'query',
                    prop: 'revisions',
                    rvprop: ['content', 'timestamp'],
                    titles: [title],
                    formatversion: '2',
                    curtimestamp: !0
                }).then(function(data) {
                    var page, revision;
                    if (!data.query || !data.query.pages) {
                        return $.Deferred().reject('unknown');
                    }
                    page = data.query.pages[0];
                    if (!page || page.invalid) {
                        return $.Deferred().reject('invalidtitle');
                    }
                    if (page.missing) {
                        return $.Deferred().reject('nocreate-missing');
                    }
                    revision = page.revisions[0];
                    basetimestamp = revision.timestamp;
                    curtimestamp = data.curtimestamp;
                    return transform({
                        timestamp: revision.timestamp,
                        content: revision.content
                    });
                }).then(function(params) {
                    var editParams = typeof params === 'object' ? params : {
                        text: String(params)
                    };
                    return api.postWithEditToken($.extend({
                        action: 'edit',
                        title: title,
                        formatversion: '2',
                        assert: mw.config.get('wgUserName') ? 'user' : undefined,
                        basetimestamp: basetimestamp,
                        starttimestamp: curtimestamp,
                        nocreate: !0
                    }, editParams));
                }).then(function(data) {
                    return data.edit;
                });
            },
            newSection: function(title, header, message, additionalParams) {
                return this.
                postWithEditToken($.extend({
                    action: 'edit',
                    section: 'new',
                    title: String(title),
                    summary: header,
                    text: message
                }, additionalParams));
            }
        });
    }());
    (function() {
        'use strict';
        $.extend(mw.Api.prototype, {
            login: function(username, password) {
                var params, apiPromise, innerPromise, api = this;
                params = {
                    action: 'login',
                    lgname: username,
                    lgpassword: password
                };
                apiPromise = api.post(params);
                return apiPromise.then(function(data) {
                    params.lgtoken = data.login.token;
                    innerPromise = api.post(params).then(function(data) {
                        var code;
                        if (data.login.result !== 'Success') {
                            code = data.error && data.error.code || 'unknown';
                            return $.Deferred().reject(code, data);
                        }
                        return data;
                    });
                    return innerPromise;
                }).promise({
                    abort: function() {
                        apiPromise.abort();
                        if (innerPromise) {
                            innerPromise.abort();
                        }
                    }
                });
            }
        });
    }());
    (function() {
        'use strict';
        $.extend(mw.Api.prototype, {
            getMessages: function(messages, options) {
                options = options || {};
                return this.get($.extend({
                    action: 'query',
                    meta: 'allmessages',
                    ammessages: messages,
                    amlang: mw.config.get('wgUserLanguage'),
                    formatversion: 2
                }, options)).then(
                    function(data) {
                        var result = {};
                        data.query.allmessages.forEach(function(obj) {
                            if (!obj.missing) {
                                result[obj.name] = obj.content;
                            }
                        });
                        return result;
                    });
            },
            loadMessages: function(messages, options) {
                return this.getMessages(messages, options).then(mw.messages.set.bind(mw.messages));
            },
            loadMessagesIfMissing: function(messages, options) {
                var missing = messages.filter(function(msg) {
                    return !mw.message(msg).exists();
                });
                if (missing.length === 0) {
                    return $.Deferred().resolve();
                }
                return this.loadMessages(missing, options);
            }
        });
    }());
    (function() {
        var saveOptionsRequests = {};
        $.extend(mw.Api.prototype, {
            saveOption: function(name, value) {
                var param = {};
                param[name] = value;
                return this.saveOptions(param);
            },
            saveOptions: function(options) {
                var name, value, bundleable, grouped = [],
                    promise;
                if (mw.config.get('wgUserName') === null) {
                    return $.Deferred().reject('notloggedin').promise();
                }
                if (saveOptionsRequests[this.defaults.ajax.url] && saveOptionsRequests[this.defaults.ajax.url].state() === 'pending') {
                    promise = saveOptionsRequests[this.defaults.ajax.url].then(
                        function() {
                            return $.Deferred().resolve();
                        },
                        function() {
                            return $.Deferred().resolve();
                        });
                } else {
                    promise = $.Deferred().resolve();
                }
                for (name in options) {
                    value = options[name] === null ? null : String(options[name]);
                    if (this.defaults.useUS) {
                        bundleable = name.indexOf('=') === -1;
                    } else {
                        bundleable = (value === null || value.indexOf('|') === -1) && (name.indexOf('|') === -1 && name.indexOf('=') === -1);
                    }
                    if (bundleable) {
                        if (value !== null) {
                            grouped.push(name + '=' + value);
                        } else {
                            grouped.push(name);
                        }
                    } else {
                        if (value !== null) {
                            promise = promise.then(function(name, value) {
                                return this.postWithToken('csrf', {
                                    formatversion: 2,
                                    action: 'options',
                                    optionname: name,
                                    optionvalue: value
                                });
                            }.bind(this, name, value));
                        } else {
                            promise = promise.then(function(name) {
                                return this.postWithToken('csrf', {
                                    formatversion: 2,
                                    action: 'options',
                                    optionname: name
                                });
                            }.bind(this, name));
                        }
                    }
                }
                if (grouped.length) {
                    promise = promise.then(function() {
                        return this.postWithToken('csrf', {
                            formatversion: 2,
                            action: 'options',
                            change: grouped
                        });
                    }.bind(this));
                }
                saveOptionsRequests[this.defaults.ajax.url] = promise;
                return promise;
            }
        });
    }());
    (function() {
        $.extend(mw.Api.prototype, {
            parse: function(content, additionalParams) {
                var apiPromise, config = $.extend({
                    formatversion: 2,
                    action: 'parse',
                    contentmodel: 'wikitext'
                }, additionalParams);
                if (mw.Title && content instanceof mw.Title) {
                    config.page = content.getPrefixedDb();
                    apiPromise = this.get(config);
                } else {
                    config.text = String(content);
                    apiPromise = this.post(config);
                }
                return apiPromise.then(function(data) {
                    return data.parse.text;
                }).promise({
                    abort: apiPromise.abort
                });
            }
        });
    }());
    (function() {
        $.extend(mw.Api.prototype, {
            rollback: function(page, user, params) {
                return this.postWithToken('rollback', $.extend({
                    action: 'rollback',
                    title: String(page),
                    user: user,
                    uselang: mw.config.get('wgUserLanguage')
                }, params)).then(function(data) {
                    return data.rollback;
                });
            }
        });
    }());
    (function() {
        var nonce = 0,
            fieldsAllowed = {
                stash: !0,
                filekey: !0,
                filename: !0,
                comment: !0,
                text: !0,
                watchlist: !0,
                ignorewarnings: !0,
                chunk: !0,
                offset: !0,
                filesize: !0,
                async: !0
            };

        function getNonce() {
            return nonce++;
        }

        function getFirstKey(
            obj) {
            var key;
            for (key in obj) {
                return key;
            }
        }

        function getNewIframe(id) {
            var frame = document.createElement('iframe');
            frame.id = id;
            frame.name = id;
            return frame;
        }

        function getHiddenInput(name, val) {
            return $('<input>').attr('type', 'hidden').attr('name', name).val(val);
        }

        function processIframeResult(iframe) {
            var json, doc = iframe.contentDocument || frames[iframe.id].document;
            if (doc.XMLDocument) {
                return doc.XMLDocument;
            }
            if (doc.body) {
                json = $(doc.body).find('pre').text();
                return JSON.parse(json);
            }
            return doc;
        }

        function formDataAvailable() {
            return window.FormData !== undefined && window.File !== undefined && window.File.prototype.slice !== undefined;
        }
        $.extend(mw.Api.prototype, {
            upload: function(file, data) {
                var isFileInput, canUseFormData;
                isFileInput = file && file.nodeType === Node.ELEMENT_NODE;
                if (formDataAvailable() && isFileInput && file.files) {
                    file = file.files[0];
                }
                if (!file) {
                    throw new Error('No file');
                }
                canUseFormData = formDataAvailable() && (file instanceof window.File || file instanceof window.Blob);
                if (!isFileInput && !canUseFormData) {
                    throw new Error('Unsupported argument type passed to mw.Api.upload');
                }
                if (canUseFormData) {
                    return this.uploadWithFormData(file, data);
                }
                return this.uploadWithIframe(file, data);
            },
            uploadWithIframe: function(file, data) {
                var key, tokenPromise = $.Deferred(),
                    api = this,
                    deferred = $.Deferred(),
                    nonce = getNonce(),
                    id = 'uploadframe-' + nonce,
                    $form = $('<form>'),
                    iframe = getNewIframe(id),
                    $iframe = $(iframe);
                for (key in data) {
                    if (!fieldsAllowed[key]) {
                        delete data[key];
                    }
                }
                data = $.extend({}, this.defaults.parameters, {
                    action: 'upload'
                }, data);
                $form.addClass('mw-api-upload-form');
                $form.css('display', 'none').attr({
                    action: this.defaults.ajax.url,
                    method: 'POST',
                    target: id,
                    enctype: 'multipart/form-data'
                });
                $iframe.one('load', function() {
                    $iframe.one('load', function() {
                        var result = processIframeResult(iframe);
                        deferred.notify(1);
                        if (!result) {
                            deferred.reject('ok-but-empty', 'No response from API on upload attempt.');
                        } else if (result.error) {
                            if (result.error.code === 'badtoken') {
                                api.badToken('csrf');
                            }
                            deferred.reject(result.error.code, result);
                        } else if (result.upload &&
                            result.upload.warnings) {
                            deferred.reject(getFirstKey(result.upload.warnings), result);
                        } else {
                            deferred.resolve(result);
                        }
                    });
                    tokenPromise.done(function() {
                        $form.trigger('submit');
                    });
                });
                $iframe.on('error', function(error) {
                    deferred.reject('http', error);
                });
                $iframe.prop('src', 'about:blank').hide();
                file.name = 'file';
                $.each(data, function(key, val) {
                    $form.append(getHiddenInput(key, val));
                });
                if (!data.filename && !data.stash) {
                    throw new Error('Filename not included in file data.');
                }
                if (this.needToken()) {
                    this.getEditToken().then(function(token) {
                        $form.append(getHiddenInput('token', token));
                        tokenPromise.resolve();
                    }, tokenPromise.reject);
                } else {
                    tokenPromise.resolve();
                }
                $('body').append($form, $iframe);
                deferred.always(function() {
                    $form.remove();
                    $iframe.remove();
                });
                return deferred.promise();
            },
            uploadWithFormData: function(file, data) {
                var key, request, deferred = $.Deferred();
                for (key in data) {
                    if (!fieldsAllowed[key]) {
                        delete data[key];
                    }
                }
                data = $.extend({}, this.defaults.parameters, {
                    action: 'upload'
                }, data);
                if (!data.chunk) {
                    data.file = file;
                }
                if (!data.filename && !data.stash) {
                    throw new Error('Filename not included in file data.');
                }
                request = this[this.needToken() ? 'postWithEditToken' : 'post'](data, {
                    contentType: 'multipart/form-data',
                    timeout: 0,
                    xhr: function() {
                        var xhr = $.ajaxSettings.xhr();
                        if (xhr.upload) {
                            xhr.upload.addEventListener('progress', function(ev) {
                                if (ev.lengthComputable) {
                                    deferred.notify(ev.loaded / ev.total);
                                }
                            });
                        }
                        return xhr;
                    }
                }).done(function(result) {
                    deferred.notify(1);
                    if (result.upload && result.upload.warnings) {
                        deferred.reject(getFirstKey(result.upload.warnings), result);
                    } else {
                        deferred.resolve(result);
                    }
                }).fail(function(errorCode, result) {
                    deferred.notify(1);
                    deferred.reject(errorCode, result);
                });
                return deferred.promise({
                    abort: request.abort
                });
            },
            chunkedUpload: function(file, data, chunkSize, chunkRetries) {
                var start, end, promise, next, active, deferred = $.Deferred();
                chunkSize = chunkSize === undefined ? 5 * 1024 * 1024 : chunkSize;
                chunkRetries = chunkRetries === undefined ? 1 : chunkRetries;
                if (!data.filename) {
                    throw new Error('Filename not included in file data.');
                }
                active = promise = this.uploadChunk(file, data, 0, chunkSize, '', chunkRetries).done(chunkSize >= file.size ? deferred.resolve : null).fail(deferred.reject).progress(deferred.notify);
                for (start = chunkSize; start < file.size; start += chunkSize) {
                    end = Math.min(start + chunkSize, file.size);
                    next = $.Deferred();
                    promise.done(function(start, end, next, result) {
                        var filekey = result.upload.filekey;
                        active = this.uploadChunk(file, data, start, end, filekey, chunkRetries).done(end === file.size ? deferred.resolve : next.resolve).fail(deferred.reject).progress(deferred.notify);
                    }.bind(this, start, end, next));
                    promise = next;
                }
                return deferred.promise({
                    abort: active.abort
                });
            },
            uploadChunk: function(file, data, start, end, filekey, retries) {
                var upload, api = this,
                    chunk = this.slice(file, start, end);
                retries = retries === undefined ? 1 : retries;
                data.filesize = file.size;
                data.chunk = chunk;
                data.offset = start;
                if (filekey && start !== 0) {
                    data.filekey = filekey;
                }
                upload = this.uploadWithFormData(file, data);
                return upload.then(null, function(code, result) {
                    var retry;
                    if (result.upload && result.upload.warnings && code in result.upload.warnings) {
                        if (end === file.size) {
                            return $.Deferred().reject(code, result);
                        } else {
                            return $.Deferred().resolve(result);
                        }
                    }
                    if (retries === 0) {
                        return $.Deferred().reject(code, result);
                    }
                    retry = api.uploadChunk.bind(api, file, data, start, end, filekey, retries - 1);
                    return api.retry(code, result, retry);
                }, function(fraction) {
                    return (start + fraction * (end - start)) / file.size;
                }).promise({
                    abort: upload.abort
                });
            },
            retry: function(code, result, callable) {
                var uploadPromise, retryTimer, deferred = $.Deferred(),
                    retry = function() {
                        uploadPromise = callable();
                        uploadPromise.then(deferred.resolve, deferred.reject);
                    };
                if (code !== 'http' || result.textStatus === 'abort') {
                    return deferred.reject(code, result);
                }
                retryTimer = setTimeout(retry, 1000);
                return deferred.promise({
                    abort: function() {
                        if (retryTimer) {
                            clearTimeout(retryTimer);
                        }
                        if (uploadPromise.abort) {
                            uploadPromise.abort();
                        }
                    }
                });
            },
            slice: function(file, start, stop) {
                if (file.mozSlice) {
                    return file.mozSlice(start, stop, file.type);
                } else if (file.webkitSlice) {
                    return file.webkitSlice(start,
                        stop, file.type);
                } else {
                    return file.slice(start, stop, file.type);
                }
            },
            finishUploadToStash: function(uploadPromise, data) {
                var filekey, api = this;

                function finishUpload(moreData) {
                    return api.uploadFromStash(filekey, $.extend(data, moreData));
                }
                return uploadPromise.then(function(result) {
                    filekey = result.upload.filekey;
                    return finishUpload;
                }, function(errorCode, result) {
                    if (result && result.upload && result.upload.result === 'Success' && result.upload.filekey) {
                        filekey = result.upload.filekey;
                        return $.Deferred().resolve(finishUpload);
                    }
                    return $.Deferred().reject(errorCode, result);
                });
            },
            uploadToStash: function(file, data) {
                var promise;
                if (!data.filename) {
                    throw new Error('Filename not included in file data.');
                }
                promise = this.upload(file, {
                    stash: !0,
                    filename: data.filename
                });
                return this.finishUploadToStash(promise, data);
            },
            chunkedUploadToStash: function(file, data, chunkSize, chunkRetries) {
                var promise;
                if (!data.filename) {
                    throw new Error('Filename not included in file data.');
                }
                promise = this.chunkedUpload(file, {
                    stash: !0,
                    filename: data.
                    filename
                }, chunkSize, chunkRetries);
                return this.finishUploadToStash(promise, data);
            },
            uploadFromStash: function(filekey, data) {
                data.filekey = filekey;
                data.action = 'upload';
                data.format = 'json';
                if (!data.filename) {
                    throw new Error('Filename not included in file data.');
                }
                return this.postWithEditToken(data).then(function(result) {
                    if (result.upload && result.upload.warnings) {
                        return $.Deferred().reject(getFirstKey(result.upload.warnings), result).promise();
                    }
                    return result;
                });
            },
            needToken: function() {
                return true;
            }
        });
    }());
    (function() {
        $.extend(mw.Api.prototype, {
            getUserInfo: function() {
                return this.get({
                    action: 'query',
                    meta: 'userinfo',
                    uiprop: ['groups', 'rights']
                }).then(function(data) {
                    if (data.query && data.query.userinfo) {
                        return data.query.userinfo;
                    }
                    return $.Deferred().reject().promise();
                });
            }
        });
    }());
    (function() {
        function doWatchInternal(pages, addParams) {
            var apiPromise = this.postWithToken('watch', $.extend({
                formatversion: 2,
                action: 'watch',
                titles: Array.isArray(pages) ? pages : String(pages)
            }, addParams));
            return apiPromise.then(
                function(data) {
                    return Array.isArray(pages) ? data.watch : data.watch[0];
                }).promise({
                abort: apiPromise.abort
            });
        }
        $.extend(mw.Api.prototype, {
            watch: function(pages) {
                return doWatchInternal.call(this, pages);
            },
            unwatch: function(pages) {
                return doWatchInternal.call(this, pages, {
                    unwatch: 1
                });
            }
        });
    }());
});
mw.loader.implement("mediawiki.base@0f03fxc", function($, jQuery, require, module) {
    (function() {
        'use strict';
        mw.errorLogger = {
            installGlobalHandler: function(window) {
                var oldHandler = window.onerror || function() {
                    return false;
                };
                window.onerror = function(errorMessage, url, lineNumber, columnNumber, errorObject) {
                    mw.track('global.error', {
                        errorMessage: errorMessage,
                        url: url,
                        lineNumber: lineNumber,
                        columnNumber: columnNumber,
                        errorObject: errorObject
                    });
                    return oldHandler.apply(this, arguments);
                };
            }
        };
        mw.errorLogger.installGlobalHandler(window);
    }());
    (function() {
        'use strict';
        var slice = Array.prototype.slice,
            mwLoaderTrack = mw.track,
            trackCallbacks = $.Callbacks('memory'),
            trackHandlers = [],
            queue;

        function Message(map, key, parameters) {
            this.format = 'text';
            this.map = map;
            this.key = key;
            this.parameters = parameters === undefined ? [] : slice.call(parameters);
            return this;
        }
        Message.prototype = {
            parser: function() {
                var text = this.map.get(this.key);
                if (mw.config.get('wgUserLanguage') === 'qqx' && (!text || text === '(' + this.key + ')')) {
                    text = '(' + this.key + '$*)';
                }
                return mw
                    .format.apply(null, [text].concat(this.parameters));
            },
            params: function(parameters) {
                var i;
                for (i = 0; i < parameters.length; i++) {
                    this.parameters.push(parameters[i]);
                }
                return this;
            },
            toString: function() {
                var text;
                if (!this.exists()) {
                    return '⧼' + mw.html.escape(this.key) + '⧽';
                }
                if (this.format === 'plain' || this.format === 'text' || this.format === 'parse') {
                    text = this.parser();
                }
                if (this.format === 'escaped') {
                    text = this.parser();
                    text = mw.html.escape(text);
                }
                return text;
            },
            parse: function() {
                this.format = 'parse';
                return this.toString();
            },
            plain: function() {
                this.format = 'plain';
                return this.toString();
            },
            text: function() {
                this.format = 'text';
                return this.toString();
            },
            escaped: function() {
                this.format = 'escaped';
                return this.toString();
            },
            exists: function() {
                if (mw.config.get('wgUserLanguage') === 'qqx') {
                    return true;
                }
                return this.map.exists(this.key);
            }
        };
        mw.libs = {};
        mw.widgets = {};
        mw.inspect = function() {
            var args = arguments;
            mw.loader.using('mediawiki.inspect', function() {
                mw.inspect.runReports.apply(mw.inspect, args);
            });
        };
        mw.internalDoTransformFormatForQqx = function(formatString, parameters) {
            var parametersString;
            if (formatString.indexOf('$*') !== -1) {
                parametersString = '';
                if (parameters.length) {
                    parametersString = ': ' + parameters.map(function(_, i) {
                        return '$' + (i + 1);
                    }).join(', ');
                }
                return formatString.replace('$*', parametersString);
            }
            return formatString;
        };
        mw.format = function(formatString) {
            var parameters = slice.call(arguments, 1);
            formatString = mw.internalDoTransformFormatForQqx(formatString, parameters);
            return formatString.replace(/\$(\d+)/g, function(str, match) {
                var index = parseInt(match, 10) - 1;
                return parameters[index] !== undefined ? parameters[index] : '$' + match;
            });
        };
        mw.Message = Message;
        mw.message = function(key) {
            var parameters = slice.call(arguments, 1);
            return new Message(mw.messages, key, parameters);
        };
        mw.msg = function() {
            return mw.message.apply(mw.message, arguments).toString();
        };
        mw.track = function(topic, data) {
            mwLoaderTrack(topic, data);
            trackCallbacks.fire(mw.trackQueue);
        };
        mw.trackSubscribe = function(topic, callback) {
            var seen = 0;

            function handler(trackQueue) {
                var event;
                for (; seen <
                    trackQueue.length; seen++) {
                    event = trackQueue[seen];
                    if (event.topic.indexOf(topic) === 0) {
                        callback.call(event, event.topic, event.data);
                    }
                }
            }
            trackHandlers.push([handler, callback]);
            trackCallbacks.add(handler);
        };
        mw.trackUnsubscribe = function(callback) {
            trackHandlers = trackHandlers.filter(function(fns) {
                if (fns[1] === callback) {
                    trackCallbacks.remove(fns[0]);
                    return false;
                }
                return true;
            });
        };
        trackCallbacks.fire(mw.trackQueue);
        mw.hook = (function() {
            var lists = Object.create(null);
            return function(name) {
                var list = lists[name] || (lists[name] = $.Callbacks('memory'));
                return {
                    add: list.add,
                    remove: list.remove,
                    fire: function() {
                        return list.fireWith.call(this, null, slice.call(arguments));
                    }
                };
            };
        }());
        mw.html = (function() {
            function escapeCallback(s) {
                switch (s) {
                    case '\'':
                        return '&#039;';
                    case '"':
                        return '&quot;';
                    case '<':
                        return '&lt;';
                    case '>':
                        return '&gt;';
                    case '&':
                        return '&amp;';
                }
            }
            return {
                escape: function(s) {
                    return s.replace(/['"<>&]/g, escapeCallback);
                },
                element: function(name, attrs, contents) {
                    var v, attrName, s = '<' + name;
                    if (attrs) {
                        for (attrName in attrs) {
                            v
                                = attrs[attrName];
                            if (v === true) {
                                v = attrName;
                            } else if (v === false) {
                                continue;
                            }
                            s += ' ' + attrName + '="' + this.escape(String(v)) + '"';
                        }
                    }
                    if (contents === undefined || contents === null) {
                        s += '/>';
                        return s;
                    }
                    s += '>';
                    switch (typeof contents) {
                        case 'string':
                            s += this.escape(contents);
                            break;
                        case 'number':
                        case 'boolean':
                            s += String(contents);
                            break;
                        default:
                            if (contents instanceof this.Raw) {
                                s += contents.value;
                            } else if (contents instanceof this.Cdata) {
                                if (/<\/[a-zA-z]/.test(contents.value)) {
                                    throw new Error('Illegal end tag found in CDATA');
                                }
                                s += contents.value;
                            } else {
                                throw new Error('Invalid type of contents');
                            }
                    }
                    s += '</' + name + '>';
                    return s;
                },
                Raw: function(value) {
                    this.value = value;
                },
                Cdata: function(value) {
                    this.value = value;
                }
            };
        }());
        mw.loader.using = function(dependencies, ready, error) {
            var deferred = $.Deferred();
            if (!Array.isArray(dependencies)) {
                dependencies = [dependencies];
            }
            if (ready) {
                deferred.done(ready);
            }
            if (error) {
                deferred.fail(error);
            }
            try {
                dependencies = mw.loader.resolve(dependencies);
            } catch (e) {
                return deferred.reject(e).promise();
            }
            mw.loader.enqueue(
                dependencies,
                function() {
                    deferred.resolve(mw.loader.require);
                }, deferred.reject);
            return deferred.promise();
        };
        mw.loader.getScript = function(url) {
            return $.ajax(url, {
                dataType: 'script',
                cache: !0
            }).catch(function() {
                throw new Error('Failed to load script');
            });
        };
        mw.user = {
            options: new mw.Map(),
            tokens: new mw.Map()
        };
        mw.log.deprecate(window, '$j', $, 'Use $ or jQuery instead.');
        queue = window.RLQ;
        window.RLQ = {
            push: function(entry) {
                if (typeof entry === 'function') {
                    entry();
                } else {
                    mw.loader.using(entry[0], entry[1]);
                }
            }
        };
        while (queue[0]) {
            window.RLQ.push(queue.shift());
        }
    }());
    (function() {
        var msg, loadedScripts = {};

        function wikiUrlencode(page) {
            return encodeURIComponent(String(page)).replace(/'/g, '%27').replace(/%20/g, '_').replace(/%3B/g, ';').replace(/%40/g, '@').replace(/%24/g, '$').replace(/%21/g, '!').replace(/%2A/g, '*').replace(/%28/g, '(').replace(/%29/g, ')').replace(/%2C/g, ',').replace(/%2F/g, '/').replace(/%7E/g, '~').replace(/%3A/g, ':');
        }
        mw.log.deprecate(window, 'addOnloadHook', function(fn) {
                $(function() {
                    fn();
                });
            },
            'Use jQuery instead.');

        function importScriptURI(url) {
            var s;
            if (loadedScripts[url]) {
                return null;
            }
            loadedScripts[url] = !0;
            s = document.createElement('script');
            s.setAttribute('src', url);
            document.head.appendChild(s);
            return s;
        }

        function importScript(page) {
            var uri = mw.config.get('wgScript') + '?title=' + wikiUrlencode(page) + '&action=raw&ctype=text/javascript';
            return importScriptURI(uri);
        }

        function importStylesheetURI(url, media) {
            var l = document.createElement('link');
            l.rel = 'stylesheet';
            l.href = url;
            if (media) {
                l.media = media;
            }
            document.head.appendChild(l);
            return l;
        }

        function importStylesheet(page) {
            var uri = mw.config.get('wgScript') + '?title=' + wikiUrlencode(page) + '&action=raw&ctype=text/css';
            return importStylesheetURI(uri);
        }
        msg = 'Use mw.loader instead.';
        mw.log.deprecate(window, 'loadedScripts', loadedScripts, msg);
        mw.log.deprecate(window, 'importScriptURI', importScriptURI, msg);
        mw.log.deprecate(window, 'importStylesheetURI', importStylesheetURI, msg);
        window.importScript = importScript;
        window.importStylesheet = importStylesheet;
        ['write', 'writeln'].forEach(function(method) {
            mw.log.deprecate(document, method, function() {
                $('body').append($.parseHTML(Array.prototype.join.call(arguments, '')));
            }, 'Use jQuery or mw.loader.load instead.', 'document.' + method);
        });
    }());
});
mw.loader.implement("mediawiki.cldr@00tc5i3", function($, jQuery, require, module) {
    (function() {
        'use strict';
        mw.cldr = {
            getPluralForm: function(number, pluralRules) {
                var i, pluralRuleParser = require('mediawiki.libs.pluralruleparser');
                for (i = 0; i < pluralRules.length; i++) {
                    if (pluralRuleParser(pluralRules[i], number)) {
                        break;
                    }
                }
                return i;
            }
        };
    }());
});
mw.loader.implement("mediawiki.cookie@1565wnf", {
    "main": "index.js",
    "files": {
        "index.js": function(require, module) {
            'use strict';
            var config = require('./config.json'),
                defaults = {
                    prefix: config.prefix,
                    domain: config.domain,
                    path: config.path,
                    expires: config.expires,
                    secure: !1
                };
            mw.cookie = {
                set: function(key, value, options) {
                    var date;
                    if (arguments.length > 2 && (!options || options instanceof Date || typeof options === 'number')) {
                        options = {
                            expires: options
                        };
                    }
                    options = $.extend({}, defaults, options);
                    key = options.prefix + key;
                    delete options.prefix;
                    if (!options.expires) {
                        delete options.expires;
                    } else if (typeof options.expires === 'number') {
                        date = new Date();
                        date.setTime(Number(date) + (options.expires * 1000));
                        options.expires = date;
                    }
                    if (value !== null) {
                        value = String(value);
                    }
                    $.cookie(key, value, options);
                },
                get: function(key, prefix, defaultValue) {
                    var result;
                    if (prefix === undefined || prefix === null) {
                        prefix = defaults.prefix;
                    }
                    if (arguments.length < 3) {
                        defaultValue = null;
                    }
                    result = $.cookie(prefix + key);
                    return result !== null ? result : defaultValue;
                }
            };
            if (window.QUnit) {
                module.exports = {
                    setDefaults: function(value) {
                        var prev = defaults;
                        defaults = value;
                        return prev;
                    }
                };
            }
        },
        "config.json": {
            "prefix": "enwiki",
            "domain": "",
            "path": "/",
            "expires": 2592000
        }
    }
});
mw.loader.implement("mediawiki.experiments@17uc3m9", function($, jQuery, require, module) {
    (function() {
        var CONTROL_BUCKET = 'control',
            MAX_INT32_UNSIGNED = 4294967295;

        function hashString(string) {
            var hash = 0,
                i = string.length;
            while (i--) {
                hash += string.charCodeAt(i);
                hash += (hash << 10);
                hash ^= (hash >> 6);
            }
            hash += (hash << 3);
            hash ^= (hash >> 11);
            hash += (hash << 15);
            return hash >>> 0;
        }
        mw.experiments = {
            getBucket: function(experiment, token) {
                var buckets = experiment.buckets,
                    key, range = 0,
                    hash, max, acc = 0;
                if (!experiment.enabled || $.isEmptyObject(experiment.buckets)) {
                    return CONTROL_BUCKET;
                }
                for (key in buckets) {
                    range += buckets[key];
                }
                hash = hashString(experiment.name + ':' + token);
                max = (hash / MAX_INT32_UNSIGNED) * range;
                for (key in buckets) {
                    acc += buckets[key];
                    if (max <= acc) {
                        return key;
                    }
                }
            }
        };
    }());
});
mw.loader.implement("mediawiki.jqueryMsg@1y0xho0", {
    "main": "mediawiki.jqueryMsg.js",
    "files": {
        "mediawiki.jqueryMsg.js": function(require, module) {
            var oldParser, slice = Array.prototype.slice,
                parserDefaults = {
                    magic: {
                        PAGENAME: mw.config.get('wgPageName'),
                        PAGENAMEE: mw.util.wikiUrlencode(mw.config.get('wgPageName'))
                    },
                    allowedHtmlElements: [],
                    allowedHtmlCommonAttributes: ['id', 'class', 'style', 'lang', 'dir', 'title', 'role'],
                    allowedHtmlAttributesByElement: {},
                    messages: mw.messages,
                    language: mw.language,
                    format: 'parse'
                };
            $.extend(true, parserDefaults, require('./parserDefaults.json'));

            function appendWithoutParsing($parent, children) {
                var i, len;
                if (!Array.isArray(children)) {
                    children = [children];
                }
                for (i = 0, len = children.length; i < len; i++) {
                    if (typeof children[i] !== 'object') {
                        children[i] = document.createTextNode(children[i]);
                    }
                    if (children[i] instanceof $ && children[i].hasClass('mediaWiki_htmlEmitter')) {
                        children[i] = children[i].contents();
                    }
                }
                return $parent.append(children);
            }

            function decodePrimaryHtmlEntities(encoded) {
                return encoded.
                replace(/&#039;/g, '\'').replace(/&quot;/g, '"').replace(/&lt;/g, '<').replace(/&gt;/g, '>').replace(/&amp;/g, '&');
            }

            function textify(input) {
                if (input instanceof $) {
                    input = input.text();
                }
                return String(input);
            }

            function getFailableParserFn(options) {
                return function(args) {
                    var fallback, parser = new mw.jqueryMsg.Parser(options),
                        key = args[0],
                        argsArray = Array.isArray(args[1]) ? args[1] : slice.call(args, 1);
                    try {
                        return parser.parse(key, argsArray);
                    } catch (e) {
                        fallback = parser.settings.messages.get(key);
                        mw.log.warn('mediawiki.jqueryMsg: ' + key + ': ' + e.message);
                        mw.track('mediawiki.jqueryMsg.error', {
                            messageKey: key,
                            errorMessage: e.message
                        });
                        return $('<span>').text(fallback);
                    }
                };
            }
            mw.jqueryMsg = {};
            mw.jqueryMsg.setParserDefaults = function(data, deep) {
                if (deep) {
                    $.extend(true, parserDefaults, data);
                } else {
                    $.extend(parserDefaults, data);
                }
            };
            mw.jqueryMsg.getParserDefaults = function() {
                return $.extend({}, parserDefaults);
            };
            mw.jqueryMsg.getMessageFunction = function(options) {
                var failableParserFn, format;
                if (options && options.format !== undefined) {
                    format = options.format;
                } else {
                    format = parserDefaults.format;
                }
                return function() {
                    var failableResult;
                    if (!failableParserFn) {
                        failableParserFn = getFailableParserFn(options);
                    }
                    failableResult = failableParserFn(arguments);
                    if (format === 'text' || format === 'escaped') {
                        return failableResult.text();
                    } else {
                        return failableResult.html();
                    }
                };
            };
            mw.jqueryMsg.getPlugin = function(options) {
                var failableParserFn;
                return function() {
                    var $target;
                    if (!failableParserFn) {
                        failableParserFn = getFailableParserFn(options);
                    }
                    $target = this.empty();
                    appendWithoutParsing($target, failableParserFn(arguments));
                    return $target;
                };
            };
            mw.jqueryMsg.Parser = function(options) {
                this.settings = $.extend({}, parserDefaults, options);
                this.settings.onlyCurlyBraceTransform = (this.settings.format === 'text' || this.settings.format === 'escaped');
                this.astCache = {};
                this.emitter = new mw.jqueryMsg.HtmlEmitter(this.settings.language, this.settings.magic);
            };
            mw.jqueryMsg.parser = mw.jqueryMsg.Parser;
            mw.jqueryMsg.Parser.prototype = {
                parse: function(key, replacements) {
                    var ast = this.getAst(key,
                        replacements);
                    return this.emitter.emit(ast, replacements);
                },
                getAst: function(key, replacements) {
                    var wikiText;
                    if (!Object.prototype.hasOwnProperty.call(this.astCache, key)) {
                        wikiText = this.settings.messages.get(key);
                        if (mw.config.get('wgUserLanguage') === 'qqx' && (!wikiText || wikiText === '(' + key + ')')) {
                            wikiText = '(' + key + '$*)';
                        } else if (typeof wikiText !== 'string') {
                            wikiText = '⧼' + key + '⧽';
                        }
                        wikiText = mw.internalDoTransformFormatForQqx(wikiText, replacements);
                        this.astCache[key] = this.wikiTextToAst(wikiText);
                    }
                    return this.astCache[key];
                },
                wikiTextToAst: function(input) {
                    var pos, regularLiteral, regularLiteralWithoutBar, regularLiteralWithoutSpace, regularLiteralWithSquareBrackets, doubleQuote, singleQuote, backslash, anyCharacter, asciiAlphabetLiteral, escapedOrLiteralWithoutSpace, escapedOrLiteralWithoutBar, escapedOrRegularLiteral, whitespace, dollar, digits, htmlDoubleQuoteAttributeValue, htmlSingleQuoteAttributeValue, htmlAttributeEquals, openHtmlStartTag, optionalForwardSlash, openHtmlEndTag, closeHtmlTag, openExtlink, closeExtlink, wikilinkContents, openWikilink, closeWikilink, templateName, pipe, colon, templateContents, openTemplate, closeTemplate, nonWhitespaceExpression, paramExpression, expression, curlyBraceTransformExpression, result, settings = this.settings,
                        concat = Array.prototype.concat;
                    pos = 0;

                    function choice(ps) {
                        return function() {
                            var i, result;
                            for (i = 0; i < ps.length; i++) {
                                result = ps[i]();
                                if (result !== null) {
                                    return result;
                                }
                            }
                            return null;
                        };
                    }

                    function sequence(ps) {
                        var i, res, originalPos = pos,
                            result = [];
                        for (i = 0; i < ps.length; i++) {
                            res = ps[i]();
                            if (res === null) {
                                pos = originalPos;
                                return null;
                            }
                            result.push(res);
                        }
                        return result;
                    }

                    function nOrMore(n, p) {
                        return function() {
                            var originalPos = pos,
                                result = [],
                                parsed = p();
                            while (parsed !== null) {
                                result.push(parsed);
                                parsed = p();
                            }
                            if (result.length < n) {
                                pos = originalPos;
                                return null;
                            }
                            return result;
                        };
                    }

                    function transform(p, fn) {
                        return function() {
                            var result = p();
                            return result === null ? null : fn(result);
                        };
                    }

                    function makeStringParser(s) {
                        var len = s.length;
                        return function() {
                            var result = null;
                            if (input.substr(pos, len) === s) {
                                result = s;
                                pos += len;
                            }
                            return result;
                        };
                    }

                    function makeRegexParser(regex) {
                        return function() {
                            var matches = input.slice(pos).match(regex);
                            if (matches === null) {
                                return null;
                            }
                            pos += matches[0].length;
                            return matches[0];
                        };
                    }
                    regularLiteral = makeRegexParser(/^[^{}\[\]$<\\]/);
                    regularLiteralWithoutBar = makeRegexParser(/^[^{}\[\]$\\|]/);
                    regularLiteralWithoutSpace = makeRegexParser(/^[^{}\[\]$\s]/);
                    regularLiteralWithSquareBrackets = makeRegexParser(/^[^{}$\\]/);
                    backslash = makeStringParser('\\');
                    doubleQuote = makeStringParser('"');
                    singleQuote = makeStringParser('\'');
                    anyCharacter = makeRegexParser(/^./);
                    openHtmlStartTag = makeStringParser('<');
                    optionalForwardSlash = makeRegexParser(/^\/?/);
                    openHtmlEndTag = makeStringParser('</');
                    htmlAttributeEquals = makeRegexParser(/^\s*=\s*/);
                    closeHtmlTag = makeRegexParser(/^\s*>/);

                    function escapedLiteral() {
                        var result = sequence([backslash, anyCharacter]);
                        return result === null ? null : result[1];
                    }
                    escapedOrLiteralWithoutSpace = choice([escapedLiteral, regularLiteralWithoutSpace]);
                    escapedOrLiteralWithoutBar = choice([escapedLiteral,
                        regularLiteralWithoutBar
                    ]);
                    escapedOrRegularLiteral = choice([escapedLiteral, regularLiteral]);

                    function literalWithoutSpace() {
                        var result = nOrMore(1, escapedOrLiteralWithoutSpace)();
                        return result === null ? null : result.join('');
                    }

                    function literalWithoutBar() {
                        var result = nOrMore(1, escapedOrLiteralWithoutBar)();
                        return result === null ? null : result.join('');
                    }

                    function literal() {
                        var result = nOrMore(1, escapedOrRegularLiteral)();
                        return result === null ? null : result.join('');
                    }

                    function curlyBraceTransformExpressionLiteral() {
                        var result = nOrMore(1, regularLiteralWithSquareBrackets)();
                        return result === null ? null : result.join('');
                    }
                    asciiAlphabetLiteral = makeRegexParser(/^[A-Za-z]+/);
                    htmlDoubleQuoteAttributeValue = makeRegexParser(/^[^"]*/);
                    htmlSingleQuoteAttributeValue = makeRegexParser(/^[^']*/);
                    whitespace = makeRegexParser(/^\s+/);
                    dollar = makeStringParser('$');
                    digits = makeRegexParser(/^\d+/);

                    function replacement() {
                        var result = sequence([dollar, digits]);
                        if (result === null) {
                            return null;
                        }
                        return ['REPLACE', parseInt(result[1], 10) - 1];
                    }
                    openExtlink = makeStringParser('[');
                    closeExtlink = makeStringParser(']');

                    function extlink() {
                        var result, parsedResult, target;
                        result = null;
                        parsedResult = sequence([openExtlink, nOrMore(1, nonWhitespaceExpression), whitespace, nOrMore(1, expression), closeExtlink]);
                        if (parsedResult !== null) {
                            target = parsedResult[1].length === 1 ? parsedResult[1][0] : ['CONCAT'].concat(parsedResult[1]);
                            result = ['EXTLINK', target, ['CONCAT'].concat(parsedResult[3])];
                        }
                        return result;
                    }
                    openWikilink = makeStringParser('[[');
                    closeWikilink = makeStringParser(']]');
                    pipe = makeStringParser('|');

                    function template() {
                        var result = sequence([openTemplate, templateContents, closeTemplate]);
                        return result === null ? null : result[1];
                    }

                    function pipedWikilink() {
                        var result = sequence([nOrMore(1, paramExpression), pipe, nOrMore(1, expression)]);
                        return result === null ? null : [
                            ['CONCAT'].concat(result[0]), ['CONCAT'].concat(result[2])
                        ];
                    }

                    function unpipedWikilink() {
                        var result = sequence([nOrMore(1, paramExpression)]);
                        return result === null ? null : [
                            ['CONCAT'].concat(result[0])
                        ];
                    }
                    wikilinkContents = choice([pipedWikilink, unpipedWikilink]);

                    function wikilink() {
                        var result, parsedResult, parsedLinkContents;
                        result = null;
                        parsedResult = sequence([openWikilink, wikilinkContents, closeWikilink]);
                        if (parsedResult !== null) {
                            parsedLinkContents = parsedResult[1];
                            result = ['WIKILINK'].concat(parsedLinkContents);
                        }
                        return result;
                    }

                    function doubleQuotedHtmlAttributeValue() {
                        var parsedResult = sequence([doubleQuote, htmlDoubleQuoteAttributeValue, doubleQuote]);
                        return parsedResult === null ? null : parsedResult[1];
                    }

                    function singleQuotedHtmlAttributeValue() {
                        var parsedResult = sequence([singleQuote, htmlSingleQuoteAttributeValue, singleQuote]);
                        return parsedResult === null ? null : parsedResult[1];
                    }

                    function htmlAttribute() {
                        var parsedResult = sequence([whitespace, asciiAlphabetLiteral, htmlAttributeEquals, choice([doubleQuotedHtmlAttributeValue, singleQuotedHtmlAttributeValue])]);
                        return parsedResult === null ? null : [parsedResult[1], parsedResult[3]];
                    }

                    function isAllowedHtml(startTagName, endTagName, attributes) {
                        var i, len, attributeName;
                        startTagName = startTagName.toLowerCase();
                        endTagName = endTagName.toLowerCase();
                        if (startTagName !== endTagName || settings.allowedHtmlElements.indexOf(startTagName) === -1) {
                            return false;
                        }
                        for (i = 0, len = attributes.length; i < len; i += 2) {
                            attributeName = attributes[i];
                            if (settings.allowedHtmlCommonAttributes.indexOf(attributeName) === -1 && (settings.allowedHtmlAttributesByElement[startTagName] || []).indexOf(attributeName) === -1) {
                                return false;
                            }
                        }
                        return true;
                    }

                    function htmlAttributes() {
                        var parsedResult = nOrMore(0, htmlAttribute)();
                        return concat.apply(['HTMLATTRIBUTES'], parsedResult);
                    }

                    function html() {
                        var parsedOpenTagResult, parsedHtmlContents, parsedCloseTagResult, wrappedAttributes, attributes, startTagName, endTagName, startOpenTagPos, startCloseTagPos, endOpenTagPos, endCloseTagPos, result = null;
                        startOpenTagPos = pos;
                        parsedOpenTagResult = sequence([openHtmlStartTag, asciiAlphabetLiteral, htmlAttributes, optionalForwardSlash, closeHtmlTag]);
                        if (parsedOpenTagResult === null) {
                            return null;
                        }
                        endOpenTagPos = pos;
                        startTagName = parsedOpenTagResult[1];
                        parsedHtmlContents = nOrMore(0, expression)();
                        startCloseTagPos = pos;
                        parsedCloseTagResult = sequence([openHtmlEndTag, asciiAlphabetLiteral, closeHtmlTag]);
                        if (parsedCloseTagResult === null) {
                            return ['CONCAT', input.slice(startOpenTagPos, endOpenTagPos)].concat(parsedHtmlContents);
                        }
                        endCloseTagPos = pos;
                        endTagName = parsedCloseTagResult[1];
                        wrappedAttributes = parsedOpenTagResult[2];
                        attributes = wrappedAttributes.slice(1);
                        if (isAllowedHtml(startTagName, endTagName, attributes)) {
                            result = ['HTMLELEMENT', startTagName, wrappedAttributes].concat(parsedHtmlContents);
                        } else {
                            result = ['CONCAT', input.slice(startOpenTagPos, endOpenTagPos)].concat(parsedHtmlContents, input.slice(startCloseTagPos, endCloseTagPos));
                        }
                        return result;
                    }

                    function nowiki() {
                        var parsedResult, plainText, result = null;
                        parsedResult = sequence([makeStringParser('<nowiki>'), makeRegexParser(/^.*?(?=<\/nowiki>)/), makeStringParser('</nowiki>')]);
                        if (parsedResult !== null) {
                            plainText = parsedResult[1];
                            result = ['CONCAT'].concat(plainText);
                        }
                        return result;
                    }
                    templateName = transform(
                        makeRegexParser(/^[ !"$&'()*,./0-9;=?@A-Z^_`a-z~\x80-\xFF+-]+/),
                        function(result) {
                            return result.toString();
                        });

                    function templateParam() {
                        var expr, result;
                        result = sequence([pipe, nOrMore(0, paramExpression)]);
                        if (result === null) {
                            return null;
                        }
                        expr = result[1];
                        return expr.length > 1 ? ['CONCAT'].concat(expr) : expr[0];
                    }

                    function templateWithReplacement() {
                        var result = sequence([templateName, colon, replacement]);
                        return result === null ? null : [result[0], result[2]];
                    }

                    function templateWithOutReplacement() {
                        var result = sequence([templateName, colon, paramExpression]);
                        return result === null ? null : [result[0], result[2]];
                    }

                    function templateWithOutFirstParameter() {
                        var result = sequence([templateName, colon]);
                        return result === null ? null : [result[0], ''];
                    }
                    colon = makeStringParser(':');
                    templateContents = choice([function() {
                        var res = sequence([choice([templateWithReplacement, templateWithOutReplacement, templateWithOutFirstParameter]), nOrMore(0, templateParam)]);
                        return res === null ? null : res[0].concat(res[1]);
                    }, function() {
                        var res = sequence([templateName,
                            nOrMore(0, templateParam)
                        ]);
                        if (res === null) {
                            return null;
                        }
                        return [res[0]].concat(res[1]);
                    }]);
                    openTemplate = makeStringParser('{{');
                    closeTemplate = makeStringParser('}}');
                    nonWhitespaceExpression = choice([template, wikilink, extlink, replacement, literalWithoutSpace]);
                    paramExpression = choice([template, wikilink, extlink, replacement, literalWithoutBar]);
                    expression = choice([template, wikilink, extlink, replacement, nowiki, html, literal]);
                    curlyBraceTransformExpression = choice([template, replacement, curlyBraceTransformExpressionLiteral]);

                    function start(rootExpression) {
                        var result = nOrMore(0, rootExpression)();
                        if (result === null) {
                            return null;
                        }
                        return ['CONCAT'].concat(result);
                    }
                    result = start(this.settings.onlyCurlyBraceTransform ? curlyBraceTransformExpression : expression);
                    if (result === null || pos !== input.length) {
                        throw new Error('Parse error at position ' + pos.toString() + ' in input: ' + input);
                    }
                    return result;
                }
            };
            mw.jqueryMsg.HtmlEmitter = function(language, magic) {
                var jmsg = this;
                this.language = language;
                $.each(magic, function(key, val) {
                    jmsg[
                        key.toLowerCase()] = function() {
                        return val;
                    };
                });
                this.emit = function(node, replacements) {
                    var ret, subnodes, operation, jmsg = this;
                    switch (typeof node) {
                        case 'string':
                        case 'number':
                            ret = node;
                            break;
                        case 'object':
                            subnodes = $.map(node.slice(1), function(n) {
                                return jmsg.emit(n, replacements);
                            });
                            operation = node[0].toLowerCase();
                            if (typeof jmsg[operation] === 'function') {
                                ret = jmsg[operation](subnodes, replacements);
                            } else {
                                throw new Error('Unknown operation "' + operation + '"');
                            }
                            break;
                        case 'undefined':
                            ret = '';
                            break;
                        default:
                            throw new Error('Unexpected type in AST: ' + typeof node);
                    }
                    return ret;
                };
            };
            mw.jqueryMsg.HtmlEmitter.prototype = {
                concat: function(nodes) {
                    var $span = $('<span>').addClass('mediaWiki_htmlEmitter');
                    $.each(nodes, function(i, node) {
                        appendWithoutParsing($span, node);
                    });
                    return $span;
                },
                replace: function(nodes, replacements) {
                    var index = parseInt(nodes[0], 10);
                    if (index < replacements.length) {
                        return replacements[index];
                    } else {
                        return '$' + (index + 1);
                    }
                },
                wikilink: function(nodes) {
                    var page, anchor, url, $el;
                    page = textify(nodes[0]);
                    if (page.charAt(
                            0) === ':') {
                        page = page.slice(1);
                    }
                    url = mw.util.getUrl(page);
                    if (nodes.length === 1) {
                        anchor = page;
                    } else {
                        anchor = nodes[1];
                    }
                    $el = $('<a>').attr({
                        title: page,
                        href: url
                    });
                    return appendWithoutParsing($el, anchor);
                },
                htmlattributes: function(nodes) {
                    var i, len, mapping = {};
                    for (i = 0, len = nodes.length; i < len; i += 2) {
                        mapping[nodes[i]] = decodePrimaryHtmlEntities(nodes[i + 1]);
                    }
                    return mapping;
                },
                htmlelement: function(nodes) {
                    var tagName, attributes, contents, $element;
                    tagName = nodes.shift();
                    attributes = nodes.shift();
                    contents = nodes;
                    $element = $(document.createElement(tagName)).attr(attributes);
                    return appendWithoutParsing($element, contents);
                },
                extlink: function(nodes) {
                    var $el, arg = nodes[0],
                        contents = nodes[1];
                    if (arg instanceof $ && !arg.hasClass('mediaWiki_htmlEmitter')) {
                        $el = arg;
                    } else {
                        $el = $('<a>');
                        if (typeof arg === 'function') {
                            $el.attr({
                                role: 'button',
                                tabindex: 0
                            }).on('click keypress', function(e) {
                                if (e.type === 'click' || e.type === 'keypress' && e.which === 13) {
                                    arg.call(this, e);
                                }
                            });
                        } else {
                            $el.attr('href', textify(arg));
                        }
                    }
                    return appendWithoutParsing($el.empty(), contents);
                },
                plural: function(nodes) {
                    var forms, firstChild, firstChildText, explicitPluralFormNumber, formIndex, form, count, explicitPluralForms = {};
                    count = parseFloat(this.language.convertNumber(textify(nodes[0]), true));
                    forms = nodes.slice(1);
                    for (formIndex = 0; formIndex < forms.length; formIndex++) {
                        form = forms[formIndex];
                        if (form instanceof $ && form.hasClass('mediaWiki_htmlEmitter')) {
                            firstChild = form.contents().get(0);
                            if (firstChild && firstChild.nodeType === Node.TEXT_NODE) {
                                firstChildText = firstChild.textContent;
                                if (/^\d+=/.test(firstChildText)) {
                                    explicitPluralFormNumber = parseInt(firstChildText.split(/=/)[0], 10);
                                    firstChild.textContent = firstChildText.slice(firstChildText.indexOf('=') + 1);
                                    explicitPluralForms[explicitPluralFormNumber] = form;
                                    forms[formIndex] = undefined;
                                }
                            }
                        } else if (/^\d+=/.test(form)) {
                            explicitPluralFormNumber = parseInt(form.split(/=/)[0], 10);
                            explicitPluralForms[explicitPluralFormNumber] = form.slice(form.indexOf('=') + 1);
                            forms[formIndex] = undefined;
                        }
                    }
                    forms = $.map(forms, function(form) {
                        return form;
                    });
                    return this
                        .language.convertPlural(count, forms, explicitPluralForms);
                },
                gender: function(nodes) {
                    var gender, maybeUser = nodes[0],
                        forms = nodes.slice(1);
                    if (maybeUser === '') {
                        maybeUser = mw.user;
                    }
                    if (maybeUser && maybeUser.options instanceof mw.Map) {
                        gender = maybeUser.options.get('gender');
                    } else {
                        gender = textify(maybeUser);
                    }
                    return this.language.gender(gender, forms);
                },
                grammar: function(nodes) {
                    var form = nodes[0],
                        word = nodes[1];
                    if (typeof form === 'string' && typeof word === 'string') {
                        return this.language.convertGrammar(word, form);
                    }
                    return word;
                },
                int: function(nodes) {
                    var msg = textify(nodes[0]);
                    return mw.jqueryMsg.getMessageFunction()(msg.charAt(0).toLowerCase() + msg.slice(1));
                },
                ns: function(nodes) {
                    var ns = textify(nodes[0]).trim();
                    if (!/^\d+$/.test(ns)) {
                        ns = mw.config.get('wgNamespaceIds')[ns.replace(/ /g, '_').toLowerCase()];
                    }
                    ns = mw.config.get('wgFormattedNamespaces')[ns];
                    return ns || '';
                },
                formatnum: function(nodes) {
                    var isInteger = !!nodes[1] && nodes[1] === 'R',
                        number = nodes[0];
                    if (typeof number === 'string' || typeof number === 'number') {
                        return this.
                        language.convertNumber(number, isInteger);
                    }
                    return number;
                },
                lc: function(nodes) {
                    return textify(nodes[0]).toLowerCase();
                },
                uc: function(nodes) {
                    return textify(nodes[0]).toUpperCase();
                },
                lcfirst: function(nodes) {
                    var text = textify(nodes[0]);
                    return text.charAt(0).toLowerCase() + text.slice(1);
                },
                ucfirst: function(nodes) {
                    var text = textify(nodes[0]);
                    return text.charAt(0).toUpperCase() + text.slice(1);
                }
            };
            $.fn.msg = mw.jqueryMsg.getPlugin();
            oldParser = mw.Message.prototype.parser;
            mw.Message.prototype.parser = function() {
                if (this.format === 'plain' || (!/\{\{|[<>[&]/.test(this.map.get(this.key)) && !this.parameters.some(function(param) {
                        return param instanceof $ || (param && param.nodeType !== undefined);
                    }))) {
                    return oldParser.apply(this);
                }
                if (!Object.prototype.hasOwnProperty.call(this.map, this.format)) {
                    this.map[this.format] = mw.jqueryMsg.getMessageFunction({
                        messages: this.map,
                        format: this.format
                    });
                }
                return this.map[this.format](this.key, this.parameters);
            };
            mw.Message.prototype.parseDom = (function() {
                var $wrapper = $('<div>');
                return function() {
                    return $wrapper.msg(this.key, this.parameters).contents().detach();
                };
            }());
        },
        "parserDefaults.json": {
            "allowedHtmlElements": ["b", "bdi", "del", "i", "ins", "u", "font", "big", "small", "sub", "sup", "h1", "h2", "h3", "h4", "h5", "h6", "cite", "code", "em", "s", "strike", "strong", "tt", "var", "div", "center", "blockquote", "ol", "ul", "dl", "table", "caption", "pre", "ruby", "rb", "rp", "rt", "rtc", "p", "span", "abbr", "dfn", "kbd", "samp", "data", "time", "mark", "li", "dt", "dd"],
            "magic": {
                "SITENAME": "Wikipedia",
                "WBREPONAME": "Wikidata"
            }
        }
    }
});
mw.loader.implement("mediawiki.language@0aytd5t", function($, jQuery, require, module) {
    (function() {
        mw.language = {
            data: {},
            getData: function(langCode, dataKey) {
                var langData = mw.language.data;
                langCode = langCode.toLowerCase();
                if (langData && langData[langCode] instanceof mw.Map) {
                    return langData[langCode].get(dataKey);
                }
                return undefined;
            },
            setData: function(langCode, dataKey, value) {
                var langData = mw.language.data;
                langCode = langCode.toLowerCase();
                if (!(langData[langCode] instanceof mw.Map)) {
                    langData[langCode] = new mw.Map();
                }
                if (arguments.length > 2) {
                    langData[langCode].set(dataKey, value);
                } else {
                    langData[langCode].set(dataKey);
                }
            }
        };
    }());
    (function() {
        $.extend(mw.language, {
            convertPlural: function(count, forms, explicitPluralForms) {
                var pluralRules, pluralFormIndex = 0;
                if (explicitPluralForms && (explicitPluralForms[count] !== undefined)) {
                    return explicitPluralForms[count];
                }
                if (!forms || forms.length === 0) {
                    return '';
                }
                pluralRules = mw.language.getData(mw.config.get('wgUserLanguage'), 'pluralRules');
                if (!pluralRules) {
                    return (count === 1) ? forms[0] :
                        forms[1];
                }
                pluralFormIndex = mw.cldr.getPluralForm(count, pluralRules);
                pluralFormIndex = Math.min(pluralFormIndex, forms.length - 1);
                return forms[pluralFormIndex];
            },
            preConvertPlural: function(forms, count) {
                while (forms.length < count) {
                    forms.push(forms[forms.length - 1]);
                }
                return forms;
            },
            gender: function(gender, forms) {
                if (!forms || forms.length === 0) {
                    return '';
                }
                forms = mw.language.preConvertPlural(forms, 2);
                if (gender === 'male') {
                    return forms[0];
                }
                if (gender === 'female') {
                    return forms[1];
                }
                return (forms.length === 3) ? forms[2] : forms[0];
            },
            convertGrammar: function(word, form) {
                var userLanguage, forms, transformations, patterns, i, rule, sourcePattern, regexp, replacement;
                userLanguage = mw.config.get('wgUserLanguage');
                forms = mw.language.getData(userLanguage, 'grammarForms');
                if (forms && forms[form]) {
                    return forms[form][word];
                }
                transformations = mw.language.getData(userLanguage, 'grammarTransformations');
                if (!(transformations && transformations[form])) {
                    return word;
                }
                patterns = transformations[form];
                if (typeof patterns === 'string') {
                    patterns = transformations[
                        patterns];
                }
                for (i = 0; i < patterns.length; i++) {
                    rule = patterns[i];
                    sourcePattern = rule[0];
                    if (sourcePattern === '@metadata') {
                        continue;
                    }
                    regexp = new RegExp(sourcePattern);
                    replacement = rule[1];
                    if (word.match(regexp)) {
                        return word.replace(regexp, replacement);
                    }
                }
                return word;
            },
            listToText: function(list) {
                var text = '',
                    i = 0;
                for (; i < list.length; i++) {
                    text += list[i];
                    if (list.length - 2 === i) {
                        text += mw.msg('and') + mw.msg('word-separator');
                    } else if (list.length - 1 !== i) {
                        text += mw.msg('comma-separator');
                    }
                }
                return text;
            },
            bcp47: function(languageTag) {
                var bcp47Map, formatted, segments, isFirstSegment = !0,
                    isPrivate = !1;
                languageTag = languageTag.toLowerCase();
                bcp47Map = mw.language.getData(mw.config.get('wgUserLanguage'), 'bcp47Map');
                if (bcp47Map && Object.prototype.hasOwnProperty.call(bcp47Map, languageTag)) {
                    languageTag = bcp47Map[languageTag];
                }
                segments = languageTag.split('-');
                formatted = segments.map(function(segment) {
                    var newSegment;
                    if (isPrivate) {
                        newSegment = segment.toLowerCase();
                    } else if (segment.length === 2 && !isFirstSegment) {
                        newSegment = segment.
                        toUpperCase();
                    } else if (segment.length === 4 && !isFirstSegment) {
                        newSegment = segment.charAt(0).toUpperCase() + segment.substring(1).toLowerCase();
                    } else {
                        newSegment = segment.toLowerCase();
                    }
                    isPrivate = segment.toLowerCase() === 'x';
                    isFirstSegment = !1;
                    return newSegment;
                });
                return formatted.join('-');
            }
        });
    }());
    (function() {
        function replicate(str, num) {
            var buf = [];
            if (num <= 0 || !str) {
                return '';
            }
            while (num--) {
                buf.push(str);
            }
            return buf.join('');
        }

        function pad(text, size, ch, end) {
            var out, padStr;
            if (!ch) {
                ch = '0';
            }
            out = String(text);
            padStr = replicate(ch, Math.ceil((size - out.length) / ch.length));
            return end ? out + padStr : padStr + out;
        }

        function commafyNumber(value, pattern, options) {
            var padLength, patternDigits, index, whole, off, remainder, patternParts = pattern.split('.'),
                maxPlaces = (patternParts[1] || []).length,
                valueParts = String(Math.abs(value)).split('.'),
                fractional = valueParts[1] || '',
                groupSize = 0,
                groupSize2 = 0,
                pieces = [];
            options = options || {
                group: ',',
                decimal: '.'
            };
            if (isNaN(value)) {
                return value;
            }
            if (patternParts[1]) {
                padLength = (patternParts[1] &&
                    patternParts[1].lastIndexOf('0') + 1);
                if (padLength > fractional.length) {
                    valueParts[1] = pad(fractional, padLength, '0', true);
                }
                if (maxPlaces < fractional.length) {
                    valueParts[1] = fractional.slice(0, maxPlaces);
                }
            } else {
                if (valueParts[1]) {
                    valueParts.pop();
                }
            }
            patternDigits = patternParts[0].replace(',', '');
            padLength = patternDigits.indexOf('0');
            if (padLength !== -1) {
                padLength = patternDigits.length - padLength;
                if (padLength > valueParts[0].length) {
                    valueParts[0] = pad(valueParts[0], padLength);
                }
                if (patternDigits.indexOf('#') === -1) {
                    valueParts[0] = valueParts[0].slice(valueParts[0].length - padLength);
                }
            }
            index = patternParts[0].lastIndexOf(',');
            if (index !== -1) {
                groupSize = patternParts[0].length - index - 1;
                remainder = patternParts[0].slice(0, index);
                index = remainder.lastIndexOf(',');
                if (index !== -1) {
                    groupSize2 = remainder.length - index - 1;
                }
            }
            if (options.minimumGroupingDigits === null || valueParts[0].length >= groupSize + options.minimumGroupingDigits) {
                for (whole = valueParts[0]; whole;) {
                    off = groupSize ? whole.length - groupSize : 0;
                    pieces.push((off > 0) ? whole.slice(off) :
                        whole);
                    whole = (off > 0) ? whole.slice(0, off) : '';
                    if (groupSize2) {
                        groupSize = groupSize2;
                        groupSize2 = null;
                    }
                }
                valueParts[0] = pieces.reverse().join(options.group);
            }
            return valueParts.join(options.decimal);
        }

        function flipTransform() {
            var i, key, table, flipped = {};
            flipped[','] = '';
            for (i = 0; i < arguments.length; i++) {
                table = arguments[i];
                for (key in table) {
                    flipped[table[key]] = key === ',' ? '' : key;
                }
            }
            return flipped;
        }
        $.extend(mw.language, {
            convertNumber: function(num, integer) {
                var transformTable, digitTransformTable, separatorTransformTable, i, numberString, convertedNumber, pattern, minimumGroupingDigits;
                if (integer && parseInt(num, 10) === num) {
                    return num;
                }
                digitTransformTable = mw.language.getDigitTransformTable();
                separatorTransformTable = mw.language.getSeparatorTransformTable();
                if (integer) {
                    transformTable = flipTransform(separatorTransformTable, digitTransformTable);
                    numberString = String(num);
                } else {
                    if (mw.config.get('wgTranslateNumerals')) {
                        transformTable = digitTransformTable;
                    }
                    pattern = mw.language.getData(mw.config.get('wgUserLanguage'),
                        'digitGroupingPattern') || '#,##0.###';
                    minimumGroupingDigits = mw.language.getData(mw.config.get('wgUserLanguage'), 'minimumGroupingDigits') || null;
                    numberString = mw.language.commafy(num, pattern, minimumGroupingDigits);
                }
                if (transformTable) {
                    convertedNumber = '';
                    for (i = 0; i < numberString.length; i++) {
                        if (Object.prototype.hasOwnProperty.call(transformTable, numberString[i])) {
                            convertedNumber += transformTable[numberString[i]];
                        } else {
                            convertedNumber += numberString[i];
                        }
                    }
                } else {
                    convertedNumber = numberString;
                }
                if (integer) {
                    convertedNumber = parseInt(convertedNumber, 10);
                }
                return convertedNumber;
            },
            getDigitTransformTable: function() {
                return mw.language.getData(mw.config.get('wgUserLanguage'), 'digitTransformTable') || [];
            },
            getSeparatorTransformTable: function() {
                return mw.language.getData(mw.config.get('wgUserLanguage'), 'separatorTransformTable') || [];
            },
            commafy: function(value, pattern, minimumGroupingDigits) {
                var numberPattern, transformTable = mw.language.getSeparatorTransformTable(),
                    group = transformTable[','] || ',',
                    numberPatternRE =
                    /[#0,]*[#0](?:\.0*#*)?/,
                    decimal = transformTable['.'] || '.',
                    patternList = pattern.split(';'),
                    positivePattern = patternList[0];
                pattern = patternList[(value < 0) ? 1 : 0] || ('-' + positivePattern);
                numberPattern = positivePattern.match(numberPatternRE);
                minimumGroupingDigits = minimumGroupingDigits !== undefined ? minimumGroupingDigits : null;
                if (!numberPattern) {
                    throw new Error('unable to find a number expression in pattern: ' + pattern);
                }
                return pattern.replace(numberPatternRE, commafyNumber(value, numberPattern[0], {
                    minimumGroupingDigits: minimumGroupingDigits,
                    decimal: decimal,
                    group: group
                }));
            }
        });
    }());
    (function() {
        $.extend(mw.language, {
            getFallbackLanguages: function() {
                return mw.language.getData(mw.config.get('wgUserLanguage'), 'fallbackLanguages') || [];
            },
            getFallbackLanguageChain: function() {
                return [mw.config.get('wgUserLanguage')].concat(mw.language.getFallbackLanguages());
            }
        });
    }());
    mw.language.setData("en", {
        "digitTransformTable": null,
        "separatorTransformTable": null,
        "minimumGroupingDigits": null,
        "grammarForms": [],
        "grammarTransformations": [],
        "pluralRules": ["i = 1 and v = 0 @integer 1"],
        "digitGroupingPattern": null,
        "fallbackLanguages": [],
        "bcp47Map": {
            "als": "gsw",
            "bat-smg": "sgs",
            "be-x-old": "be-tarask",
            "fiu-vro": "vro",
            "roa-rup": "rup",
            "zh-classical": "lzh",
            "zh-min-nan": "nan",
            "zh-yue": "yue",
            "cbk-zam": "cbk",
            "de-formal": "de-x-formal",
            "eml": "egl",
            "en-rtl": "en-x-rtl",
            "es-formal": "es-x-formal",
            "hu-formal": "hu-x-formal",
            "map-bms": "jv-x-bms",
            "mo": "ro-Cyrl-MD",
            "nrm": "nrf",
            "nl-informal": "nl-x-informal",
            "roa-tara": "nap-x-tara",
            "simple": "en-simple",
            "sr-ec": "sr-Cyrl",
            "sr-el": "sr-Latn",
            "zh-cn": "zh-Hans-CN",
            "zh-sg": "zh-Hans-SG",
            "zh-my": "zh-Hans-MY",
            "zh-tw": "zh-Hant-TW",
            "zh-hk": "zh-Hant-HK",
            "zh-mo": "zh-Hant-MO"
        }
    });
}, {}, {
    "and": " and",
    "comma-separator": ", ",
    "word-separator": " "
});
mw.loader.implement("mediawiki.notify@1w9s9af", function($, jQuery, require, module) {
    (function() {
        'use strict';
        mw.notify = function(message, options) {
            return mw.loader.using('mediawiki.notification').then(function() {
                return mw.notification.notify(message, options);
            });
        };
    }());
});
mw.loader.implement("mediawiki.router@10tacll", function($, jQuery, require, module) {
    (function() {
        var Router = require('oojs-router');
        module.exports = new Router();
    }());
});
mw.loader.implement("mediawiki.storage@1r040mh", function($, jQuery, require, module) {
    (function() {
        'use strict';
        var localStorage = (function() {
                try {
                    return window.localStorage;
                } catch (e) {}
            }()),
            sessionStorage = (function() {
                try {
                    return window.sessionStorage;
                } catch (e) {}
            }());

        function SafeStorage(store) {
            this.store = store;
        }
        SafeStorage.prototype.get = function(key) {
            try {
                return this.store.getItem(key);
            } catch (e) {}
            return false;
        };
        SafeStorage.prototype.set = function(key, value) {
            try {
                this.store.setItem(key, value);
                return true;
            } catch (e) {}
            return false;
        };
        SafeStorage.prototype.remove = function(key) {
            try {
                this.store.removeItem(key);
                return true;
            } catch (e) {}
            return false;
        };
        SafeStorage.prototype.getObject = function(key) {
            var json = this.get(key);
            if (json === false) {
                return false;
            }
            try {
                return JSON.parse(json);
            } catch (e) {}
            return null;
        };
        SafeStorage.prototype.setObject = function(key, value) {
            var json;
            try {
                json = JSON.stringify(value);
                return this.set(key, json);
            } catch (e) {}
            return false;
        };
        mw.storage = new SafeStorage(localStorage);
        mw.storage.session = new
        SafeStorage(sessionStorage);
    }());
});
mw.loader.implement("mediawiki.template@1oeb3iv", function($, jQuery, require, module) {
    (function() {
        var compiledTemplates = {},
            compilers = {};
        mw.template = {
            registerCompiler: function(name, compiler) {
                if (!compiler.compile) {
                    throw new Error('Compiler must implement a compile method');
                }
                compilers[name] = compiler;
            },
            getCompilerName: function(templateName) {
                var nameParts = templateName.split('.');
                if (nameParts.length < 2) {
                    throw new Error('Template name must have a suffix');
                }
                return nameParts[nameParts.length - 1];
            },
            getCompiler: function(name) {
                var compiler = compilers[name];
                if (!compiler) {
                    throw new Error('Unknown compiler ' + name);
                }
                return compiler;
            },
            add: function(moduleName, templateName, templateBody) {
                var compiled = this.compile(templateBody, this.getCompilerName(templateName));
                if (!compiledTemplates[moduleName]) {
                    compiledTemplates[moduleName] = {};
                }
                compiledTemplates[moduleName][templateName] = compiled;
                return compiled;
            },
            get: function(moduleName, templateName) {
                var moduleTemplates;
                if (compiledTemplates[moduleName] && compiledTemplates[
                        moduleName][templateName]) {
                    return compiledTemplates[moduleName][templateName];
                }
                moduleTemplates = mw.templates.get(moduleName);
                if (!moduleTemplates || !moduleTemplates[templateName]) {
                    throw new Error('Template ' + templateName + ' not found in module ' + moduleName);
                }
                return this.add(moduleName, templateName, moduleTemplates[templateName]);
            },
            compile: function(templateBody, compilerName) {
                return this.getCompiler(compilerName).compile(templateBody);
            }
        };
        mw.template.registerCompiler('html', {
            compile: function(src) {
                return {
                    render: function() {
                        return $($.parseHTML(src.trim()));
                    }
                };
            }
        });
    }());
});
mw.loader.implement("mediawiki.user@1qvt595", function($, jQuery, require, module) {
    (function() {
        var userInfoPromise, pageviewRandomId;

        function getUserInfo() {
            if (!userInfoPromise) {
                userInfoPromise = new mw.Api().getUserInfo();
            }
            return userInfoPromise;
        }
        $.extend(mw.user, {
            generateRandomSessionId: function() {
                var rnds, i, crypto = window.crypto || window.msCrypto;
                if (crypto && crypto.getRandomValues && typeof Uint16Array === 'function') {
                    rnds = new Uint16Array(5);
                    crypto.getRandomValues(rnds);
                } else {
                    rnds = new Array(5);
                    for (i = 0; i < 5; i++) {
                        rnds[i] = Math.floor(Math.random() * 0x10000);
                    }
                }
                return (rnds[0] + 0x10000).toString(16).slice(1) + (rnds[1] + 0x10000).toString(16).slice(1) + (rnds[2] + 0x10000).toString(16).slice(1) + (rnds[3] + 0x10000).toString(16).slice(1) + (rnds[4] + 0x10000).toString(16).slice(1);
            },
            getPageviewToken: function() {
                if (!pageviewRandomId) {
                    pageviewRandomId = mw.user.generateRandomSessionId();
                }
                return pageviewRandomId;
            },
            getId: function() {
                return mw.config.get('wgUserId') || 0;
            },
            getName: function() {
                return mw.config.get('wgUserName');
            },
            getRegistration: function() {
                var registration;
                if (mw.user.isAnon()) {
                    return false;
                }
                registration = mw.config.get('wgUserRegistration');
                return !registration ? null : new Date(registration);
            },
            isAnon: function() {
                return mw.user.getName() === null;
            },
            sessionId: function() {
                var sessionId = mw.storage.session.get('mwuser-sessionId');
                if (!sessionId) {
                    sessionId = mw.user.generateRandomSessionId();
                    mw.storage.session.set('mwuser-sessionId', sessionId);
                }
                return sessionId;
            },
            id: function() {
                return mw.user.getName() || mw.user.sessionId();
            },
            getGroups: function(callback) {
                var userGroups = mw.config.get('wgUserGroups', []);
                return $.Deferred().resolve(userGroups).then(callback);
            },
            getRights: function(callback) {
                return getUserInfo().then(function(userInfo) {
                    return userInfo.rights;
                }, function() {
                    return [];
                }).then(callback);
            }
        });
    }());
});
mw.loader.implement("mediawiki.util@192klfe", {
    "main": "mediawiki.util.js",
    "files": {
        "mediawiki.util.js": function(require, module) {
            (function() {
                'use strict';
                var util, config = require('./config.json');

                function rawurlencode(str) {
                    str = String(str);
                    return encodeURIComponent(str).replace(/!/g, '%21').replace(/'/g, '%27').replace(/\(/g, '%28').replace(/\)/g, '%29').replace(/\*/g, '%2A').replace(/~/g, '%7E');
                }

                function escapeIdInternal(str, mode) {
                    str = String(str);
                    switch (mode) {
                        case 'html5':
                            return str.replace(/ /g, '_');
                        case 'legacy':
                            return rawurlencode(str.replace(/ /g, '_')).replace(/%3A/g, ':').replace(/%/g, '.');
                        default:
                            throw new Error('Unrecognized ID escaping mode ' + mode);
                    }
                }
                util = {
                    rawurlencode: rawurlencode,
                    escapeIdForAttribute: function(str) {
                        var mode = config.FragmentMode[0];
                        return escapeIdInternal(str, mode);
                    },
                    escapeIdForLink: function(str) {
                        var mode = config.FragmentMode[0];
                        return escapeIdInternal(str, mode);
                    },
                    wikiUrlencode: function(str) {
                        return util.rawurlencode(str).replace(/%20/g, '_').replace(/%3B/g, ';').replace(
                            /%40/g, '@').replace(/%24/g, '$').replace(/%21/g, '!').replace(/%2A/g, '*').replace(/%28/g, '(').replace(/%29/g, ')').replace(/%2C/g, ',').replace(/%2F/g, '/').replace(/%7E/g, '~').replace(/%3A/g, ':');
                    },
                    getUrl: function(pageName, params) {
                        var titleFragmentStart, url, query, fragment = '',
                            title = typeof pageName === 'string' ? pageName : mw.config.get('wgPageName');
                        titleFragmentStart = title.indexOf('#');
                        if (titleFragmentStart !== -1) {
                            fragment = title.slice(titleFragmentStart + 1);
                            title = title.slice(0, titleFragmentStart);
                        }
                        if (params) {
                            query = $.param(params);
                        }
                        if (query) {
                            url = title ? util.wikiScript() + '?title=' + util.wikiUrlencode(title) + '&' + query : util.wikiScript() + '?' + query;
                        } else {
                            url = mw.config.get('wgArticlePath').replace('$1', util.wikiUrlencode(title).replace(/\$/g, '$$$$'));
                        }
                        if (fragment.length) {
                            url += '#' + util.escapeIdForLink(fragment);
                        }
                        return url;
                    },
                    wikiScript: function(str) {
                        str = str || 'index';
                        if (str === 'index') {
                            return mw.config.get('wgScript');
                        } else if (str === 'load') {
                            return config.LoadScript;
                        } else {
                            return mw.config.get('wgScriptPath') +
                                '/' + str + '.php';
                        }
                    },
                    addCSS: function(text) {
                        var s = mw.loader.addStyleTag(text);
                        return s.sheet || s.styleSheet || s;
                    },
                    getParamValue: function(param, url) {
                        var re = new RegExp('^[^#]*[&?]' + mw.RegExp.escape(param) + '=([^&#]*)'),
                            m = re.exec(url !== undefined ? url : location.href);
                        if (m) {
                            return decodeURIComponent(m[1].replace(/\+/g, '%20'));
                        }
                        return null;
                    },
                    $content: null,
                    addPortletLink: function(portletId, href, text, id, tooltip, accesskey, nextnode) {
                        var item, link, $portlet, portlet, portletDiv, ul, next;
                        if (!portletId) {
                            return null;
                        }
                        portlet = document.getElementById(portletId);
                        if (!portlet) {
                            return null;
                        }
                        link = document.createElement('a');
                        link.href = href;
                        link.textContent = text;
                        if (tooltip) {
                            link.title = tooltip;
                        }
                        if (accesskey) {
                            link.accessKey = accesskey;
                        }
                        $portlet = $(portlet);
                        $portlet.removeClass('emptyPortlet');
                        if ($portlet.hasClass('vectorTabs')) {
                            item = $('<li>').append($('<span>').append(link)[0])[0];
                        } else {
                            item = $('<li>').append(link)[0];
                        }
                        if (id) {
                            item.id = id;
                        }
                        ul = portlet.querySelector('ul');
                        if (!ul) {
                            ul = document.createElement('ul');
                            portletDiv = portlet
                                .querySelector('div');
                            if (portletDiv) {
                                portletDiv.appendChild(ul);
                            } else {
                                portlet.appendChild(ul);
                            }
                        }
                        if (nextnode && (typeof nextnode === 'string' || nextnode.nodeType || nextnode.jquery)) {
                            nextnode = $(ul).find(nextnode);
                            if (nextnode.length === 1 && nextnode[0].parentNode === ul) {
                                nextnode.before(item);
                                next = !0;
                            }
                        }
                        if (!next) {
                            ul.appendChild(item);
                        }
                        if (accesskey) {
                            $(link).updateTooltipAccessKeys();
                        }
                        return item;
                    },
                    validateEmail: function(mailtxt) {
                        var rfc5322Atext, rfc1034LdhStr, html5EmailRegexp;
                        if (mailtxt === '') {
                            return null;
                        }
                        rfc5322Atext = 'a-z0-9!#$%&\'*+\\-/=?^_`{|}~';
                        rfc1034LdhStr = 'a-z0-9\\-';
                        html5EmailRegexp = new RegExp('^' + '[' + rfc5322Atext + '\\.]+' + '@' + '[' + rfc1034LdhStr + ']+' + '(?:\\.[' + rfc1034LdhStr + ']+)*' + '$', 'i');
                        return (mailtxt.match(html5EmailRegexp) !== null);
                    },
                    isIPv4Address: function(address, allowBlock) {
                        var block, RE_IP_BYTE, RE_IP_ADD;
                        if (typeof address !== 'string') {
                            return false;
                        }
                        block = allowBlock ? '(?:\\/(?:3[0-2]|[12]?\\d))?' : '';
                        RE_IP_BYTE = '(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|0?[0-9]?[0-9])';
                        RE_IP_ADD = '(?:' + RE_IP_BYTE +
                            '\\.){3}' + RE_IP_BYTE;
                        return (new RegExp('^' + RE_IP_ADD + block + '$').test(address));
                    },
                    isIPv6Address: function(address, allowBlock) {
                        var block, RE_IPV6_ADD;
                        if (typeof address !== 'string') {
                            return false;
                        }
                        block = allowBlock ? '(?:\\/(?:12[0-8]|1[01][0-9]|[1-9]?\\d))?' : '';
                        RE_IPV6_ADD = '(?:' + ':(?::|(?::' + '[0-9A-Fa-f]{1,4}' + '){1,7})' + '|' + '[0-9A-Fa-f]{1,4}' + '(?::' + '[0-9A-Fa-f]{1,4}' + '){0,6}::' + '|' + '[0-9A-Fa-f]{1,4}' + '(?::' + '[0-9A-Fa-f]{1,4}' + '){7}' + ')';
                        if (new RegExp('^' + RE_IPV6_ADD + block + '$').test(address)) {
                            return true;
                        }
                        RE_IPV6_ADD = '[0-9A-Fa-f]{1,4}' + '(?:::?' + '[0-9A-Fa-f]{1,4}' + '){1,6}';
                        return (new RegExp('^' + RE_IPV6_ADD + block + '$').test(address) && /::/.test(address) && !/::.*::/.test(address));
                    },
                    isIPAddress: function(address, allowBlock) {
                        return util.isIPv4Address(address, allowBlock) || util.isIPv6Address(address, allowBlock);
                    }
                };
                if (window.QUnit) {
                    util.setOptionsForTest = function(opts) {
                        var oldConfig = config;
                        config = $.extend({}, config, opts);
                        return oldConfig;
                    };
                }

                function init() {
                    util.$content = (function() {
                        var i, l, $node, selectors;
                        selectors = ['.mw-body-primary', '.mw-body', '#mw-content-text'];
                        for (i = 0, l = selectors.length; i < l; i++) {
                            $node = $(selectors[i]);
                            if ($node.length) {
                                return $node.first();
                            }
                        }
                        return $('body');
                    }());
                }
                $(init);
                mw.util = util;
                module.exports = util;
            }());
        },
        "config.json": {
            "FragmentMode": ["html5", "legacy"],
            "LoadScript": "/w/load.php"
        }
    }
});
mw.loader.implement("mediawiki.viewport@0cme4d2", function($, jQuery, require, module) {
    (function() {
        'use strict';
        var viewport = {
            makeViewportFromWindow: function() {
                var $window = $(window),
                    scrollTop = $window.scrollTop(),
                    scrollLeft = $window.scrollLeft();
                return {
                    top: scrollTop,
                    left: scrollLeft,
                    right: scrollLeft + $window.width(),
                    bottom: (window.innerHeight ? window.innerHeight : $window.height()) + scrollTop
                };
            },
            isElementInViewport: function(el, rectangle) {
                var $el = $(el),
                    offset = $el.offset(),
                    rect = {
                        height: $el.height(),
                        width: $el.width(),
                        top: offset.top,
                        left: offset.left
                    },
                    viewport = rectangle || this.makeViewportFromWindow();
                return ((viewport.bottom >= rect.top) && (viewport.right >= rect.left) && (viewport.top <= rect.top + rect.height) && (viewport.left <= rect.left + rect.width));
            },
            isElementCloseToViewport: function(el, threshold, rectangle) {
                var viewport = rectangle ? $.extend({}, rectangle) : this.makeViewportFromWindow();
                threshold = threshold || 50;
                viewport.top -= threshold;
                viewport.left -= threshold;
                viewport.right += threshold;
                viewport.bottom += threshold;
                return this.isElementInViewport(el, viewport);
            }
        };
        mw.viewport = viewport;
    }());
});
mw.loader.implement("mediawiki.libs.pluralruleparser@0zqfng3", function($, jQuery, require, module) {
    (function(root, factory) {
        if (typeof define === 'function' && define.amd) {
            define(factory);
        } else if (typeof exports === 'object') {
            module.exports = factory();
        } else {
            root.pluralRuleParser = factory();
        }
    }(this, function() {
        function pluralRuleParser(rule, number) {
            'use strict';
            rule = rule.split('@')[0].replace(/^\s*/, '').replace(/\s*$/, '');
            if (!rule.length) {
                return true;
            }
            var pos = 0,
                operand, expression, relation, result, whitespace = makeRegexParser(/^\s+/),
                value = makeRegexParser(/^\d+/),
                _n_ = makeStringParser('n'),
                _i_ = makeStringParser('i'),
                _f_ = makeStringParser('f'),
                _t_ = makeStringParser('t'),
                _v_ = makeStringParser('v'),
                _w_ = makeStringParser('w'),
                _is_ = makeStringParser('is'),
                _isnot_ = makeStringParser('is not'),
                _isnot_sign_ = makeStringParser('!='),
                _equal_ = makeStringParser('='),
                _mod_ = makeStringParser('mod'),
                _percent_ = makeStringParser('%'),
                _not_ = makeStringParser('not'),
                _in_ = makeStringParser('in'),
                _within_ = makeStringParser('within'),
                _range_ =
                makeStringParser('..'),
                _comma_ = makeStringParser(','),
                _or_ = makeStringParser('or'),
                _and_ = makeStringParser('and');

            function debug() {}
            debug('pluralRuleParser', rule, number);

            function choice(parserSyntax) {
                return function() {
                    var i, result;
                    for (i = 0; i < parserSyntax.length; i++) {
                        result = parserSyntax[i]();
                        if (result !== null) {
                            return result;
                        }
                    }
                    return null;
                };
            }

            function sequence(parserSyntax) {
                var i, parserRes, originalPos = pos,
                    result = [];
                for (i = 0; i < parserSyntax.length; i++) {
                    parserRes = parserSyntax[i]();
                    if (parserRes === null) {
                        pos = originalPos;
                        return null;
                    }
                    result.push(parserRes);
                }
                return result;
            }

            function nOrMore(n, p) {
                return function() {
                    var originalPos = pos,
                        result = [],
                        parsed = p();
                    while (parsed !== null) {
                        result.push(parsed);
                        parsed = p();
                    }
                    if (result.length < n) {
                        pos = originalPos;
                        return null;
                    }
                    return result;
                };
            }

            function makeStringParser(s) {
                var len = s.length;
                return function() {
                    var result = null;
                    if (rule.substr(pos, len) === s) {
                        result = s;
                        pos += len;
                    }
                    return result;
                };
            }

            function makeRegexParser(regex) {
                return function() {
                    var matches = rule.substr(pos).match(regex);
                    if (matches === null) {
                        return null;
                    }
                    pos += matches[0].length;
                    return matches[0];
                };
            }

            function i() {
                var result = _i_();
                if (result === null) {
                    debug(' -- failed i', parseInt(number, 10));
                    return result;
                }
                result = parseInt(number, 10);
                debug(' -- passed i ', result);
                return result;
            }

            function n() {
                var result = _n_();
                if (result === null) {
                    debug(' -- failed n ', number);
                    return result;
                }
                result = parseFloat(number, 10);
                debug(' -- passed n ', result);
                return result;
            }

            function f() {
                var result = _f_();
                if (result === null) {
                    debug(' -- failed f ', number);
                    return result;
                }
                result = (number + '.').split('.')[1] || 0;
                debug(' -- passed f ', result);
                return result;
            }

            function t() {
                var result = _t_();
                if (result === null) {
                    debug(' -- failed t ', number);
                    return result;
                }
                result = (number + '.').split('.')[1].replace(/0$/, '') || 0;
                debug(' -- passed t ', result);
                return result;
            }

            function v() {
                var result = _v_();
                if (result === null) {
                    debug(' -- failed v ', number);
                    return result;
                }
                result = (number + '.').split('.')[1].length || 0;
                debug(' -- passed v ', result);
                return result;
            }

            function w() {
                var result = _w_();
                if (result === null) {
                    debug(' -- failed w ', number);
                    return result;
                }
                result = (number + '.').split('.')[1].replace(/0$/, '').length || 0;
                debug(' -- passed w ', result);
                return result;
            }
            operand = choice([n, i, f, t, v, w]);
            expression = choice([mod, operand]);

            function mod() {
                var result = sequence([operand, whitespace, choice([_mod_, _percent_]), whitespace, value]);
                if (result === null) {
                    debug(' -- failed mod');
                    return null;
                }
                debug(' -- passed ', parseInt(result[0], 10), result[2], parseInt(result[4], 10));
                return parseFloat(result[0]) % parseInt(result[4], 10);
            }

            function not() {
                var result = sequence([whitespace, _not_]);
                if (result === null) {
                    debug(' -- failed not');
                    return null;
                }
                return result[1];
            }

            function is() {
                var result = sequence([expression, whitespace, choice([_is_]), whitespace, value]);
                if (result !== null) {
                    debug(' -- passed is :', result[0], ' == ', parseInt(result[4], 10));
                    return result[0] === parseInt(result[4], 10);
                }
                debug(' -- failed is');
                return null;
            }

            function isnot() {
                var result = sequence([expression, whitespace, choice([_isnot_, _isnot_sign_]), whitespace,
                    value
                ]);
                if (result !== null) {
                    debug(' -- passed isnot: ', result[0], ' != ', parseInt(result[4], 10));
                    return result[0] !== parseInt(result[4], 10);
                }
                debug(' -- failed isnot');
                return null;
            }

            function not_in() {
                var i, range_list, result = sequence([expression, whitespace, _isnot_sign_, whitespace, rangeList]);
                if (result !== null) {
                    debug(' -- passed not_in: ', result[0], ' != ', result[4]);
                    range_list = result[4];
                    for (i = 0; i < range_list.length; i++) {
                        if (parseInt(range_list[i], 10) === parseInt(result[0], 10)) {
                            return false;
                        }
                    }
                    return true;
                }
                debug(' -- failed not_in');
                return null;
            }

            function rangeList() {
                var result = sequence([choice([range, value]), nOrMore(0, rangeTail)]),
                    resultList = [];
                if (result !== null) {
                    resultList = resultList.concat(result[0]);
                    if (result[1][0]) {
                        resultList = resultList.concat(result[1][0]);
                    }
                    return resultList;
                }
                debug(' -- failed rangeList');
                return null;
            }

            function rangeTail() {
                var result = sequence([_comma_, rangeList]);
                if (result !== null) {
                    return result[1];
                }
                debug(' -- failed rangeTail');
                return null;
            }

            function range() {
                var i, array, left, right, result = sequence([value, _range_, value]);
                if (result !== null) {
                    debug(' -- passed range');
                    array = [];
                    left = parseInt(result[0], 10);
                    right = parseInt(result[2], 10);
                    for (i = left; i <= right; i++) {
                        array.push(i);
                    }
                    return array;
                }
                debug(' -- failed range');
                return null;
            }

            function _in() {
                var result, range_list, i;
                result = sequence([expression, nOrMore(0, not), whitespace, choice([_in_, _equal_]), whitespace, rangeList]);
                if (result !== null) {
                    debug(' -- passed _in:', result);
                    range_list = result[5];
                    for (i = 0; i < range_list.length; i++) {
                        if (parseInt(range_list[i], 10) === parseFloat(result[0])) {
                            return (result[1][0] !== 'not');
                        }
                    }
                    return (result[1][0] === 'not');
                }
                debug(' -- failed _in ');
                return null;
            }

            function within() {
                var range_list, result;
                result = sequence([expression, nOrMore(0, not), whitespace, _within_, whitespace, rangeList]);
                if (result !== null) {
                    debug(' -- passed within');
                    range_list = result[5];
                    if ((result[0] >= parseInt(range_list[0], 10)) && (result[0] < parseInt(range_list[range_list.length - 1], 10))) {
                        return (result[1][0] !== 'not');
                    }
                    return (result[1][0] === 'not');
                }
                debug(
                    ' -- failed within ');
                return null;
            }
            relation = choice([is, not_in, isnot, _in, within]);

            function and() {
                var i, result = sequence([relation, nOrMore(0, andTail)]);
                if (result) {
                    if (!result[0]) {
                        return false;
                    }
                    for (i = 0; i < result[1].length; i++) {
                        if (!result[1][i]) {
                            return false;
                        }
                    }
                    return true;
                }
                debug(' -- failed and');
                return null;
            }

            function andTail() {
                var result = sequence([whitespace, _and_, whitespace, relation]);
                if (result !== null) {
                    debug(' -- passed andTail', result);
                    return result[3];
                }
                debug(' -- failed andTail');
                return null;
            }

            function orTail() {
                var result = sequence([whitespace, _or_, whitespace, and]);
                if (result !== null) {
                    debug(' -- passed orTail: ', result[3]);
                    return result[3];
                }
                debug(' -- failed orTail');
                return null;
            }

            function condition() {
                var i, result = sequence([and, nOrMore(0, orTail)]);
                if (result) {
                    for (i = 0; i < result[1].length; i++) {
                        if (result[1][i]) {
                            return true;
                        }
                    }
                    return result[0];
                }
                return false;
            }
            result = condition();
            if (result === null) {
                throw new Error('Parse error at position ' + pos.toString() + ' for rule: ' + rule);
            }
            if (pos !== rule.length) {
                debug(
                    'Warning: Rule not parsed completely. Parser stopped at ', rule.substr(0, pos), ' for rule: ', rule);
            }
            return result;
        }
        return pluralRuleParser;
    }));
    module.exports = window.pluralRuleParser;
    mw.libs.pluralRuleParser = window.pluralRuleParser;
});
mw.loader.implement("mediawiki.page.startup@0aw03it", function($, jQuery, require, module) {
    (function() {
        if (mw.config.get('wgBreakFrames')) {
            if (window.top !== window.self) {
                window.top.location.href = location.href;
            }
        }
        $(function() {
            var $diff;
            mw.hook('wikipage.content').fire($('#mw-content-text'));
            $diff = $('table.diff[data-mw="interface"]');
            if ($diff.length) {
                mw.hook('wikipage.diff').fire($diff.eq(0));
            }
        });
    }());
});
mw.loader.implement("mediawiki.template.mustache@1o9pbhg", function($, jQuery, require, module) {
    (function defineMustache(global, factory) {
        if (typeof exports === 'object' && exports && typeof exports.nodeName !== 'string') {
            factory(exports);
        } else if (typeof define === 'function' && define.amd) {
            define(['exports'], factory);
        } else {
            global.Mustache = {};
            factory(global.Mustache);
        }
    }(this, function mustacheFactory(mustache) {
        var objectToString = Object.prototype.toString;
        var isArray = Array.isArray || function isArrayPolyfill(object) {
            return objectToString.call(object) === '[object Array]';
        };

        function isFunction(object) {
            return typeof object === 'function';
        }

        function typeStr(obj) {
            return isArray(obj) ? 'array' : typeof obj;
        }

        function escapeRegExp(string) {
            return string.replace(/[\-\[\]{}()*+?.,\\\^$|#\s]/g, '\\$&');
        }

        function hasProperty(obj, propName) {
            return obj != null && typeof obj === 'object' && (propName in obj);
        }

        function primitiveHasOwnProperty(primitive, propName) {
            return (primitive != null && typeof primitive !== 'object' && primitive.hasOwnProperty &&
                primitive.hasOwnProperty(propName));
        }
        var regExpTest = RegExp.prototype.test;

        function testRegExp(re, string) {
            return regExpTest.call(re, string);
        }
        var nonSpaceRe = /\S/;

        function isWhitespace(string) {
            return !testRegExp(nonSpaceRe, string);
        }
        var entityMap = {
            '&': '&amp;',
            '<': '&lt;',
            '>': '&gt;',
            '"': '&quot;',
            "'": '&#39;',
            '/': '&#x2F;',
            '`': '&#x60;',
            '=': '&#x3D;'
        };

        function escapeHtml(string) {
            return String(string).replace(/[&<>"'`=\/]/g, function fromEntityMap(s) {
                return entityMap[s];
            });
        }
        var whiteRe = /\s*/;
        var spaceRe = /\s+/;
        var equalsRe = /\s*=/;
        var curlyRe = /\s*\}/;
        var tagRe = /#|\^|\/|>|\{|&|=|!/;

        function parseTemplate(template, tags) {
            if (!template) return [];
            var sections = [];
            var tokens = [];
            var spaces = [];
            var hasTag = !1;
            var nonSpace = !1;

            function stripSpace() {
                if (hasTag && !nonSpace) {
                    while (spaces.length) delete tokens[spaces.pop()];
                } else {
                    spaces = [];
                }
                hasTag = !1;
                nonSpace = !1;
            }
            var openingTagRe, closingTagRe, closingCurlyRe;

            function compileTags(tagsToCompile) {
                if (typeof tagsToCompile === 'string') tagsToCompile = tagsToCompile.split(
                    spaceRe, 2);
                if (!isArray(tagsToCompile) || tagsToCompile.length !== 2) throw new Error('Invalid tags: ' + tagsToCompile);
                openingTagRe = new RegExp(escapeRegExp(tagsToCompile[0]) + '\\s*');
                closingTagRe = new RegExp('\\s*' + escapeRegExp(tagsToCompile[1]));
                closingCurlyRe = new RegExp('\\s*' + escapeRegExp('}' + tagsToCompile[1]));
            }
            compileTags(tags || mustache.tags);
            var scanner = new Scanner(template);
            var start, type, value, chr, token, openSection;
            while (!scanner.eos()) {
                start = scanner.pos;
                value = scanner.scanUntil(openingTagRe);
                if (value) {
                    for (var i = 0, valueLength = value.length; i < valueLength; ++i) {
                        chr = value.charAt(i);
                        if (isWhitespace(chr)) {
                            spaces.push(tokens.length);
                        } else {
                            nonSpace = !0;
                        }
                        tokens.push(['text', chr, start, start + 1]);
                        start += 1;
                        if (chr === '\n') stripSpace();
                    }
                }
                if (!scanner.scan(openingTagRe)) break;
                hasTag = !0;
                type = scanner.scan(tagRe) || 'name';
                scanner.scan(whiteRe);
                if (type === '=') {
                    value = scanner.scanUntil(equalsRe);
                    scanner.scan(equalsRe);
                    scanner.scanUntil(closingTagRe);
                } else if (type === '{') {
                    value = scanner.scanUntil(closingCurlyRe);
                    scanner.scan(curlyRe);
                    scanner.scanUntil(closingTagRe);
                    type = '&';
                } else {
                    value = scanner.scanUntil(closingTagRe);
                }
                if (!scanner.scan(closingTagRe)) throw new Error('Unclosed tag at ' + scanner.pos);
                token = [type, value, start, scanner.pos];
                tokens.push(token);
                if (type === '#' || type === '^') {
                    sections.push(token);
                } else if (type === '/') {
                    openSection = sections.pop();
                    if (!openSection) throw new Error('Unopened section "' + value + '" at ' + start);
                    if (openSection[1] !== value) throw new Error('Unclosed section "' + openSection[1] + '" at ' + start);
                } else if (type === 'name' || type === '{' || type === '&') {
                    nonSpace = !0;
                } else if (type === '=') {
                    compileTags(value);
                }
            }
            openSection = sections.pop();
            if (openSection) throw new Error('Unclosed section "' + openSection[1] + '" at ' + scanner.pos);
            return nestTokens(squashTokens(tokens));
        }

        function squashTokens(tokens) {
            var squashedTokens = [];
            var token, lastToken;
            for (var i = 0, numTokens = tokens.length; i < numTokens; ++i) {
                token = tokens[i];
                if (token) {
                    if (token[0] === 'text' && lastToken && lastToken[0] === 'text') {
                        lastToken[1] += token[1];
                        lastToken[3] = token[3];
                    } else {
                        squashedTokens.push(token);
                        lastToken = token;
                    }
                }
            }
            return squashedTokens;
        }

        function nestTokens(tokens) {
            var nestedTokens = [];
            var collector = nestedTokens;
            var sections = [];
            var token, section;
            for (var i = 0, numTokens = tokens.length; i < numTokens; ++i) {
                token = tokens[i];
                switch (token[0]) {
                    case '#':
                    case '^':
                        collector.push(token);
                        sections.push(token);
                        collector = token[4] = [];
                        break;
                    case '/':
                        section = sections.pop();
                        section[5] = token[2];
                        collector = sections.length > 0 ? sections[sections.length - 1][4] : nestedTokens;
                        break;
                    default:
                        collector.push(token);
                }
            }
            return nestedTokens;
        }

        function Scanner(string) {
            this.string = string;
            this.tail = string;
            this.pos = 0;
        }
        Scanner.prototype.eos = function eos() {
            return this.tail === '';
        };
        Scanner.prototype.scan = function scan(re) {
            var match = this.tail.match(re);
            if (!match || match.index !== 0) return '';
            var string = match[0];
            this.tail = this.tail.substring(string.length);
            this.pos += string.length;
            return string;
        };
        Scanner.prototype.scanUntil = function scanUntil(re) {
            var index = this.tail.search(re),
                match;
            switch (index) {
                case -1:
                    match = this.tail;
                    this.tail = '';
                    break;
                case 0:
                    match = '';
                    break;
                default:
                    match = this.tail.substring(0, index);
                    this.tail = this.tail.substring(index);
            }
            this.pos += match.length;
            return match;
        };

        function Context(view, parentContext) {
            this.view = view;
            this.cache = {
                '.': this.view
            };
            this.parent = parentContext;
        }
        Context.prototype.push = function push(view) {
            return new Context(view, this);
        };
        Context.prototype.lookup = function lookup(name) {
            var cache = this.cache;
            var value;
            if (cache.hasOwnProperty(name)) {
                value = cache[name];
            } else {
                var context = this,
                    intermediateValue, names, index, lookupHit = !1;
                while (context) {
                    if (name.indexOf('.') > 0) {
                        intermediateValue = context.view;
                        names = name.split('.');
                        index = 0;
                        while (intermediateValue != null && index < names.length) {
                            if (index === names.length - 1) lookupHit = (hasProperty(intermediateValue, names[index]) || primitiveHasOwnProperty(intermediateValue, names[index]));
                            intermediateValue = intermediateValue[names[index++]];
                        }
                    } else {
                        intermediateValue = context.view[name];
                        lookupHit = hasProperty(context.view, name);
                    }
                    if (lookupHit) {
                        value = intermediateValue;
                        break;
                    }
                    context = context.parent;
                }
                cache[name] = value;
            }
            if (isFunction(value)) value = value.call(this.view);
            return value;
        };

        function Writer() {
            this.cache = {};
        }
        Writer.prototype.clearCache = function clearCache() {
            this.cache = {};
        };
        Writer.prototype.parse = function parse(template, tags) {
            var cache = this.cache;
            var cacheKey = template + ':' + (tags || mustache.tags).join(':');
            var tokens = cache[cacheKey];
            if (tokens == null) tokens = cache[cacheKey] = parseTemplate(template, tags);
            return tokens;
        };
        Writer.prototype.render = function render(template, view, partials, tags) {
            var tokens = this.parse(template, tags);
            var context = (view instanceof Context) ? view : new Context(view);
            return this.renderTokens(tokens, context, partials, template, tags);
        };
        Writer.prototype.renderTokens = function renderTokens(tokens, context, partials, originalTemplate, tags) {
            var buffer = '';
            var token, symbol, value;
            for (var i = 0, numTokens = tokens.length; i < numTokens; ++i) {
                value = undefined;
                token = tokens[i];
                symbol = token[0];
                if (symbol === '#') value = this.
                renderSection(token, context, partials, originalTemplate);
                else if (symbol === '^') value = this.renderInverted(token, context, partials, originalTemplate);
                else if (symbol === '>') value = this.renderPartial(token, context, partials, tags);
                else if (symbol === '&') value = this.unescapedValue(token, context);
                else if (symbol === 'name') value = this.escapedValue(token, context);
                else if (symbol === 'text') value = this.rawValue(token);
                if (value !== undefined) buffer += value;
            }
            return buffer;
        };
        Writer.prototype.renderSection = function renderSection(token, context, partials, originalTemplate) {
            var self = this;
            var buffer = '';
            var value = context.lookup(token[1]);

            function subRender(template) {
                return self.render(template, context, partials);
            }
            if (!value) return;
            if (isArray(value)) {
                for (var j = 0, valueLength = value.length; j < valueLength; ++j) {
                    buffer += this.renderTokens(token[4], context.push(value[j]), partials, originalTemplate);
                }
            } else if (typeof value === 'object' || typeof value === 'string' || typeof value === 'number') {
                buffer += this.renderTokens(token[4], context.push(value),
                    partials, originalTemplate);
            } else if (isFunction(value)) {
                if (typeof originalTemplate !== 'string') throw new Error('Cannot use higher-order sections without the original template');
                value = value.call(context.view, originalTemplate.slice(token[3], token[5]), subRender);
                if (value != null) buffer += value;
            } else {
                buffer += this.renderTokens(token[4], context, partials, originalTemplate);
            }
            return buffer;
        };
        Writer.prototype.renderInverted = function renderInverted(token, context, partials, originalTemplate) {
            var value = context.lookup(token[1]);
            if (!value || (isArray(value) && value.length === 0)) return this.renderTokens(token[4], context, partials, originalTemplate);
        };
        Writer.prototype.renderPartial = function renderPartial(token, context, partials, tags) {
            if (!partials) return;
            var value = isFunction(partials) ? partials(token[1]) : partials[token[1]];
            if (value != null) return this.renderTokens(this.parse(value, tags), context, partials, value);
        };
        Writer.prototype.unescapedValue = function unescapedValue(token, context) {
            var value = context.lookup(token[1]);
            if (
                value != null) return value;
        };
        Writer.prototype.escapedValue = function escapedValue(token, context) {
            var value = context.lookup(token[1]);
            if (value != null) return mustache.escape(value);
        };
        Writer.prototype.rawValue = function rawValue(token) {
            return token[1];
        };
        mustache.name = 'mustache.js';
        mustache.version = '3.0.1';
        mustache.tags = ['{{', '}}'];
        var defaultWriter = new Writer();
        mustache.clearCache = function clearCache() {
            return defaultWriter.clearCache();
        };
        mustache.parse = function parse(template, tags) {
            return defaultWriter.parse(template, tags);
        };
        mustache.render = function render(template, view, partials, tags) {
            if (typeof template !== 'string') {
                throw new TypeError('Invalid template! Template should be a "string" ' + 'but "' + typeStr(template) + '" was given as the first ' + 'argument for mustache#render(template, view, partials)');
            }
            return defaultWriter.render(template, view, partials, tags);
        };
        mustache.to_html = function to_html(template, view, partials, send) {
            var result = mustache.render(template, view, partials);
            if (isFunction(send)) {
                send(
                    result);
            } else {
                return result;
            }
        };
        mustache.escape = escapeHtml;
        mustache.Scanner = Scanner;
        mustache.Context = Context;
        mustache.Writer = Writer;
        return mustache;
    }));
    (function() {
        mw.template.registerCompiler('mustache', {
            compile: function(src) {
                return {
                    getSource: function() {
                        return src;
                    },
                    render: function(data, partialTemplates) {
                        var partials = {};
                        if (partialTemplates) {
                            $.each(partialTemplates, function(name, template) {
                                partials[name] = template.getSource();
                            });
                        }
                        return $($.parseHTML(Mustache.render(src, data, partials)));
                    }
                };
            }
        });
    }());
});
mw.loader.implement("mediawiki.template.regexp@1h7vja1", function($, jQuery, require, module) {
    mw.template.registerCompiler('regexp', {
        compile: function(src) {
            return {
                render: function() {
                    return new RegExp(src.replace(/\s+/g, '').replace(/\?<\w+?>/g, ''));
                }
            };
        }
    });
});
mw.loader.implement("mediawiki.ui.anchor@06mkbvx", null, {
    "css": [".mw-ui-anchor.mw-ui-progressive{color:#3366cc}.mw-ui-anchor.mw-ui-progressive:hover{color:#6a8fda}.mw-ui-anchor.mw-ui-progressive:focus,.mw-ui-anchor.mw-ui-progressive:active{color:#254a95;outline:0}.mw-ui-anchor.mw-ui-progressive.mw-ui-quiet{color:#54595d;text-decoration:none}.mw-ui-anchor.mw-ui-progressive.mw-ui-quiet:hover{color:#3366cc}.mw-ui-anchor.mw-ui-progressive.mw-ui-quiet:focus,.mw-ui-anchor.mw-ui-progressive.mw-ui-quiet:active{color:#254a95}.mw-ui-anchor.mw-ui-destructive{color:#dd3333}.mw-ui-anchor.mw-ui-destructive:hover{color:#e76e6e}.mw-ui-anchor.mw-ui-destructive:focus,.mw-ui-anchor.mw-ui-destructive:active{color:#ae1d1d;outline:0}.mw-ui-anchor.mw-ui-destructive.mw-ui-quiet{color:#54595d;text-decoration:none}.mw-ui-anchor.mw-ui-destructive.mw-ui-quiet:hover{color:#dd3333}.mw-ui-anchor.mw-ui-destructive.mw-ui-quiet:focus,.mw-ui-anchor.mw-ui-destructive.mw-ui-quiet:active{color:#ae1d1d}"]
});
mw.loader.implement("mobile.init@0brfoay", function($, jQuery, require, module) {
    this.mfModules = this.mfModules || {}, this.mfModules["mobile.init"] = (window.webpackJsonp = window.webpackJsonp || []).push([
        [12], {
            "./src/mobile.init/BetaOptInPanel.js": function(e, t, n) {
                function o(e) {
                    return (o = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(e) {
                        return typeof e
                    } : function(e) {
                        return e && "function" == typeof Symbol && e.constructor === Symbol && e !== Symbol.prototype ? "symbol" : typeof e
                    })(e)
                }

                function i(e, t) {
                    for (var n = 0; n < t.length; n++) {
                        var o = t[n];
                        o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, o.key, o)
                    }
                }

                function r(e, t) {
                    return !t || "object" !== o(t) && "function" != typeof t ? function(e) {
                        if (void 0 === e) throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
                        return e
                    }(e) : t
                }

                function a(e) {
                    return (a = Object.setPrototypeOf ? Object.getPrototypeOf : function(e) {
                        return e.__proto__ || Object.getPrototypeOf(e)
                    })(e)
                }

                function l(e, t) {
                    return (l = Object.setPrototypeOf || function(e, t) {
                        return e.__proto__ = t, e
                    })(e, t)
                }
                var s = n("./src/mobile.startup/Button.js"),
                    u = n("./src/mobile.startup/util.js"),
                    c = n("./src/mobile.startup/View.js"),
                    d = mw.user,
                    m = function(e) {
                        "use strict";

                        function t(e) {
                            return function(e, t) {
                                if (!(e instanceof t)) throw new TypeError("Cannot call a class as a function")
                            }(this, t), r(this, a(t).call(this, u.extend({
                                isTemplateMode: !0,
                                events: {
                                    "click .optin": "_onOptin",
                                    "click .cancel": e.onCancel
                                }
                            }, e)))
                        }
                        return function(e, t) {
                                if ("function" != typeof t && null !== t) throw new TypeError("Super expression must either be null or a function");
                                e.prototype = Object.create(t && t.prototype, {
                                    constructor: {
                                        value: e,
                                        writable: !0,
                                        configurable: !0
                                    }
                                }), t && l(e, t)
                            }(t, c),
                            function(e, t, n) {
                                t && i(e.prototype, t), n && i(e, n)
                            }(t, [{
                                key: "postRender",
                                value: function() {
                                    this.$el.find(".message").append(this.options.buttons.map(function(e) {
                                        return e.$el
                                    }))
                                }
                            }, {
                                key: "_onOptin",
                                value: function(e) {
                                    this.$el.find(e.currentTarget).closest("form").trigger("submit")
                                }
                            }, {
                                key: "template",
                                get: function() {
                                    return u.template('\n\t<div class="beta-opt-in-panel panel panel-inline visible">\n\t\t<form class="message content" action="{{postUrl}}" method="POST">\n\t\t\t<p>{{text}}</p>\n\t\t\t<input type="hidden" name="updateSingleOption" value="enableBeta">\n\t\t\t<input type="hidden" name="enableBeta" value="true">\n\t\t\t<input type="hidden" name="token" value="{{editToken}}">\n\t\t</form>\n\t</div>\n\t')
                                }
                            }, {
                                key: "defaults",
                                get: function() {
                                    return u.extend({}, c.prototype.defaults, {
                                        postUrl: void 0,
                                        editToken: d.tokens.get("editToken"),
                                        text: mw.msg("mobile-frontend-panel-betaoptin-msg"),
                                        buttons: [new s({
                                            progressive: !0,
                                            additionalClassNames: "optin",
                                            label: mw.msg("mobile-frontend-panel-ok")
                                        }), new s({
                                            additionalClassNames: "cancel",
                                            label: mw.msg("mobile-frontend-panel-cancel")
                                        })]
                                    })
                                }
                            }]), t
                    }();
                e.exports = m
            },
            "./src/mobile.init/editor.js": function(e, t, n) {
                var o = n("./src/mobile.startup/moduleLoaderSingleton.js"),
                    i = n("./src/mobile.startup/util.js"),
                    r = mw.loader.require("mediawiki.router"),
                    a = n(
                        "./src/mobile.init/editorLoadingOverlay.js"),
                    l = n("./src/mobile.startup/OverlayManager.js").getSingleton(),
                    s = $("#ca-edit, .mw-editsection a, .edit-link"),
                    u = mw.user,
                    c = n("./src/mobile.startup/toast.js"),
                    d = n("./src/mobile.startup/CtaDrawer.js"),
                    m = /MSIE \d\./.test(navigator.userAgent),
                    g = mw.config.get("wgPageContentModel"),
                    f = r.isSupported() && !m,
                    p = mw.config.get("wgVisualEditorConfig"),
                    b = mw.config.get("wgUserEditCount"),
                    w = /^\/editor\/(\d+|all)$/;

                function v(e) {
                    var t = new mw.Uri(this.href).query.section || "all";
                    r.navigate("#/editor/" + t), e.preventDefault()
                }

                function h(e, t, n) {
                    var c, d, m, g = 0 === e.id;
                    s.on("click", v), l.add(w, function(r) {
                            var s, c, d, f, w = window.innerWidth - document.documentElement.clientWidth,
                                v = window.pageYOffset,
                                h = $("#mw-content-text"),
                                y = {
                                    overlayManager: l,
                                    currentPageHTMLParser: n,
                                    fakeScroll: 0,
                                    api: new mw.Api,
                                    licenseMsg: t.getLicenseMsg(),
                                    title: e.title,
                                    titleObj: e.titleObj,
                                    isAnon: u.isAnon(),
                                    isNewPage: g,
                                    editCount: b,
                                    oldId: mw.util.getParamValue("oldid"),
                                    contentLang: h.attr("lang"),
                                    contentDir: h.attr(
                                        "dir"),
                                    sessionId: u.generateRandomSessionId()
                                },
                                k = mw.util.getParamValue("redlink") ? "new" : "click";

                            function S(e) {
                                mw.track("mf.schemaEditAttemptStep", {
                                    action: "init",
                                    type: "section",
                                    mechanism: k,
                                    editor_interface: e,
                                    editing_session_id: y.sessionId
                                })
                            }

                            function P() {
                                var t = !!p,
                                    n = function() {
                                        var e = mw.storage.get("preferredEditor");
                                        if (e) return e;
                                        switch (mw.config.get("wgMFDefaultEditor")) {
                                            case "source":
                                                return "SourceEditor";
                                            case "visual":
                                                return "VisualEditor";
                                            case "preference":
                                                return "visualeditor" === mw.user.options.get("visualeditor-editor") ? "VisualEditor" : "SourceEditor"
                                        }
                                        return "SourceEditor"
                                    }(),
                                    o = p && p.namespaces || [];
                                return t && e.isWikiText() && -1 !== o.indexOf(mw.config.get("wgNamespaceNumber")) && "translation" !== mw.config.get("wgTranslatePageTranslation") && ("VisualEditor" === n || "VisualEditor" === m) && "SourceEditor" !== m
                            }

                            function O() {
                                return S("wikitext"), mw.hook("mobileFrontend.editorOpening").fire(), mw.loader.using("mobile.editor.overlay").then(function() {
                                    return new(o.require(
                                        "mobile.editor.overlay/SourceEditorOverlay"))(y)
                                })
                            }
                            return "all" !== r && (y.sectionId = e.isWikiText() ? +r : null), s = i.Deferred(), d = a(function() {
                                var e, t, n, o, i;
                                $(document.body).addClass("ve-loading"), e = $("#mw-mf-page-center"), t = $("#content"), n = "0" === r || "all" === r ? $("#bodyContent") : $('[data-section="' + r + '"]').closest("h1, h2, h3, h4, h5, h6"), e.css({
                                    "padding-right": "+=" + w,
                                    "box-sizing": "border-box"
                                }), e.prop("scrollTop", v), o = n[0].getBoundingClientRect().top, o -= 48, P() ? (i = !0 === p.enableVisualSectionEditing || "mobile" === p.enableVisualSectionEditing, ("0" === r || "all" === r || i) && (o -= 16)) : "0" !== r && "all" !== r || (o -= 16), t.css({
                                    transform: "translate( 0, " + -o + "px )",
                                    "padding-bottom": "+=" + o,
                                    "margin-bottom": "-=" + o
                                }), y.fakeScroll = o, setTimeout(s.resolve, 500)
                            }, function() {
                                c && c.abort && c.abort(), $("#content").css({
                                    transform: "",
                                    "padding-bottom": "",
                                    "margin-bottom": ""
                                }), $("#mw-mf-page-center").css({
                                    "padding-right": "",
                                    "box-sizing": ""
                                }), $(document.body).removeClass("ve-loading")
                            }), P() ? (S("visualeditor"), mw.hook(
                                "mobileFrontend.editorOpening").fire(), y.mode = "visual", y.dataPromise = mw.loader.using("ext.visualEditor.targetLoader").then(function() {
                                return c = mw.libs.ve.targetLoader.requestPageData(y.mode, y.titleObj.getPrefixedDb(), {
                                    sessionStore: !0,
                                    section: void 0 === y.sectionId ? null : y.sectionId,
                                    oldId: y.oldId || void 0,
                                    targetName: "mobile"
                                })
                            }), f = mw.loader.using("ext.visualEditor.targetLoader").then(function() {
                                return mw.libs.ve.targetLoader.addPlugin("mobile.editor.ve"), mw.libs.ve.targetLoader.loadModules(y.mode)
                            }).then(function() {
                                var e = o.require("mobile.editor.overlay/VisualEditorOverlay"),
                                    t = o.require("mobile.editor.overlay/SourceEditorOverlay");
                                return y.SourceEditorOverlay = t, new e(y)
                            }, function() {
                                return O()
                            })) : f = O(), i.Promise.all([f, s]).then(function(e) {
                                e.getLoadingPromise().then(function() {
                                    var t = l.stack[0];
                                    t && t.overlay === d && l.replaceCurrent(e)
                                }, function(e) {
                                    l.router.back(), e.show()
                                })
                            }), d
                        }), $("#ca-edit a").prop("href", function(e, t) {
                            var n = new mw.Uri(t);
                            return n.query.section = 0, n.toString()
                        }), r.getPath() || !
                        mw.util.getParamValue("veaction") && "edit" !== mw.util.getParamValue("action") || ("edit" === mw.util.getParamValue("veaction") ? m = "VisualEditor" : "editsource" === mw.util.getParamValue("veaction") && (m = "SourceEditor"), d = "#/editor/" + (mw.util.getParamValue("section") || "edit" === mw.util.getParamValue("action") && "all" || "0"), window.history && history.pushState ? (delete(c = mw.Uri()).query.action, delete c.query.veaction, delete c.query.section, history.replaceState(null, document.title, c.toString() + d)) : r.navigate(d))
                }

                function y(e, t, n) {
                    var o, i;
                    !(o = mw.config.get("wgMinervaReadOnly")) && mw.config.get("wgIsProbablyEditable") ? h(e, n, t) : (function(e) {
                        e.$el.find(".mw-editsection").hide()
                    }(t), i = mw.config.get("wgRestrictionEdit"), mw.user.isAnon() && Array.isArray(i) && -1 !== i.indexOf("*") ? function() {
                        var e = new d({
                            content: mw.msg("mobile-frontend-editor-disabled-anon"),
                            signupQueryParams: {
                                warning: "mobile-frontend-watchlist-signup-action"
                            }
                        });
                        s.on("click", function(t) {
                            return e.show(), t.preventDefault(), e
                        }), r.route(w, function() {
                            e.show()
                        }), r.checkRoute()
                    }() : k(o ? mw.msg("apierror-readonly") : mw.msg("mobile-frontend-editor-disabled")))
                }

                function k(e) {
                    s.on("click", function(t) {
                        c.show(e), t.preventDefault()
                    }), r.route(w, function() {
                        c.show(e)
                    }), r.checkRoute()
                }
                e.exports = function(e, t, n) {
                    var o = 0 === e.id;
                    "wikitext" === g && (mw.util.getParamValue("undo") || f && (e.inNamespace("file") && o ? k(mw.msg("mobile-frontend-editor-uploadenable")) : y(e, t, n)))
                }
            },
            "./src/mobile.init/editorLoadingOverlay.js": function(e, t, n) {
                var o = n("./src/mobile.init/fakeToolbar.js"),
                    i = n("./src/mobile.startup/Overlay.js");
                e.exports = function(e, t) {
                    var n = o(),
                        r = new i({
                            className: "overlay overlay-loading",
                            noHeader: !0,
                            isBorderBox: !1,
                            onBeforeExit: function(e) {
                                e(), t()
                            }
                        });
                    return r.show = function() {
                        i.prototype.show.call(this), e()
                    }, n.appendTo(r.$el.find(".overlay-content")), n.addClass("toolbar-hidden"), setTimeout(function() {
                        n.addClass("toolbar-shown"), setTimeout(function() {
                            n.addClass("toolbar-shown-done")
                        }, 250)
                    }), r
                }
            },
            "./src/mobile.init/mobile.init.js": function(e, t, n) {
                var o, i = mw.storage,
                    r = mw.config.get("skin"),
                    a = mw.config.get("wgMFIsPageContentModelEditable"),
                    l = n("./src/mobile.init/editor.js"),
                    s = n("./src/mobile.startup/currentPage.js")(),
                    u = n("./src/mobile.startup/currentPageHTMLParser.js")(),
                    c = n("./src/mobile.init/BetaOptInPanel.js"),
                    d = mw.util,
                    m = n("./src/mobile.startup/util.js"),
                    g = m.getWindow(),
                    f = m.getDocument(),
                    p = mw.user,
                    b = n("./src/mobile.startup/context.js"),
                    w = mw.experiments,
                    v = mw.config.get("wgMFExperiments") || {},
                    h = n("./src/mobile.startup/Skin.js"),
                    y = n("./src/mobile.startup/eventBusSingleton.js"),
                    k = n("./src/mobile.startup/amcOutreach/amcOutreach.js");

                function S(e, t) {
                    return function() {
                        return [e.apply(this, arguments), t.apply(this, arguments)]
                    }
                }

                function P() {
                    var e = i.get("userFontSize", "regular");
                    f.addClass("mf-font-size-" + e)
                }
                o = h.getSingleton(), g.on("resize", S($.debounce(100, function() {
                    y.emit("resize")
                }), $.throttle(200, function() {
                    y.emit("resize:throttled")
                }))).on("scroll", S($.debounce(100, function() {
                    y.emit("scroll")
                }), $.throttle(200, function() {
                    y.emit("scroll:throttled")
                }))), g.on("pageshow", function() {
                    P()
                }), P(), v.betaoptin && function(e, t, n) {
                    var o, r, a, l = i.get("mobile-betaoptin-token");
                    !1 === l || "~" === l || t.isMainPage() || t.inNamespace("special") || (l || (l = p.generateRandomSessionId(), i.set("mobile-betaoptin-token", l)), r = "stable" === b.getMode(), a = "A" === w.getBucket(e, l), r && (a || d.getParamValue("debug")) && (o = new c({
                        postUrl: d.getUrl("Special:MobileOptions", {
                            returnto: t.title
                        }),
                        onCancel: function(e) {
                            e.preventDefault(), i.set("mobile-betaoptin-token", "~"), this.remove()
                        }
                    })).appendTo(n.getLeadSectionElement()), mw.track("mobile.betaoptin", {
                        isPanelShown: void 0 !== o
                    }))
                }(v.betaoptin, s, u), mw.requestIdleCallback(function() {
                    k.loadCampaign().showIfEligible(k.ACTIONS.onLoad)
                }), window.console && window.console.log && window.console.log.apply && mw.config.get("wgMFEnableJSConsoleRecruitment") && console.log(mw.msg("mobile-frontend-console-recruit")), !s.inNamespace("special") && a && "minerva" === r && null !== b.getMode() && l(s, u, o), mw.mobileFrontend.deprecate("mobile.init/skin", o,
                    "instance of mobile.startup/Skin. Minerva should have no dependencies on mobile.init")
            }
        },
        [
            ["./src/mobile.init/mobile.init.js", 0, 1]
        ]
    ]);
}, {
    "css": [
        ".beta-opt-in-panel{padding-top:1em;padding-bottom:1em}.panel-inline{display:none}.panel-inline.visible{display:block} .mf-font-size-small #content p,.mf-font-size-small .content p{font-size:90%}.mf-font-size-large #content p,.mf-font-size-large .content p{font-size:120%}.mf-font-size-x-large .content p,.mf-font-size-x-large #content p{font-size:140%} .ve-mobile-fakeToolbar-container{position:fixed;z-index:2;left:0;right:0;top:0;height:3em;background:#fff;border-bottom:1px solid #c8ccd1;box-shadow:0 1px 1px rgba(0,0,0,0.1)}.ve-mobile-fakeToolbar-header{display:table !important;height:3em}.ve-mobile-fakeToolbar{height:3em;line-height:0;display:block !important;width:100% !important;box-sizing:content-box;border-color:#c8ccd1;border-style:solid;border-width:0 1px;margin:0 -1px}.ve-mobile-fakeToolbar .mw-ui-icon-close,.ve-mobile-fakeToolbar .mw-ui-icon-mf-spinner{display:inline-block;width:3em;min-width:3em;max-width:3em;height:3em}.ve-mobile-fakeToolbar .mw-ui-icon-close:before,.ve-mobile-fakeToolbar .mw-ui-icon-mf-spinner:before{background-size:20px 20px;height:3em;margin:0}.ve-mobile-fakeToolbar .mw-ui-icon-close:hover{background-color:#eaecf0}.ve-mobile-fakeToolbar .mw-ui-icon-mf-spinner{line-height:3.164em;vertical-align:top;border-left:1px solid #eaecf0;cursor:auto;padding:0 0.875em;width:calc(100vw - 3em);max-width:500px;overflow:hidden;text-overflow:ellipsis}.ve-mobile-fakeToolbar .mw-ui-icon-mf-spinner:before{margin-right:0.3em}.ve-loading .content .section-heading .indicator{transition:width 400ms 100ms,min-width 400ms 100ms,opacity 400ms 100ms;width:0;min-width:0;opacity:0}.ve-loading .content .section-heading .indicator:before{width:1.5em}.ve-loading .mw-editsection{transition:opacity 400ms 100ms;opacity:0}.ve-loading .page-actions-menu{transition:border-bottom-color 400ms 100ms;border-bottom-color:transparent}.ve-loading #content{transition:padding-bottom 400ms 100ms,margin-bottom 400ms 100ms,transform 400ms 100ms}.toolbar-hidden{transform:translateY(-100%)}.toolbar-shown{transition:transform 250ms ease;transform:translateY(0);opacity:1}.toolbar-shown-done{ transform:none}"
    ]
}, {
    "apierror-readonly": "The wiki is currently in read-only mode.",
    "mobile-frontend-console-recruit": "Hello friend! 🍦\n\nWikipedia is powered by MediaWiki. MediaWiki is open source software and we're always keen to hear from fellow developers about what you love (😍 ) or what we could be doing better (😱 ).\n\n💬 Come chat with us on irc @ https://webchat.freenode.net/?channels=#wikimedia-mobile\n\n💪 Or work for us @ https://wikimediafoundation.org/about/jobs/",
    "mobile-frontend-editor-disabled": "This page is protected to prevent vandalism.",
    "mobile-frontend-editor-disabled-anon": "You need to log in to edit this page.",
    "mobile-frontend-editor-loading": "Loading editor…",
    "mobile-frontend-editor-uploadenable": "Please upload a photo first to enable editing.",
    "mobile-frontend-panel-betaoptin-msg": "Do you want to try some new features? By joining the beta, you will get access to experimental features, at the risk of encountering bugs and issues.",
    "mobile-frontend-panel-cancel": "No thanks",
    "mobile-frontend-panel-ok": "Okay"
});
mw.loader.implement("mobile.site@1hihcg6");
mw.loader.implement("mobile.startup@0l9nr6u", function($, jQuery, require, module) {
    mw.config.set({
        "wgMFMobileFormatterHeadings": ["h1", "h2", "h3", "h4", "h5", "h6", "h1", "h2", "h3", "h4", "h5", "h6"],
        "wgMFSearchAPIParams": {
            "ppprop": "displaytitle",
            "piprop": "thumbnail",
            "pithumbsize": 150,
            "pilimit": 50
        },
        "wgMFQueryPropModules": ["pageprops", "pageprops", "pageimages"],
        "wgMFSearchGenerator": {
            "name": "prefixsearch",
            "prefix": "ps"
        },
        "wgMFThumbnailSizes": {
            "tiny": 80,
            "small": 150
        },
        "wgMFLicense": {
            "msg": "mobile-frontend-copyright",
            "link": "\u003Ca href=\"https://creativecommons.org/licenses/by-sa/3.0/\" title=\"Definition of the Creative Commons Attribution-ShareAlike License\" target=\"_blank\"\u003ECC BY-SA 3.0\u003C/a\u003E and \u003Ca href=\"https://www.gnu.org/licenses/fdl.html\" title=\"Definition of the GFDL\" target=\"_blank\"\u003EGFDL\u003C/a\u003E",
            "plural": 2
        },
        "wgMFSchemaSearchSampleRate": 0.001,
        "wgMFExperiments": {
            "betaoptin": {
                "name": "betaoptin",
                "enabled": !1,
                "buckets": {
                    "control": 0.97,
                    "A": 0.03
                }
            }
        },
        "wgMFEnableJSConsoleRecruitment": !0,
        "wgMFDeviceWidthTablet": "720px",
        "wgMFCollapseSectionsByDefault": !0,
        "wgMFContentProviderScriptPath": "",
        "wgMFEditorOptions": {
            "skipPreview": !1
        },
        "wgMFSchemaEditAttemptStepOversample": "visualeditor"
    });
    ! function(e) {
        function r(r) {
            for (var n, f, i = r[0], l = r[1], a = r[2], c = 0, s = []; c < i.length; c++) f = i[c], o[f] && s.push(o[f][0]), o[f] = 0;
            for (n in l) Object.prototype.hasOwnProperty.call(l, n) && (e[n] = l[n]);
            for (p && p(r); s.length;) s.shift()();
            return u.push.apply(u, a || []), t()
        }

        function t() {
            for (var e, r = 0; r < u.length; r++) {
                for (var t = u[r], n = !0, i = 1; i < t.length; i++) {
                    var l = t[i];
                    0 !== o[l] && (n = !1)
                }
                n && (u.splice(r--, 1), e = f(f.s = t[0]))
            }
            return e
        }
        var n = {},
            o = {
                0: 0
            },
            u = [];

        function f(r) {
            if (n[r]) return n[r].exports;
            var t = n[r] = {
                i: r,
                l: !1,
                exports: {}
            };
            return e[r].call(t.exports, t, t.exports, f), t.l = !0, t.exports
        }
        f.m = e, f.c = n, f.d = function(e, r, t) {
            f.o(e, r) || Object.defineProperty(e, r, {
                enumerable: !0,
                get: t
            })
        }, f.r = function(e) {
            "undefined" != typeof Symbol && Symbol.toStringTag && Object.defineProperty(e, Symbol.toStringTag, {
                value: "Module"
            }), Object.defineProperty(e, "__esModule", {
                value: !0
            })
        }, f.t = function(e, r) {
            if (1 & r && (e = f(e)), 8 & r) return e;
            if (4 & r && "object" == typeof e && e && e.__esModule) return e;
            var t = Object.create(null);
            if (f.r(t), Object.defineProperty(t, "default", {
                    enumerable: !0,
                    value: e
                }), 2 & r && "string" != typeof e)
                for (var n in e) f.d(t, n, function(r) {
                    return e[r]
                }.bind(null, n));
            return t
        }, f.n = function(e) {
            var r = e && e.__esModule ? function() {
                return e.default
            } : function() {
                return e
            };
            return f.d(r, "a", r), r
        }, f.o = function(e, r) {
            return Object.prototype.hasOwnProperty.call(e, r)
        }, f.p = "";
        var i = window.webpackJsonp = window.webpackJsonp || [],
            l = i.push.bind(i);
        i.push = r, i = i.slice();
        for (var a = 0; a < i.length; a++) r(i[a]);
        var p = l;
        t()
    }([]);
    (window.webpackJsonp = window.webpackJsonp || []).push([
        [1], {
            "./src/mobile.editor.overlay/parseSaveError.js": function(e, t) {
                e.exports = function(e, t) {
                    var i;
                    if (t) switch (t) {
                        case "readonly":
                            return {
                                type: "readonly", details: e.error
                            };
                        case "editconflict":
                            return {
                                type: "editconflict", details: e.error
                            };
                        default:
                            return {
                                type:
                                    "error", details: "http"
                            }
                    }
                    if (e) {
                        if (e.error) return {
                            type: "error",
                            details: e.error.code
                        };
                        if (e.edit && e.edit.captcha) return {
                            type: "captcha",
                            details: e.edit.captcha
                        };
                        if (e.edit && e.edit.code) return t = e.edit.code, i = e.edit.warning, /^abusefilter-warning/.test(t) ? {
                            type: "abusefilter",
                            details: {
                                type: "warning",
                                message: i
                            }
                        } : /^abusefilter-disallow/.test(t) ? {
                            type: "abusefilter",
                            details: {
                                type: "disallow",
                                message: i
                            }
                        } : /^abusefilter/.test(t) ? {
                            type: "abusefilter",
                            details: {
                                type: "other",
                                message: i
                            }
                        } : {
                            type: "error",
                            details: t
                        }
                    }
                    return {
                        type: "error",
                        details: "unknown"
                    }
                }
            },
            "./src/mobile.init/fakeToolbar.js": function(e, t) {
                e.exports = function() {
                    var e, t;
                    return e = $("<a>").attr("tabindex", "0").attr("role", "button").addClass("mw-ui-icon mw-ui-icon-close mw-ui-icon-element").addClass("cancel").text(mw.msg("mobile-frontend-overlay-close")), t = $("<span>").addClass("mw-ui-icon mw-ui-icon-mf-spinner mw-ui-icon-before").text(mw.msg("mobile-frontend-editor-loading")), $("<div>").addClass("ve-mobile-fakeToolbar-container").append($("<div>").addClass("ve-mobile-fakeToolbar-header").addClass("header").append($("<div>").addClass("ve-mobile-fakeToolbar").append(e, t)))
                }
            },
            "./src/mobile.startup/Anchor.js": function(e, t, i) {
                function n(e) {
                    return (n = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(e) {
                        return typeof e
                    } : function(e) {
                        return e && "function" == typeof Symbol && e.constructor === Symbol && e !== Symbol.prototype ? "symbol" : typeof e
                    })(e)
                }

                function s(e, t) {
                    for (var i = 0; i < t.length; i++) {
                        var n = t[i];
                        n.enumerable = n.enumerable || !1, n.configurable = !0, "value" in n && (n.writable = !0), Object.defineProperty(e, n.key, n)
                    }
                }

                function r(e, t) {
                    return !t || "object" !== n(t) && "function" != typeof t ? function(e) {
                        if (void 0 === e) throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
                        return e
                    }(e) : t
                }

                function a(e) {
                    return (a = Object.setPrototypeOf ? Object.getPrototypeOf : function(e) {
                        return e.__proto__ || Object.getPrototypeOf(e)
                    })(e)
                }

                function o(e, t) {
                    return (o = Object.setPrototypeOf || function(e, t) {
                        return e.__proto__ = t, e
                    })(e, t)
                }
                var l = i(
                        "./src/mobile.startup/View.js"),
                    c = i("./src/mobile.startup/util.js"),
                    u = function(e) {
                        "use strict";

                        function t() {
                            return function(e, t) {
                                if (!(e instanceof t)) throw new TypeError("Cannot call a class as a function")
                            }(this, t), r(this, a(t).apply(this, arguments))
                        }
                        return function(e, t) {
                                if ("function" != typeof t && null !== t) throw new TypeError("Super expression must either be null or a function");
                                e.prototype = Object.create(t && t.prototype, {
                                    constructor: {
                                        value: e,
                                        writable: !0,
                                        configurable: !0
                                    }
                                }), t && o(e, t)
                            }(t, l),
                            function(e, t, i) {
                                t && s(e.prototype, t), i && s(e, i)
                            }(t, [{
                                key: "isTemplateMode",
                                get: function() {
                                    return !0
                                }
                            }, {
                                key: "defaults",
                                get: function() {
                                    return {
                                        progressive: void 0,
                                        destructive: void 0,
                                        additionalClassNames: "",
                                        href: void 0,
                                        label: void 0
                                    }
                                }
                            }, {
                                key: "template",
                                get: function() {
                                    return c.template('\n<a {{#href}}href="{{href}}"{{/href}} class="mw-ui-anchor\n\t{{#progressive}} mw-ui-progressive{{/progressive}}\n\t{{#destructive}} mw-ui-destructive{{/destructive}}\n\t {{additionalClassNames}}">{{label}}</a>\n\t')
                                }
                            }]), t
                    }();
                e.
                exports = u
            },
            "./src/mobile.startup/Browser.js": function(e, t, i) {
                var n, s = i("./src/mobile.startup/util.js");

                function r(e) {
                    var t = function t() {
                        var i = this["__cache" + t.cacheId] || (this["__cache" + t.cacheId] = {}),
                            n = [].join.call(arguments, "|");
                        return Object.prototype.hasOwnProperty.call(i, n) ? i[n] : i[n] = e.apply(this, arguments)
                    };
                    return t.cacheId = Date.now().toString() + Math.random().toString(), t
                }

                function a(e, t) {
                    this.userAgent = e, this.$el = t
                }
                a.prototype = {
                    isIos: r(function(e) {
                        var t = this.userAgent,
                            i = /ipad|iphone|ipod/i.test(t);
                        if (!i || !e) return i;
                        switch (e) {
                            case 8:
                                return /OS 8_/.test(t) || /Version\/8/.test(t);
                            case 4:
                                return /OS 4_/.test(t);
                            case 5:
                                return /OS 5_/.test(t);
                            default:
                                return !1
                        }
                    }),
                    isWideScreen: r(function() {
                        var e = parseInt(mw.config.get("wgMFDeviceWidthTablet"), 10);
                        return window.innerWidth >= e
                    }),
                    supportsAnimations: r(function() {
                        var e = document.createElement("foo").style;

                        function t(t) {
                            return t in e || "webkit" + t[0].toUpperCase() + t.slice(1) in e
                        }
                        return t("animationName") && t("transform") && t("transition")
                    }),
                    supportsTouchEvents: r(function() {
                        return "ontouchstart" in window
                    }),
                    supportsGeoLocation: r(function() {
                        return "geolocation" in window.navigator
                    })
                }, a.getSingleton = function() {
                    var e;
                    return n || (e = s.getDocument(), n = new a(window.navigator.userAgent, e)), n
                }, e.exports = a
            },
            "./src/mobile.startup/Button.js": function(e, t, i) {
                var n = i("./src/mobile.startup/mfExtend.js"),
                    s = i("./src/mobile.startup/util.js"),
                    r = i("./src/mobile.startup/View.js");

                function a(e) {
                    e.href && (e.tagName = "a"), r.call(this, e)
                }
                n(a, r, {
                    isTemplateMode: !0,
                    defaults: {
                        tagName: "a",
                        disabled: !1,
                        block: void 0,
                        progressive: void 0,
                        destructive: void 0,
                        quiet: void 0,
                        additionalClassNames: "",
                        href: void 0,
                        label: void 0
                    },
                    template: s.template('<{{tagName}} {{#disabled}}disabled{{/disabled}}\n{{#href}}href="{{href}}"{{/href}}\nclass="mw-ui-button {{#progressive}}mw-ui-progressive{{/progressive}}\n\t{{#block}}mw-ui-block{{/block}}\n\t{{#quiet}}mw-ui-quiet{{/quiet}}\n\t{{#destructive}}mw-ui-destructive{{/destructive}} {{additionalClassNames}}\n">{{label}}</{{tagName}}>')
                }), e.exports = a
            },
            "./src/mobile.startup/CtaDrawer.js": function(e, t, i) {
                var n = i("./src/mobile.startup/Drawer.js"),
                    s = i("./src/mobile.startup/util.js"),
                    r = i("./src/mobile.startup/Button.js"),
                    a = i("./src/mobile.startup/Anchor.js");

                function o() {
                    var e = arguments.length > 0 && void 0 !== arguments[0] ? arguments[0] : {},
                        t = l(e.queryParams, e.returnTo);
                    return new n(s.extend({
                        children: [s.parseHTML("<p>").text(e.content), new r(s.extend({
                            progressive: !0,
                            href: mw.util.getUrl("Special:UserLogin", t),
                            label: mw.msg("mobile-frontend-watchlist-cta-button-login")
                        }, e.progressiveButton)).$el, s.parseHTML("<div>").addClass("cta-drawer__anchors").append(new a(s.extend({
                            href: mw.util.getUrl("Special:UserLogin", c(t, e.signupQueryParams)),
                            progressive: !0,
                            label: mw.msg("mobile-frontend-watchlist-cta-button-signup")
                        }, e.actionAnchor)).$el)]
                    }, e))
                }

                function l(e, t) {
                    return s.extend({
                        returnto: t || mw.config.get("wgPageName")
                    }, e)
                }

                function c() {
                    return [].push.call(arguments, {
                        type: "signup"
                    }), s.extend.apply(s, arguments)
                }
                o.prototype.test = {
                    redirectParams: l,
                    signUpParams: c
                }, e.exports = o
            },
            "./src/mobile.startup/Drawer.js": function(e, t, i) {
                var n = i("./src/mobile.startup/mfExtend.js"),
                    s = i("./src/mobile.startup/View.js"),
                    r = i("./src/mobile.startup/util.js"),
                    a = new(i("./src/mobile.startup/Icon.js"))({
                        name: "arrow",
                        additionalClassNames: "cancel"
                    });

                function o(e) {
                    s.call(this, r.extend({
                        closeOnScroll: !0,
                        onBeforeHide: function() {}
                    }, e, {
                        className: "drawer position-fixed ".concat(e && e.className || "").trim()
                    }, {
                        events: r.extend({
                            "click .cancel": function(e) {
                                e.preventDefault(), this.hide()
                            }.bind(this),
                            click: function(e) {
                                e.stopPropagation()
                            }
                        }, (e || {}).events)
                    }))
                }
                n(o, s, {
                    minHideDelay: 10,
                    show: function() {
                        var e = r.Deferred();
                        return this.isVisible() ? e.resolve() : setTimeout(function() {
                            this.$el.addClass("visible animated");
                            var t = this.options.closeOnScroll;
                            this.$el.parent().addClass("drawer-visible".concat(t ? "" : " has-drawer--with-scroll-locked")), setTimeout(function() {
                                var i = r.getWindow();
                                i.one("click.drawer", this.hide.bind(this)), t && i.one("scroll.drawer", this.hide.bind(this)), e.
                                resolve()
                            }.bind(this), this.minHideDelay)
                        }.bind(this), this.minHideDelay), e.promise()
                    },
                    hide: function() {
                        setTimeout(function() {
                            this.$el.removeClass("visible"), this.options.onBeforeHide(), this.$el.parent().removeClass("drawer-visible has-drawer--with-scroll-locked"), r.getWindow().off(".drawer")
                        }.bind(this), this.minHideDelay)
                    },
                    isVisible: function() {
                        return this.$el.hasClass("visible")
                    },
                    toggle: function() {
                        this.isVisible() ? this.hide() : this.show()
                    },
                    appendToElement: "body",
                    postRender: function() {
                        this.$el.prepend(a.$el), this.options.children && this.$el.append(this.options.children), r.docReady(function() {
                            this.appendTo(this.appendToElement), this.$el.parent().addClass("has-drawer")
                        }.bind(this))
                    }
                }), e.exports = o
            },
            "./src/mobile.startup/Icon.js": function(e, t, i) {
                var n = i("./src/mobile.startup/mfExtend.js"),
                    s = i("./src/mobile.startup/util.js"),
                    r = i("./src/mobile.startup/View.js");

                function a(e) {
                    e.hasText && (e.modifier = "mw-ui-icon-before ".concat(e.modifier || "")), e.href && (e.tagName = "a"), r.call(this, e)
                }
                n(a, r, {
                    preRender: function() {
                        this.setRotationClass()
                    },
                    setRotationClass: function() {
                        var e = this.options;
                        if (e.rotation) switch (e.rotation) {
                            case -180:
                            case 180:
                                e._rotationClass = "mf-mw-ui-icon-rotate-flip";
                                break;
                            case -90:
                                e._rotationClass = "mf-mw-ui-icon-rotate-anti-clockwise";
                                break;
                            case 90:
                                e._rotationClass = "mf-mw-ui-icon-rotate-clockwise";
                                break;
                            case 0:
                                break;
                            default:
                                throw new Error("Bad value for rotation given. Must be ±90, 0 or ±180.")
                        }
                    },
                    isTemplateMode: !0,
                    defaults: {
                        rotation: 0,
                        hasText: !1,
                        href: void 0,
                        glyphPrefix: "mf",
                        tagName: "div",
                        isSmall: !1,
                        base: "mw-ui-icon",
                        name: "",
                        modifier: "mw-ui-icon-element",
                        title: ""
                    },
                    getClassName: function() {
                        return this.$el.attr("class")
                    },
                    getGlyphClassName: function() {
                        return this.options.base + "-" + this.options.glyphPrefix + "-" + this.options.name
                    },
                    toHtmlString: function() {
                        return this.parseHTML("<div>").append(this.$el).html()
                    },
                    template: s.template(
                        '\n<{{tagName}} class="{{base}} {{base}}-{{glyphPrefix}}-{{name}} {{modifier}} {{#isSmall}}mw-ui-icon-small{{/isSmall}} {{#_rotationClass}}{{_rotationClass}}{{/_rotationClass}} {{additionalClassNames}}"\n{{#id}}id="{{id}}"{{/id}}\n{{#href}}href="{{href}}"{{/href}}\n{{#title}}title="{{title}}"{{/title}}>{{label}}</{{tagName}}>\n\t')
                }), mw.log.deprecate(a.prototype, "toHtmlString", a.prototype.toHtmlString), e.exports = a
            },
            "./src/mobile.startup/MessageBox.js": function(e, t, i) {
                var n = i("./src/mobile.startup/View.js"),
                    s = i("./src/mobile.startup/util.js");

                function r() {
                    n.apply(this, arguments)
                }
                i("./src/mobile.startup/mfExtend.js")(r, n, {
                    isTemplateMode: !0,
                    defaults: {},
                    template: s.template('\n<div class="{{className}}">\n\t{{#heading}}<h2>{{heading}}</h2>{{/heading}}\n\t{{{msg}}}\n</div>\n\t')
                }), e.exports = r
            },
            "./src/mobile.startup/Overlay.js": function(e, t, i) {
                var n = i("./src/mobile.startup/View.js"),
                    s = i("./src/mobile.startup/headers.js").header,
                    r = i("./src/mobile.startup/Anchor.js"),
                    a = i(
                        "./src/mobile.startup/util.js"),
                    o = i("./src/mobile.startup/Browser.js").getSingleton();

                function l(e) {
                    this.isIos = o.isIos(), n.call(this, a.extend(!0, {
                        headerChrome: !1,
                        className: "overlay"
                    }, e, {
                        events: a.extend({
                            "click .cancel, .confirm, .initial-header .back": "onExitClick",
                            click: function(e) {
                                e.stopPropagation()
                            }
                        }, e.events)
                    }))
                }
                i("./src/mobile.startup/mfExtend.js")(l, n, {
                    template: a.template('\n{{^noHeader}}\n<div class="overlay-header-container header-container{{#headerChrome}}\n\theader-chrome{{/headerChrome}} position-fixed">\n</div>\n{{/noHeader}}\n<div class="overlay-content">\n\t{{>content}}\n</div>\n<div class="overlay-footer-container position-fixed"></div>\n\t'),
                    hideTimeout: null,
                    showSpinner: function() {
                        this.$el.find(".spinner").removeClass("hidden")
                    },
                    hideSpinner: function() {
                        this.$el.find(".spinner").addClass("hidden")
                    },
                    postRender: function() {
                        var e, t = this.options.footerAnchor;
                        this.$overlayContent = this.$el.find(".overlay-content"), this.isIos && this.$el.addClass("overlay-ios"), t && this.$el.find(
                            ".overlay-footer-container").append(new r(t).$el), e = this.options.headers || [s(this.options.heading, this.options.headerActions)], this.$el.find(".overlay-header-container").append(e)
                    },
                    onExitClick: function(e) {
                        var t = function() {
                            this.hide()
                        }.bind(this);
                        e.preventDefault(), e.stopPropagation(), this.options.onBeforeExit ? this.options.onBeforeExit(t, function() {}) : t()
                    },
                    show: function() {
                        var e = a.getDocument();
                        this.scrollTop = window.pageYOffset, e.addClass("overlay-enabled"), window.scrollTo(0, 1), this.$el.addClass("visible"), null !== this.hideTimeout && (clearTimeout(this.hideTimeout), this.hideTimeout = null)
                    },
                    hide: function() {
                        return a.getDocument().removeClass("overlay-enabled"), window.scrollTo(window.pageXOffset, this.scrollTop), this.hideTimeout = setTimeout(function() {
                            this.$el.detach(), this.hideTimeout = null
                        }.bind(this), 0), this.emit("hide"), !0
                    },
                    showHidden: function(e) {
                        this.$el.find(".hideable").addClass("hidden"), this.$el.find(e).removeClass("hidden")
                    }
                }), l.make = function(e, t) {
                    var i = new l(e);
                    return i.$el.find(
                        ".overlay-content").append(t.$el), i
                }, e.exports = l
            },
            "./src/mobile.startup/OverlayManager.js": function(e, t, i) {
                var n = i("./src/mobile.startup/util.js"),
                    s = null,
                    r = "MobileFrontend OverlayManager was here!";

                function a(e, t) {
                    e.on("route", this._checkRoute.bind(this)), this.router = e, this.entries = {}, this.stack = [], this.hideCurrent = !0, this.container = t
                }

                function o(e) {
                    e.on("hide", function() {
                        e.emit("_om_hide")
                    })
                }
                a.prototype = {
                    _onHideOverlay: function() {
                        this.hideCurrent = !1, this.router.back()
                    },
                    _attachOverlay: function(e) {
                        e.$el.parents().length || this.container.appendChild(e.$el[0])
                    },
                    _show: function(e) {
                        window.history.replaceState(r, null, window.location.href), e.once("_om_hide", this._onHideOverlay.bind(this)), this._attachOverlay(e), e.show()
                    },
                    _hideOverlay: function(e) {
                        var t;

                        function i() {
                            t = !0, e.hide()
                        }
                        return e.off("_om_hide"), e.options && e.options.onBeforeExit ? e.options.onBeforeExit(i) : i(), t || e.once("_om_hide", this._onHideOverlay.bind(this)), t
                    },
                    _processMatch: function(e) {
                        var t, i = this;
                        e && (e.overlay ? i._show(e.overlay) :
                            "function" == typeof(t = e.factoryResult).promise ? t.then(function(t) {
                                e.overlay = t, o(t), i._show(t)
                            }) : (e.overlay = t, o(e.overlay), i._show(t)))
                    },
                    _checkRoute: function(e) {
                        var t, i = this.stack[0];
                        i || (this.scrollTop = window.pageYOffset), i && void 0 !== i.overlay && this.hideCurrent && !this._hideOverlay(i.overlay) ? e.preventDefault() : ((t = Object.keys(this.entries).reduce(function(t, i) {
                            return t || this._matchRoute(e.path, this.entries[i])
                        }.bind(this), null)) || (this.stack = [], window.scrollTo(window.pageXOffset, this.scrollTop)), this.hideCurrent = !0, this._processMatch(t))
                    },
                    _matchRoute: function(e, t) {
                        var i, n = e.match(t.route),
                            s = this.stack[1],
                            r = this;
                        return n ? s && s.path === e ? (r.stack.shift(), s) : (i = {
                            path: e,
                            factoryResult: t.factory.apply(r, n.slice(1))
                        }, this.stack[0] && i.path === this.stack[0].path ? r.stack[0] = i : r.stack.unshift(i), i) : null
                    },
                    add: function(e, t) {
                        var i = this,
                            s = {
                                route: e,
                                factory: t
                            };
                        this.entries[e] = s, n.docReady(function() {
                            i._processMatch(i._matchRoute(i.router.getPath(), s))
                        })
                    },
                    replaceCurrent: function(e) {
                        if (0 === this.stack.length)
                            throw new Error("Trying to replace OverlayManager's current overlay, but stack is empty");
                        this._hideOverlay(this.stack[0].overlay), this.stack[0].overlay = e, o(e), this._show(e)
                    }
                }, a.getSingleton = function() {
                    if (!s) {
                        var e = document.createElement("div"),
                            t = window.location.hash,
                            i = window.history.state;
                        e.className = "mw-overlays-container", document.body.appendChild(e), t && i !== r && (window.history.replaceState(null, null, "#"), window.history.pushState(r, null, t)), s = new a(mw.loader.require("mediawiki.router"), e)
                    }
                    return s
                }, e.exports = a
            },
            "./src/mobile.startup/Page.js": function(e, t, i) {
                function n(e, t) {
                    for (var i = 0; i < t.length; i++) {
                        var n = t[i];
                        n.enumerable = n.enumerable || !1, n.configurable = !0, "value" in n && (n.writable = !0), Object.defineProperty(e, n.key, n)
                    }
                }
                var s = mw.html,
                    r = i("./src/mobile.startup/util.js"),
                    a = i("./src/mobile.startup/Section.js"),
                    o = function() {
                        "use strict";

                        function e(t) {
                            ! function(e, t) {
                                if (!(e instanceof t)) throw new TypeError("Cannot call a class as a function")
                            }(this, e);
                            var i = t.title || "";
                            r.extend(this, {
                                id: t.
                                id || 0,
                                title: i,
                                relevantTitle: t.relevantTitle || i,
                                titleObj: t.titleObj,
                                displayTitle: t.displayTitle || s.escape(i),
                                namespaceNumber: t.namespaceNumber || 0,
                                protection: t.protection,
                                sections: [],
                                url: t.url || mw.util.getUrl(i),
                                wikidataDescription: t.wikidataDescription,
                                _isMainPage: t.isMainPage || !1,
                                isMissing: void 0 !== t.isMissing ? t.isMissing : 0 === t.id,
                                lastModified: t.lastModified,
                                anchor: t.anchor,
                                revId: t.revId,
                                _isWatched: t.isWatched,
                                thumbnail: !!Object.prototype.hasOwnProperty.call(t, "thumbnail") && t.thumbnail,
                                _sectionLookup: {}
                            }), (t.sections || []).forEach(function(e) {
                                var t = new a(e);
                                this.sections.push(t), this._sectionLookup[t.id] = t
                            }.bind(this)), this.thumbnail && this.thumbnail.width && (this.thumbnail.isLandscape = this.thumbnail.width > this.thumbnail.height)
                        }
                        return function(e, t, i) {
                            t && n(e.prototype, t), i && n(e, i)
                        }(e, [{
                            key: "getDisplayTitle",
                            value: function() {
                                return this.displayTitle
                            }
                        }, {
                            key: "inNamespace",
                            value: function(e) {
                                return this.namespaceNumber === mw.config.get("wgNamespaceIds")[e]
                            }
                        }, {
                            key: "isWikiText",
                            value: function() {
                                return "wikitext" === mw.config.get("wgPageContentModel")
                            }
                        }, {
                            key: "isMainPage",
                            value: function() {
                                return this._isMainPage
                            }
                        }, {
                            key: "isWatched",
                            value: function() {
                                return this._isWatched
                            }
                        }, {
                            key: "getRevisionId",
                            value: function() {
                                return this.revId
                            }
                        }, {
                            key: "getTitle",
                            value: function() {
                                return this.title
                            }
                        }, {
                            key: "getNamespaceId",
                            value: function() {
                                var e = this.title.split(":");
                                return e[1] && mw.config.get("wgNamespaceIds")[e[0].toLowerCase().replace(" ", "_")] || 0
                            }
                        }, {
                            key: "getSection",
                            value: function(e) {
                                return this._sectionLookup[e]
                            }
                        }, {
                            key: "getSections",
                            value: function() {
                                return this.sections
                            }
                        }]), e
                    }();
                e.exports = o
            },
            "./src/mobile.startup/PageGateway.js": function(e, t, i) {
                var n = i("./src/mobile.startup/util.js"),
                    s = i("./src/mobile.startup/Section.js").prototype.template,
                    r = i("./src/mobile.startup/Page.js"),
                    a = i("./src/mobile.startup/actionParams.js"),
                    o = {};

                function l(e) {
                    var t, i = e.map(function(e) {
                            return e.level
                        }).filter(function(e) {
                            return !!e
                        }),
                        n = Math.min.apply(this, i).toString(),
                        r = [];
                    return e.forEach(function(e) {
                        void 0 !== e.line &&
                            (e.line = e.line.replace(/<\/?a\b[^>]*>/g, "")), e.subsections = [], !t || !e.level || e.level === n || t.subsections.length && t.subsections[0].level > e.level || t.level && t.level >= e.level ? (r.push(e), t = e) : (! function e(t, i) {
                                var n;
                                0 === t.length ? t.push(i) : (n = t[t.length - 1], parseInt(n.level, 10) === parseInt(i.level, 10) ? t.push(i) : e(n.subsections, i))
                            }(t.subsections, e), t.text += s.render(e))
                    }), r
                }

                function c(e) {
                    this.api = e
                }
                c.prototype = {
                    getSections: function(e) {
                        var t = n.Deferred();
                        return this.getPage(e).then(function(e) {
                            var i = new r(e);
                            t.resolve(i.getSections())
                        }, function(e) {
                            "missingtitle" === e ? t.resolve([]) : t.reject()
                        }), t.promise()
                    },
                    getPage: function(e, t, i) {
                        var s, r = n.Deferred(),
                            c = t ? {
                                url: t,
                                dataType: "jsonp"
                            } : {},
                            u = {
                                edit: ["*"]
                            };
                        return n.extend(c, a({
                            action: "mobileview",
                            page: e,
                            variant: mw.config.get("wgPageContentLanguage"),
                            redirect: "yes",
                            prop: "id|sections|text|lastmodified|lastmodifiedby|languagecount|hasvariants|protection|displaytitle|revision",
                            noheadings: "yes",
                            sectionprop: "level|line|anchor",
                            sections: i ? 0 : "all"
                        })), o[e] || (o[e] =
                            this.api.get(c).then(function(t) {
                                var i, a, o, c;
                                return t.error ? r.reject(t.error) : t.mobileview.sections ? (i = l((c = t.mobileview).sections), s = new Date(c.lastmodified).getTime() / 1e3, a = c.lastmodifiedby, u = Array.isArray(c.protection) ? u : n.extend(u, c.protection), o = {
                                    title: e,
                                    id: c.id,
                                    revId: c.revId,
                                    protection: u,
                                    lead: i[0].text,
                                    sections: i.slice(1),
                                    isMainPage: void 0 !== c.mainpage,
                                    historyUrl: mw.util.getUrl(e, {
                                        action: "history"
                                    }),
                                    lastModifiedTimestamp: s,
                                    languageCount: c.languagecount,
                                    hasVariants: void 0 !== c.hasvariants,
                                    displayTitle: c.displaytitle
                                }, a && n.extend(o, {
                                    lastModifiedUserName: a.name,
                                    lastModifiedUserGender: a.gender
                                }), o) : r.reject("No sections")
                            }, function(e) {
                                return r.reject(e)
                            })), o[e]
                    },
                    invalidatePage: function(e) {
                        delete o[e]
                    },
                    _getLanguageVariantsFromApiResponse: function(e, t) {
                        var i = t.query.general,
                            n = i.variantarticlepath,
                            s = [];
                        return !!i.variants && (Object.keys(i.variants).forEach(function(t) {
                            var r = i.variants[t],
                                a = {
                                    autonym: r.name,
                                    lang: r.code
                                };
                            a.url = n ? n.replace("$1", e).replace("$2", r.code) : mw.util.getUrl(e, {
                                variant: r.code
                            }), s.push(a)
                        }), s)
                    },
                    getPageLanguages: function(e, t) {
                        var i = this,
                            s = a({
                                meta: "siteinfo",
                                siprop: "general",
                                prop: "langlinks",
                                lllimit: "max",
                                titles: e
                            });
                        return t ? (s.llprop = "url|autonym|langname", s.llinlanguagecode = t) : s.llprop = "url|autonym", this.api.get(s).then(function(t) {
                            return {
                                languages: t.query.pages[0].langlinks || [],
                                variants: i._getLanguageVariantsFromApiResponse(e, t)
                            }
                        }, function() {
                            return n.Deferred().reject()
                        })
                    },
                    _getAPIResponseFromHTML: function(e) {
                        var t = [];
                        return e.find("h1,h2,h3,h4,h5,h6").each(function() {
                            var i = this.tagName.substr(1),
                                n = e.find(this).find(".mw-headline");
                            n.length && t.push({
                                level: i,
                                line: n.html(),
                                anchor: n.attr("id") || "",
                                text: ""
                            })
                        }), t
                    },
                    getSectionsFromHTML: function(e) {
                        return l(this._getAPIResponseFromHTML(e))
                    }
                }, e.exports = c
            },
            "./src/mobile.startup/PageHTMLParser.js": function(e, t, i) {
                function n(e, t) {
                    for (var i = 0; i < t.length; i++) {
                        var n = t[i];
                        n.enumerable = n.enumerable || !1, n.configurable = !0, "value" in n && (n.writable = !0), Object.defineProperty(e, n.key, n)
                    }
                }
                var s = i(
                        "./src/mobile.startup/Thumbnail.js"),
                    r = mw.config.get("wgMFMobileFormatterHeadings", ["h1", "h2", "h3", "h4", "h5"]).join(","),
                    a = ["noviewer", "metadata"],
                    o = function() {
                        "use strict";

                        function e(t) {
                            ! function(e, t) {
                                if (!(e instanceof t)) throw new TypeError("Cannot call a class as a function")
                            }(this, e), this.$el = t, this.$headings = this.$el.find(r)
                        }
                        return function(e, t, i) {
                            t && n(e.prototype, t), i && n(e, i)
                        }(e, [{
                            key: "findSectionHeadingByIndex",
                            value: function(e) {
                                return e < 1 ? $([]) : this.$headings.filter('.mw-parser-output > *, [class^="mf-section-"] > *').eq(e - 1)
                            }
                        }, {
                            key: "findChildInSectionLead",
                            value: function(e, t) {
                                var i, n, s, a, o = r;

                                function l(e) {
                                    return e.find(t).addBack()
                                }
                                return 0 === e ? (a = this.getLeadSectionElement()) && a.length ? l(a.children(t)) : (i = this.findSectionHeadingByIndex(1)).length ? l(i.prevAll(t)) : this.$el.find(t) : (i = this.findSectionHeadingByIndex(e)).hasClass("section-heading") ? (n = (s = i.next()).find(o).eq(0)).length ? l(n.prevAll(t)) : l(s.children(t)) : (n = i.eq(0).nextAll(o).eq(0), i.nextUntil(n, t))
                            }
                        }, {
                            key: "getLeadSectionElement",
                            value: function() {
                                var e = this.$el.find(".mf-section-0");
                                return e.length ? e : null
                            }
                        }, {
                            key: "getThumbnails",
                            value: function(e) {
                                var t = "." + a.join(",."),
                                    i = [];
                                return (e = e || this.$el).find("a.image, a.thumbimage").not(t).each(function() {
                                    var n = e.find(this),
                                        r = n.find(".lazy-image-placeholder"),
                                        o = 0 === n.parents(t).length && 0 === n.find(t).length,
                                        l = n.attr("href").match(/title=([^/&]+)/),
                                        c = n.attr("href").match(/[^/]+$/);
                                    r.length && o && (o = !new RegExp("\\b(" + a.join("|") + ")\\b").test(r.data("class"))), o && (l || c) && i.push(new s({
                                        el: n,
                                        filename: decodeURIComponent(l ? l[1] : c[0])
                                    }))
                                }), i
                            }
                        }, {
                            key: "getRedLinks",
                            value: function() {
                                return this.$el.find(".new")
                            }
                        }]), e
                    }();
                o.HEADING_SELECTOR = r, e.exports = o
            },
            "./src/mobile.startup/PageList.js": function(e, t, i) {
                var n = i("./src/mobile.startup/util.js"),
                    s = i("./src/mobile.startup/mfExtend.js"),
                    r = i("./src/mobile.startup/View.js"),
                    a = i("./src/mobile.startup/Browser.js").getSingleton();

                function o() {
                    r.apply(this, arguments)
                }
                s(o, r, {
                    defaults: {
                        pages: []
                    },
                    renderPageImages: function() {
                        var e = this;
                        setTimeout(function() {
                            e.$el.find(".list-thumb").each(function() {
                                var t = e.$el.find(this).data("style");
                                e.$el.find(this).attr("style", t)
                            })
                        }, a.isWideScreen() ? 0 : 1e3)
                    },
                    postRender: function() {
                        this.renderPageImages()
                    },
                    template: n.template('\n<ul class="page-list thumbs actionable">\n\t{{#pages}}\n\t\t{{>item}}\n\t{{/pages}}\n</ul>\n\t'),
                    templatePartials: {
                        item: n.template(
                            '\n<li title="{{title}}" data-id="{{id}}" class="page-summary">\n  <a href="{{url}}" class="title {{#isMissing}}new{{/isMissing}}"\n    {{#anchor}}name="{{anchor}}"{{/anchor}}\n    {{#latitude}}data-latlng="{{latitude}},{{longitude}}"{{/latitude}}\n    data-title="{{title}}">\n    <div class="list-thumb\n      {{^thumbnail}}list-thumb-none list-thumb-x{{/thumbnail}}\n      {{#thumbnail.isLandscape}}list-thumb-y{{/thumbnail.isLandscape}}\n      {{^thumbnail.isLandscape}}list-thumb-x{{/thumbnail.isLandscape}}"\n      {{#thumbnail}}data-style="background-image: url( {{thumbnail.source}} )"{{/thumbnail}}></div>\n    <h3>{{{displayTitle}}}</h3>\n    {{#wikidataDescription}}\n    <div class="wikidata-description">{{wikidataDescription}}</div>\n    {{/wikidataDescription}}\n    {{#lastModified}}\n    <div class="info">{{lastModified}}</div>\n    {{/lastModified}}\n    {{#proximity}}\n    <div class="info proximity">{{proximity}}</div>\n    {{/proximity}}\n  </a>\n</li>\n\t')
                    }
                }), e.exports = o
            },
            "./src/mobile.startup/ScrollEndEventEmitter.js": function(e, t, i) {
                var n = i("./src/mobile.startup/util.js"),
                    s = i("./src/mobile.startup/mfExtend.js");

                function r(e, t) {
                    this.threshold = t || 100, this.eventBus = e, this.enable(), OO.EventEmitter.call(this)
                }
                OO.mixinClass(r, OO.EventEmitter), r.EVENT_SCROLL_END = "ScrollEndEventEmitter-scrollEnd", s(r, {
                    _bindScroll: function() {
                        this._scrollHandler || (this._scrollHandler = this._onScroll.bind(this), this.eventBus.on("scroll:throttled", this._scrollHandler))
                    },
                    _unbindScroll: function() {
                        this._scrollHandler && (this.eventBus.off("scroll:throttled", this._scrollHandler), this._scrollHandler = null)
                    },
                    _onScroll: function() {
                        this.$el && this.enabled && this.scrollNearEnd() && (this.disable(), this.emit(r.EVENT_SCROLL_END))
                    },
                    scrollNearEnd: function() {
                        var e = n.getWindow(),
                            t = e.scrollTop() + e.height(),
                            i = this.$el.offset().top + this.$el.outerHeight();
                        return t + this.threshold > i
                    },
                    enable: function() {
                        this.enabled = !0, this._bindScroll()
                    },
                    disable: function() {
                        this.enabled = !1, this._unbindScroll()
                    },
                    setElement: function(e) {
                        this.$el = e
                    }
                }), e.exports = r
            },
            "./src/mobile.startup/Section.js": function(e, t, i) {
                var n = i("./src/mobile.startup/util.js"),
                    s = i("./src/mobile.startup/mfExtend.js"),
                    r = i("./src/mobile.startup/View.js");

                function a(e) {
                    var t = this;
                    e.tag = "h" + e.level, this.line = e.line, this.text = e.text, this.hasReferences = e.hasReferences || !1, this.id = e.id || null, this.anchor = e.anchor, this.subsections = [], (e.subsections || []).forEach(function(e) {
                        t.subsections.push(new a(e))
                    }), r.call(this, e)
                }
                s(a, r, {
                    template: n.template('\n<h{{level}} id="{{anchor}}">{{{line}}}</h{{level}}>\n{{{text}}}\n\t'),
                    defaults: {
                        line: void 0,
                        text: ""
                    }
                }), e.exports = a
            },
            "./src/mobile.startup/Skin.js": function(e, t, i) {
                var n, s = i("./src/mobile.startup/Browser.js").getSingleton(),
                    r = i("./src/mobile.startup/lazyImages/lazyImageLoader.js"),
                    a = i("./src/mobile.startup/lazyImages/lazyImageTransformer.js"),
                    o = i("./src/mobile.startup/View.js"),
                    l = i("./src/mobile.startup/util.js"),
                    c = i("./src/mobile.startup/currentPage.js"),
                    u = i(
                        "./src/mobile.startup/eventBusSingleton.js");

                function d(e) {
                    var t = this,
                        i = l.extend({}, e);
                    this.page = i.page, this.name = i.name, this.eventBus = i.eventBus, i.isBorderBox = !1, o.call(this, i), mw.config.get("wgMFLazyLoadImages") && l.docReady(function() {
                        var e = document.getElementById("content"),
                            i = e && r.queryPlaceholders(e) || [];
                        t.lazyImageTransformer = a.newLazyImageTransformer(t.eventBus, t.$el.find.bind(t.$el), 1.5 * l.getWindow().height(), i), t.lazyImageTransformer.loadImages()
                    })
                }
                i("./src/mobile.startup/mfExtend.js")(d, o, {
                    defaults: {
                        page: void 0
                    },
                    postRender: function() {
                        var e = this.$el;
                        s.supportsAnimations() && e.addClass("animations"), s.supportsTouchEvents() && e.addClass("touch-events"), l.parseHTML('<div class="transparent-shield cloaked-element">').appendTo(e.find("#mw-mf-page-center")), this.lazyImageTransformer && this.lazyImageTransformer.loadImages(), this.$el.find("#mw-mf-page-center").on("click", this.emit.bind(this, "click"))
                    },
                    getLicenseMsg: function() {
                        var e, t = mw.config.get("wgMFLicense"),
                            i = mw.language.
                        convertNumber(t.plural);
                        return t.link && (e = this.$el.find("#footer-places-terms-use").length > 0 ? mw.msg("mobile-frontend-editor-licensing-with-terms", mw.message("mobile-frontend-editor-terms-link", this.$el.find("#footer-places-terms-use a").attr("href")).parse(), t.link, i) : mw.msg("mobile-frontend-editor-licensing", t.link, i)), e
                    }
                }), d.getSingleton = function() {
                    return n || (n = new d({
                        el: "body",
                        page: c(),
                        eventBus: u
                    })), n
                }, e.exports = d
            },
            "./src/mobile.startup/Thumbnail.js": function(e, t, i) {
                var n = i("./src/mobile.startup/mfExtend.js"),
                    s = i("./src/mobile.startup/util.js"),
                    r = i("./src/mobile.startup/View.js");

                function a(e) {
                    r.call(this, s.extend({
                        isBorderBox: !1
                    }, e))
                }
                n(a, r, {
                    defaults: {
                        filename: void 0
                    },
                    postRender: function() {
                        this.options.description = this.$el.siblings(".thumbcaption").text()
                    },
                    getDescription: function() {
                        return this.options.description
                    },
                    getFileName: function() {
                        return this.options.filename
                    }
                }), e.exports = a
            },
            "./src/mobile.startup/View.js": function(e, t, i) {
                var n = i("./src/mobile.startup/util.js"),
                    s = i(
                        "./src/mobile.startup/mfExtend.js"),
                    r = /^(\S+)\s*(.*)$/,
                    a = 0;

                function o() {
                    this.initialize.apply(this, arguments)
                }
                OO.mixinClass(o, OO.EventEmitter), s(o, {
                    tagName: "div",
                    isTemplateMode: !1,
                    template: void 0,
                    templatePartials: {},
                    defaults: {},
                    initialize: function(e) {
                        var t = this;
                        OO.EventEmitter.call(this), e = n.extend({}, this.defaults, e), this.options = e, this.cid = function(e) {
                            var t = (++a).toString();
                            return e ? e + t : t
                        }("view"), "string" == typeof this.template && (this.template = mw.template.compile(this.template)), e.el ? this.$el = $(e.el) : this.$el = this.parseHTML("<" + this.tagName + ">"), this.$el.length ? this._postInitialize(e) : n.docReady(function() {
                            t.$el = $(e.el), t._postInitialize(e)
                        })
                    },
                    _postInitialize: function(e) {
                        var t = e.className;
                        this.$el.addClass(t), !1 !== e.isBorderBox && this.$el.addClass("view-border-box"), this.render({})
                    },
                    preRender: function() {},
                    postRender: function() {},
                    render: function(e) {
                        var t, i;
                        return n.extend(this.options, e), this.preRender(), this.undelegateEvents(), this.template && !this.options.skipTemplateRender && (i = this.template.render(this.options, this.templatePartials), this.isTemplateMode ? (t = $(i), this.$el.replaceWith(t), this.$el = t) : this.$el.html(i)), this.postRender(), this.delegateEvents(), this
                    },
                    delegateEvents: function(e) {
                        var t, i, n;
                        if (e = e || this.options.events)
                            for (i in this.undelegateEvents(), e) "function" != typeof(n = e[i]) && (n = this[e[i]]), n && (t = i.match(r), this.delegate(t[1], t[2], n.bind(this)))
                    },
                    delegate: function(e, t, i) {
                        this.$el.on(e + ".delegateEvents" + this.cid, t, i)
                    },
                    undelegateEvents: function() {
                        this.$el && this.$el.off(".delegateEvents" + this.cid)
                    },
                    undelegate: function(e, t, i) {
                        this.$el.off(e + ".delegateEvents" + this.cid, t, i)
                    },
                    parseHTML: function(e) {
                        return n.parseHTML(e, document)
                    }
                }), ["append", "prepend", "appendTo", "prependTo", "after", "before", "insertAfter", "insertBefore", "remove", "detach"].forEach(function(e) {
                    o.prototype[e] = function() {
                        return this.$el[e].apply(this.$el, arguments), this
                    }
                }), o.make = function() {
                    var e = arguments.length > 0 && void 0 !== arguments[0] ? arguments[0] : {},
                        t = arguments.length > 1 && void 0 !== arguments[1] ?
                        arguments[1] : [],
                        i = new o(e);
                    return t.forEach(function(e) {
                        i.append(e)
                    }), i
                }, e.exports = o
            },
            "./src/mobile.startup/actionParams.js": function(e, t, i) {
                var n = i("./src/mobile.startup/util.js"),
                    s = {
                        action: "query",
                        formatversion: 2
                    };
                e.exports = function(e) {
                    var t = mw.config.get("wgMFContentProviderScriptPath");
                    return n.extend({}, s, {
                        origin: t ? "*" : void 0
                    }, e)
                }
            },
            "./src/mobile.startup/amcOutreach/amcOutreach.js": function(e, t, i) {
                var n, s = i("./src/mobile.startup/moduleLoaderSingleton.js"),
                    r = i("./src/mobile.startup/toast.js"),
                    a = i("./src/mobile.startup/promoCampaign/promoCampaign.js"),
                    o = i("./src/mobile.startup/currentPage.js")(),
                    l = {
                        onLoad: "onLoad"
                    };
                e.exports = {
                    loadCampaign: function() {
                        return n || (n = a(function(e) {
                            mw.loader.using("mobile.amcOutreachDrawer").then(function() {
                                s.require("mobile.amcOutreachDrawer").amcOutreachDrawer(e, n, mw.message, mw.util, o, r, mw.user.tokens.get("editToken")).show()
                            })
                        }, l, "amc-outreach", !!mw.config.get("wgMFAmcOutreachActive"), !!mw.config.get("wgMFAmcOutreachUserEligible"), mw.storage))
                    },
                    ACTIONS: l
                }
            },
            "./src/mobile.startup/context.js": function(e, t) {
                e.exports = {
                    getMode: function() {
                        return mw.config.get("wgMFMode")
                    }
                }
            },
            "./src/mobile.startup/currentPage.js": function(e, t, i) {
                var n, s = i("./src/mobile.startup/Page.js"),
                    r = i("./src/mobile.startup/PageGateway.js");
                e.exports = function() {
                    if (n) return n;
                    var e = mw.config.get("wgRestrictionEdit", []),
                        t = $("#content #bodyContent"),
                        i = new r(new mw.Api),
                        a = mw.Title.newFromText(mw.config.get("wgRelevantPageName")),
                        o = mw.Title.newFromText(mw.config.get("wgPageName"));
                    return 0 === e.length && e.push("*"), n = new s({
                        title: o.getPrefixedText(),
                        titleObj: o,
                        relevantTitle: a.getPrefixedText(),
                        protection: {
                            edit: e
                        },
                        revId: mw.config.get("wgRevisionId"),
                        isMainPage: mw.config.get("wgIsMainPage"),
                        isWatched: $("#ca-watch").hasClass("watched"),
                        sections: i.getSectionsFromHTML(t),
                        isMissing: 0 === mw.config.get("wgArticleId"),
                        id: mw.config.get("wgArticleId"),
                        namespaceNumber: mw.config.get("wgNamespaceNumber")
                    })
                }
            },
            "./src/mobile.startup/currentPageHTMLParser.js": function(e, t, i) {
                var n, s = i(
                    "./src/mobile.startup/PageHTMLParser.js");
                e.exports = function() {
                    return n || (n = new s($("#content #bodyContent")))
                }
            },
            "./src/mobile.startup/eventBusSingleton.js": function(e, t) {
                e.exports = new OO.EventEmitter
            },
            "./src/mobile.startup/extendSearchParams.js": function(e, t, i) {
                var n = i("./src/mobile.startup/util.js"),
                    s = i("./src/mobile.startup/actionParams.js");
                e.exports = function(e) {
                    var t, i, r = mw.config.get("wgMFDisplayWikibaseDescriptions") || {
                            search: !0,
                            nearby: !0,
                            watchlist: !0,
                            tagline: !1
                        },
                        a = mw.config.get("wgMFContentProviderScriptPath");
                    if (!Object.prototype.hasOwnProperty.call(r, e)) throw new Error('"' + e + "\" isn't a feature that shows Wikibase descriptions.");
                    return (t = Array.prototype.slice.call(arguments, 1)).unshift({
                        prop: []
                    }), t.push(mw.config.get("wgMFSearchAPIParams")), (i = n.extend.apply({}, t)).prop = i.prop.concat(mw.config.get("wgMFQueryPropModules")), r[e] && -1 === i.prop.indexOf("description") && i.prop.push("description"), a && (i.origin = "*"), s(i)
                }
            },
            "./src/mobile.startup/headers.js": function(e, t, i) {
                var n = i(
                        "./src/mobile.startup/util.js"),
                    s = i("./src/mobile.startup/Button.js"),
                    r = i("./src/mobile.startup/icons.js");

                function a(e, t, i, s) {
                    var a, o = n.template('\n<div class="overlay-header header '.concat(s || "", ' hideable">\n\t<ul class="header-cancel">\n\t\t<li></li>\n\t</ul>\n\t{{{heading}}}\n\t{{#hasActions}}\n\t<div class="header-action"></div>\n\t{{/hasActions}}\n</div>\n\t\t')).render({
                        hasActions: t && t.length,
                        isHidden: !1,
                        heading: e
                    });
                    return i = i || r.cancel(), (a = n.parseHTML(o)).find("h2 span").addClass("truncated-text"), a.find(".header-cancel li").append(i.$el), t && t.length && a.find(".header-action").append(t.map(function(e) {
                        return e.$el
                    })), a[0]
                }

                function o(e, t, i, n) {
                    return a(e = '<div class="overlay-title"><h2>'.concat(e, "</h2></div>"), t, i, n)
                }
                e.exports = {
                    savingHeader: function(e) {
                        return o(e, [r.spinner({
                            additionalClassNames: "savespinner loading"
                        })], r.cancel(), "saving-header hidden")
                    },
                    saveHeader: function(e, t) {
                        return o(e, [new s({
                            tagName: "button",
                            additionalClassNames: "save submit",
                            disabled: !0,
                            label: n.
                            saveButtonMessage()
                        })], r.back(), t)
                    },
                    formHeader: function(e, t, i, n) {
                        return a(e, t, i, n)
                    },
                    header: o
                }
            },
            "./src/mobile.startup/icons.js": function(e, t, i) {
                var n = i("./src/mobile.startup/Icon.js"),
                    s = i("./src/mobile.startup/util.js");
                e.exports = {
                    CANCEL_GLYPH: "overlay-close",
                    Icon: n,
                    back: function() {
                        return new n({
                            tagName: "button",
                            name: "back",
                            additionalClassNames: "back",
                            label: mw.msg("mobile-frontend-overlay-close")
                        })
                    },
                    cancel: function(e) {
                        var t = e ? "overlay-close-" + e : "overlay-close";
                        return new this.Icon({
                            tagName: "button",
                            name: t,
                            additionalClassNames: "cancel",
                            label: mw.msg("mobile-frontend-overlay-close")
                        })
                    },
                    spinner: function(e) {
                        return (e = e || {}).additionalClassNames = e.additionalClassNames || "spinner loading", new this.Icon(s.extend(e, {
                            name: "spinner",
                            label: mw.msg("mobile-frontend-loading-message")
                        }))
                    },
                    error: function() {
                        return new n({
                            name: "alert-invert",
                            additionalClassNames: "load-fail-msg-icon"
                        })
                    },
                    watchIcon: function() {
                        return new this.Icon({
                            name: "watch",
                            additionalClassNames: "watch-this-article"
                        })
                    },
                    watchedIcon: function() {
                        return new this.Icon({
                            name: "watched",
                            additionalClassNames: "watch-this-article watched"
                        })
                    }
                }
            },
            "./src/mobile.startup/lazyImages/lazyImageLoader.js": function(e, t, i) {
                var n = i("./src/mobile.startup/util.js"),
                    s = "lazy-image-placeholder";
                e.exports = {
                    queryPlaceholders: function(e) {
                        return Array.prototype.slice.call(e.getElementsByClassName(s))
                    },
                    loadImages: function(t) {
                        return n.Promise.all(t.map(function(t) {
                            return e.exports.loadImage(t).promise
                        }))
                    },
                    loadImage: function(e) {
                        var t = n.Deferred(),
                            i = e.dataset.width || "0",
                            s = e.dataset.height || "0",
                            r = new Image(parseInt(i, 10), parseInt(s, 10));
                        return r.className = e.dataset.class || "", r.alt = e.dataset.alt || "", r.style.cssText = e.style.cssText || "", r.addEventListener("load", function() {
                            r.classList.add("image-lazy-loaded"), e.parentNode && e.parentNode.replaceChild(r, e), t.resolve("load")
                        }, {
                            once: !0
                        }), r.addEventListener("error", function() {
                            t.resolve("error")
                        }, {
                            once: !0
                        }), r.src = e.dataset.src || "", r.srcset = e.dataset.srcset || "", {
                            promise: t,
                            image: r
                        }
                    },
                    test: {
                        placeholderClass: s
                    }
                }
            },
            "./src/mobile.startup/lazyImages/lazyImageTransformer.js": function(e, t, i) {
                var n = i("./src/mobile.startup/lazyImages/lazyImageLoader.js"),
                    s = i("./src/mobile.startup/util.js");
                e.exports = {
                    newLazyImageTransformer: function(e, t, i, r) {
                        function a() {
                            var o = [];
                            return (r = s.grep(r, function(e) {
                                var n = t(e);
                                return !n.length || ! function(e, t) {
                                    return mw.viewport.isElementCloseToViewport(e[0], t) && (e.is(":visible") || 0 === e.height())
                                }(n, i) || (o.push(e), !1)
                            })).length || (e.off("scroll:throttled", a), e.off("resize:throttled", a), e.off("section-toggled", a)), n.loadImages(o)
                        }
                        return e.on("scroll:throttled", a), e.on("resize:throttled", a), e.on("section-toggled", a), {
                            loadImages: a
                        }
                    }
                }
            },
            "./src/mobile.startup/mfExtend.js": function(e, t) {
                e.exports = function(e, t, i) {
                    var n;
                    for (n in i ? OO.inheritClass(e, t) : (OO.initClass(e), i = t), i) e.prototype[n] = i[n]
                }
            },
            "./src/mobile.startup/moduleLoader.js": function(e, t) {
                function i() {
                    this._register = {}
                }
                i.prototype = {
                    require: function(e) {
                        var t, i, n = this._register;

                        function s() {
                            if (!Object.hasOwnProperty.call(n, e))
                                throw new Error("MobileFrontend Module not found: " + e);
                            return n[e]
                        }
                        i = e.split("/");
                        try {
                            return (t = mw.loader.require(i[0]))[i[1]] ? t[i[1]] : s()
                        } catch (e) {
                            return s()
                        }
                    },
                    define: function(e, t) {
                        var i = this;
                        if (Object.hasOwnProperty.call(this._register, e)) throw new Error("Module already exists: " + e);
                        return this._register[e] = t, {
                            deprecate: function(n) {
                                i.deprecate(n, t, e)
                            }
                        }
                    },
                    deprecate: function(e, t, i) {
                        var n;
                        i && (n = "Use " + i + " instead."), mw.log.deprecate(this._register, e, t, n)
                    }
                }, e.exports = i
            },
            "./src/mobile.startup/moduleLoaderSingleton.js": function(e, t, i) {
                var n = i("./src/mobile.startup/moduleLoader.js");
                e.exports = new n
            },
            "./src/mobile.startup/page/pageJSONParser.js": function(e, t, i) {
                var n = i("./src/mobile.startup/Page.js"),
                    s = i("./src/mobile.startup/time.js"),
                    r = i("./src/mobile.startup/util.js");
                e.exports = {
                    parse: function(e) {
                        var t, i, a = e.thumbnail,
                            o = e.pageprops || {
                                displaytitle: mw.html.escape(e.title)
                            },
                            l = e.terms;
                        return (o || l) && (i = l && l.label ? mw.html.escape(l.label[0]) : o.displaytitle), e.wikidataDescription = e.description ||
                            void 0, a && (e.thumbnail.isLandscape = a.width > a.height), e.revisions && e.revisions[0] && (t = e.revisions[0], e.lastModified = s.getLastModifiedMessage(new Date(t.timestamp).getTime() / 1e3, t.user)), new n(r.extend(e, {
                                id: e.pageid,
                                isMissing: !!e.missing,
                                url: mw.util.getUrl(e.title),
                                displayTitle: i
                            }))
                    }
                }
            },
            "./src/mobile.startup/promisedView.js": function(e, t, i) {
                var n = i("./src/mobile.startup/icons.js"),
                    s = i("./src/mobile.startup/View.js");
                e.exports = function(e) {
                    var t = new s({
                        className: "promised-view"
                    });
                    return t.$el.append(n.spinner().$el), e.then(function(e) {
                        t.$el.replaceWith(e.$el), t.$el = e.$el
                    }, function(e) {
                        e && e.$el && (t.$el.replaceWith(e.$el), t.$el = e.$el)
                    }), t
                }
            },
            "./src/mobile.startup/promoCampaign/promoCampaign.js": function(e, t) {
                e.exports = function(e, t, i, n, s, r) {
                    var a, o, l = {};
                    for (a in t) o = t[a], l[o] = "mobile-frontend-".concat(i, "-ineligible-").concat(o);

                    function c() {
                        return n
                    }

                    function u(e) {
                        if (!(e in t)) throw new Error("Action '".concat(e,
                            "' not found in 'actions' object. Please add this to\n\t\t\t\tthe object when creating a campaign with promoCampaign.js if you believe\n\t\t\t\tthis is a valid action."))
                    }
                    return {
                        showIfEligible: function(t) {
                            (function(e) {
                                return u(e), c() && s && null === r.get(l[e])
                            })(t) && e(t)
                        },
                        makeActionIneligible: function(e) {
                            return u(e), r.set(l[e], "~")
                        },
                        makeAllActionsIneligible: function() {
                            var e, i;
                            for (e in t) i = t[e], this.makeActionIneligible(i)
                        },
                        isCampaignActive: c
                    }
                }
            },
            "./src/mobile.startup/search/SearchGateway.js": function(e, t, i) {
                var n = i("./src/mobile.startup/page/pageJSONParser.js"),
                    s = i("./src/mobile.startup/util.js"),
                    r = i("./src/mobile.startup/extendSearchParams.js");

                function a(e) {
                    this.api = e, this.searchCache = {}, this.generator = mw.config.get("wgMFSearchGenerator")
                }
                a.prototype = {
                    searchNamespace: 0,
                    getApiData: function(e) {
                        var t = this.generator.prefix,
                            i = r("search", {
                                generator: this.generator.name
                            });
                        return i.redirects = "", i["g" + t + "search"] = e, i["g" + t + "namespace"] = this.searchNamespace, i["g" + t + "limit"] = 15, i.pilimit && (i.pilimit =
                            15, i.pithumbsize = mw.config.get("wgMFThumbnailSizes").tiny), i
                    },
                    _createSearchRegEx: function(e) {
                        return e = e.replace(/[-\[\]{}()*+?.,\\^$|#\s]/g, "\\$&"), new RegExp("^(" + e + ")", "ig")
                    },
                    _highlightSearchTerm: function(e, t) {
                        return e = s.parseHTML("<span>").text(e).html(), t = s.parseHTML("<span>").text(t).html(), e.replace(this._createSearchRegEx(t), "<strong>$1</strong>")
                    },
                    _getPage: function(e, t) {
                        var i = n.parse(t);
                        return i.displayTitle = this._highlightSearchTerm(t.displaytext ? t.displaytext : i.title, e), i.index = t.index, i
                    },
                    _processData: function(e, t) {
                        var i = this,
                            n = [];
                        return t.query && (n = t.query.pages || {}, (n = Object.keys(n).map(function(t) {
                            return i._getPage(e, n[t])
                        })).sort(function(e, t) {
                            return e.index < t.index ? -1 : 1
                        })), n
                    },
                    search: function(e) {
                        var t, i, n = this;
                        return this.isCached(e) || (i = (t = this.api.get(this.getApiData(e))).then(function(t) {
                            return {
                                query: e,
                                results: n._processData(e, t)
                            }
                        }, function() {
                            n.searchCache[e] = void 0
                        }), this.searchCache[e] = i.promise({
                            abort: function() {
                                t.abort()
                            }
                        })), this.searchCache[e]
                    },
                    isCached: function(e) {
                        return Boolean(this.searchCache[e])
                    }
                }, e.exports = a
            },
            "./src/mobile.startup/time.js": function(e, t, i) {
                var n = ["seconds", "minutes", "hours", "days", "months", "years"],
                    s = i("./src/mobile.startup/util.js"),
                    r = [1, 60, 3600, 86400, 2592e3, 31536e3];

                function a(e) {
                    for (var t = 0; t < r.length && e > r[t + 1];) ++t;
                    return {
                        value: Math.round(e / r[t]),
                        unit: n[t]
                    }
                }

                function o(e) {
                    return a(Math.round((new Date).getTime() / 1e3) - e)
                }

                function l(e) {
                    return "seconds" === e.unit && e.value < 10
                }
                e.exports = {
                    getLastModifiedMessage: function(e, t, i, n) {
                        var r, a, c = [];
                        return i = i || "unknown", l(r = o(e)) ? c.push("mobile-frontend-last-modified-with-user-just-now", i, t) : c.push({
                            seconds: "mobile-frontend-last-modified-with-user-seconds",
                            minutes: "mobile-frontend-last-modified-with-user-minutes",
                            hours: "mobile-frontend-last-modified-with-user-hours",
                            days: "mobile-frontend-last-modified-with-user-days",
                            months: "mobile-frontend-last-modified-with-user-months",
                            years: "mobile-frontend-last-modified-with-user-years"
                        } [r.unit], i, t, mw.language.convertNumber(r.value)), c.push(n ||
                            "#", mw.language.convertNumber(t ? 1 : 0), t ? mw.util.getUrl("User:" + t) : ""), a = mw.message.apply(this, c).parse(), n ? a : s.parseHTML("<div>").html(a).text()
                    },
                    getRegistrationMessage: function(e, t) {
                        var i, n = [];
                        return t = t || "unknown", l(i = o(parseInt(e, 10))) ? n.push("mobile-frontend-joined-just-now", t) : n.push({
                            seconds: "mobile-frontend-joined-seconds",
                            minutes: "mobile-frontend-joined-minutes",
                            hours: "mobile-frontend-joined-hours",
                            days: "mobile-frontend-joined-days",
                            months: "mobile-frontend-joined-months",
                            years: "mobile-frontend-joined-years"
                        } [i.unit], t, mw.language.convertNumber(i.value)), mw.message.apply(this, n).parse()
                    },
                    timeAgo: a,
                    getTimeAgoDelta: o,
                    isNow: l,
                    isRecent: function(e) {
                        return ["seconds", "minutes", "hours"].indexOf(e.unit) > -1
                    }
                }
            },
            "./src/mobile.startup/toast.js": function(e, t, i) {
                var n = i("./src/mobile.startup/util.js"),
                    s = "mobileFrontend/toast";

                function r() {
                    mw.requestIdleCallback(this._showPending.bind(this))
                }
                r.prototype.show = function(e, t) {
                    "string" == typeof t && (mw.log.warn(
                        "The use of the cssClass parameter of Toast.show is deprecated, please convert it to an options object."), t = {
                        type: t
                    }), t = n.extend({
                        tag: "toast"
                    }, t), this.notification = mw.notify(e, t)
                }, r.prototype.hide = function() {
                    void 0 !== this.notification && this.notification.then(function(e) {
                        e.close()
                    })
                }, r.prototype.showOnPageReload = function(e, t) {
                    mw.storage.get(s) ? mw.log.warn("A pending toast message already exits. The page should have been reloaded by now.") : mw.storage.set(s, JSON.stringify({
                        content: e,
                        options: t
                    }))
                }, r.prototype._showPending = function() {
                    var e = mw.storage.get(s);
                    e && (e = JSON.parse(e), this.show(e.content, e.options), mw.storage.remove(s))
                }, e.exports = new r
            },
            "./src/mobile.startup/util.js": function(e, t) {
                e.exports = {
                    saveButtonMessage: function() {
                        return mw.config.get("wgEditSubmitButtonLabelPublish") ? mw.msg("mobile-frontend-editor-publish") : mw.msg("mobile-frontend-editor-save")
                    },
                    Promise: {
                        all: function(e) {
                            return $.when.apply($, e)
                        }
                    },
                    escapeSelector: function(e) {
                        return $.escapeSelector(e)
                    },
                    grep: function() {
                        return $
                            .grep.apply($, arguments)
                    },
                    docReady: function(e) {
                        return $(e)
                    },
                    Deferred: function() {
                        return $.Deferred()
                    },
                    getDocument: function() {
                        return $(document.documentElement)
                    },
                    getWindow: function() {
                        return $(window)
                    },
                    parseHTML: function(e, t) {
                        return t = t || document, $($.parseHTML(e, t))
                    },
                    isNumeric: function() {
                        return $.isNumeric.apply($, arguments)
                    },
                    extend: function() {
                        return $.extend.apply($, arguments)
                    },
                    escapeHash: function(e) {
                        return e.replace(/(:|\.)/g, "\\$1")
                    },
                    isModifiedEvent: function(e) {
                        return e.altKey || e.ctrlKey || e.metaKey || e.shiftKey
                    },
                    repeatEvent: function(e, t, i, n) {
                        return e.on(i, function(e) {
                            return t.emit(i, e)
                        }, n)
                    },
                    template: function(e) {
                        return {
                            getSource: function() {
                                return e
                            },
                            render: function(t, i) {
                                var n = {};
                                return Object.keys(i || {}).forEach(function(e) {
                                    n[e] = i[e].getSource()
                                }), Mustache.render(e.trim(), t, n)
                            }
                        }
                    }
                }
            },
            "./src/mobile.startup/watchstar/Watchstar.js": function(e, t, i) {
                var n = i("./src/mobile.startup/View.js"),
                    s = i("./src/mobile.startup/watchstar/WatchstarGateway.js"),
                    r = i("./src/mobile.startup/icons.js"),
                    a = i(
                        "./src/mobile.startup/util.js"),
                    o = i("./src/mobile.startup/mfExtend.js"),
                    l = i("./src/mobile.startup/toast.js"),
                    c = i("./src/mobile.startup/CtaDrawer.js");

                function u(e) {
                    n.call(this, a.extend(!0, {
                        className: r.watchIcon().getClassName(),
                        events: {
                            click: "onStatusToggle"
                        }
                    }, e))
                }
                o(u, n, {
                    defaults: {
                        page: void 0,
                        funnel: "unknown"
                    },
                    ctaDrawerOptions: {
                        content: mw.msg("mobile-frontend-watchlist-cta"),
                        queryParams: {
                            warning: "mobile-frontend-watchlist-purpose",
                            campaign: "mobile_watchPageActionCta",
                            returntoquery: "article_action=watch"
                        },
                        signupQueryParams: {
                            warning: "mobile-frontend-watchlist-signup-action"
                        }
                    },
                    initialize: function(e) {
                        var t = n.prototype.initialize;
                        this._watched = e.isWatched, this.gateway = new s(e.api), t.call(this, e)
                    },
                    preRender: function() {
                        this.options.tooltip = this._watched ? mw.msg("unwatch") : mw.msg("watch")
                    },
                    postRender: function() {
                        var e = r.watchIcon().getGlyphClassName(),
                            t = r.watchedIcon().getGlyphClassName() + " watched";
                        this.$el.text(this.options.tooltip), !mw.user.isAnon() && this._watched ? this.$el.addClass(t).
                        removeClass(e): this.$el.addClass(e).removeClass(t), this.$el.removeClass("hidden")
                    },
                    onStatusToggleAnon: function() {
                        this.drawer || (this.drawer = new c(this.ctaDrawerOptions)), this.drawer.show()
                    },
                    onStatusToggleUser: function() {
                        var e, t = this,
                            i = this.gateway,
                            n = this.options.page,
                            s = !this._watched;

                        function r() {
                            clearInterval(e)
                        }
                        e = setInterval(function() {
                            l.show(mw.msg("mobile-frontend-watchlist-please-wait"))
                        }, 1e3), i.postStatusesByTitle([n.getTitle()], s).then(function() {
                            r(), t._watched = s, s ? (t.render(), t.emit("watch"), l.show(mw.msg("mobile-frontend-watchlist-add", n.title))) : (t.emit("unwatch"), t.render(), l.show(mw.msg("mobile-frontend-watchlist-removed", n.title)))
                        }, function() {
                            r(), l.show(mw.msg("mobile-frontend-watchlist-error"), {
                                type: "error"
                            })
                        })
                    },
                    onStatusToggle: function(e) {
                        e.preventDefault(), mw.user.isAnon() ? this.onStatusToggleAnon.apply(this, arguments) : this.onStatusToggleUser.apply(this, arguments)
                    }
                }), e.exports = u
            },
            "./src/mobile.startup/watchstar/WatchstarGateway.js": function(e, t, i) {
                var n = i(
                        "./src/mobile.startup/util.js"),
                    s = i("./src/mobile.startup/actionParams.js");

                function r(e) {
                    this.api = e
                }
                r.prototype = {
                    getStatuses: function(e, t) {
                        return n.Promise.all([this.getStatusesByID(e), this.getStatusesByTitle(t)]).then(function() {
                            return n.extend.apply(n, arguments)
                        })
                    },
                    getStatusesByID: function(e) {
                        var t = this;
                        return e.length ? this.api.get({
                            formatversion: 2,
                            action: "query",
                            prop: "info",
                            inprop: "watched",
                            pageids: e
                        }).then(function(e) {
                            return t._unmarshalGetResponse(e)
                        }) : n.Deferred().resolve({})
                    },
                    getStatusesByTitle: function(e) {
                        var t = this;
                        return e.length ? this.api.get(s({
                            prop: "info",
                            inprop: "watched",
                            titles: e
                        })).then(function(e) {
                            return t._unmarshalGetResponse(e)
                        }) : n.Deferred().resolve({})
                    },
                    postStatusesByTitle: function(e, t) {
                        var i = {
                            action: "watch",
                            titles: e
                        };
                        return t || (i.unwatch = !t), this.api.postWithToken("watch", i)
                    },
                    _unmarshalGetResponse: function(e) {
                        return (e && e.query && e.query.pages || []).reduce(function(e, t) {
                            return e[t.title] = t.watched, e
                        }, {})
                    }
                }, e.exports = r
            },
            "./src/mobile.startup/watchstar/WatchstarPageList.js": function(e, t, i) {
                var n = i("./src/mobile.startup/PageList.js"),
                    s = i("./src/mobile.startup/watchstar/Watchstar.js"),
                    r = mw.user,
                    a = i("./src/mobile.startup/util.js"),
                    o = i("./src/mobile.startup/Page.js"),
                    l = i("./src/mobile.startup/mfExtend.js"),
                    c = i("./src/mobile.startup/watchstar/WatchstarGateway.js");

                function u(e) {
                    this.wsGateway = new c(e.api), n.apply(this, arguments)
                }
                l(u, n, {
                    postRender: function() {
                        var e, t, i = this,
                            s = [],
                            r = [];
                        return n.prototype.postRender.apply(this), e = this.queryUnitializedItems(), t = this.parsePagesFromItems(e), Object.keys(t).forEach(function(e) {
                            var i = t[e];
                            i && "0" !== i ? s.push(i) : r.push(e)
                        }), this.getPages(s, r).then(function(t) {
                            i.renderItems(e, t)
                        })
                    },
                    queryUnitializedItems: function() {
                        return this.$el.find("li:not(.with-watchstar)")
                    },
                    getPages: function(e, t) {
                        return r.isAnon() ? a.Deferred().resolve({}) : this.wsGateway.getStatuses(e, t)
                    },
                    parsePagesFromItems: function(e) {
                        var t = this,
                            i = {};
                        return e.each(function(e, n) {
                            var s = t.$el.find(n);
                            i[s.attr("title")] = s.data("id")
                        }), i
                    },
                    renderItems: function(e, t) {
                        var i = this;
                        r.isAnon() || e.each(function(e, n) {
                            var s = i.$el.find(n),
                                r = new o({
                                    sections: [],
                                    title: s.attr("title"),
                                    id: s.data("id")
                                }),
                                a = i.parseHTML("<div>").appendTo(s),
                                l = t[r.getTitle()];
                            i._appendWatchstar(a, r, l), s.addClass("with-watchstar")
                        })
                    },
                    _appendWatchstar: function(e, t, i) {
                        var n = new s({
                            api: this.options.api,
                            funnel: this.options.funnel,
                            isAnon: r.isAnon(),
                            isWatched: i,
                            page: t,
                            el: e
                        });
                        return a.repeatEvent(n, this, "watch"), a.repeatEvent(n, this, "unwatch"), n
                    }
                }), e.exports = u
            }
        }
    ]);
    this.mfModules = this.mfModules || {}, this.mfModules["mobile.startup"] = (window.webpackJsonp = window.webpackJsonp || []).push([
        [3], {
            "./src/mobile.startup/Toggler.js": function(e, t, r) {
                var s = r("./src/mobile.startup/Browser.js").getSingleton(),
                    n = r("./src/mobile.startup/util.js"),
                    a = n.escapeHash,
                    o = {
                        name: "arrow",
                        additionalClassNames: "indicator"
                    },
                    i = r("./src/mobile.startup/Icon.js");

                function l(e) {
                    this.eventBus = e.eventBus, this._enable(e.$container, e.prefix, e.page, e.isClosed)
                }

                function c(e) {
                    var t = JSON.
                    parse(mw.storage.get("expandedSections") || "{}");
                    return t[e.title] = t[e.title] || {}, t
                }

                function u(e) {
                    mw.storage.set("expandedSections", JSON.stringify(e))
                }

                function p(e, t, r) {
                    var s, n, a = c(r);
                    t.find(".section-heading span").each(function() {
                        n = t.find(this), s = n.parents(".section-heading"), a[r.title][n.attr("id")] && !s.hasClass("open-block") && e.toggle(s, r)
                    })
                }

                function h(e) {
                    var t = (new Date).getTime(),
                        r = c(e);
                    Object.keys(r).forEach(function(e) {
                        var s = r[e];
                        Object.keys(s).forEach(function(n) {
                            var a = s[n];
                            Math.floor((t - a) / 1e3 / 60 / 60 / 24) >= 1 && delete r[e][n]
                        })
                    }), u(r)
                }
                l.prototype.toggle = function(e, t) {
                    var r, n = e.is(".open-block"),
                        a = e.next();
                    e.toggleClass("open-block"), e.data("indicator").remove(), o.rotation = n ? 0 : 180, r = new i(o).prependTo(e), e.data("indicator", r), a.toggleClass("open-block").attr({
                        "aria-pressed": !n,
                        "aria-expanded": !n
                    }), this.eventBus.emit("section-toggled", {
                        expanded: n,
                        isReferenceSection: Boolean(a.attr("data-is-reference-section")),
                        $heading: e
                    }), s.isWideScreen() || function(e, t) {
                        var r = e.find("span").attr(
                                "id"),
                            s = e.hasClass("open-block"),
                            n = c(t);
                        r && (s ? n[t.title][r] = (new Date).getTime() : delete n[t.title][r], u(n))
                    }(e, t)
                }, l.prototype.reveal = function(e, t, r) {
                    var s, n;
                    try {
                        (n = (s = t.find(a(e))).parents(".collapsible-heading")).length || (n = s.parents(".collapsible-block").prev(".collapsible-heading")), n.length && !n.hasClass("open-block") && this.toggle(n, r), n.length && window.scrollTo(0, s.offset().top)
                    } catch (e) {}
                }, l.prototype._enable = function(e, t, r, a) {
                    var l, c, u, m, d, f = this,
                        g = mw.config.get("wgMFCollapseSectionsByDefault");

                    function b() {
                        var t = window.location.hash;
                        0 === t.indexOf("#") && f.reveal(decodeURIComponent(t), e, r)
                    }
                    l = e.find("> h1,> h2,> h3,> h4,> h5,> h6,.section-heading").eq(0).prop("tagName") || "H1", void 0 === g && (g = !0), c = !g || "true" === mw.storage.get("expandSections"), e.children(l).each(function(n) {
                            var l, p = e.find(this),
                                h = p.find(".indicator"),
                                d = t + "collapsible-block-" + n;
                            p.next().is("div") && (m = p.next("div"), l = Boolean(m.attr("data-is-reference-section")), p.addClass("collapsible-heading ").data("section-number", n).attr({
                                tabindex: 0,
                                "aria-haspopup": "true",
                                "aria-controls": d
                            }).on("click", function(e) {
                                e.target.href || (e.preventDefault(), f.toggle(p, r))
                            }), o.rotation = c ? 180 : 0, u = new i(o), h.length ? h.replaceWith(u.$el) : u.prependTo(p), p.data("indicator", u.$el), m.addClass("collapsible-block").eq(0).attr({
                                id: d,
                                "aria-pressed": "false",
                                "aria-expanded": "false"
                            }), function(e, t, r) {
                                t.on("keypress", function(s) {
                                    13 !== s.which && 32 !== s.which || e.toggle(t, r)
                                }).find("a").on("keypress mouseup", function(e) {
                                    e.stopPropagation()
                                })
                            }(f, p, r), !l && (!a && s.isWideScreen() || c) && f.toggle(p, r))
                        }),
                        function() {
                            var t = mw.config.get("wgInternalRedirectTargetUrl"),
                                s = !!t && t.split("#")[1];
                            s && (window.location.hash = s, f.reveal(s, e, r))
                        }(), b(), (d = e.find("a:not(.reference a)")).on("click", function() {
                            void 0 !== d.attr("href") && d.attr("href").indexOf("#") > -1 && b()
                        }), n.getWindow().on("hashchange", function() {
                            b()
                        }), !s.isWideScreen() && r && (p(this, e, r), h(r))
                }, l._getExpandedSections = c, l._expandStoredSections = p, l._cleanObsoleteStoredSections = h, e.exports = l
            },
            "./src/mobile.startup/languageOverlay/getDeviceLanguage.js": function(e, t) {
                e.exports = function(e) {
                    var t = e.languages ? e.languages[0] : e.language || e.userLanguage || e.browserLanguage || e.systemLanguage;
                    return t ? t.toLowerCase() : void 0
                }
            },
            "./src/mobile.startup/languageOverlay/languageOverlay.js": function(e, t, r) {
                var s = r("./src/mobile.startup/moduleLoaderSingleton.js"),
                    n = r("./src/mobile.startup/languageOverlay/getDeviceLanguage.js"),
                    a = r("./src/mobile.startup/Overlay.js"),
                    o = r("./src/mobile.startup/promisedView.js");

                function i(e) {
                    return mw.loader.using("mobile.languages.structured").then(function() {
                        return e.getPageLanguages(mw.config.get("wgPageName"), mw.config.get("wgUserLanguage"))
                    }).then(function(e) {
                        return new(s.require("mobile.languages.structured/LanguageSearcher"))({
                            languages: e.languages,
                            variants: e.variants,
                            deviceLanguage: n(navigator)
                        })
                    })
                }

                function l(e) {
                    return a.make({
                        heading: mw.msg("mobile-frontend-language-heading"),
                        className: "overlay language-overlay"
                    }, o(i(e)))
                }
                l.test = {
                        loadLanguageSearcher: i
                    },
                    e.exports = l
            },
            "./src/mobile.startup/loadingOverlay.js": function(e, t, r) {
                var s = r("./src/mobile.startup/icons.js"),
                    n = r("./src/mobile.startup/Overlay.js");
                e.exports = function() {
                    var e = new n({
                        className: "overlay overlay-loading",
                        noHeader: !0
                    });
                    return s.spinner().$el.appendTo(e.$el.find(".overlay-content")), e
                }
            },
            "./src/mobile.startup/mediaViewer/overlay.js": function(e, t, r) {
                var s = r("./src/mobile.startup/moduleLoaderSingleton.js"),
                    n = r("./src/mobile.startup/promisedView.js"),
                    a = r("./src/mobile.startup/util.js"),
                    o = r("./src/mobile.startup/headers.js").header,
                    i = r("./src/mobile.startup/icons.js"),
                    l = r("./src/mobile.startup/Overlay.js");
                e.exports = function(e) {
                    return l.make({
                        headers: [o("", [], i.cancel("gray"))],
                        className: "overlay media-viewer"
                    }, n(a.Promise.all([mw.loader.using("mobile.mediaViewer")]).then(function() {
                        return new(0, s.require("mobile.mediaViewer").ImageCarousel)(e)
                    })))
                }
            },
            "./src/mobile.startup/mobile.startup.js": function(e, t, r) {
                var s = r("./src/mobile.startup/moduleLoaderSingleton.js"),
                    n = r(
                        "./src/mobile.startup/search/schemaMobileWebSearch.js");
                e.exports = {
                    moduleLoader: s,
                    mfExtend: r("./src/mobile.startup/mfExtend.js"),
                    context: r("./src/mobile.startup/context.js"),
                    time: r("./src/mobile.startup/time.js"),
                    util: r("./src/mobile.startup/util.js"),
                    View: r("./src/mobile.startup/View.js"),
                    PageGateway: r("./src/mobile.startup/PageGateway.js"),
                    Browser: r("./src/mobile.startup/Browser.js"),
                    Button: r("./src/mobile.startup/Button.js"),
                    Icon: r("./src/mobile.startup/Icon.js"),
                    ReferencesGateway: r("./src/mobile.startup/references/ReferencesGateway.js"),
                    ReferencesHtmlScraperGateway: r("./src/mobile.startup/references/ReferencesHtmlScraperGateway.js"),
                    icons: r("./src/mobile.startup/icons.js"),
                    Page: r("./src/mobile.startup/Page.js"),
                    currentPage: r("./src/mobile.startup/currentPage.js"),
                    PageHTMLParser: r("./src/mobile.startup/PageHTMLParser.js"),
                    currentPageHTMLParser: r("./src/mobile.startup/currentPageHTMLParser.js"),
                    Anchor: r("./src/mobile.startup/Anchor.js"),
                    Skin: r("./src/mobile.startup/Skin.js"),
                    OverlayManager: r("./src/mobile.startup/OverlayManager.js"),
                    Overlay: r("./src/mobile.startup/Overlay.js"),
                    loadingOverlay: r("./src/mobile.startup/loadingOverlay.js"),
                    CtaDrawer: r("./src/mobile.startup/CtaDrawer.js"),
                    toast: r("./src/mobile.startup/toast.js"),
                    Watchstar: r("./src/mobile.startup/watchstar/Watchstar.js"),
                    rlModuleLoader: r("./src/mobile.startup/rlModuleLoader.js"),
                    eventBusSingleton: r("./src/mobile.startup/eventBusSingleton.js"),
                    Toggler: r("./src/mobile.startup/Toggler.js"),
                    toc: {
                        TableOfContents: r("./src/mobile.startup/toc/TableOfContents.js")
                    },
                    notifications: {
                        overlay: r("./src/mobile.startup/notifications/overlay.js")
                    },
                    references: r("./src/mobile.startup/references/references.js"),
                    search: {
                        SearchOverlay: r("./src/mobile.startup/search/SearchOverlay.js"),
                        MobileWebSearchLogger: r("./src/mobile.startup/search/MobileWebSearchLogger.js"),
                        SearchGateway: r("./src/mobile.startup/search/SearchGateway.js")
                    },
                    lazyImages: {
                        lazyImageLoader: r("./src/mobile.startup/lazyImages/lazyImageLoader.js")
                    },
                    talk: {
                        overlay: r("./src/mobile.startup/talk/overlay.js")
                    },
                    languageOverlay: r("./src/mobile.startup/languageOverlay/languageOverlay.js"),
                    mediaViewer: {
                        overlay: r("./src/mobile.startup/mediaViewer/overlay.js")
                    }
                }, mw.mobileFrontend = s, s.define("mobile.startup", e.exports), n.subscribeMobileWebSearchSchema()
            },
            "./src/mobile.startup/notifications/overlay.js": function(e, t, r) {
                var s = r("./src/mobile.startup/Overlay.js"),
                    n = r("./src/mobile.startup/moduleLoaderSingleton.js"),
                    a = r("./src/mobile.startup/promisedView.js"),
                    o = r("./src/mobile.startup/View.js"),
                    i = r("./src/mobile.startup/Anchor.js");

                function l(e, t) {
                    var r, l = mw.loader.using("mobile.notifications.overlay").then(function() {
                            return r = new OO.ui.ButtonWidget({
                                icon: "checkAll",
                                title: mw.msg("echo-mark-all-as-read")
                            }), o.make({
                                class: "notifications-overlay-header-markAllRead"
                            }, [r.$element])
                        }),
                        u = a(l);
                    u.$el.hide();
                    var p = s.make({
                        heading: "<strong>" + mw.message("notifications").escaped() + "</strong>",
                        footerAnchor: new i({
                            href: mw.util.getUrl("Special:Notifications"),
                            progressive: !0,
                            additionalClassNames: "footer-link notifications-archive-link",
                            label: mw.msg("echo-overlay-link")
                        }).options,
                        headerActions: [u],
                        isBorderBox: !1,
                        className: "overlay notifications-overlay navigation-drawer",
                        onBeforeExit: function(e) {
                            return c(p, e)
                        }
                    }, a(l.then(function() {
                        var s = n.require("mobile.notifications.overlay");
                        return (0, s.list)(s.echo(), r, e, t)
                    })));
                    return p
                }

                function c(e, t) {
                    "transition" in e.$el[0].style ? (e.$el[0].addEventListener("transitionend", t, {
                        once: !0
                    }), e.$el[0].classList.remove("visible")) : t()
                }
                l.test = {
                    onBeforeExit: c
                }, e.exports = l
            },
            "./src/mobile.startup/references/ReferencesGateway.js": function(e, t) {
                function r(e) {
                    this.api = e
                }
                r.prototype.getReference = null, r.ERROR_NOT_EXIST = "NOT_EXIST_ERROR", r.ERROR_OTHER = "OTHER_ERROR", e.exports = r
            },
            "./src/mobile.startup/references/ReferencesHtmlScraperGateway.js": function(e, t, r) {
                var s = r("./src/mobile.startup/references/ReferencesGateway.js"),
                    n = r("./src/mobile.startup/mfExtend.js"),
                    a = r("./src/mobile.startup/util.js");

                function o() {
                    s.apply(this,
                        arguments)
                }
                n(o, s, {
                    EXTERNAL_LINK_CLASS: "external--reference",
                    getReferenceFromContainer: function(e, t) {
                        var r, n = a.Deferred();
                        return (r = t.find("#" + a.escapeSelector(e.substr(1)))).length ? (r.find(".external").addClass(this.EXTERNAL_LINK_CLASS), n.resolve({
                            text: r.html()
                        })) : n.reject(s.ERROR_NOT_EXIST), n.promise()
                    },
                    getReference: function(e, t, r) {
                        return this.getReferenceFromContainer(decodeURIComponent(e), r.$el.find("ol.references"))
                    }
                }), e.exports = o
            },
            "./src/mobile.startup/references/references.js": function(e, t, r) {
                var s, n = r("./src/mobile.startup/Drawer.js"),
                    a = r("./src/mobile.startup/util.js"),
                    o = r("./src/mobile.startup/icons.js"),
                    i = r("./src/mobile.startup/references/ReferencesGateway.js"),
                    l = r("./src/mobile.startup/Icon.js");

                function c(e) {
                    return function(t) {
                        var r = t.target;
                        if ("A" === r.tagName) return e(r.getAttribute("href"), r.textContent), !1
                    }
                }

                function u(e) {
                    var t = e.error ? new l({
                        name: "error",
                        hasText: !0,
                        isSmall: !0
                    }).getClassName() : "";
                    return new n(a.extend({
                        closeOnScroll: !1,
                        className: "drawer position-fixed text references-drawer",
                        events: {
                            "click sup": e.onNestedReferenceClick && c(e.onNestedReferenceClick)
                        },
                        children: [a.parseHTML("<div>").addClass("references-drawer__header").append([new l({
                            isSmall: !0,
                            name: "citation-invert",
                            additionalClassNames: "references-drawer__title",
                            hasText: !0,
                            label: mw.msg("mobile-frontend-references-citation")
                        }).$el, o.cancel("gray").$el]), a.parseHTML("<div>").addClass(t).append(a.parseHTML("<sup>").text(e.title), e.text ? a.parseHTML("<span>").html(e.text) : o.spinner().$el)]
                    }, e))
                }
                s = {
                    test: {
                        makeOnNestedReferenceClickHandler: c
                    },
                    referenceDrawer: u,
                    showReference: function(e, t, r, n, a) {
                        return a.getReference(e, t, n).then(function(e) {
                            var o = u({
                                title: r,
                                text: e.text,
                                onNestedReferenceClick: function(e, r) {
                                    s.showReference(e, t, r, n, a), o.$el.remove()
                                }
                            });
                            o.show()
                        }, function(e) {
                            e !== i.ERROR_NOT_EXIST && u({
                                error: !0,
                                title: r,
                                text: mw.msg("mobile-frontend-references-citation-error")
                            }).show()
                        })
                    }
                }, e.exports = s
            },
            "./src/mobile.startup/rlModuleLoader.js": function(e, t, r) {
                var s = r(
                        "./src/mobile.startup/loadingOverlay.js"),
                    n = r("./src/mobile.startup/util.js");
                e.exports = {
                    newLoadingOverlay: function() {
                        return s()
                    },
                    loadModule: function(e, t, r) {
                        var s = this.newLoadingOverlay();

                        function a() {
                            !t && r && s.hide()
                        }
                        return (r = void 0 === r || r) && (s.appendTo(document.body), s.show()), mw.loader.using(e).then(function() {
                            return a(), s
                        }, function() {
                            return a(), n.Deferred().reject().promise()
                        })
                    }
                }
            },
            "./src/mobile.startup/search/MobileWebSearchLogger.js": function(e, t) {
                function r() {
                    this.userSessionToken = null, this.searchSessionToken = null
                }
                r.prototype = {
                    _newUserSession: function() {
                        this.userSessionToken = mw.user.generateRandomSessionId()
                    },
                    _newSearchSession: function() {
                        this.searchSessionToken = mw.user.generateRandomSessionId(), this.searchSessionCreatedAt = (new Date).getTime()
                    },
                    onSearchShow: function() {
                        this._newUserSession()
                    },
                    onSearchStart: function() {
                        this._newSearchSession(), mw.track("mf.schemaMobileWebSearch", {
                            action: "session-start",
                            userSessionToken: this.userSessionToken,
                            searchSessionToken: this.searchSessionToken,
                            timeOffsetSinceStart: 0
                        })
                    },
                    onSearchResults: function(e) {
                        var t = (new Date).getTime() - this.searchSessionCreatedAt;
                        mw.track("mf.schemaMobileWebSearch", {
                            action: "impression-results",
                            resultSetType: "prefix",
                            numberOfResults: e.results.length,
                            userSessionToken: this.userSessionToken,
                            searchSessionToken: this.searchSessionToken,
                            timeToDisplayResults: t,
                            timeOffsetSinceStart: t
                        })
                    },
                    onSearchResultClick: function(e) {
                        var t = (new Date).getTime() - this.searchSessionCreatedAt;
                        mw.track("mf.schemaMobileWebSearch", {
                            action: "click-result",
                            clickIndex: e.resultIndex + 1,
                            userSessionToken: this.userSessionToken,
                            searchSessionToken: this.searchSessionToken,
                            timeOffsetSinceStart: t
                        })
                    }
                }, r.register = function(e) {
                    var t = new r;
                    e.on("search-show", t.onSearchShow.bind(t)), e.on("search-start", t.onSearchStart.bind(t)), e.on("search-results", t.onSearchResults.bind(t)), e.on("search-result-click", t.onSearchResultClick.bind(t))
                }, e.exports = r
            },
            "./src/mobile.startup/search/SearchOverlay.js": function(e, t, r) {
                var s = r("./src/mobile.startup/mfExtend.js"),
                    n = r(
                        "./src/mobile.startup/Overlay.js"),
                    a = r("./src/mobile.startup/util.js"),
                    o = r("./src/mobile.startup/icons.js"),
                    i = r("./src/mobile.startup/headers.js").formHeader,
                    l = r("./src/mobile.startup/Icon.js"),
                    c = r("./src/mobile.startup/search/SearchResultsView.js"),
                    u = r("./src/mobile.startup/watchstar/WatchstarPageList.js");

                function p(e) {
                    var t = a.extend(!0, {
                        headerChrome: !0,
                        isBorderBox: !1,
                        className: "overlay search-overlay",
                        headers: [i(a.template('<div class="overlay-title">\n<form method="get" action="{{action}}" class="search-box">\n\t<input class="search" type="search" name="search" autocomplete="off" placeholder="{{placeholderMsg}}" aria-label="{{placeholderMsg}}" value="{{searchTerm}}">\n</form>\n</div>\n\t\t\t\t\t').render({
                            placeholderMsg: e.placeholderMsg,
                            action: e.action || mw.config.get("wgScript")
                        }), [o.cancel()], !1)],
                        events: {
                            "input input": "onInputInput",
                            "click .clear": "onClickClear",
                            "click .search-content": "onClickSearchContent",
                            "click .overlay-content": "onClickOverlayContent",
                            "click .overlay-content > div": "onClickOverlayContentDiv",
                            "touchstart .results": "hideKeyboardOnScroll",
                            "mousedown .results": "hideKeyboardOnScroll",
                            "click .results a": "onClickResult"
                        }
                    }, e);
                    n.call(this, t), this.api = t.api, this.gateway = t.gateway || new t.gatewayClass(this.api), this.router = t.router
                }
                s(p, n, {
                    defaults: a.extend({}, n.prototype.defaults, {
                        clearIcon: new l({
                            tagName: "button",
                            name: "search-clear",
                            isSmall: !0,
                            label: mw.msg("mobile-frontend-clear-search"),
                            additionalClassNames: "clear"
                        }),
                        searchTerm: ""
                    }),
                    onInputInput: function() {
                        this.performSearch(), this.$clear.toggle("" !== this.$input.val())
                    },
                    onClickClear: function() {
                        return this.$input.val("").trigger("focus"), this.performSearch(), this.$clear.hide(), !1
                    },
                    onClickSearchContent: function() {
                        var e = a.getDocument().find("body"),
                            t = this.$el.find("form");
                        this.parseHTML("<input>").attr({
                            type: "hidden",
                            name: "fulltext",
                            value: "search"
                        }).appendTo(t), setTimeout(function() {
                            t.appendTo(e), t.trigger("submit")
                        }, 0)
                    },
                    onClickOverlayContent: function() {
                        this.$el.find(".cancel").
                        trigger("click")
                    },
                    onClickOverlayContentDiv: function(e) {
                        e.stopPropagation()
                    },
                    hideKeyboardOnScroll: function() {
                        this.$input.trigger("blur")
                    },
                    onClickResult: function(e) {
                        var t = this.$el.find(e.currentTarget),
                            r = t.closest("li");
                        this.emit("search-result-click", {
                            result: r,
                            resultIndex: this.$results.index(r),
                            originalEvent: e
                        }), e.preventDefault(), this.router.back().then(function() {
                            window.location.href = t.attr("href")
                        })
                    },
                    postRender: function() {
                        var e, t = this,
                            r = this.options,
                            s = new c({
                                searchContentLabel: mw.msg("mobile-frontend-search-content"),
                                noResultsMsg: mw.msg("mobile-frontend-search-no-results"),
                                searchContentNoResultsMsg: mw.message("mobile-frontend-search-content-no-results").parse()
                            });

                        function a() {
                            t.$spinner.hide(), clearTimeout(e)
                        }
                        this.$el.find(".overlay-content").append(s.$el), n.prototype.postRender.call(this), this.$el.find(".overlay-title").append(r.clearIcon.$el), this.$input = this.$el.find("input"), this.$clear = this.$el.find(".clear"), this.$searchContent = s.$el.hide(), this.$resultContainer = s.$el.find(
                            ".results-list-container"), this.$spinner = s.$el.find(".spinner-container"), this.on("search-start", function(r) {
                            e && a(), e = setTimeout(function() {
                                t.$spinner.show()
                            }, 2e3 - r.delay)
                        }), this.on("search-results", a), "" === t.$input.val() && this.$clear.hide()
                    },
                    showKeyboard: function() {
                        var e = this.$input.val().length;
                        this.$input.trigger("focus"), this.$input[0].setSelectionRange && this.$input[0].setSelectionRange(e, e)
                    },
                    show: function() {
                        n.prototype.show.apply(this, arguments), this.showKeyboard(), this.emit("search-show")
                    },
                    performSearch: function() {
                        var e = this,
                            t = this.api,
                            r = this.$input.val(),
                            s = this.gateway.isCached(r) ? 0 : 300;
                        r !== this.lastQuery && (e._pendingQuery && e._pendingQuery.abort(), clearTimeout(this.timer), r.length ? this.timer = setTimeout(function() {
                            var n;
                            e.emit("search-start", {
                                query: r,
                                delay: s
                            }), n = e.gateway.search(r), e._pendingQuery = n.then(function(r) {
                                r && r.query === e.$input.val() && (e.$el.toggleClass("no-results", 0 === r.results.length), e.$searchContent.show().find("p").hide().filter(r.results.length ? ".with-results" :
                                    ".without-results").show(), new u({
                                    api: t,
                                    funnel: "search",
                                    pages: r.results,
                                    el: e.$resultContainer
                                }), e.$results = e.$resultContainer.find("li"), e.emit("search-results", {
                                    results: r.results
                                }))
                            }).promise({
                                abort: function() {
                                    n.abort()
                                }
                            })
                        }, s) : e.resetSearch(), this.lastQuery = r)
                    },
                    resetSearch: function() {
                        this.$el.find(".overlay-content").children().hide()
                    }
                }), e.exports = p
            },
            "./src/mobile.startup/search/SearchResultsView.js": function(e, t, r) {
                function s(e) {
                    return (s = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(e) {
                        return typeof e
                    } : function(e) {
                        return e && "function" == typeof Symbol && e.constructor === Symbol && e !== Symbol.prototype ? "symbol" : typeof e
                    })(e)
                }

                function n(e, t) {
                    for (var r = 0; r < t.length; r++) {
                        var s = t[r];
                        s.enumerable = s.enumerable || !1, s.configurable = !0, "value" in s && (s.writable = !0), Object.defineProperty(e, s.key, s)
                    }
                }

                function a(e, t) {
                    return !t || "object" !== s(t) && "function" != typeof t ? function(e) {
                        if (void 0 === e) throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
                        return e
                    }(e) : t
                }

                function o(e, t, r) {
                    return (o = "undefined" != typeof Reflect && Reflect.get ? Reflect.get : function(e, t, r) {
                        var s = function(e, t) {
                            for (; !Object.prototype.hasOwnProperty.call(e, t) && null !== (e = i(e)););
                            return e
                        }(e, t);
                        if (s) {
                            var n = Object.getOwnPropertyDescriptor(s, t);
                            return n.get ? n.get.call(r) : n.value
                        }
                    })(e, t, r || e)
                }

                function i(e) {
                    return (i = Object.setPrototypeOf ? Object.getPrototypeOf : function(e) {
                        return e.__proto__ || Object.getPrototypeOf(e)
                    })(e)
                }

                function l(e, t) {
                    return (l = Object.setPrototypeOf || function(e, t) {
                        return e.__proto__ = t, e
                    })(e, t)
                }
                var c = r("./src/mobile.startup/View.js"),
                    u = r("./src/mobile.startup/Icon.js"),
                    p = r("./src/mobile.startup/Anchor.js"),
                    h = r("./src/mobile.startup/icons.js"),
                    m = mw.config.get("wgCirrusSearchFeedbackLink"),
                    d = h.spinner().$el,
                    f = r("./src/mobile.startup/util.js"),
                    g = function(e) {
                        "use strict";

                        function t() {
                            return function(e, t) {
                                if (!(e instanceof t)) throw new TypeError("Cannot call a class as a function")
                            }(this, t), a(this, i(t).apply(this, arguments))
                        }
                        return function(e, t) {
                                if ("function" != typeof t &&
                                    null !== t) throw new TypeError("Super expression must either be null or a function");
                                e.prototype = Object.create(t && t.prototype, {
                                    constructor: {
                                        value: e,
                                        writable: !0,
                                        configurable: !0
                                    }
                                }), t && l(e, t)
                            }(t, c),
                            function(e, t, r) {
                                t && n(e.prototype, t), r && n(e, r)
                            }(t, [{
                                key: "preRender",
                                value: function() {
                                    m && (this.options.feedback = {
                                        prompt: mw.msg("mobile-frontend-search-feedback-prompt")
                                    })
                                }
                            }, {
                                key: "postRender",
                                value: function(e) {
                                    o(i(t.prototype), "postRender", this).call(this, e), this.$el.find(".search-content li").append(new u({
                                        tagName: "a",
                                        href: "#",
                                        name: "search-content",
                                        label: mw.msg("mobile-frontend-search-content")
                                    }).$el), this.$el.find(".spinner-container").append(d), m && this.$el.find(".search-feedback").append(new p({
                                        label: mw.msg("mobile-frontend-search-feedback-link-text"),
                                        href: m
                                    }).$el)
                                }
                            }, {
                                key: "isTemplateMode",
                                get: function() {
                                    return !0
                                }
                            }, {
                                key: "template",
                                get: function() {
                                    return f.template(
                                        '\n<div class="search-results-view">\n\t<div class="search-content overlay-header search-results-view">\n\t\t<ul>\n\t\t\t<li>{{! search content icon goes here }}</li>\n\t\t</ul>\n\t\t<div class="caption">\n\t\t\t<p class="with-results">{{searchContentLabel}}</p>\n\t\t\t<p class="without-results">{{noResultsMsg}}</p>\n\t\t\t<p class="without-results">{{{searchContentNoResultsMsg}}}</p>\n\t\t</div>\n\t</div>\n\t<div class="spinner-container position-fixed"></div>\n\t<div class="results">\n\t\t<div class="results-list-container"></div>\n\t\t{{#feedback}}\n\t\t\t<div class="search-feedback">\n\t\t\t\t{{prompt}}\n\t\t\t</div>\n\t\t{{/feedback}}\n\t</div>\n</div>')
                                }
                            }]), t
                    }();
                e.exports = g
            },
            "./src/mobile.startup/search/schemaMobileWebSearch.js": function(e, t, r) {
                var s = r("./src/mobile.startup/context.js");
                e.exports = {
                    subscribeMobileWebSearchSchema: function() {
                        mw.loader.using(["ext.eventLogging"]).then(function() {
                            var e = new(0, mw.eventLog.Schema)("MobileWebSearch", mw.config.get(
                                "wgMFSchemaSearchSampleRate", .001), {
                                platform: "mobileweb",
                                platformVersion: s.getMode()
                            });
                            mw.trackSubscribe("mf.schemaMobileWebSearch", function(t, r) {
                                e.log(r)
                            })
                        })
                    }
                }
            },
            "./src/mobile.startup/talk/overlay.js": function(e, t, r) {
                var s = r("./src/mobile.startup/moduleLoaderSingleton.js"),
                    n = r("./src/mobile.startup/promisedView.js"),
                    a = r("./src/mobile.startup/Anchor.js"),
                    o = r("./src/mobile.startup/util.js"),
                    i = r("./src/mobile.startup/headers.js").header,
                    l = r("./src/mobile.startup/Overlay.js");
                e.exports = function(e, t) {
                    var r = mw.user;
                    return l.make({
                        headers: [i("<strong>" + mw.msg("mobile-frontend-talk-overlay-header") + "</strong>", r.isAnon() ? [] : [new a({
                            href: "#/talk/new",
                            additionalClassNames: "continue",
                            label: mw.msg("mobile-frontend-talk-add-overlay-submit")
                        })])],
                        footerAnchor: new a({
                            progressive: !0,
                            href: mw.util.getUrl(e),
                            additionalClassNames: "footer-link talk-fullpage",
                            label: mw.msg("mobile-frontend-talk-fullpage")
                        }).options,
                        className: "talk-overlay overlay"
                    }, n(o.Promise.all([t.getSections(e), mw.loader.using(
                        "mobile.talk.overlays")]).then(function(e) {
                        return s.require("mobile.talk.overlays/talkBoard")(e)
                    })))
                }
            },
            "./src/mobile.startup/toc/TableOfContents.js": function(e, t, r) {
                var s = r("./src/mobile.startup/View.js"),
                    n = r("./src/mobile.startup/mfExtend.js"),
                    a = r("./src/mobile.startup/util.js"),
                    o = r("./src/mobile.startup/Icon.js");

                function i(e) {
                    s.call(this, a.extend({
                        className: "toc-mobile",
                        contentsMsg: mw.msg("toc")
                    }, e))
                }
                n(i, s, {
                    templatePartials: {
                        tocHeading: a.template('\n<li>\n\t<a href="#{{anchor}}">{{{line}}}</a>\n\t<ul>\n\t\t{{#subsections}}\n\t\t{{>tocHeading}}\n\t\t{{/subsections}}\n\t</ul>\n</li>\n\t\t')
                    },
                    template: a.template('\n<h2><span>{{contentsMsg}}</span></h2>\n<div>\n\t<ul>\n\t{{#sections}}\n\t{{>tocHeading}}\n\t{{/sections}}\n\t</ul>\n</div>\n<div style="clear:both;"></div>\n\t'),
                    postRender: function() {
                        new o({
                            name: "toc",
                            additionalClassNames: "toc-button"
                        }).$el.prependTo(this.$el.find("h2"))
                    }
                }), e.exports = i
            }
        },
        [
            ["./src/mobile.startup/mobile.startup.js", 0, 1]
        ]
    ]);
}, {
    "css": [
        ".mf-mw-ui-icon-rotate-anti-clockwise:before{-webkit-transform:rotate(-90deg);-moz-transform:rotate(-90deg);transform:rotate(-90deg)}.mf-mw-ui-icon-rotate-clockwise:before{-webkit-transform:rotate(90deg);-moz-transform:rotate(90deg);transform:rotate(90deg)}.mf-mw-ui-icon-rotate-flip:before{-webkit-transform:scaleY(-1);-moz-transform:scaleY(-1);transform:scaleY(-1)}.rtl .mf-mw-ui-icon-rotate-anti-clockwise:before{-webkit-transform:rotate(90deg);-moz-transform:rotate(90deg);transform:rotate(90deg)}.rtl .mf-mw-ui-icon-rotate-clockwise:before{-webkit-transform:rotate(-90deg);-moz-transform:rotate(-90deg);transform:rotate(-90deg)}     .panel \u003E p{padding:1em}.panel input,.panel textarea{border-radius:0;padding:16px 1em}#bodyContent .panel{background-color:#eaecf0;clear:both;margin-top:1em;text-align:center}.promised-view{position:absolute;top:0;left:0;right:0;bottom:0;display:flex;justify-content:center;align-items:center}.overlay-content .loading.spinner,.media-viewer .loading.spinner,.content .loading.spinner{text-align:center;background-position:50%;margin:8px auto}.drawer{text-align:center;padding-top:0;padding-bottom:1em;max-width:500px;margin:0 auto}.drawer.text{line-height:1;font-size:0.9em;text-align:left;padding-top:0.5em}.drawer p{line-height:1.4;margin-top:0.5em}.drawer p,.drawer a:not(:last-child),.drawer .mw-ui-button{margin-bottom:1em}.drawer .mw-ui-icon.cancel{display:block;margin:0.5em auto 0.625em auto}.has-drawer--with-scroll-locked{overflow:hidden}.has-drawer.drawer-visible .transparent-shield{visibility:visible;opacity:0.5}.overlay-enabled .drawer{display:none !important}.overlay.overlay-loading{display:flex;align-items:center;justify-content:center;background-color:rgba(255,255,255,0.5)}.overlay.overlay-loading .header{display:none}.overlay.overlay-loading .overlay-content{overflow:hidden} .overlay-enabled #mw-mf-page-center{overflow:hidden;display:block}.overlay-enabled .overlay,.overlay-enabled #mw-mf-page-center{height:100%}.hidden{display:none !important}.overlay{position:absolute;top:0;left:0;width:100%;background:#fff;z-index:1;display:none}.overlay.visible{display:block}.overlay input,.overlay textarea{font-size:16px}.overlay .captcha-word,.overlay .summary{margin:0 0 0.7em;width:100%}.overlay .wikitext-editor{min-height:50%;line-height:1.5;border:0}.overlay .panel{padding-top:12px;padding-bottom:12px;border-bottom:1px solid #eaecf0}.overlay .content .cancel{display:block;margin:1em auto}.overlay .content-header{background-color:#f8f9fa;border-bottom:1px solid #eaecf0;padding-top:20px;padding-bottom:20px;line-height:inherit}.overlay-header .overlay-title{width:100%}.overlay-header .header-action a,.overlay-header .header-action button{display:table-cell;vertical-align:middle;width:auto;padding:0 1.2em;font-weight:bold;white-space:nowrap;text-decoration:none;border-radius:0;cursor:pointer;position:relative}.overlay-header .header-action a:before,.overlay-header .header-action button:before{top:0}.overlay-header .header-action a[disabled],.overlay-header .header-action button[disabled]{opacity:0.5}.overlay-header .header-action a:focus,.overlay-header .header-action button:focus{outline:0}.overlay-header .header-action .continue,.overlay-header .header-action .submit,.overlay-header .header-action .progressive{background-color:#3366cc;color:#fff}.overlay-header .header-action .continue[disabled],.overlay-header .header-action .submit[disabled],.overlay-header .header-action .progressive[disabled]{background-color:#c8ccd1;opacity:1}.overlay-header .header-action .oo-ui-widget{margin-right:1em}.overlay-header button{cursor:pointer}.overlay-header .submit{background-color:#3366cc;color:#fff}.overlay-header h2{display:table;width:100%;font-size:1em}.overlay-header h2 \u003E *{width:1em;display:table-cell;padding-right:0.4em}.overlay-header h2 span{width:auto;max-width:1em}.overlay-header \u003E ul,.overlay-header \u003E div{display:table-cell;vertical-align:middle}.overlay-header \u003E ul li{display:block}.overlay-header-container,.overlay-footer-container{width:100%;background:#fff;z-index:2}.overlay-header-container.position-fixed,.overlay-footer-container.position-fixed{left:0;right:0}.overlay-header-container{top:0}.overlay-footer-container{background-color:#fff;bottom:0;border-top:1px solid #c8ccd1}.overlay-footer-container a{display:block;padding:1em 1em 1em 10px;text-align:center}.overlay-bottom{border-top:1px solid #c8ccd1;top:auto;bottom:0;height:auto !important;background:#f8f9fa}.overlay-bottom .overlay-header-container{background:#f8f9fa}@media all and (min-width:720px){.overlay .panel{padding-top:12px;padding-bottom:12px}}@media print{.collapsible-heading .indicator,.collapsible-heading .edit-page{display:none}.client-js .collapsible-block{display:block}}.language-overlay{background-color:#eaecf0}.language-overlay .overlay-content{background-color:#eaecf0;position:relative;min-height:100%}.drawer.references-drawer{background-color:#000000;color:#c8ccd1;max-height:50%;overflow-y:auto;padding:20px}.drawer.references-drawer .mw-ui-icon:before{opacity:0.5}.drawer.references-drawer a{color:#69f}.drawer.references-drawer .references-drawer__header{padding-bottom:20px}.drawer.references-drawer .references-drawer__header:after{content:'';display:table;clear:both}.drawer.references-drawer .references-drawer__title{color:#72777d;cursor:default;letter-spacing:0.2em;font-size:0.75em;padding-top:0.25em;text-transform:uppercase}.drawer.references-drawer .cancel{cursor:pointer;font-size:0.8em;margin:-1em -1em -1em 0; padding:1em 0;position:absolute;right:20px;top:20px}.drawer.references-drawer .mw-cite-backlink{display:none}.drawer.references-drawer .reference-text{line-height:1.4}@media all and (min-width:720px){.drawer.references-drawer{max-height:400px}}.overlay.search-overlay{background:#fff}.overlay.search-overlay a.mw-ui-icon{display:inline-block}.overlay.search-overlay .spinner-container{background-color:#ffffff;bottom:0;display:none;left:0;opacity:0.7;right:0;z-index:2}.overlay.search-overlay .spinner-container .spinner{display:block;left:50%;margin-left:-9px;position:absolute;top:10%}.overlay.search-overlay .search-box{display:block}.overlay.search-overlay .results,.overlay.search-overlay .search-feedback{background-color:#fff}.overlay.search-overlay .overlay-header{background-color:transparent}.overlay.search-overlay .header-cancel{display:none}.overlay.search-overlay .header-action{width:auto}.overlay.search-overlay .header-action button:before{top:auto}.overlay.search-overlay .overlay-title{position:relative;padding-left:15px}.overlay.search-overlay .header input{padding-right:17.5px}.overlay.search-overlay .header input::-ms-clear{display:none}.overlay.search-overlay .overlay-content{position:relative;height:100%;width:100%}.overlay.search-overlay .search-content{border-bottom:1px solid #c8ccd1;cursor:pointer}.overlay.search-overlay .search-content .caption{padding:1em 0}.overlay.search-overlay .search-content.overlay-header{padding:0}.overlay.search-overlay .results{box-shadow:0 3px 3px 0 rgba(117,117,117,0.3)}.overlay.search-overlay .results li:last-child{border-bottom:0}.overlay.search-overlay .results h2{font:inherit}.overlay.search-overlay li.page-summary{display:table;height:70px;width:100%}.overlay.search-overlay li.page-summary .title{display:table-cell;vertical-align:middle}.overlay.search-overlay li.page-summary h3{margin:0;font-weight:normal}.overlay.search-overlay li.page-summary h3 strong{text-decoration:none}.overlay.search-overlay li.page-summary .wikidata-description{font-size:0.8em;margin-top:0.5em}.overlay.search-overlay .search-feedback{box-shadow:0 3px 3px 0 rgba(117,117,117,0.3);border-top:1px solid #c8ccd1;font-size:0.8em;padding:0.5em 1em}.overlay.search-overlay.no-results .search-feedback{border-top:0}@-webkit-keyframes fadeIn{from{opacity:0}to{opacity:1}}@keyframes fadeIn{from{opacity:0}to{opacity:1}}@-webkit-keyframes fadeOut{from{opacity:1}to{opacity:0}}@keyframes fadeOut{from{opacity:1}to{opacity:0}}.animations .overlay.search-overlay.visible{-webkit-animation:fadeIn 0.5s;-moz-animation:fadeIn 0.5s;animation:fadeIn 0.5s}.animations .overlay.search-overlay.overlay-ios{-webkit-animation:none;-moz-animation:none;animation:none}.client-js .toc-mobile{background-color:#f8f9fa;border:solid 1px #eaecf0}.client-js .toc-mobile .toc-button{float:left;display:block;font-size:0.8em}.client-js .toc-mobile .collapsible-heading{visibility:visible;background-position:right center}.client-js .toc-mobile .collapsible-heading .indicator{margin:0.2em 0.4em 0 0;float:right;font-size:0.6em}.client-js .toc-mobile .collapsible-block{border-bottom:0;margin-left:52px;margin-right:24px;font-size:0.7em;width:auto}.client-js .toc-mobile .collapsible-block \u003E ul{padding-bottom:1.3em}.client-js .toc-mobile .collapsible-block ul{list-style:none;margin-bottom:1px}.client-js .toc-mobile .collapsible-block ul li{padding-top:0.8em;line-height:1.2}.client-js .toc-mobile .collapsible-block ul li a{white-space:pre-wrap;word-break:break-word}.animations .watch-this-article:before{-webkit-backface-visibility:hidden;-webkit-transition:-webkit-transform 0.5s;-moz-transition:-moz-transform 0.5s;transition:transform 0.5s}.animations .watch-this-article.watched:before{-webkit-transform:rotate(72deg);-moz-transform:rotate(72deg);transform:rotate(72deg)}.overlay.media-viewer{background:#000;padding-top:0}.overlay.media-viewer .overlay-content{height:100%;width:100%;margin:0;max-width:none}.overlay.media-viewer .header-container{background:transparent;border-bottom:0}.overlay.media-viewer .main{height:100%;position:relative;width:100%}.overlay.media-viewer .cancel{position:absolute;right:0;top:10px;z-index:2}.cloaked-element{opacity:0;position:absolute;top:0;left:0;right:0;bottom:0}.position-fixed{position:fixed !important}.touch-events :focus{outline:0}        .drawer{background-color:#f8f9fa;position:absolute;width:100%}.mw-notification,.toast,.drawer{-webkit-box-sizing:border-box;-moz-box-sizing:border-box;box-sizing:border-box;position:fixed;bottom:0;left:0;right:0;box-shadow:0 -1px 8px 0 rgba(0,0,0,0.35);word-wrap:break-word;z-index:4;display:none}.mw-notification.visible,.toast.visible,.drawer.visible,.mw-notification.mw-notification-visible,.toast.mw-notification-visible,.drawer.mw-notification-visible{display:block}.mw-notification,.toast{font-size:0.875em;padding:0.9em 1em;background-color:#222222;color:#fff;margin:0 10% 20px;width:80%;text-align:center;border-radius:2px}.mw-notification.mw-notification-type-error,.toast.mw-notification-type-error{background-image:url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2224%22 height=%2224%22 viewBox=%220 0 24 24%22%3E%3Cpath fill=%22%23d33%22 fill-rule=%22evenodd%22 d=%22M11.643 22.364c1.234 0 2.235-.956 2.235-2.136h-4.47c0 1.18 1 2.136 2.234 2.136zm7.25-12.284v3.998l1.77 3.603v1.1H2.623v-1.1l1.77-3.602V10.08c0-2.894 1.822-5.41 4.475-6.47.26-1.283 1.415-2.227 2.773-2.227s2.51.944 2.776 2.227c2.653 1.06 4.477 3.576 4.477 6.47zM12.92 4.974h-2.554c-2.438.553-4.255 2.64-4.255 5.14v4.474l-1.7 2.44h14.47l-1.702-2.44v-4.474c.024-4.076-3.616-5.09-4.255-5.14z%22/%3E%3C/svg%3E\");background-size:24px;background-position:16px 50%;background-repeat:no-repeat;padding-left:5%;width:75%;border:0}.mw-notification a,.toast a{color:#ffffff;text-decoration:underline}.mw-notification-area{z-index:4;position:fixed;bottom:0;width:100%}.animations .mw-notification,.animations .drawer{display:block;visibility:hidden; -webkit-transform:translate(0,100px);-moz-transform:translate(0,100px);transform:translate(0,100px);bottom:100px;opacity:0}.animations .mw-notification.animated,.animations .drawer.animated,.animations .mw-notification.mw-notification-tag-toast,.animations .drawer.mw-notification-tag-toast{-webkit-backface-visibility:hidden;-webkit-transition:-webkit-transform 0.25s,opacity 0.25s,visibility 0s 0.25s,bottom 0s 0.25s;-moz-transition:-moz-transform 0.25s,opacity 0.25s,visibility 0s 0.25s,bottom 0s 0.25s;transition:transform 0.25s,opacity 0.25s,visibility 0s 0.25s,bottom 0s 0.25s}.animations .mw-notification.visible,.animations .drawer.visible,.animations .mw-notification.mw-notification-visible,.animations .drawer.mw-notification-visible{bottom:0;-webkit-backface-visibility:hidden;-webkit-transition:-webkit-transform 0.25s,opacity 0.25s;-moz-transition:-moz-transform 0.25s,opacity 0.25s;transition:transform 0.25s,opacity 0.25s;visibility:visible;opacity:1;-webkit-transform:translate(0,0);-moz-transform:translate(0,0);transform:translate(0,0)}.overlay{padding-top:3.375em}.overlay textarea{-webkit-box-sizing:border-box;-moz-box-sizing:border-box;box-sizing:border-box;width:100%;padding:10px 16px 10px 16px;resize:none}.overlay \u003E ul,.overlay button{width:3.375em}.overlay .license{font-size:0.875em;color:#72777d;margin-top:0.5em;line-height:1.4}.overlay-header .header-action \u003E a,.overlay-header .header-action \u003E button{height:3.375em}.overlay-header .overlay-title{padding:0.15em 0}.overlay-header .overlay-title:last-child{padding-right:1em}@media all and (min-width:1000px){.overlay-header{max-width:995.3px}}.client-js .collapsible-heading,.client-js .collapsible-block{clear:left}.client-js .collapsible-heading{cursor:pointer;position:relative}.client-js .collapsible-heading .indicator{float:left;margin-top:0.7em;font-size:0.4em}.overlay.search-overlay .spinner-container{top:3.375em}.overlay.search-overlay .clear{position:absolute;top:0.9em;right:0}@media all and (min-width:720px){.overlay.search-overlay .search-box{display:table-cell}.overlay.search-overlay .spinner-container,.overlay.search-overlay .search-content,.overlay.search-overlay .results{width:23.4375em;margin-left:13.5625em}.overlay.search-overlay .overlay-title{width:23.4375em;padding-left:13.5625em}.overlay.search-overlay .spinner-container{left:auto;right:auto}.overlay.search-overlay ul{width:auto}}@media all and (min-width:1000px){.overlay.search-overlay .overlay-content{max-width:995.3px}}@media screen and (min-width:1000px){.language-overlay .overlay-content{margin:0;min-width:100%}}"
    ]
}, {
    "echo-mark-all-as-read": "Mark all as read",
    "echo-overlay-link": "All notifications",
    "mobile-frontend-clear-search": "Clear",
    "mobile-frontend-editor-licensing": "By saving changes, you agree to release your contribution under the $1 {{PLURAL:$2|license|licenses}}.",
    "mobile-frontend-editor-licensing-with-terms": "By saving changes, you agree to the $1 and agree to release your contribution under the $2 {{PLURAL:$3|license|licenses}}.",
    "mobile-frontend-editor-publish": "Publish",
    "mobile-frontend-editor-save": "Save",
    "mobile-frontend-editor-terms-link": "[$1 Terms of Use]",
    "mobile-frontend-joined-days": "{{GENDER:$1|Joined}} {{PLURAL:$2|$2 day|$2 days}} ago",
    "mobile-frontend-joined-hours": "{{GENDER:$1|Joined}} {{PLURAL:$2|$2 hour|$2 hours}} ago",
    "mobile-frontend-joined-just-now": "{{GENDER:$1|Joined}} just now",
    "mobile-frontend-joined-minutes": "{{GENDER:$1|Joined}} {{PLURAL:$2|$2 minute|$2 minutes}} ago",
    "mobile-frontend-joined-months": "{{GENDER:$1|Joined}} {{PLURAL:$2|$2 month|$2 months}} ago",
    "mobile-frontend-joined-seconds": "{{GENDER:$1|Joined}} {{PLURAL:$2|$2 second|$2 seconds}} ago",
    "mobile-frontend-joined-years": "{{GENDER:$1|Joined}} {{PLURAL:$2|$2 year|$2 years}} ago",
    "mobile-frontend-language-heading": "\u003Cstrong\u003ELanguages\u003C/strong\u003E",
    "mobile-frontend-last-modified-with-user-days": "[$4 Last {{GENDER:$1|edited}} {{PLURAL:$3|$3 day|$3 days}} ago] by {{PLURAL:$5|[$6 $2]|0=an anonymous user}}",
    "mobile-frontend-last-modified-with-user-hours": "[$4 Last {{GENDER:$1|edited}} {{PLURAL:$3|$3 hour|$3 hours}} ago] by {{PLURAL:$5|[$6 $2]|0=an anonymous user}}",
    "mobile-frontend-last-modified-with-user-just-now": "[$3 Last {{GENDER:$1|edited}}] just now by {{PLURAL:$4|[$5 $2]|0=an anonymous user}}",
    "mobile-frontend-last-modified-with-user-minutes": "[$4 Last {{GENDER:$1|edited}} {{PLURAL:$3|$3 minute|$3 minutes}} ago] by {{PLURAL:$5|[$6 $2]|0=an anonymous user}}",
    "mobile-frontend-last-modified-with-user-months": "[$4 Last {{GENDER:$1|edited}} {{PLURAL:$3|$3 month|$3 months}} ago] by {{PLURAL:$5|[$6 $2]|0=an anonymous user}}",
    "mobile-frontend-last-modified-with-user-seconds": "[$4 Last {{GENDER:$1|edited}} {{PLURAL:$3|$3 second|$3 seconds}} ago] by {{PLURAL:$5|[$6 $2]|0=an anonymous user}}",
    "mobile-frontend-last-modified-with-user-years": "[$4 Last {{GENDER:$1|edited}} {{PLURAL:$3|$3 year|$3 years}} ago] by {{PLURAL:$5|[$6 $2]|0=an anonymous user}}",
    "mobile-frontend-loading-message": "Loading…",
    "mobile-frontend-overlay-close": "Close",
    "mobile-frontend-references-citation": "Citation",
    "mobile-frontend-references-citation-error": "An error occurred and it was not possible to load this citation.",
    "mobile-frontend-search-content": "Search within pages",
    "mobile-frontend-search-content-no-results": "\u003Cb\u003E{{int:mobile-frontend-search-content}}\u003C/b\u003E to see if this phrase appears anywhere.",
    "mobile-frontend-search-feedback-link-text": "Give us your feedback.",
    "mobile-frontend-search-feedback-prompt": "Couldn't find what you were looking for?",
    "mobile-frontend-search-no-results": "No page with this title.",
    "mobile-frontend-talk-add-overlay-submit": "Add discussion",
    "mobile-frontend-talk-fullpage": "Read as wiki page",
    "mobile-frontend-talk-overlay-header": "Talk",
    "mobile-frontend-watchlist-add": "Added $1 to your watchlist",
    "mobile-frontend-watchlist-cta": "Keep track of this page and all changes to it.",
    "mobile-frontend-watchlist-cta-button-login": "Log in",
    "mobile-frontend-watchlist-cta-button-signup": "Sign up",
    "mobile-frontend-watchlist-error": "There was a problem watching this page. Please try again.",
    "mobile-frontend-watchlist-please-wait": "Please wait, the watch action is taking longer than expected.",
    "mobile-frontend-watchlist-removed": "Removed $1 from your watchlist",
    "notifications": "Notifications",
    "toc": "Contents",
    "unwatch": "Unwatch",
    "watch": "Watch"
});
mw.loader.implement("mobile.init.icons@1s7x1uf", null, {
    "css": [
        ".oo-ui-icon-close,.mw-ui-icon-close:before{background-image:url(/w/load.php?modules=mobile.init.icons\u0026image=close\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2220%22 height=%2220%22 viewBox=%220 0 20 20%22%3E%3Ctitle%3Eclose%3C/title%3E%3Cpath d=%22M4.34 2.93l12.73 12.73-1.41 1.41L2.93 4.35z%22/%3E%3Cpath d=%22M17.07 4.34L4.34 17.07l-1.41-1.41L15.66 2.93z%22/%3E%3C/svg%3E\")}.oo-ui-image-invert.oo-ui-icon-close,.mw-ui-icon-close-invert:before{background-image:url(/w/load.php?modules=mobile.init.icons\u0026image=close\u0026variant=invert\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2220%22 height=%2220%22 viewBox=%220 0 20 20%22%3E%3Ctitle%3Eclose%3C/title%3E%3Cg fill=%22%23fff%22%3E%3Cpath d=%22M4.34 2.93l12.73 12.73-1.41 1.41L2.93 4.35z%22/%3E%3Cpath d=%22M17.07 4.34L4.34 17.07l-1.41-1.41L15.66 2.93z%22/%3E%3C/g%3E%3C/svg%3E\")}.oo-ui-image-progressive.oo-ui-icon-close,.mw-ui-icon-close-progressive:before{background-image:url(/w/load.php?modules=mobile.init.icons\u0026image=close\u0026variant=progressive\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2220%22 height=%2220%22 viewBox=%220 0 20 20%22%3E%3Ctitle%3Eclose%3C/title%3E%3Cg fill=%22%2336c%22%3E%3Cpath d=%22M4.34 2.93l12.73 12.73-1.41 1.41L2.93 4.35z%22/%3E%3Cpath d=%22M17.07 4.34L4.34 17.07l-1.41-1.41L15.66 2.93z%22/%3E%3C/g%3E%3C/svg%3E\")}"
    ]
});
mw.loader.implement("mobile.legacy.icons@1fn1nn2", null, {
    "css": [".mw-ui-icon-mf-spinner:before{background-image:url(/w/load.php?modules=mobile.legacy.icons\u0026image=mw-ui-icon-mf-spinner%3Abefore\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=mobile.legacy.icons\u0026image=mw-ui-icon-mf-spinner%3Abefore\u0026format=original\u0026skin=minerva\u0026version=0pqhkqt)}.list-thumb-none{background-image:url(/w/load.php?modules=mobile.legacy.icons\u0026image=list-thumb-none\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=mobile.legacy.icons\u0026image=list-thumb-none\u0026format=original\u0026skin=minerva\u0026version=0pqhkqt)}"]
});
mw.loader.implement("mobile.messageBox.styles@05g9509", null, {
    "css": [".previewnote p,.warningbox,.mw-revision{border:1px solid #c8ccd1;background-color:#eaecf0;color:#222222}.previewnote p,.successbox,.errorbox,.warningbox,.mw-revision{padding-top:1em;padding-bottom:1em;margin:0 0 1em}.errorbox{background-color:#fee7e6;color:#000000}.successbox{background-color:#d5fdf4;color:#000000}.successbox h2,.errorbox h2,.warningbox h2,.mw-revision h2{font:bold 100% -apple-system,'BlinkMacSystemFont','Segoe UI','Roboto','Lato','Helvetica','Arial',sans-serif;padding:0;margin:0 0 0.5em 0}.successbox a,.errorbox a,.warningbox a,.mw-revision a{margin-bottom:1em}"]
});
mw.loader.implement("mobile.pagelist.styles@0ptlk19", null, {
    "css": [
        ".page-list,.topic-title-list,.site-link-list{overflow:hidden}.page-list li,.topic-title-list li,.site-link-list li{color:#54595d;position:relative;border-bottom:1px solid #eaecf0;padding-top:0.8em;padding-bottom:0.8em;margin:-1px 0 0;line-height:1}.page-list li .watch-this-article,.topic-title-list li .watch-this-article,.site-link-list li .watch-this-article{position:absolute;right:0;top:0.8em;margin-top:1px}.page-list li .watch-this-article button,.topic-title-list li .watch-this-article button,.site-link-list li .watch-this-article button{position:absolute;text-indent:inherit;outline:0}.page-list li \u003E a,.topic-title-list li \u003E a,.site-link-list li \u003E a{display:block;color:#54595d}.page-list li \u003E a:active,.topic-title-list li \u003E a:active,.site-link-list li \u003E a:active,.page-list li \u003E a:hover,.topic-title-list li \u003E a:hover,.site-link-list li \u003E a:hover,.page-list li \u003E a:visited,.topic-title-list li \u003E a:visited,.site-link-list li \u003E a:visited{text-decoration:none;color:#54595d}.page-list.thumbs li,.topic-title-list.thumbs li,.site-link-list.thumbs li,.page-list.side-list li,.topic-title-list.side-list li,.site-link-list.side-list li{padding-left:85px}.page-list .info,.topic-title-list .info,.site-link-list .info{font-size:0.7em;text-transform:uppercase}.page-list .info,.topic-title-list .info,.site-link-list .info,.page-list .component,.topic-title-list .component,.site-link-list .component{color:#72777d;line-height:1.2}.page-list .title h3,.topic-title-list .title h3,.site-link-list .title h3,.page-list .title .mw-mf-user,.topic-title-list .title .mw-mf-user,.site-link-list .title .mw-mf-user,.page-list .title .component,.topic-title-list .title .component,.site-link-list .title .component,.page-list .title .info,.topic-title-list .title .info,.site-link-list .title .info{margin:0.5em 0}.page-list.thumbs .title,.topic-title-list.thumbs .title,.site-link-list.thumbs .title{padding-right:24px}.page-list .list-thumb,.topic-title-list .list-thumb,.site-link-list .list-thumb{position:absolute;width:70px;height:100%;top:0;left:0}.page-list p,.topic-title-list p,.site-link-list p{font-size:0.9em;line-height:normal}.page-list.side-list .list-thumb,.topic-title-list.side-list .list-thumb,.site-link-list.side-list .list-thumb{padding-top:0.8em;padding-bottom:0.8em;color:#222222}.page-list.side-list .list-thumb p,.topic-title-list.side-list .list-thumb p,.site-link-list.side-list .list-thumb p{line-height:1.2;margin:0.5em 0}.page-list.side-list .list-thumb .timestamp,.topic-title-list.side-list .list-thumb .timestamp,.site-link-list.side-list .list-thumb .timestamp{margin-bottom:0.65em}@media all and (min-width:720px){.page-summary-list,.topic-title-list,.site-link-list{padding-top:0;padding-bottom:0}}"
    ]
});
mw.loader.implement("mobile.pagesummary.styles@04z8xwn", null, {
    "css": [".page-summary h2,.page-summary h3{font:inherit;font-weight:bold;color:#54595d}.page-summary h2 a,.page-summary h3 a{color:inherit}.page-summary h2 strong,.page-summary h3 strong{text-decoration:underline}.list-header{font-weight:bold;font-size:0.85em;padding-top:0.5em;padding-bottom:0.4em;background-color:#eaecf0;color:#72777d}.list-thumb{background-repeat:no-repeat;background-position:center center}.list-thumb.list-thumb-x{background-size:100% auto}.list-thumb.list-thumb-y{background-size:auto 100%}.list-thumb.list-thumb-none{background-color:#eaecf0;background-size:40px 40px;opacity:0.51}"]
});
mw.loader.implement("mobile.site.styles@1abadp5", null, {
    "css": [
        ".geo-nondefault,.geo-multi-punct, .checkuser-show,.sysop-show,.templateeditor-show,.extendedmover-show,.patroller-show,.extendedconfirmed-show,.autoconfirmed-show, .hide-when-compact, .noprint.portal{display:none} @media screen,handheld{.citation *.printonly{display:none}} .citation{word-wrap:break-word}  .navbar{display:inline;font-size:88%;font-weight:normal}.navbar ul{display:inline;white-space:nowrap}.navbar li{word-spacing:-0.125em}.navbar.mini li span{font-variant:small-caps} .navbox .navbar,.infobox .navbar{font-size:100%}.navbox .navbar{display:block}.navbox-title .navbar{ float:left; text-align:left; margin-right:0.5em;width:6em} .plainlist ul{list-style:none;padding-left:0; }.visualhide{position:absolute;left:-10000px;top:auto;width:1px;height:1px;overflow:hidden}  .mw-parser-output .nowrap,.nowraplinks a,.nowraplinks .selflink,sup.reference a{white-space:nowrap}.mw-parser-output .infobox .nowrap{white-space:normal !important} .wrap,.wraplinks a{white-space:normal} .times-serif,span.texhtml{font-family:serif}span.texhtml{white-space:nowrap} .reflist ol.references{list-style-type:inherit} .refbegin-hanging-indents \u003E ul,.refbegin-hanging-indents \u003E dl{list-style-type:none;margin-left:0}.refbegin-hanging-indents \u003E ul \u003E li,.refbegin-hanging-indents \u003E dl \u003E dd{margin-left:0;padding-left:1.0em;text-indent:-1.0em;list-style:none} .flagicon img{min-width:25px} .cnotice{margin-bottom:0 !important} .mbox-text{ padding:0.25em 0.9em}   #content .hlist-separated li:after{font-size:0.8em;color:#333}  .mw-parser-output .hlist ul,.mw-parser-output .hlist ol{padding-left:0} .mw-parser-output .hlist li,.mw-parser-output .hlist dd,.mw-parser-output .hlist dt{margin-right:0}.mw-parser-output .hlist li:after,.mw-parser-output .hlist dd:after{content:\" · \";font-weight:bold}.mw-parser-output .hlist dt:after{content:\":\"} .heading-holder hlist li:after,.mw-parser-output .hlist dd:last-child:after,.mw-parser-output .hlist dt:last-child:after,.mw-parser-output .hlist li:last-child:after{content:none}  .mw-parser-output .hlist dd dd:first-child:before,.mw-parser-output .hlist dd dt:first-child:before,.mw-parser-output .hlist dd li:first-child:before,.mw-parser-output .hlist dt dd:first-child:before,.mw-parser-output .hlist dt dt:first-child:before,.mw-parser-output .hlist dt li:first-child:before,.mw-parser-output .hlist li dd:first-child:before,.mw-parser-output .hlist li dt:first-child:before,.mw-parser-output .hlist li li:first-child:before{content:\" (\";font-weight:normal} .mw-parser-output .hlist dd dd:last-child:after,.mw-parser-output .hlist dd dt:last-child:after,.mw-parser-output .hlist dd li:last-child:after,.mw-parser-output .hlist dt dd:last-child:after,.mw-parser-output .hlist dt dt:last-child:after,.mw-parser-output .hlist dt li:last-child:after,.mw-parser-output .hlist li dd:last-child:after,.mw-parser-output .hlist li dt:last-child:after,.mw-parser-output .hlist li li:last-child:after{content:\") \";font-weight:normal}  .mw-parser-output .hlist ol{counter-reset:listitem} .mw-parser-output .hlist ol \u003E li{counter-increment:listitem} .mw-parser-output .hlist ol \u003E li:before{content:\" \" counter(listitem) \" \";white-space:nowrap} .mw-parser-output .hlist dd ol \u003E li:first-child:before,.mw-parser-output .hlist dt ol \u003E li:first-child:before,.mw-parser-output .hlist li ol \u003E li:first-child:before{content:\" (\" counter(listitem) \" \"} @media print{.navbox,.hatnote,.ambox{display:none}}"
    ]
});
mw.loader.implement("mobile.startup.images@0gkum3n", null, {
    "css": [
        ".mw-ui-icon-mf-toc:before{background-image:url(/w/load.php?modules=mobile.startup.images\u0026image=toc\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2240%22 height=%2240%22 viewBox=%220 0 40 40%22%3E%3Cpath fill=%22%23a2a9b1%22 d=%22M2.176 37.586v-6.202c0-.17.113-.298.243-.298h35.16c.13 0 .24.133.24.298v6.202c0 .164-.11.296-.244.296H2.42c-.13 0-.244-.132-.244-.296zm35.648-20.984v6.207c0 .16-.11.292-.243.292H2.42c-.13 0-.244-.135-.244-.298V16.6c0-.16.113-.296.243-.296h35.16c.13 0 .24.136.24.296zM2.176 2.355c0-.132.108-.237.236-.237h21.94c.134 0 .236.105.236.237v6.677a.234.234 0 0 1-.236.237H2.412a.236.236 0 0 1-.236-.24V2.352z%22/%3E%3C/svg%3E\")}.mw-ui-icon-mf-citation:before{background-image:url(/w/load.php?modules=mobile.startup.images\u0026image=citation\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2220%22 height=%2220%22 viewBox=%220 0 20 20%22%3E%3Ctitle%3Ereference%3C/title%3E%3Cpath d=%22M15 10l-2.78-2.78L9.44 10V1H5a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V3a2 2 0 0 0-2-2z%22/%3E%3C/svg%3E\")}.mw-ui-icon-mf-citation-invert:before{background-image:url(/w/load.php?modules=mobile.startup.images\u0026image=citation\u0026variant=invert\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2220%22 height=%2220%22 viewBox=%220 0 20 20%22%3E%3Ctitle%3Ereference%3C/title%3E%3Cg fill=%22%23fff%22%3E%3Cpath d=%22M15 10l-2.78-2.78L9.44 10V1H5a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V3a2 2 0 0 0-2-2z%22/%3E%3C/g%3E%3C/svg%3E\")}.mw-ui-icon-mf-error:before{background-image:url(/w/load.php?modules=mobile.startup.images\u0026image=error\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 24 24%22%3E%3Cpath id=%22Bell%22 fill=%22%23d33%22 fill-rule=%22evenodd%22 d=%22M11.643 22.364c1.234 0 2.235-.956 2.235-2.136h-4.47c0 1.18 1 2.136 2.234 2.136zm7.25-12.284v3.998l1.77 3.603v1.1H2.623v-1.1l1.77-3.602V10.08c0-2.894 1.822-5.41 4.475-6.47.26-1.283 1.415-2.227 2.773-2.227s2.51.944 2.776 2.227c2.653 1.06 4.477 3.576 4.477 6.47zM12.92 4.974h-2.554c-2.438.553-4.255 2.64-4.255 5.14v4.474l-1.7 2.44h14.47l-1.702-2.44v-4.474c.024-4.076-3.616-5.09-4.255-5.14z%22/%3E%3C/svg%3E\")}.mw-ui-icon-mf-watch:before{background-image:url(/w/load.php?modules=mobile.startup.images\u0026image=watch\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2224%22 height=%2224%22 viewBox=%220 0 24 24%22%3E%3Cg id=%22Page-1%22 fill=%22none%22 fill-rule=%22evenodd%22%3E%3Cg id=%22watchstar%22 fill=%22%2354595d%22%3E%3Cpath d=%22M8.91 9H1l6.007 5.86L5 23l7-4.319L19 23l-1.995-8.14L23 9h-7.909l-3.09-8-3.09 8zm.054 5.188L5.312 11h4.818L12 5.747 13.881 11h4.82l-3.653 3.2 1.099 4.956L12 16.516l-4.136 2.627 1.1-4.955z%22/%3E%3C/g%3E%3C/g%3E%3C/svg%3E\")}.mw-ui-icon-mf-watched:before{background-image:url(/w/load.php?modules=mobile.startup.images\u0026image=watched\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2224%22 height=%2224%22 viewBox=%220 0 24 24%22%3E%3Cpath fill=%22%2300af89%22 d=%22M8.91 9H1l6.007 5.86L5 23l7-4.319L19 23l-1.995-8.14L23 9h-7.909l-3.09-8z%22/%3E%3C/svg%3E\")}.mw-ui-icon-mf-search-clear:before{background-image:url(/w/load.php?modules=mobile.startup.images\u0026image=search-clear\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2218%22 height=%2218%22 viewBox=%220 0 18 18%22%3E%3Ctitle%3Eclear-field%3C/title%3E%3Cpath fill=%22%23a2a9b1%22 fill-rule=%22evenodd%22 d=%22M9 0C4.05 0 0 4.05 0 9s4.05 9 9 9 9-4.05 9-9-4.05-9-9-9zm4.5 12.375L12.375 13.5 9 10.125 5.625 13.5 4.5 12.375 7.875 9 4.5 5.625 5.625 4.5 9 7.875 12.375 4.5 13.5 5.625 10.125 9l3.375 3.375z%22/%3E%3C/svg%3E\")}.mw-ui-icon-mf-search-content:before{background-image:url(/w/load.php?modules=mobile.startup.images\u0026image=search-content\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 24 24%22%3E%3Cpath fill=%22%2372777d%22 d=%22M10.55 2.06c2.287 0 4.43.88 6.04 2.49 3 3 3.37 7.654.88 11.083l-.143.204 4.285 4.285c.204.205.327.49.327.776A1.115 1.115 0 0 1 20.834 22c-.286 0-.572-.122-.776-.327l-4.245-4.244-.04-.044-.205.143a8.53 8.53 0 0 1-5.02 1.632 8.49 8.49 0 0 1-6.04-2.51A8.49 8.49 0 0 1 2 10.612 8.46 8.46 0 0 1 4.51 4.57a8.49 8.49 0 0 1 6.04-2.51zm5.083 13.613c2.796-2.795 2.796-7.346 0-10.142-1.368-1.344-3.164-2.1-5.082-2.1a7.117 7.117 0 0 0-5.06 2.1 7.236 7.236 0 0 0-2.1 5.08 7.13 7.13 0 0 0 2.08 5.064c1.37 1.347 3.165 2.103 5.083 2.103a7.164 7.164 0 0 0 5.083-2.103zm-.98-5.49c.265 0 .49.205.49.49 0 .266-.225.49-.49.49H6.776a.497.497 0 0 1-.49-.49c0-.265.224-.49.49-.49h7.877zm-7.837-1.49a.497.497 0 0 1-.49-.49c0-.264.225-.49.49-.49h7.878c.265 0 .49.226.49.49s-.225.49-.49.49H6.816zm4.613 3.94c.262 0 .49.224.49.49a.5.5 0 0 1-.49.49H6.813a.497.497 0 0 1-.49-.49c0-.266.225-.49.49-.49h4.613z%22/%3E%3C/svg%3E\")}.mw-ui-icon-mf-amc-outreach:before{background-image:url(/w/load.php?modules=mobile.startup.images\u0026image=amc-outreach\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%22210%22 height=%22229%22%3E%3Cg fill=%22none%22 fill-rule=%22evenodd%22%3E%3Cpath d=%22M0 0h210v229H0z%22/%3E%3Cpath fill=%22%2336C%22 fill-rule=%22nonzero%22 d=%22M8.409 12.552l98.03 181.322a37.5 37.5 0 0 0 25.868 18.983l62.97 12.177a9.417 9.417 0 0 0 9.953-13.94l-96.843-168.46a47.556 47.556 0 0 0-27.64-21.873L17.148 1.798a7.5 7.5 0 0 0-8.74 10.754z%22/%3E%3Cpath stroke=%22%23454545%22 stroke-width=%222.825%22 d=%22M3.984 14.998l94.753 175.259a47.085 47.085 0 0 0 32.48 23.836l58.516 11.315a9.417 9.417 0 0 0 9.952-13.94l-96.842-168.46a47.556 47.556 0 0 0-27.64-21.872L13.861 2.845a8.475 8.475 0 0 0-9.878 12.153z%22/%3E%3Cpath stroke=%22%23EAECF0%22 stroke-linecap=%22round%22 stroke-width=%222.25%22 d=%22M148.622 201.53l19.341 3.798%22/%3E%3Cpath fill=%22%23EAF3FF%22 d=%22M20.5 17.5l93 165 77.08 17L98.67 39.897z%22/%3E%3Cg fill-rule=%22nonzero%22%3E%3Cpath fill=%22%23FC3%22 d=%22M155.898 147.714l14.392-38.825 38.241-14.612-38.241-14.612-14.392-38.826-14.392 38.826-38.241 14.612 38.241 14.612z%22/%3E%3Cpath stroke=%22%23454545%22 stroke-width=%222.825%22 d=%22M151.485 142.248l12.661-33.273 33.642-12.521-33.642-12.522-12.661-33.273-12.661 33.273-33.642 12.522 33.642 12.521z%22/%3E%3Cpath fill=%22%23FC3%22 d=%22M90.696 90.348l7.71-20.435 20.486-7.69-20.486-7.691-7.71-20.434-7.71 20.434L62.5 62.223l20.486 7.69zm18.798 69.375l7.71-20.435 20.486-7.69-20.486-7.691-7.71-20.434-7.71 20.434-20.487 7.691 20.487 7.69z%22/%3E%3Cpath stroke=%22%23454545%22 stroke-width=%222.825%22 d=%22M92.834 90.348l6.753-17.746 17.942-6.678-17.942-6.679L92.834 41.5l-6.752 17.745-17.943 6.679 17.943 6.678zm18.521 61.059l5.065-13.309 13.457-5.009-13.457-5.009-5.065-13.309-5.064 13.309-13.457 5.009 13.457 5.009z%22/%3E%3C/g%3E%3C/g%3E%3C/svg%3E\")}"
    ]
});
mw.loader.implement("mobile.startup.images.variants@06ats9l", null, {
    "css": [
        ".mw-ui-icon-mf-alert:before{background-image:url(/w/load.php?modules=mobile.startup.images.variants\u0026image=alert\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2220%22 height=%2220%22 viewBox=%220 0 20 20%22%3E%3Ctitle%3Ealert%3C/title%3E%3Cpath d=%22M19.64 16.36L11.53 2.3A1.85 1.85 0 0 0 10 1.21 1.85 1.85 0 0 0 8.48 2.3L.36 16.36C-.48 17.81.21 19 1.88 19h16.24c1.67 0 2.36-1.19 1.52-2.64zM11 16H9v-2h2zm0-4H9V6h2z%22/%3E%3C/svg%3E\")}.mw-ui-icon-mf-alert-gray:before{background-image:url(/w/load.php?modules=mobile.startup.images.variants\u0026image=alert\u0026variant=gray\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2220%22 height=%2220%22 viewBox=%220 0 20 20%22%3E%3Ctitle%3Ealert%3C/title%3E%3Cg fill=%22%23a2a9b1%22%3E%3Cpath d=%22M19.64 16.36L11.53 2.3A1.85 1.85 0 0 0 10 1.21 1.85 1.85 0 0 0 8.48 2.3L.36 16.36C-.48 17.81.21 19 1.88 19h16.24c1.67 0 2.36-1.19 1.52-2.64zM11 16H9v-2h2zm0-4H9V6h2z%22/%3E%3C/g%3E%3C/svg%3E\")}.mw-ui-icon-mf-alert-invert:before{background-image:url(/w/load.php?modules=mobile.startup.images.variants\u0026image=alert\u0026variant=invert\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2220%22 height=%2220%22 viewBox=%220 0 20 20%22%3E%3Ctitle%3Ealert%3C/title%3E%3Cg fill=%22%23fff%22%3E%3Cpath d=%22M19.64 16.36L11.53 2.3A1.85 1.85 0 0 0 10 1.21 1.85 1.85 0 0 0 8.48 2.3L.36 16.36C-.48 17.81.21 19 1.88 19h16.24c1.67 0 2.36-1.19 1.52-2.64zM11 16H9v-2h2zm0-4H9V6h2z%22/%3E%3C/g%3E%3C/svg%3E\")}.mw-ui-icon-mf-arrow:before{background-image:url(/w/load.php?modules=mobile.startup.images.variants\u0026image=arrow\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2220%22 height=%2220%22 viewBox=%220 0 20 20%22%3E%3Cpath d=%22M19 6.25l-1.5-1.5-7.5 7.5-7.5-7.5L1 6.25l9 9 9-9z%22/%3E%3C/svg%3E\")}.mw-ui-icon-mf-arrow-gray:before{background-image:url(/w/load.php?modules=mobile.startup.images.variants\u0026image=arrow\u0026variant=gray\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2220%22 height=%2220%22 viewBox=%220 0 20 20%22%3E%3Cg fill=%22%23a2a9b1%22%3E%3Cpath d=%22M19 6.25l-1.5-1.5-7.5 7.5-7.5-7.5L1 6.25l9 9 9-9z%22/%3E%3C/g%3E%3C/svg%3E\")}.mw-ui-icon-mf-arrow-invert:before{background-image:url(/w/load.php?modules=mobile.startup.images.variants\u0026image=arrow\u0026variant=invert\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2220%22 height=%2220%22 viewBox=%220 0 20 20%22%3E%3Cg fill=%22%23fff%22%3E%3Cpath d=%22M19 6.25l-1.5-1.5-7.5 7.5-7.5-7.5L1 6.25l9 9 9-9z%22/%3E%3C/g%3E%3C/svg%3E\")}.mw-ui-icon-mf-back:before{background-image:url(/w/load.php?modules=mobile.startup.images.variants\u0026image=back\u0026format=rasterized\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 24 24%22%3E%3Cpath fill=%22%2354595d%22 d=%22M24 10.667H5.107l4.78-4.78L8 4l-8 8 8 8 1.887-1.887-4.78-4.78H24z%22/%3E%3C/svg%3E\")}.mw-ui-icon-mf-back-gray:before{background-image:url(/w/load.php?modules=mobile.startup.images.variants\u0026image=back\u0026variant=gray\u0026format=rasterized\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 24 24%22%3E%3Cg fill=%22%23a2a9b1%22%3E%3Cpath fill=%22%2354595d%22 d=%22M24 10.667H5.107l4.78-4.78L8 4l-8 8 8 8 1.887-1.887-4.78-4.78H24z%22/%3E%3C/g%3E%3C/svg%3E\")}.mw-ui-icon-mf-back-invert:before{background-image:url(/w/load.php?modules=mobile.startup.images.variants\u0026image=back\u0026variant=invert\u0026format=rasterized\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 24 24%22%3E%3Cg fill=%22%23fff%22%3E%3Cpath fill=%22%2354595d%22 d=%22M24 10.667H5.107l4.78-4.78L8 4l-8 8 8 8 1.887-1.887-4.78-4.78H24z%22/%3E%3C/g%3E%3C/svg%3E\")}.mw-ui-icon-mf-overlay-close:before{background-image:url(/w/load.php?modules=mobile.startup.images.variants\u0026image=overlay-close\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2224%22 height=%2224%22 viewBox=%220 0 24 24%22%3E%3Ctitle%3Eclose%3C/title%3E%3Cpath fill=%22%2354595d%22 fill-rule=%22evenodd%22 d=%22M19.75 5.592L18.158 4l-6.283 6.283L5.592 4 4 5.592l6.283 6.283L4 18.158l1.592 1.592 6.283-6.283 6.283 6.283 1.592-1.592-6.283-6.283z%22/%3E%3C/svg%3E\")}.mw-ui-icon-mf-overlay-close-gray:before{background-image:url(/w/load.php?modules=mobile.startup.images.variants\u0026image=overlay-close\u0026variant=gray\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2224%22 height=%2224%22 viewBox=%220 0 24 24%22%3E%3Ctitle%3Eclose%3C/title%3E%3Cg fill=%22%23a2a9b1%22%3E%3Cpath fill=%22%2354595d%22 fill-rule=%22evenodd%22 d=%22M19.75 5.592L18.158 4l-6.283 6.283L5.592 4 4 5.592l6.283 6.283L4 18.158l1.592 1.592 6.283-6.283 6.283 6.283 1.592-1.592-6.283-6.283z%22/%3E%3C/g%3E%3C/svg%3E\")}.mw-ui-icon-mf-overlay-close-invert:before{background-image:url(/w/load.php?modules=mobile.startup.images.variants\u0026image=overlay-close\u0026variant=invert\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2224%22 height=%2224%22 viewBox=%220 0 24 24%22%3E%3Ctitle%3Eclose%3C/title%3E%3Cg fill=%22%23fff%22%3E%3Cpath fill=%22%2354595d%22 fill-rule=%22evenodd%22 d=%22M19.75 5.592L18.158 4l-6.283 6.283L5.592 4 4 5.592l6.283 6.283L4 18.158l1.592 1.592 6.283-6.283 6.283 6.283 1.592-1.592-6.283-6.283z%22/%3E%3C/g%3E%3C/svg%3E\")}"
    ]
});
mw.loader.implement("mw.externalguidance.init@1fgk895", function($, jQuery, require, module) {
    (function() {
        var context, originalUserLang = mw.config.get('wgUserLanguage');

        function getTargetLanguage(targetLanguage) {
            var googleLanguageNameMap = {
                no: 'nb',
                iw: 'he',
                jw: 'jv',
                'zh-CN': 'zh'
            };
            if (targetLanguage.indexOf('-x-mtfrom-') > 0) {
                targetLanguage = targetLanguage.split('-')[0];
            }
            return googleLanguageNameMap[targetLanguage] || targetLanguage;
        }
        context = {
            info: {
                from: mw.config.get('wgContentLanguage'),
                to: getTargetLanguage(document.documentElement.lang),
                page: mw.config.get('wgTitle')
            }
        };

        function detectContext() {
            var parentURL, translatedByMetaElement;
            translatedByMetaElement = document.head.querySelector('meta[http-equiv="X-Translated-By"]');
            if (translatedByMetaElement) {
                context.name = 'machine-translation';
                context.info.service = translatedByMetaElement.getAttribute('content');
                return context;
            }
            if (document.referrer) {
                parentURL = new mw.Uri(document.referrer);
                if (mw.config.get('wgExternalGuidanceMTReferrers').indexOf(parentURL.host) >= 0) {
                    context.name = 'machine-translation';
                    context.info.service = parentURL.host;
                    return context;
                }
            }
            return null;
        }
        context = detectContext();
        if (context) {
            if (context.info.to === originalUserLang) {
                return;
            }
            mw.log('[ExternalGuidance] Context detected ' + JSON.stringify(context));
            mw.loader.using(['mw.externalguidance']).then(function() {
                var eg = new mw.ExternalGuidance(context.name, context.info);
                eg.init();
            });
        }
    }());
});
mw.loader.implement("skins.minerva.icons.images.scripts@03m1464");
mw.loader.implement("skins.minerva.icons.images.scripts.misc@1vq7puh", null, {
    "css": [".mw-ui-icon-minerva-download:before{background-image:url(/w/load.php?modules=skins.minerva.icons.images.scripts.misc\u0026image=download\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2224%22 height=%2224%22 viewBox=%220 0 24 24%22%3E%3Cpath fill=%22%2354595d%22 fill-rule=%22evenodd%22 d=%22M19.778 14.222H22v5.556A2.223 2.223 0 0 1 19.778 22H4.222A2.223 2.223 0 0 1 2 19.778v-5.556h2V20h16v-5.436l-.222-.342zM12 18l-5.611-7h11.11L12 18zm1-16v9h-2V2h2z%22/%3E%3C/svg%3E\")}"]
});
mw.loader.implement("skins.minerva.icons.page.issues.default.color@19pxgso", null, {
    "css": [
        ".mw-ui-icon-minerva-issue-generic-defaultColor:before{background-image:url(/w/load.php?modules=skins.minerva.icons.page.issues.default.color\u0026image=issue-generic\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2220%22 height=%2220%22 viewBox=%220 0 20 20%22%3E%3Ctitle%3Ealert%3C/title%3E%3Cg fill=%22%2354595d%22%3E%3Cpath d=%22M19.64 16.36L11.53 2.3A1.85 1.85 0 0 0 10 1.21 1.85 1.85 0 0 0 8.48 2.3L.36 16.36C-.48 17.81.21 19 1.88 19h16.24c1.67 0 2.36-1.19 1.52-2.64zM11 16H9v-2h2zm0-4H9V6h2z%22/%3E%3C/g%3E%3C/svg%3E\")}.mw-ui-icon-minerva-issue-generic-lowColor:before{background-image:url(/w/load.php?modules=skins.minerva.icons.page.issues.default.color\u0026image=issue-generic\u0026variant=lowColor\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2220%22 height=%2220%22 viewBox=%220 0 20 20%22%3E%3Ctitle%3Ealert%3C/title%3E%3Cg fill=%22%23fc3%22%3E%3Cpath d=%22M19.64 16.36L11.53 2.3A1.85 1.85 0 0 0 10 1.21 1.85 1.85 0 0 0 8.48 2.3L.36 16.36C-.48 17.81.21 19 1.88 19h16.24c1.67 0 2.36-1.19 1.52-2.64zM11 16H9v-2h2zm0-4H9V6h2z%22/%3E%3C/g%3E%3C/svg%3E\")}.mw-ui-icon-minerva-issue-generic-mediumColor:before{background-image:url(/w/load.php?modules=skins.minerva.icons.page.issues.default.color\u0026image=issue-generic\u0026variant=mediumColor\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2220%22 height=%2220%22 viewBox=%220 0 20 20%22%3E%3Ctitle%3Ealert%3C/title%3E%3Cg fill=%22%23ff5d01%22%3E%3Cpath d=%22M19.64 16.36L11.53 2.3A1.85 1.85 0 0 0 10 1.21 1.85 1.85 0 0 0 8.48 2.3L.36 16.36C-.48 17.81.21 19 1.88 19h16.24c1.67 0 2.36-1.19 1.52-2.64zM11 16H9v-2h2zm0-4H9V6h2z%22/%3E%3C/g%3E%3C/svg%3E\")}.mw-ui-icon-minerva-issue-generic-highColor:before{background-image:url(/w/load.php?modules=skins.minerva.icons.page.issues.default.color\u0026image=issue-generic\u0026variant=highColor\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2220%22 height=%2220%22 viewBox=%220 0 20 20%22%3E%3Ctitle%3Ealert%3C/title%3E%3Cg fill=%22%23d33%22%3E%3Cpath d=%22M19.64 16.36L11.53 2.3A1.85 1.85 0 0 0 10 1.21 1.85 1.85 0 0 0 8.48 2.3L.36 16.36C-.48 17.81.21 19 1.88 19h16.24c1.67 0 2.36-1.19 1.52-2.64zM11 16H9v-2h2zm0-4H9V6h2z%22/%3E%3C/g%3E%3C/svg%3E\")}"
    ]
});
mw.loader.implement("skins.minerva.icons.page.issues.medium.color@1squc26", null, {
    "css": [
        ".mw-ui-icon-minerva-issue-severity-medium-mediumColor:before{background-image:url(/w/load.php?modules=skins.minerva.icons.page.issues.medium.color\u0026image=issue-severity-medium\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2220%22 height=%2220%22 viewBox=%220 0 20 20%22%3E%3Ctitle%3Enotice%3C/title%3E%3Cg fill=%22%23ff5d01%22%3E%3Cpath d=%22M10 0a10 10 0 1 0 10 10A10 10 0 0 0 10 0zm1 16H9v-2h2zm0-4H9V4h2z%22/%3E%3C/g%3E%3C/svg%3E\")}.mw-ui-icon-minerva-issue-type-point-of-view-mediumColor:before{background-image:url(/w/load.php?modules=skins.minerva.icons.page.issues.medium.color\u0026image=issue-type-point-of-view\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 id=%22Layer_1%22 viewBox=%220 0 20 20%22%3E%3Cg fill=%22%23ff5d01%22%3E%3Cg%3E%3Cpath d=%22M6 8l2-3 2-3H2l2 3z%22/%3E%3Cpath d=%22M14 12l-2 3-2 3h8l-2-3z%22/%3E%3Cpath transform=%22rotate%28-22.5 10 10%29%22 d=%22M1.5 9h17v2h-17z%22/%3E%3C/g%3E%3C/g%3E%3C/svg%3E\")}"
    ]
});
mw.loader.implement("skins.minerva.icons.page.issues.uncolored@0zfg068", null, {
    "css": [
        ".mw-ui-icon-minerva-issue-severity-low-lowColor:before{background-image:url(/w/load.php?modules=skins.minerva.icons.page.issues.uncolored\u0026image=issue-severity-low-lowColor\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg id=%22Layer_1%22 xmlns=%22http://www.w3.org/2000/svg%22 xmlns:xlink=%22http://www.w3.org/1999/xlink%22 viewBox=%220 0 20 20%22%3E%3Cstyle%3E.st0%7Bclip-path:url%28%23SVGID_2_%29%3Bfill:%23f5a623%7D.st1%7Bclip-path:url%28%23SVGID_4_%29%3Bfill:%23774b20%7D%3C/style%3E%3Cg%3E%3Cg%3E%3Cdefs%3E%3Cpath id=%22SVGID_1_%22 d=%22M17.9 11.9c-1.4-.3-3.4-2.8-4.8-4.6l-8.8 6.2 2.3 3.3c.8 1.2 2.3 1.8 4 1.7-.8-.9-1.5-1.9-1.6-2-.1-.2 0-.4.1-.5.2-.1.4-.1.5.1.3.5 1.1 1.5 1.8 2.3.8-.2 1.7-.6 2.6-1.1-1.3-.8-2.2-2.4-2.2-2.4-.1-.2 0-.4.1-.5.2-.1.4 0 .5.1 0 0 1 1.7 2.3 2.3.9-.7 1.6-1.3 2.1-1.9-2.1-.4-3.1-1.9-3.2-1.9-.1-.2-.1-.4.1-.5.2-.1.4-.1.5.1a4.907 4.907 0 0 0 3.1 1.7c.6-.9.8-1.6.8-2 .1-.2 0-.3-.2-.4z%22/%3E%3C/defs%3E%3CclipPath id=%22SVGID_2_%22%3E%3Cuse xlink:href=%22%23SVGID_1_%22 overflow=%22visible%22/%3E%3C/clipPath%3E%3Cpath class=%22st0%22 d=%22M-.7 2.3h23.9v21.2H-.7z%22/%3E%3C/g%3E%3Cg%3E%3Cdefs%3E%3Cpath id=%22SVGID_3_%22 d=%22M11.6 5.2c-.3-.4-.9-.5-1.3-.2L7.9 6.6 4.5 1.8c-.2-.3-.6-.4-.9-.2L2.1 2.7c-.3.2-.3.6-.2.8l3.4 4.8L3 10c-.4.3-.5.9-.2 1.3L4 13l8.8-6.1c-.8-1-1.2-1.7-1.2-1.7z%22/%3E%3C/defs%3E%3CclipPath id=%22SVGID_4_%22%3E%3Cuse xlink:href=%22%23SVGID_3_%22 overflow=%22visible%22/%3E%3C/clipPath%3E%3Cpath class=%22st1%22 d=%22M-3.2-3.5h20.9V18H-3.2z%22/%3E%3C/g%3E%3C/g%3E%3C/svg%3E\")}.mw-ui-icon-minerva-issue-type-move-defaultColor:before{background-image:url(/w/load.php?modules=skins.minerva.icons.page.issues.uncolored\u0026image=issue-type-move-defaultColor\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg id=%22Layer_1%22 xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 20 20%22%3E%3Cstyle%3E.st0%7Bfill:%2336c%7D.st1%7Bfill:%23d33%7D%3C/style%3E%3Cg%3E%3Cpath class=%22st0%22 d=%22M6 8.7V12H0v4h6v3l3-2.6 3-2.6-3-2.5z%22/%3E%3Cpath class=%22st1%22 d=%22M14 4V.7l-3 2.6-3 2.5 3 2.6 3 2.6V8h6V4z%22/%3E%3C/g%3E%3C/svg%3E\")}"
    ]
});
mw.loader.implement("skins.minerva.mainMenu.icons@1e70voy", null, {
    "css": [
        ".mw-ui-icon-minerva-contributions:before{background-image:url(/w/load.php?modules=skins.minerva.mainMenu.icons\u0026image=contributions\u0026format=rasterized\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2220%22 height=%2220%22 viewBox=%220 0 20 20%22%3E%3Cg fill=%22%234A4F53%22%3E%3Cg%3E%3Cpath d=%22M6 15h3v2H6z%22/%3E%3Cpath d=%22M6 9h5v2H6z%22/%3E%3Cpath d=%22M6 3h11v2H6z%22/%3E%3Ccircle cx=%222%22 cy=%224%22 r=%222%22/%3E%3Ccircle cx=%222%22 cy=%2210%22 r=%222%22/%3E%3Ccircle cx=%222%22 cy=%2216%22 r=%222%22/%3E%3Cpath d=%22M15.5 13.556c-3.33 0-4.5 1.666-4.5 2.777V18h9v-1.667c0-1.11-1.17-2.777-4.5-2.777z%22/%3E%3Ccircle cx=%2215.444%22 cy=%2210.5%22 r=%222.5%22/%3E%3C/g%3E%3C/g%3E%3C/svg%3E\")}.mw-ui-icon-minerva-login:before{background-image:url(/w/load.php?modules=skins.minerva.mainMenu.icons\u0026image=login\u0026format=rasterized\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2220%22 height=%2220%22 viewBox=%220 0 20 20%22%3E%3Ctitle%3ElogIn-ltr%3C/title%3E%3Cg fill=%22%234A4F53%22%3E%3Cpath d=%22M1 11v6c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V3c0-1.1-.9-2-2-2H3c-1.1 0-2 .9-2 2v6h8V5l4.75 5L9 15v-4H1z%22 fill-rule=%22evenodd%22 clip-rule=%22evenodd%22/%3E%3C/g%3E%3C/svg%3E\")}.mw-ui-icon-minerva-home:before{background-image:url(/w/load.php?modules=skins.minerva.mainMenu.icons\u0026image=home\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2220%22 height=%2220%22 viewBox=%220 0 20 20%22%3E%3Ctitle%3Elog out%3C/title%3E%3Cg fill=%22%234A4F53%22%3E%3Cpath d=%22M10 1L0 10h3v9h4v-4.6c0-1.47 1.31-2.66 3-2.66s3 1.19 3 2.66V19h4v-9h3L10 1z%22/%3E%3C/g%3E%3C/svg%3E\")}.mw-ui-icon-minerva-logout:before{background-image:url(/w/load.php?modules=skins.minerva.mainMenu.icons\u0026image=logout\u0026format=rasterized\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2220%22 height=%2220%22 viewBox=%220 0 20 20%22%3E%3Ctitle%3Elog out%3C/title%3E%3Cg fill=%22%234A4F53%22%3E%3Cpath d=%22M3 3h8V1H3a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h8v-2H3z%22/%3E%3Cpath d=%22M13 5v4H5v2h8v4l6-5z%22/%3E%3C/g%3E%3C/svg%3E\")}.mw-ui-icon-minerva-nearby:before{background-image:url(/w/load.php?modules=skins.minerva.mainMenu.icons\u0026image=nearby\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2220%22 height=%2220%22 viewBox=%220 0 20 20%22%3E%3Ctitle%3Emap pin%3C/title%3E%3Cg fill=%22%234A4F53%22%3E%3Cpath d=%22M10 0a7.65 7.65 0 0 0-8 8c0 2.52 2 5 3 6s5 6 5 6 4-5 5-6 3-3.48 3-6a7.65 7.65 0 0 0-8-8zm0 11.25A3.25 3.25 0 1 1 13.25 8 3.25 3.25 0 0 1 10 11.25z%22/%3E%3C/g%3E%3C/svg%3E\")}.mw-ui-icon-minerva-random:before{background-image:url(/w/load.php?modules=skins.minerva.mainMenu.icons\u0026image=random\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2220%22 height=%2220%22 viewBox=%220 0 20 20%22%3E%3Ctitle%3Edie%3C/title%3E%3Cg fill=%22%234A4F53%22%3E%3Cpath d=%22M3 1a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V3a2 2 0 0 0-2-2zm2 16a2 2 0 1 1 2-2 2 2 0 0 1-2 2zM5 7a2 2 0 1 1 2-2 2 2 0 0 1-2 2zm5 5a2 2 0 1 1 2-2 2 2 0 0 1-2 2zm5 5a2 2 0 1 1 2-2 2 2 0 0 1-2 2zm0-10a2 2 0 1 1 2-2 2 2 0 0 1-2 2z%22/%3E%3C/g%3E%3C/svg%3E\")}.mw-ui-icon-minerva-settings:before{background-image:url(/w/load.php?modules=skins.minerva.mainMenu.icons\u0026image=settings\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 xmlns:xlink=%22http://www.w3.org/1999/xlink%22 width=%2220%22 height=%2220%22 viewBox=%220 0 20 20%22%3E%3Ctitle%3Esettings%3C/title%3E%3Cg fill=%22%234A4F53%22%3E%3Cg xmlns:xlink=%22http://www.w3.org/1999/xlink%22 transform=%22translate%2810 10%29%22%3E%3Cpath id=%22a%22 d=%22M1.5-10h-3l-1 6.5h5m0 7h-5l1 6.5h3%22/%3E%3Cuse transform=%22rotate%2845%29%22 xlink:href=%22%23a%22/%3E%3Cuse transform=%22rotate%2890%29%22 xlink:href=%22%23a%22/%3E%3Cuse transform=%22rotate%28135%29%22 xlink:href=%22%23a%22/%3E%3C/g%3E%3Cpath d=%22M10 2.5a7.5 7.5 0 0 0 0 15 7.5 7.5 0 0 0 0-15v4a3.5 3.5 0 0 1 0 7 3.5 3.5 0 0 1 0-7%22/%3E%3C/g%3E%3C/svg%3E\")}.mw-ui-icon-minerva-watchlist:before{background-image:url(/w/load.php?modules=skins.minerva.mainMenu.icons\u0026image=watchlist\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2220%22 height=%2220%22 viewBox=%220 0 20 20%22%3E%3Ctitle%3Eun-star%3C/title%3E%3Cg fill=%22%234A4F53%22%3E%3Cpath d=%22M20 7h-7L10 .5 7 7H0l5.46 5.47-1.64 7 6.18-3.7 6.18 3.73-1.63-7z%22/%3E%3C/g%3E%3C/svg%3E\")}"
    ]
});
mw.loader.implement("skins.minerva.mainMenu.styles@1co7oxq", null, {
    "css": [
        "#mw-mf-page-left{position:fixed;top:0;left:0;bottom:0;min-width:275px;max-width:80%;z-index:2;overflow-y:auto;background-color:#eaecf0;-webkit-transform:translate(-100%,0);-moz-transform:translate(-100%,0);transform:translate(-100%,0)}@media screen and (min-width:720px){#mw-mf-page-left{min-width:320px}}#mw-mf-page-left ul{padding-bottom:22px}.primary-navigation-enabled{overflow:hidden;touch-action:none}.animations #mw-mf-page-left{-webkit-transition:transform 0.25s ease-in-out;-moz-transition:transform 0.25s ease-in-out;transition:transform 0.25s ease-in-out}.animations.primary-navigation-enabled #mw-mf-page-left{-webkit-transform:translate(0,0);-moz-transform:translate(0,0);transform:translate(0,0)}.animations.primary-navigation-enabled .transparent-shield{visibility:visible;opacity:0.5}.navigation-full-screen #mw-mf-page-left{max-width:none;transform:none}       #mw-mf-page-left .secondary-action{border:0;font-size:16px;position:absolute;right:0;top:0;bottom:0;padding-right:0;border-left:1px solid #a2a9b1}#mw-mf-page-left .primary-action{margin-right:3.5em}#mw-mf-page-left ul:first-child li:first-child{border-top:0}#mw-mf-page-left ul li{background-color:#ffffff;position:relative;border-top:1px solid #eaecf0;margin-top:-1px;font-size:0.875em}#mw-mf-page-left ul li:first-child{border-top:0}#mw-mf-page-left ul li a{color:#54595d;display:block;padding:12px 10px 12px 15px;max-width:100%;text-overflow:ellipsis;white-space:nowrap;overflow:hidden}#mw-mf-page-left ul li a:hover{box-shadow:inset 4px 0 0 0 #3366cc;text-decoration:none}#mw-mf-page-left ul li a:visited,#mw-mf-page-left ul li a:active{color:#54595d}#mw-mf-page-left ul li a.mw-ui-icon{font-weight:bold;line-height:1.857}#mw-mf-page-left ul li a.mw-ui-icon:before{font-size:16px}      #mw-mf-page-left ul.hlist li{background-color:transparent;border:0;margin:0;font-size:0.75em}#mw-mf-page-left ul.hlist li:after{content:none}#mw-mf-page-left ul.hlist li a{color:#3366cc;padding:0.7em 12px}#mw-mf-page-left ul.hlist li a:hover{background-color:transparent;box-shadow:none}         .notifications-overlay{visibility:visible}.notifications-overlay.navigation-drawer{display:block;width:auto;right:0;box-shadow:-5px 0 0 0 rgba(0,0,0,0.3)}@media all and (min-width:720px){.notifications-overlay.navigation-drawer{left:40%}.notifications-overlay.navigation-drawer .overlay-header{margin:0;width:100%;max-width:none}.notifications-overlay.navigation-drawer .overlay-header .cancel{left:0}}.animations .notifications-overlay.navigation-drawer{-webkit-transform:translate(100%,0);-moz-transform:translate(100%,0);transform:translate(100%,0);-webkit-transition:transform 0.25s ease-in-out;-moz-transition:transform 0.25s ease-in-out;transition:transform 0.25s ease-in-out}.animations .notifications-overlay.navigation-drawer.visible{-webkit-transform:translate(0,0);-moz-transform:translate(0,0);transform:translate(0,0)}.secondary-navigation-enabled .transparent-shield{visibility:visible;opacity:0.5}"
    ]
});
mw.loader.implement("skins.minerva.options@1h07ipx", {
    "main": "resources/skins.minerva.options/index.js",
    "files": {
        "resources/skins.minerva.options/index.js": function(require, module) {
            require('./backtotop.js')();
            require('./categories.js')();
            require('./share.js')();
        },
        "resources/skins.minerva.options/BackToTopOverlay.js": function(require, module) {
            (function(M) {
                var mobile = M.require('mobile.startup'),
                    mfExtend = mobile.mfExtend,
                    View = mobile.View,
                    util = mobile.util;

                function BackToTopOverlay(props) {
                    View.call(this, util.extend({}, props, {
                        className: 'backtotop',
                        events: {
                            click: 'onBackToTopClick'
                        }
                    }));
                }
                mfExtend(BackToTopOverlay, View, {
                    template: mw.template.get('skins.minerva.options', 'BackToTopOverlay.mustache'),
                    show: function() {
                        this.$el.css('visibility', 'visible').addClass('visible');
                    },
                    hide: function() {
                        this.$el.removeClass('visible');
                    },
                    onBackToTopClick: function() {
                        $('html, body').animate({
                            scrollTop: 0
                        }, 400);
                    }
                });
                module.exports = BackToTopOverlay;
            }(mw.mobileFrontend));
        },
        "resources/skins.minerva.options/backtotop.js": function(require, module) {
            module.exports = function() {
                var BackToTopOverlay = require('./BackToTopOverlay.js'),
                    backtotop = new BackToTopOverlay(),
                    features = mw.config.get('wgMinervaFeatures', {}),
                    M = mw.mobileFrontend,
                    mobile = M.require('mobile.startup'),
                    browser = mobile.Browser.getSingleton(),
                    eventBus = mobile.eventBusSingleton;
                if (browser.isIos() || !features.backToTop) {
                    return;
                }
                backtotop.appendTo('body');
                eventBus.on('scroll', function() {
                    if ($(window).height() - $(window).scrollTop() <= 0) {
                        backtotop.show();
                    } else {
                        backtotop.hide();
                    }
                });
            };
        },
        "resources/skins.minerva.options/categories.js": function(require, module) {
            module.exports = function() {
                var M = mw.mobileFrontend,
                    mobile = M.require('mobile.startup'),
                    loader = mobile.rlModuleLoader,
                    features = mw.config.get('wgMinervaFeatures', {}),
                    overlayManager = require('skins.minerva.scripts').overlayManager,
                    eventBus = mobile.eventBusSingleton,
                    isAnon = mw.user.isAnon();
                if (!features.categories) {
                    return;
                }
                overlayManager.add(/^\/categories$/, function() {
                    return loader.loadModule(
                        'mobile.categories.overlays', true).then(function(loadingOverlay) {
                        var categoryOverlay = M.require('mobile.categories.overlays/categoryOverlay');
                        eventBus.on('category-added', function() {
                            window.location.hash = '#/categories';
                        });
                        loadingOverlay.hide();
                        return categoryOverlay({
                            api: new mw.Api(),
                            isAnon: isAnon,
                            title: mobile.currentPage().title,
                            eventBus: eventBus
                        });
                    });
                });
                overlayManager.add(/^\/categories\/add$/, function() {
                    return loader.loadModule('mobile.categories.overlays').then(function(loadingOverlay) {
                        var CategoryAddOverlay = M.require('mobile.categories.overlays/CategoryAddOverlay');
                        loadingOverlay.hide();
                        return new CategoryAddOverlay({
                            api: new mw.Api(),
                            isAnon: isAnon,
                            title: mobile.currentPage().title,
                            eventBus: eventBus
                        });
                    });
                });

                function initButton() {
                    $('.category-button').removeClass('hidden');
                }
                $(initButton);
            };
        },
        "resources/skins.minerva.options/trackShare.js": function(require, module) {
            (function(track, user, config) {
                function trackShare(action) {
                    track('event.MobileWebShareButton', {
                        pageTitle: config.get('wgTitle'),
                        namespaceId: config.get('wgNamespaceNumber'),
                        isAnon: user.isAnon(),
                        action: action,
                        pageToken: user.getPageviewToken()
                    });
                }
                module.exports = trackShare;
            }(mw.track, mw.user, mw.config));
        },
        "resources/skins.minerva.options/shareIcon.js": function(require, module) {
            (function(M, msg, config) {
                var Icon = M.require('mobile.startup').Icon,
                    trackShare = require('./trackShare.js');

                function clickShareHandler(navigator) {
                    return function() {
                        var url = new URL(window.location.href);
                        url.searchParams.append('wprov', 'mfsw1');
                        url.searchParams.delete('debug');
                        trackShare('clickShareButton');
                        navigator.share({
                            title: config.get('wgTitle'),
                            text: config.get('wgTitle'),
                            url: url.toString()
                        }).then(function() {
                            trackShare('SharedToApp');
                        });
                    };
                }

                function shareIcon(navigator) {
                    return new Icon({
                        tagName: 'button',
                        glyphPrefix: 'minerva',
                        title: msg('skin-minerva-share'),
                        name: 'share',
                        events: {
                            click: clickShareHandler(navigator)
                        }
                    });
                }
                module.exports = shareIcon;
            }(mw.mobileFrontend, mw.msg, mw.config));
        },
        "resources/skins.minerva.options/share.js": function(require,
            module) {
            module.exports = function() {
                var config = mw.config,
                    shareIcon = require('./shareIcon.js'),
                    trackShare = require('./trackShare.js'),
                    features = config.get('wgMinervaFeatures', {});

                function isShareAvailable() {
                    return navigator.share !== undefined;
                }
                if (isShareAvailable() && features.shareButton) {
                    $('<li>').addClass('page-actions-menu__list-item').append(shareIcon(navigator).$el).insertAfter($('#ca-watch').parent());
                    trackShare('shownShareButton');
                }
            };
        }
    }
}, {
    "css": [
        ".backtotop{ visibility:hidden;opacity:0; position:fixed;width:2.5em;height:2.5em;border-radius:100%;box-shadow:0.1em 0.2em 0.3em #c8ccd1;bottom:20px;right:0;cursor:pointer;z-index:3;background-color:#3366cc; -webkit-transition:opacity 0.5s 0s;-moz-transition:opacity 0.5s 0s;transition:opacity 0.5s 0s}.backtotop.visible{opacity:0.8}.backtotop.visible:hover{opacity:1}.backtotop \u003E .arrow-up{width:0;height:0;border-left:7px solid transparent;border-right:7px solid transparent;border-bottom:7px solid #fff;position:absolute;top:50%;left:50%;transform:translate(-50%,-50%)}"
    ]
}, {
    "skin-minerva-share": "Share"
}, {
    "BackToTopOverlay.mustache": "\u003Cdiv class=\"arrow-up\"\u003E\u003C/div\u003E\n"
});
mw.loader.implement("skins.minerva.scripts@1ynb5wg", {
    "main": "resources/skins.minerva.scripts/init.js",
    "files": {
        "resources/skins.minerva.scripts/init.js": function(require, module) {
            (function(M) {
                var mobile = M.require('mobile.startup'),
                    PageGateway = mobile.PageGateway,
                    toast = mobile.toast,
                    time = mobile.time,
                    toc = require('./toc.js'),
                    errorLogging = require('./errorLogging.js'),
                    notifications = require('./notifications.js'),
                    preInit = require('./preInit.js'),
                    mobileRedirect = require('./mobileRedirect.js'),
                    search = require('./search.js'),
                    references = require('./references.js'),
                    TitleUtil = require('./TitleUtil.js'),
                    issues = require('./page-issues/index.js'),
                    Toolbar = require('./Toolbar.js'),
                    ToggleList = require('../../components/ToggleList/ToggleList.js'),
                    TabScroll = require('./TabScroll.js'),
                    router = require('mediawiki.router'),
                    CtaDrawer = mobile.CtaDrawer,
                    desktopMMV = mw.loader.getState('mmv.bootstrap'),
                    Button = mobile.Button,
                    Anchor = mobile.Anchor,
                    overlayManager = require('./overlayManager.js'),
                    currentPage = mobile.currentPage(),
                    currentPageHTMLParser = mobile.currentPageHTMLParser(),
                    $redLinks = currentPageHTMLParser.getRedLinks(),
                    api = new mw.Api(),
                    eventBus = mobile.eventBusSingleton,
                    namespaceIDs = mw.config.get('wgNamespaceIds');

                function onClickImage(ev) {
                    if ((ev.button !== 0 && ev.which !== 1) || ev.altKey || ev.ctrlKey || ev.shiftKey || ev.metaKey) {
                        return;
                    }
                    ev.preventDefault();
                    routeThumbnail($(this).data('thumb'));
                }

                function routeThumbnail(thumbnail) {
                    router.navigate('#/media/' + encodeURIComponent(thumbnail.getFileName()));
                }

                function initMediaViewer($container) {
                    currentPageHTMLParser.getThumbnails($container).forEach(function(thumb) {
                        thumb.$el.off().data('thumb', thumb).on('click', onClickImage);
                    });
                }

                function initButton() {
                    var $primaryBtn = $('.language-selector');
                    if ($primaryBtn.length) {
                        $primaryBtn.on('click', function(ev) {
                            ev.preventDefault();
                            if ($primaryBtn.attr('href') || $primaryBtn.find('a').length) {
                                router.navigate('/languages');
                            } else {
                                toast.show(mw.msg('mobile-frontend-languages-not-available'));
                            }
                        });
                    }
                }

                function makeMediaViewerOverlayIfNeeded(
                    title) {
                    if (mw.loader.getState('mmv.bootstrap') === 'ready') {
                        return $.Deferred().reject();
                    }
                    return mobile.mediaViewer.overlay({
                        api: api,
                        thumbnails: currentPageHTMLParser.getThumbnails(),
                        title: decodeURIComponent(title),
                        eventBus: eventBus
                    });
                }
                overlayManager.add(/^\/media\/(.+)$/, makeMediaViewerOverlayIfNeeded);
                overlayManager.add(/^\/languages$/, function() {
                    return mobile.languageOverlay(new PageGateway(api));
                });
                $(function() {
                    initButton();
                });
                if (desktopMMV === null || desktopMMV === 'registered') {
                    mw.hook('wikipage.content').add(initMediaViewer);
                }

                function initHistoryLink($lastModifiedLink) {
                    var delta, historyUrl, msg, $bar, ts, username, gender;
                    historyUrl = $lastModifiedLink.attr('href');
                    ts = $lastModifiedLink.data('timestamp');
                    username = $lastModifiedLink.data('user-name') || !1;
                    gender = $lastModifiedLink.data('user-gender');
                    if (ts) {
                        delta = time.getTimeAgoDelta(parseInt(ts, 10));
                        if (time.isRecent(delta)) {
                            $bar = $lastModifiedLink.closest('.last-modified-bar');
                            $bar.addClass('active');
                            $bar.find('.mw-ui-icon-minerva-clock').
                            addClass('mw-ui-icon-minerva-clock-invert');
                            $bar.find('.mw-ui-icon-mf-arrow-gray').addClass('mw-ui-icon-mf-arrow-invert');
                        }
                        msg = time.getLastModifiedMessage(ts, username, gender, historyUrl);
                        $lastModifiedLink.replaceWith(msg);
                    }
                }

                function initModifiedInfo() {
                    $('.modified-enhancement').each(function() {
                        initHistoryLink($(this));
                    });
                }

                function initRegistrationDate($tagline) {
                    var msg, ts;
                    ts = $tagline.data('userpage-registration-date');
                    if (ts) {
                        msg = time.getRegistrationMessage(ts, $tagline.data('userpage-gender'));
                        $tagline.text(msg);
                    }
                }

                function initRegistrationInfo() {
                    $('#tagline-userpage').each(function() {
                        initRegistrationDate($(this));
                    });
                }

                function isUserUri(url) {
                    var title = TitleUtil.newFromUri(url),
                        namespace = title ? title.getNamespaceId() : undefined;
                    return namespace === namespaceIDs.user;
                }

                function initUserRedLinks() {
                    $redLinks.filter(function(_, element) {
                        return isUserUri(element.href);
                    }).each(function(_, element) {
                        var uri = new mw.Uri(element.href);
                        if (uri.query.action !== 'edit') {
                            return;
                        }
                        delete uri.query.action;
                        element.
                        href = uri.toString();
                    });
                }

                function initRedlinksCta() {
                    $redLinks.filter(function(_, element) {
                        return !isUserUri(element.href);
                    }).on('click', function(ev) {
                        var drawerOptions = {
                                progressiveButton: new Button({
                                    progressive: !0,
                                    label: mw.msg('mobile-frontend-editor-redlink-create'),
                                    href: $(this).attr('href')
                                }).options,
                                actionAnchor: new Anchor({
                                    progressive: !0,
                                    label: mw.msg('mobile-frontend-editor-redlink-leave'),
                                    additionalClassNames: 'cancel'
                                }).options,
                                content: mw.msg('mobile-frontend-editor-redlink-explain')
                            },
                            drawer = new CtaDrawer(drawerOptions);
                        ev.preventDefault();
                        drawer.show();
                    });
                }
                $(function() {
                    var $toc, toolbarElement = document.querySelector(Toolbar.selector),
                        userMenu = document.querySelector('.minerva-user-menu');
                    preInit();
                    references();
                    search();
                    mobileRedirect();
                    initModifiedInfo();
                    initRegistrationInfo();
                    initHistoryLink($('.last-modifier-tagline a'));
                    if (toolbarElement) {
                        Toolbar.bind(window, toolbarElement);
                        Toolbar.render(window, toolbarElement);
                    }
                    if (userMenu) {
                        ToggleList.bind(window, userMenu);
                    }
                    initRedlinksCta();
                    initUserRedLinks();
                    TabScroll.initTabsScrollPosition();
                    if (!currentPage.isMissing) {
                        issues.init(overlayManager, currentPageHTMLParser);
                    }
                    if (!mw.user.isAnon() && mw.config.get('wgEchoMaxNotificationCount') !== undefined) {
                        notifications();
                    }
                    $toc = $('#toc');
                    if (mw.config.get('wgAction') === 'view' && $toc.length > 0) {
                        toc(currentPage, $toc);
                    }
                    mw.requestIdleCallback(errorLogging);
                    mw.log.deprecate(router, 'navigate', router.navigate, 'use navigateTo instead');
                });
                module.exports = {
                    overlayManager: overlayManager
                };
            }(mw.mobileFrontend));
        },
        "resources/skins.minerva.scripts/NotificationBadge.js": function(require, module) {
            (function(M) {
                var mobile = M.require('mobile.startup'),
                    mfExtend = mobile.mfExtend,
                    View = mobile.View,
                    util = mobile.util,
                    Icon = mobile.Icon,
                    notificationIcon = new Icon({
                        name: 'bellOutline-base20',
                        glyphPrefix: 'wikimedia'
                    });

                function NotificationBadge(options) {
                    var $el, $notificationAnchor, count = options.notificationCountRaw || 0,
                        el = options.el;
                    if (el) {
                        $el = $(el);
                        options.hasUnseenNotifications = $el.find('.notification-unseen').length > 0;
                        options.hasNotifications = options.hasUnseenNotifications;
                        $notificationAnchor = $el.find('a');
                        options.title = $notificationAnchor.attr('title');
                        options.url = $notificationAnchor.attr('href');
                        count = Number($el.find('span').data('notification-count'));
                    }
                    View.call(this, util.extend({
                        notificationIconClass: notificationIcon.getClassName(),
                        hasNotifications: !1,
                        hasUnseenNotifications: !1,
                        notificationCountRaw: 0
                    }, options, {
                        isBorderBox: !1
                    }));
                    this.url = options.url;
                    this.setCount(count);
                    if (options.onClick) {
                        this.$el.on('click', options.onClick);
                    }
                }
                mfExtend(NotificationBadge, View, {
                    template: mw.template.get('skins.minerva.scripts', 'badge.mustache'),
                    setCount: function(count) {
                        if (count > 100) {
                            count = 100;
                        }
                        this.options.notificationCountRaw = count;
                        this.options.notificationCountString = mw.message('echo-badge-count', mw.language.convertNumber(count)).text();
                        this.options.isNotificationCountZero = count === 0;
                        this.render();
                    },
                    markAsSeen: function() {
                        this.options.hasUnseenNotifications = !1;
                        this.render();
                    }
                });
                module.exports =
                    NotificationBadge;
            }(mw.mobileFrontend));
        },
        "resources/skins.minerva.scripts/notifications.js": function(require, module) {
            module.exports = function() {
                var badge, M = mw.mobileFrontend,
                    mainMenu = require('./menu.js'),
                    router = require('mediawiki.router'),
                    mobile = M.require('mobile.startup'),
                    util = mobile.util,
                    NotificationBadge = require('./NotificationBadge.js'),
                    overlayManager = require('./overlayManager.js'),
                    initialized = !1;

                function showNotificationOverlay() {
                    var $pageCenter = $('#mw-mf-page-center'),
                        overlay = mobile.notifications.overlay(badge.setCount.bind(badge), badge.markAsSeen.bind(badge));
                    mainMenu.openNavigationDrawer('secondary');
                    overlay.on('hide', function() {
                        mainMenu.closeNavigationDrawers();
                        $pageCenter.off('.secondary');
                    });
                    $pageCenter.one('click.secondary', function() {
                        router.back();
                    });
                    return overlay;
                }
                util.docReady(function() {
                    badge = new NotificationBadge({
                        onClick: function(ev) {
                            router.navigate('#/notifications');
                            ev.preventDefault();
                        },
                        el: $('#user-notifications.user-button').parent()
                    });
                    overlayManager.add(
                        /^\/notifications$/, showNotificationOverlay);

                    function addFilterButton() {
                        var filterStatusButton = new OO.ui.ButtonWidget({
                            href: '#/notifications-filter',
                            classes: ['mw-echo-ui-notificationsInboxWidget-main-toolbar-nav-filter-placeholder'],
                            icon: 'funnel',
                            label: mw.msg('mobile-frontend-notifications-filter')
                        });
                        $('.mw-echo-ui-notificationsInboxWidget-cell-placeholder').append($('<div>').addClass('mw-echo-ui-notificationsInboxWidget-main-toolbar-nav-filter').addClass('mw-echo-ui-notificationsInboxWidget-cell').append(filterStatusButton.$element));
                    }
                    mw.hook('ext.echo.special.onInitialize').add(function() {
                        if (initialized) {
                            return;
                        }
                        mw.loader.using('mobile.notifications.overlay').then(function() {
                            var $crossWikiUnreadFilter = $('.mw-echo-ui-crossWikiUnreadFilterWidget'),
                                $notifReadState = $('.mw-echo-ui-notificationsInboxWidget-main-toolbar-readState'),
                                NotificationsFilterOverlay = M.require('mobile.notifications.overlay/NotificationsFilterOverlay');
                            addFilterButton();
                            overlayManager.add(/^\/notifications-filter$/,
                                function() {
                                    mainMenu.openNavigationDrawer('secondary');
                                    return new NotificationsFilterOverlay({
                                        $notifReadState: $notifReadState,
                                        mainMenu: mainMenu,
                                        $crossWikiUnreadFilter: $crossWikiUnreadFilter
                                    });
                                });
                        });
                        initialized = !0;
                    });
                });
            };
        },
        "resources/skins.minerva.scripts/overlayManager.js": function(require, module) {
            var mobile = mw.mobileFrontend.require('mobile.startup'),
                OverlayManager = mobile.OverlayManager,
                overlayManager = OverlayManager.getSingleton();
            module.exports = overlayManager;
        },
        "resources/skins.minerva.scripts/menu/MainMenu.js": function(require, module) {
            (function(M) {
                var mobile = M.require('mobile.startup'),
                    mfExtend = mobile.mfExtend,
                    browser = mobile.Browser.getSingleton(),
                    View = mobile.View;

                function MainMenu(options) {
                    this.activator = options.activator;
                    View.call(this, options);
                }
                mfExtend(MainMenu, View, {
                    isTemplateMode: !0,
                    template: mw.template.get('skins.minerva.scripts', 'menu.mustache'),
                    templatePartials: {
                        menuGroup: mw.template.get('skins.minerva.scripts', 'menuGroup.mustache')
                    },
                    defaults: {
                        activator: undefined
                    },
                    postRender: function() {
                        if (!browser.supportsGeoLocation()) {
                            this.$el.find('.nearby').parent().remove();
                        }
                        this.registerClickEvents();
                    },
                    registerClickEvents: function() {
                        var self = this;
                        $(this.activator).off('click').on('click', function(ev) {
                            if (self.isOpen()) {
                                self.closeNavigationDrawers();
                            } else {
                                self.openNavigationDrawer();
                            }
                            ev.preventDefault();
                        });
                    },
                    isOpen: function() {
                        return $(document.body).hasClass('navigation-enabled');
                    },
                    closeNavigationDrawers: function() {
                        $(document.body).removeClass('navigation-enabled').removeClass('secondary-navigation-enabled').removeClass('primary-navigation-enabled');
                    },
                    openNavigationDrawer: function(drawerType) {
                        this.closeNavigationDrawers();
                        drawerType = drawerType || 'primary';
                        $(document.body).toggleClass('navigation-enabled').toggleClass(drawerType + '-navigation-enabled');
                        this.emit('open');
                    }
                });
                module.exports = MainMenu;
            }(mw.mobileFrontend));
        },
        "resources/skins.minerva.scripts/menu.js": function(require, module) {
            var MainMenu = require('./menu/MainMenu.js'),
                mainMenu = createMainMenu();

            function createMainMenu() {
                var options = mw.config.get('wgMinervaMenuData', {});
                options.activator = '.header .main-menu-button';
                return new MainMenu(options);
            }
            $(function() {
                if (!$('#mw-mf-page-left').find('.menu').length) {
                    mainMenu.registerClickEvents();
                    mainMenu.appendTo('#mw-mf-page-left');
                }
            });
            module.exports = mainMenu;
        },
        "resources/skins.minerva.scripts/errorLogging.js": function(require, module) {
            (function(M, track, config, trackSubscribe, user, experiments) {
                module.exports = function() {
                    function handleError(errorMessage, lineNumber, columnNumber, errorUrl) {
                        var suffix, errorSamplingRate = config.get('wgMinervaErrorLogSamplingRate', 0),
                            sessionToken = user.sessionId(),
                            EVENT_CLIENT_ERROR_LOG = 'wikimedia.event.WebClientError',
                            mobile = M.require('mobile.startup'),
                            currentPage = mobile.currentPage(),
                            util = mobile.util,
                            errorExperiment = {
                                name: 'WebClientError',
                                enabled: errorSamplingRate > 0,
                                buckets: {
                                    on: errorSamplingRate,
                                    off: 1 - errorSamplingRate
                                }
                            },
                            isErrorLoggingEnabled = experiments.getBucket(errorExperiment, sessionToken) === 'on',
                            DEFAULT_ERROR_DATA = {
                                sessionToken: sessionToken,
                                skin: config.get('skin'),
                                wgVersion: config.get('wgVersion'),
                                mobileMode: config.get('wgMFMode', 'desktop'),
                                isAnon: user.isAnon(),
                                revision: currentPage.getRevisionId()
                            };
                        if (isErrorLoggingEnabled) {
                            track(EVENT_CLIENT_ERROR_LOG, util.extend({
                                userUrl: window.location.href,
                                errorUrl: errorUrl,
                                errorMessage: errorMessage,
                                errorStackTrace: '',
                                errorLineNumber: lineNumber || 0,
                                errorColumnNumber: columnNumber || 0
                            }, DEFAULT_ERROR_DATA));
                        }
                        if (config.get('wgMinervaCountErrors')) {
                            suffix = user.isAnon() ? '.anon' : '.loggedin';
                            mw.track('counter.MediaWiki.minerva.WebClientError' + suffix, 1);
                        }
                    }
                    trackSubscribe('resourceloader.exception', function(topic, data) {
                        var error = data.exception;
                        handleError(error.message, error.lineNumber, error.columnNumber);
                    });
                    trackSubscribe('global.error', function(topic, error) {
                        handleError(error.errorMessage, error.lineNumber, error.columnNumber, error.url);
                    });
                };
            }(mw.mobileFrontend, mw.track, mw.config, mw.trackSubscribe, mw.user, mw.experiments));
        },
        "resources/skins.minerva.scripts/preInit.js": function(require, module) {
            module.exports = function() {
                var M = mw.mobileFrontend,
                    mobile = M.require('mobile.startup'),
                    skin = mobile.Skin.getSingleton(),
                    mainMenu = require('./menu.js');

                function onSkinClick(ev) {
                    var $target = $(ev.target);
                    if (mainMenu && mainMenu.isOpen() && !$target.hasClass('main-menu-button')) {
                        mainMenu.closeNavigationDrawers();
                        ev.preventDefault();
                    }
                }
                skin.on('click', onSkinClick.bind(skin));
                (function(wgRedirectedFrom) {
                    var redirectedFrom, $msg, title;
                    if (wgRedirectedFrom === null) {
                        return;
                    }
                    redirectedFrom = mw.Title.newFromText(wgRedirectedFrom);
                    if (redirectedFrom) {
                        title = redirectedFrom.getPrefixedText();
                        $msg = $('<div>').html(mw.message('mobile-frontend-redirected-from', title).parse());
                        $msg.find('a').attr('href', mw.util.getUrl(title, {
                            redirect: 'no'
                        }));
                        mw.notify($msg);
                    }
                }(mw.config.get('wgRedirectedFrom')));
            };
        },
        "resources/skins.minerva.scripts/downloadPageAction.js": function(require, module) {
            (function(M, track, msg) {
                var MAX_PRINT_TIMEOUT = 3000,
                    mobile = M.require(
                        'mobile.startup'),
                    Icon = mobile.Icon,
                    icons = mobile.icons,
                    lazyImageLoader = mobile.lazyImages.lazyImageLoader,
                    browser = mobile.Browser.getSingleton(),
                    GLYPH = 'download';

                function getAndroidVersion(userAgent) {
                    var match = userAgent.toLowerCase().match(/android\s(\d\.]*)/);
                    return match ? parseInt(match[1]) : !1;
                }

                function getChromeVersion(userAgent) {
                    var match = userAgent.toLowerCase().match(/chrom(e|ium)\/(\d+)\./);
                    return match ? parseInt(match[2]) : !1;
                }

                function isAvailable(windowObj, page, userAgent, supportedNamespaces) {
                    var androidVersion = getAndroidVersion(userAgent),
                        chromeVersion = getChromeVersion(userAgent);
                    if (supportedNamespaces.indexOf(page.getNamespaceId()) === -1 || page.isMainPage() || page.isMissing) {
                        return false;
                    }
                    if (browser.isIos() || chromeVersion === false || windowObj.chrome === undefined) {
                        return false;
                    }
                    if ((androidVersion && androidVersion < 5) || chromeVersion < 41) {
                        return false;
                    }
                    return true;
                }

                function onClick(icon, spinner) {
                    function doPrint() {
                        icon.timeout = clearTimeout(icon.timeout);
                        track('minerva.downloadAsPDF', {
                            action: 'callPrint'
                        });
                        window.print();
                        icon.$el.show();
                        spinner.$el.hide();
                    }

                    function doPrintBeforeTimeout() {
                        if (icon.timeout) {
                            doPrint();
                        }
                    }
                    if (!icon.timeout) {
                        track('minerva.downloadAsPDF', {
                            action: 'fetchImages'
                        });
                        icon.$el.hide();
                        spinner.$el.show();
                        icon.timeout = setTimeout(doPrint, MAX_PRINT_TIMEOUT);
                        lazyImageLoader.loadImages(lazyImageLoader.queryPlaceholders(document.getElementById('content'))).then(doPrintBeforeTimeout, doPrintBeforeTimeout);
                    }
                }

                function getOnClickHandler(spinner) {
                    return function() {
                        onClick(this, spinner);
                    };
                }

                function downloadPageAction(page, supportedNamespaces, windowObj, hasText) {
                    var modifier = hasText ? 'toggle-list-item__anchor--menu' : 'mw-ui-icon-element',
                        icon, spinner = icons.spinner({
                            hasText: hasText,
                            modifier: modifier
                        });
                    if (isAvailable(windowObj, page, navigator.userAgent, supportedNamespaces)) {
                        icon = new Icon({
                            glyphPrefix: 'minerva',
                            title: msg('minerva-download'),
                            name: GLYPH,
                            tagName: 'button',
                            events: {
                                click: getOnClickHandler(spinner)
                            },
                            hasText: hasText,
                            label: hasText ? mw.msg('minerva-download') : '',
                            modifier: modifier
                        });
                        return $('<li>').addClass('page-actions-menu__list-item').append(icon.$el).append(spinner.$el.hide());
                    } else {
                        return null;
                    }
                }
                module.exports = {
                    downloadPageAction: downloadPageAction,
                    test: {
                        isAvailable: isAvailable,
                        getOnClickHandler: getOnClickHandler
                    }
                };
            }(mw.mobileFrontend, mw.track, mw.msg));
        },
        "resources/skins.minerva.scripts/page-issues/parser.js": function(require, module) {
            (function(M) {
                var Icon = M.require('mobile.startup').Icon,
                    ICON_NAME = {
                        SEVERITY: {
                            DEFAULT: 'issue-generic',
                            LOW: 'issue-severity-low',
                            MEDIUM: 'issue-severity-medium',
                            HIGH: 'issue-generic'
                        },
                        TYPE: {
                            MOVE: 'issue-type-move',
                            POINT_OF_VIEW: 'issue-type-point-of-view'
                        }
                    },
                    ICON_COLOR = {
                        DEFAULT: 'defaultColor',
                        LOW: 'lowColor',
                        MEDIUM: 'mediumColor',
                        HIGH: 'highColor'
                    },
                    SEVERITY_LEVEL = {
                        DEFAULT: 0,
                        LOW: 1,
                        MEDIUM: 2,
                        HIGH: 3
                    },
                    SEVERITY_REGEX = {
                        LOW: /mobile-issue-severity-low|ambox-style|avviso-stile/,
                        MEDIUM: /mobile-issue-severity-medium|ambox-content|avviso-contenuto/,
                        HIGH: /mobile-issue-severity-high|ambox-speedy|ambox-delete|ambox-serious|avviso-importante/
                    },
                    TYPE_REGEX = {
                        MOVE: /mobile-issue-move|ambox-converted|ambox-move|ambox-merge|avviso-struttura/,
                        POINT_OF_VIEW: new RegExp(['mobile-issue-pov', 'ambox-Advert', 'ambox-autobiography', 'ambox-believerpov', 'ambox-COI', 'ambox-coverage', 'ambox-criticism', 'ambox-fanpov', 'ambox-fringe-theories', 'ambox-geographical-imbalance', 'ambox-globalize', 'ambox-npov-language', 'ambox-POV', 'ambox-pseudo', 'ambox-systemic-bias', 'ambox-unbalanced', 'ambox-usgovtpov'].join('|'))
                    },
                    GROUPED_PARENT_REGEX = /mw-collapsible-content/,
                    TYPE_SEVERITY = {
                        MOVE: 'DEFAULT',
                        POINT_OF_VIEW: 'MEDIUM'
                    };

                function parseSeverity(box) {
                    var severity, identified;
                    identified = Object.keys(SEVERITY_REGEX).some(function(key) {
                        var regex = SEVERITY_REGEX[key];
                        severity = key;
                        return regex.test(box.className);
                    });
                    return identified ? severity : 'DEFAULT';
                }

                function parseType(box, severity) {
                    var identified, identifiedType;
                    identified = Object.keys(TYPE_REGEX).some(function(type) {
                        var regex =
                            TYPE_REGEX[type];
                        identifiedType = type;
                        return regex.test(box.className);
                    });
                    return {
                        name: identified ? ICON_NAME.TYPE[identifiedType] : ICON_NAME.SEVERITY[severity],
                        severity: identified ? TYPE_SEVERITY[identifiedType] : severity
                    };
                }

                function parseGroup(box) {
                    return !!box.parentNode && GROUPED_PARENT_REGEX.test(box.parentNode.className);
                }

                function iconName(box, severity) {
                    var nameSeverity = parseType(box, severity);
                    return nameSeverity.name + '-' + ICON_COLOR[nameSeverity.severity];
                }

                function maxSeverity(severityLevels) {
                    return severityLevels.reduce(function(max, severity) {
                        return SEVERITY_LEVEL[max] > SEVERITY_LEVEL[severity] ? max : severity;
                    }, 'DEFAULT');
                }

                function parse(box) {
                    var severity = parseSeverity(box);
                    return {
                        severity: severity,
                        grouped: parseGroup(box),
                        icon: new Icon({
                            glyphPrefix: 'minerva',
                            name: iconName(box, severity)
                        })
                    };
                }

                function extract($box) {
                    var SELECTOR = '.mbox-text, .ambox-text',
                        $container = $('<div>'),
                        pageIssue;
                    $box.find(SELECTOR).each(function() {
                        var contents, $this = $(this);
                        $this.find('table, .noprint').remove();
                        contents =
                            $this.html();
                        if (contents) {
                            $('<p>').html(contents).appendTo($container);
                        }
                    });
                    pageIssue = parse($box.get(0));
                    return {
                        issue: pageIssue,
                        $el: $box,
                        iconString: pageIssue.icon.toHtmlString(),
                        text: $container.html()
                    };
                }
                module.exports = {
                    extract: extract,
                    parse: parse,
                    maxSeverity: maxSeverity,
                    iconName: iconName,
                    test: {
                        parseSeverity: parseSeverity,
                        parseType: parseType,
                        parseGroup: parseGroup
                    }
                };
            }(mw.mobileFrontend));
        },
        "resources/skins.minerva.scripts/AB.js": function(require, module) {
            (function(M, mwExperiments) {
                var bucket = {
                    UNSAMPLED: 'unsampled',
                    CONTROL: 'control',
                    TREATMENT: 'treatment'
                };

                function AB(config) {
                    var testName = config.testName,
                        samplingRate = config.samplingRate,
                        sessionId = config.sessionId,
                        test = {
                            name: testName,
                            enabled: !!samplingRate,
                            buckets: {
                                unsampled: 1 - samplingRate,
                                control: samplingRate / 2,
                                treatment: samplingRate / 2
                            }
                        };

                    function getBucket() {
                        return mwExperiments.getBucket(test, sessionId);
                    }

                    function isControl() {
                        return getBucket() === bucket.CONTROL;
                    }

                    function isTreatment() {
                        return getBucket() === bucket.TREATMENT;
                    }

                    function isSampled() {
                        return getBucket() !== bucket.UNSAMPLED;
                    }
                    return {
                        isControl: isControl,
                        isTreatment: isTreatment,
                        isSampled: isSampled
                    };
                }
                module.exports = AB;
            }(mw.mobileFrontend, mw.experiments));
        },
        "resources/skins.minerva.scripts/page-issues/overlay/IssueNotice.js": function(require, module) {
            (function(M) {
                var mobile = M.require('mobile.startup'),
                    mfExtend = mobile.mfExtend,
                    View = mobile.View;

                function IssueNotice(props) {
                    View.call(this, props);
                }
                mfExtend(IssueNotice, View, {
                    tagName: 'li',
                    template: mw.template.get('skins.minerva.scripts', 'IssueNotice.mustache'),
                    postRender: function() {
                        View.prototype.postRender.apply(this, arguments);
                        this.$el.find('.issue-notice').prepend(this.options.issue.icon.$el);
                    }
                });
                module.exports = IssueNotice;
            }(mw.mobileFrontend));
        },
        "resources/skins.minerva.scripts/page-issues/overlay/IssueList.js": function(require, module) {
            (function(M) {
                var mobile = M.require('mobile.startup'),
                    mfExtend = mobile.mfExtend,
                    View = mobile.View,
                    IssueNotice = require('./IssueNotice.js');

                function IssueList(issues) {
                    this.issues = issues;
                    View.call(
                        this, {
                            className: 'cleanup'
                        });
                }
                mfExtend(IssueList, View, {
                    tagName: 'ul',
                    postRender: function() {
                        View.prototype.postRender.apply(this, arguments);
                        this.append(this.issues.map(function(issue) {
                            return new IssueNotice(issue).$el;
                        }));
                    }
                });
                module.exports = IssueList;
            }(mw.mobileFrontend));
        },
        "resources/skins.minerva.scripts/page-issues/overlay/pageIssuesOverlay.js": function(require, module) {
            (function(M, mwMsg) {
                var Overlay = M.require('mobile.startup').Overlay,
                    IssueList = require('./IssueList.js'),
                    KEYWORD_ALL_SECTIONS = 'all',
                    NS_MAIN = 0,
                    NS_TALK = 1,
                    NS_CATEGORY = 14;

                function pageIssuesOverlay(issues, section, namespaceID) {
                    var overlay, headingText = section === '0' || section === KEYWORD_ALL_SECTIONS ? getNamespaceHeadingText(namespaceID) : mwMsg('minerva-meta-data-issues-section-header');
                    overlay = new Overlay({
                        className: 'overlay overlay-issues',
                        heading: '<strong>' + headingText + '</strong>'
                    });
                    overlay.$el.find('.overlay-content').append(new IssueList(issues).$el);
                    return overlay;
                }

                function getNamespaceHeadingText(namespaceID) {
                    switch (namespaceID) {
                        case NS_CATEGORY:
                            return mw.msg('mobile-frontend-meta-data-issues-categories');
                        case NS_TALK:
                            return mw.msg('mobile-frontend-meta-data-issues-talk');
                        case NS_MAIN:
                            return mw.msg('mobile-frontend-meta-data-issues');
                        default:
                            return '';
                    }
                }
                module.exports = pageIssuesOverlay;
            }(mw.mobileFrontend, mw.msg));
        },
        "resources/skins.minerva.scripts/page-issues/page/PageIssueLearnMoreLink.js": function(require, module) {
            (function() {
                function newPageIssueLearnMoreLink(msg) {
                    return $('<span>').addClass('ambox-learn-more').text(msg);
                }
                module.exports = newPageIssueLearnMoreLink;
            }());
        },
        "resources/skins.minerva.scripts/page-issues/page/PageIssueLink.js": function(require, module) {
            (function() {
                function newPageIssueLink(labelText) {
                    return $('<a>').addClass('cleanup mw-mf-cleanup').text(labelText);
                }
                module.exports = newPageIssueLink;
            }());
        },
        "resources/skins.minerva.scripts/page-issues/page/pageIssueFormatter.js": function(require, module) {
            (function() {
                var newPageIssueLink = require('./PageIssueLink.js'),
                    newPageIssueLearnMoreLink = require(
                        './PageIssueLearnMoreLink.js');

                function insertPageIssueBanner(issue, msg, overlayUrl, overlayManager, multiple) {
                    var $learnMoreEl = newPageIssueLearnMoreLink(msg),
                        $issueContainer = multiple ? issue.$el.parents('.mbox-text-span, .mbox-text-div') : issue.$el.find('.mbox-text'),
                        $clickContainer = multiple ? issue.$el.parents('.mbox-text') : issue.$el;
                    $issueContainer.prepend(issue.iconString);
                    $issueContainer.prepend($learnMoreEl);
                    $clickContainer.on('click', function() {
                        overlayManager.router.navigate(overlayUrl);
                        return false;
                    });
                }

                function insertPageIssueNotice(labelText, section) {
                    var $link = newPageIssueLink(labelText);
                    $link.attr('href', '#/issues/' + section);
                    $link.insertAfter($('h1#section_0'));
                }
                module.exports = {
                    insertPageIssueBanner: insertPageIssueBanner,
                    insertPageIssueNotice: insertPageIssueNotice
                };
            }());
        },
        "resources/skins.minerva.scripts/page-issues/index.js": function(require, module) {
            (function(M) {
                var PageHTMLParser = M.require('mobile.startup').PageHTMLParser,
                    KEYWORD_ALL_SECTIONS = 'all',
                    config = mw.config,
                    NS_MAIN = 0,
                    NS_TALK = 1,
                    NS_CATEGORY = 14,
                    CURRENT_NS = config.get('wgNamespaceNumber'),
                    features = mw.config.get('wgMinervaFeatures', {}),
                    pageIssuesParser = require('./parser.js'),
                    pageIssuesOverlay = require('./overlay/pageIssuesOverlay.js'),
                    pageIssueFormatter = require('./page/pageIssueFormatter.js'),
                    QUERY_STRING_FLAG = mw.util.getParamValue('minerva-issues'),
                    newTreatmentEnabled = features.pageIssues || QUERY_STRING_FLAG;

                function insertBannersOrNotice(pageHTMLParser, labelText, section, inline, overlayManager) {
                    var $metadata, issueUrl = section === KEYWORD_ALL_SECTIONS ? '#/issues/' + KEYWORD_ALL_SECTIONS : '#/issues/' + section,
                        selector = ['.ambox', '.tmbox', '.cmbox', '.fmbox'].join(','),
                        issueSummaries = [];
                    if (section === KEYWORD_ALL_SECTIONS) {
                        $metadata = pageHTMLParser.$el.find(selector);
                    } else {
                        $metadata = pageHTMLParser.findChildInSectionLead(parseInt(section, 10), selector);
                    }
                    $metadata.find('.NavFrame').remove();
                    $metadata.each(function() {
                        var issueSummary, $this = $(this);
                        if ($this.find(selector).length === 0) {
                            issueSummary = pageIssuesParser.extract($this);
                            if (issueSummary.text) {
                                issueSummaries.push(issueSummary);
                            }
                        }
                    });
                    if (inline) {
                        issueSummaries.forEach(function(issueSummary, i) {
                            var isGrouped = issueSummary.issue.grouped,
                                lastIssueIsGrouped = issueSummaries[i - 1] && issueSummaries[i - 1].issue.grouped,
                                multiple = isGrouped && !lastIssueIsGrouped;
                            pageIssueFormatter.insertPageIssueBanner(issueSummary, mw.msg('skin-minerva-issue-learn-more'), issueUrl, overlayManager, multiple);
                        });
                    } else if (issueSummaries.length) {
                        pageIssueFormatter.insertPageIssueNotice(labelText, section);
                    }
                    return {
                        ambox: $metadata,
                        issueSummaries: issueSummaries
                    };
                }

                function getIssues(allIssues, section) {
                    if (section !== KEYWORD_ALL_SECTIONS) {
                        return allIssues[section] || [];
                    }
                    return allIssues[KEYWORD_ALL_SECTIONS] || Object.keys(allIssues).reduce(function(all, key) {
                        return all.concat(allIssues[key]);
                    }, []);
                }

                function initPageIssues(overlayManager, pageHTMLParser) {
                    var section, issueSummaries = [],
                        allIssues = {},
                        label, $lead = pageHTMLParser.getLeadSectionElement(),
                        issueOverlayShowAll = CURRENT_NS === NS_CATEGORY || CURRENT_NS ===
                        NS_TALK || !$lead,
                        inline = newTreatmentEnabled && CURRENT_NS === 0;
                    if (newTreatmentEnabled) {
                        $('html').addClass('issues-group-B');
                    }
                    if (CURRENT_NS === NS_TALK || CURRENT_NS === NS_CATEGORY) {
                        section = KEYWORD_ALL_SECTIONS;
                        issueSummaries = insertBannersOrNotice(pageHTMLParser, mw.msg('mobile-frontend-meta-data-issues-header-talk'), section, inline, overlayManager).issueSummaries;
                        allIssues[section] = issueSummaries;
                    } else if (CURRENT_NS === NS_MAIN) {
                        label = mw.msg('mobile-frontend-meta-data-issues-header');
                        if (issueOverlayShowAll) {
                            section = KEYWORD_ALL_SECTIONS;
                            issueSummaries = insertBannersOrNotice(pageHTMLParser, label, section, inline, overlayManager).issueSummaries;
                            allIssues[section] = issueSummaries;
                        } else {
                            section = '0';
                            issueSummaries = insertBannersOrNotice(pageHTMLParser, label, section, inline, overlayManager).issueSummaries;
                            allIssues[section] = issueSummaries;
                            if (newTreatmentEnabled) {
                                pageHTMLParser.$el.find(PageHTMLParser.HEADING_SELECTOR).each(function(i, headingEl) {
                                    var $headingEl = $(headingEl),
                                        sectionNum = $headingEl.find('.edit-page').
                                    data('section');
                                    if (sectionNum) {
                                        section = sectionNum.toString();
                                        issueSummaries = insertBannersOrNotice(pageHTMLParser, label, section, inline, overlayManager).issueSummaries;
                                        allIssues[section] = issueSummaries;
                                    }
                                });
                            }
                        }
                    }
                    overlayManager.add(new RegExp('^/issues/(\\d+|' + KEYWORD_ALL_SECTIONS + ')$'), function(section) {
                        return pageIssuesOverlay(getIssues(allIssues, section), section, CURRENT_NS);
                    });
                }
                module.exports = {
                    init: initPageIssues,
                    test: {
                        insertBannersOrNotice: insertBannersOrNotice
                    }
                };
            }(mw.mobileFrontend));
        },
        "resources/skins.minerva.scripts/UriUtil.js": function(require, module) {
            (function() {
                function isInternal(uri) {
                    return uri.host === mw.Uri().host;
                }
                module.exports = {
                    isInternal: isInternal
                };
            }());
        },
        "resources/skins.minerva.scripts/TitleUtil.js": function(require, module) {
            (function() {
                var UriUtil = require('./UriUtil.js');

                function newFromUri(uri, options) {
                    var mwUri, regExp, matches, title;
                    try {
                        mwUri = new mw.Uri(uri, options);
                    } catch (e) {
                        return null;
                    }
                    if (!UriUtil.isInternal(mwUri)) {
                        return null;
                    }
                    if ((options || {}).validateReadOnlyLink && !
                        isReadOnlyUri(mwUri)) {
                        return null;
                    }
                    if (mwUri.query.title) {
                        regExp = new RegExp('^' + mw.RegExp.escape(mw.config.get('wgScriptPath')) + '/');
                        matches = regExp.test(mwUri.path);
                        if (!matches) {
                            return null;
                        }
                        title = mwUri.query.title;
                    } else {
                        regExp = new RegExp('^' + mw.RegExp.escape(mw.config.get('wgArticlePath')).replace('\\$1', '(.+)'));
                        matches = regExp.exec(mwUri.path);
                        if (!matches || !matches[1]) {
                            return null;
                        }
                        try {
                            title = decodeURIComponent(matches[1]);
                        } catch (e) {
                            return null;
                        }
                    }
                    title += mwUri.fragment ? '#' + mwUri.fragment : '';
                    return mw.Title.newFromText(title);
                }

                function isReadOnlyUri(uri) {
                    var length = Object.keys(uri.query).length;
                    return length === (('oldid' in uri.query ? 1 : 0) + ('title' in uri.query ? 1 : 0));
                }
                module.exports = {
                    newFromUri: newFromUri
                };
            }());
        },
        "components/ToggleList/ToggleList.js": function(require, module) {
            (function() {
                var selector = '.toggle-list',
                    toggleSelector = '.toggle-list__toggle',
                    checkboxSelector = '.toggle-list__checkbox';

                function bind(window, component) {
                    var toggle = component.querySelector(toggleSelector),
                        checkbox = (
                            component.querySelector(checkboxSelector));
                    window.addEventListener('click', function(event) {
                        if (event.target !== toggle && event.target !== checkbox) {
                            _dismiss(checkbox);
                        }
                    }, true);
                    window.addEventListener('focusin', function(event) {
                        if (event.target instanceof Node && !component.contains(event.target)) {
                            _dismiss(checkbox);
                        }
                    }, true);
                    checkbox.addEventListener('change', _updateAriaExpanded.bind(undefined, checkbox));
                }

                function _dismiss(checkbox) {
                    checkbox.checked = !1;
                    _updateAriaExpanded(checkbox);
                }

                function _updateAriaExpanded(checkbox) {
                    checkbox.setAttribute('aria-expanded', (!!checkbox.checked).toString());
                }
                module.exports = Object.freeze({
                    selector: selector,
                    bind: bind
                });
            }());
        },
        "resources/skins.minerva.scripts/TabScroll.js": function(require, module) {
            var scrollLeftStyle = null;

            function testScrollLeftStyle() {
                var definer, $definer;
                if (scrollLeftStyle !== null) {
                    return scrollLeftStyle;
                }
                $definer = $('<div>').attr({
                    dir: 'rtl',
                    style: 'font-size: 14px; width: 4px; height: 1px; position: absolute; top: -1000px; overflow: scroll;'
                }).
                text('ABCD');
                $definer.appendTo('body');
                definer = $definer[0];
                if (definer.scrollLeft > 0) {
                    scrollLeftStyle = 'default';
                } else {
                    definer.scrollLeft = 1;
                    if (definer.scrollLeft === 0) {
                        scrollLeftStyle = 'negative';
                    } else {
                        scrollLeftStyle = 'reverse';
                    }
                }
                $definer.remove();
                return scrollLeftStyle;
            }

            function initTabsScrollPosition() {
                var selectedTab, tabContainer, $tabContainer, maxScrollLeft, leftMostChild, rightMostChild, dir, widthDiff, tabPosition, containerPosition, left, increaseScrollLeft, $selectedTab = $('.minerva__tab.selected');

                function setScrollLeft(sl) {
                    if (dir === 'ltr') {
                        tabContainer.scrollLeft = sl;
                        return;
                    }
                    if (testScrollLeftStyle() === 'reverse') {
                        sl = maxScrollLeft - sl;
                    } else if (testScrollLeftStyle() === 'negative') {
                        sl = -(maxScrollLeft - sl);
                    }
                    tabContainer.scrollLeft = sl;
                }
                if ($selectedTab.length !== 1) {
                    return;
                }
                selectedTab = $selectedTab.get(0);
                $tabContainer = $selectedTab.closest('.minerva__tab-container');
                tabContainer = $tabContainer.get(0);
                maxScrollLeft = tabContainer.scrollWidth - tabContainer.clientWidth;
                dir = $tabContainer.css('direction') || 'ltr';
                leftMostChild = dir === 'ltr' ? tabContainer.firstElementChild : tabContainer.lastElementChild;
                rightMostChild = dir === 'ltr' ? tabContainer.lastElementChild : tabContainer.firstElementChild;
                widthDiff = tabContainer.clientWidth - selectedTab.clientWidth;
                if (selectedTab === leftMostChild) {
                    if (dir === 'ltr' || widthDiff >= 0) {
                        setScrollLeft(0);
                    } else {
                        setScrollLeft(-widthDiff);
                    }
                } else if (selectedTab === rightMostChild) {
                    if (dir === 'rtl' || widthDiff >= 0) {
                        setScrollLeft(maxScrollLeft);
                    } else {
                        setScrollLeft(maxScrollLeft + widthDiff);
                    }
                } else {
                    tabPosition = $selectedTab.position();
                    containerPosition = $tabContainer.position();
                    left = tabPosition.left - containerPosition.left;
                    if (widthDiff >= 0) {
                        increaseScrollLeft = left - widthDiff / 2;
                    } else if (dir === 'ltr') {
                        increaseScrollLeft = left;
                    } else {
                        increaseScrollLeft = left - widthDiff;
                    }
                    tabContainer.scrollLeft += increaseScrollLeft * (testScrollLeftStyle() === 'reverse' ? -1 : 1);
                }
            }
            module.exports = {
                initTabsScrollPosition: initTabsScrollPosition
            };
        },
        "resources/skins.minerva.scripts/Toolbar.js": function(require, module) {
            (function(M) {
                var
                    mobile = M.require('mobile.startup'),
                    ToggleList = require('../../components/ToggleList/ToggleList.js'),
                    downloadPageAction = require('./downloadPageAction.js').downloadPageAction,
                    Icon = mobile.Icon,
                    page = mobile.currentPage(),
                    toolbarSelector = '.page-actions-menu',
                    overflowSubmenuSelector = '#page-actions-overflow',
                    overflowListSelector = '.toggle-list__list';

                function bind(window, toolbar) {
                    var overflowSubmenu = toolbar.querySelector(overflowSubmenuSelector);
                    if (overflowSubmenu) {
                        ToggleList.bind(window, overflowSubmenu);
                    }
                }

                function render(window, toolbar) {
                    var overflowList = toolbar.querySelector(overflowListSelector);
                    renderEditButton();
                    renderDownloadButton(window, overflowList);
                }

                function renderEditButton() {
                    var disabledEditIcon = new Icon({
                            name: 'edit',
                            glyphPrefix: 'minerva'
                        }),
                        enabledEditIcon = new Icon({
                            name: 'edit-enabled',
                            glyphPrefix: 'minerva'
                        }),
                        enabledClass = enabledEditIcon.getGlyphClassName(),
                        disabledClass = disabledEditIcon.getGlyphClassName();
                    if (mw.config.get('wgMinervaReadOnly')) {
                        $('#ca-edit').removeClass(enabledClass)
                            .addClass(disabledClass);
                    }
                }

                function renderDownloadButton(window, overflowList) {
                    var $downloadAction = downloadPageAction(page, mw.config.get('wgMinervaDownloadNamespaces', []), window, !!overflowList);
                    if ($downloadAction) {
                        if (overflowList) {
                            $downloadAction.appendTo(overflowList);
                        } else {
                            $downloadAction.insertAfter('.page-actions-menu__list-item:first-child');
                        }
                        mw.track('minerva.downloadAsPDF', {
                            action: 'buttonVisible'
                        });
                    }
                }
                module.exports = {
                    selector: toolbarSelector,
                    bind: bind,
                    render: render
                };
            }(mw.mobileFrontend));
        },
        "resources/skins.minerva.scripts/mobileRedirect.js": function(require, module) {
            module.exports = function() {
                function cookiesEnabled() {
                    if ($.cookie('mf_testcookie') === 'test_value') {
                        return true;
                    } else {
                        $.cookie('mf_testcookie', 'test_value', {
                            path: '/'
                        });
                        return $.cookie('mf_testcookie') === 'test_value';
                    }
                }

                function desktopViewClick() {
                    if (!cookiesEnabled()) {
                        mw.notify(mw.msg('mobile-frontend-cookies-required'), {
                            type: 'error'
                        });
                        return false;
                    }
                }
                $('#mw-mf-display-toggle').on('click', desktopViewClick);
            };
        },
        "resources/skins.minerva.scripts/search.js": function(require, module) {
            module.exports = function() {
                var M = mw.mobileFrontend,
                    mobile = M.require('mobile.startup'),
                    SearchOverlay = mobile.search.SearchOverlay,
                    SearchGateway = mobile.search.SearchGateway,
                    overlayManager = require('./overlayManager.js'),
                    searchLogger = mobile.search.MobileWebSearchLogger,
                    $searchInput = $('#searchInput'),
                    placeholder = $searchInput.attr('placeholder'),
                    $searchBar = $('#searchInput, #searchIcon, .skin-minerva-search-trigger'),
                    searchRoute = new RegExp(/\/search/),
                    searchOverlayInstance;
                if (mw.config.get('skin') !== 'minerva') {
                    return;
                }

                function removeSearchOnPageLoad() {
                    if (searchRoute.test(overlayManager.router.getPath())) {
                        history.replaceState('', document.title, window.location.pathname);
                    }
                }

                function getSearchOverlay() {
                    if (!searchOverlayInstance) {
                        searchOverlayInstance = new SearchOverlay({
                            router: overlayManager.router,
                            gatewayClass: SearchGateway,
                            api: new mw.Api(),
                            searchTerm: $searchInput.val(),
                            placeholderMsg: placeholder
                        });
                        searchLogger.register(
                            searchOverlayInstance);
                    }
                    return searchOverlayInstance;
                }
                removeSearchOnPageLoad();
                overlayManager.add(searchRoute, getSearchOverlay);
                $searchBar.prop('readonly', true);
                $searchBar.on('touchstart click', function(ev) {
                    ev.preventDefault();
                    overlayManager.router.navigate('/search');
                });
                $searchBar.on('touchend', function(ev) {
                    ev.preventDefault();
                    getSearchOverlay().showKeyboard();
                });
            };
        },
        "resources/skins.minerva.scripts/references.js": function(require, module) {
            module.exports = function() {
                var M = mw.mobileFrontend,
                    mobile = M.require('mobile.startup'),
                    references = mobile.references,
                    currentPage = mobile.currentPage(),
                    currentPageHTMLParser = mobile.currentPageHTMLParser(),
                    ReferencesHtmlScraperGateway = mobile.ReferencesHtmlScraperGateway,
                    gateway = new ReferencesHtmlScraperGateway(new mw.Api());

                function showReference(ev) {
                    var urlComponents, $dest = $(ev.currentTarget),
                        href = $dest.attr('href');
                    ev.preventDefault();
                    urlComponents = href.split('#');
                    if (urlComponents.length > 1) {
                        href = '#' + urlComponents[1];
                    }
                    references.showReference(href,
                        currentPage, $dest.text(), currentPageHTMLParser, gateway);
                }

                function onClickReference(ev) {
                    showReference(ev);
                }

                function init() {
                    mobile.util.getDocument().on('click', 'sup.reference a', onClickReference);
                }
                init();
            };
        },
        "resources/skins.minerva.scripts/toc.js": function(require, module) {
            (function(M) {
                var mobile = M.require('mobile.startup'),
                    Toggler = mobile.Toggler,
                    TableOfContents = mobile.toc.TableOfContents,
                    eventBus = mobile.eventBusSingleton;

                function init(page, $toc) {
                    var sections = page.getSections(),
                        toc = new TableOfContents({
                            sections: sections
                        });
                    new Toggler({
                        $container: toc.$el,
                        prefix: 'toc-',
                        page: page,
                        isClosed: !0,
                        eventBus: eventBus
                    });
                    if ($toc.length > 0) {
                        $toc.replaceWith(toc.$el);
                    } else {
                        toc.appendTo(page.getLeadSectionElement());
                    }
                }
                module.exports = init;
            }(mw.mobileFrontend));
        }
    }
}, {
    "css": [
        ".last-modified-bar.active{background-color:#00af89;color:#fff}.last-modified-bar.active a{color:#fff}.truncated-text{white-space:nowrap;overflow:hidden;-webkit-text-overflow:ellipsis;text-overflow:ellipsis}@-webkit-keyframes fadeInImage{from{opacity:0}to{opacity:1}}@keyframes fadeInImage{from{opacity:0}to{opacity:1}}img.image-lazy-loaded{-webkit-animation:fadeInImage 0.3s ease-in;-moz-animation:fadeInImage 0.3s ease-in;animation:fadeInImage 0.3s ease-in}      .mw-mf-cleanup{display:block;margin:0;padding:0;font-size:0.8em;color:#72777d}.overlay-issues .cleanup \u003E li{border-bottom:solid 1px #c8ccd1}.overlay-issues .cleanup \u003E li .issue-notice{padding:24px 24px 24px 0}.overlay-issues .cleanup \u003E li .issue-notice .mw-ui-icon{float:left}.overlay-issues .cleanup \u003E li small,.overlay-issues .cleanup \u003E li .hide-when-compact{font-size:0.8em}.overlay-issues .cleanup \u003E li .hide-when-compact{display:block;margin:8px 0}.overlay-issues .issue-details{padding-left:3.5em}.overlay-issues .issue-details \u003E :first-line{line-height:1}.overlay-issues .issue-details small i{color:#72777d}"
    ]
}, {
    "echo-badge-count": "{{PLURAL:$1|$1|100={{formatnum:99}}+}}",
    "minerva-download": "Download PDF",
    "minerva-meta-data-issues-section-header": "Section issues",
    "mobile-frontend-cookies-required": "Cookies are required to switch view modes. Please enable them and try again.",
    "mobile-frontend-editor-redlink-create": "Create page",
    "mobile-frontend-editor-redlink-explain": "This page has not yet been created.",
    "mobile-frontend-editor-redlink-leave": "No, thanks.",
    "mobile-frontend-languages-not-available": "This page is not available in other languages.",
    "mobile-frontend-meta-data-issues": "Page issues",
    "mobile-frontend-meta-data-issues-categories": "About this category",
    "mobile-frontend-meta-data-issues-header": "Page issues",
    "mobile-frontend-meta-data-issues-header-talk": "About this page",
    "mobile-frontend-meta-data-issues-talk": "About this talk page",
    "mobile-frontend-notifications-filter": "Filter",
    "mobile-frontend-redirected-from": "Redirected from \"[[$1]]\"",
    "skin-minerva-issue-learn-more": "Learn more"
}, {
    "badge.mustache": "{{^hasNotifications}}\n\u003Ca href=\"{{url}}\" title=\"{{title}}\"\n   class=\"{{notificationIconClass}} user-button\" id=\"user-notifications\" data-event-name=\"ui.notifications\"\u003E\n    {{title}}\n\u003C/a\u003E\n{{/hasNotifications}}\n{{#hasNotifications}}\n\u003Ca href=\"{{url}}\" title=\"{{title}}\"\n  id=\"user-notifications\"\n  data-event-name=\"ui.notifications\"\n  class=\"notification-count user-button {{#hasUnseenNotifications}}notification-unseen{{/hasUnseenNotifications}}\n    {{#isNotificationCountZero}}zero{{/isNotificationCountZero}}\"\u003E\n\t\u003Cdiv class=\"circle\"\u003E\n\t\t \u003Cspan data-notification-count=\"{{notificationCountRaw}}\"\u003E\n\t\t\t{{notificationCountString}}\n\t\t\u003C/span\u003E\n\t\u003C/div\u003E\n\u003C/a\u003E\n{{/hasNotifications}}\n",
    "IssueNotice.mustache": "\u003Cdiv class=\"issue-notice\" data-severity=\"{{#issue}}{{severity}}{{/issue}}\"\u003E\n\t\u003Cdiv class=\"issue-details\"\u003E{{{text}}}\u003C/div\u003E\n\u003C/div\u003E\n",
    "menu.mustache": "\u003Cdiv class=\"menu view-border-box\"\u003E\n\t{{#groups}}\n\t\u003Cul\u003E\n\t\t{{! \".\" means \"current context\", which allows us to iterate over Plain Old PHP Arrays™. }}\n\t\t{{#.}}\n\t\t\t{{\u003E menuGroup}}\n\t\t{{/.}}\n\t\u003C/ul\u003E\n\t{{/groups}}\n\t\u003Cul class=\"hlist\"\u003E\n\t\t{{#sitelinks}}\n\t\t\t{{\u003E menuGroup}}\n\t\t{{/sitelinks}}\n\t\u003C/ul\u003E\n\u003C/div\u003E\n",
    "menuGroup.mustache": "\u003Cli class=\"{{class}}\"\u003E\n  {{#components}}\n    \u003Ca href=\"{{href}}\" class=\"{{class}}\" data-event-name=\"{{data-event-name}}\"\u003E{{text}}\u003C/a\u003E\n  {{/components}}\n\u003C/li\u003E\n"
});
mw.loader.implement("skins.minerva.toggling@05j1x23", function($, jQuery, require, module) {
    (function(M) {
        var $contentContainer = $('#mw-content-text > .mw-parser-output'),
            mobile = M.require('mobile.startup'),
            currentPage = mobile.currentPage(),
            currentPageHTMLParser = mobile.currentPageHTMLParser(),
            Toggler = mobile.Toggler,
            eventBus = mobile.eventBusSingleton;
        if (!currentPageHTMLParser.getLeadSectionElement()) {
            return;
        }
        if ($contentContainer.length === 0) {
            $contentContainer = $('#mw-content-text');
        }

        function init($container, prefix, page) {
            $container.find('> h1,> h2,> h3,> h4,> h5,> h6').addClass('section-heading').removeAttr('onclick');
            if (window.mfTempOpenSection !== undefined) {
                delete window.mfTempOpenSection;
            }
            new Toggler({
                $container: $container,
                prefix: prefix,
                page: page,
                eventBus: eventBus
            });
        }
        if (!currentPage.inNamespace('special') && mw.config.get('wgAction') === 'view') {
            init($contentContainer, 'content-', currentPage);
        }
    }(mw.mobileFrontend));
});
mw.loader.implement("skins.minerva.watchstar@10u0zq2", function($, jQuery, require, module) {
    (function(M) {
        var mobile = M.require('mobile.startup'),
            currentPage = mobile.currentPage(),
            Watchstar = mobile.Watchstar,
            user = mw.user;

        function init(page) {
            var $container = $('#ca-watch');
            if (!page.inNamespace('special')) {
                new Watchstar({
                    api: new mw.Api(),
                    el: $container,
                    isWatched: page.isWatched(),
                    page: page,
                    funnel: 'page',
                    isAnon: user.isAnon()
                });
            }
        }
        init(currentPage);
    }(mw.mobileFrontend));
});
mw.loader.implement("skins.minerva.options.share.icon@1hk9ko9", null, {
    "css": [".mw-ui-icon-minerva-share:before{background-image:url(/w/load.php?modules=skins.minerva.options.share.icon\u0026image=share\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(\"data:image/svg+xml,%3Csvg width=%2224%22 height=%2224%22 xmlns=%22http://www.w3.org/2000/svg%22%3E%3Cpath fill=%22none%22 d=%22M-1-1h26v26H-1z%22/%3E%3Cg%3E%3Cpath d=%22M12.763 8.75v9h-2v-9H6.264l5.499-7 5.611 7h-4.611zm6.89 5.222h2.222v5.556a2.223 2.223 0 0 1-2.222 2.222H4.097a2.223 2.223 0 0 1-2.222-2.222v-5.556h2v5.778h16v-5.436l-.222-.342z%22 fill=%22%2354595D%22 fill-rule=%22evenodd%22/%3E%3C/g%3E%3C/svg%3E\")}"]
});
mw.loader.implement("user.defaults@02dn5og", function($, jQuery, require, module) {
    mw.user.options.set({
        "monobook-responsive": !0,
        "flaggedrevssimpleui": 1,
        "flaggedrevsstable": 0,
        "flaggedrevseditdiffs": !0,
        "flaggedrevsviewdiffs": !1,
        "globaluserpage": !0,
        "rcenhancedfilters-seen-highlight-button-counter": 0,
        "usebetatoolbar": 1,
        "visualeditor-enable": 1,
        "visualeditor-betatempdisable": 0,
        "visualeditor-newwikitext": 0,
        "visualeditor-visualdiffpage": 0,
        "visualeditor-enable-experimental": 0,
        "visualeditor-enable-language": 0,
        "visualeditor-hidebetawelcome": 0,
        "visualeditor-autodisable": 0,
        "visualeditor-tabs": "remember-last",
        "visualeditor-editor": "wikitext",
        "visualeditor-hidetabdialog": 0,
        "math": "mathml",
        "echo-subscriptions-web-page-review": !0,
        "echo-subscriptions-email-page-review": !1,
        "echo-subscriptions-web-login-fail": !0,
        "echo-subscriptions-email-login-fail": !0,
        "echo-subscriptions-web-login-success": !1,
        "echo-subscriptions-email-login-success": !0,
        "echo-email-frequency": 0,
        "echo-subscriptions-web-edit-thank": !0,
        "echo-subscriptions-email-edit-thank": !1,
        "usecodeeditor": 1,
        "gettingstarted-task-toolbar-show-intro": !0,
        "eventlogging-display-web": 0,
        "uls-preferences": "",
        "compact-language-links": 1,
        "echo-subscriptions-web-cx": !0,
        "cx-enable-entrypoints": !0,
        "cx-entrypoint-fd-status": "notshown",
        "echo-subscriptions-web-oauth-owner": !0,
        "echo-subscriptions-email-oauth-owner": !0,
        "echo-subscriptions-web-oauth-admin": !0,
        "echo-subscriptions-email-oauth-admin": !0,
        "ores-damaging-flag-rc": !1,
        "oresDamagingPref": "soft",
        "rcOresDamagingPref": "soft",
        "oresHighlight": !1,
        "oresRCHideNonDamaging": !1,
        "oresWatchlistHideNonDamaging": !1,
        "ccmeonemails": 0,
        "cols": 80,
        "date": "default",
        "diffonly": 0,
        "disablemail": 0,
        "editfont": "monospace",
        "editondblclick": 0,
        "editsectiononrightclick": 0,
        "email-allow-new-users": 1,
        "enotifminoredits": !1,
        "enotifrevealaddr": 0,
        "enotifusertalkpages": 1,
        "enotifwatchlistpages": 0,
        "extendwatchlist": 0,
        "fancysig": 0,
        "forceeditsummary": 0,
        "gender": "unknown",
        "hideminor": 0,
        "hidepatrolled": 0,
        "hidecategorization": 1,
        "imagesize": 2,
        "minordefault": 0,
        "newpageshidepatrolled": 0,
        "nickname": "",
        "norollbackdiff": 0,
        "numberheadings": 0,
        "previewonfirst": 0,
        "previewontop": 1,
        "rcdays": 7,
        "rcenhancedfilters-disable": 0,
        "rclimit": 50,
        "rows": 25,
        "showhiddencats": !1,
        "shownumberswatching": 1,
        "showrollbackconfirmation": 0,
        "skin": "vector",
        "stubthreshold": 0,
        "thumbsize": 4,
        "underline": 2,
        "uselivepreview": 0,
        "usenewrc": 0,
        "watchcreations": !0,
        "watchdefault": 0,
        "watchdeletion": 0,
        "watchuploads": 1,
        "watchlistdays": 3,
        "watchlisthideanons": 0,
        "watchlisthidebots": 0,
        "watchlisthideliu": 0,
        "watchlisthideminor": 0,
        "watchlisthideown": 0,
        "watchlisthidepatrolled": 0,
        "watchlisthidecategorization": 1,
        "watchlistreloadautomatically": 0,
        "watchlistunwatchlinks": 0,
        "watchmoves": 0,
        "watchrollback": 0,
        "wlenhancedfilters-disable": 0,
        "wllimit": 250,
        "useeditwarning": 1,
        "prefershttps": 1,
        "wlshowwikibase": 0,
        "wikilove-enabled": 1,
        "echo-cross-wiki-notifications": 1,
        "timecorrection": "System|0",
        "language": "en",
        "variant": "en",
        "variant-crh": "crh",
        "variant-gan": "gan",
        "variant-iu": "iu",
        "variant-kk": "kk",
        "variant-ku": "ku",
        "variant-shi": "shi",
        "variant-sr": "sr",
        "variant-tg": "tg",
        "variant-uz": "uz",
        "variant-zh": "zh",
        "searchNs0": !0,
        "gadget-confirmationRollback-mobile": 1,
        "gadget-teahouse": 1,
        "gadget-ReferenceTooltips": 1,
        "gadget-formWizard": 1,
        "gadget-geonotice": 1,
        "gadget-watchlist-notice": 1,
        "gadget-WatchlistBase": 1,
        "gadget-WatchlistGreenIndicators": 1,
        "gadget-WatchlistGreenIndicatorsMono": 1,
        "gadget-SubtleUpdatemarker": 1,
        "gadget-DRN-wizard": 1,
        "gadget-charinsert": 1,
        "gadget-refToolbar": 1,
        "gadget-extra-toolbar-buttons": 1,
        "gadget-switcher": 1,
        "gadget-responsiveContentTimeless": 1,
        "cirrussearch-pref-completion-profile": "fuzzy",
        "multimediaviewer-enable": 1,
        "popups": "0",
        "mf_amc_optin": "0",
        "echo-email-format": "html",
        "echo-subscriptions-email-system": !0,
        "echo-subscriptions-web-system": !0,
        "echo-subscriptions-email-system-noemail": !1,
        "echo-subscriptions-web-system-noemail": !0,
        "echo-subscriptions-email-user-rights": !0,
        "echo-subscriptions-web-user-rights": !0,
        "echo-subscriptions-email-other": !1,
        "echo-subscriptions-web-other": !0,
        "echo-subscriptions-email-edit-user-talk": !1,
        "echo-subscriptions-web-edit-user-talk": !0,
        "echo-subscriptions-email-reverted": !1,
        "echo-subscriptions-web-reverted": !0,
        "echo-subscriptions-email-article-linked": !1,
        "echo-subscriptions-web-article-linked": !1,
        "echo-subscriptions-email-mention": !1,
        "echo-subscriptions-web-mention": !0,
        "echo-subscriptions-email-mention-failure": !1,
        "echo-subscriptions-web-mention-failure": !1,
        "echo-subscriptions-email-mention-success": !1,
        "echo-subscriptions-web-mention-success": !1,
        "echo-subscriptions-email-emailuser": !1,
        "echo-subscriptions-web-emailuser": !0,
        "echo-subscriptions-email-thank-you-edit": !1,
        "echo-subscriptions-web-thank-you-edit": !0,
        "echo-subscriptions-email-wikibase-action": !1,
        "echo-subscriptions-web-wikibase-action": !0,
        "echo-subscriptions-email-cx": !1
    });
});
mw.loader.implement("wikimedia.ui@1hq2mjv", null, {
    "css": [
        ".wikimedia-ui-articleRedirect-base20:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=articleRedirect\u0026format=rasterized\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=articleRedirect\u0026format=original\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt)}.oo-ui-image-invert.oo-ui-icon-articleRedirect,.mw-ui-icon-articleRedirect-invert:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=articleRedirect\u0026variant=invert\u0026format=rasterized\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=articleRedirect\u0026variant=invert\u0026format=original\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt)}.oo-ui-image-progressive.oo-ui-icon-articleRedirect,.mw-ui-icon-articleRedirect-progressive:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=articleRedirect\u0026variant=progressive\u0026format=rasterized\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=articleRedirect\u0026variant=progressive\u0026format=original\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt)}.wikimedia-ui-infoFilled-base20:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=infoFilled\u0026format=rasterized\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=infoFilled\u0026format=original\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt)}.oo-ui-image-invert.oo-ui-icon-infoFilled,.mw-ui-icon-infoFilled-invert:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=infoFilled\u0026variant=invert\u0026format=rasterized\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=infoFilled\u0026variant=invert\u0026format=original\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt)}.oo-ui-image-progressive.oo-ui-icon-infoFilled,.mw-ui-icon-infoFilled-progressive:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=infoFilled\u0026variant=progressive\u0026format=rasterized\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=infoFilled\u0026variant=progressive\u0026format=original\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt)}.wikimedia-ui-upload-base20:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=upload\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=upload\u0026format=original\u0026skin=minerva\u0026version=0pqhkqt)}.oo-ui-image-invert.oo-ui-icon-upload,.mw-ui-icon-upload-invert:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=upload\u0026variant=invert\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=upload\u0026variant=invert\u0026format=original\u0026skin=minerva\u0026version=0pqhkqt)}.oo-ui-image-progressive.oo-ui-icon-upload,.mw-ui-icon-upload-progressive:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=upload\u0026variant=progressive\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=upload\u0026variant=progressive\u0026format=original\u0026skin=minerva\u0026version=0pqhkqt)}.wikimedia-ui-quotes-base20:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=quotes\u0026format=rasterized\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=quotes\u0026format=original\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt)}.oo-ui-image-invert.oo-ui-icon-quotes,.mw-ui-icon-quotes-invert:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=quotes\u0026variant=invert\u0026format=rasterized\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=quotes\u0026variant=invert\u0026format=original\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt)}.oo-ui-image-progressive.oo-ui-icon-quotes,.mw-ui-icon-quotes-progressive:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=quotes\u0026variant=progressive\u0026format=rasterized\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=quotes\u0026variant=progressive\u0026format=original\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt)}.wikimedia-ui-link-base20:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=link\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=link\u0026format=original\u0026skin=minerva\u0026version=0pqhkqt)}.oo-ui-image-invert.oo-ui-icon-link,.mw-ui-icon-link-invert:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=link\u0026variant=invert\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=link\u0026variant=invert\u0026format=original\u0026skin=minerva\u0026version=0pqhkqt)}.oo-ui-image-progressive.oo-ui-icon-link,.mw-ui-icon-link-progressive:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=link\u0026variant=progressive\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=link\u0026variant=progressive\u0026format=original\u0026skin=minerva\u0026version=0pqhkqt)}.wikimedia-ui-listBullet-base20:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=listBullet\u0026format=rasterized\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=listBullet\u0026format=original\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt)}.oo-ui-image-invert.oo-ui-icon-listBullet,.mw-ui-icon-listBullet-invert:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=listBullet\u0026variant=invert\u0026format=rasterized\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=listBullet\u0026variant=invert\u0026format=original\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt)}.oo-ui-image-progressive.oo-ui-icon-listBullet,.mw-ui-icon-listBullet-progressive:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=listBullet\u0026variant=progressive\u0026format=rasterized\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=listBullet\u0026variant=progressive\u0026format=original\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt)}.wikimedia-ui-userAvatar-base20:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=userAvatar\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=userAvatar\u0026format=original\u0026skin=minerva\u0026version=0pqhkqt)}.oo-ui-image-invert.oo-ui-icon-userAvatar,.mw-ui-icon-userAvatar-invert:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=userAvatar\u0026variant=invert\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=userAvatar\u0026variant=invert\u0026format=original\u0026skin=minerva\u0026version=0pqhkqt)}.oo-ui-image-progressive.oo-ui-icon-userAvatar,.mw-ui-icon-userAvatar-progressive:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=userAvatar\u0026variant=progressive\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=userAvatar\u0026variant=progressive\u0026format=original\u0026skin=minerva\u0026version=0pqhkqt)}.wikimedia-ui-userAvatarOutline-base20:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=userAvatarOutline\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=userAvatarOutline\u0026format=original\u0026skin=minerva\u0026version=0pqhkqt)}.oo-ui-image-invert.oo-ui-icon-userAvatarOutline,.mw-ui-icon-userAvatarOutline-invert:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=userAvatarOutline\u0026variant=invert\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=userAvatarOutline\u0026variant=invert\u0026format=original\u0026skin=minerva\u0026version=0pqhkqt)}.oo-ui-image-progressive.oo-ui-icon-userAvatarOutline,.mw-ui-icon-userAvatarOutline-progressive:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=userAvatarOutline\u0026variant=progressive\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=userAvatarOutline\u0026variant=progressive\u0026format=original\u0026skin=minerva\u0026version=0pqhkqt)}.wikimedia-ui-userTalk-base20:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=userTalk\u0026format=rasterized\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=userTalk\u0026format=original\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt)}.oo-ui-image-invert.oo-ui-icon-userTalk,.mw-ui-icon-userTalk-invert:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=userTalk\u0026variant=invert\u0026format=rasterized\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=userTalk\u0026variant=invert\u0026format=original\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt)}.oo-ui-image-progressive.oo-ui-icon-userTalk,.mw-ui-icon-userTalk-progressive:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=userTalk\u0026variant=progressive\u0026format=rasterized\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=userTalk\u0026variant=progressive\u0026format=original\u0026lang=en\u0026skin=minerva\u0026version=0pqhkqt)}.wikimedia-ui-logoWikidata-base20:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=logoWikidata\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=logoWikidata\u0026format=original\u0026skin=minerva\u0026version=0pqhkqt)}.oo-ui-image-invert.oo-ui-icon-logoWikidata,.mw-ui-icon-logoWikidata-invert:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=logoWikidata\u0026variant=invert\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=logoWikidata\u0026variant=invert\u0026format=original\u0026skin=minerva\u0026version=0pqhkqt)}.oo-ui-image-progressive.oo-ui-icon-logoWikidata,.mw-ui-icon-logoWikidata-progressive:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=logoWikidata\u0026variant=progressive\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=logoWikidata\u0026variant=progressive\u0026format=original\u0026skin=minerva\u0026version=0pqhkqt)}.wikimedia-ui-logoWikimedia-base20:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=logoWikimedia\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=logoWikimedia\u0026format=original\u0026skin=minerva\u0026version=0pqhkqt)}.oo-ui-image-invert.oo-ui-icon-logoWikimedia,.mw-ui-icon-logoWikimedia-invert:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=logoWikimedia\u0026variant=invert\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=logoWikimedia\u0026variant=invert\u0026format=original\u0026skin=minerva\u0026version=0pqhkqt)}.oo-ui-image-progressive.oo-ui-icon-logoWikimedia,.mw-ui-icon-logoWikimedia-progressive:before{background-image:url(/w/load.php?modules=wikimedia.ui\u0026image=logoWikimedia\u0026variant=progressive\u0026format=rasterized\u0026skin=minerva\u0026version=0pqhkqt);background-image:linear-gradient(transparent,transparent),url(/w/load.php?modules=wikimedia.ui\u0026image=logoWikimedia\u0026variant=progressive\u0026format=original\u0026skin=minerva\u0026version=0pqhkqt)}"
    ]
});
